---
name: brick-assembly-loop
description: Build, generate, inspect, validate, repair, and iteratively improve LEGO-compatible brick models and the AI-native brick modeling app and harness. Use for work in the lego repo involving BrickDocument, BuildProgram, AssemblyPatch, LDraw, text- or image-to-model generation, scoped copilot edits, canonical renders, visual critique, technique or template learning, recursive engineering passes, or co-evolution with 3d-maker.
---

# Brick Assembly Loop (pointer)

**Read the canonical procedure now:** `C:\Users\38909\Documents\github\lego\docs\skills\brick-assembly-loop.md`. It routes model generation, render inspection, repair, knowledge promotion, app and harness improvement, and `3d-maker` co-evolution. Treat that tracked file and the design documents it names as the source of truth; do not act from an older summary of this skill.

Use `$building-recursive-loop` to add the engineering evidence/finding/fix/prove machinery when it does not exist. Use `$recursive-playtest` to operate it once implemented. Keep LEGO's domain maker loop distinct from the fleet engineering loop.

Non-negotiables even before reading: the canonical part-and-connection graph is authoritative; provider and critic output is untrusted data; hard validators dominate visual scores; inspect canonical pixels and structured state together; every repair creates a child; AI patches never silently mutate the document; stable knowledge and code promotion require protected evidence and human authority.
