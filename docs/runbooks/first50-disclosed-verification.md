# First-50 disclosed verification

## Decision and status

This is a prospective successor protocol for the private first-50 booklet campaign. Its evidence claim is **disclosed verification**. It preserves the PDF, exact first-50 scope, thresholds, validators, and technical acceptance conditions. It does not claim an untouched holdout, an unexposed coordinator, or a blind post-incident evaluation.

The current task contains this exact approval exchange:

> Do you approve continuing with disclosed, independently reviewed verification—keeping the same PDF, first-50 scope, thresholds and validators, but no claim of an untouched holdout?

> Yes

This records conversational authorization, not a signature, authenticated broker event, source-access capability, or inferred timestamp. The user approved the successor evidence claim. Independent review of this concrete protocol remains a prerequisite to resuming source access. At drafting, that review and all successor execution remain pending.

The [frozen search contract](../../output/hard-problem/step44-first50-golden-20260831/search-contract.md), [browser-boundary incident](../../output/step44-workflow-repair-20260905/browser-boundary-incident.md), and [independent incident review](../../output/step44-workflow-repair-20260905/source-isolation-independent-review.md) remain unchanged. The accidental browser run is permanently disqualified. This protocol establishes a separate prospective lineage; it cannot turn that run into an ordinary retry or restore its withheld-source status.

## Preserved identities and commitments

The drafting base is HEAD `a400ec567c5de2b129853eeee9a8e3011e83cf89` in a dirty working tree. HEAD alone does not identify the reviewed implementation. A later run must bind the actual working-tree bytes that its existing verifiers inspect.

| Predecessor record | SHA-256 |
| --- | --- |
| Frozen search contract | `ef8d041f1649cc229d8645d8e4af9723fcd836c3c4b7dba95b9a80fe0e14bd61` |
| Browser-boundary incident | `b0e3c636f8dd9b31bcc25ad24205fce50096f9b58caa6f705e065e327e189b2e` |
| Independent incident review | `e4f05f80cbb3cf75503c56881512d7fd09417830894b076fd31608342d3f7634` |

The fixed input is `recipes/6651557.pdf`: 70,238,655 bytes, SHA-256 `baef0a373164b58d7c982984b52d4e50b10cc59ed28007acb456faa72359bd27`. The exact 211-candidate review batch retains SHA-256 `5b8aab325eca84433ed49922772002f3981cb883bb70889436509c9aa3974894`. These are preserved commitments from the predecessor contract, not claims that this drafting session reopened either payload.

The baseline `page44-real-domain-calibration-20260830-v1` remains `calibration-refused`, with manifest commitment `sha256:9c9306468d5d2f2b5ece9865a9e77f136a096398196750aec0ff2303140e8849`. Failed later attempts and rejected camera branches remain counterevidence under their original identities.

Frozen threshold bodies, validator snapshots, admitted source identities and roles, exact normalized first-50 program, predecessor roster, complete 211-candidate roster, accounting denominators, confirmation rules, and runtime rosters retain their admitted commitments. Source-extraction and measurement semantics remain unchanged. Necessary implementation development, including extraction implementation, may proceed through independent review and an exact source-closure rebind that checks the source-extraction rule and preserves those frozen contracts. Merely blessing changed source-spec bytes with a new digest is forbidden. Adoption of this document automatically changes no source, extraction, threshold, target mask, shared-parent population, candidate membership, tie-break rule, truth snapshot, or runtime pin. Semantic color, HOG, validation observations, and retry order gain no selection authority.

Before a real-source stage, a separate campaign control manifest must identify the existing verifier inputs and frozen commitment bodies by path, byte length, and digest, together with the approved protocol and its independent review. Keep this manifest, the protocol, review records, and inventory outside the closed qualification output tree: its existing verifier rejects extra files. This is an ordinary campaign inventory, not a new capability scheme or a substitute for existing receipts. Required opaque values must still come from their genuine producer and reach their genuine consumer. A mismatch is a named refusal; updating a digest to bless the observed bytes is not a repair.

## Owners and evidence boundaries

The chief of staff coordinates stage admission, assignments, shared contracts, review, and integration. A named worker owns each bounded implementation or execution. An independent read-only reviewer inspects the exact proposed protocol and relevant changes before the coordinator admits their next stage. Reviewers must check that documentation matches implementation and flag stale claims. The coordinator records the actual reviewed revision and file digests; a worker's success summary alone is insufficient.

Successor run evidence belongs under fresh, run-owned directories within ignored `output/` or `var/runs/`. The separate campaign control manifest records the owner, unique identity, permitted inputs, stage, exact invoked entry point, toolchain and code commitments, attempts, terminal outcome, and process cleanup. Keep verifier-owned qualification trees exact and store campaign control records separately. Every writer, including browser helpers and diagnostic output, must be bound to its declared owned directory with exclusive publication. Setting Playwright's `--output` alone does not establish this property. A pre-existing output identity is refused rather than reused.

The disqualified run `output/playwright/first50-resume-full-browser-20260905-b` and its `output/real-panel-scoring` writes are excluded from camera derivation, qualification, selection, validation evidence, and promotion. Their diagnostics must not be imported into successor prompts or decision inputs. The old v2/v3 failure artifacts retain their frozen access exclusion. Shared-root history remains unknown where the incident had no prior byte inventory; a new inventory cannot reconstruct or certify that history.

Record necessary provenance and conclusions in tracked documentation. Keep source payloads, raw images, run output, private session material, and diagnostic logs in their authorized ignored locations. Preserve rejected alternatives, partial publications, incidents, and unresolved evidence; clean only task-owned resources no longer needed.

## Staged execution

### 0. Review the successor before source access

Independent review must accept this exact protocol's scope and evidence claim, confirm that it preserves the old incident and technical gates, and identify any remaining conflicts. The coordinator resolves material findings and records the accepted protocol digest before authorizing source resumption. Drafting, reviewing, or accepting this document does not itself qualify a camera or issue an opaque capability.

Source-free preparation can continue within the existing mandate. It uses only explicitly selected source-free checks. Ordinary browser selection and the complete browser gate remain unavailable at this stage because the default suite includes an ungated all-panel reader. The shipped source-closure and source-lock instruments are the first probes; any new probe must state which existing harness answers the question or why none does.

### 1. Prove readiness without protected measurements

Verify the exact admitted runtime and source closure through their existing source-free checks. Prove source isolation at the actual Node, browser, child-process, Vite `/@fs/`, ancestor-recipe-discovery, shared-output, and diagnostic-release boundaries before relying on it. An opt-in flag, fresh browser context, worktree, or unique Playwright output argument alone is not proof of denial. Use source-free synthetic controls for refusal paths; their success proves only those controls.

The review must establish that the selected entry point releases only the source material permitted by its next stage and that all output writers honor the owned root. Do not run a source-bearing command to discover whether it was safe. Availability checks, control fixtures, structural tree checks, and synthetic browser demonstrations cannot substitute for real qualification, runtime integrity, or an opaque-capability replay.

The existing runtime, camera, and authority refusals remain real work items. The [resume devlog](../devlog/detailed/2026-09-05-first50-calibration-resume.md) records incomplete exact Poppler recovery, installed-runtime roster refusal, unavailable independent signed camera-axis evidence, and the absent genuine Step-44 selection needed by canonical prefix compilation. Its ideal camera derivation assumes genuine signed correspondence; the retained pixel experiment recovered all four top/base pairs in only one of twelve positive cases. Those results do not supply the missing real-image camera branch. These are recorded predecessor observations and must be checked at the relevant source-safe boundary before they are reported as current measurements. Runtime restoration must recover the exact frozen bytes through its reviewed workflow. No runtime repinning, forged token, reconstructed capability object, or invented success is authorized by this protocol.

### 2. Qualify Steps 41 and 42 under the frozen rules

After stages 0 and 1, use the existing real-domain calibration entry point named by the frozen search contract, with a fresh owned output identity and its established source-access mechanics. Its qualification phase may expose only the inputs already admitted for Steps 41 and 42. Step 43 validation pixels, printed Step 44, physical page 45, prohibited failure artifacts, and candidate results remain withheld from that phase.

Recover one stable source-to-world orientation law. A branch chosen separately for each panel because it wins is disqualified. Keep the entire shared predecessor in each parent-only mask and exclude only highlighted additions. Retain exact source and candidate images, camera, face, handedness, turn, mask bytes, scores, margins, case populations, source-lock results, lifecycle records, and cleanup evidence. Inspect each relevant native-resolution image independently; a contact sheet or a passing metric cannot approve it.

Both Steps 41 and 42 must qualify under every unchanged threshold and confirmation rule. Before any Step 43 validation-source read, freeze the selected candidate and its complete code, source-extraction, camera/orientation law, masks, policy, program, roster, and runtime commitments through the existing qualification machinery. The frozen record must be sufficient for the existing verifier to reproduce the decision. No validation observation may participate in this freeze.

### 3. Validate Step 43 once against the frozen candidate

Only successful sealed Steps 41/42 qualification and the genuine existing source authority may admit the one-shot Step 43 validation stage. Use the frozen candidate unchanged. The current Step 43 session and crop guards consume in-memory `WeakSet` entries before the protected import and crop read. Those process-local guards do not provide durable cross-process opportunity accounting. The later-source durable ledger applies downstream of qualification and is not a pre-Step-43 ledger.

The operator must predeclare one successor validation attempt in the separate campaign control manifest before launching the run that may perform it. Record its unique attempt identity, intended entry point and stage, protocol and review, and admitted input commitments; bind the candidate freeze from stage 2 before validation-source access. Preserve the existing local guards, lifecycle, failure, and cleanup rules. The campaign record supplies prospective operator no-retry accounting, not a new durable capability, cryptographic anti-rollback guarantee, or proof that an unrecorded read could not occur. A fresh directory or process does not replenish the campaign's attempt. An uncertain crash or outcome cannot justify an automatic retry; retain the evidence and return the unresolved opportunity to the coordinator.

Validation may accept or refuse the frozen candidate. It may not tune a threshold, camera, orientation law, crop, mask, source extraction, roster, program, or implementation and then validate the edited candidate again. A failed or ambiguous result remains a closed refusal with its exact evidence. Any proposed successor after validation requires a separately documented evaluation decision and independent review; automatic repair does not authorize repeated validation.

The legacy machine outcome `qualified-and-validated` retains all of its technical predicates. If produced, describe it as **disclosed verification under this successor protocol**. A legacy field, filename, function, or test containing `blind` or `holdout` is a compatibility name and does not establish blindness or restore untouched-source status. Do not relabel historical bytes to make their claim match the successor.

### 4. Run the genuine Step 43-to-44 consumer and all 211 candidates

After stage 3 passes, run the genuine source-locked, camera-only Step 43-to-44 consumer on fresh owned output. Preserve its exact source boundaries, checks, and opaque capability flow. A structurally valid evidence tree, serialized proof, digest, or test-produced object does not establish that this consumer ran with a genuine capability.

The complete 211-candidate production path must then finalize and promote through its existing five-root binding. Retain every required root and all candidate members, including rejected alternatives. No manual candidate pick, favorable subset, source leak, omitted confirmation, or synthetic positive can replace this path. Its result is disclosed verification even when the implementation retains a legacy `blind` name. This protocol changes neither production/test namespaces nor the actor permitted to issue or consume authority.

### 5. Verify the real first-50 UI and the full repository gate

After the preceding source stages admit the required material, drive the actual app controls and replay exactly printed Steps 1 through 50. Never assemble, accept, report as completed, or replay Step 51. The existing bounded later-panel witness rule remains available only within its admitted passive-source scope: later panels cannot supply their action, pieces, placement, report, or document mutation. This grants no broader suffix access.

For each claimed step, retain its source, identity, placement, replay, structural result, and native visual evidence. Inspect canonical isometric and orthographic views, useful orbit angles and zoom levels, and the surrounding UI. Read the structured app and model snapshots beside the validator output and compare them with the actual pixels. Bind each visual verdict to the digest of the inspected bytes; regeneration requires a new review. Hidden claims remain `not-observable`.

Verify a nonzero pixel delta from Step 43 to Step 44, exactly three relational additions at Step 45, and the exact detached nine-part child at Step 50 with only the intended disconnected-assembly finding. Hard structural validity, legal connection and collision behavior, proper physical frame, part accounting, scope, and deterministic replay remain independent acceptance conditions. A high panel score cannot excuse a hard failure or establish a general physical-stability claim.

Run the smallest relevant checks during repair and the complete browser and `npm run verify` gates only when the source-access stage permits their actual reads. Inspect their test selection and output writers first. Gate ordering changes no assertion or completion requirement. A passing subset, interrupted suite, unavailable environment, or skipped required stage is incomplete evidence.

Obtain independent read-only review of the final integrated changes and affected native visual evidence. Resolve material findings and rerun affected checks within the established repair budget. Complete staged secret, size, artifact, and diff checks, canonical documentation and devlog updates, and any local commits already authorized by the campaign only after the required gates pass. Never push without explicit approval naming the exact remote.

## Stopping and reporting

Preserve every frozen disqualifier except the successor's explicitly approved change in the prospective evidence claim and lineage. This document does not edit or waive the old contract. A conflict involving protected input, extraction rules, truth, thresholds, validators, runtime pins, acceptance capability, source access, or further evaluation attempts must return to the coordinator before dependent work continues. Additional consequential policy changes require the appropriate user decision.

After two failures for the same reason, reassess the approach. Keep automatic repair within the existing five-substantive-attempt limit unless a different budget is explicitly authorized. Source-free repairs may continue while a later stage is blocked, but they do not advance that stage's result.

Every handoff reports the exact revision and working-tree commitments, protocol and review digests, current admitted stage, source accesses actually performed, candidate and run identities, measured prefix and piece counts, closed outcomes or named refusals, checks run and their bounds, material review findings, and task-owned process cleanup. Separate documented, implemented, verified, reviewed, and blocked work. Completion requires every preserved golden acceptance condition; no partial result is called golden, blind, or an untouched-holdout result.
