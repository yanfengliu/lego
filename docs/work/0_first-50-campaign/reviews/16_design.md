# Review 16: design

## Target

This round reviews camera contract audit 01 at `output/first50-fresh-search-20260908/camera-contract-audit-01/audit.md`, 19,685 bytes, SHA-256 `357e847ac22cb681995258b315372dcb612001595c68140cb8200cb71195facb`, retained byte-for-byte at [snapshot 16](../snapshots/16_camera-contract-audit.md). Its plain-text inventory is `output/first50-fresh-search-20260908/camera-contract-audit-01/audited-closure.json`, 8,011 bytes, SHA-256 `30cba822264c137164a34bdf8523b034d2d879d3482737d390beb942d1d96994`. All 22 listed source files, totaling 294,475 bytes, matched that inventory during this review.

The declared base is `6300c6b817722d2652924ba9df7e06264c135e31` plus its preserved dirty tree. This reviewer did not run Git or independently establish the author's untracked-file observation. The review binds the actual document and inspected source bytes rather than treating HEAD as their identity. It reviews the audit's requirements interpretation and proposed research correction, not executable changes, a camera result, historical admission, or source-stage readiness.

## Reviewers and coverage

Reviewer: `/root/camera_contract_independent`, independent of audit author `/root/camera_contract_audit`. Before reading audit 01, the reviewer read the permitted frozen search contract, successor runbook and necessary current source interfaces, and sent initial findings to the root. The later review whole-read the exact audit and inventory, rechecked every inventory entry's length and digest, and inspected the additional cited producer, registration, confirmation and session passages needed to test its claims.

The independent pre-audit raw-source set included the camera files with suffixes `real-domain-source-spec.ts`, `real-domain-source-anchor.ts`, `real-domain-source-axis.ts`, `real-domain-preunlock-receipt.ts`, `preregistered-law.ts`, `real-domain-capture.ts`, `registration-types.ts`, `search-decisions.ts`, `real-domain-calibration-contract.ts`, `real-domain-source.ts` and `real-domain-preregistration.ts` under `apps/web/e2e/real-build-prefix50-subbuild-return-review-camera-`, plus `packages/rendering/src/camera-fit-lattice.ts` and `packages/rendering/src/camera-fit.ts`. Their inspected identities match the audit inventory. Applicable repository instructions, local rules and lessons were also read; relevant specification text was consulted as context rather than historical result authority.

Three additional plain-source files outside the author's inventory informed the independent interpretation. These are source-file byte hashes, not evaluated exported-object commitments.

| Path | Bytes | SHA-256 |
|---|---:|---|
| `apps/web/src/assembly/panel-face.ts` | 8,579 | `7d0b50d992c1b3c18864310cba91a20f395135b2e9111abf14f95c4d18000fcd` |
| `apps/web/e2e/real-build-prefix50-subbuild-return-review-camera-search-types.ts` | 11,732 | `5a193862549a56b3693007595d3313a0cc72ab4473d8f320e6c29d9d6c573f8a` |
| `apps/web/e2e/real-build-prefix50-subbuild-return-review-camera-calibration.ts` | 9,044 | `323acae85c66fe727e6afcc56c8e63c7fc0e03672d3862a0b98ee10bfb679c0d` |

No protected PDF, PNG, raster, historical route result, truth artifact or native-runtime payload was inspected. No project import, test, browser, model, network, renderer, solver or native program ran. The plain-text reads and metadata checks do not establish runtime behavior, actual pixel evidence, a transitive runtime closure, genuine capability execution or historical frozen admission. Only this report and its exact target snapshot are authorized writes.

## Reports

### Independent requirements and proposed-correction assessment

#### The proposed correction is supported within the inspected scope

Audit 01 lines 5 and 58-60 correctly separate the campaign's required source-derived signed full-rank orientation from a route's stronger promise to exclude every alternative over a continuous global camera domain. The frozen contract requires one stable source-to-world orientation law, exact finite comparisons, unchanged qualification conditions and genuine consumers. It does not state that every possible continuous camera must be excluded. The inspected numeric interfaces accept point parameters and a discrete branch; they carry no global continuous-camera exclusion certificate.

The permissible correction is consequently narrow: a missing universal exclusion proof is not itself a campaign failure on these sources alone. A route that promises a complete enclosure or no-false-pruning result still owes that result under its own reviewed contract. Failing such a route does not prove every compliant source-camera method impossible. This audit supplies no alternate producer, implementation assignment or right to weaken a route after seeing its output.

The full-rank requirement remains substantive. Four collinear top cores and an identity order do not independently resolve the transverse signed axis. A plausible seed, favorable silhouette or apparently full-rank number cannot dispose of a surviving source ambiguity. The audit retains these falsifiers and does not turn its narrower research question into authority to bypass the current refusal.

#### The consumer map matches the inspected code

The existing camera family has azimuth, elevation, scale, image center and discrete `upSign`; it has no free continuous roll. The extra independent read of [panel-face.ts](../../../../apps/web/src/assembly/panel-face.ts:44) confirms face compensation before the hand transform. The quarter-turn is applied before reflection, and `viewForPanelFace` supplies the discrete up sign. The 16-branch law therefore cannot be reduced to a convenient set of apparent two-dimensional mirror images without preserving physical face and polarity.

Audit lines 25-35 accurately distinguish the common orientation from per-panel registration. A shared anchor supplies both calibration cases, while failed per-panel fits remain counterevidence. The capture path derives a frame from the exact predecessor and re-seeds each branch's scale from the complete target-mask and projected predecessor bounds. Registration changes scale and image center only. Identical full camera parameters across panels, a source-only global translation and a freely fitted roll are not requirements of this consumer.

The actual search keeps all 16 ordered branches. All eight expected-face branches need comparable registration: complete coarse search, coarse and exact tie classification, local final-cell containment, and a settled published non-seed pass. The final registration cells remain 0.1 pixel and 0.0005 relative scale. Geometry chooses by eligible silhouette IoU, with the strongest other expected-face branch as its runner-up. Semantic F1 may refuse that decision; it cannot select a different branch or break a tie. HOG remains diagnostic. Seed, confirmation and possible rebase captures are real retained measurements in the described path, not a license to treat a predicted warp as a captured result.

The additional [search threshold definition](../../../../apps/web/e2e/real-build-prefix50-subbuild-return-review-camera-search-types.ts:15) confirms IoU at least 0.9, branch margin at least 0.015, prediction/actual drift at most 0.02 and at most three alignment passes. The independent [calibration source read](../../../../apps/web/e2e/real-build-prefix50-subbuild-return-review-camera-calibration.ts:191) confirms that both semantic thresholds derive from fixed synthetic controls whose expected commitment is pinned. This review did not execute that derivation or substitute guessed decimal values. The audit correctly preserves both thresholds rather than giving them new selection authority.

#### Current extraction obligations remain intact

Audit lines 15-21 and 39 accurately report the current unavailable producer. The source-spec keeps signed +Y, the full-rank seed and expected branch null. The source-axis guard always throws. The availability-only preunlock helper does not mint the missing evidence, and the registered anchor wrapper rejects its current refused status and null commitments. A public object shape or new digest does not change that chain.

The four native top/base correspondences and mirror/erasure controls are specific current registered method obligations. Their mathematical necessity for every imaginable alternative is unproved, but this audit does not waive them. Any replacement must preserve the admitted source-extraction and measurement semantics through independent review and an exact source-closure rebind; a conflict returns to the coordinator under the [successor protocol](../../../runbooks/first50-disclosed-verification.md:31). The word signed in the inspected source-axis interfaces refers to geometric polarity and does not satisfy any separate authentication or capability requirement.

Audit line 43 handles the fixed preregistration branch correctly. Its `authority: none`, future-qualification purpose and `selectionAuthority: false` prevent treating the declared `face:studs-up/hand:as-fitted/turn:0` as the missing independent source observation. The difference from the currently null independent branch is an explicit assumption to reconcile against genuine future evidence, not a demonstrated current bypass or proof that the branch must change. The path presently refuses.

#### Historical admission and final acceptance are not inferred

The frozen search-contract file matches its preserved runbook pin. That does not establish historical equivalence of today's source-spec or imported commitment bodies. The file-byte digest, full exported object commitment and bounded exported object commitment are distinct identities. No admitted historical closure was supplied for comparison, and no module was evaluated. Audit lines 64-68 state that limit explicitly. Its current inventory is useful evidence of what was read, not a historical admission or executable closure. The author's reported Git status is not needed for the conclusion and was not repeated by this reviewer.

The audit preserves genuine observations for both 41/42, expected-branch equality, source lattice qualification, all metric gates, shared source/session/predecessor bindings, candidate freeze and one unchanged Step-43 validation. The later 43-to-44 consumer and all 211 production candidates/five roots remain separate. Actual first-50 UI replay, native visual inspection, structural validity, exact step-specific acceptance, full repository gates and final independent review remain required under the runbook. A source method, global certificate or synthetic result alone cannot complete the camera subgoal or campaign.

No material inconsistency was found between the frozen audit's proposed correction and these independently inspected contracts. Its falsifiers cover the main false-success routes: unsupported polarity, unbound correspondences, per-panel selection, missing branches or confirmation, altered thresholds or masks, public-object bypass and unsupported impossibility claims.

## Findings and disposition

No new material finding was identified. F19 and F20 remain unused.

The root integration owner accepted this bounded review verdict: audit 01 is accurate within its 22-source plain-text scope, and the proposed distinction preserves current correspondence/control, extraction, signed-evidence, genuine-path and downstream obligations. The chief retains adoption of the research correction and all implementation and source-stage admission. This report records the received root disposition only; it does not infer a chief decision or permission to change registered source-spec bytes.

## Verification

Whole-read the exact audit and its inventory. Rehashed all 22 listed files and independently totaled 294,475 bytes with zero length or digest mismatches. Checked the necessary cited source passages against the prior independent readings, including branch transformations, current unconditional refusal, numeric parameter and registration semantics, confirmation, selection/corroboration roles, session bindings and fixed threshold derivation. The three additional source files also retained their initially inspected identities.

Documentation checks cover exact target/snapshot byte equality and SHA-256, readback of the report, required review headings, existing local link targets, balanced Markdown fences and trailing whitespace. These are documentation and metadata checks only. No runtime, camera, visual, opaque-capability or historical-admission verification was performed. No task-owned persistent process was created.

## Round outcome

The reviewer accepts audit 01's bounded requirements interpretation and narrow proposed correction. The root accepts that review verdict; chief adoption and subsequent implementation/source admission remain separate. Universal continuous-camera exclusion is a route-specific guarantee, while the source-bound signed full-rank estimate, current four-correspondence/control requirements, unchanged qualification consumer and all downstream gates remain necessary. No arbitrary seed, method waiver, source-spec edit or source access is authorized. This verdict binds audit `357e847ac22cb681995258b315372dcb612001595c68140cb8200cb71195facb` and inventory `30cba822264c137164a34bdf8523b034d2d879d3482737d390beb942d1d96994`; it does not transfer to revised bytes.
