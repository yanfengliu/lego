# Review 8: design

## Target

Repository: `%USERPROFILE%/Documents/github/lego`. Independently observed HEAD `6300c6b817722d2652924ba9df7e06264c135e31` with the preserved dirty tree. This review covers the prospective runtime-location stage amendment, retained byte-for-byte as [snapshot 8](../snapshots/8_runtime-location-stage-addendum.md): 9,388 bytes, SHA-256 `5926a6fe71ce026ed12904914a1270997682885a9debd86c192a14c93560cf2e`. Its original [identity inventory](../../../../output/first50-fresh-search-20260908/runtime-location-stage-addendum/identities.json) is 3,948 bytes at SHA-256 `b3bf5cafb9b196f786f224ae5f82399009f0fe01a3f97082a27c3e94c8981a7d`.

The source review is bound to sixteen current untracked source inputs in the retained [source patch](../../../../output/first50-fresh-search-20260908/runtime-location-stage-review-8/reviewed-source.patch), 209,396 bytes at SHA-256 `ebe0f918c9996f7c910ee7ca5af1f4c9ace689227eeb65889e25ec530a227431`. The patch contains exact new-file additions relative to the observed base, including original line endings. The [review inventory](../../../../output/first50-fresh-search-20260908/runtime-location-stage-review-8/identities.json), 4,066 bytes at SHA-256 `ff71df6c62740c40b5767a797edc1340d39d20e1562b573f56085c462a2c522f`, records every source path, length and digest. Keep these ignored inputs while this uncommitted code is needed to recover the review target.

The governing [successor runbook](../../../runbooks/first50-disclosed-verification.md) is 17,827 bytes at SHA-256 `e990c7498aaf04d4701275cd55597c5255fa8eb1c4ff10f578c0b2ae9c3cfa59`. The [frozen search contract](../../../../output/hard-problem/step44-first50-golden-20260831/search-contract.md) is 3,984 bytes at SHA-256 `ef8d041f1649cc229d8645d8e4af9723fcd836c3c4b7dba95b9a80fe0e14bd61`.

## Reviewers and coverage

Reviewer: `/root/runtime_stage_review`, an independent read-only source reviewer. Read the raw runbook and search contract before the proposal, then followed source-lock projection, source closure, live calibration, persisted calibration reproduction, the Step 43 consume and crop guards, and the committed-replay wrapper. Read applicable repository rules, lessons and relevant canonical domain/evidence contracts. A quick memory lookup supplied navigation and the prior nonadmission reminder; current documents and source supplied this verdict.

After the raw analysis, checked attempt03 and review 5 solely to establish their actual disposition. Attempt03 remains 32,677 bytes at SHA-256 `3f405a63fd919060ce043e9a15ebcb6bbaf8c770eda1dbe6840b5ea2ec592cb8`; [review 5](5_design.md) remains 10,856 bytes at SHA-256 `8b422688a3db595c8c0fc1c817a0126f895f27c83798c70c4ed2fd85f822b42f`. Review 5 accepted an explicit route exclusion and expressly did not derive universal pre-read rejection as a new user requirement. This round preserves that history and assesses a prospective amendment.

No production import, test, runtime payload read, PDF/image read, native invocation, browser, server, installation, production edit, Git mutation or process with a persistent lifetime occurred. This is static review, not proof that source isolation or an executable guard works. Runtime restoration, native cleanup, actual qualification, candidate freeze and one-shot execution remain outside this review.

## Reports

### /root/runtime_stage_review

#### The narrower stage principle is supported

The runbook requires reviewed readiness and entry-point admission before real-source work at lines 49–63. Within Stage 2 it admits Steps 41/42, then requires the complete candidate freeze before any Step 43 validation-source read at line 67. Lines 71–75 preserve genuine qualification, authority and prospective no-retry accounting. It does not independently require an old-runtime metadata discriminator before every otherwise admitted Stage 2 PDF identity check or exact calibration crop. A new location would still need the separate explicit exception, exact runtime verification and reviewed source-closure binding required by lines 31–33 and 55–59.

The inspected direct replay matches the proposal's conditional lineage argument. `real-build-prefix50-step44-camera-only-source-lock.ts:122–156` constructs public evidence from one frozen PDF row and retains full current lock identity separately. `real-build-prefix50-subbuild-return-review-camera-real-domain-persisted-qualification.ts:394–424` reproduces source geometry and both calibration cases before reproducing the session. `real-build-prefix50-subbuild-return-review-camera-real-domain-calibration-raster.ts:53–75` checks the whole PDF identity and renders only the sealed Steps 41/42 crops. The raster commitment at `real-build-prefix50-subbuild-return-review-camera-real-domain-calibration-raster-commitment.ts:69–79` includes the toolchain commitment. The persisted case comparison at `real-build-prefix50-subbuild-return-review-camera-real-domain-persisted-case.ts:249–250` therefore rejects an unchanged old-runtime case against a changed current raster lineage before the direct replay reaches its Step 43 consume at line 426. This inference assumes the proposed location change and all earlier admission requirements were actually approved and satisfied; none were executed here.

The proposal correctly identifies a separate incomplete join: current full-lock coverage does not establish equality to the prior full candidate/code freeze. It also correctly notes that the ordered case-proof comparison currently occurs after consume at persisted-qualification lines 441–446. Its proposed full-closure and calibration-proof joins are reasonable obligations, not claims of implemented authority. A campaign inventory alone cannot supply the missing genuine producer/consumer binding.

#### F8: the proposed consume boundary misses an earlier Step 43 payload read

The proposal's lines 11 and 25–29 place the decisive joins and synthetic sentinel before `consumeRealBuildPrefix50Step44CalibrationSessionForHeldOut`. That is not the earliest protected read in the normal committed-replay caller. `real-build-prefix50-step44-calibration-persisted-binding.ts:30–39` enters `withStableRealBuildPrefix50Step44RealDomainGateOutputTree` with `persistedCaseCount: 3` before invoking the offline verifier. `real-build-prefix50-subbuild-return-review-camera-real-domain-gate-tree.ts:159` calls `gateTreeIdentity` before the verification callback. Its case walk at lines 90–110 hashes every file in all three case directories, and `digest` at lines 32–37 reads those file bytes. The persisted case contains `source-crop.png`, `parent-target-mask.png` and the other source/render images, as shown by `real-build-prefix50-subbuild-return-review-camera-real-domain-evidence.ts:26–28` and `real-build-prefix50-subbuild-return-review-camera-real-domain-persisted-case.ts:274–294`.

Consequently, the existing wrapper can read `step-43/source-crop.png` before either current calibration proof is reproduced, before the complete-freeze comparison proposed in obligation 1, and before a sentinel installed only at the consume/import seam. A later refusal cannot undo that read. This finding concerns an excluded Step 43 image payload, not the explicitly admitted whole-PDF identity check, and does not require universal metadata rejection before otherwise admitted calibration work. It is established by source order only; no protected tree was opened to reproduce it.

The proposal already withholds invocation, so this finding does not identify a performed breach or implied runtime approval. It does make the listed admission obligations incomplete. The smallest design correction is to retain the Stage 2 principle, explicitly keep this three-case wrapper and its callers unadmitted during qualification, and require the complete qualification/freeze/attempt checks before the earliest Step 43 payload read anywhere in the chosen entry point. Name the tree-stabilization read in the obligation and in the synthetic read tripwires. A concrete implementation must preserve exact publication/tree closure and stability checks at their admissible stage; bypassing the wrapper or dropping its Step 43 files is not an acceptable shortcut. Review the actual selected caller path and show source-free negative controls refuse before both the tree-image read and the consume/import/crop seams. A valid synthetic chain must also show how the later full-tree check remains intact.

This adds no new threshold, validator, runtime roster or validation opportunity. It closes the same withheld-source boundary the proposal expressly preserves at line 9. If a later route runs only after a valid Stage 3 admission has already made those exact Step 43 artifacts available, record that stage and opportunity provenance explicitly; do not classify the whole three-case replay as a Stage 2-only operation.

#### Runtime repinning remains a separate decision

The proposal repeatedly marks the location exception unapproved and limits any future exception to the literal root, derived `FONTCONFIG_PATH` and necessary independently reviewed dependent commitments. It preserves all 178 ordered runtime rows, the PDF, thresholds, validators, first-50 scope, 211 candidates and one-shot limit. No hidden runtime approval was found in the text. The inherited `ad3675e...` and `f36d5a...` toolchain identities are design metadata here; this review neither rehashed their payloads nor certified an installed runtime.

## Findings and disposition

| ID | Finding | Disposition and reason | Repair or follow-up |
|---|---|---|---|
| F8 | [Committed replay reads the Step 43 tree before the proposed consume boundary](#f8-the-proposed-consume-boundary-misses-an-earlier-step-43-payload-read) | Unresolved; reviewer recommends a narrow design correction. Coordinator disposition is pending and is not supplied by this authored report. | Preserve the stage principle, name and exclude the premature three-case wrapper path, bind all prerequisites before its earliest protected read, retain full closure checks, and review source-free controls at both tree-read and consume seams. |

## Verification

Verified the assigned proposal and inventory hashes and all nine file identities listed by that inventory. The earlier audit assertions record was hashed only; its findings were not imported as authority. Verified the snapshot is byte-identical to the proposal. Captured exact reviewed source additions and verified their line reconstruction preserves every source byte; no patch was applied. The sixteen source identities and the governing/proposal/historical document identities were rechecked after drafting. Checked the two authored documents for required review structure, local link targets, balanced Markdown fences and trailing whitespace. No executable source-closure, native, unit, browser or full repository gate was run, and no source-stage result is inferred from these document checks.

The source patch and metadata remain ignored, task-owned review evidence for the unresolved finding. The only permanent files created by this reviewer are this report and snapshot 8. Production source, proposal, previous designs/reviews and unrelated work remain unchanged. No task-owned browser/GUI/server process was launched, so there is no such process to retain or terminate.

## Round outcome

The proposed distinction between admitted calibration reads and withheld Step 43 access is supported by the raw requirements. Revise the concrete stage-boundary obligations for F8 before accepting the amendment as an admission design. This is a bounded correction, not rejection of the location strategy or a demand for universal pre-calibration metadata rejection. The full-closure join, proof joins and executable controls also remain unimplemented. Runtime relocation is still unapproved, all affected replay entry points remain unadmitted, and this source-only review establishes no qualification, validation opportunity, source readiness or campaign completion.
