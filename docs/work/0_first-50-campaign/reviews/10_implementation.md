# Review 10: implementation

## Target

Independent review of the frozen RGBA extractor's first transfer measurement over all sixteen predeclared retained catalog captures. Repository: `%USERPROFILE%/Documents/github/lego`. Assignment base: `6300c6b817722d2652924ba9df7e06264c135e31`, supplied by the persistent implementer. This reviewer performed no Git operation and does not independently attest HEAD or the broad dirty tree. The target is a measurement wrapper and existing captures, not a new extractor or production implementation.

The exact pre-execution proposal is preserved as [snapshot 10](../snapshots/10_catalog-transfer-preparation.md), 6,625 bytes, SHA-256 `7ee62e33e874376882c40e643fd34874b4c6d2d8a61860055a81c084e7b70872`. Its statements that execution and image admission are pending describe preparation time. The subsequent admitted execution and image review are recorded here without rewriting that snapshot.

Complete source and metadata remain in [the ignored transfer root](../../../../output/first50-fresh-search-20260908/rgba-catalog-transfer-01/). Its `pre-execution-identities.json` is SHA-256 `d81b6ddfac733e39746b23d680ed409fe9655e4a0e2b856fc0fb314ff7f261cb`. All eleven listed source/metadata identities matched before this review's payload access and again after its sixteen image views. The five `frozen/` files are byte-identical to the final candidate reviewed in [review 7](7_implementation.md).

| Executable source | SHA-256 |
|---|---|
| `transfer.mjs` | `66b6251a7dba73d8e2d61a014236fcc4b7cf5f771b36c0c0ab95d63551a48c92` |
| `observations.mjs` | `8aa16e25f595fefd35b3e5921ac93127c611f4f56c4f78eb51988fb123287abb` |
| `png-codec.ts` | `21fb281b7de222c9452a46c0731ef93400ba9ad68e0e24c094aa940345b3fa5c` |
| `frozen/extractor.mjs` | `5a5aa67ac248969c64b808237deda4e6a5a256a3dee401d805add737d42da584` |
| `frozen/frame.mjs` | `e689c9f8c6f8ba7aac23f8a78e40a06b1093c414208fed32dbf45a2d7beabdf5` |
| `frozen/grammar.mjs` | `753b86d6971a3416ea81fa00d95257ef2333c956bb06d222e1a39c7c0e292a7e` |
| `frozen/intervals.mjs` | `703988cdebdb4ad22810ba144c00177b5b9215e0f9dfed8e66acd88cd8080bc2` |
| `frozen/pixels.mjs` | `75a62c5df579858aa316f0a4325aeb3eab76c5b86c6a5875569567d786f4c46a` |

The exact sixteen-case input plan is `inputs.json`, SHA-256 `7276d10ddfce2874eb7cd87c9543954569c396398898bae67490bbcfafa86f6b`; it binds ordered names, unique case IDs, file sizes, encoded PNG hashes and decoded RGBA hashes. The separate `held-labels.json` is SHA-256 `baf6dfc87b8f6c01ead9eb8b7ae0ca22e204695c275623ab005eefe7acc4b31d`. The metadata inventory read for provenance is [inventory.md](../../../../output/first50-fresh-search-20260908/catalog-capture-inventory-01/inventory.md), SHA-256 `7f9cacafcb005ed62c9e5a24b9a76f75a0d2b0e3df3ea583d4e1fc3a71630542`. Its account of the earlier capture process is attributed provenance; this review does not reclose that historical renderer or rerun its capture.

## Reviewers and coverage

Reviewer: `rgba_independent_review`, 2026-09-08. The first pass was limited to the proposed wrapper, codec, descriptive observer, exact frozen extractor identities, input/label metadata and inventory. After explicit payload admission by the persistent implementer, the reviewer inspected the actual sixteen case records and two summaries, viewed every exact original PNG individually at 720 by 470 resolution, and checked every PNG's digest before and after those views.

The persistent implementer task `01a073ae` / `root` performed the one admitted run. This reviewer executed no wrapper, decoder probe, test, renderer or browser, made no source edit, and read no old camera/semantic truth report, protected PDF, booklet panel or runtime payload. Only this report and the byte-exact proposal snapshot were authored. No task-owned browser, GUI, server or native process was launched. The independent views described below were image-file inspection, separate from the implementer's execution and views.

## Reports

### rgba_independent_review

The measurement is valid for its declared complete roster, and the transfer result is negative: **zero of twelve recorded positives yielded an observed-and-unique correspondence**. All four original negatives also refused. The command's exit 0 reports that all sixteen exact files decoded and were measured; it does not report estimator success. No material wrapper or scoring defect was found within this pinned scope. No new finding ID is assigned merely because a correctly conducted experiment returns a negative result.

#### Exact inputs, execution closure and held labels

The executable import closure is precisely the eight files above, with `node:fs`, `node:url`, `node:path`, `node:crypto`, `node:perf_hooks`, `node:buffer` and `node:zlib`. The frozen extractor receives only decoded RGBA and the fixed public metadata `{mirrorX:false,mirrorY:false,upSign:1}`. Filenames, labels, historical camera parameters, supplied supports and selected winners do not enter its call. No image is cropped, padded, scaled, recolored or patched; the wrapper checks that extraction leaves RGBA unchanged.

The runtime read closure contains those eight sources for identity recording, the exact input plan, the safe original manifest, all sixteen named PNGs, and the held-label file. The input-plan digest fixes the paths, sizes, unique IDs and complete order before processing. Each image read checks its ordinary-file/path identity, holds and closes one descriptor in `finally`, caps encoded bytes at one MiB, checks exact size and encoded digest, and then requires 720 by 470, 1,353,600 RGBA bytes and the exact decoded digest. The copied canonical PNG decoder bounds inflated output and checks PNG structure, CRCs, filters, compressed-stream consumption and exact canonical re-encoding. This is a bounded reader for the admitted retained files, not a new historical-renderer equivalence or adversarial filesystem-isolation claim.

The wrapper writes each complete `case-NN.json`, then `measurement-summary.json`, before reading and parsing `held-labels.json`. It then writes only the separate reporting summary. The original manifest bytes are read earlier to check their digest, and that manifest contains recorded-kind fields according to the inventory; those fields are not parsed or passed to the extractor. Therefore the supported claim is delayed label parsing/scoring and label-free estimator inputs. It is not a claim that no label-bearing bytes were read before extraction or that the experiment was blind. The twelve/four labels are reporting strata, not newly verified physical or camera truth.

The write closure is one fresh run directory containing sixteen case records and the two summaries. A failure after directory creation instead retains an instrument-failure record with its completed-case count. Reusing a run ID refuses. The descriptive observer has its own bounded work counter and never supplies measurements to the extractor. It does share the frozen component helpers and predicates, so it is a separate observation path rather than an independent implementation of connected-component analysis.

#### Actual refusal location and pixel evidence

Every one of the sixteen actual case records returns `not-observable` with reason `dark-core-count`. Each descriptive record has zero qualified dark components, and each extractor result has `contourCensus:null`. Inspection of the frozen source places that refusal before auxiliary shell generation, physical-pair enumeration, owner comparison or frame recovery. No case reached the physical-correspondence stage, and none exhausted the resource bound.

The fixed core gate requires exactly four four-connected components inside x 330..480 and y 220..290, each with 80..110 pixels, width 20..24 and height 8..10. The recorded components fail those joint predicates. For example, `b-20.png` has five ROI components of 31, 34, 32, 28 and 68 pixels, with widths 11 or 12; visible stud caps do not make those components satisfy the unchanged gate. `a.png` has only two ROI components, of 57 and 372 pixels. The complete images visibly contain four studs, but that does not imply the required four components exist inside this fixed ROI at these scales and poses.

Every input is recorded as fully opaque and non-gray at all 338,400 pixels. Every exact synthetic-tone count is zero for 16, 64, 160, 196, 216 and 255. These counts accord with the individually viewed blue-gray renderings and their shaded surfaces. In the frozen extractor, exact gray INK/CORE membership is another necessary condition for the later shell mask, so the observations also expose a separate mismatch with the named schematic's tone grammar. That later stage was not executed in this run; it is a source-grounded implication of the measured zero tone counts, not an additional observed refusal.

The pictures are complete catalog renderings with visible caps, bodies and shading; they are not empty decode failures. The twelve positive images each visibly contain four stud tops at different orientations and scales. The negatives visibly have zero, zero, six and two stud tops. All are unclipped within the full image. At the smaller scales, thin boundaries occupy few pixels and some surfaces are occluded; this visual review does not establish exact physical rim coordinates or owners. The four negative refusals therefore add no demonstrated negative-specific discrimination: they fail at the same initial gate as every positive.

#### Interpretation boundary

The result establishes that the unchanged final schematic extractor did not transfer to any of these twelve catalog positives under the declared fixed metadata. It does not establish that the captures lack useful geometric information, that any alternative raster method must fail, or that the unread booklet's crop has the same properties. The row's physical orientation, exact historical compiled renderer and camera truth were not independently requalified here. A hypothetical positive would still need independent correspondence and frame validation; there was no positive to validate.

No frozen detector limit, color rule, ROI, source pin or qualification threshold was changed to make the measurement pass. The useful outcome is the measured failure boundary before ownership, with complete preserved cases. This review grants no native-source admission, producer promotion, signed-anchor replacement, calibration, placement or completion authority.

## Findings and disposition

| ID | Finding | Disposition and reason | Repair or follow-up |
|---|---|---|---|
| None | No material defect found in the exact measurement wrapper or held-label reporting. | Reviewer accepts the recorded measurement as complete and its transfer result as negative. | Preserve the sixteen cases, source identities and exact images. Any successor observation model requires its own bounded review; this run supplies no permission to alter frozen qualification predicates. |

The source-only review caveat about early manifest-byte hashing is retained explicitly above. It is a boundary on the label-withholding claim, not a hidden estimator input or a request to rerun this completed measurement. Chief integration acceptance remains separate.

## Verification

The reviewer checked actual source/metadata digests, all sixteen case records, the complete result strata and their digest linkage, all sixteen original PNG size/digest pins, and every image at original resolution. PNG hashes remained unchanged after viewing. The wrapper's actual recorded run performed encoded and decoded identity checks and input-mutation checks; this reviewer did not rerun the decoder or extractor. The persistent implementer reported one command exit 0 and 765.2729 ms. The actual measurement summary states `measurementComplete:true`, `nativeCameraQualification:false` and `productionAuthority:false`.

| Reporting stratum | Total | Observed-and-unique | Ambiguous | Not-observable | Resource-refused |
|---|---:|---:|---:|---:|---:|
| Recorded positives | 12 | 0 | 0 | 12 | 0 |
| Original negatives | 4 | 0 | 0 | 4 | 0 |

The actual [measurement summary](../../../../output/first50-fresh-search-20260908/rgba-catalog-transfer-01/run-admitted-01/measurement-summary.json) is SHA-256 `863ea751a07de7a63a1fdca898bd4b10de07d00f02259450b7d56c24fbf642dd`. The [held-label summary](../../../../output/first50-fresh-search-20260908/rgba-catalog-transfer-01/run-admitted-01/held-label-summary.json) is SHA-256 `f9f75bde2824018247f4b67296eb9e80e6c61069176c90ab83b80f70ad71c75e` and binds that exact measurement digest plus the held-label file digest. The safe manifest commitment is `c05f8827076c1973e49d9827fce1c12edad2bd50ecd526b79318b22723da91ca`. No old report, scorer output or camera truth was used to assign a correctness score.

The individually inspected case records remain under `rgba-catalog-transfer-01/run-admitted-01/` with these exact identities. They bind the complete refusal and component-detail evidence, beyond the compact measurement summary.

| Case record | SHA-256 |
|---|---|
| `case-00.json` | `6d39a2ccade5c53a6ad0d3b4e7006476db11ac74981ae67e5eba6ff8143acef2` |
| `case-01.json` | `613514a513bb38a6756285d4c3f52143a0eafa8d59119c0f1b4a75dd9292f2bc` |
| `case-02.json` | `b39f06f4ee0b99df2728b804f836e57c31f94a9694a30cd5c30b5fe659c44f6d` |
| `case-03.json` | `909641624e0438fa71ffc5a1993359a6ed60a1c3c03d66100239472dd3d738f4` |
| `case-04.json` | `40b2543c4ab675200d699eef3b0bc9ea24089040d32ce8213fec0f06c6644825` |
| `case-05.json` | `63c705b004f848a6fdd77ccf508daad68d594effe567c199f8888cf1c528b414` |
| `case-06.json` | `bcbb7d46dcf8211a87162c741dbb3e860eaba9f2bda71871547747d657a2a884` |
| `case-07.json` | `41e2b5118ed924c71791cde8bf314c8274fc899450e617d0f3ee8f78dc270913` |
| `case-08.json` | `72e19ce10f7665e1b5732f6c66f26d12f02cdce83b7fff01f91b3878a2c61584` |
| `case-09.json` | `fce7046645a956efa94449e49041927aff5fd394ba3ecd249112a638c9b93f0f` |
| `case-10.json` | `fb2db274419e3e9a3339691a1115ae6658b9c76f089acbcfc718d98a6aad26ce` |
| `case-11.json` | `a326f4bf3cfa819d4073d63762c63d0cbcfb342cfa41a1a358e0379b15885066` |
| `case-12.json` | `a3664cc980b020be96ee659b80ef195e49bdd0d3bf5aad27be67e611775d2b99` |
| `case-13.json` | `a6f2e2ee5de737df1dbc228201ea1e6bccccc55211593d73d42248771b1f5f8b` |
| `case-14.json` | `98b3fab5b3b34a27f39ea8a98ff7d55fe06826ec9b8824797d243bf86bb1fcc5` |
| `case-15.json` | `286245a6665341815e3b00790924d5b8076c67e6d9137585bbe214f8e8fe6ca0` |

All images below are the original files under `output/first50-campaign-control-20260905/pixel-correspondence/prepared-inputs/`. Each was opened separately at 720 by 470, with no aggregate sheet or resampling. “Same” below means the visible stud population agrees with its declared catalog fixture class, not that the image matches the earlier schematic or that hidden physical geometry is certified. The exact input-plan digest above additionally binds each decoded RGBA digest and file size.

| Case | Inspected PNG | SHA-256 | Bound visual outcome |
|---|---|---|---|
| case-00 | `a.png` | `b6fb4642c8fbde2e8cdf0b293c322b1e9aadd997ca2bcf8557c151195c235fa5` | Same visible four-stud class; large diagonal view. |
| case-01 | `a-20.png` | `c396659e3ceb40bec47f76baf58347cf083cee377514260f082f66a0470dea6d` | Same visible four-stud class; small diagonal view. |
| case-02 | `a-30.png` | `bd97ea7928fea069f2d2fed1a29b9e6f86cb56aa5ee7e8073809938ca67ea49e` | Same visible four-stud class; intermediate diagonal view. |
| case-03 | `b.png` | `8d2bf104f18098790c016fbbd821acedec6122bf802cbd1b6fa49bdeffcbcd8a` | Same visible four-stud class; large broadside oblique view. |
| case-04 | `b-20.png` | `c6711d8a67f983edbc14eabb611336a060f3b27b3993bccd1d9f823c4cd27d78` | Same visible four-stud class; small broadside oblique view. |
| case-05 | `b-30.png` | `b56795837a49f85c6e3004ecf77993ffc2251a082544cd9538136b8df25d7b11` | Same visible four-stud class; intermediate broadside oblique view. |
| case-06 | `c.png` | `2a04f5fb714000186d1e6216dde52b983b122706719ea5a10216bb8020e28fb7` | Same visible four-stud class; large elevated diagonal view. |
| case-07 | `c-20.png` | `ae4858c4c7b499ba470ece6217781874b5244d7eead14aa55638cf5c08ae6393` | Same visible four-stud class; small elevated diagonal view. |
| case-08 | `c-30.png` | `4436626cc4ebd3cb226da7a3a3bc9bd1a6103d168da25b5c8543206eb91304c6` | Same visible four-stud class; intermediate elevated diagonal view. |
| case-09 | `d.png` | `90b14ea5d750e43450aac9b8b6fe55c7538813bcf587d993c49249083b7d749c` | Same visible four-stud class; large low oblique view. |
| case-10 | `d-20.png` | `c06f1f6071c675ee749932e3cdc59a12af0846941ef66d78f6ec9976d444126d` | Same visible four-stud class; small low oblique view. |
| case-11 | `d-30.png` | `65579f96f00f97165fc985b890f4a7a5efb2db6b7b4ec1eda664624b0bafb4bf` | Same visible four-stud class; intermediate low oblique view. |
| case-12 | `absent.png` | `f56f732070192a848a5c5d66e58ac0e8e7ecb69edadbebec9215d05b45e72da9` | Different from four-stud target: complete smooth tile, no studs. |
| case-13 | `rectangle.png` | `538799e97015e4d731d5b6409e9be8483df04dca590f06884f81fe26a9695c49` | Different from four-stud target: flat rectangular face, no studs. |
| case-14 | `extra.png` | `ff76750ae2f64d69ee7e4090e790ca30578a3511106b46efcbed30274a626e3c` | Different from four-stud target: six visible stud tops. |
| case-15 | `missing.png` | `69faa393b5feb80a3f2cb2367be257a5c3d1ba5adbef9445f695913307c774f0` | Different from four-stud target: two visible stud tops. |

Unavailable and intentionally unrun: frame-correctness scoring, independent owner/rim correspondence validation, historical renderer closure reconstruction, fresh rendering, the prior 96 mutation captures, native booklet access, calibration, one-shot validation, authority/placement flow, browser/default tests and full repository gates. No claim beyond the declared fixed sixteen-input measurement is inferred from their absence. Local documentation checks cover the exact proposal copy, local links, Markdown fences and trailing whitespace. No task-owned long-lived process or disposable output was created; the exact source and retained measurement remain needed for the handoff.

## Round outcome

The frozen extractor's transfer success is 0/12 on the predeclared catalog positives. All sixteen files were measured with exact identities, and the first-stage `dark-core-count` refusal was observed in every case. The actual pixels demonstrate visible catalog geometry with scales, positions, shading and tones outside the successful schematic's observation conditions. They do not demonstrate that physical correspondence is impossible.

This review accepts the bounded measurement and rejects any extrapolation from the earlier schematic positive to this catalog roster. It promotes no producer and admits no protected source. Chief integration acceptance remains pending. Only this review and the exact proposal snapshot were authored; neither was committed, merged to main or pushed by this reviewer, and no existing documentation checkpoint was changed.
