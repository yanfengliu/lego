# Prospective exact-byte Poppler relocation

Status: corrected prospective design for a user decision; no implementation, source closure, runtime readiness or source stage is accepted by this text. Owner: `/root/fresh_runtime_relocation_design`. Assignment base remains `6300c6b817722d2652924ba9df7e06264c135e31` plus the preserved dirty tree; this correction performs no Git operation to re-attest HEAD. It changes authored text only and preserves the same five core hunks, two conditional closure hunks, exact 178-file body and proposed root commitment. Read scope was allowed plain source, authored documents and retained metadata. No production source was changed, and no runtime payload, PDF or image was opened. No production import, native invocation, test, browser, OS query, installation or source-bearing command ran.

This is an immutable child of the 23,177-byte original `../design.md` at `sha256:9523d0883a0fbb32bafe6fcad6f443fd6239d10610f6b16f329f314e3a324d87`. It responds to F5 in `docs/work/0_first-50-campaign/reviews/4_design.md` at `sha256:ad89580983595120cc355203c3c4da1fc012f5d9b2e4797b9bd94516767c84f1`. The original design, its tracked review snapshot, review 4 and all attempt02 artifacts remain frozen. References below beginning `attempt02/` resolve from the parent proposal directory, not from this child. The original proposal's measurements are retained historical results; this correction separately verifies source identities and patch preservation.

## Decision sought

Approve one prospective exception to the current no-runtime-repin rule: bind the exact frozen Poppler 26.05.0 roster to the already recovered real directory `%USERPROFILE%\Documents\github\lego\output\first50-fresh-search-20260905\runtime-restoration-20260905T211128-cde41bb3\Library`, with toolchain commitment `sha256:f36d5a2829890b1d55fd74a48577260599569bcfb386a32383bd84da286aa7d6`. This replaces the production binding to `%USERPROFILE%\.cache\codex-runtimes\codex-primary-runtime\dependencies\native\poppler\Library` at `sha256:ad3675e883db5966ee288583783841777cd63d908789ec4d2f77247f7433824f` only for fresh successor execution after independent review and readiness checks. Keep all predecessor commitments and evidence under their original identities. Retain the recovered directory for the campaign while it is referenced by an accepted binding.

The decision permits the two exact location fields and their necessary dependent code/evidence commitments to change. It grants no substitute runtime file, roster reduction, shared-runtime modification, threshold or extraction change, extra validation opportunity, reconstructed capability, or source access. The coordinator separately accepted the current diagnostic wrapper repair; its exact source-closure rebind remains pending and is not supplied by a relocation hash. The persisted offline qualification replay route remains unadmitted under this decision until a separately concrete metadata-only gate and mixed-lineage controls are reviewed before any protected read. If the user declines, the existing frozen binding remains authoritative and this alternative cannot proceed.

For this specific current-host literal-binding design, the five core hunks are the smallest identified code delta: the existing module owns one literal configuration and the PowerShell helper owns the same literal root and body digest. They need no environment-variable override, caller-supplied configuration, new capability, multiple accepted runtime roots, path alias, junction, hardlink, or new broker/framework. A root-independent identity would change the meaning of the existing commitment and is excluded. This is not a claim that every alternative strategy is impossible or that relocation is the globally smallest scope decision.

## Source authority and evidence

The original attachment `<codex-attachment>`, lines 88–100, identifies the frozen 26.05.0 recovery, its exact README and roster, and says: “Do not invent its build prefix, substitute an equivalent README, remove it from the roster, change the shared runtime, or silently repin the runtime root.” The current `docs/runbooks/first50-disclosed-verification.md` preserves runtime pins, says adoption changes none, requires exact reviewed source-closure rebinding, and explicitly does not authorize runtime repinning. The approved disclosed evidence claim is therefore insufficient to implement this proposal.

The recovery metadata reports 178 files, 96,810,767 bytes, six directories and a distinct file identity for every entry. This pass compared every ordered path, byte length and SHA-256 against the literal production roster and found all 178 equal. This is a fresh metadata comparison, not a fresh payload hash, link/realpath check, or runnable-runtime finding. The entire input metadata and its digest are retained in `attempt02/recovery-metadata.json` and `attempt02/static-proof.json`.

`attempt02/before/` contains the original pass's exact source snapshots for the inspected producers, consumers, closure rules and protocol. `attempt02/static-proof.json` identifies every snapshot by path, byte length and SHA-256. These are captured bytes, not reconstructed LF versions; HEAD alone is not their identity. The original pass did not read prior branch reports, transcripts, strategy diagnoses or memory. This correction reads the assigned F5 review and directly checks the relevant current plain source. `verification.json` identifies these current reads, checks the seven patch-target source snapshots and records the unchanged attempt02 patch/body hashes. It supplies no runtime or source-stage evidence.

## Proof of necessary changes

The toolchain body is `JSON.stringify({ root, version, directories, files, loaderEnvironment })`. The independently parsed source roster and fixed fields reproduced its existing `ad3675...` digest exactly before any hypothetical edit. `old-toolchain-body.json` and `proposed-toolchain-body.json` differ only at `root` and `loaderEnvironment.FONTCONFIG_PATH`. The latter remains the exact current expression `root + '/etc/fonts'`, including its existing separator spelling. Version, all six directory names, all 178 file rows, `SystemRoot`, `WINDIR`, and `FONTCONFIG_FILE` remain byte-identical values. The total bytes remain 96,810,767. The helper's frozen launcher digest and wrapper's host-PowerShell and launcher pins remain unchanged.

| Commitment | Current | Proposed | Why it changes |
| --- | --- | --- | --- |
| Toolchain body | `ad3675e883db5966ee288583783841777cd63d908789ec4d2f77247f7433824f` | `f36d5a2829890b1d55fd74a48577260599569bcfb386a32383bd84da286aa7d6` | Exactly the two location fields |
| PowerShell lock helper | `e19bf2be3e201ccf89f993e76bdfce6a47a244f9f8e91c5f871d3f565305b53d`, 9,977 bytes | `bcfc4e56159d020dff582eca564c55ee6644d8a6b1a7775273948ada4e31cc56`, 10,009 bytes | Its embedded root and expected body digest |
| Execution roster | `5ef0dd3515831d7f06f06d705d27d0c473eefcfc640af2b75f72adfaa321172f` | `6eaf7d9ad7c1453b1172f2246adbc939432f72083d94e2d3fded482fcc135026` | One literal `pdftoppm.exe` target path |
| Source manifest | `6747a2642bbb01cf2d10177ce1ca5d9333abbe4a14c39ba03414ddffd4c2892a` | Conditional `680b2404b082c78713831f42e718035b049f5309c4d6dae0f5c48b27cd0ecf76` | Three changed source rows; exact closure rebind remains pending, including the separately accepted wrapper repair |

All hashes in this table are SHA-256. The original execution-roster and source-manifest digests were also reproduced by static parsing before their proposed edits. No production module was imported to calculate them. The conditional source-manifest result copies every other existing row unchanged; it is not a claim that the rest of this dirty tree currently satisfies those rows.

## Reviewable proposed hunks

`attempt02/core-prospective.patch` has five files. `attempt02/proposed/` retains the exact resulting source bytes. It changes:

1. `apps/web/e2e/real-build-prefix50-subbuild-return-review-poppler-toolchain.ts`: replace the literal root and the literal body commitment. The derived `FONTCONFIG_PATH` changes automatically. The roster and version do not change.
2. `apps/web/e2e/real-build-prefix50-subbuild-return-review-poppler-lock.ps1`: replace only `$ExpectedToolchainRoot` and `$ExpectedToolchainCommitment`.
3. `apps/web/e2e/real-build-prefix50-subbuild-return-review-poppler.ts`: replace only `HELPER_BYTES` and `HELPER_DIGEST` to authenticate that exact proposed helper. Its production factory still ignores caller configuration; no new override is added.
4. `apps/web/test/real-build-prefix50-step44-source-reader-closure-execution-roster.ts`: replace only the exact Poppler executable path in `externalTargets` and the independently pinned roster commitment. Keep the existing ten execution entries, capabilities, local targets, reasons and IDs. `shared-pinned-poppler` remains a compatibility ID for the shared code boundary; its ID does not certify storage in the shared runtime.
5. `apps/web/test/real-build-prefix50-subbuild-return-review-poppler.test.ts`: replace the two independent expected literal body digests in the production-factory pin test. The synthetic configurations, guard tests and no-spawn assertions remain unchanged. Additional negative controls below must be authored and independently reviewed before implementation acceptance; these five hunks alone do not establish adequate verification.

`attempt02/conditional-closure-prospective.patch` has two files: the three changed rows of `real-build-prefix50-step44-source-reader-closure-manifest-a.ts`, and the resulting aggregate pin in `real-build-prefix50-step44-source-reader-closure-manifest.ts`. It is a concrete calculation for review, explicitly not an applicable authorization to rebind.

The current manifest pins `poppler.ts` to 9,875 bytes and `sha256:794598a43a968f8abac9be1fb93093663dd3e29fbd0180cc9f4848964f370a50`. The current source snapshot is 9,821 bytes and `sha256:da79335c191aaf11bff8a42ee5d80ac7abb21defca0ddbd49bbd3345b02c05f7`. Its proposed bytes remain 9,821 at `sha256:53d4cca008da359c1054995208e1e0005dd4fc1f097006a3b74f45f43d02f359`. The coordinator separately accepted `da793...` as the diagnostic repair; this correction does not re-review that repair. The relocation accounts only for its two helper-pin substitutions. The earlier 54-byte difference is not unexplained, attributed to relocation, rejected as unauthorized, or approved by the conditional aggregate. Exact integrated source-closure review and rebind remain pending. If that work changes a reviewed source byte, regenerate and review the affected proposal hashes. Do not update the frozen row merely because the conditional aggregate is self-consistent.

## Exact prospective successor addendum

The following is the exact proposed addendum for the runtime-root decision. It has no effect before explicit user approval and independent review; this document records no approval event. The campaign record must bind the accepted text and review, exact before/after source snapshots, old/new runtime bodies, separately accepted diagnostic repair and current admitted stage. Keep that control record outside closed qualification trees and preserve the old protocol, prohibition, incident and receipts.

> Upon explicit user approval of this addendum, the disclosed first-50 successor may prospectively bind the unchanged Poppler 26.05.0 roster of 178 files, 96,810,767 bytes and six directories to `%USERPROFILE%\Documents\github\lego\output\first50-fresh-search-20260905\runtime-restoration-20260905T211128-cde41bb3\Library`, at toolchain commitment `sha256:f36d5a2829890b1d55fd74a48577260599569bcfb386a32383bd84da286aa7d6`. This one binding replaces `%USERPROFILE%\.cache\codex-runtimes\codex-primary-runtime\dependencies\native\poppler\Library` at `sha256:ad3675e883db5966ee288583783841777cd63d908789ec4d2f77247f7433824f` only for fresh successor execution. The allowed body changes are exactly `root` and its derived `loaderEnvironment.FONTCONFIG_PATH`; all file digests, lengths, roster entries, version and other loader values remain unchanged. The five reviewed core hunks and their necessary helper, execution-roster and source-closure commitments remain subject to exact integrated review. The separately accepted diagnostic wrapper repair still requires its exact closure rebind. This addendum authorizes no shared-runtime modification, file substitution, guard weakening, source-extraction change, threshold change or retagging of predecessor evidence. Retain the recovered directory while this binding is in use.

> Root approval is not runtime readiness or source-stage admission. Before any native readiness invocation, independently review its exact source closure, permitted reads, owned output and cleanup; then run only the explicitly admitted checks for exact payload/filesystem integrity and synthetic bounded execution. PDF-only source-lock evidence remains unchanged for the unchanged PDF and does not identify a successor runtime. The five relocation hunks do not implement a pre-read runtime-lineage check. No new authority, schema or source-lock predicate is included in this decision.

> `verifyPersistedRealBuildPrefix50Step44RealDomainQualificationOffline` in `apps/web/e2e/real-build-prefix50-subbuild-return-review-camera-real-domain-persisted-qualification.ts`, including any caller that can enter it, remains unadmitted. Before admitting that route, separately define and independently review a concrete metadata-only check that binds its exact approved successor lineage, full source closure and runtime body before source-geometry reproduction or any protected PDF/hash/crop read. Prove mixed-lineage refusal with source-free read/spawn tripwires. Those checks are pending; campaign inventory or a matching PDF-only digest is not a capability or a substitute for them. This exclusion grants no bypass of a required persisted replay gate, and campaign completion remains incomplete while the required route is unadmitted.

> Every other source-bearing entry point still needs its own reviewed readiness and stage admission. Preserve the fixed PDF, exact first-50 scope, thresholds, validators, scientific inputs, opaque capability flow, 211-candidate population and one-shot Step-43 accounting. A new root, process, directory or source-closure rebind creates no additional validation opportunity. Preserve all old commitments and incidents under their original identities. The ordinary browser/full gate remains unavailable until its actual source reads are admitted, and Step 51 is never assembled.

## Direct consumer map

Paths below are relative to the repository solely to keep this table legible. Full absolute source snapshot locations and digests are in the proof file. Direct consumers were located by literal root, old digest, exported symbol, import-name, helper digest and propagated commitment searches over `apps`, `packages` and `scripts`; ignored historical evidence was not searched.

| Consumer | Dependency and treatment |
| --- | --- |
| `apps/web/e2e/real-build-prefix50-subbuild-return-review-poppler-toolchain-verifier.ts` | Uses all configuration fields and verifies body digest, canonical real root, exact directories/files, single-name identity, file bytes and digest. No source edit proposed. |
| `apps/web/e2e/real-build-prefix50-subbuild-return-review-poppler-lock.ps1` | Independently pins root/body and preserves all locked-file and job-execution guards. Two literal edits only. |
| `apps/web/e2e/real-build-prefix50-subbuild-return-review-poppler.ts` | Sends body/commitment in the framed request and returns the verified commitment; authenticates the changed helper. Two helper-pin edits only. |
| `apps/web/e2e/real-build-prefix50-subbuild-return-review-camera-real-domain-pdf-crop.ts` | Carries `poppler.toolchainCommitment` into bounded crop evidence. No edit; future evidence has the new identity. |
| `apps/web/e2e/real-build-prefix50-subbuild-return-review-camera-real-domain-calibration-raster-commitment.ts` | Rejects a mismatching Poppler pin, requires unchanged sealed crop dimensions/pixels/canonical PNG, then includes toolchain identity in the raster commitment. No guard edit. |
| `apps/web/e2e/real-build-prefix50-subbuild-return-review-camera-real-domain-gate-evidence.ts` | Constructs and checks runtime-branded qualification bindings with the imported Poppler pin and held output-tree identity. No edit. |
| `apps/web/e2e/real-build-prefix50-step44-camera-only-source-lock.ts` | Public `evidence` at lines 122–150 projects only the unchanged frozen PDF; separate `runtimeIdentity` at lines 151–156 retains the full lock digest and live repository/helper identity. No edit. The projected evidence does not identify a changed runtime. |
| `apps/web/e2e/real-build-prefix50-subbuild-return-review-camera-real-domain-persisted-qualification.ts` | Its initial comparison at lines 388–392 uses the PDF-only public evidence. It next reproduces source geometry and current source cases, reaching a protected PDF read before the later session comparison at line 424. No edit; this offline replay route is explicitly unadmitted pending separately reviewed metadata-only pre-read checks and source-free mixed-lineage controls. |
| `apps/web/e2e/real-build-prefix50-step44-later-source-derived-raster.ts` | Carries the genuine runtime receipt into derived raster evidence. No edit. |
| `apps/web/e2e/real-build-prefix50-step44-later-source-derived-contract.ts` and `...camera-real-domain-gate-contract.ts` | Declare the propagated digest fields. Shape unchanged. |
| `apps/web/test/real-build-prefix50-step44-source-reader-closure-manifest-a.ts` and `...manifest.ts` | Pin exact source/helper bytes and complete source roster. Three conditional row edits and one conditional aggregate edit, with no row added/dropped. |
| `apps/web/test/real-build-prefix50-step44-source-reader-closure-execution-roster.ts` | Independently pins the literal native target and complete execution roster. One path and aggregate edit. |
| `apps/web/test/real-build-prefix50-step44-source-reader-closure-process-assertions.ts` | Expects the imported root's `bin/pdftoppm.exe` plus host, helper and launcher; verifies existing roster IDs. No edit. |
| `apps/web/test/real-build-prefix50-step44-source-reader-closure-roster.ts` | Retains the same audited I/O categories for wrapper and verifier. No edit or added capability. |
| `apps/web/test/real-build-prefix50-subbuild-return-review-poppler.test.ts` | Independent production body pins plus synthetic wrapper guards. Two expected-digest edits; additional negative controls still required. |
| `apps/web/test/real-build-prefix50-subbuild-return-review-poppler-lock-integration.test.ts` | Real locked execution with a synthetic generated PDF checks the imported commitment and quiescence. Not run in this pass. |
| `apps/web/test/real-build-prefix50-subbuild-return-review-poppler-test-support.ts` | Calls default production verification. No edit; it reads actual runtime files and was not run. |
| `apps/web/test/real-build-prefix50-step44-later-source-derived-operation.test.ts` | Uses the imported commitment in a synthetic expected evidence row. No literal rebind needed. |
| `apps/web/test/real-build-prefix50-step44-later-source-authority.test.ts` | Consumes closure manifest and execution-roster commitments through their existing audit. No edit proposed; its actual reads must be preflighted before future selection. |

The managed launcher C# source accepts an executable, working directory and exact environment from the helper. The helper already derives executable and working directory from its pinned root; those runtime argument values necessarily relocate. The launcher binary/source and its digest, process-tree limits, timeout, stdio, environment construction, job assignment and quiescence implementation remain unchanged. Its binary was neither read nor loaded.

## Evidence separation and unchanged scientific inputs

The new runtime body does not assert that old evidence was produced at the new location. Preserve the old body, source closure, runtime receipts, refused calibration, incident, rejected branches and all candidate parents. Keep new campaign control records outside closed qualification trees. Use fresh, exclusive output identities for source-free readiness and every subsequently admitted stage. Never insert relocation metadata into an existing verifier-owned tree or rewrite old receipt fields to the new digest.

After reviewed implementation, the changed source bytes, exact integrated closure and full bootstrap lock roster commitments must identify the successor. The camera-only lock's public `evidence` is a different projection: `bootstrapSourceManifestDigest`, `lockManifestDigest`, file/byte counts and its final `commitment` are constructed from the one frozen PDF row at `real-build-prefix50-step44-camera-only-source-lock.ts:122–150`. For this unchanged PDF and projection, that public evidence remains unchanged. The full lock digest is retained separately in `runtimeIdentity.lockManifestDigest`, alongside directory, helper PID and repository root at lines 151–156. A fresh runtime-branded object does not make its PDF-only evidence identify new code or a new Poppler binding. Neither the public evidence shape nor its predicates change in this proposal.

New raster commitments that include the changed Poppler body commitment change even when actual PNG/RGBA bytes are identical. Session, qualification, later-source and downstream commitments must be freshly produced and checked where their bodies incorporate changed inputs; do not infer that every similarly named source-lock field changes. Matching public PDF-only evidence cannot migrate an old receipt or establish runtime equality. Existing opaque capabilities must come from their genuine producers and cannot be reconstructed or transferred by a campaign record.

The direct persisted replay ordering is decisive. `real-build-prefix50-subbuild-return-review-camera-real-domain-persisted-qualification.ts:388–395` compares the manifest to `sourceLock.evidence`, validates calibration metadata and enters source-geometry reproduction. Lines 397–403 prepare current source cases. `real-build-prefix50-subbuild-return-review-camera-real-domain-source.ts:296–303` validates the live lock and enters the calibration raster reader; `...camera-real-domain-calibration-raster.ts:53–65` hashes the protected PDF before its two crops. The persisted session comparison occurs later at line 424. A later raster/case/session refusal therefore cannot prove refusal before those earlier reads. This static ordering does not show that an old qualification passes, and no protected input was read here. The five relocation hunks leave this route unchanged and do not supply a universal pre-read lineage discriminator.

The fixed PDF identity, exact source-extraction rule, crop coordinates and density, sealed target PNG/pixel digests, normalized first-50 program, admitted source roles, truth snapshots, validator snapshots, threshold bodies, orientation-law requirement, entire shared predecessor masks, all 211 candidates, denominators, tie-break/confirmation rules and passive later-panel limits are unaffected commitments. No value in those bodies is proposed to change. Actual raster equality is not assumed: the unchanged sealed crop checks must still reject any difference at the proper admitted stage. Qualification is still Steps 41/42; Step 43 remains the same one prospective campaign validation opportunity; page 45 and the Step 44 consumer remain downstream; nothing assembles Step 51.

There is no historical receipt migration. This is a fresh runtime-location binding for a disclosed successor with unchanged source-stage and attempt obligations. Retained old evidence cannot become current acceptance evidence by matching a new constant or the unchanged PDF-only projection. The persisted offline replay route is not admitted even for historical audit by this proposal; it remains unavailable until the separate pre-read admission work below is concretely reviewed. No required replay gate is waived by leaving it unavailable.

## Acceptance and negative controls after a decision

Before implementing, a fresh independent read-only reviewer must accept the corrected explicit scope, current-byte snapshots, exact two-field body change, unchanged execution guards and actual downstream dependencies. The diagnostic wrapper repair is separately accepted by the coordinator, but the exact integrated source-closure rebind remains pending. That review must preserve every roster member, source-extraction rule and forbidden access boundary. Any additional discrepancy remains a refusal rather than a digest update. Acceptance of this bounded root decision must explicitly preserve the persisted replay exclusion; it is not acceptance of a complete source-admission plan.

Source-free static controls must reproduce both old and new body commitments from independent expected fields, prove all 178 rows/six directories/version are identical, and remove the two changed fields to show identical remaining bodies. Mutating one file digest, byte count, directory, version, root or `FONTCONFIG_PATH` must fail the production factory or a separately pinned admission assertion. Keeping the new root with the old digest, or new digest with old root/environment, must fail before any runtime read or spawn. A third root with exact files must remain unapproved; there is no accepted-root set or caller-selected override.

Exercise explicitly selected synthetic refusal classes: extra/missing file, changed digest, hardlink/duplicate identity, symlink/reparse/aliased root, malformed body/header, caller override, hostile inherited PATH/NODE_OPTIONS, helper/body mismatch, diagnostic leakage and non-quiescent/malformed receipt. Maintain independently pinned launch-target controls that reject a changed target despite identical call structure. These checks prove bounded controls only. The existing Poppler unit suite can still read its pinned native host/helper inputs; its source-free label means no protected booklet input, not permission to run it in a native-payload-free stage. Inspect and admit each selected check's actual reads before execution. New expected pins must be independent literals rather than imported from their producer. No such check ran in this correction.

Only after the user decision and reviewed readiness-stage admission may the real verifier read all 178 files at the proposed location and check canonical roots, single-name identities, full roster and digests. Then the existing synthetic-PDF locked integration path must demonstrate the real helper, authenticated unchanged launcher/host, bounded stdin, timeout and complete child-tree quiescence. Do not qualify relocation using the test-only configuration seam. Every task-owned output/process must be cleaned in its declared finally path. The current pass did not run any of these checks or inspect host availability.

The persisted offline qualification replay route remains unadmitted. Its release requires a separately concrete metadata-only check that binds the exact approved successor lineage, full source closure and runtime body before source-geometry reproduction and the first protected PDF/hash/crop read. This correction does not choose or implement that mechanism, add an authority/schema, edit source-lock predicates, or claim that the five core hunks supply it. The eventual proposal must include source-free controls with read/spawn tripwires for same-PDF old/new runtime mixtures, mismatched full closures, copied manifests, caller-shaped records and an unapproved third runtime. Retagged old receipts or a campaign inventory alone cannot satisfy it. Independent review must verify that every entry into this replay route reaches the proposed metadata check before protected operations. Until then, do not invoke the route to learn whether a later check refuses.

At any other independently admitted real-source entry point, every unchanged sealed crop/pixel and frozen scientific gate still applies; that entry point's actual source-release order needs its own review. A validation attempt remains consumed or unresolved under existing campaign accounting even if the runtime fails. No fresh directory, process, root, source-closure rebind or relocation decision resets it. The full browser gate remains unavailable until its actual source reads are admitted. Withholding the persisted route is a restriction on execution, not a substitute for any required final acceptance gate.

## Material risks and honest unresolveds

Exact bytes alone do not prove relocation is operationally equivalent. Embedded resource paths, host dependencies, system PowerShell pins, loader behavior and the longer working directory could cause refusal or different output. This pass deliberately did not read config/font/DLL/PDF payloads, run Poppler, inspect OS features, install anything or transfer a payload. The unchanged guards and pixel checks must expose those failures later; do not fix one by changing a frozen runtime file or shared environment under this decision.

The proposed directory is under ignored task output rather than a managed permanent installation. The coordinator must retain it while bound and exclude it from ordinary task cleanup. Moving it again requires another explicit location decision and new dependent commitments. Selecting this already recovered directory avoids copying another payload or altering file identities in this pass. Any future copy would still need all normal exact-runtime checks and an explicit destination binding.

The directory remains mutable by its owner; the existing canonical-path, single-name, exact-roster and open-stream checks are preserved. This proposal neither proves new OS isolation nor claims that same-user path races, ambient system resources or authority weaknesses have been repaired. Independent review must assess the changed location against existing source-lock and filesystem assumptions. If those assumptions require a broader trust change, stop the relocation implementation and return that distinct design decision rather than weakening guards.

The unchanged five core files and two conditional closure files remain a feasible static location-binding delta. The corrected design distinguishes the unchanged PDF-only evidence from the full runtime identity and explicitly withholds persisted offline replay. This text fix establishes neither a metadata-only pre-read mechanism nor safe execution of that route. Operational feasibility, exact integrated source closure, native quiescence, source-stage admission, camera qualification and downstream acceptance remain unverified. The wrapper diagnostic repair's separate acceptance does not supply its pending closure rebind. A root decision removes no other required gate.

## Pass record and cleanup

The first static proposal-builder attempt failed while converting a literal execution roster to JSON because a key-rewrite regex also reached the `C:` inside a quoted path. No runtime or production code ran. The corrected attempt restricts key rewriting to the start of a source line and independently reproduces the old roster commitment before calculating the new one. `attempt02` is the complete proposal. Duplicate partial snapshots from the failed static instrument were removed after the complete snapshots were checked.

Original-pass static checks, retained unchanged in `attempt02/verification.json`: the 30 captured source/protocol snapshots then matched the tree; reverting the two location fields reproduced the old runtime body; the 527-row source manifest retained all rows with only the three named source rows changed; both patches passed plain `git apply --check` without application; the design/proof were ignored and the design had no trailing whitespace. This correction does not rerun those gates or reinterpret their scope. Its own `verification.json` records current source/hash inspection, unchanged core/conditional patches and exact authored before/after identities. No unit, integration, source-closure audit, native, browser or full gate ran.

No persistent process, browser, GUI, server, watcher, installed runtime change, commit or push was created. Retain the complete proposal, exact snapshots and metadata for user/independent review. These ignored artifacts are the intended handoff, not a new production runtime or accepted source input.
