# Initial fixture and raw-support source preparation 01

Owner: `/root/inverse_boundary_source`, 2026-09-08. Base supplied by root: `6300c6b817722d2652924ba9df7e06264c135e31` plus its preserved dirty tree. This is the first measurable source increment of the existing reviewed inverse-boundary-graph candidate. It is not a new strategy, a complete inverse solver, an execution admission or campaign acceptance.

Root authorized only source authoring inside this candidate directory. New implementation is confined to `fixture.mjs`, `trace.mjs` and initial orchestration/checks in `run.mjs`. `inverse.mjs` and `interval.mjs` remain absent. No project code was imported, parsed, typechecked or executed. No image was generated, decoded, read or inspected. No browser, model, network, solver, renderer, subprocess, native toolchain execution, Git operation, protected source or old result artifact was used. Required canonical text included historical status paragraphs; none supplied fixture geometry, observation decisions or evidence for this increment.

The immutable design parents remain `proposal.md` (`f91e05d14a9eb9c4849da764a56c44c8300c4889bebfeda24bae83b92c36c3c4`), `proposal-v2.md` (`bb765b22632b2448931a0fa65f8220ac68c7490f6e51af364cecfc302dfd11e9`), `proposal-v3.md` (`b3d91e608d45831320982c1530a7fcd5cc9b607a59a8e33a550c84d9f3b60536`) and `source-closure.json` (`4838d4eb314c3eb589367a4f6d393917c3945fa9ee85898f9931c75d7cd7697c`). Their matching review snapshots 12/14/17 and authored reviews were checked as text/metadata. Parent hashes are unchanged.

## Concrete authored behavior

`fixture.mjs` declares the eight reviewed cases at 720 by 470. Samples are exact doubled integer pixel centers, alpha is 255, there is no antialiasing and ink width is zero. Ellipse and polygon predicates use exact safe-integer arithmetic over this fixed coordinate domain. Opaque platform faces are filled first, then each disjoint cylinder's body and front base half, then its cap, and finally the P2 foreground polygon. No independent stroke can cross an occluder. Distinct studs have disjoint projected horizontal interiors, so the stud iteration order does not choose an overlap between them. Cap fill hides the rear-platform crossing and hidden wall portions. These are source reasoning claims awaiting independent visibility review and actual raster inspection.

The palette, platform corners and height, cap centers and semiaxes, wall heights and P2 polygon are exactly those frozen in the reviewed parents. `P1` receives the decoded saved `P0.png` from the same invocation and inverts its RGB bytes, preserving alpha. It does not render a separately rounded photometric variant. `A0` omits platform and bodies; `A1` retains cylinder index 0 and no platform; `N0` draws only platform. Fixture indices and colors never enter the raw tracer's observation object.

The previously unspecified mutated member is frozen as index 0 for both N1 and N2, the cap centered at `(210,130)`. N1 preserves that cap and changes only its underlying body/base displacement from 16 to 24 pixels. N2 replaces only that cap by its same-extrema closed rectangle `[180,240] x [112,148]`; underlying body/base geometry remains at displacement 16. The cap rectangle wins visibility ties and hides covered portions of the original flanks. Root explicitly resolved “retaining wall/base boundaries” as retaining underlying geometry and drawing only its frontmost visible remainder, consistent with v2's no-hidden-line clause. Both flanks can be hidden at their extrema because base tangency y=146 is above rectangle bottom y=148. N2 therefore supplies no conic-discrimination result in this increment. A future missing-incidence refusal must not be counted as a successful conic control; its required extrema-only mutant remains untested and may leave the candidate incomplete.

`trace.mjs` accepts only exact-size RGBA/dimensions plus an operational budget callback. It has no import or fixture/case/geometry access. Every unequal horizontal or vertical neighboring pixel pair is one retained dual-grid segment. Its ID is `2*p+axis`, with axis 0 for the right neighbor and 1 for the lower neighbor. Both original pixel indices and RGBA tuples survive. Strict maximum-channel contrast membership is recorded at all seven levels `0,2,4,8,16,32,64`. Identical nested graph populations share one topology with multiple membership bits; they are not multiple physical samples.

Maximal chains stop at degree-one or junction vertices. Every degree-two component retains its entire ordered cycle. Four-way nodes retain all three perfect pairings of their four incident raw support IDs. No continuation is selected, no gap is joined and no physical boundary ownership is assigned. Degree-three junctions remain branch nodes. The graph includes its declared Chebyshev-radius-2 envelope as `declared-unverified`; it contains no camera, center, stud index, cap/base role or confidence.

## Independent initial-stage checks

The future runner writes all eight fixture PNGs, raw graph JSON files and overlays before it invokes the coordinate-based support audit or any case-specific expected interpretation. Fixture source necessarily contains geometry from import time; this is operational separation of interfaces and evaluation, not byte-unread labels, a blind experiment or a separate security identity.

The graph checker does not import tracer helpers or its threshold export. It independently scans all neighboring pixel pairs in a different order, recomputes contrasts and memberships, checks original sample bytes and dual-grid endpoints, enumerates incidence, and verifies exact support coverage, chain adjacency/maximality, closed-component claims and complete four-way continuations at every contrast level. Its denominator is every horizontal and vertical neighbor pair, `470*719 + 720*469 = 675610`, for each input; it reports the measured unequal-pair and chain populations. Empty graphs are legitimate observations, not a physical success.

After that barrier, four wrong-output controls act on independent copies of P0's actual graph: delete a support record, corrupt one membership, break one chain's support coverage, and remove one four-way pairing if one exists. Each reports its actual population and exercised denominator. The last control is explicitly `not-exercised-no-applicable-population` if P0 contains no four-way node. Budget failures cannot count as detected mutants. No extra fixture or raster is created for these controls. Source authoring does not establish that any control has run or failed as intended.

Separate finite support measurements inspect actual raw segments near the reviewed extrema and exact rational ellipse sites. Each unchanged audit rim uses 258 rational sites including repeated parameter endpoints; all four extrema are also reported separately. N2's changed rim has zero ellipse sites with an explicit rectangle/conic-control caveat. P2's occluded sites stay in the measurement population and carry an explicit warning that foreground proximity is not rim support. For P0/P1/P2 the audit lists actual raw-chain candidates whose support lies within 2 pixels of both D and C and checks 161 equally spaced points along the declared front edge against that same raw chain. A candidate is not labeled a physical platform edge by this audit.

These probes name their finite populations. Proximity to the union of raw boundaries does not establish ownership, full continuous boundary coverage, visible-rim completeness, an observed vertex localization box or a camera constraint. The finite report deliberately leaves `continuousCoverage`, `visibilityProof` and physical ownership unverified/unimplemented. It never silently omits a missed site or enlarges the fixed two-pixel envelope.

Camera regions, physical assignments, conic rejection, partition certificates, second-point feasibility, signed incidence and all corresponding falsifiers are explicitly `not-implemented` or `not-run`. No zero-valued placeholder means pass. The final initial-stage status requires actual full-pixel/graph checks and the applicable raw-graph mutants; it does not imply this candidate's stronger reviewed acceptance criteria pass. The flat printed-texture alternative remains explicit.

## Static dependency and file closure

The exact metadata is in `source-closure-initial-01.json`. `fixture.mjs` and `trace.mjs` have no imports. `run.mjs` imports them, the already source-inspected codec `apps/web/e2e/real-build-prefix50-subbuild-return-review-png.ts`, and builtins `node:fs`, `node:crypto`, `node:path`, `node:url`, `node:process`, `node:buffer`. The codec imports only `node:buffer` and `node:zlib`; it has no filesystem, browser, provider, process-launch or network APIs. The extra builtin `node:url` resolves the exact script location; it is not a loader helper or external dependency. There is no catalog, rendering, provider, evaluator or project-runner import.

Node's explicit `.mjs` entries and native stripped `.ts` codec use the frozen root and `apps/web/package.json` metadata. Potential nearer `package.json` paths are recorded absent and must stay absent during admission. Root must recheck source bytes, loader metadata and path identity before invocation. Direct Node loading does not use npm scripts, tsconfig, package-lock, `.nvmrc` or installed dependencies. The command uses the exact Node executable identity recorded by metadata, not a PATH-selected binary. OS executable/DLL loading is not measured by this source-only inspection; no claim of complete OS runtime isolation or DLL closure is made.

The runner explicitly reads only its three source files, the codec and those two package JSON files for source hashes, then only PNG and graph files created by its own current invocation. Their sizes and hashes must match before their content is accepted. Runtime imports never discover a fixture path. P1's only data input is this invocation's P0 PNG. No external PNG, PDF, native geometry, archived source/truth report or protected path is referenced by an application read call. `NODE_OPTIONS` and `NODE_PATH` must be absent or empty before launch; the in-program guard does not prevent a preload that ran before main, so source admission must check the launch environment separately.

Only a fresh exact `run-v2-initial-01` child can be created. All ordinary files use exclusive `wx` writes. Output is 8 each of `<case>.png`, `<case>.graph.json`, `<case>.overlay.png`, `<case>.check.json`, plus `sources.json`, `graph-controls.json`, `summary.json`, `manifest.json` and a possible `failure.json`. Only the 8 own PNGs and 8 own graph JSONs are reread. A failed partial run remains for root disposition; there is no unlink, recursive filesystem operation or cleanup outside that child. No subprocess, server, browser or GUI is launched, so no such task-owned resource was created in source preparation.

## Limits and remaining admission questions

The fixed union graph ceiling is 65,536 segments; all compact topologies together permit at most 1,024 chains. A segment row has 15 numeric values, so at most 983,040 such values are retained. There are at most 7 topology populations, each with at most 65,536 active support references and at most twice that many incident vertex entries. Each RGBA plane is exactly 1,353,600 bytes. JSON strings, codec buffers, JavaScript object/Map overhead and garbage-collector live retention are additional and have no independently proved resident-size bound here.

Exclusive output writes are checked against a cumulative 64 MiB byte budget, with 32 KiB reserved for a terminal failure record. Per-image generation, checking and applicable controls accumulate toward a 60-second work budget sampled at checkpoints. Other images' intervening work is not charged as that image's work. RSS is sampled and an observed excess over 256 MiB causes incomplete failure. These checks detect observed overruns; they do not enforce a true 256 MiB process cap or an external hard wall-time cap. No heap flag or sampled number is presented as that missing enforcement. Runtime admission must resolve that limit honestly before a run can meet the parent resource contract.

Static review covered the written source as text: fixed read/write roots, import edges, exact case constants, integer predicate ranges, raw identity preservation, topology traversal, evaluation barrier, explicit incomplete paths and absence of a solver/authority claim. Source inspection is not syntax, runtime, visual, support or memory verification. `executionReady` remains false pending independent source review, runtime-closure and resource admission, and the root's exact command approval. Continuous support and all later camera obligations remain required for this candidate's stronger eventual result but are not universal campaign requirements.

## Exact proposed command, not executed

Working directory: `%USERPROFILE%/Documents/github/lego`. Prelaunch checks must establish unchanged recorded source/metadata, no nearer package metadata, no injected loaders/preloads, approved runtime/resource handling and an absent output directory.

```powershell
& 'C:/Program Files/nodejs/node.exe' --experimental-strip-types 'output/first50-fresh-search-20260908/inverse-boundary-graph-01/run.mjs' --stage raw-support --case-set inverse-boundary-graph-synthetic-v2 --out 'output/first50-fresh-search-20260908/inverse-boundary-graph-01/run-v2-initial-01'
```

This initial-stage child is deliberately separate from the parent's future full-solver `run-v2-01`. Neither child exists from this assignment. The initial command is concrete for review, not admitted or proven runnable. No full project test, ordinary browser gate or native/protected-source operation is requested.
