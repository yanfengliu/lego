# Small native-RGBA ownership scope

Owner: fresh_vector_correspondence. Status: scope for review, not implementation or source-stage approval. Original proposal and algebra script remain frozen. This scope consumes review `vector-review-8b490f/REVIEW.md`, SHA-256 `1181f96a695fc3c16152d48b369e96332d6d87c64e67548f0e0a4172a9a3fa7b`. Current rules read: AGENTS SHA-256 `4e689a389ad2ebfaa01e4944b7d32ec17d9e284381a6b49bfb8fbd509e282e97`; local-rules SHA-256 `a1ad87cde370c430b8c42d2c92ad519896ad017e364b52b338fd70c723f84543`. No protected payload, repository import, test, browser, native/production process, product edit, pin edit, commit or push was performed. Only this fresh ignored directory is owned. The original named-remote/no-push campaign restriction remains binding.

Harness first: `deriveRealBuildPrefix50Step42SourceAxisDiagnostic` already receives the exact native RGBA and opaque geometry admission and runs the fixed dark-core/roster measurement. The existing `source-axis.test.ts:24-143` is source-bearing, including its beforeAll and raster helper; do not run it before source admission. Its import-closure check at 146 onward is useful source code but is not safely selected by running that whole file. No shipped RGBA-to-physical-owner verifier was found. The next source-free fixture must therefore exercise the new pure core in isolation, without importing the source-bearing wrapper or its fixture loader.

## Smallest proposed boundary

Keep the existing public inputs at `source-axis.ts:363-380`: `{ panelStep: 42, rgba: Uint8Array, sourceGeometryAdmission }`. Keep exact 720x470 byte length, pinned pixel digest and process-local real-verifier admission. The existing native check runs before internal control transforms. No URL, file path, PDF/operator/page input, caller-authored physical label, supplied survivor list, camera winner or new capability enters this function.

Add one internal pure helper, provisionally `extractStep42NativeOwnership`, with parameters copied/derived by that wrapper: native RGBA, exact four core component supports from the unchanged detector, exact physical stud feature descriptors from the already admitted catalog/member transform, and the existing independently supplied source-face binding. Production callers do not supply an abstract incidence graph. A separate pure graph solver may exist beneath the helper for falsification, but a graph-only fixture cannot validate RGBA extraction. No generic certificate service or authority mint is proposed.

Output is an internal discriminated result: `observed-and-unique`, `ambiguous`, `not-observable`, `resource-refused`, or `unsupported-observation-policy`. It includes actual pixel support and all-four associations only in the first case. Until the missing observation decisions below are reviewed, the helper may produce unlabeled boundary evidence and diagnostic candidates, but **must not produce `observed-and-unique`**. No search or selection authority is added.

## Physical labels and frames

Derive four labels from the admitted occurrence 276 / `builtin:plate-1x4`, not an image-space sorting guess: `276:<actual-top-stud-connector-id>:top-rim` and `276:<same-id>:base-rim`, for j=0,1,2,3. Each ID comes from the four actual top stud connectors selected by the existing normal/kind rule in `step42-admission.ts:73-76`. Canonically order the four descriptors by transformed document coordinates plus connector ID; enumerate every bijection to the four observed cores, rejecting those inconsistent with the existing signed row/6-4-2 evidence. Do not invent numerical connector IDs in the proposal.

The top/base objects must be actual geometric rim curves, not merely connector seats or ink centroids. `step42-admission.ts:62-124` currently exports member topology, dimensions and count, not those rim descriptors. The smallest proposed extension derives radius, top/base planes and their relation to each stud connector from the already-loaded exact catalog definition and binds that derivation to the same admission. `catalog/types.ts:535-536` demonstrates render-profile radius/height fields, not that this particular definition has a verified exact two-rim cylindrical profile. Implementation must inspect the exact definition and refuse unsupported shape semantics; collision radius and nominal connector defaults are not interchangeable with visible geometry. No external mesh/Builder/page-44 geometry read is added. If the needed profile cannot be derived from the currently admitted definition, this producer is unavailable for that profile; admitting more geometry would be a separate decision, outside this scope.

For a source-local rim point p, apply the admitted source-to-catalog rigid transform if the descriptor is source-local, then the admitted member catalog-to-document rigid transform: `pD = TD TC p`. If the descriptor is already catalog-local, do not apply TC a second time. Convert LDU document to Three with `C = diag(1,-1,1)/20`, as implemented at `rendering/coordinates.ts:18-20`; `pW = C pD`. Positive image x is right, positive image y is down. Project `q = B pW + t` through the existing no-roll convention.

For two corresponding stud rim features, divide their image displacement by their actual transformed physical separation in **Three units**, with sign derived from the transformed stud axis. Do the same for pitch. Top-minus-base is not automatically +document Y: local top has -Y normal, and the admitted orientation can rotate that axis. If the world-axis conversion yields `aY` per +Three-Y unit, persist `measuredPlusDocumentY = -aY/20` per +document-Y LDU, with unit/sign named. Analogously document X uses `aX/20`. Do not mix LDU pitch with Three-unit stud height.

`fullRankCameraSeedCommitment` would mean a camera identifiable within the existing no-roll physical camera family, conditional on the independent absolute source face/seed and its icon fold. It does not mean rank-three data: these four top/base pairs remain coplanar. Bind u and elevation sign from the existing face adapter (`panel-face.ts:148-160`) and its reviewed face provenance, not from a fitted sign. Retain a conditioning interval and refuse zero/near-singular cases rather than choose a branch numerically.

## Executable unlabeled RGBA extraction

This first stage can be specified without physical labels or new numerical image thresholds:

1. Reuse the existing exact dark-core detector and its threshold RGB<=35, 4-connectivity, ROI, component count/size and TLS/roster checks. Preserve its output unchanged. Core ink supports identify four search anchors only; their centroids are never geometric centers.
2. On the **entire original crop**, build a bitset with the same RGB<=35 predicate. This is auxiliary observation data, never a new target/exclusion mask. Keep every source pixel and original RGBA. No resampling, smoothing, morphological closing or ROI expansion of the qualified-core detector occurs.
3. For every dark/light cell adjacency, emit its exact grid boundary edge, endpoint lattice coordinates and the adjacent pixel IDs. Build the 4-neighbor cell-boundary graph and enumerate every boundary circuit/maximal chain; retain all topologically legal pairings at degree-four contacts. A border-reaching/open chain is explicit. These are boundaries of thresholded ink, not physical edges or vector curves.
4. Enumerate candidate rim-support arcs as every pair of endpoints on a boundary circuit and both directed subchains between them, plus complete circuits. Enumerate potential wall supports from boundary subchains incident on the same observed component or contacting candidate arc endpoint cells. Do not keep only the longest, smoothest, nearest, best fitting or camera-compatible chain before assignment enumeration. Store indices into the graph rather than copying pixels for each candidate.
5. Preserve every admissible chain splitting and junction pairing. Exact RGB region adjacency can be recorded, but matching fill colors does not merge physical owners and provides no score/tie-break authority. At this point the output is an executable **unlabeled ink-boundary extractor**, not physical ownership extraction.

Necessary next pixel-observable step, not yet supplied by current policy: converting an ink-boundary strip into an uncertainty set for a physical rim/tangent, and deciding when a observed junction establishes a continuous physical wall rather than an overlapping predecessor stroke. Grid adjacency alone cannot do either. Calling a graph node `directed T` in a fixture does not implement this step. A positive producer is not ready until the rule is concrete and independently tested against actual RGBA controls.

## Finite ownership population and work refusal

After an observation rule is reviewed, the internal solver enumerates, in canonical graph order, the Cartesian product of all four physical-to-core bijections (at most 24), every candidate top/base support pair incident on those anchors, every legal contour segmentation/junction pairing, and every legal visible surface ownership. Assignments are injective for distinct stud rims; shared base-plane surface support is permitted only with an explicit geometric incidence, not duplicated evidence. Generate them from graph data; never accept a producer list of survivors.

At every ambiguous crossing/contact, branch over both front/back continuations and both `same admitted member` versus `different/unresolved owner` interpretations when compatible with the reviewed observation grammar. This includes a competing predecessor interpretation even when it uses the same support pixels and produces the same camera. For each assignment solve/check the no-roll camera and known pitch/height/rim/tangent equations with outward-rounded observation intervals. Solving inside each ownership hypothesis is allowed. Render scores, nearest-fit ranking and semantic color are not used.

Reject an alternative only with an explicit contradictory observation/geometric constraint. If two ownership assignments remain, refuse even when their camera matrices/signs coincide. If a required physical pair is absent, refuse. If two different continuous camera intervals remain within one owner mapping, retain ambiguity unless the unchanged downstream frozen requirements can prove the entire retained interval acceptable without choosing a favorable point. No subpixel tolerance is invented to collapse it.

Proposed resource ceilings for the source-free increment: 338,400 pixels; at most 1,353,600 directed cell-boundary edges; 128 MiB total charged live storage; 100,000 candidate arcs; 100,000 full assignment leaves; 1,000,000 continuous interval boxes; 20,000,000 charged graph/constraint operations. Process mirrors sequentially and charge aggregate operations across native plus controls (four times that limit is not silently available). Charge before allocation/expansion. Exceeding any ceiling returns `resource-refused` with exact completed counts, never a truncated positive. These are provisional work limits, not qualification tolerances. The maximal-universe enumeration can exhaust them; that is an honest implementation limitation, not a proof the crop lacks information.

## Ownership disambiguation and required decisions

A physical wall association would need observed continuous tangent generators joining its top and base rims, plus an independently justified connection to the admitted member's visible face/outline; any crossing must have a decisive observed ownership continuation. A borrowed edge rejected only because it is lower, close, cyan/black, or connected in the bitmap has not been excluded. A directed T-junction can settle which contour continues in front **under a reviewed drawing/occlusion model**. It does not by itself establish a part ID or exclude a second owner with the same projected contours.

The existing policy `source-axis.ts:16-50` supplies dark/yellow segmentation, exact core count, row pitch checks, roster and all-four/mirror/erasure obligation. It does **not** supply: allowable ink thickness/offset from the physical curve; antialias coverage uncertainty; curve-fit residual or missing-arc allowance; endpoint/contact neighborhood; minimum visible tangent length; junction gap/continuation rule; or a completeness premise about visible inter-part seams/occluders. Pixel-cell quantization alone bounds thresholded-ink location, not physical rim location. Proposed extraction parameters must list each of these and whether it is exact, bounded from source-independent rendering controls, or unavailable. None may silently become a new calibration tolerance.

Smallest reviewable decision D1: approve an explicit conservative ink-to-geometry interval and junction observation rule as additional source-extraction semantics, while all existing qualification thresholds remain byte-identical, or keep the increment diagnostic-only. It must be derived and tested source-free before any real crop inspection/tuning. Its numeric values are deliberately not guessed here.

Smallest reviewable decision D2: establish what current admitted information can bind the visible member outline to occurrence 276 and exclude other-owner surface continuations. `step42-admission.ts:62-124` proves only the three-member topology/roster and four studs; it does not close the predecessor's visible contour-owner universe. An unrestricted unknown owner can reproduce an image-identical contour, so it cannot be eliminated by image connectivity alone. If the intended scope assumes complete visible seam/occlusion drawing semantics, that premise must be explicitly justified as an extraction predicate. If not justified, any affected pair stays ambiguous. This is the exact missing pixel-to-physical-owner step, not a claim that all raster approaches are impossible.

## Smallest decisive source-free fixture

First a synthetic RGBA fixture, not an array of labelled survivors: four dark rim islands with unequal synthetic stud height/pitch, connecting wall traces and one shared platform contour; at the fourth base trace create a contact where two surface continuations support different owners but imply the same camera. A generator paints unlabeled pixels and keeps scene ownership only as withheld test truth. The extractor must discover and enumerate both explanations and refuse all-four ownership. A graph-only version may first falsify the assignment solver, but its result is labelled graph-only and is not extractor verification.

Disambiguation companion: change only the junction pixels to create the exact visible termination/gap and continuous member-outline relation required by D1/D2; keep counts, camera and the other three pairs fixed. Acceptance is permitted only when the implemented RGBA observation rule actually extracts that discriminating fact. With the junction patch erased/reverted, ambiguity must return. If D2 cannot bind that fact to physical ownership, this companion has no honest positive expectation yet; do not encode one by attaching an `owner=276` label.

Controls all rerun the full primitive-generation/assignment pipeline: native; horizontal mirror `x'=719-x`; vertical `y'=469-y`; combined. Transform support indices and core-control ROI consistently and retain the exact pre-existing unsigned control outcomes (horizontal/combined may already refuse the fixed roster). Where measurement is admissible, enforce transformed signed-axis laws; do not turn an image reflection into a document reflection. The four physical pair conditions are unchanged.

Actual supported-wall erasure: derive the union of native pixel support that was necessary for the four observed lower-wall/base associations, keep top-core/rim support disjoint and byte-identical, and replace only that wall support with an explicitly declared non-ink control value. Rerun extraction from the mutated RGBA; require no all-four ownership result. If support overlaps required top evidence, the erasure control is unavailable and cannot pass. Store before/after support/byte digests and recomputed graphs. Deleting an association record is not the erasure test. Also remove one pair, swap a physical top/base interpretation, and impose the same-camera/different-owner case. None may produce positive ownership.

Only after independent scope/fixture review should the later admitted native-crop inventory test whether D1/D2 are observable in the unchanged crop. Source-free success predicts no 41/42 qualification result.

## Minimal positive consumer delta, if observation predicates are accepted

1. `source-axis.ts:410-446`: retain historical diagnostic/2 refusal representation. Implement an explicitly reviewed successor positive/refusal union with the bounded support/assignment/axis evidence above, using existing canonical digest utilities. `require...SignedSourceAxis` must recompute the claims and return the derived branch/camera evidence only for a genuine admitted input. No caller-authored success flag or new authority capability.
2. `source-vector.ts:157-203`: continue the same page-43-capped face fold and the existing Step-42 raster role. Join its face commitments to the axis result and return the actual checked branch. Add no page-44 operator, shape or panel geometry reader. Preserve `source-spec.ts:124-127` and `170-175` verbatim.
3. `preunlock-receipt.ts:48-56`: availability from a registered non-null expected branch remains only availability, not evidence. Bind/recheck the live signed result through the existing real-source receipt path before its expected branch reaches calibration. Source-spec nulls cannot be replaced by guessed constants.
4. `source-anchor.ts:10-71` and its caller `source.ts:317-326`: the old two lattice-RGBA input call has no physical-axis receipt. Supply the checked same-crop signed evidence alongside those same two existing inputs and derive one shared source-world law. Preserve both 41/42 corroboration, fixed thresholds and per-panel lattice counterevidence. Do not bypass the current anchor by handing its consumers a cast object.
5. Exact representational conflict: `lattice.ts:23-48` permits only two older anchor authority strings and requires a `RealBuildPrefix50Step44TypedLatticeFit`, `qualified:true`, `thresholdsChanged:false`. `persisted-case-contract.ts:177-187` requires the corresponding fit digest, qualified=true/failure=null, and the fixed expected branch. A camera axis solution alone does not satisfy those predicates. Smallest decision D3 is an explicitly reviewed signed-source anchor variant plus a deterministic adapter that **actually reruns every unchanged lattice-fit/corroboration predicate**; if those predicates cannot consume the signed camera without alteration, name the individual incompatible assertion and remain refused. Never fill them with success literals. The fixed expected branch is checked, not retuned to the recovered camera.
6. Persistence: embed bounded evidence inside the registered existing case/anchor representation or register a new exact role through its existing schema; never append undeclared files to a closed tree. Update exact parsers/replayers in `persisted-case.ts` and `persisted-case-contract.ts`, source case commitments in `source.ts`, and qualification replay in `persisted-qualification.ts` so both cases replay the same signed anchor and all support/assignment claims. Retain old schemas/counterevidence without relabeling.

Source-closure rebind inventory: changed/new pure ownership helper; `source-axis`, `step42-admission` if feature descriptors extend it, `source-vector`, `source-anchor`, `lattice`/typed adapter if D3 is approved, `preunlock-receipt`, `source`, `source-spec`, preregistration and the affected persisted-case/qualification parsers. Include transitive helpers used for coordinates and exact catalog feature extraction. Check actual import reachability before adding any member. `apps/web/test/real-build-prefix50-step44-source-reader-closure-manifest-a.ts:212,218,220` already lists anchor/spec/admission source digests; the existing source-axis import-closure test names axis/vector roots. Rebuild/review applicable exact manifests using the repository's existing closure workflow, not a hand-edited digest bless. Keep runtime/PDF/review-batch/source-geometry pins, source-transform fixtures, face inputs, source role exclusions, masks, crops and all threshold bodies unchanged. `source-spec.ts:283-326`, `preregistration.ts:86-94`, and source/persisted case bindings must reflect only the independently reviewed implementation/extraction successor. If their workflow requires anything broader, stop that dependent action for review.

No required downstream stage disappears: both 41/42 qualifications and sealing, the one predeclared 43 validation opportunity, genuine opaque 43-to-44, all 211/five-root production, legal exact 1-50/no51 compilation, UI/native visual/replay and complete gates remain required. This scope claims no positive producer, source admission, qualification or campaign progress.

## Handoff

Ready for scope criticism: exact existing input seam, executable unlabeled native-boundary extraction, finite exhaustive assignment construction with explicit refusal bounds, and concrete consumer/rebind inventory. Not ready for positive implementation: D1 physical-rim uncertainty/junction measurement, D2 observed-to-physical-owner binding, and D3 exact signed-anchor/lattice-fit representation need review. The smallest authorized next increment after scope review is the source-free RGBA ambiguity fixture plus diagnostic extractor; no protected inventory invocation is authorized by this document. Original investigation evidence remains needed and retained. No task-owned long-lived process exists to clean up.
