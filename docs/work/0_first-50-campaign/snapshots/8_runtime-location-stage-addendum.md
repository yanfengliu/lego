# Prospective runtime-location stage addendum

Status: authored proposal only; independent review pending. Owner: `/root/stage_requirement_audit_resume`. Assignment base: `6300c6b817722d2652924ba9df7e06264c135e31` plus the preserved dirty tree; HEAD was not re-attested. Current admission remains Stage 1 preparation. The runtime-root/`FONTCONFIG_PATH` change remains **UNAPPROVED**. No repin, implementation, source access or validation attempt is authorized or performed by this text.

## Decision text for owner review

> Replace only attempt03's blanket persisted-replay exclusion requiring a metadata-only lineage gate before source-geometry reproduction or every PDF/hash/crop read with the stage conditions below. This is a prospective ordering amendment requiring independent review. It does not itself approve the runtime-location exception or admit an invocation.
>
> After the existing Stage 0/1 requirements and explicit Stage 2 entry-point admission are satisfied, persisted qualification replay may establish genuine live inputs, reproduce admitted source geometry, verify the fixed whole-PDF identity, and reconstruct only the exact sealed Steps 41/42 calibration crops under the reviewed runtime before comparing their current calibration lineage. These operations remain subject to their existing guards and owned-output rules. Step 43 pixels and every other excluded source remain unavailable during this qualification work.
>
> Before `consumeRealBuildPrefix50Step44CalibrationSessionForHeldOut`, both Steps 41/42 must independently reproduce successful qualification. Bind their exact proofs and reproduced session to the selected candidate's complete, independently accepted code/source-extraction/camera/mask/policy/program/roster/runtime freeze. Predeclare the campaign attempt before launching a run that may validate; bind its Stage 2 freeze and establish genuine existing authority before protected access. No Step 43 observation may select or alter that freeze. Keep the session and crop one-shot guards intact. Complete Step 43-dependent validation and final publication verification after access; a later refusal cannot cure a missing admission prerequisite.
>
> Preserve all 178 ordered runtime path/length/digest rows and payload bytes, the same PDF, exact Steps 1–50 and no Step 51, thresholds, validators, scientific rules, 211 candidates, existing source guards and one-shot validation limit. Continued repair authorization supplies no extra validation opportunity. Any future approved location exception changes only the literal root, its derived `FONTCONFIG_PATH`, and necessary reviewed dependent commitments. Preserve predecessor evidence and every existing guard. Persisted replay remains unadmitted until the concrete obligations below are reviewed and satisfied.

## Why this narrower condition is justified

Attempt03 expressly withheld persisted replay at lines 58, 75, 102 and 114. Its acceptance in review 5 was under that disclosed exclusion, as reported in this assignment; this author has not reopened reviews 4/5 and does not invalidate that acceptance. This child proposal changes the proposed future admission condition and needs its own independent review. The original design and reviews remain unchanged. [Attempt03](%USERPROFILE%/Documents/github/lego/output/prospective-runtime-relocation-20260905-design/attempt03-design-correction/design.md:58)

The raw successor permits only admitted Steps 41/42 in Stage 2 and requires the complete freeze “Before any Step 43 validation-source read.” It does not require old-runtime rejection before every Stage 2 PDF identity check or calibration crop. Protocol acceptance still grants no source invocation. [Protocol lines 31–33](%USERPROFILE%/Documents/github/lego/docs/runbooks/first50-disclosed-verification.md:31), [lines 63–75](%USERPROFILE%/Documents/github/lego/docs/runbooks/first50-disclosed-verification.md:63)

The unchanged fixed-PDF lock projection cannot distinguish old and new runtimes: its public evidence is built from one PDF row, while the full current lock digest is separate in `runtimeIdentity`. The bounded calibration raster commitment includes the actual toolchain commitment. Thus, under the hypothetical authorized location change, an unchanged old calibration tree fails the current raster-lineage comparison at persisted-case lines 249–250 before the Step 43 consume at persisted-qualification line 426, even if the crop pixels remain identical. This is a static conditional result, not an executed test or a guarantee for arbitrary rewritten metadata. [PDF projection](%USERPROFILE%/Documents/github/lego/apps/web/e2e/real-build-prefix50-step44-camera-only-source-lock.ts:122), [raster lineage](%USERPROFILE%/Documents/github/lego/apps/web/e2e/real-build-prefix50-subbuild-return-review-camera-real-domain-calibration-raster-commitment.ts:70), [case comparison](%USERPROFILE%/Documents/github/lego/apps/web/e2e/real-build-prefix50-subbuild-return-review-camera-real-domain-persisted-case.ts:249), [consume boundary](%USERPROFILE%/Documents/github/lego/apps/web/e2e/real-build-prefix50-subbuild-return-review-camera-real-domain-persisted-qualification.ts:424)

## Unresolved implementation obligations

1. **Bind the exact full closure to the freeze.** The inspected persisted/session bodies carry the PDF projection and calibrated lineage; they do not retain the prior complete bootstrap roster digest. A valid current full lock proves current coverage, not equality to the frozen candidate's code closure. The smallest proposed connection is an independently reviewed campaign freeze/inventory that identifies the existing genuine bootstrap manifest and exact runtime body, binds them to the selected calibration receipts/proofs/session, and requires equality with the current genuine producer outputs before the consume. The implementation must specify and verify this join; a caller-shaped record, self-consistent digest or inventory alone is no capability. Prefer that existing producer/freeze path over a new authority scheme. [Current lock identity](%USERPROFILE%/Documents/github/lego/apps/web/e2e/real-build-prefix50-step44-camera-only-source-lock.ts:151), [session body](%USERPROFILE%/Documents/github/lego/apps/web/e2e/real-build-prefix50-subbuild-return-review-camera-real-domain-session.ts:180), [required inventory/freeze](%USERPROFILE%/Documents/github/lego/docs/runbooks/first50-disclosed-verification.md:33)

2. **Finish the calibration-side joins before relying on the persisted freeze.** Existing replay independently verifies both current calibration cases, then reproduces and compares the session. For this proposed admission path, compare those returned proofs with the manifest's ordered first two `caseProofCommitments` before the consume; that comparison currently occurs afterward. Keep genuine case-proof brands, exact batch/predecessor/source/observation bindings, thresholds and semantic replay. This is a concrete proposed way to establish the exact freeze, not a claim that the frozen user contract names this particular array check. [Replay and delayed joins](%USERPROFILE%/Documents/github/lego/apps/web/e2e/real-build-prefix50-subbuild-return-review-camera-real-domain-persisted-qualification.ts:404)

3. **Prove the boundary using source-free controls.** Pin old-runtime/current-PDF, current-runtime/old-calibration, mixed calibration proofs, changed full closure and caller-shaped opaque inputs. Show their qualifying prerequisites refuse before a Step 43 read/import sentinel and that a valid synthetic prerequisite chain reaches the sentinel only once. These controls must exercise the actual consumer seam; they prove only their stated synthetic bounds and cannot qualify real source.

Requiring every malformed final-result field to refuse before Step 43 would be a stronger optional guarantee. Held-out receipt self-consistency or publication-descriptor preflight may be useful refusal-only checks, but this proposal does not promote them into frozen requirements or use validation observations for selection. Genuine Step 43 semantic checks necessarily finish after access. Current ordering does not establish universal pre-read rejection of arbitrary mixed/caller-shaped trees. [Checks after consume](%USERPROFILE%/Documents/github/lego/apps/web/e2e/real-build-prefix50-subbuild-return-review-camera-real-domain-persisted-qualification.ts:426)

## Metadata and handoff

`identities.json` records the exact prior-design, contract and relevant source digests. The unchanged location proposal is inherited as metadata from attempt03: old toolchain commitment `sha256:ad3675e883db5966ee288583783841777cd63d908789ec4d2f77247f7433824f`; proposed commitment `sha256:f36d5a2829890b1d55fd74a48577260599569bcfb386a32383bd84da286aa7d6`; 178 files, 96,810,767 bytes, six directories. No runtime payload was reopened or readiness asserted.

Only this proposal and its metadata inventory were created under ignored `output/`. No production/protocol/docs-work/Git mutation, payload read, import, test, native command, browser, server or persistent process occurred. This is the same audit author's proposal, not fresh independent review. The next owner decision is whether to send this bounded ordering amendment for independent review; source admission and the unapproved location exception remain separate.
