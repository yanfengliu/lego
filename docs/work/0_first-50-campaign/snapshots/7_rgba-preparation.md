# Recovered RGBA experiment preparation

This is the same unfinished raster candidate, recovered from three surviving modules. Their original bytes remain under `recovered-before/`. No predecessor frame experiment is imported or rerun. All new files and execution evidence stay in this ignored root. No production file, pin, threshold, schema, source stage, authority or Git state changes.

Harness first: the source-axis wrapper is a source-bearing native reader and is excluded. This isolated harness exercises the new source-free helper; it does not exercise the wrapper, opaque geometry admission, exact source digest, yellow segmentation or 6/4/2 binding. The detector's ROI, dark threshold, connectivity, component sizes/count, pitch and deviation are copied from inspected pure source text without changing their values.

## Finite named family

`snapped-cylinder-schematic/1` is an engineering schematic with every top and base ellipse fully outlined. It uses exact grayscale tones, explicit wall tangent strokes, an asymmetric enclosing member silhouette, no hidden seams, and at most one observed straight seam joining a base apex to that silhouette. A complete such contact admits two physical continuation interpretations: the cylinder's own rim and a coincident platform rim at the contact. No contact supports one member continuation. A partially seen contact is unresolved. Other primitives or unmodelled contacts are outside this experiment. This is a stipulated synthetic drawing grammar, not a proved model of the unread booklet.

The extractor detects actual dark components, obtains top bounds from the full ink-plus-core component, checks every emitted shell pixel against the original RGBA, enumerates base offsets and both signs, requires both complete wall tangent supports, binds the row hand through the observed unequal member clearances, and enumerates all four pair assignments and contact interpretations. Centers come from full ellipse extents, never dark centroids. Dark centroids serve only the unchanged four-component row predicate.

The synthetic renderer snaps shared projected pitch, height, ellipse radii and origin to integer image coordinates before drawing. Physical parameters range over the inverse rounding cells. An ellipse shell consists of integer pixel centers inside the outer ellipse and outside the strict inner ellipse with both radii reduced by one. This gives a declared physical-curve parameter set, rather than assuming an arbitrary ink boundary is a physical rim. Unknown origin phase, radius rounding and layout rounding are bounded explicitly. The full camera region is retained under the exact positive quadratic relation; its adjacent parameter subdivisions merge only because they carry identical constraints and overlapping domain intervals. Its midpoint frame is diagnostic only. No production tolerance is applied.

Generator truth is in `generator.mjs`, imported only by the harness. The extractor cannot import generator, harness, files, network, providers, browser, runtime or source data. Shared `grammar.mjs` holds only fixed image dimensions, tone grammar, physical family constants and resource bounds, with no camera, centers or fixture identity. No exact-image digest lookup or supplied supports enter extraction.

The same-camera contact pair is changed by two visible seam pixels. Wall erasure overwrites the extracted necessary wall-support pixels in the actual RGBA, preserving disjoint top pixels, and reruns extraction. All mirrors rerun RGBA extraction with declared image-mirror metadata. A-B-C support segmentation is actually split and canonicalized by full support plus owner; different physical owners keep different keys. Always-refuse, ignore-contact and ignore-wall mutations are explicit falsification controls, not admitted producer paths.

## Run boundary

Exact invocation from the repository root, after coordinator admission: `node output/first50-fresh-search-20260905/raster-rgba-producer-2e8509/harness.mjs admitted-01`.

The coordinator executed `admitted-01` once: exit 1, 5/21 checks, 437.7394 ms; source identities matched and did not drift. All initial correspondence cases hit the auxiliary line-component cap, so that run established no positive or physical correspondence. Its source snapshot is `snapshot-admitted-01/`, its output is `run-admitted-01/`, and its summary digest is `a054f93e332fc62d1422baa0b743826fc380dfe759d27fe530461c65162dbd88`. The five skipped wall controls incorrectly disappeared from its denominator; an always-refuse mutation check incorrectly passed before its mutation ran. Both are retained as failed instrument evidence.

The prepared second invocation is `node output/first50-fresh-search-20260905/raster-rgba-producer-2e8509/harness.mjs admitted-02`. It adds an exact count-only four-versus-eight component census and uses eight-neighbor adjacency solely for the one-cell auxiliary line shells. The frozen dark-core detector still uses four-connectivity, and the 64-component extraction cap is unchanged. Whether that explains the initial resource refusal remains a measured question until run 2. The harness now has a fixed 30-check denominator, records unavailable wall controls as failed/not-run, and requires baseline positives plus evidence that each mutation reached its intended behavior. It also checks complete input-image B, translation and up-sign laws for all three declared mirrors; the canonical world stays unchanged.

The coordinator executed `admitted-02` once: exit 0, 30/30 checks, 479.8719 ms, with no source drift. The auxiliary line mask had 72 four-connected components and 12 eight-connected components over the same 1,116 foreground pixels. All four dark cores had 94 pixels; pitch was 39 with zero deviation. The run produced a four-pair positive, two same-camera contact owners, a one-owner patched positive, three declared mirror positives, actual all-wall and one-wall erasure refusals, and mutation controls that reached their target behavior. This validates those bounded instrument paths, not native extraction. Snapshot `snapshot-admitted-02/` and `run-admitted-02/` are preserved; summary SHA-256 is `58ebd71f3b1ebbe08b7a5f4f13132fc8c498d243e45ac7a680f91bfe56c63205`.

The round-2 physical-center uncertainty was wrong: independent rounding of origin and shared pitch allows error to accumulate with stud index. Origin 348.49 and pitch 39.49 yield physical last center 466.96 while the drawn center is 465. Independent center plus-or-minus 0.5 does not contain it. Both coordinator and independent critic found this defect. Round 2 is retained with this unresolved error and is not accepted as a correct physical-curve uncertainty proof.

Prepared third invocation: `node output/first50-fresh-search-20260905/raster-rgba-producer-2e8509/harness.mjs admitted-03`. It records shared origin, pitch, height and radii inverse cells with explicit center expressions, coupled to the same feasible camera region. Marginal center uncertainty becomes plus-or-minus `(0.5 + index*0.5)` in x and plus-or-minus 1 for top y or 0.5 for base y. Radius uncertainty adds another 0.5 to the all-phase coordinate error bound. A withheld noninteger physical scene renders identical RGBA while exposing the old error. Six fixed checks cover the same-image positive, all four true rim centers/radii, camera coupling, the old counterexample and an all-phase triangle-inequality curve bound. The fixed denominator is now 36. No execution is claimed for this preparation.

The coordinator executed `admitted-03` once: exit 0, 36/36 checks, 859.4726 ms, with stable source hashes. The true last center 466.96 lies in the revised `[463,467]` marginal interval, and the true top y lies in `[257,259]`. The continuous camera's physically coupled pitch, height and radii produce exactly the positive image bytes and are contained by the shared camera constraints. Snapshot `snapshot-admitted-03/` preserves all eight executed files plus this document's then-current bytes; `run-admitted-03/` is unchanged, with summary SHA-256 `682d94eea7e2a8f9fa43f96d8442a0e0edfc9d6ca05f0097908143beddebe30a`.

The final bounded commands were `node output/first50-fresh-search-20260905/raster-rgba-producer-2e8509/harness.mjs admitted-04` and `node output/first50-fresh-search-20260905/raster-rgba-producer-2e8509/mutant-old-bound-03/harness.mjs admitted-mutant-old-bound-03`. Run 4 adds the original scope's physical top/base-swap control: it exchanges the actual complete top/base elliptical RGBA patches found in the positive extraction, preserving all surrounding wall/platform bytes. It refused ownership with zero physical hypotheses while retaining four 94-pixel dark cores at pitch 39 and zero gap deviation. Run 4 exited 0 with 38/38 fixed checks over 14 cases in 661.7067 ms; its summary is `94b510bb76443ee367f0827fd877e88dfa4f23c7f9e39dd33dd08822779794c0`.

The isolated mutant uses the exact run-3 harness and seven other run-3 files, with only `rimUncertainty` restored to the actual run-2 function. It exited 1 with 33/36 checks in 656.5505 ms. Exactly three checks failed: `continuous-all-four-correlated-centers-and-radii`, `old-independent-half-pixel-center-bound-falsified`, and `continuous-full-curve-all-angles-bound`. The first failure is an actual center containment failure, so this is not merely a mismatch with the old pointwise-field shape. Mutant summary SHA-256 is `4580543b25232fc162160cfea40060f4e3af8fe71c7597cf2fa9dce3bf9bf541`. Both closures stayed byte-stable before/after execution and used the same eight local module names and six builtins. No previous source or output was overwritten.

All executions and native-resolution image views were performed by the persistent implementer task `01a073ae` / coordinator `root`, not the chief task `01a07391`. This recovery worker prepared source and retained evidence; it did not execute the harness or launch a visual application. Root reported digest-bound inspection of each unique image, including the final swapped image `c88a4f2e05460de65eede359ae5bb87c50cdbf1231fc065999aba11750150419`. Those observations are visual evidence for the synthetic pictures only. The independent review remains a separate report and does not derive from these author assertions.

Local import closure: `harness.mjs`, `generator.mjs`, `extractor.mjs`, `intervals.mjs`, `pixels.mjs`, `frame.mjs`, `grammar.mjs`, `png.mjs`. Builtins: `node:fs`, `node:url`, `node:path`, `node:crypto`, `node:perf_hooks`, `node:zlib`. The harness reads only these eight source files for identities and creates one fresh child run folder. It writes synthetic PNGs, extracted support/interval JSON, erasure evidence and a summary there. It cannot overwrite an existing run ID. No child process, GUI, browser, server or network is launched.

Bounds: 720x470 raster arrays, at most 64 extraction components, at most 64 base offsets and eight base candidates per cap, one seam contact, at most 4,096 assignment leaves, at most 32 million charged operations per extraction. The count-only connectivity census does not relax the extraction cap. Fixed loops and raster allocations are finite; no production memory/OS-isolation guarantee is claimed. Each module is below 500 lines. This record includes preparation and attributed execution history; independent review and native source admission remain separate boundaries. All prior source snapshots and runs remain needed counterevidence and handoff material.
