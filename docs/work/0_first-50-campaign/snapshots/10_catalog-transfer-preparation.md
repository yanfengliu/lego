# Frozen extractor transfer: source-only preparation

Owner: `/root/rgba_recovery`. This is an ordinary continuation of the same bounded candidate. No image payload, old camera/semantic truth report, old detector, old Python harness, browser, runtime or source-bearing probe has been read or executed for this preparation. Existing first experiment source snapshots, runs and mutants remain unchanged. This folder contains only source and metadata preparation; its command is not yet admitted.

## Smallest meaningful measurement

Run the unchanged final extractor implementation from `snapshot-admitted-04` against every one of the sixteen retained original catalog captures in the predeclared roster. These are already 720x470; there is no crop, padding, rescale, mask adjustment, color conversion beyond the existing lossless PNG decoder, or ROI change. Ignore the 96 old generated controls for this first transfer measurement. The entire original roster is fixed before viewing any picture; no case is selected by the new estimator's result.

The estimator receives only decoded RGBA and the same public metadata `{mirrorX:false,mirrorY:false,upSign:1}`. It never receives a filename, old label, camera pose, stud support, or winner. Source inventory found no capture mirror/up-sign override, and the current renderer's omitted up sign defaults to +1. The exact historical compiled bundle was not reclosed against current source; this public metadata statement is the declared capture-family convention, not newly proved historical renderer equivalence.

Persist all sixteen actual outputs before reading the separate held label file. Then report observed-and-unique, ambiguous, not-observable and resource-refused counts separately for the twelve recorded positives and four original negatives. No old camera truth is read in this first applicability check, so there is no frame-correctness score or camera qualification claim. A positive would require later independent correspondence validation. Zero positives would measure failure of the current finite grammar to transfer to this roster; it would not prove all raster methods impossible.

Independent descriptive measurements count exact synthetic-tone values, opacity, non-gray pixels, full/ROI dark pixels, the frozen ROI's four-connected component size/bounds, and qualified dark-component count under the unchanged numerical predicates. These measurements never enter the estimator. They explain whether refusal occurs before a physical support could be attempted. Exit 0 means all sixteen identities decoded and were measured; it never means the transfer succeeded.

## Frozen lawful inputs and provenance

Retained input root: `output/first50-campaign-control-20260905/pixel-correspondence/prepared-inputs/`. Safe manifest SHA-256: `c05f8827076c1973e49d9827fce1c12edad2bd50ecd526b79318b22723da91ca`. The original experiment's metadata freeze is `84269a286e6b60af387e515c46c69db9a324bb2ead2b2c033be1192f5f7a655c`. Inventory verified source hashes for capture `5eb2a948e856a15c434ef1a867e84db50b89f8d9a7a56e3836f306a3c4b916bd` and plan `a45cd5f37aa513f50f63e2c7ebf76b80323f6688f6b3f9ad53cf059b9be4adb6` against the retained pre-capture identity. The capture source imports one synthetic catalog document through the real UI and calls `capture_instruction_view`; canonical PNG encoding preserves pixel order. Its retained authored visual-review digest is `c4d3c9d41e5423478bc1f911d1e9c4bb080719b90e126638c25dfe4ade8f2577`, reported by the independent inventory worker; that worker checked all sixteen image pins. No new historical compiled-renderer identity claim follows.

Exact ordered originals: `a`, `a-20`, `a-30`, `b`, `b-20`, `b-30`, `c`, `c-20`, `c-30`, `d`, `d-20`, `d-30`, `absent`, `rectangle`, `extra`, `missing`. `inputs.json` contains all sixteen encoded PNG and decoded RGBA digests copied from the verified safe manifest, plus metadata-only file sizes. Its digest is `7276d10ddfce2874eb7cd87c9543954569c396398898bae67490bbcfafa86f6b`; `held-labels.json` is `baf6dfc87b8f6c01ead9eb8b7ae0ca22e204695c275623ab005eefe7acc4b31d`. The reader checks both plans' exact bytes, the original manifest digest, each PNG size/digest, exact decoded dimensions/length and each decoded RGBA digest. It reads neither the old report nor the source plan's numeric cameras.

## Exact command, imports and reads/writes

Proposed command from repository root: `node --experimental-strip-types output/first50-fresh-search-20260908/rgba-catalog-transfer-01/transfer.mjs admitted-01`.

Complete executable import closure: `transfer.mjs`, `observations.mjs`, `png-codec.ts`, and `frozen/{extractor,frame,grammar,intervals,pixels}.mjs`. The five frozen modules are byte-exact copies of the executed `snapshot-admitted-04` modules. The codec is a byte-exact copy of existing `apps/web/e2e/real-build-prefix50-subbuild-return-review-png.ts`, SHA-256 `21fb281b7de222c9452a46c0731ef93400ba9ad68e0e24c094aa940345b3fa5c`; its only imports are `node:buffer` and `node:zlib`. Other builtins are `node:fs`, `node:url`, `node:path`, `node:crypto`, and `node:perf_hooks`. There are no package imports or child/browser/server/network operations.

Runtime read closure: those eight executable source files for identity recording; local `inputs.json`; the exact safe original `manifest.json`; sixteen named original PNGs under the retained input root; then local `held-labels.json` only after all measurements are persisted. No image is read during preparation. The reader rejects nonregular/symlinked or unexpectedly resolved image files, caps and checks held-handle file bytes, and closes each file descriptor in `finally`.

Write closure: one fresh `run-admitted-01/` child in this folder, with sixteen `case-NN.json` records, `measurement-summary.json`, and `held-label-summary.json`; an instrument failure instead retains `instrument-failure.json` with its completed-case count. Existing run IDs refuse reuse. No original PNG, tracked file, source snapshot or old report is changed.

Resources: exactly sixteen sequential 720x470 inputs; 1,353,600 decoded RGBA bytes each; at most 1 MiB encoded PNG read per input (the known files are 9,524–11,163 bytes); the existing decoder's 338,400-pixel output bound; unchanged 32-million-operation extraction budget and finite assignment cap per image. The independent descriptive scan has its own same-size bounded work counter and does not relax the extractor's 64-component cap. There is no new renderer or fitting mechanism. Parent admission of this exact closure and lawful input identity is required before payload reads or execution.
