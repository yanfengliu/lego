# Brick modeling studio and instruction-booklet build loop — product and architecture specification

Date: 2026-07-09

Revised 2026-08-26: current implementation includes catalog `/26` with measured `78329` admitted from its exact official LDraw surface and exact LDCad five-stud/five-clutch composition; `/25` `33909` and the earlier `/24` through `/18` visual tranches remain immutable historical evidence. The `78329` admission uses the exact `p/stud.dat`, `p/stud3.dat`, and `parts/78329.dat` route; ordinary collision keeps the exact source radius while only an exact validated stud/clutch edge may use the nominal 6 LDU source-rounding profile. The measured catalog intersection is 20 of 121 required leaf designs at `sha256:4e5df0a3aa5723b5aa5abef96e705562913c560a8a7452a2cf29582c2f05ba8f`; this is catalog coverage, not a verified booklet prefix, because all seven retained `78329` locators at steps 265, 268, 271, 274, 277, 282, and 336 are stale and self-contradictory. The retained action ledger still ends at printed step 25, and the step-26 `28802` versus Builder `10201` contradiction remains unresolved. The production farther-step attempt uses the fixed-8,192 refundable subject-raster ledger and semantics-preserving 24-bit depth route; the retained historical `/15` denominator reproduced all 14,172 baseline rows and 17 leaves with 6,559 subject rasters. The focused Gate-3 compatibility control was freshly rebound to `/26` through all four immutable parents without rerunning that denominator, advancing scores, admitting a frontier, or publishing a document. Standalone authority-absent prerequisites include parent-bound exact lineages, multi-root atomic compilation, compiled-observation replay, dense branch-role transport, streamed 359-panel source evidence, D4 camera evidence, zero-piece transitions, the fixed-five source-parity request seam, and a bounded canonical Ed25519 broker-receipt verifier whose output explicitly has authority absent. The real 359-step source-parity diagnostic remains calibration-only under measurement digest `sha256:39a810d8a30f28fb93bb619f2ce522ce01e95146e8fe14666d6606270c0cb615`. No authentic manifest-v6 `/5` identification answers exist. A separate module-owned diagnostic authenticates the unchanged full `/5`-to-`/6` 881-row source roster and exactly bridges 73 positive first-50 truth relations / 113 pieces through identical PNG bytes or strict background-only bottom recuts; by itself it creates no semantic identity. Its exact official-cut consumer now publishes semantic element identity for only 70 compatible listed relations / 107 pieces in a 42,105-byte artifact at `sha256:e92ef982f9039b7fd94fb2cdca23fa5e56fb34fb6820ef4fa7ee9b999a0a63ea`, while four incompatible or changed claims remain quarantined and physical Brick assignment, catalog, coverage, frame, placement, acceptance, and completion authority remain absent. Current coverage remains incomplete, action-ledger publication still refuses, no live browser-output `/4` producer exists, and public browser-output `/3` remains the exact eight-root step-1 refusal when current input closure reaches it. Superseded implementation history lives in the devlog.

A private authority-absent step-1/panel-2 diagnostic now reuses one prepared child scene across its eight D4 camera renders and disposes it before preparing another child. A real 320 × 192 WebGL control reproduced all eight ordered binary-mask digests against an independent direct-render path, reducing measured scene derives and disposals from eight to one while leaving eight WebGL renders unchanged; a two-child control pins peak live prepared scenes at one. Immutable factory raster dimensions, frame dimensions and panel number are checked against detached source metadata before work, and cleanup failure rejects the diagnostic. The source descriptor does not inspectably bind fitted view, frame target/radius or centre, so this is not source-authenticated or proper-frame evidence and cannot authorize placement, document mutation or completion.

The exact-three source packet binds placement steps `[1,2,3]` to registration panels `[2,3,4]` on page 11 without weakening the full 359-label source/index contract or the four-panel page cut. Digest-bound high RGBA, work RGBA, and packed-mask roles are independently replayed by the reader; the genuine capture retained 20,009 manifest bytes plus 8,592,000/2,150,000/738,816 role bytes under manifest `sha256:6b09685905bab077c254adf1e8b32ee2eebd7eefac76d04da678ff47a77c3138`. This is current source imagery only: source execution, prepared-run, physical-frame, placement, accepted-document, and completion authority are absent, and no current authenticated placement exists for steps 1–3.

The proper-C4 quotient and render reduction are now an executable authority-absent sidecar over the current `/26` step-1 roster. The quotient proves 400 raw candidates form exactly 100 four-member proper-yaw orbits and retains every representative plus all 400 inverse mappings. Twenty deterministic closures of five representatives share cumulative fixed-8,192 search and camera ledgers, compile 100 unique physical transitions, execute 800 physical renders for the 800 representative/camera score rows, and retain the projected 6,400 logical camera branches. Before inverse expansion or global ranking, a separate verifier reconstructs the other 300 exact raw members and runs them in 60 five-member closures across eight cameras: all 2,400 verification renders share their own fixed-8,192 ledger, each closure passes the same role-byte and pixel-visit cap, and every packed mask digest must equal the mapped mask from the actual 800-row reduction. This exhaustive current-population proof establishes identical scores and the exact zero-margin global tie while retaining its per-invocation 2,400-render verification work separately from the 800-render algorithm; no batch-local winner, accepted transition, or document survives. A repository-owned asymmetric four-member control still calibrates the bound instrument across 32 real WebGL renders before work, but it is calibration only; the exhaustive member comparison, not that sample, supports semantics preservation for this exact current factory/configuration/source execution. The genuine run retained integration `sha256:4087bd1e7ce0be01549dfceb72b26b9deaa7eeee8bf32c700aa00c71e57614bc`; it captured and visually inspected all 100 current representatives at each of the eight D4 cameras and again at camera zero under a separate 20-pixels-per-unit wider-scale control. Those nine sheets were complete, nonempty, bounded, and unclipped, while source-execution, prepared-run, physical-frame, placement, and completion authority remain absent and no arbitrary-backend theorem is claimed.

The atomic compiled-branch prerequisite admits a bounded ordered set of distinct canonical root documents, keeps exact-byte-convergent identities in one root group, binds each exact parent byte string into its lineage, proves a worst bounded terminal envelope fits, and reserves the aggregate frontier before compilation. Its deterministic branch-role finalizer rejects row and observation Proxies, preflights aggregate byte and replay-work ceilings, rechecks lineage and closure digests, verifies optional observations, writes unexposed dense storage, self-parses the finished roles, and returns fresh copies with authority absent. It checks only a terminal-respecting strictly increasing placement-step sequence; because it has no prepared-run input it cannot classify skipped panels, and the complete detached `/4` reader is responsible for requiring every skipped prepared panel to be an evidenced transition. Together these seams provide a standalone `/4` report envelope with local source/camera and terminal continuity, but no live producer, authenticated source provenance, capability issuer, accepted document, or document mutation.

The 14,172-row reproduction remains the retained historical `/15` long-browser denominator; catalog `/26` repins the focused production compatibility boundary, but no post-`/15` long rerun or score rebind is claimed.

## Decision summary

This repository is scoped to two surfaces and nothing else; their current implementation status is stated below.

1. A precise manual brick editor.
2. A closed loop that reads a printed LEGO instruction booklet and assembles the set it describes — counting every part, compiling the printed steps into a build program, placing each piece, and verifying each step against the booklet's own printed panel before playing the result back.

Do not merge this with `3d-maker`. `3d-maker`'s source of truth is a compact genome whose output is a disposable mesh, and it can safely jitter numeric parameters; this product's source of truth is an editable assembly graph of real parts at real seats, where arbitrary mutation just makes collisions. Co-evolution means compatible experiment envelopes, not merged repositories — a shared package is justified only after both projects independently implement the same behavior.

The canonical source of truth is a versioned part-and-connection graph. Three.js scenes, renders, LDraw files, GLB files, printed-panel rasters, and model responses are derived artifacts.

The load-bearing deterministic compilation invariant is:

> Base-document structural hash + exact normalized build-program bytes + compiler, schema, catalog, template, transform-policy, connector-taxonomy, collision-model, and validator snapshots produce the same document structural hash and validation report.

Run replay is a separate, weaker contract, because a run may call a model and a model may be nondeterministic. Under the target replay contract, a `full` replay uses captured model outputs, a `downstream-only` replay begins at the earliest retained output, and a `metadata-only` record is not replayable. Compilation, validation, render configuration and operation application remain deterministic at every replay level; current tooling performs data-only closure inspection rather than executable replay.

A model may propose data. It may not author executable code in the build loop, silently mutate the user's document, waive a deterministic validator failure, or rewrite the running application.

## Status and how to read this specification

This document contains both implemented behavior and target product contracts. Durable domain, validation, authority, consent, provenance and untrusted-input rules bind now. Product promises, end-state workflows, production topology, complete user experience, delivery exits and the success criterion describe the target unless a section explicitly says that it is current.

As of 2026-08-24, the current product is an offline-capable manual React and Three.js editor with integrity-checked IndexedDB projects, a searchable catalog, snapped placement and attachment, precise transform and color editing, deletion, undo and redo, live structural validation, manual build playback, and bounded LDraw import and export. The deterministic document, command, compiler, validator, catalog and rendering packages are implemented. Builtin catalog `/26` has 98 definitions: 61 render parametric recipes and 37 render bundled LDraw surfaces with source-faithful normals. Sixteen are render-only promotions that preserve their preceding connector, allowance, and conservative collision declarations; the other twenty-one are fully measured definitions, and 77 definitions retain project-authored physical recipes.

Current `/26` truth is overall `sha256:3226590b11882fea03d8a6370d4ca3c6c8201feaddb56882a243a69acba627e9`: catalog `sha256:f86310b89f3224cff7a8d571de5a26fd36440ab46235abf1cf530e2f65f41b37`, connector taxonomy `sha256:93f0a5fc899083be25c5364266e7046b397683204e0e0991f106425ec5a99059`, collision model `sha256:7e9905d9f988c288eaeddee3d7befb7af79266518612bbba171d9b7f7fb1c463`, transform policy `sha256:a8694ddcdc39da5afd946a6012ac2588233bebe2eed457e8501cf572661b2956`, and unchanged validator set `sha256:fb0676931eb66a0096f393794d0be1297227811a77b986c0a1d05847ee3127d4`. The migration-history gate reconstructs 27 reviewed source truths and verifies 2,311 current endpoints at `sha256:ff3ec2777e568cdab960bea84bdf54dd6aafb26497c262ebdfa42487312c3f93`, three reachable pairs at unchanged `sha256:7431a242907aa9829ead6a279d0b530fe5f5d00ee31e6ddc1576fe66a8a07add`, 22 endpoint-delta rows, and zero pair-delta rows. Historical `/25` remains pinned separately rather than being relabeled as current evidence.

The booklet loop currently runs through internal Node, Python, Vitest and Playwright tooling. The web app's Instructions control ingests a PDF locally into bounded page and text metadata plus a content hash, but does not render panels, compile build steps, verify placements or apply a run to a user document. `building-system.md` owns the exact measured reconstruction frontier and the ordered work still missing.

Current retained-run infrastructure is local and development-only: `apps/companion` is a library and test slice for artifact storage, a test run ledger and a test recorder, while ignored run evidence lives under `var/` and `output/`. There is no released loopback broker, credential proxy, production signing identity, unprivileged worker, production authority namespace or executable sealed replay path.

Current model-assistance code consists of one bounded opt-in strict `/5` local pilot transport for exactly six retained part-identification cards, one opt-in legacy script that describes a cropped printed-step placement, and a quarantined N/N+1/conditional-K checker contract and one-tool subscription-CLI adapter exercised only by mocked tests. The former part-identification `/4` transport was retired after a live child created local files despite its nominal `Read` allowance; 73 current-input replies captured before termination remain quarantined and cannot resume under `/5`. The replacement exposes exactly one no-argument MCP image tool, validates a sanitized five-event stream, and binds exact card bytes, answer lines, call proofs and content-addressed checkpoint predecessors. Its development-only Gate 0 path now retains the exact request and configured-fetch policy bodies, creates a short-lived trusted-local-caller assertion that explicitly authenticates neither a user event nor identity, fixes its state namespace to the real checkout, atomically consumes one global pilot slot before any executable invocation, reserves one local exact-binary version preflight plus one model-bearing launch, and settles success or bounded failure against the same immutable lineage. The native path checks the pinned CLI version in an empty task root after the durable claim, writes only the request and MCP config afterward, and revalidates the global slot immediately before the model-bearing child. This is not a broker-backed consent or production-authority path: configured policy retrieval trusts pre-import process state, retained policy URLs and content digests are not authenticated by the record, provider execution remains unauthenticated, and the static local assertion is self-mintable by same-process code. No `/5` model-bearing call or trusted `/5` answer has yet been retained; the separate explicitly enabled real `--version` probe measures executable identity and version only, not absence of network or provider activity. The older panel script accepts one arbitrary resolved local file path without containment, file-type, dimension, PDF, page, or crop-bounds validation and exposes an authenticated temporary copy of those bytes to the CLI with piece text and a fixed relation prompt. It supplies no candidate render, later panel, alternate view, deterministic face-parity record, or source provenance, and its record keeps digests and the raw reply but not the exact input bytes. The quarantined checker binds and validates exact source/render bytes, face and candidate lineage, hard budgets, strict same/different/unjudgeable refusal semantics, and an exact MCP tool result without granting repository `Read`, but no producer proves PDF/crop correspondence or consent, no driver consumes it, and no successful live verdict has been retained. These ignored outputs are not called by the studio or wired into the real-booklet driver; candidate panel agreement in that driver is scored deterministically from renders and pixels. The prepared transition classifier is raster-blind, blind same-or-different verdicts are retained evidence rather than an integrated checker, and the driver remains linear instead of using the implemented backtracking library. Model output remains untrusted under every current and target path.

The deterministic real-booklet driver now integrates a bounded branch-aware farther-panel path for an ambiguous step N: it retains every origin with its cached exact N+1 score render, carries one intervening step under shared candidate and narrowing ledgers, and scores conditional K only after the carry completes atomically. Hash, piece-witness, lineage, report, parser and artifact bindings prevent one branch or image from being relabeled as another. A family decision may reject an origin family but cannot falsely settle an ambiguous descendant, and a typed budget or observability refusal leaves all completed alternatives immutable without changing the document. A separate calibrated shortcut applies only when the exact retained step-5 origin IDs and piece witnesses, prepared step-5/6/7 data, run options, input digests, and captured source closure match their reviewed attestation; it scores those two origins directly against panel 7 after panel 6 remains ambiguous. Generic carry still earns K only after its intervening step completes, and neither path is generic deep backtracking or an integration of the model checker.

The live browser boundary deep-detaches the complete JSON-like run input before asynchronous preparation, including panels, order and page bindings, digests, accounting, coverage, and budgets, then re-snapshots the caller input after module and PDF preparation and refuses any drift before raster, search, or placement. It globally sorts the detached panels by printed step, asserts exact contiguous execution before each page raster, and renders booklet pages on demand. Page-render failure retains one row for every requested step, the exact preceding document, and causal blocking; module, page, PDF-document, or loading-task failures use bounded non-invoking diagnostics, and cleanup failure produces typed failed output without erasing already completed reports, document bytes, or input digests. Browser-output `/3` bounds the terminal `documentJson` before parsing it once, derives only the requested transition-prefix witnesses, exact-binds every report action and action-evidence digest to its prepared panel, and then binds the terminal hash, part count, global validity, ordered accepted `BuildStep` IDs, indexes, semantic names and owned-part counts, plus canonical document identity, root ownership, metadata and manual provenance. A genuine validated zero-piece transition advances the accepted hash only through one unique empty canonical `BuildStep` whose independently reconstructed target prefix changes the hash without changing the part count; a forged hash-preserving no-op is rejected. A root rendering refusal may retain bounded elapsed time but no successful raster or candidate work, while every later blocked row must retain exact zero-work causality, including zero elapsed work, with no validation, candidate, raster, render or document drift; a genuine camera-handedness refusal alone may retain the panel evidence it measured before refusing. These execution guarantees do not make the still-unthreaded panel-camera frontier authoritative.

The separate source-raster diagnostic distinguishes H, the high-resolution cleaned-art mask; P, production assembly obtained by isolating at high resolution and then downsampling; D, the counterfactual obtained by downsampling before isolation; and W, the work-raster candidate. Its exact-byte stage trace makes H, P, D, intermediate masks, topology, and bounded component summaries inspectable while labeling source descriptors opaque and non-reproducing. The opt-in source-parity `/4` path then compares production and W over assembly, own-panel source, own-panel exclusion, built, and exclusion classes for every prepared step, retaining exact packed production/candidate/XOR evidence under the bound execution closure. The 2026-08-22 real-booklet execution retained all 359 rows under measurement digest `sha256:39a810d8a30f28fb93bb619f2ce522ce01e95146e8fe14666d6606270c0cb615`; none of these measurements establishes source truth, physical truth, or browser-output authority.

The exact-five calibration path binds only steps/pages 90/79, 101/87, 346/213, 358/218, and 359/219 to a contract derived from all 359 prepared panels. Its browser-only constructor retains exact high/work RGBA, H/P/D stage bytes, W, pairwise P/D/W facts, and ten lossless PNGs in five bounded external roles; the Node parser reconstructs the transient wire, decodes every PNG to the retained RGBA, replays H/P/D, and independently re-derives W from the work raster and prepared crop. The source-bound publication wrapper accepts only a reproduced capture plus the exact full 359-row manifest and held source closure, cross-binds the PDF, subset contract, browser-result bytes, source snapshot, provenance, served source, checkout, and runtime facts, then derives its execution identity internally as audit data. It writes the capture manifest, full manifest, five roles, ten PNGs, and provenance into a content-addressed run tree and atomically replaces the discoverable summary last. Authority remains explicitly absent and review state pending-unreviewed; the separate review parser still labels human review as an unverified claim, and adjudication remains `needs-adjudication` for missing truth, drift, W mismatch, and exact W equality alike because no human-owned review issuer or released broker path can mint the module-owned admission capability. The real-booklet exact-five opt-in has now published those fixed rows under audit-only execution identity `sha256:979157ed12ef36fcd59a85aba13268d56a47d77ee886070cd31221805d756462`; its summary remains `pending-unreviewed` with authority absent, and the publication cannot admit or launch the full 359-step source-parity probe.

A structurally valid searched document is not automatically the official target. After finalization binds the retained document, reports, exact identity metadata, searched transforms, build sequence, canonical step ownership, and structural validation, the deterministic target-equivalence audit exhausts the four proper upright yaws with one global integer-LDU translation. It matches same-step identities as exact design/material/catalog/color groups, compares complete connector, collision, allowance, bounds, and flat render triangle-and-normal realization modulo catalog-proved upright self-symmetry, and accepts only a proper frame that realizes every placement. Inputs are bounded to 1..1,464 unique identity/part rows with safe-integer upright transforms, and a per-audit catalog-definition and relative-transform cache prevents repeated realization work. Official transforms enter only this post-search evaluator, never enumeration or scoring. A D4 reflection may be retained as a physical-layer and inferred-contact diagnostic, but it is never accepted as a world frame or completion. When no proper frame survives, the exact candidate bytes remain only a `diagnosticPrefix` with `targetEquivalence: "unreconciled"`; canonical `documentJson`, `structuralHash`, and `finalParts` remain null, null, and zero.

## Target product promise

The user can:

- Build accurately with a curated catalog of brick parts, with every illegal placement refused at the command rather than flagged afterwards.
- Hand the app a printed instruction booklet and have it read the set's inventory, printed steps, callouts, and panels.
- Watch it build that set step by step, each placement checked against the booklet's own drawing before the build moves on.
- See exactly why a step was refused, in the booklet's own terms — which panel, which highlight, which arrow, which candidate, and by how much it missed.
- Edit the resulting model with the same commands used for manual work.
- Export to LDraw-compatible tools for further editing, rendering, instructions, or sourcing.
- Replay any booklet run to the level permitted by its retained artifacts and consent policy.
- See whether a claim is known, inferred, advisory, unverified, or physically verified.

## Goals

1. Read a printed booklet accurately enough that the booklet's own internal checks — step numbering, callout counts, inventory totals — reconcile.
2. Place every piece the booklet places, in the frame the booklet draws, and prove it against the printed panel rather than against a hash.
3. Make an illegal placement unreachable through the editor, not merely detectable after the fact.
4. Keep every step's verdict measurable and reproducible, so a change with no number attached is visibly not progress.
5. Recover from a wrong placement by backtracking, rather than by trying to be right the first time.
6. Remain useful as an offline manual editor with no network and no model available.
7. Preserve interchange with the LDraw ecosystem without making raw LDraw text the internal authoring model.

## Non-goals

- Rebuilding every BrickLink Studio feature, or full official-part coverage.
- Claiming physical stability, clutch strength, or instruction accessibility from geometry alone.
- Photorealistic path tracing, marketplace transactions, or real-time collaboration.
- Generating an original model from a text brief, or any copilot that proposes designs the user did not draw or print.
- Autonomous production deployment or self-approval of application code.
- Training a foundation model on anything here.

## Current and target workflows

### Current manual editor

The current UI searches a part palette, previews placement as a ghost, places or attaches a catalog part, moves and rotates a selected part, edits its transform and color, deletes it, and applies undo and redo through bounded command history. Explicit viewport placements create manual playback steps; connected transforms detach explicitly rather than silently preserving an invalid edge.

The current interface has single-part selection and does not expose copy, grouping, submodel creation or direct step assignment as user commands. The target authoring workflow adds those operations, explicit attach and detach controls, scene/submodel/step organization, and collision, disconnected-component and exposed-port overlays while preserving the rule that an illegal placement is refused by the command that would create it and the refusal names the observed values.

### Target: reading a booklet

The reader derives the set inventory, the printed step sequence, each step's callouts and quantities, each step's panel raster, and the printed annotations on that panel — highlight regions, displacement arrows, and the rotation icon that says which face the panel is drawn from.

The booklet checks itself, and those checks are the loop's early measurable: step numbers run 1..N with no gaps, callout quantities reconcile against the back-matter inventory, and a parts-bin quantity is distinguished from a repeat multiplier by printed type size, with an unclassifiable size failing rather than defaulting into either class.

Panel geometry is fitted from the panel's own printed stud lattice, which gives azimuth, scale and phase but cannot give the face or horizontal hand — a projected square lattice reads identically from above and below and under reversal of one horizontal basis vector. The face comes from the printed rotation icon as a running parity from a named seed step, while the horizontal hand is resolved only from asymmetric bound visual evidence; a step outside the contiguous read prefix refuses rather than defaulting to studs-up, and an exact cross-hand tie retains the ambiguity rather than defaulting to an enumeration order.

### Target: building and verifying a printed step

A printed step compiles into build-program operations against the settled prefix — the single canonical document representing every step already settled. Candidates are enumerated from the prefix's own free connectors, seeded from both sides of a stud-tube joint, and pruned by lattice, connector graph, collision and the build-plate rule before anything is rendered. The whole printed step is proposed as one object, because its pieces are placed together and scored together.

A placement is settled only inside a bound observation packet. For a placement made at step N, inspect panel N, panel N+1 as the minimum later witness, and the first farther panel that actually reveals the placement when N+1 occludes it or remains ambiguous. N+1 is not a guarantee: if no retained panel exposes the claimed surface or relation, record it as `not-observable` rather than infer correctness from silence. A booklet image cannot certify a hidden surface it never shows.

Steps therefore have distinct evidence classes, chosen from a free signal rather than assumed: a panel with a usable highlight is scored against it, a step drawn exploded has each candidate redrawn back along the printed arrow's line and compared against the drawn ghost, and a step with neither begins later-panel deferral at N+1 and continues under the observation packet above. Every bar in that comparison is derived from the panel's own geometry, never from a global constant — a panel has a reachable ceiling, so one fixed threshold asks a different question on every page.

The printed arrow states a direction, not a length: an exploded step's seat is occluded, so the ink stops at the visible surface while the part comes to rest behind it. A candidate that ranks first on pixel agreement is not thereby correct; when a score and an image disagree, the image is the evidence and the score is a lossy summary of it.

Missing parts are work items, not blockers. A step needing a part the catalog lacks gets the part added, with family, real LDraw identifier, connectors, collision primitives and provenance; it is never substituted or skipped.

`building-system.md` owns the measured position of this loop and the ordered plan for what is missing. Outside the exact attested step-5 shortcut, the current runner does not yet produce the complete arbitrary-depth N/N+1/farther observation packet, does not use raster evidence in transition classification, has no integrated vision checker, and does not backtrack. Its deterministic post-search audit refuses the measured reflection after complete catalog-realization comparison. The pure registration primitive scores all four proper quarter turns and all four x-reflected counterparts with a separate translation for each and refuses an exact cross-hand silhouette tie. `RealBuildPanelCameraRegistration` validates, copies, and deeply freezes `{latticeHand,latticeDeterminant,registrationPanelStepNumber,turnDegrees,shiftPx}` as panel-local raster evidence, never as authority for a physical transform. The resolver snapshots the canonical document, masks, and shared ledger state around hashing and rendering, verifies the supplied document digest before work, requires every observation panel to be strictly later than the candidate prefix, and atomically reserves complete eight-way D4 families. Browser-output `/3` now creates the exact canonical empty `real-build` document, reserves and retains its eight explicitly unregistered step-0 roots before any panel placement callback, and then refuses printed step 1 because placement scoring is not yet carried through those immutable lineages; the document stays empty and every later requested row is causally blocked. Nested panel-camera evidence `/2` commits the prepared PDF/page/crop/face, exact retained panel and build captures, camera, raster dimensions, built/excluded/render-mask digests, score, centre, shift, hand, determinant, and turn, but it does not retain the exact mask bytes required to recompute the score, so a positive-piece completion remains unauthorized. The new source-parity `/4` schema is a separate authority-free diagnostic generation, not browser-output `/4`; it binds how source-stage and work-raster comparisons were produced but does not cross-bind those masks to compiled candidates or report continuity. Omitted-piece and `multi-build-copy` runner call sites still enter the opaque fixed-action guard before their executor and return `fixed-ledger-frame-unresolved` because no trusted physical-frame authority producer exists. The horizontal-hand repair therefore remains open even though the root and fixed-action refusals are executable in the live runner.

### Target: backtracking

A locally symmetric placement contradicts nothing at the step that makes it, and the model keeps growing on top of every placement, so a symmetric mistake surfaces later rather than never.

The requirement is therefore not to resolve every ambiguity before committing, but to go back far enough when one surfaces: the search commits to its best candidate, retains every rejected alternative as counterevidence, and walks back to the shallowest step with an untried one. That gives the loop a measurable — how many steps were undone and how far back the deepest reversal reached — where "prove this placement is unique" gives none.

### Current manual playback and target booklet playback

The current editor plays back manually authored placement steps. The target booklet workflow plays a completed reconstructed build step by step from the canonical document, using the same renderer and the same step membership the build produced.

## Architecture

### Target product architecture

The following diagram is the target end state. The web editor, domain core, interchange, booklet derivation, deterministic compilation, rendering and panel-scoring slices exist in different degrees; the companion trust-broker path does not.

```mermaid
flowchart LR
  U["User"] --> W["Web editor"]
  W --> M["Brick model and command core"]
  M --> X["LDraw import and export"]
  K["Printed booklet"] --> RB["Booklet reader: inventory, steps, callouts, panels"]
  RB --> PG["Build program for one printed step"]
  PG --> C["Compiler and deterministic validators"]
  C --> M
  M --> R["Canonical renderer"]
  R --> CP["Panel comparison and scoring"]
  RB --> CP
  CP --> PG
  W --> B["Companion trust broker"]
  B --> S["Run, artifact and event store"]
  S --> B
  B --> W
```

### Repository layout (current)

```text
apps/
  web/                       React, Vite, TypeScript, Three.js editor and booklet run driver
  companion/                 Library/test artifact store, test run ledger and test recorder; future broker home
packages/
  protocol/                  Versioned JSON Schema and generated types and validators
  brick-kernel/              Documents, commands, compiler, patches, validation, migrations
  catalog/                   Parts, colors, geometry, connectors, collision, licenses
  rendering/                 Three.js derivation, canonical captures, render packets
scripts/                     Booklet, LDraw, Builder and catalog derivation tooling
docs/                        Design, policy, provenance and devlog
var/state/                   Ignored local broker index, CAS, and development state
var/runs/                    Ignored immutable run bundles
output/                      Ignored booklet run evidence and scoreboards
```

Split a package only when a boundary has real behavior.

### Technology decisions: current boundaries and target obligations

- React manages editor panels and application state; it does not own the Three.js scene graph, which renders disposable state derived from the canonical document.
- Pure TypeScript owns model semantics, commands, import/export normalization and validators. The browser and current companion test slice use those rules; a future released broker must use the same packages. A future worker may call them, but its validity claims are never authoritative.
- Vitest covers pure domain behavior; Playwright covers interaction, deterministic capture, booklet runs, and visual workflows.
- Python is confined to derivation tooling — LDraw, LDCad and Builder source measurement — and never becomes the source of truth for model validity.
- Cross-language messages conform to versioned JSON Schema and contract tests. Each claimed closed shape or canonical scalar spelling requires an explicit schema gate; relationships across messages require an explicitly exported semantic contract.
- The manual editor starts and remains usable without the broker and without any model.

### Target production runtime topology (unbuilt)

The target delivery is an installed browser/PWA shell, a minimal loopback companion trust broker and an unprivileged worker. None of the released broker, production identity, credential proxy, production authority namespace or worker exists today. `apps/companion` currently exports library/test implementations of an artifact store, test ledger and test recorder; it has no server entry point and confers no production authority.

The target production contract is:

- The browser owns the interactive document and offline manual editing in IndexedDB. A service-worker-cached shell keeps the primary per-install origin usable when the broker is stopped.
- The released companion serves the pinned web bundle and versioned job API, and exclusively owns the authoritative event ledger, rebuildable SQLite index, content-addressed blobs, OS-keystore-backed signing identity and allowlisted model-credential proxy. At boot it verifies its release manifest; production keys and credentials are reachable only by the approved released binary, while dirty, development and challenger builds are forced into a visibly separate test namespace. The broker runs only released compiler, validator, canonicalization and policy code, never loads challenger code, and never exposes raw credentials.
- The unprivileged worker holds bounded capabilities and calls models only through the broker's quota- and consent-enforcing proxy. Broker and worker are separate processes with distinct identities and sanitized environments, and a seal created by a worker or challenger identity is invalid.
- The primary web origin uses a stable per-install numeric-loopback scheme, host and reserved port so IndexedDB, the non-extractable device key, pairing pins and queued outbox events survive restarts. Tokens and nonces rotate, not the origin; an origin change requires an explicit authenticated migration served by both origins, and if the old origin cannot run the app refuses to migrate and preserves the old profile for recovery.
- Pairing passes a one-time secret in the URL fragment, exchanged once and immediately removed with `history.replaceState` for a short-lived capability token whose audience is the exact origin and whose scopes name permitted API families. The token stays only in app memory, and every request carries a non-extractable device-key proof over method, canonical path and query, content type, body digest, token audience and scopes, exact origin, nonce and token hash. One-time secrets and bearer material never enter query strings, persistent browser storage, logs or artifacts; no host-scoped cookie is used.
- If the target broker is absent, manual editing, local projects, deterministic validation, rendering, LDraw interchange and trusted browser-local template tools continue to work. A local template tool compiles directly into a hard-validated manual command transaction with local provenance; it is not a broker-sealed patch. Broker-mediated model calls, recorded production runs, ledger queries and sealed evidence are visibly unavailable.

The future loopback API and every current or future imported, printed or model-supplied artifact are untrusted boundaries. The production service must bind only numeric loopback addresses and reject unexpected `Host` or `Origin` values, DNS-rebinding attempts, missing request proofs and replayed nonces. Every implementation path must cap JSON depth and bytes, operation and part counts, image dimensions, archive expansion, LDraw recursion, external references, render memory and runtime; reject path traversal; escape stored text in the UI; forbid dynamic evaluation; and treat artifact prose as evidence rather than operational instructions.

## Canonical domain model

### `BrickDocument`

```ts
interface BrickDocument {
  schemaVersion: string;
  id: string;
  revision: string;
  truth: TruthSnapshot;
  name: string;
  parts: PartInstance[];
  connections: ConnectionEdge[];
  submodels: Submodel[];
  steps: BuildStep[];
  semanticRegions: SemanticRegion[];
  constraints: DocumentConstraints;
  provenance: DocumentProvenance;
}
```

`TruthSnapshot` pins the catalog, connector taxonomy, collision model, transform policy and validator versions required to interpret the document. Saved documents never float to newer truth implicitly; migration produces a new revision with an explicit report. Every reviewed historical full-truth hash selects an immutable catalog-part and color roster plus an exact source-commit connector-semantics authority; caller-owned constraints may narrow the roster, but neither constraints, actual part instances, nor connection edges may introduce an identifier or port that the selected source truth did not define. Migration refuses a source port that disappeared, moved, or otherwise changed and a pair whose behavior changed, while a stable endpoint still carries forward even when the document remains draft-invalid for an unrelated transform. `npm run migration-history:check` independently rebuilds all reviewed source catalogs from their Git archives and proves the complete source/current endpoint and reachable-pair delta before absence from the compact runtime manifest is trusted.

Each part instance has a stable ID, namespaced catalog part ID, color ID, rigid transform, submodel and step membership, semantic tags, and provenance. Position is stored in integer LDraw units. Legal orientations are catalog IDs; articulated joints add bounded joint parameters whose transforms are derived rather than accumulated.

Part transforms are authoritative and connection edges are validated annotations that must agree with the relative transforms implied by their ports. This ordering exists because of the booklet: instructions give placements and never connections, so the loop derives which studs meet which tubes from geometry — and if edges were authoritative a document could assert a connection the geometry contradicts, with nothing able to say which was right.

Moving a part must either preserve and revalidate an edge or detach it explicitly. The model rejects dangling IDs, duplicate edges, over-capacity ports, incompatible or multiply occupied ports, transform/edge disagreement, inconsistent loops, and invalid submodel or step membership.

Canonical serialization defines stable array ordering, ID generation, numeric normalization and the exact provenance fields excluded from or included in the structural hash. Cosmetic metadata never changes structural identity. The structural hash covers part identifiers, so it answers whether two documents are the same document, not whether two models are the same model.

### Part catalog

`part-model.md` owns how a part is organised, indexed, defined and constructed. A `PartDefinition` carries namespaced ID and aliases, geometry source and content hash, bounds and simplified collision representation, typed connection ports with local transforms and compatibility rules, legal orientations, a substitution-group identifier, available colors, inventory metadata, and source/license/attribution/catalog-version provenance. Current builtin inventory records leave `knownMassGrams` unset; richer substitution rules and measured mass are not implemented catalog truth.

Catalog measurement, mesh generation, semantic checks, and focused tests automate parts of admission, but no tool or model admits a part automatically. Admission remains a reviewed repository change that records sources, inspects matched views, advances the truth snapshot, and passes migration and verification gates; an unexposed surface is named `not-observable`, not approved by omission.

Truth imported from differently licensed datasets stays in separately attributable catalog layers, never flattened into an untraceable application-owned blob.

Adding a part is a catalog-truth change: it advances the builtin catalog version, extends the migratable set, and the migration report says what changed. Reviewed historical full-truth hashes keep their exact immutable catalog-part/color rosters and source-commit connector projections so existing documents keep hashing as they did and migration cannot legitimize a future identifier, later port, removed port, or changed connection frame through current definitions or caller-owned constraints.

### Connection graph

A connection edge joins two named ports and records a wire-level connection class, joint parameters and provenance. Catalog ports implement ten kinds: `stud`, `undersideClutch`, `axle`, `axleHole`, `pin`, `pinHole`, `bar`, `clip`, `hinge` and `hingeSocket`.

The six catalog pair rules are `stud` to `undersideClutch` with rigid quarter-turn alignment and opposed axes; `axle` to `axleHole` with rigid quarter-turn alignment and collinear axes; `axle` to `pinHole` with continuous revolute motion and collinear axes; `pin` to `pinHole` with continuous revolute motion and collinear axes; `bar` to `clip` with continuous revolute motion and collinear axes; and `hinge` to `hingeSocket` with continuous revolute motion and opposed axes. Placement discovery uses those port rules, but `ConnectionEdge.kind` and `AttachInstruction.connectionKind` still carry only `stud-tube`; the referenced ports currently supply the more specific pair. End-to-end edge semantics require a versioned protocol expansion before the other pair identities can travel on the wire.

### Restricted `BuildProgram`

A printed step or other proposer emits a restricted declarative program or typed operation suggestions. Nothing emits a trusted patch envelope, JavaScript, Python, shaders, SQL, or arbitrary commands.

The current program schema and compiler, not the studio's exposed command set, support placing a catalog part at an explicit rigid transform, attaching two named ports through the `stud-tube` wire class, removing or replacing a named part, moving or recoloring a named part, and assigning one part to a build step. The schema also contains `instantiateTemplate`, but the current compiler rejects it explicitly because no template compiler is installed.

The current compiler validates the closed operation schema and enforces exactly three trusted scope budgets—maximum operations, added parts, and removed parts—before deterministically producing document commands; compilation failure is a normal rejected result rather than an application error. Expansion depth, recursion, memory, elapsed time, and template part counts are not current compiler budgets.

The target template path uses a schema-constrained, non-Turing-complete declarative AST and adds expansion-depth, recursion, memory, part-count and time budgets. Imports, scripts, callbacks, arbitrary expressions, dynamic evaluation, TypeScript and Python remain forbidden even in quarantine. Those template-expansion capabilities and resource limits are specified, not implemented.

Model output is always untrusted candidate data. It cannot author revision, scope, provenance, consent or validation fields. Current local model tooling remains outside the user-document command path. Under the target broker topology, a caller submits an untrusted value with only opaque broker-issued job and attempt IDs; the broker resolves the trusted revision, scope, provenance, consent, truth and budget context from its job record and hands it to the released compiler, and only that compiler can create an unsigned candidate patch.

### Compiled patch and scope

The compiler's output is an `AssemblyPatch`: schema version, base revision, base document hash, truth snapshot hash, scope capability ID, scope digest, operations, and provenance. It is a compiler product, not a proposer product, and a raw patch is never directly applicable.

The current compiler and verifier receive scope as a separate trusted input; no proposer may supply or widen it. In the target broker topology, the complete allowed scope lives in the trusted job record created from the user's action and the browser retains an immutable copy for the life of the job. `scopeCapabilityId` is an opaque reference and grants no authority.

`verifyAssemblyPatchAgainstCapability` in `packages/brick-kernel` independently rechecks a received patch against the exact retained base document and scope capability, returning typed issues. It is a verification boundary only: it authorizes nothing and writes nothing.

Manual editing may temporarily create a draft-invalid document. A compiled patch must introduce no new blocking issue outside its scope, must leave its affected scope hard-valid, and must preserve global validity when the base was globally valid. The UI distinguishes `patchValid` from `documentGloballyValid`.

There is no automatic path from a compiled patch into a user document, and the former presented-envelope and acceptance-authorization types do not exist. A user document changes only through an explicit manual command. Any future automatic path is a new design decision requiring a newly specified, user-originated, scope-bounded and one-use authorization ceremony; it cannot be re-enabled from retained code.

## Model calls and consent

The current repository has one opt-in legacy model-assisted derivation producer that describes placement relations from one printed panel. Part-identification first-pass provider execution is available only as one exact six-card development pilot behind the local Gate 0 lineage described above; re-ask remains disabled, retired `/4` answers are explicitly non-resumable, and no `/5` model-bearing call has yet completed. A third quarantined local foundation defines source-bound multi-panel checker requests, attempts, results, refusal consumption, and a one-tool subscription-CLI adapter, but it has no real-PDF/candidate-render producer, driver consumer, consent preflight, or successful live verdict. All write or are designed to write ignored development evidence, are not broker-backed or exposed in the web product, are not wired into the real-booklet driver, and have no path to mutate a user document.

Current booklet placement and panel agreement are decided by deterministic candidate enumeration, rendering and pixel scoring. Blind same-or-different pair verdicts are retained trust evidence produced outside the integrated runtime and consumed by the coverage tooling. A verdict applies only to the exact full crop SHA-256 plus claimed element that was judged; similarity or refined-cluster membership never transfers it to another crop or member. Two blind raters agreed on all 84 presented pairs, recording 82 verdicts and two unjudgeable pairs, while the earlier open pick-one-of-N vision pass managed 39.9 percent self-consistency. That is an agreement measurement, not independent proof that every verdict is correct; the retained evidence is not a live checker service, and narrowing can discard the settled truth while leaving only safe candidates. No integrated checker model or broker-backed provider path exists today.

The target booklet loop may use a **proposer** to read printed art and a **checker** to ask the closed same-or-different question over a render and printed panel. Both current and target uses are bound by the same authority rule: model output is untrusted data. It proposes while a deterministic check disposes, and it cannot declare itself valid, author trusted scope or provenance, execute code, waive a hard validator, admit a part, or mutate the user document. A production call must record provider, model, parameters, seed where supported and a raw response hash.

Under the target broker topology, before any prompt, reference or model summary is transmitted, the broker evaluates a versioned `ProviderCapabilities` record from a maintainer-reviewed policy registry. Each entry is independently signed or pinned and declares supported protocol, schema and catalog versions, accepted input kinds, cancellation and seed behavior, size and budget limits, local or external execution, retention and training policy, and accepted consent classes. Runtime discovery may narrow that trusted record but never broaden it or relax its data-handling policy, and an incompatible or insufficiently consented job fails preflight without sending user data.

Consent is scoped and specific under both the current local tooling and any target broker path. Cropped regions of the user's own instruction booklets — callout thumbnails and step panels — may be sent to a model for part identification or printed-step placement description only after explicit operational authorization. Crops only: never a whole booklet, other repository content, credentials, or another user reference, design, or artifact; retained responses stay under ignored local output roots. The strict part-identification pilot binds purpose, exact ordered card/request digests, provider and model, configured-fetch privacy/provider-policy evidence, one model launch, one local executable preflight, two provider turns, full model capacity ceilings, cost, elapsed time and proof bytes through proposal, trusted-local assertion, reservation, one global pilot slot, launch, proof and terminal settlement. The assertion record explicitly does not authenticate a user event or repository-owner identity and therefore cannot prove the conversational authorization that operationally precedes it; same-process code and compromised pre-import fetch state are outside this local control. Its sanitized proof intentionally discards raw stream/session data and is `local-diagnostic`, `authenticated: false`, and non-executable; it cannot establish that Anthropic ran, which remote weights or policy applied, that the account policy matched the reviewed public pages, or that no process wrote outside the final audited task root. The current panel-placement script intends the crop boundary but accepts any resolved local path without source or image validation, launches the CLI from the repository with repository-wide `Read`, and retains no tool trace, so that boundary is policy rather than an enforced or auditable data limit. The quarantined checker constrains which already-bound bytes its adapter can expose and minimizes model-visible metadata, but a digest plus claimed PDF/page/bounds is not proof of crop origin or consent, and candidate-render transmission also requires explicit scoped authorization. A replayable successor must enforce an authenticated exact authorized snapshot rather than merely state the restriction in its prompt.

The model a product calls is pinned in this repository at the call site's own module. No model ID is hardcoded anywhere else.

## Validation hierarchy

Validity is lexicographic. A high visual score cannot compensate for a hard failure.

1. **Compilation:** supported schema, catalog parts and colors, finite canonical transforms, legal operations.
2. **Structural:** compatible ports, permitted orientations, no material collision under the defined model, required connectivity, support against the build plate, allowed envelope, frozen scope, and part budget.
3. **Buildability advisory:** support, approximate stability, insertion accessibility, and plausible build order — clearly advisory until calibrated with physical evidence.
4. **Panel agreement:** does the render of this candidate match what the booklet draws, at that panel's own camera, face, horizontal hand, and reachable ceiling.
5. **Set accounting:** does the finished build reconcile with the booklet's own inventory, callout quantities and piece totals.

Hard validators return typed issues with implicated part and port IDs, geometric evidence, and permitted repair classes. Pixels cannot prove graph correctness, and graph correctness cannot prove the model is the one the booklet draws; both are inspected.

A physical claim applies only to the exact document and catalog hash actually tested, and any structural edit invalidates it. A general physical-buildability claim requires a separately reviewed calibration program, not an arbitrary count of successful builds.

Historical `/24` definition `11212` expands official root `parts/11212.dat` (`sha256:c527adbbc5db2983cdc9d0b28481d57a248fd0125d8aa00c13aebd7c32b6633f`) through its exact ten-file, 11,078-byte closure into 844 triangles over 873 stored vertices. Its collision field has 129 positive-volume boxes and nine exact-source-radius stud cylinders. The checksum-pinned LDCad walk consults `p/stud.dat`, `p/stud4.dat`, and `parts/11212.dat`; active `p/stud.dat` metadata authors nine studs, active root metadata authors the nine clutches, and `p/stud4.dat` contributes no active meta because its anti-stud declaration is disabled. Ordinary collision retains the measured stud radius, while only an exact validated stud/clutch edge uses nominal radius 6; the square geometry is quarter-turn symmetric, so yaw zero is canonical. The admission score is `0.9925023760839874` with zero hard failures. Revision-I record metadata from the checksum-pinned native pack reports nine clutches without a reviewed frame and therefore remains count-only counterevidence rather than connector authority.

Historical `/25` definition `33909` expands official root `parts/33909.dat` (`sha256:8da6789db82746f179997ed4b917d00d34d03a6486d6aa27c76d17c9b21d8609`) through its exact nine-file, 10,203-byte closure into 220 triangles over 242 stored vertices. Its collision field has 41 positive-volume boxes and two exact-source-radius stud cylinders. The checksum-pinned LDCad walk consults `p/stud.dat`, `p/stud4.dat`, and `parts/33909.dat`, authoring two studs along one edge and four underside clutches. Ordinary collision retains the measured stud radius, while only an exact validated stud/clutch edge uses nominal radius 6; yaw zero plus `[0,-4,0]` is the selected frame. The 401,980-point admission score is `0.9955832518073061` with zero hard failures. Builder revision-E metadata has no reviewed frame and therefore remains count-only corroboration rather than connector authority. The stale step-76 locator grants no printed identity, placement, action, or frontier authority.

Current `/26` definition `78329` expands official root `parts/78329.dat` (`sha256:79ec75c5092750b0f2022dab9c7561376d8b2b33fc3dea7059081ef273d4f7fc`) through its exact nine-file, 8,761-byte closure at `sha256:d203ae681cfa3842e210b894d46e69e555e64e638796d260c3a2cabdb474f283` into 460 triangles over 489 stored vertices. Yaw 90 plus `[0,-4,0]` gives body bounds `[-10,-4,-50]..[10,4,50]` and visual bounds `[-10,-8,-50]..[10,4,50]`. Its collision field has 39 positive-volume boxes and five exact-source-radius stud cylinders. The checksum-pinned LDCad walk consults `p/stud.dat`, `p/stud3.dat`, and `parts/78329.dat`, authoring five studs and five underside clutches on one regular 1 × 5 lattice. Ordinary collision retains the measured stud radius, while only an exact validated stud/clutch edge uses nominal radius 6. The 659,766-point admission score is `0.9968390298840539` with zero hard failures. Builder revision-F metadata has no reviewed frame and therefore remains count-only corroboration rather than connector authority. Seven stale, self-contradictory locators at steps 265, 268, 271, 274, 277, 282, and 336 grant no printed identity, placement, action, or frontier authority.

## Rendering and visual inspection

Part admission uses a dedicated browser harness rather than treating the editor's seven-view presentation hook as review evidence. It materializes a checksum-pinned LDraw closure, renders it through an independent `LDrawLoader`, renders the production catalog definition in a clean detached scene, and captures matched top, bottom, front, back, left, right, isometric, and underside-oblique pairs through one union-fitted camera packet. Generated packets bind sources, frames, renderer policy, cameras, PNG and decoded-RGBA hashes, and diagnostics, but remain `pending` until a separate immutable explicit review records `same`, `different`, or `not-observable` for every view. The historical `/13` through consolidated `/18` tranches and separate `/19` `41682`, `/20` `2877`, `/21` `3040`, `/22` `4519`, `/23` `32064`, `/24` `11212`, and `/25` `33909` tranches remain evidence under their original truth. Current `/26` packet `sha256:3dcce6065d94e243c65485c0b29effba307ae2e9e508ef3c49fa7e8d6a059531`, capture batch `sha256:0305f056cff14aa658d801f73a5cd852aff851c13378dc19ab7f08962f1c26f6`, native-pair manifest `sha256:774dd19d354961b1da5907a8b31dea01d97160bf152f6152184acc8699bf397b`, review `sha256:f16c939d3f333b5b581532010093a98f0d368d5964aeeb59f77a94358e8d482b`, and review batch `sha256:0031abc4205d40e9c2d2e1543674bc7a3bd12916c32ef5c75200db0c34fdface` close all eight original 640 × 640 `78329` pairs: seven are decoded-RGBA exact, the isometric pair is visually indistinguishable at original size, and every pair was inspected and reviewed `same`. The clean underside-oblique pair exposes matching walls, central rail, four tube forms, and cavities, but no consolidated all-37 `/26` recapture is claimed and exterior agreement still cannot certify hidden material, exact collision, connector fit, grip, stability, insertion access, or trusted placement.

The historical `/20` editor audit searched for `2877`, used `Place at origin`, and retained globally valid document `sha256:c692331b132f52a4aaee3934dc88ad7dd99e7478862a1800b432fdb019c7b76f`. Its 194,264-byte full-editor screenshot is `sha256:3346e445323a8ae300dd75fc3f308ff64cf1e807a12e35b25421725dcb50bdab`; all seven canonical presentation views were inspected, and no console or page errors were recorded.

The historical `/21` editor audit searched for `3040`, used `Place at origin` after the exact validated-connection profile repair, and retained globally valid document `sha256:752fc4da4bf61139d24d14743443a4b8d5fd3cbbe4f21f1986e8d13a8d2f50a0`. Its 123,501-byte full-editor screenshot is `sha256:cb9e123e4979cf3728dc880a28e584f4ed0fe28a7333ff0c8d1322e35c5c77f4`; all seven canonical presentation views and the filtered palette were inspected at original resolution, and the retained audit records empty console- and page-error arrays.

The historical `/22` editor audit searched for `4519`, confirmed 94 total definitions and three Axles entries, used `Place at origin`, and retained globally valid and hard-valid document `sha256:eaac23045e78e09b3f71bc83ff906d10db5cffb7349c2370d44c55dd55174e9a` under truth `sha256:7f64021239ab6395a3666f1f72908fd420b73065909822bc68e5226785bfa12e`. Rendered bounds were `[-1.475,-0.3,-0.3]..[1.475,0.3,0.3]`; the full UI and isometric, front, back, left, right, top, and underside views were inspected at original size and showed the expected axle geometry and framing. No app-logic errors occurred, while one favicon 404 and WebGL precision and shadow-map deprecation warnings remain visible in the console record.

The historical 2026-08-24 local `/23` Vite development-server observation searched for `32064`, confirmed 95 total definitions and two Technic bricks entries, selected the sole result, and used `Place at origin`. The one-part document was globally valid with no validation issues at `sha256:1293be3ca50674cf12534ad8a29dc4da67f9a4694ca4f19ccde002b2827c8d26` under the truth reported by that run, with rendered bounds `[-0.5,-0.6000000000000001,-1]..[0.5,0.8,1]` and no fallback geometry. The 140,108-byte full-editor screenshot is `sha256:1dc0ee3dce94bd2bf67796bf6065cfcbf11e2c09996cb5e01a2b43a744390e4f`, and the audit JSON is `sha256:a6702297dda4f61ee379e038e4955ef2cb9504c515745982a83d58b64adf9feb`. The full UI, filtered palette, and isometric, front, back, left, right, top, and underside views were inspected at original resolution; no console or page errors occurred, while the existing Three.js shadow-map deprecation and Chromium readback-stall warnings remained non-fatal. The ignored evidence remains bound to historical `/23` and is not an exact current-source or production-editor receipt.

The 2026-08-24 local `/24` headless Playwright observation searched for `11212`, confirmed 96 total definitions and 31 parts in the Plates family, selected the sole result, and used `Place at origin`. `render_app_to_text()` and `get_model_snapshot()` agreed on one globally valid part, zero connections, zero validation issues, and document `sha256:ae554c96f474c9e0b7f2c7de9c4fbd59aed09617e8e01950077aa68d02b6d6b3` under then-current `/24` truth. The 206,633-byte full-editor screenshot is `sha256:d867cb32c673873d163406c3612287c3cd93e830f04b625914f382d0c4935b09`, and the 11,874-byte observation is `sha256:511cf592b8f902cce0efb88b937f8af66573086865f2dd0eae88a6d8e803dbdc`. The full UI, filtered palette, and all seven canonical captures were inspected at original resolution; the straight underside view was occupied by the coincident build grid, so only the separate clean admission underside-oblique pair is used to certify the visible tube geometry. No console or page errors occurred. The ignored editor audit is a dated local pixel observation, not an exact current-source or production-editor receipt.

The historical 2026-08-24 local `/25` headless Playwright observation searched for `33909`, confirmed 97 total definitions and 32 parts in the Plates family, selected the sole result, and used `Place at origin`. The one-part document was globally valid with zero validation issues at `sha256:1694c915d36a1a4110193046949ffb10df41dfa61d60783683d02634c336d065`. The 198,617-byte full-editor screenshot is `sha256:cbd823589a92c7e6fb23f9522d2bae624358e2d79cf4c3b14f310431ecf6b03c`, and the 12,112-byte observation is `sha256:7b673bfca74ecf97cfbea02c01b993356348f30e566c9162e5ce1e0b897b8f12`. The full UI, filtered palette, and all seven canonical captures were inspected at original resolution; the straight underside was grid-occluded, so only the separate clean admission underside-oblique pair carries the ring-and-cavity claim. No console or page errors occurred. The ignored editor audit remains a historical dated local pixel observation, not an exact current-source or production-editor receipt.

The current 2026-08-24 local `/26` headless Playwright observation searched for `78329`, confirmed 98 total definitions and 33 parts in the Plates family, selected the sole 460-triangle LDraw-backed result, and used `Place at origin`. The one-part document was globally valid with zero validation issues at `sha256:7365cbf1980f43536b40472b1b8857856a635730a49ba9a9501503337488800f`. The 199,383-byte full-editor screenshot is `sha256:be21961042d5eb17668ded5c93d523662aa78e51dea5110bd90da98e7e790a7d`, and the 13,276-byte observation is `sha256:71acb1ce24c0fb3e46939cd18c75127dc0ad507f4493d81b1ed365af2e72a5db`. The full UI, filtered palette, and all seven canonical captures were inspected at original resolution; the straight underside was grid-occluded, so only the separate clean admission underside-oblique pair carries the internal-rail, tube, and cavity claim. No console or page errors occurred. The ignored editor audit is a dated local pixel observation, not an exact current-source or production-editor receipt.

The target deterministic render packet contains a fixed isometric presentation view; front, back, left, right, top and underside orthographic views; an underside-oblique view for internal walls and clutch geometry; silhouette and depth passes; a part-ID pass grounding visual findings to model entities; an exploded or layer view when useful; connection, collision, disconnected-component, exposed-port and support overlays; and closeups around each blocking validator issue. Current browser capture returns PNG data URLs from seven canonical cameras — `isometric`, `front`, `back`, `left`, `right`, `top`, and `underside` — over the live presentation scene. It has no underside-oblique view, clean admission scene, source-image pairing, artifact hashes, renderer receipt, or render-packet binding, and the presentation grid, shadow plate, lighting, and current selection overlay may be present.

For a booklet step the render that matters is the one taken at the printed panel's own camera, face, and horizontal hand, because that is the only render comparable to the drawing. The panel supplies azimuth, scale and phase from its stud lattice; the printed rotation icon supplies the face correction, including the sign of the elevation and model-up; asymmetric bound geometry must settle the remaining horizontal determinant. Binary-silhouette registration may compare both hands, but reflection reverses depth, so a silhouette winner alone does not prove RGB, occlusion, or a physical document branch.

Instruction rendering imitates measured booklet art rather than an asserted dialect: parts carry three face tones, a near-black stud wall and a per-colour ink, not one flat fill.

Renders are looked at, not only scored. Matched top, bottom, front, back, left, right, isometric, and underside-oblique views delimit what was observed; hidden surfaces remain `not-observable`. Visual defects have repeatedly escaped passing tests and been found by looking at renders; nonvisual defects are also found by source audits, digest checks, tests, and review.

## Target user experience

The current workspace provides the central 3D viewport, searchable catalog, project controls, selection and transform/color inspector, live validation and manual build playback. Its Instructions control currently extracts bounded page and text metadata and a content hash from a local PDF; it does not render booklet panels or expose placement candidates, compiled steps, verification, or a run inspector.

The main workspace contains a central 3D viewport; a part palette and template library grouped by family and searchable by name, size and identifier, with previews derived from each part's own geometry so the palette cannot drift from what gets placed; a scene, submodel and step tree; a selection and constraint inspector; a booklet panel showing the loaded booklet, its printed step list, the current step's panel with its highlight, arrows and face, and the candidate under consideration beside it; a validation panel distinguishing blocking, advisory and unknown findings; and a run inspector for replay, provenance and retained evidence.

A booklet run never blocks manual editing. A run is tied to the document revision at submission time, supports cancellation, and cannot win a race against newer user state.

## Persistence, provenance and consent

### Current implementation

- The web app stores integrity-checked local project envelopes in IndexedDB. Reloading the page is not a fresh plate; any run driver must load the intended document revision rather than assume an empty document.
- `apps/companion` provides a content-addressed artifact store, hash-chained test run ledger and development-only test recorder as importable library code with tests. It is a test namespace, has no production signing identity and is not an authoritative service.
- Development-only recorders may retain exact canonical requests and authority-free outputs only under explicit local artifact-retention consent and hard byte budgets. Their output is unsealed, unauthenticated and cannot assert production authority, a seal or promotion.
- Booklet run bundles, closure metadata, renders, panels and scoreboards live under ignored `var/` and `output/` roots. Current replay tooling verifies and inspects retained bytes but deliberately does not execute retained source as an authoritative or diagnostic replay.
- Git stores schemas, migrations, curated templates, fixtures, goldens and benchmark definitions. Run evidence enters Git only when review promotes it into a repository input. Large part libraries, model weights, raw run corpora and generated images are not committed.
- No current component writes an authoritative production ledger, holds production signing or provider credentials, issues production seals, authenticates a paired browser or performs signed deletion and consent-revocation lineage.

### Target production contract (unbuilt)

- The released companion is the exclusive writer of the authoritative hash-chained append-only event stream. It authenticates user, curator, maintainer and evaluator capabilities before recording events, seals finalized event roots with a signing key reachable only by the minimal released broker identity through the OS keystore, writes files to temporary names before atomic finalization, and includes every artifact hash in the final manifest. Later correction, edit or deletion records are linked events, never mutations of a sealed manifest.
- SQLite is a rebuildable query index over authoritative events and bundles, not a competing record of truth. Content-addressed files store programs, documents, LDraw, renders, reports, panel rasters and consented raw model responses.
- Every production run pins application, broker and worker commits plus source-tree/diff and built-bundle hashes, lockfiles, runtime versions, non-secret configuration, schema and catalog hashes, model and prompt hashes, booklet and panel hashes, budgets and seeds. Retrievable content-addressed source patches and built bundles are retained only after secret and license scans; a hash without retrievable content is insufficient for source-level replay.
- At finalization the broker validates the transitive artifact closure and seals a replay certificate containing `sealedReplayLevel: full | downstream-only | metadata-only`, the earliest retained boundary, and every required artifact hash with its retrievability evidence. Callers may request retention but cannot set the result. Later tombstones leave the certificate unchanged while the API derives a separate `effectiveReplayLevel`.
- Deletion creates an authenticated signed tombstone, removes derived indexes and thumbnails, decrements blob references and garbage-collects unreferenced blobs. Sealed manifests and prior events remain unchanged, and the API derives the effective replay level from tombstone lineage without retaining deleted sensitive content. Export and consent revocation follow the same lineage links.
- Secrets stay behind the broker's credential proxy and outside worker environments, project artifacts and browser bundles.

User references and designs are local by default under both current and target implementations. External transmission, training, benchmark inclusion, sharing and Git retention are separate consent decisions.

## Interchange

- App JSON is the editable source format; LDraw `.ldr` and `.mpd` are the primary ecosystem interchange formats.
- The supported LDraw subset covers supported parts, colors, rigid matrices, submodels and steps. Connection edges are inferred deterministically and supported golden fixtures must reproduce the same canonical edge set.
- Unsupported parts, arbitrary matrices, articulated poses, local or external references, and unknown metadata are rejected with diagnostics or preserved as explicitly opaque view-only records. They are never silently rounded, dropped or made editable.
- LDraw export preserves stable part transforms and steps and may carry a namespaced provenance metadata extension without requiring it for compatibility.
- Any future GLB support is a derived delivery or rendering export, never the authoring source; no GLB export path is implemented today.

Plain LDraw is lossy for unsupported connection semantics; exact complete replay uses app JSON or a metadata-bearing export. Round trips are verified against golden models and external viewers through the actual consumer path — a string round trip does not prove a model loads. BrickLink Studio is an interoperability and behavior reference, not a code or asset dependency.

An official set export is a corroborating witness, not an oracle. Where the repository's own derivation and a published export disagree, the disagreement is settled by geometry — collision, interlock, and support — and the outcome is recorded with its numbers.

## External research and licensing gates

Permission to reuse geometry does not imply permission to train, and that right stays unheld everywhere in this repository.

- The LDraw parts library preserves file-level source, license, and attribution metadata. Bundled LDraw geometry ships under CC BY 4.0 with per-file authorship preserved in `docs/bundled-geometry-notices.md`, rendered from the catalog itself.
- LDCad Shadow Library-derived connector data is admitted as catalog truth under the owner's 2026-08-05 decision that licence must not block private, noncommercial work. It stays separately attributed: its provenance record carries the CC BY-SA 4.0 attribution and the pinned library digest, share-alike attaches to the derived data on redistribution, and no shadow file is committed — derived positions, not source text.
- LEGO Builder native source is used for measurement and frame derivation. Native payloads stay local where practical, exact identities and hashes are preserved for reproducibility, and upstream material is never described as project-owned.
- Research systems such as BrickNet and BrickGPT remain recorded in the bill of materials as evaluation-only. Their model, dataset, connector and collision-asset terms require a complete audit before any redistribution or commercial use, and nothing here depends on them.
- Internet-curated models are not assumed to be training data merely because their referenced part geometry is reusable.
- The public product name, logo, domain, and non-affiliation language require trademark review before launch.

The dependency and data bill of materials records origin, version, license, attribution, redistribution/training rights and allowed runtime role for every code, geometry, connector, collision, model and example source.

Primary references for that audit are the [LDraw legal terms](https://www.ldraw.org/legal-info), [LDCad Shadow Library](https://github.com/RolandMelkert/LDCadShadowLibrary), and LEGO's [Fair Play policy](https://www.lego.com/en-us/legal/notices-and-policies/fair-play).

## Automation and testability

The current app exposes a stable, test-only automation bridge: `window.render_app_to_text()` returns the canonical document and hash, selection, validation, overlay, renderer snapshot and command error; `window.capture_model_views()` returns seven named canonical-camera PNG data URLs from the current presentation scene; and `window.get_model_snapshot()` returns a schema-versioned document summary and structural hash. The capture hook is not wired to the render-packet artifact contract and does not itself bind the PNGs to the structured snapshot. `window.advanceTime(ms)` currently validates a bounded delay, yields once and returns the snapshot; it does not yet drive an animation clock.

These are verifier instrumentation, not a model-facing action surface. Trusted capture code in the test boundary may call them and retain evidence; an external or model-facing actor receives only a filtered, hashed observation and a bounded action API, with no arbitrary browser evaluation, direct debug-hook access or production authority. A future production build must either omit the bridge or require a separately authenticated test namespace that cannot reach production documents, identities, credentials or ledgers.

A feature is not approved from source or hook presence alone, nor structural behavior from a pleasing screenshot alone: the served app is driven in a real browser, canonical views are captured, and pixels, structured state and intended behavior are confirmed to agree.

Current Python, Vitest, and Playwright suites cover the implemented document and command core, compiler and patch-scope invariants, LDraw expansion and source-normal reproduction, catalog and part-standard admission, connector/collision/connectivity/support/bounds rules, LDraw round trips, matched part-view capture and review contracts, editor interactions, persistence, rendering, booklet extraction, panel registration and scoring, placement search, retained partial-run evidence, data-only replay-closure inspection, and the companion artifact-store/test-ledger/test-recorder slice. Live model calls are opt-in and ordinary tests use retained or mocked responses. Those tests establish packet and review contracts, not that a human or independent reviewer performed a particular inspection; the separately retained review records the eight inspected native-resolution `78329` pairs as current `/26` evidence, while `33909` remains historical `/25` evidence, `11212` remains historical `/24` evidence, `32064` remains historical `/23` evidence, `4519` remains historical `/22` evidence, `3040` remains historical `/21` evidence, `2877` remains historical `/20` evidence, `41682` remains historical `/19` evidence, and their 29 predecessors remain historical `/18` evidence. No consolidated all-37 `/26` recapture is claimed. Neither source proves that the unbuilt production broker, worker, credential, signing, pairing, deletion, or executable replay contracts exist.

The target production suite must additionally prove that workers and challenger builds cannot read production credentials, use signing keys, write the authoritative ledger or mint production namespaces; that event-log hash chains, finalization and sealed bundles resist truncation and tampering; and that replay-level downgrade, deletion, consent revocation and content-addressed garbage collection preserve the specified lineage.

## Delivery sequence and gates

These are target exit contracts, not assertions that each gate is complete. Gates 0 through 2 have substantial implemented slices but retain open production-topology and replay work. Gate 3 is the current active product frontier; `building-system.md` owns its measured prefix, current refusal and ordered next action.

### Gate 0 — Provenance and executable contracts

Establish the dependency and data bill of materials. Define the `BrickDocument`, `BuildProgram`, `AssemblyPatch`, validator-issue, render-packet, observation, attempt-transcript, trust-namespace, sealed run-manifest and replay-closure schemas. Specify and threat-model the broker/worker process boundary, production release identity, key access, credential proxy, stable web origin and test namespaces before any broker-integrated production model path. Define catalog and connector provenance rules. Generate `THIRD_PARTY_NOTICES`, preserve per-layer license files, and add a packaging test that excludes evaluation-only assets while auditing required attribution.

Exit: every planned code, geometry, connector, collision, model and example source has an explicit allowed role or is marked evaluation-only, and a distributable test package proves those boundaries.

### Gate 1 — Deterministic assembly kernel

Curate basic bricks and plates with verified geometry and stud/tube ports. Implement the canonical graph, commands, build-program compiler, renderer, diagnostic overlays and hard validators, plus a manual editor and LDraw round trip.

Exit: supported-subset golden models round-trip with exact instance, transform and deterministically inferred connection-edge equality, and unsupported data is rejected or preserved as opaque without loss; seeded invalid fixtures are rejected with the expected typed issue; every admitted output is connected, legal, collision-free under the defined validator, and reproducible by structural hash; and no physical-stability claim is made.

### Gate 2 — Booklet reading, and run evidence

Read a real booklet into its inventory, printed steps, callouts, panels, highlights, arrows and faces. Reconcile the booklet against itself — sequence coverage, callout quantities, piece totals, and type-size classification with an unclassifiable outcome. Fit the panel camera from the printed stud lattice and derive the panel face from the printed rotation icon. Add immutable run bundles, replay levels, canonical render packets, and retained per-step evidence.

Exit: the booklet's own internal checks reconcile with recorded numbers, every panel the loop uses carries a fitted camera and a derived face or a named refusal, and a run's evidence is retained and replayable to its declared level.

### Gate 3 — Current frontier: building and verifying a printed step

Status: active. The internal real-booklet runner can settle and retain a contiguous diagnostic prefix under its frozen legacy path, carry a bounded ambiguous origin through one intervening printed step with immutable lineage, aggregate render reservations before work, aggregate child reservations before retention, exact N+1 captures, conditional K, and named numerical refusals. One source- and data-attested calibration may score the exact two measured step-5 origins directly at panel 7, while the generic path remains conditional on completing its intervening carry. Node finalization applies the bounded proper-upright target-equivalence audit after exact identity binding and separates structurally valid diagnostic bytes from canonical target completion. The current generation-3 browser path uses the pure `LatticeHand` transform and resolver only to derive and atomically retain the exact eight-way canonical empty-root frontier, records that evidence on report 1, and refuses before step-1 placement because candidate scoring has not been carried across `lineageId`; later rows name that first refusal as their causal block. Browser-local provisional preparation commits the detached input, source/module closure, fetched PDF, panel, crop, action, raster, mask and fitted-camera facts, but `/3` consumes preparation defects only in shadow mode so that they cannot replace its exact root refusal. The separate atomic compiled-branch batch can reserve, compile, replay and retain convergent parent-specific lineage evidence from exact detached offers. The observation sidecar independently replays exact retained mask bytes, optimal shift, score, ranking and selected compiled transitions under a bounded prepared policy, including own-panel step 359 and strictly later lookahead, while treating typed observation failures as unverified and granting no authority. The branch-role index independently binds the external compiled and observation byte roles. Its refusal-only local semantic consumer rejects internally orphaned source/camera tables and charges cumulative root groups/identities, search parents/proposals, child candidates, transitions, edges, witnesses/operations, legacy and typed observations, source/camera tables, selection/accepted-transition reference arrays, exact root/child canonical-document bytes and raster work before bounded root reconstruction; it then derives and charges conservative compiler work-policy units before graph/compiler or mask replay. The established public lineage `/1` parser retains its prior semantic contract while the aggregate gate is explicit on the semantic and bytes-only observation paths, and typed observation preflight requires the exact replay-admitted work inspection. The consumer exact-binds prepared step/compiler/proposal facts, requires legacy observation-generation, selection and acceptance fields to remain empty, separately verifies typed closure bytes and any nonempty raw role, and emits only authority-absent summaries; it does not bind source/camera provenance, report order or terminal continuity. The separate source-parity `/4` diagnostic retained all 359 real-booklet rows on 2026-08-22 under measurement digest `sha256:39a810d8a30f28fb93bb619f2ce522ce01e95146e8fe14666d6606270c0cb615`; its browser work/policy/derivation commitments remain opaque, its source-RGBA column is visualization-only, and its authority is absent. Its exact-five publication reproduces the captured W evidence against the exact source execution and derives an audit-only identity, but missing truth, review drift, W mismatch, and exact W equality all remain `needs-adjudication`. A canonical Ed25519 receipt verifier snapshots caller-supplied trust before one fresh local challenge, checks signed request/scope/review bindings against that locally held state, and recomputes the next transition from the supplied checkpoint, but returns authority absent; production has no released broker or authenticated trust source and cannot enter the module-owned runtime event capability. A separate detached browser-output `/4` reader snapshots a closed hostile input, cross-binds prepared, branch, all-359 source, multi-step D4 camera and transition roles, and induces selected reports, evidence-derived terminal failures, identity bindings and exact terminal bytes. It refuses failed empty prefixes without their own typed witness and completed fixed actions without physical-frame replay. This reader remains a synthetic authority-absent inspection seam with no live producer, authenticated source-render execution, provisional identity, physical frame, placement, completion or mutation authority, so current positive completion remains impossible. Fixed-ledger omissions and `multi-build-copy` call sites enter the opaque physical-frame guard before any executor and also refuse because no trusted authority producer exists. Current retained-build publication code requires the exact tuple artifact-manifest `/4`, run-contract `/4`, browser-output `/3`, and result/score `/5`; run-contract `/3` is frozen inspection-only. Only a `downstream-only` closure beginning at browser output is eligible, while metadata-only or input-rejection publication and verification are deliberately refused until the closure retains a typed digest-bound witness from which Node can reproduce the rejection. No current production artifact exists. The retained 10:28 run remains immutable inspection-only evidence under artifact-manifest `/3`, run-contract `/2`, browser-output `/2`, and result/score `/4`; its end-to-end inspector rejects the retained identification match `/2` because semantic replay now requires match `/3`. The regression independently pins the replay closure, exact prepared-options/browser-output roles, and 14,896-byte diagnostic prefix, then applies frozen generation-local browser and semantic completion predicates to reproduce the five-step document projection and its seven non-authorizing failures; neither current verifier evolution nor this narrower check can reinterpret the full historical tuple as authority. Corrected search camera/world handedness, lineage-aware deferred/farther integration, authenticated source-render execution and provisional identity for the detached `/4` roles, a released broker and trusted physical-frame authority producer, generic deep backtracking, farther reach beyond the measured paths, and one-frame reconciliation with the official ledger remain open. `building-system.md` remains the primary owner of measured position; exact measurements repeated below support this specification's boundary contracts rather than an independent status claim.

On 2026-08-24 a focused current-source control rebound the retained fixed-workload Gate-3 compatibility boundary to catalog `/26`. Its retained first phase reconstructed and closed all four ordered `/13` parents to their reviewed source hashes through the narrowly frozen truth-only compatibility projection; only after every source snapshot closed did a second phase migrate the four immutable parents to `/15`. The active reviewed-additive successor and production migration contract now target `/26`, where `25269`, `28802`, `35787`, `11253`, `15254`, `41682`, `2877`, `3040`, `4519`, `32064`, `11212`, `33909`, and `78329` are thirteen post-`/13` additive definitions under overall truth `sha256:3226590b11882fea03d8a6370d4ca3c6c8201feaddb56882a243a69acba627e9`. The focused `/13`-to-`/26` parent binding preserves the four source hashes, pins synthetic current structural hashes `[b8f3baf0843074feec51fdfbbf47181c683d0629472ae8e552b6f8607c627391, cfd428b3800064c2358db2166c6051ef1f831f53df00bca6da644e11ebb3d79f, c683e42d275e43531d93bc1e7ff371c98cc8fbe3824c08f20ef3d6388b7bf183, a7c533c60deebe0f658b209cd58514c4f05456c5f9fb0d6c2b2ecc39aa71f7d7]` and production current structural hashes `[349f031229e98f9869f218849b9a7a84dd337cd74edc534639fb5753530cf6f7, aafbd400b179b22aa052b528338489e44ef3ba2d05dbe15d7e514648d64d1006, b67a24885463d1613f513112dbad1e9c080df09cb57177c7f4f9598098ab8046, 191b67add0491ab084e4216bef1cdf607f6d8b3eb1da25791c5a4b96ac1c624f]`, and preserves the historical `/26` catalog-session checkpoint of 3,607 source files at `sha256:e0080ad0e0e93d8e132f12dfff6f17d1807c462b90ae8697c3244666c6b3751a` while the current executable-source successor attests 3,660 source files at `sha256:fd74cde3eb9e5f809ae24c36253ba99720204f0b6b4221874957c949fae1779e`. Measured catalog coverage is 20 of 121 at `sha256:4e5df0a3aa5723b5aa5abef96e705562913c560a8a7452a2cf29582c2f05ba8f`. This remains a focused compatibility and catalog-coverage rebind only: it is not a fresh 14,172-row `/26` depth run, does not rebind retained scores, admits no production frontier, and publishes no document. The exported mechanical helper labels caller-supplied pins `caller-supplied-unverified`; only the production wrapper binds fixed module-owned source/current hashes and concrete production dependencies and records the frozen result in a private captured-intrinsic `WeakSet` as `reviewed-private-pins`. Structural hashes do not bind historical revision strings or canonical wire bytes; the retained source IDs and derived `/15` IDs remain historical coordinates while active `/26` truth is separately pinned.

The retained historical `/15` control completed the full step-7 narrowing workload, measured 14,172 narrowing-render rows in 30 batches, constructed 17 immutable parent-bound diagnostic leaf records, admitted no production frontier, and published no document; the renderer reports 14,188 total calls because each parent has four additional non-narrowing calls. It froze the complete browser input and proved one exact host/before/after JSON digest, while the isolated blank runner's storage began and ended unchanged. A shadow production ledger reproduced the historical boundary at 8,037 reserved rows plus the next 599-row request under the unchanged 8,192 cap; the diagnostic ledger accepted the complete narrowing work under 32,768. No retained executable source was loaded or run. Request, response, request-failure, page and worker observers remain attached through complete context closure, and final reconciliation checks redirect state from the retained Request identities; exactly the two post-authorization CSP controls may bypass routing, each as one exact `GET`/`script` `csp` failure without response or redirect, while every other observed request must route, executable request and captured-response URL multisets must match, and no page or worker may survive. A separate delayed pagehide/ACK browser control proves close-time response-body consumption appears in both manifests; that focused control is not part of the full diagnostic trace. Retained served-module evidence contains response metadata, byte counts and digests rather than bodies. Publication uses no-delete exclusive writes, file fsync, ownership checks, same-parent rename and bounded sequential read-back; on Windows this is cooperative detection rather than a directory-fsync seal, crash-durability guarantee, simultaneous-at-return proof or malicious same-user race defense, and a partial path created before failure is intentionally retained. Node replay re-enumerated current offers and replayed retained score policy, DAG and leaves without rerendering masks or scores from pixels. The follow-on retained depth control captures one dense prefix for each collision-free batch and one sparse isolated probe per parent-local cache miss through the same instruction renderer and camera, composes only strict-nearer 24-bit depth, and invokes the exact whole-scene path on all 115 equal-depth ties. A batch with any pre-existing `builtin:magenta` part instead captures no prefix or isolated probes and uses one exact whole-scene row per offer so color-keyed legacy semantics remain unchanged; the pre-acquired `1 + 2N` lease still dominates that `N`-render path. The measured step-7 prefix had no such color collision and reproduced every baseline row, candidate, batch, outcome, logical reservation, and all 17 leaves while charging 6,559 subject rasters—30 prefixes, 6,414 probes, and 115 fallbacks—under refundable worst-case pre-work leases whose final total is 1,633 below 8,192. Its 6,444 fixed RGB8 depth-pack passes and 2,210,292,000 pack pixels are reported separately from subject work. The production farther-step attempt now uses that subject ledger and composer without changing the limit, but the fresh required 1..12 run input-rejected before browser execution because authentic manifest-v6 `/5` identification answers, rebound coverage, and a current action ledger do not exist. Neither the optimized diagnostic nor that refusal advances the verified prefix or grants target, physical-frame, acceptance, or document authority. PDF fetching and executable-response capture materialize exact bodies before their browser-side ceilings, so these exact fixed-input measurements are not hostile-input memory-bound proofs.

The full printed source index and one execution request are separate contracts. A real-booklet request retains and independently checks exactly 359 dense source labels, while current `requestedLastStep` is an explicit safe integer from 1 through 50 that limits identity coverage, action-ledger assembly, executable panels, emitted reports and candidate-document mutation. Current run-contract `/5` digest-binds one canonical `panel-source` replay envelope to the exact retained PDF bytes plus the exact verified source-art-rebound role; the envelope carries that request, the complete `InstructionSourceV1`, and exact page-shape rows for panels 1 through `min(request + 2, 359)`, while frozen `/4` remains immutable replay only. Before prefix projection, replay reconstructs all 359 panels and manifest callout boxes, independently folds the retained page shapes into the bounded panel-face sequence, exact-compares every prepared execution/passive panel's page, face, bounds and boxes, and derives ledger panel-evidence commitments from the reconstructed source instead of prepared options. This closes self-confirming prepared geometry and face only inside the local diagnostic: the retained source, shapes and manifest remain derived inputs and do not authenticate PDF parser/render execution. A bounded later-panel suffix may provide raster evidence for the last requested placement, but rows above the request cannot expose their action or pieces, enter farther expansion, emit a report, invoke placement or mutate a document. Prefix completion cannot imply that the untouched source suffix was reconstructed.

Semantic identity derived from repeated embedded source art is narrower than rendered-pixel equality and much narrower than placement authority. The current local compiler may transfer an already verified element identity only when decoded RGB24 bytes, dimensions, kind, and the declared normalized ordered image closure agree; its only normalizations are page translation, exact-linked terminal-resource alpha-renaming, and nearest-milli numeric quantization. Every source member independently proves nonempty support, internal isolated/full support-RGBA equality, and zero on-support interference. Cross-member raster phase may differ after page translation and is not itself an identity gate. The resulting sparse roster carries element identity only: it cannot authenticate source execution, assign an official Brick, choose a frame, admit a catalog part, publish coverage or an action, mutate or replay a document, place a part, accept a candidate, or claim completion. Steps 51–359 remain source/index rows only.

The downstream prefix-50 semantic closure accepts that sparse roster only through its opaque verifier result and reopens crop evidence only for the exact 101 residual relations / 173 pieces: one manifest-bound callout-crop role and one manifest-bound inventory-crop role per residual relation. A separate exact 101-row original-resolution review-outcome input is 30,429 bytes at `sha256:286696d9254e89d027eb4a244d176cb8aff064991655347865ce8d3d5f1012b7`; every row binds identity, element, and both crop digests, and only explicit `same` outcomes can enter semantic authority. The retained 41 / 66, 57 / 102, and 3 / 5 reviewed relation/piece partitions plus the carried 86 / 147 safe roster close exactly 187 callout-to-element relations / 320 pieces through printed step 50. Independently, the exact official first-320 sequence and the semantic roster aggregate to the same 86 elements / 320 pieces with zero element-quantity differences. All 881 source rows and the 359-step index remain preserved and `suffixStepsReconstructed` remains false. The canonical `/1` result is 92,426 bytes at `sha256:1902af68a13cb629d9dbac1707c8c5c6998ec355cfcb6ef4dad2fc938f76155b`. Published part-number and color fields remain input-bound resolution metadata rather than catalog admissions or aliases. This local identity result grants no authenticated source execution, prepared run, physical assignment, frame or transform, catalog, coverage, action, production-ledger, placement, replay, document mutation, acceptance, or completion authority, and no current production consumer uses it.

Printed-step quantities do not authorize an independent cut through an official source order. The implemented step-31/32 reconciler accepts only the exact official source plus three reviewed semantic rows, proves that naïve cuts are short by 1, 1, and 2 pieces, and conserves the exact combined 14-piece multiset while repartitioning phases 49 through 54 into four-piece and ten-piece aggregates. Its serialized form omits direct physical source rows, but the exact open inputs and repository derivation uniquely reconstruct the five reviewed associations; schema `/2` declares that exposure and makes no confidentiality claim. The diagnostic result still cannot grant assignment authority, establish a frame or transform, admit a catalog part, enter a production ledger, place a part, mutate or replay a document, or claim completion.

Enumerate candidate placements from the settled prefix's free connectors on both sides of a joint, pruned by lattice, connectors, collision and the build plate. Score a step by its own evidence class—highlight, exploded ghost, or later-panel deferral—against bars derived from that panel's geometry, retaining N, N+1, and the first farther panel that reveals an otherwise occluded or ambiguous placement. Keep the document node's stable candidate identity while appending panel-bound hand, determinant, turn, and registration observations at N, N+1, and K; a reflected silhouette may propose an observation but cannot authorize physical truth. Name every refusal and `not-observable` limit, publish the number it refused on, add deep backtracking with retained alternatives, and report the reversal depth.

Exit: a contiguous prefix of printed steps is placed, each settled against a printed panel and each refusal named; the placed transforms reconcile with the official ledger in one frame; and the prefix length and reversal depth are recorded in `building-system.md` and driven deliberately.

The standalone atomic path can represent multiple distinct root documents under one batch reservation and serialize a terminal-respecting strictly increasing placement-step sequence into the two branch roles through the authority-absent finalizer. The finalizer has no prepared-run input and does not classify its gaps; only the complete detached `/4` reader can consume those roles while requiring each skipped prepared panel to carry exact transition evidence. The live runner still cannot mint the prepared execution authority needed to invoke that path, and public browser output remains `/3`.

### Gate 4 — Full set, and playback

Extend catalog coverage until the set's leaf designs are all present. Complete the run to the last printed step and play the build back step by step. Calibrate build-order and stability advice with physical-build feedback before making any physical claim.

Exit: the set rebuilds end to end from its printed booklet, the finished document reconciles with the set accounting, and playback reproduces the printed step order.

### Gate 5 — Co-evolution with `3d-maker`

When both repositories contain proven duplicate behavior, extract only the stable generic pieces: run manifests, lineage, evaluator interfaces, and comparison infrastructure.

Do not share brick documents, part catalogs, connection validators, procedural genomes, mesh generators, or persistence databases.

## Target success criterion

A user hands the app a printed instruction booklet, watches it build the set that booklet describes with every step checked against the booklet's own picture, sees each refusal named in the booklet's own terms, edits the finished model manually, exports LDraw, plays the build back, and replays the run to its declared level.

The system improves only through evidence: measured booklet numbers, typed failures, promoted regression fixtures, and reversible experiments. `learning-system.md` owns what a run retains, what lifecycle its candidates travel, and what must hold before any of that becomes a repository input.

## Removed product surface

The AI copilot that generated original models from text briefs was cut on 2026-08-07 and is neither deferred nor a later gate. Its generation package, harness, browser candidate UI and automatic acceptance ceremony are not product surfaces and no acceptance path remains. Detailed removal history belongs in the devlog.

Contracts still required by the booklet loop remain normative: untrusted model output, restricted `BuildProgram` compilation, deterministic validation, scoped consent for external transmission, retained run evidence and the target production replay and sealing contracts described above.
