# Booklet-run evidence, replay, and promotion

Status date: 2026-08-24

[`spec.md`](spec.md) owns product, domain, consent, trust, and authority contracts. This document owns the evidence around a booklet run: retention, lifecycle, replay, evaluation, and the conditions under which a finding may become repository knowledge. [`building-system.md`](building-system.md) owns current measured progress, and the [devlog](../devlog/summary.md) owns history.

## Implementation status

| Capability | Status | Boundary |
| --- | --- | --- |
| Per-step booklet reports and incomplete-prefix retention | Implemented in the opt-in local driver | Completed rows and the refusal survive an unfinished run; output is ignored, unauthenticated diagnostic evidence. |
| Content-addressed artifacts and test recorder | Implemented in `apps/companion` | Library and test namespace only; no production service or seal. |
| Test run ledger and lifecycle policy | Implemented | Hash-chained test events and transition checks exist; production identity, signing, and authoritative storage do not. |
| Closure manifest and replay-level derivation | Implemented for retained local bytes | The verifier can inspect closure integrity and reproduce downstream summaries from retained outputs. |
| Current artifact publication and verification | Implemented for downstream browser closures; no production artifact | Artifact `/4` accepts only contract `/3`, browser `/3`, and result/score `/5` with a `downstream-only` browser-output boundary; metadata-only and input-rejection closures are refused until they retain a typed digest-bound rejection witness. |
| Executable replay | Unbuilt | The current replay path deliberately refuses re-execution. |
| Production-sealed replay and tombstone lineage | Specified, unbuilt | Requires the released broker, signing identity, authenticated deletion events, and an independent verifier. |
| Candidate lineage, farther-frontier, and backtracking primitives | Identity, bounded inspection and atomic deterministic transition replay implemented; executable runner carry unavailable | Stable `document:sha256:*` candidates, parent- and origin-bound `lineage:sha256:*` identities, exact canonical-document snapshots and a normalized convergent farther DAG are implemented. A standalone atomic batch snapshots detached enumerator offers, reserves the exact request before compilation, deduplicates and independently replays physical transitions, and retains parent-specific edges over convergent children. Prepared-step/search bytes and all resulting evidence remain inspection only; every live origin, carry, panel, selection and acceptance authority issuer remains unavailable until trusted preparation, enumeration, compilation and measurement are composed. The frozen candidate-only legacy path remains separate. |
| Camera horizontal-hand registration and observation lineage | Empty-root integration plus live central adapters implemented; positive authority unbuilt | Browser-output `/3` atomically retains the eight canonical empty-root seeds and then refuses step 1. Trusted resolver results can be projected into bounded parent-specific central attempt lineages while converged parents share one measurement ID, but the generic packet explicitly says completion authority is absent and does not retain recomputable mask bytes. Fixed-action call sites still refuse before their executors. |
| Observation-source stages and parity | Exact stage inspection plus source-parity `/4` diagnostic implemented; full real probe retained | H is high-resolution cleaned art, P isolates at high resolution before downsampling, D downsamples before isolating, and W is the separate work-raster candidate. The 2026-08-22 run retained all 359 rows under measurement digest `sha256:39a810d8…`; exact packed masks and five dense production/candidate comparison classes are bounded and source/runtime/PDF/prepared/browser bound, while browser work-RGBA, policy, and derivation commitments remain opaque, the source-RGBA display is visualization-only, and authority is absent. |
| Five-panel source-parity calibration | Source-bound publication, authority-absent request, and receipt verifier implemented; production admission unavailable | A contract derived from all 359 prepared panels fixes five step/page rows; five bounded roles and ten PNGs close high/work RGBA, H/P/D, pairwise facts, and independently re-derived W. Publication reproduces the exact capture and source closure before deriving an audit-only identity. Exported challenge and receipt schema roots are structural only; the detached exchange parser checks cross-record binding and one caller-supplied wall-clock sample, while the bounded inspector alone adds held challenge identity, canonical Ed25519 verification, ordered start/finish wall samples and a private monotonic deadline across verification. It snapshots supplied trust before issuing one fresh challenge, binds the request/scope and expected review-presentation digest, rejects future or expiry-crossing consumption, and recomputes the next ledger transition, but its result explicitly has authority absent. The production event seam has no released broker or authenticated pairing/key/checkpoint source and always refuses; any future admission stays five-panel-only and authorizes no placement or completion. |
| Diagnostic-prefix retention and target equivalence | Deterministic proper-upright audit implemented in Node finalization; separate local publication implemented | Exact bound identity groups and complete catalog realizations must share one proper upright yaw plus integer-LDU translation; otherwise a structurally valid searched document remains `diagnostic-prefix.json` and canonical final document, hash, and part count remain unavailable. |
| Model-call transcript schemas | Implemented protocol | No broker-backed product producer exists. |
| Local part and panel proposers | Implemented as derivation scripts; one exact part-identification pilot available | The legacy printed-panel proposer can invoke a pinned CLI model outside the product. Strict `/5` first-pass execution is limited to one exact six-card development Gate 0 lineage with unauthenticated local assertion and provider evidence; re-ask remains disabled, no `/5` model-bearing call has completed, all retained evidence is ignored, and neither path is wired into the studio or real-booklet driver. |
| Embedded callout source-art measurement | Implemented as a focused authority-free local diagnostic | The exact current PDF, manifest-v6, features, pair truth, element map, callout identities and component geometry bind three page-18/page-20 image witnesses. The measurement selects one PDF image paint by component containment, pins its operator, PDF.js `5.4.149`, RGB24 stride, decoded bytes and linear transform, and separately proves one legacy/current crop pair differs only by complete exact-background bottom rows. It claims decoded embedded source-art reuse and lossless background-only recutting only: clipping, blending, interpolation, surrounding paint, semantic identity, `/5` confidence, coverage, placement and document authority remain absent. This exact real-data case is a conditional local probe that skips in a clean checkout without the ignored PDF and current outputs; its generic association and mutation controls always run. |
| Part visual-admission packets and reviews | Implemented as developer tooling; latest completed review is current `/23` `32064` | An independent LDraw render and the production catalog render share one eight-view camera packet; every capture is pending-only until review. Historical `/13` through consolidated `/18` packets and the separate `/19` `41682`, `/20` `2877`, `/21` `3040`, and `/22` `4519` packets retain their original-truth sidecars. Current `/23` packet `sha256:c4728ac4722ccecbb630e8a9a386df09514a1666caa047e27016482e21c785ca` and review batch `sha256:24f5d697c01e8c0e65018b0042067de272c4f393dd78510c1abe9b1142cc5565` bind the eight native-resolution `32064` pairs; seven are RGBA-exact and isometric differs by three delta-1 pixels with foreground IoU 1 and mean absolute RGB delta `2.44140625e-6`, and all eight were inspected at original size and reviewed `same`. No consolidated all-34 `/23` recapture is claimed; outcome transfer still requires exact old/current image and render-input identity. |
| Blind pair-judgement evidence | Implemented as retained input and consumer | It is not an integrated live checker call. Current panel verification is deterministic pixel scoring. |
| Transition classification | Implemented as prepared local input | The current classifier is raster-blind, so its label is not a visual witness. |
| Independent evaluator and automatic promotion | Specified, unbuilt | No process may grant itself authority by satisfying a local score. |

## Unit of work

A booklet run reads prepared printed steps, compiles an ordinary step against one settled `BrickDocument` prefix, enumerates candidate placements, and compares a render with a printed panel at that panel's fitted camera and face. The registration primitive can measure both determinant cosets, and `RealBuildPanelCameraRegistration` retains hand, determinant, registering panel, turn, and pixel shift as panel-local observation state without claiming a physical transform. The resolver atomically reserves complete eight-way angular families and keeps a document's hash-bound `candidateId` stable while assigning each seed or observation a unique legacy resolver lineage. The new central identity layer deterministically binds the same document candidate to a unique parent, run-root origin, prefix step, and local decision or evidence identity; live scalar and multi-parent adapters reproduce selected score groups without translating legacy IDs into authority. The normalized farther substrate preserves convergent A-to-X and B-to-X branches as distinct lineages over one exact candidate snapshot. The standalone atomic compiled-branch batch preserves every parent proposal even when exact physical work converges, retains one canonical child snapshot and transition for that work, and derives a distinct child lineage edge for every parent. Its exact preflight request survives compiled, budget-refused and failed outcomes; one aggregate reservation happens before the first compile, and any budget, compiler, replay, alias, child-byte-cap or aggregate-closure failure emits no child frontier. The frozen base64 wire binds byte length and digest and each decode produces fresh storage, while its original observation generation, selection, accepted transition/document, score authority and completion authority remain absent. A separate compiled-observation-closure `/1` sidecar binds those exact unresolved lineage bytes to MSB-packed built, excluded and candidate masks, a bytes-derived prepared score policy, deterministic optimal registration and camera-group selection. It can reproduce one convergent accepted-transition summary for inspection, or preserve raw-empty and typed unverified refusals, but its source and camera descriptor digests remain opaque until a browser boundary proves them. A separate branch-evidence index binds dense slices in run-wide compiled-evidence and observation roles without interpreting their semantics. Its refusal-only local semantic consumer rejects orphan closure tables and cumulatively charges retained lineage tables, closure observations, source/camera rows, selection/accepted-transition references, root/child canonical-document bytes, proposal and raster work before bounded root reconstruction, then derives and charges conservative compiler work-policy units including step preparation before semantic replay; typed observation reuse requires the exact replay-admitted work inspection. It cross-binds the indexed and prepared step, action/compiler metadata and ordered proposal identities, requires legacy observation-generation, selection, and acceptance fields to remain empty, separately verifies the typed closure and any nonempty committed raw role, and returns an authority-absent summary of local lineage and observation calculations. The observation-source tracer separately retains exact H/P/D stage bytes, while source-parity `/4` compares five production/W mask classes through a source- and runtime-bound opt-in browser closure; neither path joins those pixels to a compiled child, observation lineage, or run report. Its executable origin, carry, transition and scored-panel entry points currently refuse every caller because no trusted producer can mint their opaque inputs. The live runner therefore still records only the eight root seeds under browser-output `/3` and deliberately refuses step 1; later rows remain causally blocked and no branch becomes document truth.

A printed step is the unit of evidence. The implemented local row records the fields listed under [Implemented local bundle](#implemented-local-bundle), including bounded farther evidence when invoked, and links image filenames whose bytes are hashed and cross-checked by the artifact manifest; the complete generic packet described under [Target evidence contract](#target-evidence-contract) does not exist yet.

The comparison is closed: does this exact render agree with this exact printed art under the registered measurement? A model or human may help establish an input claim, but neither may declare structural validity or bypass deterministic compilation, collision, connection, scope, or accounting checks.

Atomic batch preparation now groups a bounded ordered frontier by exact branded root snapshot instead of assuming one shared document. Its additive exact-lineage digest commits the existing structural lineage ID, the exact parent lineage, canonical-byte digest and UTF-8 length, so structurally equal but byte-distinct documents cannot substitute for one another while the generic lineage `/1` contract remains compatible. Distinct exact documents remain distinct physical compiler work, exact-byte-convergent lineages must share one group, every enumerated parent must retain frontier order, and the producer proves a worst bounded zero-frontier terminal envelope fits before the single ledger reservation covers the complete cross-root proposal set. Root, unique-child and final-wire ceilings remain separate bounded limits: an oversized success envelope may consume the admitted batch and fail late, but it closes into the pre-admitted terminal evidence with no child frontier. The standalone deterministic role finalizer rejects row and observation Proxies, derives a terminal-respecting strictly increasing placement-step sequence from module-produced batch results, admits aggregate compiled and observation role lengths plus cumulative lineage, closure, raster and compiler work before any raw observation-role copy, rechecks lineage and closure digests across the measure/copy boundary, verifies optional closure semantics against the exact lineage and prepared observation policy, writes dense references and digests into unexposed final storage, self-parses the finished roles, and exposes only fresh byte copies with authority absent. It receives no prepared run and therefore cannot classify gaps; the complete detached `/4` reader is the later boundary that requires each skipped panel to carry exact prepared transition evidence. Failed and budget-refused terminal batch evidence can be transported without observation bytes, but no later step may follow it. This finalizer is not the live runner, a capability issuer, or a source/camera/terminal provenance proof.

## Visual observation envelope

The target evidence packet for a placement made at step N includes panel N, panel N+1 as the minimum later witness, and the first farther panel that actually reveals the placement when N+1 occludes it or remains ambiguous. N+1 is a floor, not a guarantee, and looking ahead stops only at a revealing witness or a named evidence limit.

Every visual claim is scoped to the exact source crop, render, camera, face, horizontal hand and determinant, quarter turn, registration, mask, and view in which it is visible. A hidden internal surface, an occluded connection, or any feature absent from all retained views is recorded as `not-observable`; unseen pixels cannot certify it. Part admission separately inspects matched top, bottom, front, back, left, right, isometric, and underside-oblique views because a booklet sequence may never expose every surface.

The historical `/13` through consolidated `/18` tranches, historical `/19` `41682` outcome, historical `/20` `2877` outcome, historical `/21` `3040` outcome, historical `/22` `4519` outcome, and current `/23` `32064` outcome are recorded in [`part-model.md`](part-model.md#current-catalog). The `/23` native-resolution review batch closes its eight `32064` exterior pairs; its sidecar does not turn hidden interiors, connector-source claims, conservative collision recipes, continuous axle travel, axle-through-bore collision relief, grip, stability, insertion access, or trusted placement into visually observed truth.

Printed step 4 is the concrete failure that sets this rule: its underside panel visibly shows hollow clutch rings, ribs, walls, and cavities, while the former candidate render showed an almost solid slab. [Part-model catalog truth](part-model.md#render-only-promotions-and-remaining-physical-limits) owns the render correction and its preserved conservative collision; an old step verdict cannot be inherited across that render-truth change, and the images still cannot prove collision.

The frozen legacy real-booklet path implements a bounded subset of that packet. When own-panel scoring cannot separate step N and N+1 remains unrevealing, the generic path retains every N parent with its exact cached N+1 score render, carries one configured intervening step under shared candidate and narrowing ledgers, and scores a conditional K only after the entire carry succeeds. The calibrated step-5 shortcut is narrower: only the exact two retained origins, their piece witnesses, prepared step-5/6/7 data, run options, input digests, and captured source closure admitted by the reviewed attestation may be scored directly against panel 7 after panel 6 remains ambiguous. Retained 10:28 legacy evidence scored those origins `0.81657223796034` and `0.9367520589707421`, selected the second, and produced an eight-piece searched prefix through step 5. Step 6 then retained four unseparated origins; the step-7 carry expanded three parents for 2,218, 2,169, and 3,650 narrowing renders and retained eight lineages at aggregate 8,037 before refusing the next 599-render request against the shared 8,192 ceiling, with no partial frontier. Current browser-output `/3` refuses at step 1 until stable candidate and unique observation lineage are threaded through this path. Neither generation provides arbitrary farther-panel scanning, generic deep backtracking, or an integrated vision checker.

The production farther-step attempt now replaces that carry's whole-scene narrowing charge with a refundable physical subject-raster lease and an exact 24-bit depth composition route: one dense prefix for each collision-free batch, parent-local sparse isolated-probe reuse, strict-nearer composition, and whole-scene fallback for every equal-depth tie. Because the legacy probe mask is color-keyed, a batch whose prefix already contains `builtin:magenta` conservatively skips prefix/probe composition and uses one exact whole-scene render per row; this is still dominated by the pre-acquired `1 + 2N` lease. This changes only how the same mask rows are produced and charged; it does not authorize a selection, advance a prefix, weaken the 8,192 ceiling, or change the immutable-candidate and no-partial-frontier rules.

## Authority boundaries

| Actor or loop | May change | May not change |
| --- | --- | --- |
| Booklet driver | Its local candidate set, settled diagnostic prefix, and ignored run artifacts within declared budgets | A user document, catalog truth, validators, scoring policy, consent, or application code |
| Curator or developer | Quarantined tests, fixtures, thresholds, templates, and code changes through the repository workflow | Protected evaluation inputs, production identity, another actor's consent, or its own proof standard |
| Test companion | Test-namespace artifacts and ledger events | A production seal, accepted namespace, user authorization, credential, or physical claim |
| Future independent evaluator | A report under a predeclared policy | The challenger, benchmark definition, consent record, or protected holdout it evaluates |
| Human maintainer | Reviewed repository and product-policy decisions | Retroactive alteration of immutable evidence |

An experiment may change the system under test or the evaluation contract, never both in one result. Otherwise a weaker validator, easier benchmark, shifted camera, or tuned threshold can masquerade as improvement.

## Evidence contract

### Implemented local bundle

The current-generation run contract binds input digests for its booklet, prepared panels and callouts, action ledger, transition classifications, identification inputs, run options, application source/build closure, runtime environment, and any model-derived input that an execution actually consumes and lists in its contract. The separate panel-placement readings are not consumed or bound. Binding a transition-classification file does not make its raster-blind labels visual evidence. The input chain is regenerated deliberately and in dependency order; the [real-build runbook](../runbooks/real-build.md) owns the commands. No production run has yet exercised the complete current schema tuple.

The current result records local unauthenticated authority, status, requested and expected step counts, assembled target, input and completion failures, retained step rows, optional canonical final document and structural hash, canonical final part count, an optional diagnostic prefix, and elapsed time. After exact report-to-binding-to-canonical-part identity, metadata, step ownership, and transform checks pass, Node finalization evaluates the official transforms post-search under all four proper upright yaws and one global integer-LDU translation. Same-step design/material/catalog/color groups are matched as exact multisets, and each placement must match complete connector, collision, allowance, bounds, and flat render triangle-and-normal realization modulo catalog-proved upright self-symmetry. Official transforms never enter candidate enumeration or scoring, and a structurally valid reflection cannot populate canonical fields.

Each retained step row records its step and page, action and action-evidence digest, expected/attempted/placed counts, outcome, prerequisite facts, validation target/truth/validator hashes when validation ran, fit and camera facts, highlights and arrows, per-piece search and score data, whole-step/deferral/exploded evidence, elapsed time, and panel/build PNG filenames. Browser-output `/3` also carries nullable nested panel-camera evidence `/2`: its measurement commitment binds PDF digest, panel page/crop/face, exact panel/build capture digests, camera, raster dimensions, built/excluded/render-mask digests, silhouette score, centre, shift, hand, determinant, and turn. The exact mask bytes are not retained, so deterministic score recomputation is unavailable and the evidence parser refuses to let such a measurement authorize positive-piece completion. The standalone observation sidecar is the first independently recomputable successor primitive: it caps one raster at 1,048,576 pixels and aggregate comparison work at 134,217,728 pixel visits, rejects non-zero packed padding, verifies exact mask slices once, reproduces the live centroid-seeded multiscale registration and target-space exclusion, and applies score equality plus strict margin policy. Own-panel evidence may register at the compiled step, including step 359; lookahead must use a strictly later panel. Equal best camera groups remain unresolved even for one structural candidate, while convergent lineages using one camera stay distinct and may select one shared child. Typed failures remain explicitly unverified rather than replayable refusals. The sidecar is not yet a browser report and does not establish that its opaque panel, crop, camera-recipe or renderer commitments came from the prepared PDF and rendered child. When bounded farther search runs, the row also binds the origin parents and hashes, exact piece witnesses, parent-child lineages, per-panel scores, shared candidate/narrowing/panel budget facts, typed refusal or family-only decision, and dense exact source/candidate capture metadata. The artifact manifest verifies the projected capture paths and PNG bytes separately; the row does not carry a digest for each PNG.

Observation-source parity is a separate calibration-only evidence class. H is the exact high-resolution cleaned-art mask; P is high-resolution isolation followed by downsampling and is the production assembly mask; D is the downsample-then-isolate counterfactual; W is the candidate derived independently from the snapshotted work raster. The stage trace retains seven exact MSB-packed H/P/D and intermediate masks, topology, bounded connected-component facts, and an explicit missing-W marker because W is not coupled into that trace. Source-parity `/4` derives W separately and retains production, candidate, and XOR packed evidence for assembly, own-panel source, own-panel exclusion, built, and exclusion classes across the dense prepared sequence. Its closure verifies the bootstrap source lock, exact execution mirror including the PDF, served responses and retrievable served source bundle, checkout/runtime identity, prepared-panel digest, exact browser-result bytes, bounded publication, and immutable content digests. The 2026-08-22 real-booklet probe retained all 359 rows under measurement digest `sha256:39a810d8a30f28fb93bb619f2ce522ce01e95146e8fe14666d6606270c0cb615`, with aggregate assembly IoU `0.9655393602555469` and 256 differing panels. It deliberately labels the work-RGBA, candidate-policy, and candidate-derivation browser commitments `opaque-browser-assertions-not-independently-reproduced`; the diagnostic's point-sampled source-RGBA column is for visualization only and cannot reconstruct or authenticate source pixels, and every authority remains absent.

The fixed calibration contract is derived from the full 359-panel manifest but retains only step/page rows 90/79, 101/87, 346/213, 358/218, and 359/219. The exact-five browser constructor can retain five bounded roles covering exact high/work RGBA, stage manifest and H/P/D masks, and W masks plus ten lossless PNGs; Node reconstructs the transient browser wire, checks every role/range/digest, decodes the PNGs to the retained RGBA, replays the stage trace, and independently re-derives W. The separate source-bound publisher requires the exact full 359-row manifest bytes and a held source snapshot/provenance closure, then cross-binds the PDF, exact-five contract, transient browser-result bytes, bootstrap/execution/served source, checkout, and runtime facts. It derives an audit-only execution identity internally, writes the capture manifest, full manifest, five roles, ten PNGs, and provenance beneath the identity-addressed run directory, and atomically replaces the fixed discoverable summary only after all referenced bytes exist; the public retained reader reopens every byte class and reproduces the identity. The real-booklet exact-five opt-in retained this publication under audit-only execution identity `sha256:979157ed12ef36fcd59a85aba13268d56a47d77ee886070cd31221805d756462`; it remains `pending-unreviewed` and authority-free. A calibration truth packet can bind packed MSB mask bytes and claim `human-reviewed`, `external-to-packet`, and `exact-human-inspection`, but the parser records only `unverified-external-review-claim`. The adjudicator has no review-authority input: absent truth yields `human-truth-not-supplied`, drift yields `claimed-review-evidence-drifted`, and either W mismatch or exact W equality yields `human-review-authority-not-supplied`; every path is `needs-adjudication`. The exact-five request inspector reparses the branded publication, full contract and retained W bytes and creates a private authority-absent request only for exact equality while deriving the expected review-presentation digest from its exact execution, calibration, truth, comparison and step identities. The bounded receipt inspector parses fatal UTF-8 canonical JSON, charges bytes and nesting, verifies Ed25519 over the exact receipt plus key header, snapshots caller-supplied pairing/key/release/checkpoint state before issuing the exact two-minute CSPRNG challenge, binds that challenge to the request/scope and expected review presentation, rejects future or expiry-crossing consumption, recomputes the domain-separated consumption-event hash and resulting ledger root, and rejects reused broker event IDs and transitions in process. It caps in-flight inspections at 64 and retained successful receipts at 4,096, then deliberately returns authority absent because those bounded process-local checks are not durable consumption. Production admission still always refuses because no released broker or authenticated production trust source can create the module-owned runtime event capability. A Vitest module mock exercises that capability check plus request and review-presentation binding, fresh-return timing, per-event identity, sequential replay and reentrant replay only. A successful future admission would be privately capability-checked and would authorize official-frame equivalence for those five source-parity panels only; physical transforms, placement, fixed actions and dense 359-step completion remain explicitly absent, and the audit identity is never a bearer capability.

The challenge and receipt JSON Schemas deliberately stop at closed single-record wire validation. Their legacy exported validator names remain structural aliases with explicit documentation; the shared detached exchange parser cross-binds namespace, purpose, scope, request, review presentation and nonce and checks pre-verification time against caller data, while shared four-sample wall and three-sample monotonic timeline contracts are applied by the bounded inspector before and after cryptographic verification. The exchange parser itself is neither a freshness oracle nor an admission boundary because it owns no live challenge object, clock, key, ledger or one-use state.

The standalone browser-output `/4` tuple is a new replay evidence class, not a live run. Before reading roles, its public reader and provenance boundary snapshot exact closed data-only non-Proxy inputs so accessors, extra keys, aliases, or reentrant mutation cannot change the checked tuple. The envelope commits prepared-run bytes plus external branch, source, camera and zero-piece-transition roles. The source manifest retains only 359 bounded descriptors while the reader streams exact per-panel high/work RGBA and H/P/D/W source/exclusion roles; source records and arrays reject Proxies, the synchronous raster pipeline refuses if any captured realm primordial drifts, and each session marks its current step in flight before byte-copy or derivation work and clears that reservation in a `finally` path. It binds every panel to the prepared PDF, page, crop and callouts but records source render execution provenance absent. Camera evidence binds each compiled exact child byte string to its D4 recipe, renderer snapshot and matrices, retained RGBA, packed masks, and independently replayed registration; exact range-and-descriptor aliases are permitted, every other range must be disjoint and dense within a closure, and an explicit base maps each closure-local mask layout into the ordered run-wide role across multiple steps. A source-only closure contributes no camera bytes. The terminal reader replays typed selected branch edges over the exact current multi-root lineage frontier, requires convergent lineages to share one transition/receipt/validation, applies transition panels as exact validated `BuildStep` deltas, derives prepared identity-to-canonical-part bindings, and checks report order, identity bindings and terminal canonical bytes. A selected report is the exact projection of its replayed edge and uses the `/4`-only `compiled-observation` mechanism; report-native highlights, arrows, piece search diagnostics, PNGs and other legacy visual duplicates remain neutral, and report plus total elapsed time are fixed to replay-neutral zero because no live timing role exists. Missing-role and terminal failures are derived from their exact step, reason, compiled lineage, closure and observation-role identity rather than accepted as caller-authored typed failures. Completed fixed-ledger and `multi-build-copy` rows refuse because no physical-frame authority or deterministic fixed-action replay exists. The output identity is deterministic inspection only and cannot establish source execution, physical placement, user acceptance, completion, or mutation authority.

An incomplete run retains every readable row that matches its prepared input and then records the refusal that stopped the prefix. Malformed envelopes may be rejected wholesale; an ordinary reproduction defect does not erase valid earlier evidence. Browser-output `/4` refuses a failed tuple with no report because its outer caller-authored failure has no evidence-derived witness; a future empty-prefix input rejection needs its own typed digest-bound role. After the first accepted failed row, every later row must be an exact unattempted zero-placement causal block naming that step, with no validation, candidate, render, camera or document drift.

The live runner deep-detaches the complete JSON-like run input before its first await, including panels, order and page bindings, digests, accounting, coverage, and budgets, then re-snapshots the caller input after module and PDF preparation and refuses any drift before raster, search, or placement. It sorts detached panels globally by printed step and checks contiguous execution before rasterizing each page. Pages are rendered on demand; module, page, PDF-document, and loading-task failures use bounded non-invoking diagnostics, page-render failure retains the exact preceding document and causally blocked suffix, and cleanup failures preserve completed reports, document bytes, and input digests in typed failed browser output. The `/3` reader bounds terminal document bytes, depth, nodes, steps and parts, parses `documentJson` once, independently reconstructs only declared transition prefixes, exact-binds each action and action-evidence digest to its prepared panel, and binds the terminal document to accepted hash and part continuity, global validity, ordered step ID/index/semantic-name/owned-part facts, canonical root metadata and manual provenance. One genuine validated zero-piece canonical `BuildStep` changes the accepted hash without changing the part count; an absent, duplicate, unvalidated or hash-preserving no-op step cannot advance continuity, an unresolved root must terminate at the exact canonical empty document, a root rendering failure may retain bounded elapsed time but no successful raster/candidate work, and every causal suffix row retains zero elapsed and zero raster/candidate work.

When target equivalence is unreconciled but the retained document, identity bindings, build sequence, validation report, and searched transforms remain coherent, publication writes the exact candidate document to `diagnostic-prefix.json`. Current publication code accepts only the exact generation tuple artifact-manifest `/4`, run-contract `/3`, browser-output `/3`, and result/score `/5`, and only when replay is `downstream-only` from browser output. Both publication and verification reject metadata-only or input-rejection closures because no typed digest-bound rejection witness currently lets Node reproduce the exact refusal and score; no production run has yet been published under the current tuple.

Retained run `2026-08-12T10-28-52-560Z-34694c87c62e-26128a38-4d9e-4294-9091-6aae9b3ca367` is immutable legacy inspection-only evidence under artifact-manifest `/3`, run-contract `/2`, browser-output `/2`, and result/score `/4`. Its end-to-end inspector currently rejects the retained identification match `/2` because semantic replay now requires match `/3`. The ignored-run regression independently hard-pins replay-closure, prepared-options, and browser-output bytes and applies frozen generation-2 browser, farther, capture, atomic report, completion-failure, and identity predicates only to that exact browser role. That narrower check does not authenticate the full tuple, reproduce its diagnostic document under current semantics, make it a current-generation publication, or establish that it is the official model.

The target-equivalence audit is bounded to 1..1,464 unique identity/part rows with safe-integer upright transforms and fails closed on unknown catalog parts or unsupported realization layers. Its per-audit cache retains catalog self-symmetry results by definition and full comparisons by definition plus relative transform, so repeated identical parts do not regenerate the same surface along every group-matching path. A diagnostic D4 reflection must map every origin, preserve the connector, collision, allowance, and bounds layers, and reproduce the independently inferred compatible-contact multiset; its exact flat render topology is reported separately and never converts determinant -1 into completion.

Current gaps are explicit. The run contract does not separately bind a catalog snapshot or complete truth bundle; a step row does not record document revisions, a before-and-after structural-hash pair, inline mask bytes, or a complete arbitrary-depth N/N+1/first-revealing-farther observation packet; and `score.json` does not contain reversal depth, retained byte count, replay level, or the structured target-equivalence audit. The first `/3` report retains the eight unregistered horizontal-hand roots, but existing placement scoring, deferred candidates, and farther schemas still use candidate-only identities and cannot carry the distinct observation lineage through a positive placement. Replay closure and artifact-size facts live in separate manifest files, the bounded one-intervening-step driver has no generic reversal depth to report, and the implemented post-search audit diagnoses but does not repair the search camera/world hand. Source-parity `/4` proves a diagnostic execution closure, not browser-output `/4`: it does not independently reproduce its browser commitments, connect W to a human-authorized review, connect source masks to compiled children and D4 camera observations, or bind report/document continuity. Panel-local registration is never physical-transform authority, current browser reports lack exact mask bytes, and no trusted physical-frame authority producer exists. Fixed-action call sites now make that absence executable by returning `fixed-ledger-frame-unresolved` before reading their executor.

### Target evidence contract

The complete target run input binds the catalog and truth snapshots and every retained nondeterministic response in addition to the implemented closure.

The complete target step record adds document revision and structural hash before and after the step, per-row source and render digests, the bound N/N+1/first-revealing-farther observation packet with explicit `not-observable` outcomes, the step-1 face seed, rotate-icon sequence and fold, derived panel face, all eight scored or empty hand-and-turn hypotheses with their determinant and translation, any exact-tie refusal, and candidate-lineage provenance that keeps one stable document-node candidate identity while binding each unregistered root seed or panel-camera observation to a unique lineage and explicit parent so the decision can be reproduced without joining unbound filenames.

The complete target summary adds deferral and reversal totals, deepest reversal, retained byte count, replay level, termination policy, and every finding that prevents completion. These fields become current only when their schemas, producers, and verification tests exist.

Current local publication content-addresses retained artifacts and enforces the render, candidate, role-byte, and stored-byte limits declared by its participating contracts. A closure may claim only the replay level its required bytes support; broader target budgets are stated under [Operational guarantees](#operational-guarantees).

Raw evidence lives only under ignored `output/` and `var/runs/` roots. It enters Git only when review promotes a minimized, licensed, secret-scanned fixture, golden, benchmark, or contract input.

## Candidates and lineage

Every alternative is immutable. A repair, replan, or rejected placement is a child that references its parent and exact base state; it never overwrites the branch that produced counterevidence.

Structural hashes identify duplicate states, lineage identifies cycles, and metric history identifies oscillation. Backtracking walks to the shallowest ancestor with an eligible untried child and reports how many steps were undone and the maximum reversal depth.

Those rules are implemented in the generic build-tree and backtracking libraries. The frozen candidate-only real-booklet path rechecks parent and child document hashes around callbacks, retains exact atomic catalog-part/color witnesses, uses shared aggregate candidate and narrowing ledgers, and admits a family-only decision without falsely settling its ambiguous descendants. Candidate-only browser-positive and mutation fixtures are now explicitly checked through the frozen browser-output `/2` inspector rather than relabeled as current `/3`. The panel-camera resolver detaches, canonicalizes, freezes, and verifies the claimed document before reservation; copies built and excluded masks; checks shared-ledger state around the hash, reservation, and each renderer callback; requires an observation panel strictly after the candidate prefix; and reserves complete eight-way hypothesis families atomically. Browser-output `/3` consumes that boundary for the exact canonical empty root, retaining eight unregistered seeds with null observation and shift under stable candidate and unique lineage IDs, but it refuses step 1 before placement because the candidate-only deferred/farther path cannot carry those lineages. A cross-hand tie remains unresolved, and a thrown or malformed render preserves attempts and successful counterevidence but authorizes no selection. Nested evidence `/2` closes hostile descriptor, accessor, proxy, and array boundaries with bounded non-invoking diagnostics and mutates lineage-continuity state only after a whole row validates; it commits capture, camera, raster, mask-digest, score, centre, shift, hand, determinant, and turn facts, while the absence of exact mask bytes keeps positive placement non-authorizing. Fixed-ledger omissions and `multi-build-copy` call sites return `fixed-ledger-frame-unresolved` before their opaque executor; only a future trusted audit producer could mint the required physical authority. The exact historical step-5 shortcut measured a panel-7 separation of `0.12017982101040214`, but its selected eight-piece document remains a diagnostic candidate because the catalog-realization audit finds no proper upright target frame after step-2. The historical next carry retained eight lineages from three of four step-6 origins at 8,037 renders and refused the next 599-render request before work. The frozen `/2` parser split, stable central candidate/lineage identities, camera adapters, bounded inspection projection, normalized convergent farther DAG, standalone atomic transition replay, exact source-stage inspection, source-parity `/4` diagnostic closure, source-bound exact-five publication, authority-absent exact-five request, branch-role transport, local branch semantics, streamed source/camera/transition roles and the detached browser-output `/4` reader now exist. The remaining implementation boundary is executable integration: a private live-run producer must bind trusted preparation, enumeration, exact source execution, visual measurement and shared budgets to that tuple; a released broker must authenticate the exact-five user event without exposing a local mint; and trusted physical-frame replay must precede any completed fixed action. Result/score `/6`, artifact-manifest `/5` and replay closure `/4` must then carry the independently verified tuple without weakening the `/3` root or frozen `/2` bytes. Generic ancestor reversal, arbitrary-depth carry, and reversal-depth reporting remain unbuilt.

The retained 2026-08-23 step-7 workload control is a separate local diagnostic, not executable replay. It pins the historical artifact, score, prefix, prepared-options and PDF bytes as hostile data. The retained control first reconstructed and closed all four ordered `/13` parents to their reviewed hashes through a narrowly frozen truth-only compatibility projection that checked the exact predecessor truth digests, catalog ordinal basis and source allowlist, transiently presented `/15` truth only to current operation admission, restored `/13` truth and refused new part IDs. Only after all four source snapshots closed did a second phase migrate those four immutable snapshots to additive `/15`. The active reviewed-additive successor and production migration contract now target `/23`, where `25269`, `28802`, `35787`, `11253`, `15254`, `41682`, `2877`, `3040`, `4519`, and `32064` are ten post-`/13` additive definitions under overall truth `sha256:af781e7356e28622fb13afcb571d28495a0962d6aa78ef70d988126a9c4aeefb`. The focused compatibility check preserves all four source hashes, binds synthetic current structural hashes `[9760b650673c89b4edb445cafdce1df7ab54ee48e42129a259183542c56d6692, 887dbb18da017ff4bfd5d9a1b1e63efeadf4ac7b80989af33003b62428b178f7, 3eea35e613f45a9a3e47a0d9ae5b210ac6ad302ac0113288dc58cb2f63f1a793, 29b5249032bf20d57c2b947297ccc555e3a0ee10b382653c9bc6c2f09568b68b]` and production current structural hashes `[5cd71f676484f0124c9c7a0842c46a5ff0561a5832728411098ef69ede462469, 66222eee09cb2921570e99e395af7871b22d2230285fb0750cc80cd08e31aed9, aa153a5047a81683f61473fedb70af697f908e3d2be6e2660a81aad1a4477781, 7220b65481289e1f8033d2694c9306c51652384220670d318029cadb7cfd33b3]`, and re-attests 3,598 source files at `sha256:ca8411a777c37f97d070f095d0948fd57258a00237bacedf5292ce057621598e`. Focused Gate 3 plus frozen-artifact checks pass 52 of 52 cases, while measured catalog coverage moves to 17 of 121 at `sha256:c5b20fa64d9d0457822de5bbecc3a66f4f1633b2004e7e23342a3c95f99c6633`. The exported mechanical helper labels caller pins `caller-supplied-unverified`; only the production wrapper binds fixed module-owned hash pairs and concrete production dependencies and returns the privately `WeakSet`-branded `reviewed-private-pins` result. Those structural hashes do not bind historical revision strings or canonical wire bytes; the retained source IDs and derived `/15` IDs remain historical coordinates while active `/23` hashes are separately pinned. This is a focused `/13`-to-`/23` compatibility rebind only: no fresh 14,172-row `/23` depth execution or score rebind is claimed, no production frontier is admitted, and no document is published.

The retained historical `/15` control ran all four parents in fixed source order under a diagnostic-only 32,768 ceiling. The completed trace records 14,172 narrowing-render rows, 30 atomic batches, 17 diagnostic leaf records, four matching parent start/terminal pairs, renderer disposal and ledger equality; the renderer reports 14,188 total calls because each parent has four additional non-narrowing calls. Its shadow production ledger reproduces 8,037 reserved plus the next 599-row request while the real 8,192 policy remains unchanged. Node replay independently re-enumerated the then-current `/15` offers and replayed retained score policy, DAG and ordered leaves, but did not rerender masks or scores from pixels. No retained executable source is loaded or run and no production frontier or document is published; the complete browser input is frozen and has one exact host/before/after JSON digest, isolated blank-runner storage begins and ends unchanged, and the output remains local-diagnostic-only ignored evidence with no production or document authority. Request, response, failure, page and worker observers remain attached through context closure, and final reconciliation checks redirect state from the retained Request identities. Exactly two post-authorization CSP probes may be pre-route, each one exact `GET`/`script` `csp` failure with no response or redirect; every other observed request must route, executable request and captured-response URL multisets must match, and no page or worker may survive. A separate delayed pagehide/ACK browser control proves close-time response-body consumption appears in both manifests; the full diagnostic itself has no close-time control URL. The source closure retains executable response URL, status, type, length and digest evidence rather than bodies; both PDF fetching and executable-response capture materialize their exact bodies before enforcing their browser-side ceilings, so this is a fixed-workload, accounting-bounded control rather than a hostile-input memory-bound proof. Publication uses no-delete exclusive writes, file fsync, ownership checks, same-parent rename and bounded sequential read-back; on Windows this is cooperative detection rather than directory sealing, crash durability, simultaneous-at-return state or malicious same-user race protection, and any partial path created before an injected failure is deliberately retained.

The retained historical `/15` final-source depth control at `output/gate3-step7-depth-diagnostic/runs/2026-08-23T22-34-21.749Z-52fd9638354f` runs that complete denominator through the real instruction renderer after cache identity was bound to the actual placed catalog/color/transform descriptor rather than occupancy equivalence and pre-existing probe-color collisions were made exact whole-scene batches. It reproduces all 14,172 ordered logical rows, 30 batches, 17 leaves, and the baseline render-row, candidate, batch, outcome, and logical-reservation digests exactly with 6,559 charged subject rasters: 30 dense prefixes, 6,414 parent-local sparse probe misses, and 115 exact whole-scene fallbacks for equal-depth ties; this particular step-7 prefix had no probe-color collision. The fixed RGB8 pack totals 6,444 passes and 2,210,292,000 pixels but is a readback pass over completed subject surfaces, not an omitted subject-raster charge. The sparse cache records 7,758 hits, 1,294 evictions, 1,280 peak entries, 4,805,936 peak fragments, and 38,447,488 peak payload bytes. Each batch acquires its worst-case lease before work, every acquisition proves `committedBefore + maximumRequested <= 8192`, unused capacity is refunded, and final committed work is 6,559. The production farther-step attempt now constructs this ledger and passes the composer through the driver; legacy callers remain on the prior exact whole-scene accounting unless they explicitly supply the subject ledger. The retained run remains local diagnostic evidence with no production frontier or document authority.

Exploratory structural-pruning calculations do not change that ledger because no current hash-bound analyzer or retained per-row proof artifact reproduces their counts. Conservative projected-fill zero bounds, same-batch mask reuse, and positive upper-bound pruning remain hypotheses until exact bound inputs, certified row identities, skipped-row masks and baseline selection parity are retained. Any implementation also needs a pre-work certified-cost contract rather than silently charging less work after observation.

## Typed refusals

A refusal is a product surface and evidence. It names what happened, the exact input and observed value, the required condition, and any bounded next action the caller may take.

Hard validation dominates visual agreement. A candidate that looks right but collides, disconnects required structure, uses an illegal port, exceeds scope, or violates accounting is refused. A structurally valid candidate that the panel cannot distinguish is also refused rather than selected by a numerically meaningless lead. In the determinant-aware registration primitive, an exact best-score tie spanning both horizontal hands returns `camera-handedness-unresolved` and retains all eight attempted hypotheses; enumeration order has no authority to choose a hand.

Thresholds and budgets carry the measurement that set them, the metric and registration they use, their false-accept and false-refusal observations, and the population on which those counts were measured. A positive score or a passing majority-class baseline is not evidence of correctness.

## Replay

Deterministic compilation is stronger than run replay. Identical canonical base bytes, normalized program bytes, and pinned truth snapshots must produce identical compilation and validation; a run that depended on a model or missing source boundary may support only partial replay.

### Replay levels

- `full`: every required input and captured nondeterministic boundary is retained so trusted code can re-execute from the beginning. This level requires the future sealed production path and is unavailable to current local booklet runs.
- `downstream-only`: retained bytes form a complete closure from the earliest captured boundary, normally the browser output, so downstream parsing, finalization, hashing, and scoring summaries can be checked without re-running the missing upstream work.
- `metadata-only`: the record is inspectable audit metadata but lacks the bytes required for downstream execution. Current artifact `/4` deliberately refuses to publish or verify this level, or any input-rejection boundary, until a typed digest-bound rejection witness makes the exact refusal reproducible.

The verifier derives the maximum level from the transitive content-addressed closure; callers do not choose it. Unknown roles, missing required roles, hash mismatch, unavailable bytes, consent restrictions, or an exceeded role limit lower or invalidate the claim by name.

Current local closures carry `authority: "local-diagnostic"` and `authenticated: false`. The replay reader verifies retained bytes and downstream results, then explicitly refuses executable replay. A manifest that says `downstream-only` therefore does not imply that the browser or model call can be rerun.

Run `2026-08-12T09-14-05-246Z-32668097b507-2989d382-2a93-470c-aadb-14d91107a904` remains the immutable artifact-manifest `/2`, score `/3` predecessor and cannot be interpreted as if it contained a diagnostic-prefix truth snapshot. Retained run `2026-08-12T10-28-52-560Z-34694c87c62e-26128a38-4d9e-4294-9091-6aae9b3ca367` remains the immutable legacy inspection-only replacement under artifact-manifest `/3`, run-contract `/2`, browser-output `/2`, result/score `/4`, and replay-closure `/3`; its pointer binds replay digest `sha256:1c27df8a95c655f7508436489e8e31f486f806c7a5382df76d53e0a80801a66c` and artifact-manifest digest `sha256:4dc5ce021e03ba0bc86667a64a1948f9ad54d2d80f95eb887b559a593c7036aa`. The exact full-tuple inspector rejects its retained match `/2` under the current match `/3` semantic boundary. The regression therefore pins the replay closure and the prepared-options/browser-output roles independently and runs only the frozen `/2` browser predicates; it does not relabel the full tuple as verified or current authority. Current publication requires artifact-manifest `/4`, run-contract `/3`, browser-output `/3`, and result/score `/5`, accepts only downstream browser closure as eligible, and has no production run yet.

The target production design adds an immutable `sealedReplayLevel` and a separately derived `effectiveReplayLevel`. Consent-driven deletion would append an authenticated tombstone, remove permitted bytes and indexes, and lower the effective level without rewriting the original certificate. None of that is implemented by the current companion library.

## Test ledger lifecycle

The implemented test ledger is an append-only, hash-chained event stream with a rebuildable query view. It records schema version, sequence, previous-event hash, actor, transition, idempotency key, and referenced artifact hashes.

Run states are `created`, `queued`, `running`, `draining`, `cancelling`, `persistenceFailed`, `succeeded`, `exhausted`, `failed`, and `cancelled`. Normal completion and budget exhaustion pass through `draining`; no new work starts there.

Model-attempt states are `created`, `running`, `succeeded`, `failed`, `timedOut`, and `cancelled`. An attempt result that cannot be retained moves the enclosing run to `persistenceFailed` rather than allowing an unrecorded success.

Candidate states distinguish receipt, compilation, hard validity, diagnostic rendering and review, canonical rendering, panel comparison, ranking, presentation, archival, cancellation, processing failure, and persistence failure. A hard-invalid candidate can produce diagnosis or a child repair but can never become rankable.

The exact allowed transitions are enforced in `apps/companion`; this summary does not grant production authority. Recovery resumes only from a verified durable checkpoint, cancellation is idempotent, and late events from an older generation are diagnostic.

## Model calls as evidence

[`spec.md`](spec.md#model-calls-and-consent) owns consent, minimization, provider policy, and the untrusted-output rule.

Current derivation producers capable of an opt-in provider launch are the legacy local script that sends one cropped printed-step image to a pinned CLI model and the strict part-identification first pass under one exact six-card development Gate 0 lineage. Part-identification re-ask remains disabled; a quarantined multi-panel checker and subscription-CLI MCP adapter separately implement the source-bound N/N+1/conditional-K request, immutable attempt, strict verdict, and refusal contracts in local tooling. None is called by the studio or real-booklet driver, none runs through a broker, and none can mutate a document or admit a catalog part.

Part-identification answer schema `/5` is an implemented local proof boundary with one exact six-card development pilot path. It rejects every legacy `/4` answer, including the stopped current-input pass whose 73 replies are quarantined after the model created local files despite its nominal `Read` allowance. The replacement request binds the canonical prompt, instruction, ordered card IDs and exact PNG bytes; the CLI contract disables built-ins and setting sources, uses safe mode and `dontAsk`, exposes one no-argument MCP image tool, requires an exact five-event sanitized stream, empty successful stderr and a pinned Claude Code 2.1.232 Windows binary, and replays each terminal answer line back to its card. Immutable call proofs and answer-checkpoint predecessor nodes bind the retained bytes, exact image closure, final answer owner and append transition. Synthetic and hostile local tests exercise that structure, but no real `/5` proof exists.

That `/5` evidence level is deliberately `local-diagnostic/sanitized-downstream`, `providerExecutionAuthenticated: false` and `executableReplay: false`. Raw stdout is hashed and discarded; the retained skeleton excludes session metadata, and neither the PowerShell job wrapper, Node executable, mutable MCP source closure, remote weights, provider policy nor admin-managed Claude settings is executable-replay-bound. Safe mode removes model-exposed repository tools and exact final-task-root auditing detects persistent changes there, but OAuth/cache/policy state and transient or out-of-root process writes are not OS-sandboxed. A same-user process can fabricate internally consistent local CAS bytes, so `/5` can support diagnosis only and never catalog, truth, acceptance or provider-authentication claims without a broker seal.

The six-card pilot now retains one immutable Gate 0 chain through exact request and configured-fetch policy bodies, proposal, trusted-local assertion, reservation, fixed-checkout global slot, exact-binary preflight, model launch, proof and terminal settlement. Its budgets distinguish one local executable preflight from one model-bearing launch and reserve six cards, two turns, the fixed Opus 5 input/output capacity, USD 2, five minutes and exact proof bytes; the slot charges conservatively before executable work and permits no retry after failure. The assertion explicitly authenticates neither the user event nor identity, configured fetch trusts pre-import process state, and provider execution remains unauthenticated, so this is local diagnostic opt-in rather than crop-consent proof or a 96-call completion-wave budget. No model-bearing `/5` call has yet completed, and any broader run needs a separately reviewed policy after the isolated pilot measures actual usage.

The current panel-placement script accepts one arbitrary resolved local input path without containment, file-type, dimension, PDF, page, or crop-bounds validation and exposes its authenticated temporary snapshot to the CLI with a fixed relation prompt and text naming the new and already-built pieces. The CLI receives no candidate render, panel N+1, first revealing farther panel, alternate view, deterministic panel-face parity, or source binding and is granted repository-wide `Read`. The ignored reading retains image, prompt, brief, and model digests; the raw result and parsed reading; model identity; timing, call, and cost metadata; and current-piece descriptors. It does not retain the input bytes or source path, exact brief or built-piece-list bytes, action-ledger digest, CLI tool trace, or an immutable attempt ID, and the fixed per-step output filename is replaced on rerun. When the mutable input path changes, the digest says that different bytes were used without making the original bytes reproducible. The prompt prefers a same-step anchor that the consumer then refuses as not yet placed, while parsed `newPieceOutlines` and `confidence` fields have no consumer; these are proposal-schema gaps, not evidence that narrowing is correct. That record is proposal evidence only, not a replayable visual observation packet.

The new checker closes the byte-transport and refusal gaps only inside its quarantined local path. It binds exact source and render PNG bytes/digests for N, N+1, and a K supplied only after an unjudgeable predecessor; PDF digest/page/crop claims; deterministic face seed, every rotate-icon fold, and resulting face; base document, catalog, truth, ledger, candidate-node, transform-set, and same-step atomic-group identities; exact prompt, minimized model brief, and complete instruction bytes; pinned model identity; bounded usage; raw response; and raw stream-json tool/result trace. Full-sequence preflight validates every prospective panel bundle and full-budget canonical request floor, plus call, byte, decoded-pixel, retention, and farther-step limits, before the first transmission; callers may lower hard budgets but cannot widen them. PNGs require bounded dimensions, authenticated IHDR and chunk CRCs, ordered data, exact IEND closure, and an exact bounded 8-bit non-interlaced raster decode with valid row filters. Its Claude adapter disables built-ins and settings, enables safe mode, passes an allowlisted environment, and offers only a one-shot no-argument MCP image tool, so no general `Read` capability or unrelated repository environment reaches the model. The retained tool result must byte-for-byte reproduce the bound labels and image blocks for that request; farther attempts bind the remaining cumulative token, cost, time, and retention budgets, with cost and time constrained by the CLI and token and retention excess refused postflight; and a typed mid-run failure exposes every already sealed attempt instead of trapping it in a local array. Strict `different` may veto the whole candidate, `same` only corroborates pending deterministic validators, and exhausted `unjudgeable` attempts record `not-observable`; every result denies certification, mutation, validator bypass, and authenticated authority.

This checker is still diagnostic infrastructure, not an operating booklet loop. No real-PDF/candidate-render producer feeds it, no accepted driver path consumes its result, and no current code proves that a claimed source PNG was actually cropped from the named PDF/page/bounds or that a caller holds consent to transmit that crop and candidate render; those are required producer and broker preflights, not facts an image digest can create. Its 56 mocked contract, adversarial, protocol, sequencing, process, and cleanup cases prove the local byte and refusal boundaries they exercise; they do not prove source authorization, live visual accuracy, or successful provider transport. No live verdict has been retained.

For this booklet the panel face is already deterministic evidence: the detector finds 43 rotate icons across all 359 printed steps, while the independently reviewed 43-panel prefix contains eight icon-bearing steps whose fold from the explicit step-1 `studs-up` seed reproduces all 43 reviewed face states, including five underside panels. When present, the model's `viewpoint` field may agree, decline, or make the entire reading refuse; it cannot override that parity record or establish absolute face from the projected lattice. A missing panel line currently parses as `panel: null` and still permits piece predicates to narrow without a face refusal, which is another reason the reading is not a safety gate.

The tracked schema-3 blind-pair input keys each verdict by the full 64-hex SHA-256 digest of the judged callout crop plus the claimed element identifier, and the consumer binds the truth file as an input. A verdict binds only that exact crop member; different bytes, another claimed element, or similarity-cluster membership remain unjudged. The input does not retain or bind the complete comparison image, exact prompt, raw model responses or model parameters, so it is durable label evidence rather than a complete replay of the judging calls. The consumer does not make a live checker call, and a narrowing result can discard the settled truth while every retained candidate remains safe. Panel placement is decided by deterministic image measurements and structural validators; candidate safety is not visual correctness.

Protocol types for provider capabilities, actor observations, attempt transcripts, and retained responses exist and have contract tests. They have no integrated product producer or broker enforcement path today, so docs and UI must not describe their presence as an operating model service.

Any future external call records purpose, provider, pinned model identity, parameters, seed where supported, bounded input digest, raw response hash, timing, cost, terminal status, consent and provider-policy digests, and the exact boundary from which replay can resume. A response that cannot prove the pinned identity or be durably captured is refused.

## Evaluation

### Measurable intermediates

A booklet supplies its own early checks: steps run 1 through N without gaps, action and callout counts reconcile, type size distinguishes quantity from repeat multiplier, placed pieces reconcile with inventory, and candidate renders can be compared per step rather than by final hash equality.

Every claimed improvement names the number it should move and retains the image or structured evidence that makes the number meaningful. A fixture that derives its expected answer from the same path under test, or an accuracy figure without its majority baseline, is not an independent check.

### Metrics

Hard metrics are compilation, schema and catalog compatibility, connector and collision validity, required connectivity and support, scope compliance, resource limits, accounting, structural hashes, and replay-closure integrity.

Booklet metrics are sequence and callout coverage, covered catalog prefix, pieces placed, steps settled by evidence class, observation coverage and `not-observable` claims, named refusals, score and margin at each refusal, deferral reach, reversal depth, and completion-audit agreement.

Soft metrics are panel agreement, silhouette similarity, blind pair agreement, latency, render count, storage, model calls, tokens, and cost. Soft metrics never compensate for a new hard failure.

### Promotion

A confirmed failure becomes a regression test or fixture only after its provenance and consent are clear, it fails on the old behavior, passes on the proposed fix, and retains the exact test or fixture identifier. Real user or provider artifacts require separate consent, minimization, license review, and secret and personal-data scanning before Git retention.

Stable knowledge is never overwritten. A promotion changes a versioned pointer after paired evaluation under frozen inputs, policies, budgets, validators, cameras, and stopping rules; rollback restores the prior pointer and preserves the evidence.

The independent evaluator, sealed promotion policy, protected holdout service, and automatic pointer update are target architecture. Current repository work is reviewed, gated, committed, and pushed under `AGENTS.md`; the product's future improvement loop is the actor forbidden from auto-merging, deploying, changing secrets, or approving its own evaluator.

## Knowledge classes

Truth is catalog geometry, connector legality, collision, migration, and provenance. It changes only through the catalog admission path and never through a model verdict or visual score.

Templates are bounded declarative subassemblies. Snapshot schemas and admission primitives exist, but no integrated booklet or studio path currently compiles a template into an accepted user change.

Current search policy is implemented as reviewed constants and run options for candidate budgets, one-panel deferral reach, thresholds, registration, and pruning. A change travels as an ordinary code change with its measurement and regression tests; there is no search-policy schema, champion pointer, challenger lifecycle, or automatic rollback today.

The target learning system versions that policy as knowledge so a changed value becomes a challenger evaluated against the same cases and reversible by pointer rather than by rewriting history.

Lessons are scoped hypotheses with durable anchors. Their one-line index is [`docs/learning/lessons.md`](../learning/lessons.md), their evidence is [`lessons-evidence.md`](../learning/lessons-evidence.md), and obsolete or machine-enforced lessons are removed rather than accumulated.

## Physical feedback

A physical claim binds only the exact document and catalog hash that was built. Any structural edit invalidates it.

Physical reports may calibrate advisory build order and stability. They do not silently change catalog truth, promote an advisory validator to blocking, or generalize one successful model into a claim about other assemblies.

## Operational guarantees

The current real-build run contract declares requested/expected step counts, maximum and target part counts, per-piece, blind, one-panel-deferred and exploded limits, a bounded farther-panel reach, a farther-panel score-render limit, and shared aggregate candidate and narrowing ledgers for intervening carry. Reservations are made before the batch or render they authorize, and a refusal retains completed immutable evidence without admitting a partial frontier. Input, raster, artifact-role, and stored-byte bounds are enforced by their own contracts rather than collected into one run budget; no current budget grants arbitrary farther-panel depth or deep backtracking.

The target production run additionally declares ceilings for wall time, attempts, model calls, tokens, cost, total stored bytes, image dimensions, recursion, and every other external resource. A budget must be able to bind and reserve enough proof budget for a meaningful rerun.

Malformed inputs, model timeout or refusal, cancellation, WebGL context loss, catalog drift, stale revisions, partial writes, disk exhaustion, and repeated-state loops remain isolated failures. One candidate or response cannot corrupt the settled prefix or crash the manual editor.

Task-run evidence is cleaned when no active process needs it. Promoted fixtures are the exception, not a reason to retain raw corpora.

## Non-negotiable safeguards

- Model and vision output is untrusted data, never executable code or self-certifying truth.
- Every retained candidate, comparison, decision, and promotion is immutable and attributable.
- Hard validity dominates visual resemblance; pixels and structure answer different questions and both are inspected.
- A panel score is meaningful only with its exact camera, face, masks, registration, reachable bound, and image evidence.
- N+1 is only the minimum later witness; farther panels continue until the placement is revealed or the claim is recorded `not-observable`.
- Hidden surfaces are never certified by an image that does not expose them.
- User documents change only through explicit manual commands today; no automatic acceptance path exists.
- Production signing, credentials, consent authority, evaluation, and accepted namespaces cannot be created by test or challenger identities.
- Truth, technique, and preference stay separate, versioned, measured, and reversible.
