# Part model and catalog truth

Status date: 2026-08-26

This document owns how a part is identified, represented, sourced, derived, checked, admitted, and versioned. [`spec.md`](spec.md) owns document and validator semantics, [`building-system.md`](building-system.md) owns the product frontier, and the [dependency and data bill of materials](../dependency-data-bom.md) owns source, license, and allowed-role records.

## Current catalog

The builtin catalog is `builtin.basic-parts/28` with 104 definitions. Sixty-one render from repository-authored parametric recipes and 43 render bundled, content-hashed LDraw surfaces with source-faithful normals. Sixteen of the mesh-backed definitions are render-only promotions of earlier parametric parts; the other twenty-seven are fully measured definitions. Seventy-seven definitions retain repository-authored physical recipes.

Current truth is overall `sha256:643185fe21f0d0c77a7aada8b170395f11bb7da1079f97d5c0cd0a03d7464f1b`: catalog `sha256:15decef17024421dec825287923d2ae0142973f83281b3479b0eeeb5e5ddd837`, connector taxonomy `sha256:41b9011f2ae13baadd4bc173936ea962d5ef6419809bc17fa3dcfcf01e83a553`, collision model `sha256:11a791eaed761857eeb7446a4feaa278635593a8767e0d6d7ed9426d0cebeabd`, transform policy `sha256:b67a6b5226f97eeef8d18dc038df8e6e51da51843b0846cb64a61c328f46eb9a`, and validator set `lego.kernel-validators/4` at `sha256:ac785c8f5ac9f2d642bf53c8ef51764b7954c981355b1d7d508a2228a5f1bf55`.

Source-to-catalog and LDraw interchange frames have a generated registry of exactly 24 determinant-`+1` signed-permutation rotations, ordered at roster `sha256:57446894cd2b917eb5463672655baa012d7c03539ba4147cd89e9c774a309201` and reproduced byte-for-byte in TypeScript and Python. The first four entries retain the existing upright IDs and matrices; every current part still exposes exactly those four `legalOrientationIds`, `BUILTIN_CATALOG.orientations` and saved-document transform truth remain the four-row upright registry, and no current definition consumes a non-upright source frame. The scorer derives each inverse from the complete registry, LDraw import accepts the wider vocabulary only before requiring the corrected document transform to resolve upright, and reflections are rejected. That infrastructure left historical `/26` unchanged; `/27` advanced for four measured admissions and shared connector-capacity semantics, and `/28` advances for two exact-suffix measured admissions without widening placement vocabulary. A future production admission that consumes a non-upright frame must still advance catalog truth and supply the part-specific geometry, connector, collision, frame, and visual evidence that the registry alone does not authorize.

The `/23` predecessor payload was independently reproduced from source commit `d99b74d355684c8ceaca0ad6f2df76d96ebe4937`: after restoring its historical truth label, all 95 definitions occupy 1,583,353 canonical JSON bytes at `sha256:ceff2b3acbb53f647eea56d6ae4d271c1c048f94c7a035faf8a60b2979521ba3`, their connector-plus-collision projection is `sha256:cd14902b4fda0525457091ed7e59820113cf52dd5cbcf15d0c88f58690bc486f`, and their collision-only projection is `sha256:2c6a27bf11f5f9d16ef8535b053df64b8c46940af4875a94b3380cf0d52b7bee`. The `/24` admission gate reproduces those values from the first 95 current rows, so the claim that `11212` is append-only does not rely on migration report metadata.

The sixteen render-only promotions take their visible triangles, normals, exact bounds, and explicit source-to-catalog frame from LDraw while preserving their preceding connector, allowance, connector-grid, evidence, and collision declarations. Their `preserved-catalog-recipe` collision can fill a visible void and is not proof of hollow physical truth. The `/13` migration therefore reports changed render interpretation without pretending that physical semantics changed.

The tracked booklet has 121 distinct required leaf design identities. That is not a 104-of-121 coverage fraction: catalog definitions include parts outside the set, aliases and design identities do not share one denominator, and suffix-exact catalog rows do not reinterpret suffix-absent Builder revisions. A deterministic current-catalog intersection still finds 24 of those 121 identities at `sha256:1065c3a3d04787974aa43c8ede60c1937930884bdf297488d3731dc8c99ea591`; `/28` adds exact official `3245c` and `2453b` while keeping `3245`, `3245a`, `3245b`, `2453`, `2453a`, and `2453c` unresolved by that policy. Those catalog rows grant no printed identity, source execution, physical Brick assignment, frame, placement, replay, action-ledger, acceptance, or step-frontier authority. Historical `/27` added `99563`, `73230`, `35464`, and `49307`; historical `/26` added `78329`; all seven retained legacy locators at printed steps 265, 268, 271, 274, 277, 282, and 336 are stale and self-contradictory against current manifest-v6 crops. Historical `/25` added `33909` under the same catalog-only boundary because its step-76 locator is stale and self-contradictory. Historical `/24` added `11212`, likewise without crop or placement authority. Historical `/23` added `32064`; official set inventory quantity 2 and the stale step-39 pair-judged-same and step-101 unreviewed coverage locators remain context and counterevidence only. Historical `/22` added `4519`; its official inventory quantity 4 and printed step-45 callout quantity 3 remain context only because the retained crop identity is stale and untrusted. Full-booklet manifest v6 corrects printed step 18's quantity-two callout from the adjacent `3069` crop to element `6514469`, design `25269`; exact source-component replay independently reproduces that corrected quantity-two evidence, and `/14` carries the measured quarter tile. Fresh trusted crop-local model adjudication remains absent and no current `/5` answer score exists: 73 current-input replies captured before the local-write transport defect was found remain quarantined under legacy answer schema `/4`, and strict `/5` refuses them. The prior coverage `/3` report is preserved as incomplete deterministic, pair-truth and exact-rebound counterevidence. A standalone legacy-recut diagnostic proves that 73 positive truth relations / 113 pieces survive exact current crop equivalence; a separate exact official-cut consumer promotes only 70 compatible relations / 107 pieces to sparse semantic element identity and quarantines three official-capacity conflicts plus changed recut n38. The independent source-art semantic compiler adds 16 relations / 40 pieces by exact decoded RGB24 plus an explicitly normalized ordered image closure, yielding a safe sparse roster of 86 / 147 and leaving 101 / 173 residual. Its 211,319-byte artifact at `sha256:4be7bd77d386a7a656019affe9c995e77135080a7aa90df19e43a6f2167ab721` publishes neither physical Brick assignment nor catalog, frame, action, placement, document, replay, or completion authority. The complete semantic closure now feeds coverage-only `/4`; its fresh 588,467-byte `/28` report at `sha256:a12d5744f3f4417628e53227aaa4c35d9aee0eba5fdce7b865087e6f97dfbfad` measures all 49 part-bearing step rows and all 320 pieces placeable while preserving all 672 suffix rows with semantic and catalog authority null. The 592,243-byte `/27` report at `sha256:c7dd0ef1e5783384a44e123546efce82bb0da2ebc69364dc7780b2bddfa53e0b` remains counterevidence. `/15` admits distinct design `28802` from its official LDraw and LDCad evidence, but the printed-step-26 Builder source still contradicts that identity by naming `10201`; coverage `/4` grants no action trust, does not advance the retained ledger beyond step 25, and establishes no verified booklet frontier.

The downstream prefix-50 semantic closure carries those 86 / 147 safe rows only through the opaque verifier handle and reopens crop files only for the exact remaining 101 relations / 173 pieces, one manifest-bound callout and one inventory crop role per relation. Its retained 41 / 66, 57 / 102, and 3 / 5 reviewed relation/piece partitions are joined to a separate exact 101-row review-outcome input, 30,429 bytes at `sha256:286696d9254e89d027eb4a244d176cb8aff064991655347865ce8d3d5f1012b7`, that binds each identity, element, and both crop digests to an explicit original-resolution outcome and admits only `same`. They close semantic element identity for all 187 first-50 callout relations / 320 pieces, and an independent exact official first-320 parse produces the identical 86-element / 320-piece aggregate with zero quantity differences, while the 881-row / 359-step source/index contract remains intact and the suffix remains unreconstructed. The 92,426-byte `/1` result is `sha256:1902af68a13cb629d9dbac1707c8c5c6998ec355cfcb6ef4dad2fc938f76155b`. A `publishedPartNum` such as a suffix-bearing value is exact resolution metadata only: this unit supplies no catalog definition or alias, geometry, connector, collision, allowance, transform, or frame truth. A separate coverage-only `/4` consumer may resolve those exact identities against already admitted catalog truth, but the closure grants no action, physical assignment, frame, placement, or completion authority.

The bounded step-31/32 order reconciliation changes no catalog truth. Its three reviewed identities only prove that exact Builder phases 49 through 54 must be repartitioned across the two printed aggregates; the 6,680-byte `/2` result at `sha256:66451b2324142d9f563b731739532c38be06e34018eb8397bacfe4a0e6245810` does not admit `3245c` or any other definition, publish a Builder-revision alias, choose a physical frame, or authorize placement. The reviewed physical join is reconstructible from its open pinned inputs, but that diagnostic fact is not catalog or assignment authority; crop bytes were not consumed by this unit.

The next step-28 Builder identity family `3245` remains deliberately unresolved even though catalog `/28` separately admits exact official `3245c`. The exact primary model uses design `3245;M` ten times but selects no LDraw suffix, while its checksum-pinned derived LDR names `3245b.dat` ten times without retaining the converter or revision-M alias table that could make that mapping authoritative; the official archive carries distinct a, b, and c roots. A calibrated geometry contract selects c over a and b for the exact fixed Builder surface population, but the Builder manifest's expected MD5 conflicts with both the independently bound served body and ETag and the derived b mapping still lacks its authority chain. The exact-only suffix policy therefore refuses to treat the catalog row as a Builder binding; the strict result remains `unresolved-upstream-manifest-object-contradiction`, with Builder identity, physical frame, and placement authority false.

The step-2/step-16 `2453` suffix diagnostic likewise refuses to guess even though catalog `/28` separately admits exact official `2453b`. Its exact target and control images do not expose a calibrated stud-aperture distinction: both target elements remain `not-observable`, their Builder suffixes remain null, and every Builder identity, frame, assignment, placement, action-ledger, replay, and completion authority field is false. The exact-only catalog policy does not convert suffix absence into `2453b` authority.

`npm run parts:check` covers all 104 definitions and is part of `npm run verify`. It is a declaration-consistency gate, not a substitute for source review, matched visual inspection, connector evidence, collision review, or browser verification.

Current `/28` visual admission binds `3245c` packet `sha256:da3821158f5b04b87e8bc67bcd5d17d4aed79afaf7a254ca07cf05178df0c033` and `2453b` packet `sha256:5b96dea2398dfbc49c1c1d01ca9f631770d966c967c594534f7a53abacd30734` into capture batch `sha256:7ed52e1312df351154134a60d4eb0279bb6cc24f31da758957ba5e84bdcdddcb`, native-pair manifest `sha256:15c3d13113213b2cbb7d10ec31deff8dbdeffe54ad052478088296111caa1371`, reviews `sha256:470548201033532bd8ad8c81d4081eb18f829d7ca17db104c6c8fa949cecc860` and `sha256:23ae82d418860f3c781115072613155ec2d18ce61384f9e556b82021b45f7192`, and review batch `sha256:2d27d92845e619e8ff55f5b034001fbdba80a4bcc71095046e4b1e9536ce85b0`. All sixteen original 640 × 640 source/catalog pairs were inspected and reviewed `same`; every PNG and decoded-RGBA pair is exact with foreground IoU 1 and zero differing pixels. The isometric and underside-oblique views expose the two-stud open-bottom `3245c` form without a central understud and the solid-stud/open-square-socket `2453b` form. Matched exterior pixels do not independently establish connector or collision truth, and this tranche grants no Builder-revision, printed-step, frame, action, placement, or completion authority.

Historical `/13` exterior evidence under its original truth remains complete. Twenty-four immutable packets bind 192 matched native-resolution source/catalog pairs; a separate review records every visible pair as `same` in batch `sha256:e1094576c2250db8a9875828254f064384c195991304d363c5a2a9ff5a50c0dd`. Of those pairs, 181 are RGBA-exact, ten differ by 12 pixels total at maximum channel delta 1, and the remaining `cheese-slope-2x1` isometric pair differs in 104 pixels without an observable geometry or shading difference at native size.

Historical `/14` evidence under its original truth separately closes `25269`. Packet `sha256:61ff5179fd5c7bb117657b84c05a82ae77a5db7eb3c37dbeb1bf9669b815185a` and native-pair manifest `sha256:db728157c8189f070330a73075f4381536fac8a49e95906fe2a3c9e3010093c2` bind the eight original 640 × 640 top, bottom, front, back, left, right, isometric, and underside-oblique source/catalog pairs. Every pair was inspected at original size and reviewed `same`; batch `sha256:2df2c85b096549ccc4838760093f2128ef9d26871d0806a89fe2c150713d9b71` publishes review `sha256:520d1711c0c0a4e5c0e7ed2d0f110e2ab94862ab23d1213332ec54823b09d213`.

Historical `/15` evidence under its original truth separately closes `28802`. Packet `sha256:5833ac0ff3c959fab154b730d64142169555861a118f46e3bb4326e19b103531`, native-pair manifest `sha256:cad00fbefb0ecc4ea2440821612dad2ba05d423bffaf69fa47742ff7071be7a1`, and capture batch `sha256:c2b1d40ea718b23012021b389102dc7a4362d7914a2daf62bb7690411cedc427` bind its eight original 640 × 640 source/catalog pairs. Every pair was inspected at original size and reviewed `same`; review `sha256:ba63d84b83d2d5142a129c144c392a160e754b6b8d3477351e705304ee86c239` is retained by review batch `sha256:51ed92e631be31b208fbf21424e8bc3a0f4f3925881cf2c98b1b81e204a0f6c0`.

Historical `/16` evidence under its original truth separately closes `35787`. Packet `sha256:aae445b7c4018d2d8b591d9ff6ff9cfd8d451c5c0bcacefe383c7421e417e4dc`, native-pair manifest `sha256:938cec88eba5d76ee33514eda75d251dc377e95485b6818dadee402f63a55303`, and capture batch `sha256:1279d4978b403c32b409b522d52b970b7ed74a0c332773fd509579a3a60880d9` bind its eight original 640 × 640 source/catalog pairs. All eight were inspected at original size and reviewed `same`: seven are pixel-identical, while underside-oblique differs in one pixel at maximum channel delta 1, foreground IoU 1, and mean absolute RGB delta `8.138020833333334e-7`. Review `sha256:0f1c4a12a6ad2bd28e628e171f4584910be5166a4c8bdbb32deb6db98a4dc1fa` is retained by review batch `sha256:7ce9abe76d7e408a176b244183331318ca828e6f21db6dcd161af2e6a30e1605`.

Historical initial `/17` evidence under its original collision and validator truth separately closes `11253`. Packet `sha256:26d036d80af94143c8e6916e08563491df0e4f4a78730dff6f63042f6e06cd84`, native-pair manifest `sha256:ed1f5c743f223fc39c83ead947e9f387c6fb4db2bfc8a37e73334fae554acd44`, and capture batch `sha256:361c906e33df44ec7564dc76f4a32ded17971e59a7d5717d58c5294aa43f3196` bind its eight original 640 × 640 source/catalog pairs. All eight were inspected at original size and reviewed `same`: six are pixel-identical, while isometric and underside-oblique each differ in one pixel at maximum channel delta 1, foreground IoU 1, and mean absolute RGB delta `8.138020833333334e-7`. Review `sha256:fda6135585573401b00c5ed8497b95191a54761b0f2bfe5dd2d977326c2d72a9` is retained by review batch `sha256:61bc8fc80062bc96662d68e4001b1fee6bd20aaa6e5a65a0219f68c87706254a`.

The historical consolidated `/17` recapture closes all 28 then-current mesh-backed definitions under collision model `rectilinear-stud-clearance/3` and validator set `lego.kernel-validators/3`. Capture batch `sha256:edd8aa795b2e5e65d12eb39418463aa9cf1d95f147223221653a9f75abd32f1a`, native-pair manifest `sha256:6292be1e70cc1a927387b285bd64db21aa370537aa25f8fe6d7cb9722309738b`, and review batch `sha256:e53a12ba863a544b1596174bcc225d912117e7639f7105222137e134ad3cf686` bind 224 original 640 × 640 pairs: 210 are RGBA-exact and 14 differ by 119 pixels total at maximum channel delta 34. Twenty-five outcomes were transferred into new `/17`-bound sidecars only after exact old/current identity for all 16 PNG hashes, decoded RGBA hashes, mesh hash, frame hash, source manifest, camera packet, renderer policy, and actual PNG bytes; `25269`, `28802`, and `11253` were freshly inspected, including all sixteen raw roller-skate originals. This is immutable historical evidence after `/18`, not current truth.

The historical consolidated `/18` recapture closes all 29 then-current mesh-backed definitions under collision model `rectilinear-stud-clearance/3` and validator set `lego.kernel-validators/3`. Capture batch `sha256:2f4102907a3b3b059200bf4aa096779d66b34cf51e5d4823dcdcd419b5e017f3`, native-pair manifest `sha256:97ca40846cdbe4417dc5f98bcf6554516efdca8c90ee37d42f31a36f4d2bd73e`, and review batch `sha256:fcb60f2530d193ef08213701be747e3c9dc805a4f96a985316a22b358cc25ed0` bind 232 original 640 × 640 pairs: 217 are RGBA-exact and 15 differ by 120 pixels total at maximum channel delta 34. Twenty-eight outcomes were transferred into `/18`-bound sidecars only after proving exact old/current identity for all 16 PNG hashes, decoded RGBA hashes, mesh hash, frame hash, source manifest, camera packet, renderer policy, and actual PNG bytes; their isometric and underside-oblique pairs were also swept at original size. The `15254` packet `sha256:d675de2d73345a12dd756e76eb9a2eab2dcdb69b722228023905a827f368ed89` and review `sha256:cf4889ed05e2276f04a0e667976a91c885192f0128e82c33f642225cef049b46` bind the thin-top arch. All eight `15254` pairs were freshly inspected at original size: seven are pixel-identical, while isometric differs in one pixel at maximum channel delta 1, foreground IoU 1, and mean absolute RGB delta `8.138020833333334e-7`. This is immutable historical evidence after `/19`, not current truth.

The historical `/19` admission tranche closes the `41682` definition against its exact 14-file official LDraw closure. Packet `sha256:6265de5e960fdb4d9b839ebe1f36a046f6f8b95626965ffbda1a7dc50fcfb7ec`, capture batch `sha256:d7005fc30dd7fa84acadb64012efaa1c8023f6af8720f8bdf0f5e746dd680d18`, native-pair manifest `sha256:b382da75b01ea732d79a303c706dbba30d29fdabc5d9f46906a9bca322871320`, review `sha256:9b5e29a382f5fa408bd94f1e40e9497358c3ec465d4c8e4d6fad6e7802a0373e`, and review batch `sha256:6860016197eb346118fd039df36ec4086c58d9f46cbe36510535c2dc2e4df922` bind its eight original 640 × 640 source/catalog pairs. Every pair is RGBA-exact and was inspected at original size; the strict front and bottom cameras expose only matching occluding silhouettes, while the side, isometric, and underside-oblique views expose the T-profile, handedness, side-stud occlusion, central tube, wall opening, and hard-versus-smooth shading. The review records all eight as `same`. The other 29 mesh packets remain historical `/18` evidence; no consolidated all-30 `/19` recapture is claimed. Neither historical tranche certifies hidden interiors, exact physical collision, clutch or minifig fit, sideways attachment, or stability.

The historical `/20` admission tranche closes the `2877` definition against its exact official LDraw closure. Packet `sha256:d1d13723c8ed7bd9d5e213de6c489a046bb17e25c68e07e5bdae35733e096794`, capture batch `sha256:805efd95b5792c420e44c53cb95d5eb4f124c71888984d1660862176fb34bb63`, native-pair manifest `sha256:1fa91b2fc77086065786645613dc7a880a0773c3d843db531a18707492b69d04`, review `sha256:27c234910da41cfe16301b3ac997d831201b4b9b74ccf8f7f7cb35d43ef9b2c6`, and review batch `sha256:e482d05f9a77a132c4df8916eb89aaf617d0d3b766d7ec0fe532e11ef94aa33e` bind its eight original 640 × 640 source/catalog pairs. Seven pairs are RGBA-exact; underside-oblique differs by one pixel at maximum channel delta 1, foreground IoU 1, and mean absolute RGB delta `8.138020833333334e-7`. All eight pairs were inspected at original size and reviewed `same`. The matched renderer applies the declared recipe frame to both source and candidate, so agreement does not independently prove the frame; exact source triangles, frame, and connector-route regressions carry that claim. The historical `/19` `41682` tranche and 29 historical `/18` predecessors were not rebound, and no consolidated all-31 `/20` recapture is claimed.

The historical `/21` admission tranche closes the `3040` definition against its exact official LDraw closure. Packet `sha256:ea184956f0dc3138f861c1c1775d33392255f1a71ff22985f71e6f4b42867846`, capture batch `sha256:46b3900a1fe08c89552dc53ab708e9b3ec9a6ddb6c998ee5f539eaac1d6c99fe`, native-pair manifest `sha256:f39ab61dcb849da1575ac138e0b76f47dfc71d24938f3b36fdf3288c9ff726dd`, review `sha256:c4b45d6c6dac46585eadd9385c2972f4e93df7cccf0a926472536ea6ea533fea`, and review batch `sha256:530b3191de665f2de9df055de64429138149dd1243e719bc7dec927d4e5e2916` bind its eight original 640 × 640 source/catalog pairs after the exact validated-connection profile repair. Seven pairs are RGBA-exact; isometric differs by one pixel at maximum channel delta 1, foreground IoU 1, and mean absolute RGB delta `8.138020833333334e-7`. All eight pairs were inspected at original size and reviewed `same`. The matched renderer applies the declared recipe frame to both source and candidate, so exact source triangles, frame, and connector-route regressions independently carry that claim. The historical `/20` `2877` tranche and its predecessors were not rebound, and no consolidated all-32 `/21` recapture is claimed.

The historical `/22` admission tranche closes the `4519` definition against its exact official LDraw closure. Run `2026-08-24T16-57-28-261Z-ec4e1ef24984-bbb8b1c4-4788-4edd-b23b-aeb27886f3d5` produced packet `sha256:c9284e7846fab3e27a1c324b332db9f96d2aab592bc84a94c0c6f27567ac11a9`, capture batch `sha256:0f529c77a5b97186a7d9cab1bfbff7aa9b32bb55e22c8b40b81c7f961550fc59`, native-pair manifest `sha256:878250df3ab4c3478ff1c7a7c7999c16dc9d2adcef954883427b823829fd188a`, review `sha256:e2e652851df5b2fa8d7a8ed57428d149c29ee77d5c93dd9e56ca945e3bfeb0af`, and review batch `sha256:5c3e655e5ee041c2dff3a61420292ee88c28fe303550e1f86298655daed52c08`. Seven source/catalog pairs are RGBA-exact; underside-oblique differs by one pixel at maximum channel delta 1, foreground IoU 1, and mean absolute RGB delta `8.138020833333334e-7`. All eight native-resolution pairs were inspected at original size and reviewed `same`. No consolidated all-33 `/22` recapture is claimed, and exterior agreement does not establish continuous sliding, grip, stability, or axle-through-bore collision allowance. These hashes remain bound to `/22`; they are not relabeled as current `/28` evidence.

The historical `/23` admission tranche closes the `32064` definition against its exact moved-to official LDraw closure. Packet `sha256:c4728ac4722ccecbb630e8a9a386df09514a1666caa047e27016482e21c785ca`, capture batch `sha256:717249d0a06b0b2f094836316d9b4047bbd371f63eea18536c1a3e481814786e`, native-pair manifest `sha256:c39b9392d4b33a3066588c6d072b795fb9f8ae65162b414b7b511ff163e1d5bc`, review `sha256:8292738d680a785b076b01ed9626899bd1ee6b78c9a2ac1fe574bdcdf2673c09`, and review batch `sha256:24f5d697c01e8c0e65018b0042067de272c4f393dd78510c1abe9b1142cc5565` bind its eight original 640 × 640 source/catalog pairs. Seven pairs are RGBA-exact; isometric differs by three pixels at maximum channel delta 1, foreground IoU 1, and mean absolute RGB delta `2.44140625e-6`. All eight pairs were inspected at original size and reviewed `same`. No consolidated all-34 `/23` recapture is claimed, and exterior agreement does not establish hidden interior material, exact collision, continuous sliding, axle-through-bore collision relief, grip, stability, insertion access, or trusted placement. These hashes remain bound to `/23`; they are not relabeled as current `/28` evidence.

The historical `/24` admission tranche closes the `11212` definition against its exact official LDraw closure. Packet `sha256:a82d963c0e29fafdc9a9f76d6b1c8ab7c73f6127fb989e346f0ef449cba9f4fa`, capture batch `sha256:b8db94807b8616c62aee7e63e918ad8a14dac6af6eec314541508b76cedee6df`, native-pair manifest `sha256:fe0c29059f648a47febc555f853820f98540784314d247d7b0bbe90e933d7241`, review `sha256:17e76699de8bbf1ff96a447dd035c868906d3073901dd0f824a7da67fa4ae326`, and review batch `sha256:ca0ced2b265b389eb959cc321cc3cce70ed3ff34f0910e735fca8e086e29893e` bind its eight original 640 × 640 source/catalog pairs. Seven pairs are RGBA-exact; isometric differs by one pixel at maximum channel delta 1, foreground IoU 1, and mean absolute RGB delta `8.138020833333334e-7`. All eight pairs were inspected at original size and reviewed `same`; top and bottom certify only their visible flat silhouettes, while side, isometric, and underside-oblique views expose plate height, all nine studs, four tube rings, walls, ribs, winding, occlusion, and hard-versus-smooth shading. No consolidated all-35 `/24` recapture is claimed, and exterior agreement does not establish hidden material, exact collision, grip strength, stability, or trusted placement.

The historical `/25` admission tranche closes the `33909` definition against its exact official LDraw closure. Packet `sha256:bb640b711f5cf7aea4d48ff77676965ab4f78e650a2dd136ea40e18da6bec906`, capture batch `sha256:34fce749a7eaa53d1e43f7782425fc1b7cccb7b1bfa3b051955e88e7f48abc3b`, native-pair manifest `sha256:af2edbcfb887588926bb61e7fc1042c6ac0283aea654e2f7562b8ba2f2979627`, review `sha256:b3afd9c82bd1081cbeb24b5c8cf892a09abfa44e9a3d9883304ad31efbc1050f`, and review batch `sha256:a502a85d9fcc2d04689d5c172e5f7f0ec95c4fe79cf68b555a1d4b0f28a2fc17` bind its eight original 640 × 640 source/catalog pairs. Every pair is RGBA-exact and was inspected at original size and reviewed `same`; the clean underside-oblique pair exposes matching tube rings, ribs, walls, and cavities. No consolidated all-36 `/25` recapture is claimed, and exterior agreement does not establish hidden material, exact collision, grip strength, stability, or trusted placement.

The historical `/26` admission tranche closes the `78329` definition against its exact official LDraw closure. Packet `sha256:3dcce6065d94e243c65485c0b29effba307ae2e9e508ef3c49fa7e8d6a059531`, capture batch `sha256:0305f056cff14aa658d801f73a5cd852aff851c13378dc19ab7f08962f1c26f6`, native-pair manifest `sha256:774dd19d354961b1da5907a8b31dea01d97160bf152f6152184acc8699bf397b`, review `sha256:f16c939d3f333b5b581532010093a98f0d368d5964aeeb59f77a94358e8d482b`, and review batch `sha256:0031abc4205d40e9c2d2e1543674bc7a3bd12916c32ef5c75200db0c34fdface` bind its eight original 640 × 640 source/catalog pairs. Every pair was inspected at original size and reviewed `same`; seven decoded-RGBA pairs are exact and the isometric pair is visually indistinguishable at native size despite different decoded hashes. The clean underside-oblique pair exposes matching perimeter walls, central rail, four internal tube forms, end cavities, proportions, and orientation. No consolidated all-37 `/26` recapture is claimed, and exterior agreement does not establish hidden material, exact collision, grip strength, stability, insertion access, or trusted placement.

The historical `/27` admission tranche closes `99563`, `73230`, `35464`, and `49307` against their exact official LDraw closures. Their packets are `sha256:061237ddb1ab9cb5761d51010320241ab2bd2f3521d0e51fd8231588a3246167`, `sha256:7aec985e3c8ca578146e7142d443040d62cba1d3007beaaedb5deb0b7fa84066`, `sha256:76e692a39570aecd4bfde387b73221a6105133fcfa6d3a68cc9080ad12d84c66`, and `sha256:3236d09e03c4f9268badab5f66a5e3b088bdc0376abd4ffc687f24bfea9c029a`; capture batch `sha256:b4794cf1d3fd69e2ff8047f21a10720f4ab39da943875c0290610da2538fd863` and native-pair manifest `sha256:00cac7e4faf3add0f6efddeb707e184fc50fe5775e5a3f9b1a88d1b00e004c17` bind 32 original 640 × 640 pairs. Reviews `sha256:e802c975655a94f6e697c9c4ea4e47dc188067df9c2e41e085a988b80d188c99`, `sha256:42971675d7518ed35bead322387274665698c77d8dfd50d7edf7e68549c21035`, `sha256:253c69c20eb530847e4f2b5b0dc851d826176af451a0fdeee2097abdfafcd3f5`, and `sha256:e30ea73092750c13ff9d1b76d198989aa1316835a8bad865d16ad5c95da43f7c` are retained by review batch `sha256:88fcdefe513746664816be0d17003df772b982f6c7a781dd2e2c20844ec4c10e`. All 32 pairs were inspected at original size and reviewed `same`: 29 are decoded-RGBA exact; the `99563` isometric pair differs in two channel values at maximum delta 1, the `73230` and `35464` isometric pairs differ in one channel value each at maximum delta 1, and every foreground IoU is 1. Exterior visual equality grants no hidden-interior, exact-collision, grip, stability, printed identity, assignment, physical-frame, placement, replay, action-ledger, acceptance, or step-frontier authority.

The current `/28` suffix-exact admission tranche closes `3245c` and `2453b` against their exact official LDraw closures while refusing bare and cross-suffix aliases. Packets `sha256:da3821158f5b04b87e8bc67bcd5d17d4aed79afaf7a254ca07cf05178df0c033` and `sha256:5b96dea2398dfbc49c1c1d01ca9f631770d966c967c594534f7e53abacd30734`, capture batch `sha256:7ed52e1312df351154134a60d4eb0279bb6cc24f31da758957ba5e84bdcdddcb`, and native-pair manifest `sha256:15c3d13113213b2cbb7d10ec31deff8dbdeffe54ad052478088296111caa1371` bind sixteen original-resolution pairs. Reviews `sha256:470548201033532bd8ad8c81d4081eb18f829d7ca17db104c6c8fa949cecc860` and `sha256:23ae82d418860f3c781115072613155ec2d18ce61384f9e556b82021b45f7192`, retained by review batch `sha256:2d27d92845e619e8ff55f5b034001fbdba80a4bcc71095046e4b1e9536ce85b0`, record all sixteen inspected outcomes as `same`; every source/catalog pair is decoded-RGBA exact. Exact catalog suffix identity does not bind the Builder `3245;M` or `2453;I` physical frames and grants no assignment, placement, replay, action-ledger, acceptance, or step-frontier authority.

An independent exact identity route now supplies what catalog admission alone could not for `2453;I`: the checksum-pinned Builder bundle and official model identity agree on one family-0 solid top stud, zero family-1 open studs, one family-15 underside clutch, and the exact solid-suffix `2453b` LDraw/LDCad/catalog route. The Builder-to-LDraw `diag(25,-25,-25)` transform at `[0,120,0]` composes with the blueprint-bound catalog asset frame at `[0,-60,0]` to derive the proper Builder-to-catalog local transform `[0,60,0]`. The source-extraction report is 2,603 bytes at `sha256:86196d880b405b1fc4516cbdfba1d7fbedd6746d2d7a56079b927d718f036f11`; the opaque-token identity artifact is 6,730 bytes at `sha256:087a8f0308bdf83a7a585196acb4f695409350367e311b38dbb7920038d1f5d4`. This is an exact identity and local-part-frame route, not a suffix alias or catalog-intersection change, and the current first-50 Builder registry still excludes its five occurrences until a narrow typed consumer admits the route. It grants no printed physical assignment, world placement, action, mutation, replay, acceptance, or completion authority.

The `25269` definition expands the official 13-file LDraw closure into 96 render triangles over 146 stored vertices. Its 26-box collision height field is measured from that surface, and its one central underside clutch is authored separately by the pinned LDCad Shadow Library. The admission scorecard reaches composite `0.9793537209` with zero hard failures. Builder metadata names a revision-O record, but this admission consumes neither its shell nor its connector fields: the official LDraw closure owns the surface and frame, while the exact LDCad route owns the clutch.

The `28802` definition expands the official 19-file, 17,940-byte LDraw closure into 618 render triangles over 663 stored vertices. Its 23-box collision height field is measured from that surface; the pinned LDCad route authors two underside clutches and six outward stud frames. Four studs face horizontally and remain represented and validated, but the unchanged upright-only transform policy cannot attach through them. Per-outward-normal lattice scoring now measures all eight connector frames on their own tangent grids and does not misclassify the bracket's 20-LDU envelope as a malformed plate stack; the admission scorecard reaches composite `0.9869554553824675` with zero hard failures. The inspected Builder source instead names `10201`; that contradiction is retained as counterevidence and grants no surface, frame, connector, collision, or ledger authority.

The `35787` definition expands the official 22-file, 16,184-byte LDraw closure into 128 render triangles over 161 stored vertices. Its canonical source-frame diagonal occupies `x + z <= 0`; the mesh and three-cell connector set are mirror-self-symmetric across `x = z`, so this is an orientation convention rather than intrinsic chirality. A 66-box collision height field is measured from the official surface, while the exact LDCad subpart exclusively authors three underside cells at catalog positions `[-10,4,-10]`, `[-10,4,10]`, and `[10,4,-10]`; the absent `[10,4,10]` cell lies outside the occupied triangular half. The source probe sampled 374,750 collision points with none outside, found zero intrusion and zero face blockers at all three cells, and measured the first material at depth 4 LDU; the shipped mesh independently classifies all three center stops as recessed. Composite `0.987506819809668` has zero hard failures. These measurements prove clear insertion paths and a recessed center stop, not grip strength, full-footprint backing, physical stability, or hidden material. The native Builder record exposes one unframed family-15 node plus three sliders and is retained only as counterevidence; it is not merged into the selected LDCad route.

The `11253` definition expands the official 17-file, 28,352-byte LDraw closure into 690 render triangles over 705 stored vertices. Its irregular roller-skate envelope is deliberately classified as `minifig-accessory`, not projected onto the ordinary plate lattice. The official surface authors one upward stud at `[0,-4,0]`, even though the roller body reaches `y=-7`; admission therefore accepts an exact strictly interior local body-box face at `[-3,-4,-5]..[3,0,5]` while still rejecting a floating, edge-only, or centre-line-occluded seat. The 78-box conservative collision height field and source cylinder at exact measured radius `6.0001514980873605` LDU remain ordinary broad-phase and unconnected collision truth. A separately cross-bound nominal radius of 6 LDU is used only for the exact validated stud-clutch allowance edge to the LDCad-authored clutch at `[0,4,0]`; an unconnected overlap, a misaligned edge, or a third body still collides. The clutch insertion path is clear and the shipped mesh stop classifies as recessed. Composite `0.9150638159547076` has zero hard failures. The local-face check proves a source-bound, centre-line-exposed seat at the stud centre, not full-radius collision support, minifig grip, clutch strength, physical stability, or hidden material. The unframed native Builder record agrees only that one clutch exists and remains count-only counterevidence; it is not merged into the selected LDCad route.

The `15254` definition expands the official 15-file, 18,061-byte LDraw closure into 548 render triangles over 594 stored vertices, 334 unique. Its explicit source-to-catalog frame is yaw 90 degrees plus `[0,-24,0]`; the exact native Builder revision-J record and independently measured LDraw surface jointly pin that frame. The official LDraw stud ancestry authors six top studs, while the framed Builder record authors two underside end clutches. The source-derived conservative collision field contains 167 positive-volume boxes plus six source-radius stud cylinders, 173 primitives total. The containment probe samples 1,843,103 points from those emitted primitives with zero outside the official surface, finds zero intrusion and zero face blockers at both end clutch cells, and reaches composite `0.998083894257325` with zero hard failures. Those measurements establish the pinned frame, connector positions, occupied exterior and clear stud volumes at the two declared cells; they do not prove grip strength, hidden interior material, full-footprint backing, or stability.

The `41682` definition expands the official 14-file, 15,430-byte LDraw closure into 336 render triangles over 399 stored vertices. Its upright yaw-zero frame translates the source by `[0,6,0]` to exact catalog bounds `[-20,-14,-20]..[20,14,20]`. The exact pinned LDCad walk exclusively authors four underside clutch cells at the 2 × 2 grid and two z-negative side studs; LEGO Builder has no record for this design and grants no authority. The conservative collision field contains 54 positive-volume boxes plus two source-radius stud cylinders, 56 primitives total. The scorecard reaches composite `0.9762273815018168` with zero hard failures. These observations establish the exact visible surface, frame, six connector positions, occupied exterior, and measured allowance paths; they do not establish grip strength, hidden material, full-footprint backing, sideways attachment under the current upright-only policy, or stability.

The `2877` definition expands the official seven-file, 12,845-byte LDraw closure rooted at `sha256:46c3a0dc6329d3f0be9b3b4485b5e2e41295bad750a0f4c4b6a2e6631c0c0029` into 264 render triangles over 375 stored vertices. The exact native Builder revision-E record `sha256:10259d71b0993d4131d2267fcd9247a20f8593bb542a1d1eee36ad4041f029b8`, reviewed record and binary `sha256:6fc98f0a013a96db3c66dac27c9b34e4d79c88f8379b32f6c1116f346265d490`, and independently measured official surface select the proper Builder-to-LDraw frame witness `sha256:234d34ebbabe20f9d2a4b351b31ed2922356b63c7a0bd01203b2bb22ccbff78d`. Official stud ancestry authors two top studs, while the framed Builder record authors two underside clutches; the diagnostic LDraw underside tube is not female connector authority. The conservative collision field contains 26 positive-volume boxes plus two source-radius stud cylinders, 28 primitives total. The scorecard reaches composite `0.9754544869364302` with zero hard failures. These observations establish the exact visible surface, frame, four connector positions, occupied exterior, and measured clearance paths; they do not establish grip strength, hidden material, full-footprint backing, or stability.

The `3040` definition expands the official 11-file, 13,050-byte LDraw closure rooted at `sha256:12c4aaf0642dc49397b2f269a64c9298f1650fce51a97d463f910f81a9d8068e` into 178 render triangles over 184 stored vertices. The exact native Builder revision-F record `sha256:63ab72a4ff3b2d85b58af6586a1592124ab42019a84cb5faef137ee699836b28`, reviewed record and binary `sha256:17afd7907052b6e3e78343a6d26af45c81b7d277d80128b35e9f02c483905075`, and independently measured official surface select the canonical proper `turn0` Builder-to-LDraw frame with translation `[0,24,0]`, witness `sha256:65d6be01240cad2790e9fb54fabb056b99c232c26736b33b7340f8a85511a4bf`; the catalog declaration then applies yaw zero plus `[0,-12,10]`. Official stud ancestry authors the top stud, while the framed Builder record authors two underside clutches; the checksum-pinned LDraw underside tube remains diagnostic geometry rather than female connector authority. The conservative collision field contains 67 positive-volume boxes plus one source-radius stud cylinder, 68 primitives total. Ordinary collision retains the measured radius `6.0001514980873605`, while an exact validated stud/tube edge uses the independently admitted nominal 6 LDU source-rounding profile; an end-to-end kernel gate seats that stud into a plate and seats plate studs into both underside clutches, while its unconnected control still collides. The scorecard reaches composite `0.9856779517112434` with zero hard failures. These observations establish the exact visible surface, frame, three usable connector positions, occupied exterior, and measured clearance paths; they do not establish grip strength, hidden material, full-footprint backing, or stability.

The `4519` definition expands its exact four-file, 10,983-byte official LDraw closure into 176 render triangles at bounds `[-29.5,-6,-6]..[29.5,6,6]`. Root `parts/4519.dat`, source-titled `Technic Axle  3` and normalized for display as `Technic Axle 3`, credits James Jessiman, declares `Part UPDATE 2025-09`, and has digest `sha256:ecec609013e9d7af63c352cb61d990077c005c4c5453e121b3d192914ab55ff0`. Its 41 positive-volume collision boxes are surface-derived. The pinned direct LDCad shadow record `parts/4519.dat` at `sha256:f5c2c6057cae2fd6cd77bc50b932bb3692ee42645b3d034e3fe944333be68344` declares one exact capless, centered, sliding `SNAP_CYL` male axle route with `A 6 60`; the bounded source projection emits three discrete catalog seats at x `[-20,0,20]` with normals `[-X,+X,+X]`. The admission scorecard reaches composite `0.990325` with zero hard failures while retaining those source connectors as unscored rows under their separate exact-source gate. This is catalog admission only: it does not establish continuous sliding, grip strength, stability, or axle-through-bore collision allowance, and it does not authenticate the stale printed step-45 crop identity.

The `32064` definition follows the moved official root `parts/32064.dat` (`sha256:b6240d5798083701834cec8f566d7fca05cbc51123fad8500d3125fa68b4c465`) to `parts/32064a.dat` and expands its exact 23-file, 27,103-byte closure at manifest `sha256:85d190886138b76cb9a98f4d3351652d6fea64423bc576a4ef1116a99dfd686e` into 458 render triangles. Its conservative physical declaration contains 23 surface-derived body boxes, two source-radius stud cylinders, two top studs, and two underside clutches. The exact checksum-pinned LDCad composition through `p/axlehol5.dat`, `p/stud2.dat`, and `parts/32064a.dat` supplies one capless, sliding, `YOnly` female A6 × 1 axle-hole declaration; the pinned frame projects its raw `[0,10,0]` `+Z` route to catalog `[0,-2,0]` `+X`. The admission scorecard reaches composite `0.9694824982385262` with zero hard failures. This declaration establishes five discrete connector endpoints, not continuous sliding, axle-through-bore collision relief, grip, stability, insertion access, or a trusted placement. The axle-hole edge can therefore be structurally valid while the conservative body collision still blocks the pair.

The `11212` definition expands official root `parts/11212.dat` (`sha256:c527adbbc5db2983cdc9d0b28481d57a248fd0125d8aa00c13aebd7c32b6633f`) through its exact ten-file, 11,078-byte CC-BY-4.0 closure into 844 render triangles over 873 stored vertices. The square plate is quarter-turn symmetric, so yaw zero plus `[0,-4,0]` is the canonical representative. Body bounds are `[-30,-4,-30]..[30,4,30]` and visual bounds are `[-30,-8,-30]..[30,4,30]`. The conservative collision field contains 129 positive-volume boxes plus nine source-radius stud cylinders. The exact checksum-pinned LDCad walk consults `p/stud.dat`, `p/stud4.dat`, and `parts/11212.dat`; active `p/stud.dat` metadata authors nine studs, active root metadata authors nine clutch endpoints on the centered 20-LDU 3 × 3 lattice, and `p/stud4.dat` contributes no active meta because its anti-stud declaration is disabled. Ordinary collision retains the measured stud radius `6.0001514980873605`; only an exact validated stud/clutch edge uses the independently admitted nominal 6 LDU source-rounding profile, and a three-part kernel regression proves that each edge clears only its own pair while unconnected controls still collide. The admission scorecard reaches composite `0.9925023760839874` with zero hard failures. Revision-I record metadata from the checksum-pinned native pack reports nine clutches without a reviewed frame and remains count-only counterevidence rather than selected authority.

The `33909` definition expands official root `parts/33909.dat` (`sha256:8da6789db82746f179997ed4b917d00d34d03a6486d6aa27c76d17c9b21d8609`) through its exact nine-file, 10,203-byte CC-BY-4.0 closure at `sha256:72174370ab6b3d2e0d00d7b72a0687a67da1cccd4014f1f799e113eecb504a15` into 220 render triangles over 242 stored vertices. Yaw zero plus `[0,-4,0]` selects body bounds `[-20,-4,-20]..[20,4,20]` and visual bounds `[-20,-8,-20]..[20,4,20]`. The conservative collision field contains 41 positive-volume boxes plus two source-radius stud cylinders. The exact checksum-pinned LDCad walk consults `p/stud.dat`, `p/stud4.dat`, and `parts/33909.dat`, authoring two studs along one edge and four clutch endpoints on the regular 20-LDU 2 × 2 lattice. Ordinary collision retains measured stud radius `6.0001514980873605`; only an exact validated stud/clutch edge uses nominal 6 LDU, and a three-part kernel regression proves each edge clears only its own pair while unconnected controls still collide. The 401,980-point admission scorecard reaches composite `0.9955832518073061` with zero hard failures. Builder revision-E metadata is unframed and remains count-only corroboration rather than selected authority.

The `78329` definition expands official root `parts/78329.dat` (`sha256:79ec75c5092750b0f2022dab9c7561376d8b2b33fc3dea7059081ef273d4f7fc`) through its exact nine-file, 8,761-byte CC-BY-4.0 closure at `sha256:d203ae681cfa3842e210b894d46e69e555e64e638796d260c3a2cabdb474f283` into 460 render triangles over 489 stored vertices. Yaw 90 plus `[0,-4,0]` selects width-first body bounds `[-10,-4,-50]..[10,4,50]` and visual bounds `[-10,-8,-50]..[10,4,50]`; yaw zero would swap the declared one-stud width and five-stud length. The conservative collision field contains 39 positive-volume boxes plus five source-radius stud cylinders. The exact checksum-pinned LDCad walk consults `p/stud.dat`, `p/stud3.dat`, and `parts/78329.dat`, authoring five studs and five underside clutch endpoints at z `[-40,-20,0,20,40]`. Ordinary collision retains measured stud radius `6.0001514980873605`; only an exact validated stud/clutch edge uses nominal 6 LDU. The 659,766-point admission scorecard reaches composite `0.9968390298840539` with zero hard failures, zero sampled containment points outside, five of five clutch-room probes, and ten of ten lattice probes; sixteen triangles reached the bounded subdivision cap, so maximum effective spacing is `0.398360899` LDU rather than the requested `0.25`. Builder revision-F metadata reports five male and five female nodes without a reviewed frame and remains count-only corroboration rather than selected authority.

The `99563` definition expands official root `parts/99563.dat` (`sha256:c2706e2934e9fd6f4ba07a7155f83952ad5937c3b4f187ea903a3148b045307e`) through its exact ten-file, 12,212-byte closure at `sha256:80b536b9d216c1db5a225b67f9bd852d98a7faf8474016984fdbf3a2dc06f9a4` into 228 render triangles. Yaw 90 plus `[0,-4,0]` gives exact body and visual bounds `[-10,-4,-20]..[10,4,20]`; twenty conservative boxes represent its material. The exact LDCad route supplies three underside seats at `[0,4,-10]`, `[0,4,0]`, and `[0,4,10]`. These are three exact ports but only two shared physical capacity cells on the same part instance: the negative outer port claims `99563:negative-z-half`, the center claims both negative and positive cells, and the positive outer claims `99563:positive-z-half`. Consequently the two outer ports may be connected together, while the center plus either outer is rejected as `PORT_CAPACITY_EXCEEDED` independent of connection order. Composite `0.9536790218994491` over 444,058 sampled points has zero hard failures; the half-pitch center is admitted only through the exact reviewed three-seat route and shared-cell declaration, not as generic square-grid connector authority.

The `73230` definition expands official root `parts/73230.dat` (`sha256:ec023f3811316edc70dd0422b1c87ed2630cc299b63382168076947414a3deaf`) through its exact eighteen-file, 20,939-byte closure at `sha256:75581e2b138e94f7060a86cabda442b9b134375d7ef84d16c3453f3907863cf7` into 294 render triangles. Yaw 90 plus `[0,-12,0]` gives body bounds `[-10,-12,-10]..[10,12,10]` and visual bounds `[-10,-16,-10]..[10,12,10]`; ten conservative body boxes plus one exact-source-radius stud cylinder represent its material. Exact source routes author a stud at `[0,-12,0]`, clutch at `[0,12,0]`, and transverse axle hole at `[0,-2,0]` with normal `[-1,0,0]`. Composite `0.9666579470236067` over 339,052 sampled points has zero hard failures. The discrete axle-hole endpoint does not establish continuous sliding, bore collision relief, insertion access, grip, or stability.

The `35464` definition expands official root `parts/35464.dat` (`sha256:0c4a292f9040030ca40a705c617f22a12357975f47dc18d3aaa1b353aa05966b`) through its exact five-file, 3,703-byte closure at `sha256:7df7208fa114b44aa9e4104b1693d614a480d1b0336224c87dfa2b8d0a595c28` into 52 render triangles. Yaw zero plus `[0,8,0]` gives exact body and visual bounds `[-10,-8,-10]..[10,8,10]`; 75 conservative boxes represent its material and the exact LDCad route supplies one clutch at `[0,8,0]`. Composite `0.9732142857142857` over 143,676 sampled points has zero hard failures.

The `49307` definition expands official root `parts/49307.dat` (`sha256:5ab186e08cf7dfd9ff7ce2e1afcd69d08479fd44972340d53d78f67ffdceb135`) through its exact seven-file, 5,912-byte closure at `sha256:dfe1399ae48a4097300f04b2181e8733521e3d8d14c8c3468aeda09e59c36dce` into 100 render triangles. Yaw zero plus `[0,8,0]` gives exact body and visual bounds `[-10,-8,-10]..[10,8,10]`; 75 conservative boxes represent its material and the exact LDCad route supplies one clutch at `[0,8,0]`. Composite `0.9836439801158138` over 225,162 sampled points has zero hard failures.

These four `/27` declarations establish only their exact catalog identities, source surfaces, frames, connector fields, conservative collision fields, and reviewed exterior evidence. They do not authenticate any printed crop or element-to-callout relation, assign an official Brick UUID, choose a booklet search frame, publish coverage or an action ledger, authorize placement or replay, mutate a document, accept a candidate, or advance the verified prefix.

The historical `/20` production editor audit searched for `2877` and used `Place at origin`. It retained a globally valid one-part document at `sha256:c692331b132f52a4aaee3934dc88ad7dd99e7478862a1800b432fdb019c7b76f`; the 194,264-byte editor screenshot is `sha256:3346e445323a8ae300dd75fc3f308ff64cf1e807a12e35b25421725dcb50bdab`, all seven canonical views were inspected, and the run recorded no console or page errors.

The historical `/21` production editor audit searched for `3040` and used `Place at origin` after the connection-profile repair. It retained a globally valid one-part document at `sha256:752fc4da4bf61139d24d14743443a4b8d5fd3cbbe4f21f1986e8d13a8d2f50a0`; the 123,501-byte full editor screenshot is `sha256:cb9e123e4979cf3728dc880a28e584f4ed0fe28a7333ff0c8d1322e35c5c77f4`, all seven canonical views and the filtered palette were inspected at original resolution, and the retained run records empty console- and page-error arrays.

The historical `/22` production editor audit searched for `4519`, confirmed 94 total definitions and three parts in the Axles family, and used `Place at origin`. The one-part document is globally valid and hard-valid at `sha256:eaac23045e78e09b3f71bc83ff906d10db5cffb7349c2370d44c55dd55174e9a` under truth `sha256:7f64021239ab6395a3666f1f72908fd420b73065909822bc68e5226785bfa12e`, with rendered bounds `[-1.475,-0.3,-0.3]..[1.475,0.3,0.3]`. The full UI plus isometric, front, back, left, right, top, and underside views were inspected at original size and showed the expected axle geometry and framing. No app-logic errors were observed; the console retained one favicon 404 plus WebGL precision and shadow-map deprecation warnings, so an empty-console claim is not made. This remains an immutable `/22` audit rather than a current `/28` editor claim.

The 2026-08-24 local `/23` Vite development-server observation searched for `32064`, confirmed 95 total definitions and two parts in the Technic bricks family, selected the sole result, and used `Place at origin`. The one-part document was globally valid with no validation issues at `sha256:1293be3ca50674cf12534ad8a29dc4da67f9a4694ca4f19ccde002b2827c8d26` under the truth reported by that run, with rendered bounds `[-0.5,-0.6000000000000001,-1]..[0.5,0.8,1]` and no fallback geometry. The 140,108-byte full-editor screenshot is `sha256:1dc0ee3dce94bd2bf67796bf6065cfcbf11e2c09996cb5e01a2b43a744390e4f`, and the audit JSON is `sha256:a6702297dda4f61ee379e038e4955ef2cb9504c515745982a83d58b64adf9feb`. The full UI, filtered palette, and isometric, front, back, left, right, top, and underside views were inspected at original resolution and showed the expected two open studs, transverse axle opening, body walls, and exposed underside structure. No console or page errors occurred; the non-fatal messages were the existing Three.js shadow-map deprecation and Chromium readback-stall warnings. The ignored audit binds document, truth, view, and image hashes but not the app-source manifest, executed script, browser build, or a production build, so it is a dated local pixel observation rather than an exact current-source or production-editor receipt.

The 2026-08-24 local `/24` headless Playwright observation searched for `11212`, confirmed 96 total definitions and 31 parts in the Plates family, selected the sole 844-triangle LDraw-backed preview, and used `Place at origin`. `render_app_to_text()` and `get_model_snapshot()` agreed on one globally valid part, zero connections, zero validation issues, and document `sha256:ae554c96f474c9e0b7f2c7de9c4fbd59aed09617e8e01950077aa68d02b6d6b3` under the then-current `/24` truth; the renderer reported bounds `[-1.5,-0.2,-1.5]..[1.5,0.4,1.5]`, four geometries, four textures, and no context loss. The 206,633-byte full-editor screenshot is `sha256:d867cb32c673873d163406c3612287c3cd93e830f04b625914f382d0c4935b09`, and the 11,874-byte observation is `sha256:511cf592b8f902cce0efb88b937f8af66573086865f2dd0eae88a6d8e803dbdc`. The full UI, filtered palette, and all seven canonical captures were inspected at original resolution; the isometric and side views show the expected nine studs and plate profile, while the straight underside capture is occupied by the coincident build grid and is not used to certify tube geometry. The separate clean-scene underside-oblique admission pair exposes and matches the four tube rings, ribs, walls, and cavities. No console or page errors occurred. The ignored editor audit is a dated local pixel observation, not an exact current-source or production-editor receipt.

The 2026-08-24 local `/25` headless Playwright observation searched for `33909`, confirmed 97 total definitions and 32 parts in the Plates family, selected the sole LDraw-backed preview, and used `Place at origin`. `render_app_to_text()` and `get_model_snapshot()` agreed on one globally valid part, zero connections, zero validation issues, and document `sha256:1694c915d36a1a4110193046949ffb10df41dfa61d60783683d02634c336d065` under then-current `/25` truth. The 198,617-byte full-editor screenshot is `sha256:cbd823589a92c7e6fb23f9522d2bae624358e2d79cf4c3b14f310431ecf6b03c`, and the 12,112-byte observation is `sha256:7b673bfca74ecf97cfbea02c01b993356348f30e566c9162e5ce1e0b897b8f12`. The full UI, filtered palette, and all seven canonical captures were inspected at original resolution; the straight underside capture is occupied by the coincident build grid, so only the separate clean admission underside-oblique pair is used for the ring-and-cavity claim. No console or page errors occurred. The ignored editor audit is a dated local pixel observation, not an exact current-source or production-editor receipt.

The 2026-08-24 local `/26` headless Playwright observation searched for `78329`, confirmed 98 total definitions and 33 parts in the Plates family, verified the sole preview resolves `ldraw:official:78329.dat` with all 460 triangles, and used `Place at origin`. `render_app_to_text()` and `get_model_snapshot()` agreed on one globally valid part, zero connections, zero validation issues, and document `sha256:7365cbf1980f43536b40472b1b8857856a635730a49ba9a9501503337488800f` under its then-current `/26` truth. The 199,383-byte full-editor screenshot is `sha256:be21961042d5eb17668ded5c93d523662aa78e51dea5110bd90da98e7e790a7d`, and the 13,276-byte observation is `sha256:71acb1ce24c0fb3e46939cd18c75127dc0ad507f4493d81b1ed365af2e72a5db`. The full UI, filtered palette, and all seven canonical captures were inspected at original resolution; the straight underside capture is grid-occluded, so only the separate clean admission underside-oblique pair is used for the internal-rail, tube, and cavity claim. No console or page errors occurred. The ignored editor audit is a dated historical pixel observation, not an exact current-source or production-editor receipt.

## One part, four representations

Each representation answers a different question. None is authority for another.

### 1. Render surface

The viewport and palette preview resolve the same catalog definition. Parametric parts derive visible geometry from bounded features; mesh-backed parts use preloaded LDraw triangles, source-derived normals, a pinned content hash, and one explicit asset-to-catalog frame.

Render geometry is disposable view data. It cannot authorize placement, connection, collision, catalog admission, or a claim about hidden physical material. “Exact source geometry” in this repository means the admitted LDraw triangles, loader-faithful normals, and pinned source-to-catalog frame, not pixel identity across independent render paths or access to LEGO production CAD.

### 2. Construction lattice and bounds

Placement uses LDraw units and legal catalog orientations. Footprints, nominal heights, exact body bounds where needed, and connector centres provide the lattice used for enumeration and pruning.

Exact decimal bounds are retained when a measured extent cannot be projected safely as an ordinary binary float. The catalog derives the float projection from that declaration and refuses inward rounding. Off-lattice articulated poses retain their rigid transform and use geometric validators rather than becoming lattice truth.

### 3. Connection field

Catalog ports carry local transforms, capacity, gender, profile, and one of ten implemented kinds: `stud`, `undersideClutch`, `axle`, `axleHole`, `pin`, `pinHole`, `bar`, `clip`, `hinge`, and `hingeSocket`. Six pair rules cover stud/clutch, axle/axle-hole, axle/pin-hole, pin/pin-hole, bar/clip, and hinge/hinge-socket behavior.

Placement discovery consumes those rules, but document edges and attach programs still serialize only `stud-tube`; referenced ports imply the actual pair. A versioned pair identity is still needed before the full taxonomy is represented end to end.

A visible cylinder or cavity does not prove a functional connector. Builder or LDCad fields may supply that claim only through a pinned source and frame, with insertion volume and backing checked against independent geometry.

### 4. Collision and physics

Collision is a bounded union of simple primitives for deterministic validation and compound rigid bodies. It must contain represented outer material while leaving functional connector paths open.

Parametric features derive collision where that derivation is justified. Twenty-two fully measured mesh definitions use conservative column height fields; sixteen render-only promotions preserve their earlier conservative recipes. Neither mode proves that every visible cavity is physically usable.

Physics derives rigid components and joints from the connection graph, then builds compound bodies and geometric mass properties from collision. Physical mass remains unknown in current inventory metadata; repeated source defaults are not admitted as truth.

## Declaration and derivation

Repository-authored blueprints produce render recipes, nominal bounds, connector fields, collision primitives, legal orientations, search metadata, and part-standard facts from one bounded declaration. A feature may emit only the representations it can justify; missing geometry is a failing standard, not an invitation for a silent exception.

External meshes deliberately split authority. The generated full-measured path may carry reviewed render, connector, and collision evidence. The render-only path can carry only source closure, visible geometry, normals, bounds, frame, stud witnesses, and attribution; the catalog factory overlays those render fields onto the preceding physical definition and proves the preserved fields unchanged.

The bounded generator verifies archive hashes, path and expansion limits, BFC winding, LDrawLoader-equivalent hard edges, frames, bounds, visible stud witnesses, provenance, and reproducible generated bytes. Runtime admission verifies hashes, indices, normals, finite bounds, represented body collision, and resource ceilings. These checks produce evidence; they do not replace reviewed admission.

Collision containment is a finite barycentric-grid measurement, not a proof over the continuous surface. The requested spacing is validated as finite and positive, each triangle is limited to 256 subdivisions, and current emission reports distinguish the legacy requested-spacing alias from the maximum effective grid spacing and number of cap-limited triangles; zero outside means zero only over that disclosed sample population. The `/23` `32064` row requests 0.25 LDU, reaches a maximum effective spacing of 0.25 LDU without hitting the cap, and samples 561,932 points, while eight older rows disclose their coarser cap-limited resolution instead of presenting the request as achieved.

Collision-field generation discards zero-volume boundary-clamped cells before scoring or emission; a synthetic regression gates that failure class so a surface-only boundary sample cannot masquerade as a positive collision body.

## Semantic part standard

A part must draw what it claims.

- A declared stud is visible at its declared centre and exact radius.
- A declared underside clutch has a visible entrance, cavity, walls, and grip geometry from below.
- Collision neither omits represented outer material nor silently seals a claimed connector path.
- Visual bounds contain the render without inward rounding; collision is checked under its declared mesh-derived or preserved-recipe mode.
- Connector backing, reachability, capacity, and compatibility are independently checked.
- Chiral identity, legal orientation, and source frame agree with the visible hand.
- Palette preview, viewport geometry, catalog metadata, and retained admission images resolve the same definition.

`scripts/check-part-standard.mjs` automates seven current declaration rules: underside geometry, stud radius, declared geometry mode, declared collision, hollow clutch-bearing bodies, body containment, and stud extent. Connector reachability and compatibility, chirality, source frames, render/collision parity, palette parity, and matched visual inspection remain separate test and review obligations.

## Render-only promotions and remaining physical limits

Printed step 4 first exposed the old rendering failure: its underside art shows walls, hollow rings, ribs, and cavities where the candidate drew an almost solid slab. `/12` replaced the four implicated surfaces; `/13` replaced the remaining twelve flat semantic undersides and added source-faithful normals to all 24 mesh assets. All sixteen promotions preserve their preceding physical fields.

The twelve `/13` promotions cover five wedge plates, two arches, three curved slopes, and two cheese slopes. Their measured underside shapes differ enough that one generic plate shell would be wrong:

| Catalog part or mirrored pair | LDraw design | Durable underside measurement |
| --- | --- | --- |
| `wedge-plate-2x4-left` / `right` | `41770a` / `41769a` | 4-LDU cavity; 1,713 LDU^2 area |
| `wedge-plate-2x3-left` / `right` | `43723a` / `43722a` | 4-LDU cavity; 1,246 LDU^2 area |
| `wedge-plate-3x6-right` | `54383` | 4-LDU cavity; 3,841 LDU^2 area |
| `arch-1x4` | `3659` | Cavity 4 LDU from top; 768 LDU^2; 8-LDU end walls |
| `arch-1x6` | `3455` | Cavity 4 LDU from top; 1,248 LDU^2; 8-LDU end walls |
| `curved-slope-1x2` | `11477` | Stepped levels 12, 10, 8, and 4 LDU above bottom |
| `curved-slope-1x3` | `50950` | Stepped levels 8, 4, and 1 LDU above bottom |
| `curved-slope-1x4` | `61678` | Stepped levels 20, 16, 8, 4, and 0.2 LDU above bottom |
| `cheese-slope-1x1` | `54200` | 1-LDU recess over most of underside; 4-LDU, 4-LDU^2 pocket |
| `cheese-slope-2x1` | `85984` | 1-LDU recess over most of underside; 4-LDU, 8-LDU^2 pocket |

LDraw supplies no female connector authority. The pinned LDCad replay agrees with the preserved clutch centres for only `11477`, `54200`, and `85984`, supplies none for seven designs, and disagrees for `61678` and `54383`. That is why `/13` admits these twelve as render-only changes instead of laundering generated connector or collision values into catalog truth.

The source surfaces and underside checks are now green, but conservative collision remains a known approximation. A visually hollow ring is not automatically an open collision volume, and an exterior packet cannot certify an unexposed interior. Hidden claims are `not-observable` until an interior, cutaway, insertion, or other independent witness exposes them.

## Identity, versioning, and sources

A `PartDefinition` carries a namespaced catalog ID, LDraw design identifier and aliases, family and searchable name, geometry recipe and content hash, bounds, collision primitives, connectors, legal orientations, colors, substitution grouping, inventory metadata with physical mass currently unset, and source/license/attribution provenance.

Catalog IDs are semantic identities, not filenames. A changed shape, connector field, collision model, hand, or source frame is changed truth even when its display name stays similar.

Every catalog-truth change advances `BUILTIN_CATALOG_VERSION`, retains supported prior snapshots in `MIGRATABLE_CATALOG_VERSIONS`, and emits an explicit migration report. Each reviewed historical full-truth hash binds its exact immutable catalog-part/color roster and a source-commit-derived connector projection covering port frames, shared capacity cells, compatibility, effective pair rules, connection-authorized allowances, matching stud collision primitives, and any validated stud profile. Document constraints may be a legitimate subset, but a future identifier in either those caller-owned constraints or an actual part blocks migration instead of becoming current truth; a connection using a later, removed, or semantically changed endpoint also blocks, while stable endpoints and unrelated draft-invalid transforms carry forward. `npm run migration-history:check` re-archives all 29 reviewed source truths from their source commits, proves their endpoint and reachable-pair roots, and compares the exhaustive source/current deltas to the compact runtime authority. The current `/28` projection has 2,326 endpoints at `sha256:d6759940824fb337ffbbe8d99c8461f74c945bdf3d2cd7a17113523d64c382ba`. Historical `/27` remains pinned at 2,319 endpoints at `sha256:f72d9f2d06d5e593d1e6b00bff4c31d02f07a42f230b18d15963a5b437d36ad0`; its four additive rows contributed eight endpoints over `/26`. The two additive `/28` rows contribute seven endpoints while every shared `/27` identity remains unchanged; the exhaustive registry retains 22 endpoint deltas across earlier truth rows, three reachable pairs at unchanged `sha256:7431a242907aa9829ead6a279d0b530fe5f5d00ee31e6ddc1576fe66a8a07add`, and zero reachable-pair deltas. Saved documents pin catalog and other truth snapshots and are never silently reinterpreted.

Repository-authored blueprints own parametric declarations. LDraw owns the admitted render surface, normals, exact bounds, and file-level attribution of 43 mesh-backed parts. Their 43 root closures resolve to 228 unique source files by 33 authors; 224 files declare CC BY 4.0 and `parts/2453b.dat`, `parts/30503.dat`, `parts/32064a.dat`, and `parts/3245c.dat` offer the selected CC BY 4.0 option within a dual-license expression. The ordered 104-row `[partId, geometry.contentHash]` roster is `sha256:60ce979e2b452eb8d3a233f769e7e15ac3a5f5af84ae8b802d94d5cbb8d6b0b2`. Nineteen fully measured definitions retain exact LDCad connector routes. Builder and LDCad may own connector evidence only where their exact records, frames, licenses, and independent checks are pinned. Builder collision and repeated mass defaults are not catalog truth.

Every source's origin, revision, hash, license, attribution, redistribution and training rights, and allowed role live in the bill of materials. Permission to render geometry never implies permission to train on it.

## Admission checklist

The reproducible admission workflow is documented in the [part visual-admission runbook](../runbooks/part-visual-admission.md). It keeps the editor's seven-view presentation hook separate from the clean eight-view source-versus-catalog review packet.

Before a new or modified part enters catalog truth:

1. Settle stable identity, family, design ID, hand, aliases, and search name.
2. Record every source closure, revision, hash, license, attribution, and allowed role.
3. Prove one catalog frame aligns visible geometry, bounds, connectors, collision, and legal orientations.
4. Pass the automated part standard and matched top, bottom, front, back, left, right, isometric, and underside-oblique review; add interior or cutaway views where the exterior cannot expose a relevant cavity.
5. Establish connector claims from an authored or repository-owned basis and test insertion, backing, capacity, and compatibility.
6. Review collision separately for represented material, functional voids, and resource limits.
7. Verify palette search/preview and production viewport resolve the admitted geometry.
8. Update catalog migration, schema fixtures, notices, bill of materials, and durable review conclusions.
9. Run focused catalog, renderer, migration, generator, and browser checks followed by `npm run verify`.

Packet generation always leaves review outcomes pending. A separate immutable sidecar binds the packet and both image hashes for each ordered view; only an explicit `same`, `different`, or `not-observable` review closes that evidence. Tests and image metrics support this decision but cannot make it automatically. The completed `/13` sidecars follow this boundary; their all-`same` result is explicitly limited to surfaces visible in the eight retained views.

Missing parts remain work items. A booklet step is never completed by substituting a similar design, dropping a piece, mirroring a hand, or weakening a hard check.
