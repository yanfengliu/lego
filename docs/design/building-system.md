# Building system: assessment and plan

An assessment of this repository against a full brick-building-system specification, and the design for what is missing.

Written because the specification describes a system largely already built here, and the expensive mistake would be to build it a second time alongside.
Read `part-model.md` first for how a part itself is organised; this is about the system around it.

It is not the goal. The goal is in `spec.md`: read a printed instruction booklet, build the set it describes, verify each step against the booklet's own picture, and play the result back.
Everything here serves that. Parts exist because the loop cannot place a wedge plate it does not have; physics exists because a model that has been built should be shown standing up and moving.
When the two compete, the booklet wins — a building system with nothing to build is the more comfortable problem and the wrong one.

## Against the specification

**Already built, and should be reused rather than replaced.**

The construction coordinate system is exact integer LDU with stud pitch 20, brick 24, plate 8, stud 4, and four canonical quarter turns — `packages/catalog/src/constants.ts` and `transforms.ts`.
`PartDefinition` and `PartInstance` exist with stable ids, aliases, families, substitution groups and provenance; a part instance carries its submodel, step, semantic tags and provenance.
Connectors are typed and gendered with positions, axes and profiles, and connections are explicit edges.
Placement has a live ghost preview with a valid or blocked verdict, and refuses an unsupported placement at the command rather than flagging it afterwards.
Collision separates connector engagement from solid overlap: a stud may enter a matching clutch to a declared depth, and everything else that shares space is refused.
Colours are a stable palette of 45 ids, changing one edits instance data.
Undo and redo, project save and load, LDraw import and export, a searchable catalog panel grouped by family, and broad unit and browser regression suites all exist.

**Partly built.**

Connector kinds are `stud` and `undersideClutch` only — nothing articulated.
Selection is single-part; there is no multi-select or box select.
The asset pipeline has the parametric box, wedge, cylinder and analytic-plan generators, the LDraw fact reader, and a closed preloaded-mesh renderer whose immutable registry caps asset, vertex, triangle and byte totals, whose successful frame-resolution cache is a fixed-size LRU, whose admission checks content hash, generic asset-local frame and represented connector/collision/body/visual bounds, and whose finish-aware scene budget accounts for indexed sources, de-indexed instruction fills, role splits, colors and outlines.
Its production mesh registry now holds the eight bundled set 6651557 meshes admitted at `builtin.basic-parts/7` and `/8`, so the capability is exercised by real parts rather than only by fixtures.
The retained six-part 6651557 pilot was the measurable intermediate for that admission: six exact LDraw closures (70 unique files), checked file-by-file against the complete exact 439-record source audit, cover six designs while independently rehashed Builder native record slices cover five, and its pressure rows showed where fractional bounds, non-upright connectivity and oriented collision exceeded the representation. Five of those six are admitted — 5092, 35480, 51739, 77844 and 93273, composites 0.9947/0.9712/0.9901/0.9961/0.8937 with zero hard fails — taking their render mesh from bundled LDraw geometry, their collision from that surface's per-column height field, their connectors from Builder's authored field through the pinned per-part frame, and their extents from the exact closure. The fractional-bounds pressure is answered by the exact bound representation and two of the five need it. Three more followed at `/8` — 30357, 2450 and 79491, composites 0.988908/0.989972/0.990096 with zero hard fails — taking their clutch cells from the LDCad shadow library, because the Builder pack has no record of any of them and LDraw geometry supplies no female connector at all. That is what moved the booklet: the retained coverage report's covered prefix ran to step 15, step 16's only missing design was one of the three, and admitting them takes the first uncovered step from 16 to 26. Four further LDCad-covered designs were scored and refused — 44237 and 93888 on catalog identity, 41682 and 3814 on a `male-connector-over-claim` hard fail. Non-upright connectivity and oriented Builder collision still exceed the representation, and neither is used by these eight. The catalog holds 85 parts; the set needs 121 leaf designs, so eight admitted is a start and not coverage.
Collision does a one-dimensional sweep on x with an early break, which is sweep-and-prune, but connector lookup is linear.

**Not built at all.**

Physics: Rapier drives compound bodies and revolute joints from the kernel's descriptors. No simulation mode in the app yet, and no support model beyond the static placement check.
GPU instancing is absent: each part is a `Group` containing one or more body meshes plus separate stud meshes, so a 1465-piece model produces more than 1465 meshes (and instruction rendering adds outline objects).
Inertia is absent from `PartDefinition`; mass and centre of mass are derived from the declared compound solid, with an optional known-mass override.
No profiling metrics.

## One disagreement worth stating

The specification makes the connection graph the source of truth for how parts are attached.
This repository does not, and should not change: part transforms are authoritative and connection edges are validated annotations, recorded in `spec.md` as an invariant.

The reason is the booklet.
An instruction booklet gives placements, never connections, so the closed loop derives which studs meet which tubes from geometry — and if edges were authoritative, a document could assert a connection the geometry contradicts, with nothing able to say which was right.
Deriving edges from transforms can only be wrong in one place; making both authoritative can be wrong in two that disagree.

Everything the specification wants from the graph — connected components, rigid boundaries, splitting on deletion, subassembly selection — works exactly the same on derived edges.
The difference is only which one gets rebuilt when they conflict.

## Where the booklet build actually is

Measured, not asserted. Every figure here comes from a real run and is reproducible by the commands named below; if one disagrees with the code, the code is right and this section is stale.

**The build places eight pieces and completes a five-step prefix.** Printed steps 1 to 5 all report `complete` — by deferred lookahead, by exploded ghost, and by their own highlights — with `piecesPlaced` 8, `finalParts` 8 and structural hash `sha256:c37d7b5991aa525a215e8b65fbbc2140199653816351af1924f8ac10ac705e38` at `LEGO_REAL_BUILD_LAST_STEP=5`. Step 5 is the first panel whose contours all stay **open**, and its two pieces are `builtin:plate-2x4` in Dark Green at `[60,8,0] upright-yaw-270` and `builtin:plate-2x14` at `[160,8,100] upright-yaw-270`. Step 4 is the booklet's first panel drawn from underneath, and its two pieces are `builtin:plate-1x8` at `[0,8,30] upright-yaw-270` and `builtin:wedge-plate-4x4-cut-corner` at `[-20,8,80] upright-yaw-180`. The four-step and three-step prefixes place 6 and 4 pieces and hash `sha256:3475cc89448bcaf018ba3bd06d9b609311f1d7ee139087ee480327ace5ca3ec8` and `sha256:b343e96d6693bd59b239843577c46f128d87ce656096dcd32dea86152e765071`.

All three of those hashes moved at catalog `builtin.basic-parts/9`, and none of them moved because a placement did. `structuralDocumentValue` hashes the document's pinned truth snapshot along with its parts, so a catalog version bump necessarily re-hashes every document that pins it. The proof is exact rather than argued: restore the two digests the bump moved — `truth.catalog` back to `builtin.basic-parts/8` / `sha256:a9adf38b…` and `truth.collisionModel` back to `sha256:8f181d6f…` — in each retained `document.json`, change nothing else, and the three re-hash to `sha256:f15eadfa…`, `sha256:64017a83…` and `sha256:6d46478c…`, bit for bit. Step, piece and part counts are identical (5/8/8, 4/6/6, 3/4/4), and every placed transform is the one recorded above.

What stops the run reaching `completed` is unchanged and is not a placement failure: the completion audit compares every placed transform against the official ledger's and finds them in a different world frame, and `visual-evidence-unverified` stands until a Node-side visual audit exists. Everything below that says "zero pieces" describes a twelve-step request, which is still zero for the reasons that section names.

### What stops printed step 6, stated where it actually stops

Printed step 6 defers: its own panel prints a highlight — one region, 1470px of stroke, closed — and still cannot separate its best two candidates. So it needs printed step 7's panel, and requesting only six printed steps can never settle it, because `selectRequestedPanelPages` filters the panels to the requested prefix and the last requested step therefore has no deferral target. A prefix that does not fully place cannot seal, so a six-step request produces no `score.json` at all rather than a shorter one.

Requested through step 7, step 6 scores and refuses with a number. It was **`weak-deferred-agreement` at 0.4482 over 36 whole-step candidates against the 0.85 bar**; three walls came down and it is now **`ambiguous-deferred-placement` at a margin of 0.00328 — 0.8837 against 0.8804 — over the same 36 candidates, against the 0.02 noise floor.** The winner clears the absolute bar outright. What is left is a different question from the one that was being asked: not whether the prefix is the drawn assembly, but whether panel 7 can tell its best two apart.

The first was printed step 7's contour. It prints **two open contours — 1338px of stroke enclosing no filled region** — and the deferral subtracted the settling panel's *filled* highlight to stop charging step N's prefix with drawing step N+1's pieces. That refusal was printed step 5's mistake one level up, and about half this booklet's contours are open, so a lookahead that can only read a closed one cannot finish the booklet. With nothing to subtract, panel N+1's built art is what step N built **plus** what step N+1 places — a superset of anything a step-N candidate can draw — so the measure becomes containment and the term charging a candidate for ink no candidate could own is dropped, exactly as `rankStepDelta` drops precision on an open contour. `DeferralEvidence` records which measure decided it as `lookaheadMeasure`, and the separation margin is unchanged.

Containment's own ceiling is known and is why the margin gate carries the weight. A candidate that fits wholly inside the panel's art scores exactly 1 wherever it is, because the registration maximises over translation and will slide it there — measured on the deferral fixture, where the drawn panel separates its candidates and a displaced copy of the same art lets several reach 1 together and refuses with `ambiguous-deferred-placement` at a margin of zero. So containment cannot pick between candidates the drawing swallows; it refuses instead of guessing, and what it buys is that an open contour produces a number at all.

The second was a cost ceiling. Step 6's four pieces narrow against its own panel in `1 of 187 x 2 of 534 x 2 of 627 x 10 of 553` — 4187 renders against a 4096 budget. Raised to 8192, on the same basis as `maxRendersPerPiece` 220 → 1024: it refuses rather than truncating, so no step is narrowed against a set that was silently cut, and no correctness bar moved.

The third was the camera, and 0.4482 was what said so. The same registration measures 0.9031, 0.8898 and 0.8312 on panels 2, 3 and 4. Under containment, 0.4482 meant less than half of what a twelve-piece prefix draws landed anywhere on panel 7's art — and all 36 candidates share the same eight settled pieces, differing only in where four new ones sit, so a miss of that size was a whole-prefix miss and not a placement one. `output/camera-fit/overlay-007.png` showed it directly: the fitted grid's marks landed nowhere near the panel's tube rings and its folded cell was diagonal streaks, where `overlay-006.png` puts a mark on each of 151 studs at a residual of 0.086px and `overlay-004.png` does the same on 57 sites of the booklet's other underside panel. Being underside was never the explanation — printed step 4 is too, and fits.

**Two changes to the lattice ranking fixed it, and they are only correct together.** The same overlay now reads `azimuth 55.15  elevation 35.31  40.20 px per stud, residual 0.123px`, with **174 predicted studs sitting on the panel's drawn tube rings** and a folded cell that is a ring rather than a smear. 15.95 points per stud against neighbours at 15.99 and 16.10.

- **`canonicalPair` is order-free.** A peak is a repeat and not an arrow, so which of a pair is `a` and which is `b` is a fact about their directions and never about the order they arrived in. Printed step 7's grid steps are (23,19) and (-33,13); the left-pointing one is the picture's strongest repeat and the right-pointing one only its ninth, so the pair always arrived left-first and was silently dropped. Written as "put each below the horizon, then read the roles off the sign of `x`" this is *wrong* — a vector with `y = 0` is the same repeat under either sign and is a legal `a` at azimuth 0 and a legal `b` at azimuth 90, and fixing its sign costs printed step 38 its own `a=(8,0) b=(-3,14)` candidate entirely. Trying the existing assignment both ways is the whole fix.
- **`explainsStrongestPeak` ranks above the explained-peak count.** The count cannot refuse a finer lattice: a refinement has a site wherever the true grid has one, so it explains at least as many peaks *and* picks up the sub-pitch repeats a drawing carries — the ribbing under a plate, the hatching on a hull. What it does not get for free is the strongest repeat. Printed step 4's fine lattice `a=(14,6) b=(-25,21)` explains 7 of that panel's 16 peaks against its own grid's 6 and has **no site at (36,14)**, the repeat the picture attests to most loudly, while sitting 23.75% of pitch from any axonometric projection against 0.47%.

The two were measured separately and together over all forty panels, and that matrix is the argument: order-free alone fits 35 but drops printed step 4 to a refusal at 10.34 points per stud and accepts printed 38 at 7.43; the strongest-peak key alone changes nothing at all — same 33 fitted, same 7 refused. Together: **35 fitted, 34 of them at the booklet's own 15.93 to 16.54 points per stud**, printed steps 4 and 7 both fitted, printed 31, 35, 38, 39 and 40 still refused, 8 camera runs, and every assertion in the forty-panel harness unchanged.

**Ground truth by construction agrees, which is the check that killed the last five ideas.** Swept over 735 synthetic panels — 7 plate shapes, 7 azimuths, 5 elevations, 3 scales, each drawn under a camera the fitter is never told — recovery goes from **476 to 532** and mis-fits from **83 to 53**; 75 cases move into "recovered" and 19 move out, 17 of those to a refusal rather than a wrong answer. Four of the gains are pinned in `camera-fit-lattice.test.ts`, and that test fails without the change.

Three rankings were refuted along the way, all by the synthetic grids in `camera-fit-lattice.test.ts` or by the panels themselves, and the numbers are in `canonicalPair`'s comment:

- **`foldedStudShape`'s radial contrast per candidate**, which the previous pass named as the next lever. On the synthetic grids it is emphatic and right — the true basis scores 14.9 against 5.6 for its nearest rival at azimuth 34, and 20.0 against 3.1 at azimuth 62 — and on the booklet it ranks the panel's own grid first on **11 of 36** answerable panels, fitting 11 with 4 wrong acceptances where the count fits 33 with none. It *does* separate every impostor named here from its own panel's grid; what it cannot do is beat a third candidate that folds cleaner still.
- **The explained set ranked lexicographically by peak strength** — prefer the candidate explaining the strongest repeat the other misses, which subsumes the count outright. It fits printed 4 and 7 and then accepts printed 31 at 6.7 points per stud and printed 32 at 5.8, on lattices built from those panels' own sub-pitch repeats.
- **Harmonic response under the strongest-repeat key** ranks the panel's own grid first on 35 of the 36 answerable panels, the best any key measured here reaches, and puts a synthetically drawn grid's azimuth at **79.04 degrees where the test drew it at 34**. So does `coherence` first, at the same 79.0. Ground truth by construction beats agreement with neighbours, twice.

**What this costs is one panel, and it is named rather than averaged away.** Printed step 32 moves from a refusal to an acceptance at **21.00 points per stud** against the booklet's 16.1, on `a=(38,0) b=(-17,7)` at 1.78% of pitch — a pair the old ordering happened to drop. Its overlay says why and the cause is upstream of the ranking: that panel is drawn nearly edge-on with almost no studs, and its two callout boxes are joined to the assembly by their own pointer lines, so the largest-connected-region isolation keeps them and their art — drawn under a second camera at a second scale — enters the grid measurement. No ordering of these keys refuses it: over **1698 rule and pairing combinations** searched against the forty panels, every one that fits 34 accepts printed 32, and every one that refuses it fits at most 33 and leaves printed step 7 refused with it. The residual gap the gate sits in narrows accordingly — accepted panels now span 0.00005 to 0.01776 and refused ones 0.0403 to 0.3638, where it used to be 0.008 to 0.03 — and every panel reading at the booklet's own scale is still under 0.008.

One earlier correctness fix survives and is unchanged. `countExplained` does not count a peak as explained when the nearest lattice site is the candidate's own origin: every lattice has one there, the peak is at a nonzero offset, and because the tolerance is a fraction of the cell a coarse candidate collected short repeats wholesale — printed step 7 had a 2448px-cell candidate claiming the peaks at (-8,3) and (-13,5) **8.5px and 13.9px away** on a picture whose grid step is 40px.

### What stops printed step 6 now

Its best two of 36 whole-step candidates agree with panel 7's already-built art at **0.8837 and 0.8804**. Both clear the 0.85 absolute bar that 0.4482 failed; they are 0.00328 apart against a 0.02 margin that is this registration's own search-noise floor, so the deferral refuses `ambiguous-deferred-placement` rather than picking the survivor of two indistinguishable comparisons. That is the honest verdict and it is a different blocker: panel 7 confirms the prefix and cannot separate the last four pieces' seats. The remedy the booklet supplies for that is the same one it supplies for everything else — the next picture, panel 8 — and `DEFERRED_STEP_MAXIMUM_REACH_STEPS` is 1 because a reach of 2 has never been calibrated. Extending it is the next piece of work, and it is a lookahead question rather than a camera one.

### An open contour, which about half of them are

Printed step 5 prints two contours and neither closes: 1429px of keyed stroke, `closedContourRate` 0, no filled region anywhere. It is not an exception — measured over twelve sampled pages only 19 of 36 contours close — so a loop that can only score an enclosed area cannot finish this booklet.

It refused with `highlight-reuse-unexplained` at union 0px, and the refusal was arithmetically correct and about the wrong thing: the joint gate measured each piece's *area* against `highlight.mask`, which an open contour leaves entirely empty by construction. The 1429px the booklet actually drew were never consulted. Measured against the stroke instead — each piece's **visible boundary**, widened by the run's existing 3px boundary tolerance, against the keyed line — the same question has an answer: the two pieces explain 347px and 907px exclusively, against a calibrated minimum of 8.

**What replaces containment is not a pixel test, and both candidates for one were refuted by measurement.** On a closed contour the exploded road's containment decides, because the yellow is drawn outward of the silhouette so the truth is strictly inside it. Scanning all 374 distinct placements of step 5's Plate 2 x 14 and all 174 of its Plate 2 x 4 against the printed line:

- *"Every printed pixel is explained."* The best placement is the unique maximum at 907 of the 1081px its own contour prints. The 174 it misses are the outer row of a two-pixel stroke drawn outward by more than the tolerance — the artist's offset, not the placement — and no placement of that part does better. Shipped as a gate for one run, refuted, removed.
- *"No printed pixel lies inside the piece."* The same best placement has 433 printed pixels inside its own silhouette, because a two-pixel line straddling a boundary is half inside it. And 166 of the 374 placements cross nothing at all, being nowhere near the drawing.

A stroke straddles the boundary it marks rather than lying on one side of it, so neither one-sided statement is true of the right answer. The hard test therefore lives in the search: `rankStepDelta` ranks an open contour by `strokeRecall` — the printed line the candidate's boundary explains — dropping the precision term, which asks what share of the candidate's boundary the booklet printed and so charges it for the occlusion that opened the contour in the first place. The accepted placement must then beat its runner-up by the run's existing `minimumScoreMargin`, or the step defers to the next panel exactly as step 4 does. That is threshold-free in containment's way — an argmax and an existing separation bar rather than a new cut — falsifiable by a single better candidate, and it keeps containment's essential property: a candidate spilling outside the printed contour cannot win, because spilled boundary explains no printed pixel. Measured on step 5 the margins are 0.6347 against 0.4962 on the 2 x 14 and 0.2428 against 0.1854 on the 2 x 4, against a bar of 0.01.

Colour played no part: nothing on the placement road reads `colorId`, the highlight key is the printed yellow whatever it outlines, and the panel-art key is a background difference. Step 5's Dark Green plate is the booklet's first non-grey piece and it scored like any other.

The shortfall is still reported rather than dropped. `jointVisual` now carries `evidenceKind`, `printedEvidencePixels` and `unexplainedBoundsPx`, so step 5 records 1429px printed, 1254px explained and the 175px residue inside panel box `[36,353,286,445]` — which is what located it as the 2 x 14's own contour in one run rather than a search.

### The camera a panel drawn from underneath needs, which the fit cannot supply

Printed step 4 registered the model built so far against its own panel at `anchorIou` **0.393**, against 0.903 and 0.627 at steps 2 and 3, and every step-4 number was measured through that. The suspected cause was that step 4's closed highlight cuts a band out of the art the registration uses, and it is **refuted**: excluding the highlight from both sides lifts panel 3 from 0.6267 to 0.8898 and panel 4 only to 0.4821, and the overlap does not order the anchors — panel 3 loses 67.3% of its art to its own highlight and registers higher than panel 4, which loses 53.4%.

The cause is the camera, and one look at the page says so: printed step 4 draws hollow underside tubes where step 3 draws raised studs, so `panelFace: "underside"` was right and the camera built from it was not. Three things are unknown about that camera and the panel's stud lattice can supply none of them.

**The half-turn.** Turning a model over is a half-turn about a horizontal axis, and every such half-turn carries the model's up axis to `-Y`. `viewForPanelFace` negated the azimuth and the elevation and left the image upright, which produces the panel's picture rotated half a turn. It is provably outside the family a fitted view sweeps: with up pinned to `+Y` the model's own up axis projects above the view axis at every azimuth and every elevation short of straight down, so no `(A, e)` pair reproduces the rolled image. That is why sweeping all sixteen quarter-turn and sign variants of the fitted view was flat at 0.400-0.444. It is now `upSign` on `OrthographicViewParameters` and on `panelProjectionFromFit`, set by the face and never by a fit — a square lattice is invariant under it, so a fit free to choose it would only gain a way to lie.

**The quarter turn.** `a(A + 90) = b(A)` and `b(A + 90) = -a(A)`: a quarter turn permutes the projected lattice basis and spans the same lattice, so the fit is exactly as good at all four and reports whichever its search reached. The world frame pins which one is true only through the branch printed step 1 settled into, and every rotate-the-model icon reopens it because the axis the booklet turns about is not stated and lands in the same coset. `anchorStepCamera` now resolves it by registration, the same way it already resolved the translation, and publishes every turn's agreement plus the chosen `anchorTurnDegrees`.

**Where the panel stops reporting.** That registration declines to score inside the panel's own highlight, because the panel draws this step's part over what was already built. This is the refuted mechanism in its real role: without the exclusion the plain IoU puts the **wrong** quarter turn first on panel 3, 0.6435 against 0.6267, where dropping it puts the right one first at 0.8898 against 0.7762.

Measured after, per panel, anchor and chosen turn: **2 → 0.903118 turn 0** (was 0.903118), **3 → 0.889836 turn 0** (was 0.626687), **4 → 0.831192 turn 90** (was 0.393292). Step 3's placement is unchanged although its anchor shift moved from `[30,71]` to `[11,72]`. Corroborated against the next panel, which is the only independent witness step 4 has: the six-piece prefix rendered at panel 5's camera outlines exactly the half-disc panel 5 draws as already built, and neither of the two pieces panel 5 places.

Both per-piece render budgets moved from 220 to **1024** with this, and that is a cost ceiling rather than a correctness bar. Step 4's two pieces enumerate 240 and 334 placements — the same product the deferred-narrowing budget was already sized against — so both search strategies refused with `resource-budget-exhausted` before rendering anything and the step was decided by a budget rather than by its panel. Exceeding the new bound still refuses rather than truncating, so no step is ever scored against a set that was silently cut.

One thing this leaves open. The quarter-turn resolution picks the best of four with no bar under it — margins are 0.21, 0.06 and 0.27 on the three panels that have a prefix, and a rotationally symmetric prefix would tie with nothing to refuse it.

Cleared: `real-build-deferred-step.ts` used to register its lookahead prefix without resolving that panel's quarter turn or reading its `upSign`, which is right only while the booklet keeps the model the same way up between the deferring step and the one that settles it. It now carries the `upSign` its `faceCorrectedFit` already computed and resolves the turn the same way `anchorStepCamera` does — by registering the already-settled prefix against the settling panel's built art outside its highlight — with turn zero taken by definition rather than by assumption when nothing is built, because the branch printed step 1 settles into is what fixes the world frame. It publishes `lookaheadUpSign`, `lookaheadTurnDegrees`, `lookaheadTurnAnchorIou` and `lookaheadTurnMargin`, and a `settled` deferral that cannot name its face and turn is now rejected by the browser-output validator. Printed step 1's deferral reports `upSign` 1 at turn 0 with the five-step prefix hashing unchanged.

Cleared: the catalog held none of the set's parts, and now holds 85 definitions at `builtin.basic-parts/8` with the first 77 rows still hashing exactly as they did. Admitting `30357`, `2450` and `79491` — whose female connectors come from the LDCad shadow library, the only source that has them — moved the covered prefix from 15 printed steps to 25, because step 16's only missing design was one of them. The prefix still measures 25, with step 26 the first uncovered and `28802` its only missing design.

Cleared: identification confidence. Blind pair-judged verdicts are a digest-bound closure role, and printed steps 1 to 18 now carry 40 of 42 piece slots trusted, up from 17. Step 1's `80015` callout, which made every prefix impossible, resolves.

Cleared: the last two. Both refused callouts were one cluster, and the reason it could not be answered was two bad crops in the gallery it retrieves from. The inventory grid is ragged and a cell was a rectangle sized from the column pitch, so `383228`'s 2x8 plate — which overflows its column — was published with its end cut off, and that same overflow sat inside the rectangle `302028` was cut from one column over, giving it two plates and a `2x`. Both were published looking exactly like good crops, because an inventory record carried an element id and a quantity and nothing else. Cutting the page into connected components with the text masked, and assigning each element one component, fixed both; against the Builder-export truth, which is independent of the descriptor, shortlist recall went from 0.88 at every k with ranks {1:22, 17:1, 197:2} to 1.000 with all 25 at rank 1. The cluster's shortlist then led with `302028` at a 0.021 distance against 0.067, the vision call answered "plate, 4 long, 2 wide, Green" and picked it, and the two `untrusted-identification` failures are gone.

Cleared: the hand of a chiral part, which is now read from the card rather than from what the answer said about it. Five cards in the sealed run `0cc4c92ccd775dbeee360dc1` put both hands of one wedge plate in a single candidate list — cards 29, 39, 41, 76, 79 — and on four of them the twin sits under the pick. The kind/size/colour check provably cannot separate those: `wedge 6x2 White` agrees exactly as well with `Wedge Plate 6 x 2 Right` as with `Left`, and both mirror pairs in this inventory are quantity 1 and 1, so a swapped pick also conserves the printed piece counts. `handednessFromCard` compares the query silhouette with each hand and with each hand mirrored, area-normalised to a 64-cell-square letterbox, and the wider overlap decides. Measured on those four cards: query-against-pick beats query-against-twin by **0.385 (card-0039), 0.375 (0041), 0.381 (0076), 0.398 (0079)**, the picked hand scores 0.890 to 0.913 and its twin 0.508 to 0.538, and mirroring the query raises its match to the twin on all four (0.677 to 0.742). The decision floor is 0.03, set from both ends of what the run measures rather than from what would make a card pass: across all 269 cards, two candidates the comparison cannot separate come within 0.0001, and the tightest real chiral separation is 0.375.

Where the card cannot say, it says so. `card-0084`'s query is a round dish whose silhouette is 0.036 from its own mirror, below the 0.05 asymmetry floor, so no hand exists to read and the pick stays unpromoted; four panels of the 269-card run have no flat ground to separate a part from and report that instead of guessing. An unpromoted answer is a correct outcome and a promoted wrong one is not, so `handedness-unverified` and `handedness-refuted` are both untrusted labels and only the upheld hand reaches `vision-kept`.

What this replaced claimed more than it checked, and the correction matters more than the feature. The previous rule promoted a mirror-paired pick once the model's note named the twin's candidate number, under a block called `handedness` reporting `mirror named N/M`. It has no discriminating power: run against card-0039 with the pick swapped to 2 and the note `candidate 1 is the mirror`, it returns `vision-kept` carrying `6392747`, the **Left** wedge plate, identical to the correct answer's label and element. The same input now returns `handedness-refuted` with no element. The note count survives under `mirrorPairAwareness`, which is what it always measured.

Stated exactly, because "cleared" is the word this section abuses most easily: the numbers above are measured directly on the sealed run's card PNGs and on `visionPick` fed those cards, not on a re-scored book. `output/part-identification/answers-claude-opus-5.json` is a `lego.part-identification-answers/3` bundle carrying the previous prompt digest, so it no longer binds the current schema and `score --source adjudicated` refuses it before reaching any of this. What is proved is that the grader decides the hand from pixels and rejects the swap; the end-to-end tally moves when the vision pass is re-run.

Cleared: set accounting, which was rejecting the run at any length because it carried no step number. The booklet distinguishes a parts-bin quantity from a repeat multiplier by type size alone — 8pt against 16, 24 and 40pt — and the 196 step pages carry 881 Nx labels: 859 at 8pt totalling **1464**, and 22 at the larger faces totalling 48. 1464 is the assembled model, corroborated twice over: the back-matter inventory on pages 221-222 totals 1465, one more because the loose `31510` separator is never placed, and the official Builder XML yields 1395 direct + 69 MultiBuild = 1464 instruction identities from 1465 Bricks with that same separator unmatched.
Both halves of the contract were wrong. `OFFICIAL_REAL_BUILD_ACCOUNTING` still declared the superseded 870-identity generation (raw 1486, physical 1446) and closed the gap with an `omittedPhysicalPieces: 18` class no artifact ever enumerated; the publication had moved to 881/1512 but classified four multiplier labels as part art — `p59|q2|x124.683|y55.056`, `p85|q2|x662.244|y445.465`, `p96|q2|x125.941|y478.298`, `p109|q2|x723.002|y319.540` — putting its physical total 8 pieces above the set. The 26-piece discrepancy was 8 of real over-read plus an 18-piece class that never existed. The constant now reads 1512 = 1464 + 48 with omitted 0, and the four labels are preregistered semantic.

That miss was only findable by hand because the type size was extracted and then dropped: the manifest published a class and no measurement that could contradict it, and any identity absent from the curated recovery fixture defaulted to physical. The manifest is `lego.callout-thumbnails/5` and every callout record now carries its `heightPt`, so the classification has two independent sources — the preregistered fixture and the booklet's own type size — and publication refuses when they disagree. The bounds are measured, not assumed: the parts-bin band is 7-9pt and the multiplier bound is 16pt, with the 8-to-16 gap empty in this booklet. A face in that gap, or the 6pt back-matter inventory row, fails as unclassifiable rather than landing in either class, because another set's booklet will set its own sizes.

**At a three-step prefix the booklet-side failures are gone entirely, and the frontier has moved off the booklet.** Regenerating the inputs at `LEGO_REAL_BUILD_LAST_STEP=3` reports "0 remaining evidence failures through printed step 3", and the run now reaches `incomplete` rather than `input-rejected` — the inputs are accepted and the browser driver loads and runs.

Getting there meant closing three holes in the sealed replay's module graph, all of them vite resolving imports against its own graph rather than the mirror. Bare imports inside mirrored files become `/node_modules/.vite/deps/*`, relative to the dev server's root — and that cache lives under vite's root rather than beside the lockfile, so declaring the repository-root `node_modules/.vite` mirrors an empty directory and changes nothing. Vite injects an import of `/@vite/client` into every module it transforms, independently of HMR: switching HMR off removes the socket, not the import. And a served module can be handed a sibling's absolute path in the ordinary checkout, which is how that client reaches `env.mjs`. Chasing routes one at a time was the wrong cut; what the mirror vouches for is a file at a repository-relative path, and the drift check proves the checkout still holds the captured bytes, so a checkout path resolves exactly when the mirror declares the same relative path. Recorder and replay verifier apply the same rule.
Measured: served requests move from 22 fulfilled and 4 blocked to **110 fulfilled and 1**.

Two defects surfaced only because the run got that far, and both are the same shape — a check that no failing run could reach. `MAXIMUM_REPLAY_ROLE_COUNT` was 20 while the closure emits 21, and the twenty-first is `browser-output`, which exists only once the browser completes; the bound was one too small from the day it was written. And the drift check reported "digest map changed" and named nothing whenever its snapshot half fired alone, because that half was a bare `JSON.stringify` inequality; it now names the path and both digests.

The chain of harness refusals is exhausted, the arrows convert, and **the search runs on a printed panel**. The driver returns a valid document and the exact prepared PDF. What follows is the position as it stood before the deferral below: step 1 enumerated and rendered four candidates under both the pruned and exhaustive strategies, every one scored zero, and steps 2 and 3 were blocked behind it. That was the last position with zero pieces placed.

`real-build-run.ts` used to read the displacement arrows and then pass `usableArrowPlacementCount: 0` as a literal, so every exploded step refused with `no-placement-signal`. The conversion existed and nothing called it: `measureArrowClearances` reads the gaps the artist left at each end, `correctArrowForClearance` adds them back — 0.00 to 0.47 of a stud on this booklet, always the same direction — and `arrowDisplacementFamily` returns every whole-grid displacement whose projection matches what is left. Wiring those moved step 1's refusal from `no-placement-signal` to `benchmark-disagreement`.

**Why the score is zero is a fact about the booklet, not a defect, and it is what the deferral is for.** Step 1 reports `highlight 0 region(s), 0px stroke, closed 0; arrows 2 kept 0 rejected`. It is the first step, so nothing is already built and there is nothing to outline; the panel prints no highlight at all. `scoreStepDelta` therefore has `regionIou` null and an empty stroke mask, and every candidate scores zero by construction. The search is not failing — it is being asked a question this panel does not answer.

The panel's own drawn art cannot stand in for the missing highlight either, because step 1 is drawn exploded: the wedge floats above the curved plate with two arrows into it, so an assembled candidate does not match the drawing however it is placed.

The arrows do constrain it, and by a measured amount: step 1 now reports `family 2`. Its two red arrows narrow the placement from every legal enumeration to **two** whole-grid displacements and no further, so step 1 is neither underdetermined nor solved — it has two candidates and no local evidence to choose among them. That is a beam width rather than a search.

The family is published per step, closest projection first. Step 1's are `[0,56,0] [60,8,40]` in LDU, at 0.0396 and 0.0636 of a stud. It read `[-60,80,-40] [140,-88,100] [60,-24,40] [0,32,0]` until the raster defect below was fixed, and those four are what the two dated run records further down quote.

### Step 1 is settled by panel 2, and the two pieces are placed

Measured 2026-08-07 at `LEGO_REAL_BUILD_LAST_STEP=3`, one run, commit 7c6a430. The three step lines it produced, verbatim:

```
step 1 complete/deferred-lookahead 2/2 placed [highlight 0 region(s), 0px stroke, closed 0; arrows 2 kept 0 rejected, family 4 [[-60,80,-40],[140,-88,100],[60,-24,40],[0,32,0]]]
step 2 failed/deferred 0/1 placed [highlight 1 region(s), 527px stroke, closed 1; arrows 2 kept 0 rejected, family 2 [[0,-24,0],[60,-72,40]]] — benchmark-disagreement: Pruned: incomplete-placement-scoring score none from 0 renders in 0ms; exhaustive: 20,-8,20|upright-yaw-180 score 0.10007585289159732 from 88 renders in 1435ms
step 3 failed/blocked 0/1 placed [highlight 1 region(s), 766px stroke, closed 1; arrows 0 kept 0 rejected, family 0 []] — blocked-by-prior-step
```

**`piecesPlaced` is 2, `deferredSteps` 1, `settledByLookahead` 1, deepest settlement reach 1 printed step.** Zero of those numbers had ever been non-zero.

What the run does now: a step whose panel prints no highlight is not refused, it is deferred. The whole printed step is proposed as one object — the product over its pieces, **400** at step 1, four yaws of the quarter ring on the empty plate times a hundred distinct seats for the round plate on each — every candidate prefix is rendered at panel N+1's own camera and face, and scored by agreement with the art panel N+1 draws of what step N built. The panel's own highlight region is excluded from both sides of that comparison, because N+1 draws the new part *over* what N built and neither side is defined there; including it inverted the panel-3 ranking in the probe that motivated this.

The document never branches. One settled prefix exists at all times, so the eager step report, the canonical step id, the identity registrations and the per-step validation the Node finalizer recomputes from the final document's prefix are all untouched. The deferral changes *when* step N settles, not how many documents the run carries.

**It picked the branch an independent probe had already named, and the agreement is exact rather than approximate.** Step 2's exhaustive search on the settled prefix reports winner `20,-8,20|upright-yaw-180` at score `0.10007585289159732`. `output/build-search/step1-deferral.json` (probe 7762ebe, which never ran the loop) records exactly that transform and exactly that score for its branch 3 — the quarter ring at `[0,8,0] upright-yaw-90`, the branch it identified as the one the panel draws, at highlight-excluded prefix agreement 0.9031 on panel 2 and 0.8898 on panel 3. Seventeen significant figures is not a coincidence: the run settled step 1 into branch 3. That identity was measured before the enumerator was fixed and will not reproduce now — step 2 searches a different, larger set — but it is what established which branch the run settles into, and step 1 still completes both pieces under the stricter gates that replaced the margin below.

That is worth stating precisely, because branch 3 is *not* the official model's frame. The four step-1 candidates are one model at four quarter turns — the probe checked it, rendering each at azimuth A + k·90 reproduces branch 0's silhouette at IoU 1.000000 on all four — so choosing among them chooses the camera's quarter-turn branch, which the lattice fit pins no further than its coset, and not a different build. The completion audit compares placed transforms against the official ledger's, so it will report step 1's as mismatched; that mismatch is a global rotation of the world frame, not a wrong placement, and reconciling the two frames is its own piece of work.

**The refusal was calibrated on the wrong quantity, and has been re-derived in the one it gates.** `DEFERRED_STEP_MINIMUM_MARGIN` was **0.0878**, the largest margin any *wrong* pick achieved in that probe. Three of the four numbers it maximised over are margins of `bestScore` — the next step's single-piece placement score against the next panel's printed highlight, spanning 0.076 to 0.514 — and the fourth is `anchorIou`. The gate they set bounds `registerPrefixAgreement`, a prefix-silhouette IoU against already-built art, which the same probe records as `definedIou` spanning 0.507 to 0.903. Spearman rho between the borrowed metrics and the gated one flips sign panel to panel — `bestScore` **-0.600** at panel 2 and **+0.800** at panel 3, `anchorIou` **+1.000** and **-0.400** — so they do not order the candidates the same way, and a bar measured in one does not bound the other. In the metric it actually gates that bar had a false-refusal rate of one in two: it refuses the measured right pick at panel 3, whose margin is 0.0622.

**In the gated metric no wrong-pick margin was ever recorded, so no value of it is calibrated.** Two observations exist and both are right picks: 0.2085 at panel 2 and 0.0622 at panel 3, a false-accept denominator of zero. Ablating the right branch — the enumerator failing to offer the drawn placement was not hypothetical, `truthEnumerated` is false for all four branches at panel 2 — gives wrong-pick margins of 0.1776 and 0.0238. The minimum right margin, 0.0622, sits **below** the maximum wrong one, 0.1776, and the ratios interleave the same way (1.0752 against 1.3435). No threshold on margin separates right from wrong on this data, so choosing a better number is not available.

**What does separate them is the winner's own absolute agreement, and that is now the gate.** Right picks scored 0.9031 and 0.8898; the best candidate of the same field with the right branch deleted scored 0.6946 and 0.8276. The separating window is (0.827593, 0.889836], and `DEFERRED_STEP_MINIMUM_AGREEMENT` is **0.85** — false-refusal 0 of 3, false-accept 0 of 2 over every observation that exists, and it does not degrade as candidates are added, which matters because production runs a 400-candidate product whose runner-up is a near-duplicate by construction. `DEFERRED_STEP_MINIMUM_MARGIN` is demoted to **0.02** and renamed in its own docstring as a noise floor: replaying the registration at stride 1 instead of stride 4 moves the reported agreement by up to 0.009916, and a margin differences two independently registered agreements, so 0.02 says only "these two are not distinguishable by this registration". Its false-accept rate is two in two by construction; that is what a noise floor is.

The headroom is thin and is stated rather than hidden. 0.85 is **6.3 px** of registration error from the panel-2 observation and **4.5 px** from the panel-3 one, and one pixel of registration error moves this agreement by up to **0.0328**. Absolute level is also not obviously portable across panels: the excluded region is 106% of the built art at panel 2 and 236% at panel 3. The stride docstring was wrong in the same direction — it claimed subsampling moves the chosen shift "by at most a pixel", and replaying the search at stride 1 lands the optimum up to **7.3 px** away — and it now carries the measurement instead of the claim.

And the deferral now refuses a reach it has never measured. `DEFERRED_STEP_MAXIMUM_REACH_STEPS` is **1**. The panel-3 observation that would license 2 is oracle-conditioned: `step1-deferral.spec.ts:884-888` builds its panel-3 prefixes from step 2's *official* transform rotated into each branch, which no run can reproduce. Reachability comes before ranking, so two-panel agreement waits for a second reach measured without an oracle.

The five refusals it can produce are named separately: `deferred-panel-unscored` when the panel deferred to has no already-built art or no face-corrected camera — the honest limit of a one-step lookahead, written before it could bite; `deferred-reach-unmeasured` when the panel it would defer to is more than one printed step away, refused before anything is rendered so no uncalibrated margin is published; `weak-deferred-agreement` when the winner does not resemble the panel outright; `ambiguous-deferred-placement` when the winner and runner-up are inside the registration's own noise; and `resource-budget-exhausted` when the whole-step product exceeds `deferredCandidateBudget` (512), refused rather than truncated.

`real-build-deferral.test.ts` drives the same decision procedure over real enumerated geometry, through the repository's own camera and point projection rather than a hand-drawn mask: 400 candidates, the drawn assembly first at agreement **1.000** against a runner-up of **0.781**, margin **0.219** — the same order as the 0.2085 the printed panel gave. It asserts each refusal from geometry rather than from a number the test supplied. A panel whose highlight covers exactly where the candidates differ leaves them tied at margin 0 and refuses. A panel with nothing built drawn on it refuses. A panel drawing more than the step built — the assembly plus one piece this step does not place — produces a winner that beats its runner-up by more than the superseded 0.0878 bar would have required, and is refused anyway, because it explains only part of the picture. And a panel that draws exactly what the step built, with most of where the candidates disagree excluded, settles on a margin **below** 0.0878, which is the false refusal the old bar produced. The two thresholds are also checked against the probe's observations *in the gated metric only*; the superseded test compared the bar against four numbers from three foreign metrics and locked the error in.

### What step 2 fails on now, and it is the panel rather than the search

Measured 2026-08-07 at `LEGO_REAL_BUILD_LAST_STEP=3`, one run, after the changes this section describes. The three step lines it produced, verbatim:

```
step 1 complete/deferred-lookahead 2/2 placed [highlight 0 region(s), 0px stroke, closed 0; arrows 2 kept 0 rejected, family 4 [[-60,80,-40],[140,-88,100],[60,-24,40],[0,32,0]]]
step 2 failed/deferred 0/1 placed [highlight 1 region(s), 527px stroke, closed 1; arrows 2 kept 0 rejected, family 2 [[0,-24,0],[60,-72,40]]] — whole-step-score-too-low: Step 2 joint visual score is 0.200918543009241, below the measured threshold 0.45. The whole printed step is refused; a positive score alone is not evidence.
step 3 failed/blocked 0/1 placed [highlight 1 region(s), 766px stroke, closed 1; arrows 0 kept 0 rejected, family 0 []] — blocked-by-prior-step: Step 3 was not attempted because step 2 failed. The canonical document still represents the base of step 2; attempting a later printed step against it would measure a different build history. Its own callout and catalog prerequisites are locally satisfied.
```

Step 1 still completes, under gates that now require the winner to resemble the panel outright rather than merely to beat its runner-up. Step 2's refusal has moved twice — off `benchmark-disagreement`, then off the search entirely — and what is left is a disagreement between the model and the picture, which is the honest place for it to be.

**The drawn placement is in the candidate set now.** `enumeratePlacements` solved `origin = stud - rotate(clutch)` over free studs only, so it proposed a part going *on top* of the assembly and never one sliding in *underneath* — and printed step 2's arrows point up. Both sides of a stud-tube joint now seed origins. On the settled branch-3 prefix, `builtin:wedge-plate-4x4-cut-corner` goes from **88** accepted candidates to **105**, and the placement the booklet draws is one of them: `[-20,8,-20] upright-yaw-90` in that frame, which the editor accepts with three connections, every one of them the wedge's own studs entering the round plate's underside clutches. The prefix has 13 free studs and 15 free clutches, and the 15 were computed and thrown away. The two seed sets are disjoint: 226 stud-seeded origins and 260 clutch-seeded, 486 together. The ordering matters more than the count: a deferral, or any ranking, that runs on a set which does not contain the answer cannot be right for any reason, and its margin is noise. Reachability is the check that belongs before ranking.

Seeding from clutches also reaches under the *build plate*, and that is refused by name rather than scored. 147 of those 486 origins put the wedge inside the plate — its studs entering the underside of something already resting there — which the document validator permits because the kernel has no collider for the plate, and which no build sequence can pass through. `rejectedBelowBuildPlate` counts them, for the same reason `rejectedDetached` counts a part the plate holds up alone. What survives is exactly the 105 an unbounded `includeBuildPlate: true` sweep produces, with the same y-levels — 68 at `y=-8`, 20 at `y=0`, 17 at plate level — which is the check that clutch seeding recovers the plate band and invents nothing.

**Both search strategies look now, because the budget that stopped one of them could not bind.** `maxRendersPerPiece` was 24 against a `blindRenderBudget` of 220, and the pruned candidate set is a *subset* of the exhaustive one by construction — the proximity filter only removes. So a smaller pruned budget refused only renders the exhaustive strategy performed a few lines later: on step 2, 47 eligible refused against 88 rendered in the same step. It cannot save the work it exists to save, and all it bought was a benchmark disagreement between a strategy that scored and one that declined to. `preflightRealBuildOptions` now refuses that configuration as input, and both budgets are 220. The pruned strategy's own refusal also names what it refused over — the eligible count and the budget — instead of reporting `incomplete-placement-scoring`, which described the symptom of handing an empty score list to the shared selector; and `adjudicateSearchBenchmark` now quotes each strategy's message rather than only its failure code.

**And the step still failed, on a number that was measuring the wrong thing.** The joint visual score was **0.200918543009241** against the measured threshold **0.45**. Printed step 2 is drawn *exploded* — the highlighted wedge floats below the assembly with two arrows pointing up into it — so the 527px closed contour outlines a ghost rather than a seat. The seated placement it draws is almost entirely occluded by what step 1 built: scored where it seats, it reaches regionIou 0.000155. No seated candidate can match that contour, so 0.2009 was whichever wrong seat overlapped the ghost region most. The claim that "an exploded step need not be read at all, because the following step is not exploded" is false on this booklet for step 1 *and* step 2; step 3 is the first seated panel. The signal separating them is already computed and free: arrows kept is 2, 2, 0 on steps 1, 2, 3. Both of those are fixed in the section below.

One more thing the lock-free probe says, and it is larger than step 2. (Its numbers are in `output/build-search/zz-step2-winner.json`, which is run evidence and not tracked; the conclusions are here and the probe reproduces the run's own step-2 winner and score to seventeen digits.) The arrow-to-world conversion was off by exactly the run's `workFactor`: `real-build-panel-raster.ts` read the arrows off the downsampled 500px raster and then converted them through a projection built from the fit over the full-resolution 1000px image, while the renderer divides the same `pixelsPerUnit` by that factor. Measured ratios between the two bases are 1.9975 vertical, 1.9869 on x and 2.0201 on z against a `workFactor` of 2. Every arrow-derived displacement in this repository was therefore half the travel it should be, step 1's published family included. Sweeping the drawn wedge down the world lattice against panel 2's highlight peaks at 7 plates (regionIou 0.5888) where the family answered 3 (0.1012); doubling the family's answer gives 6 plates and 0.5127. That sweep also fixes the scale a score on this panel can reach at all: the highlight region is 4749 px and the rendered wedge silhouette 2796, so a fully contained placement caps at 2796/4749 = **0.58876**, and the sweep maximum equals it exactly. Step 2's 0.2009 was against that ceiling, not against the 0.87 to 0.97 the synthetic panels reach.

### Step 2 is read against the ghost now, and what is left is the arrow — SUPERSEDED

**Superseded by the section below, and by the position recorded at the top of this part.** Step 2 does not refuse: it completes by exploded ghost with one contained candidate and zero pixels outside the printed contour, and the run reaches `stepsComplete` 3 with `piecesPlaced` 4. The refusal quoted here, its `0.57288842544317` against a `0.5881238155401137` ceiling and its four-member arrow family, is the state before `arrowTravelFamily` read the arrow as a line and a floor rather than as a corrected vector. Everything from here to the next heading is history; a reader who needs the current numbers wants the section below it. This section is kept because the refusal it records is what named the blocker correctly — "the shortfall is in the travel, not in the bar" — and because a brief was later written from these numbers as if they were live.

Measured 2026-08-07 at `LEGO_REAL_BUILD_LAST_STEP=3`, one run, after both fixes below. The three step lines it produced, verbatim:

```
step 1 complete/deferred-lookahead 2/2 placed [highlight 0 region(s), 0px stroke, closed 0; arrows 2 kept 0 rejected, family 2 [[0,56,0],[60,8,40]]]
step 2 failed/deferred 0/1 placed [highlight 1 region(s), 527px stroke, closed 1; arrows 2 kept 0 rejected, family 4 [[0,-48,0],[60,-96,40],[-140,80,-100],[-60,8,-40]]] — whole-step-score-too-low: Step 2 is drawn exploded: its 4749px highlight region outlines the ghost, and each of 105 whole-step candidates was redrawn back along 4 arrow displacement(s) and compared against it. No candidate's ghost lies wholly inside that contour. The best reaches region agreement 0.57288842544317 with 46px of 2793 outside it, against the 0.5881238155401137 a contained ghost of that size would score on this panel. The booklet draws the yellow clear of the part, so a correct ghost has no pixel outside it; the shortfall is in the travel, not in the bar.
step 3 failed/blocked 0/1 placed [highlight 1 region(s), 766px stroke, closed 1; arrows 0 kept 0 rejected, family 0 []] — blocked-by-prior-step: Step 3 was not attempted because step 2 failed. The canonical document still represents the base of step 2; attempting a later printed step against it would measure a different build history. Its own callout and catalog prerequisites are locally satisfied.
```

**Step 1 still completes both pieces, which was the thing at risk.** Correcting the arrow scale changes step 1's published family from four members to two — `[0,56,0]` at 0.039558 of a stud and `[60,8,40]` at 0.063618, against a previous best of 0.123820 — and `usableArrowPlacementCount` is the only thing anything reads off it, compared against zero in `placementSignalFailure`. Two is still not zero, so nothing downstream moved: the deferral's candidate set is `enumeratePlacements`' own product and never sees the family. The correction is also visibly better rather than merely different. Step 1's arrows point straight down, and the purely vertical reading `[0,56,0]` — seven plates — is now the closest projection rather than, as the superseded paragraph below records, sorted **last** of four.

**The conversion now happens at the raster the arrow was measured on.** `panelProjectionForWorkRaster(fit, workFactor)` states the two rasters as an argument instead of leaving the division at a call site: the lattice fit is measured on the full-resolution crop and `readDisplacementArrows` reads the downsampled one, so inverting a work-pixel displacement through the full-resolution projection reported exactly `workFactor` times too little travel. Step 2's family moves from `[[0,-24,0],[60,-72,40]]` to `[[0,-48,0],[60,-96,40],[-140,80,-100],[-60,8,-40]]` — the leading member from 3 plates to 6, which is the doubling exactly. `apps/web/src/assembly/arrow-placement.test.ts` pins it as a round trip: a travel projected at full resolution, halved to what the work raster shows, must invert to the same triple, and inverting it through the full-resolution projection recovers `plates / workFactor`.

**An exploded step is now scored against the ghost the booklet draws.** Selection is the free signal — arrows kept greater than zero, which separates this booklet's first three printed steps 2, 2, 0 — and the whole step is proposed as one object, like a deferral, rather than piece by piece. Each candidate is redrawn at `seat − displacement` for every member of the arrow family, rendered *alone* because the booklet floats the ghost clear of the assembly, and compared against the printed region. `apps/web/src/assembly/ghost-placement.ts` holds the comparison and `apps/web/e2e/real-build-exploded-step.ts` the decision; `scoreExplodedStep` is not that function and its docstring now says so — it deliberately zeroes every pixel either panel's highlight claims, which on an exploded step is the ghost contour itself, because it answers the different question of where the part came to rest per the *next* panel.

**The bar is the panel's own geometry and has no constant in it.** Dilating the drawn ghost's silhouette and comparing it to panel 2's printed region gives IoU 0.5817 undilated, 0.7597 at radius 3, a peak of 0.8153 at radius 5 and 0.8052 at radius 6 — the yellow is drawn about five work pixels clear of the part all the way round, so a correctly placed ghost has no pixel outside it. A wholly contained ghost scores exactly its own area over the region's, which is this panel's containment ceiling; anything spilling outside scores strictly less. That is why a global bar cannot serve here: 0.45 is 76% of everything reachable on this panel and under half of a synthetic panel's ceiling of about 1.0, so the same number asks two different questions. The ranking key is the region term alone, not `scoreStepDelta`'s blend, because the printed yellow sits five work pixels outside the part and a three-pixel tolerance measures the artist's offset rather than the placement.

**Step 2 still refused at this point, and the refusal named the real blocker.** The best ghost on the panel reached region agreement **0.57288842544317** with **46 px of 2793 outside** the printed contour, against the **0.5881238155401137** full containment would score — 1.65% of the silhouette, which is a disagreement rather than rasterisation. The arrow is what falls short: `measureArrowClearances` returns `headToBuiltPx` 0 for both of panel 2's arrows because the heads touch the built art, so only 4.33 px of tail clearance is recovered, and the corrected vector is 37.836 px against the 46.17 px seven plates would need. That is about 1.26 plates, and no widening of a threshold can supply it. **Nothing is placed on a picture that fits several seats either:** the decision refuses `ambiguous-exploded-ghost` when more than one candidate's ghost is contained, because every candidate seat sits on the same 20/8 lattice and a ghost free to sit anywhere on it is blind to the seat.

**What that refusal replaced is worth stating, because a half-fix here is worse than no fix.** Under the corrected family and the superseded 0.45 blend, the winner would have been `[0,8,-20] upright-yaw-90` at 0.5968 — comfortably over the bar, and wrong, since the drawn placement is `[-20,8,-20] upright-yaw-90`. Scoring the ghost without also dropping the stroke term and the global bar would have turned a truthful refusal into a silent wrong placement.

`piecesPlaced` as the run's own summary reports it is still **0**, and that is the retention defect below rather than a placement result: `finalizeExecutedRealBuildResult` returns no step rows for a partially-complete prefix, so `summariseDeferrals` and the placed-piece total sum an empty list. The step lines above are the measurement, and they say step 1 placed 2 of 2.

### The arrow states a direction, not a length — and the prefix completes

Measured 2026-08-07 at `LEGO_REAL_BUILD_LAST_STEP=3`, one run, after the change this section describes. The run prints two lines and then fails on the completion audit; both lines verbatim:

```
deferral: 1 printed step(s) had no scoring signal of their own, 1 settled by a later panel, deepest settlement reach 1 printed step(s); 4 piece(s) placed.
local-diagnostic/incomplete: 3/3 steps complete; 4 piece(s) placed; retained unauthenticated evidence output\real-build\runs\2026-08-07T16-52-16-157Z-efaa42b433e7-d8171390-4584-47f1-99d0-1fd6ff9c9126
```

The per-step rows the run retained, from that bundle's `score.json` — the spec composes no step line of its own, so these are the fields the superseded quotations above were built from:

| step | outcome | placed | highlight | arrows | family | evidence |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | complete/deferred-lookahead | 2/2 | 0 regions, 0px stroke | 2 kept, 0 rejected | 21 | deferral: 400 candidates, agreement 0.9031176006314128 against runner-up 0.7784953180497255, margin 0.12462228258168728, reach 1 |
| 2 | complete/exploded-ghost | 1/1 | 1 region, 527px stroke, closed 1 | 2 kept, 0 rejected | 22 | ghost: 105 candidates x 22 displacements = 2310 renders, region 4749px, ghost 2796px, best 0.5887555274794694 = ceiling, 0px outside, contained 1, runner-up 0.5746502401336396 |
| 3 | complete/highlight | 1/1 | 1 region, 766px stroke, closed 1 | 0 kept, 0 rejected | 0 | 191 renders, best 0.4926108075463847 |

**Step 1 places the same two transforms it did before, which was the thing at risk.** `builtin:corner-plate-5x5-quarter-ring` at `[0,8,0] upright-yaw-90` and `builtin:corner-plate-4x4-round` at `[-40,0,-40] upright-yaw-90` — the branch-3 prefix, at the same 0.9031176006314128 agreement recorded above. Step 2 places `builtin:wedge-plate-4x4-cut-corner` at `[-20,8,-20] upright-yaw-90`, which is the placement the booklet draws. Step 3 places `builtin:wedge-plate-6x6-cut-corner` at `[-20,0,60] upright-yaw-180`.

**The clearance correction was built on a model of the drawing that the drawing contradicts.** `correctArrowForClearance` assumed an arrow is inked from clear of the ghost to clear of the landing surface and added both gaps back. Measured on panel 2 by a lock-free probe that drove `derivePanelRasterEvidence`'s own preparation and then read the arrows against the masks it produces; its numbers are in `output/build-search/zz-arrow-clearance.json`, which is run evidence and not tracked, and the probe itself is gone with the rest of that session's diagnostics. Both arrow tails lie **inside** the printed highlight region — distance to it 0, `tailInsideRegion` true — and both heads lie **inside** the already-built art, distance 0. The 4.333px each tail reported was its distance to the highlight *stroke*, measured from a point already inside the yellow band; adding it lengthened a vector that was 38% too short to begin with. The two arrows also disagree about length by 3.00px on a 33.50px consensus (32.005 and 35.000) while agreeing in direction to 0.14 degrees — 1.50px of scatter along the axis against 0.033px across it. The arrow measures its direction about fifty times more precisely than its length.

**The reason is that an exploded step's seat is occluded, so the arrow cannot draw the whole travel.** The head stops at the model's visible surface while the part comes to rest behind it. So the ink is a floor: `arrowTravelFamily` now returns whole-grid displacements along the arrow's *line* — perpendicular offset within the same 0.15 of a stud the old point tolerance used — with travel between the drawn length and a ceiling `measureArrowTravelCeiling` reads off the panel: the material point that starts at the tail cannot end further along than the far side of the art it is joining. On panel 2 the mean tail sits at -239.836 along the axis and the built art reaches -159.341, so the ceiling is **80.495px** against a 33.502px arrow — 46.993px of reach.

**Exactly one candidate survives, and it is the drawn one, across every setting either bound could plausibly take.** Scoring all 105 candidates through the ladder gives one contained seat — `[-20,8,-20] upright-yaw-90` — at every perpendicular tolerance from 0.05 to 0.15 of a stud and every reach from 10px to 60px. It becomes two only at reach 72px, where a sixteen-plate travel enters and a second seat fits. The panel's own ceiling lands at 46.993px: 13px below the widest reach checked that still answers uniquely, and 25px below the one that stops. The answer is not sitting on the edge of the bound that produced it.

**The hypothesis this replaced was that the residual was lattice quantisation, and it is refuted.** Two independent probes measured it. Scoring the ghost through the raw measured arrow instead of a lattice member moves the previous best from 46px outside the contour to 102px, and through the clearance-corrected arrow to 60px; on the drawn placement the same three readings are 238, 912 and 431. Rounding to the grid was saving 14 to 56px, not costing 46. The 46px belonged to a different seat entirely — `[0,8,-20] upright-yaw-90`, reached through the oblique family member `[60,-96,40]` — whose residual is a 5px horizontal strip on a panel whose arrows are vertical, and which never reaches zero at any travel along the arrow's line.

**The settlement is correct today for a reason no test could see, and that hole is now closed.** Nothing on this road may rank by `scoreStepDelta`'s blend, because the printed yellow is drawn outward of the part: measured on panel 2, the placement the booklet draws scores regionIou 0.5887555 with 0px outside and strokeF1 0.5684824, blending to 0.5806463, while a ghost spilling 46px outside scores regionIou 0.5728884 and strokeF1 0.6326254, blending to **0.5967832**. The blend ranks the spilling ghost first. `decideExplodedGhostPlacement` sorts on `regionIou` and `real-build-exploded-step.ts` picks a seat's family member by containment, so neither reads it — but `real-build-exploded-step.test.ts` built its synthetic panel with an **empty** stroke mask, which makes `strokeF1` identically 0 and the blend a monotone transform of `regionIou`, so swapping either key left every ordering in that suite identical and the whole suite green. The suite's panel now carries the stroke the booklet actually draws — the boundary of the printed *region*, four pixels out from the silhouette — and on it the contained ghost's `strokeF1` is 0 against 0.3777 for one read a plate short, whose blend is 0.3173 against 0.1833. Both swaps were performed against the repaired suite and both fail; against the empty-stroke panel both passed. The same file's per-seat selection is containment-first rather than highest-`regionIou`, so a seat holding a contained member can no longer be recorded as uncontained because a larger spilling member outscored it; measured on panel 2 that changes nothing — 0 of 105 seats diverge, and the histogram of contained members per seat is 104 seats with none and the drawn seat with 5 of its 22.

**Two bounds had to move, and neither is a correctness bar.** An exploded step renders its whole-step candidate set once per family member, so its render count is a product: 105 by 22 is 2310 where four members made it 420. That was being counted against `deferredCandidateBudget`, which bounds a *candidate set*; `explodedGhostRenderBudget` is now its own explicit option at **4096**, sized from the run's own measured cost of about 16ms a render, and preflight refuses a render budget below the candidate budget because an exploded step renders every candidate at least once. The retained-report shape check bounded `rendered` by `maxParts`, a part ceiling standing in for a render ceiling; it is now bounded by the run's render budgets. Both held only while every search rendered fewer candidates than the model has pieces.

**What stops the run now is the frame, and it is the first time the audit could reach it.** With three steps complete the finalizer retains step rows, so the completion audit runs for the first time — and reports every printed step mismatched against the official ledger's expected transforms:

| step | part | placed | ledger expects |
| --- | --- | --- | --- |
| 1 | corner-plate-5x5-quarter-ring | `[0,8,0] upright-yaw-90` | `[0,8,0] upright-yaw-0` |
| 1 | corner-plate-4x4-round | `[-40,0,-40] upright-yaw-90` | `[40,0,-40] upright-yaw-0` |
| 2 | wedge-plate-4x4-cut-corner | `[-20,8,-20] upright-yaw-90` | `[20,8,-20] upright-yaw-0` |
| 3 | wedge-plate-6x6-cut-corner | `[-20,0,60] upright-yaw-180` | `[20,0,60] upright-yaw-270` |

Every position differs by x to -x with y and z preserved, and every orientation by a quarter turn — one global frame relation across four pieces from three independently decided steps, not four independent errors. That is the mismatch the section above predicted; reconciling the two frames is still its own piece of work, and it is now the thing that blocks the prefix rather than a footnote. `visual-evidence-unverified` also fires for all three steps, as it must until a Node-side visual audit exists, so `completed` is unavailable on this run for two separate reasons regardless.

The retention defect below is *not* fixed — a partially-complete prefix still returns no step rows. It stopped biting because the prefix now completes.

### One defect the run surfaced by getting this far

A partially-complete prefix throws away every step report it produced. `assertRealBuildBrowserOutput` rejects an executed output whose `identityBindings.length` does not equal the bindings the requested panels declare — 2 against 4 here — and that throw happens inside `writeRealBuildReplayClosure`, before the browser output is written anywhere. So the step-1 deferral evidence the run computed (candidate count, best and runner-up agreement, margin, the placed transforms, both PNGs) exists only in the six-step summary the refusal message prints as a consolation. That is why this section can quote outcomes but not step 1's own margin.
The measure itself is now printed as soon as a result exists rather than with the end-of-run summary, because everything after that point can refuse and throw, and a number that only prints on runs that did not need it is not a measurement. **That relocation has now been exercised, and it did not help: the line reads `deferral: 0 printed step(s) had no scoring signal of their own, 0 settled by a later panel, deepest settlement reach 0 printed step(s); 0 piece(s) placed` on the same run whose step lines report step 1 complete with 2 of 2 placed.** The loss is upstream of the print — `finalizeExecutedRealBuildResult` returns no step rows at all for a partially-complete prefix, so `summariseDeferrals` and the placed-piece total are summing an empty list. Printing earlier moved the statement, not the data; retention has to be fixed in the finalizer.

An authored answer for step 1 exists and is worth recording, but it does **not** check the family, and saying why matters more than the numbers. The official Builder model places the two pieces at `30565;E` translation `22.8, 0.32, 8.16` unrotated and `80015;E` translation `22, 0, 10.56` under a quarter turn about Y; Builder's units are 0.8 per stud pitch and 0.32 per plate, so the offset between them is `(-0.8, -0.32, +2.4)` in Builder units. Carried into LDU through the pack's declared `diag(25, -25, -25)` frame that is `(-20, +8, -60)` — minus one stud, plus one plate, minus three studs. (This line previously recorded it as `(-20, -8, +60)`, converted with no axis flips at all; both the y and the z sign were wrong, and the z half of that error is the same mirror the section below records.) None of the four resembles it, and that is not evidence against the arrows, because the two quantities are different things. An arrow measures **ghost travel**: from where a part is drawn floating to where it seats. The official model records only seated positions, so it cannot say where the artist drew the exploded ghost, and the offset between two seated pieces is not the distance either of them travelled.
Checking the family therefore needs the drawn position too — the part's projected location in the panel, carried back through the fitted camera — and the test is whether applying one of the four to that position lands on the official seat. That is the comparison worth building, and it is still one between a reading of printed pixels and a source that never saw them.

What was measured is that **applying the four as inter-piece offsets produced no legal placement at all**: constructing the two step-1 pieces at each of the four relative displacements and asking the editor to place them, every one was refused by the support check — "would rest -80/88/24/-32 LDU above the build plate with nothing under it". That is a third independent voice, the repository's own validator, which saw neither the arrows nor the official model. **It has to be re-run.** It was posed against the mirrored step-1 target, so its verdict inherits the mirror and is not evidence about the arrows until it is scored against the corrected pair. It does not by itself condemn the family in either case, because it shares the mis-posing above.

Two things the panels say that no parser produced, and they are worth more than the arithmetic. Step 1's arrows point **down** — the quarter-round plate descends onto the curved arc — and step 2's arrows point **up**, the new wedge arriving from beneath. And of the four displacements this paragraph was written against, exactly one was purely vertical with no ground component, `[0,32,0]`, which was the only one whose direction matched what step 1 draws — and `arrowDisplacementFamily` sorted it **last**. That was a symptom of the raster defect rather than of the sort: at the corrected scale the family is `[0,56,0] [60,8,40]` and the purely vertical reading is first. The docstring's warning still stands on its own terms — that order is a ranking of pixel agreement and not of correctness — but it is no longer what this panel demonstrates.

Blocking at printed step 10 — chosen because step 11 is the only step in that prefix that needs `41769;G`, so a ten-step request separates the frame question from everything else: **seven input failures, all of them one cause.** *(Measured before `3020;L` was pinned. The frame that caused all seven now exists — see "3020;L, derived from its own two sources" below — and the ten-step position has not been re-measured end to end, because the action ledger is stale by one hop against the regenerated calibration and cannot be rebuilt until the coverage artifact reproduces.)*

That cause is a missing Builder frame, and it is no longer `41769;G`. It is **`3020;L`**, the `builtin:plate-2x4` that steps 5 and 7 now resolve to. Two `official-frame-calibration-missing`, two `action-ledger-incomplete`, two `coverage-key-mismatch` and the `set-accounting-mismatch` are all that one design. The blocker moved because the identification did: while those callouts were refused, nothing had to name a design for them; now they name `302028`, and `302028` is design 3020.

The two are not the same kind of blocker, and the difference is the whole decision.
`41769;G` is absent from the local 159-bundle capture, and the one bundle that serves it hashes md5 `fb1e8bb3…` against the manifest's declared `cab7c402…`. The payload is internally sound — Content-Length, received length and the UnityFS header's own declared size all agree at 97802, the origin ETag equals the body md5, and it is byte-identical to a copy taken four days earlier — and the declaration is the thing that looks wrong, because all 157 named captured bundles reproduce their declared checksum exactly while 18 of 175 rows reproducibly do not. But nothing independent agrees with the payload, so admitting it is a judgement about an upstream declaration, not a derivation. That is an owner decision.
`3020;L` is not, and the difference is checkable rather than argued. `3020-L-android.bundle` is in the capture at 9571 bytes, sha256 `a0bee312fc74b5f7f054c255b020933d9afb43a9feac6f12012749b6f659a030`, and the local cache report records it `verified: true` — its md5 `e94e8489ac5f11afd887ba07ab754f21` is exactly what the Builder manifest declares for that row. It is one of the 157 that reproduce their declaration, not one of the 18 that do not, so it carries the independent corroboration `41769;G` has never had. Deriving its frame is the same ordinary work the other fourteen rows went through.
What stood in the way was only that the tooling was pinned to those fourteen: `extract-builder-shell.py` admits exactly its reviewed tuples and `discover-builder-shell.py` fixes `BUNDLE_BYTES` and `BUNDLE_SHA256` to the single 3245-M capture. Turning those single pins into a reviewed set is a source change `real-build-inputs.spec.ts` deliberately refuses to make for itself, and it touches the decode boundary, so it is the kind of change this repository sends to independent review before it lands.

**That work is now done and the frame is pinned; see "3020;L, derived from its own two sources" below.**

One caveat on the coverage these numbers were taken against: the vision pass that produced them answered 224 of 269 clusters before the remaining calls began exceeding their fifteen-minute limit, so 18 callouts are unidentified and the covered prefix of 25 was compiled without them. Steps 1 to 25 are covered; the number could move either way when the rest answer.

Both of the contract defects previously listed here are closed.
`real-build-artifacts.ts` required an `input-rejected` run to carry zero step rows while `inputRejectedRealBuildResult` exists to retain one typed refusal row per requested step — a branch nothing could satisfy, so publication threw on every rejected run whatever its cause and the run's own evidence could not be read at all. It now checks what the status actually forbids: no completed step, no placed piece, no structural hash, no row that is anything but a failure, no row claiming it attempted a piece or reached a canonical step, and either one row per requested step or none.
`scripts/generate-builder-calibration.py` no longer hardcodes any catalog version — the string `basic-parts` does not appear in it, and its 22 tests pass. That entry was a claim about the file rather than a fact about it, which is what retesting an inherited blocker is for.

To see the current position rather than trusting this paragraph:

```
LEGO_REAL_BUILD_REGENERATE_COVERAGE=1 LEGO_REAL_BUILD_REGENERATE_INPUTS=1 LEGO_REAL_BUILD_LAST_STEP=10 npx playwright test apps/web/e2e/real-build-inputs.spec.ts
LEGO_REAL_BUILD_REQUIRED=1 LEGO_REAL_BUILD_LAST_STEP=10 npx playwright test apps/web/e2e/real-build.spec.ts
```

Drop `LEGO_REAL_BUILD_LAST_STEP` to see the twelve-step position instead, which adds the four failures that belong to `41769;G`.

The second takes a Windows share-mode lock over the whole source tree, so nothing else can write while it runs.

## Plan

Ordered by what unblocks the most, and by what can be proven headlessly before anything is drawn.

**1. Connector kinds beyond studs.**
`axle`, `axleHole`, `pin`, `pinHole`, `clip`, `bar`, `hinge`, `hingeSocket`, with an explicit compatibility table and an allowed-rotation field of fixed, quarter turns, or continuous.
LDCad's model is the one to copy because it has been tested against the whole library: gender, a section profile, and a grid clause that compresses a whole field to one line.
Nothing articulated is possible before this, and it is pure model work with no rendering.

**2. Mass properties. Done, except inertia.**
`partMassProperties` derives volume and centre of mass from the compound body a part already declares, rather than storing them — nothing to keep in step, and nothing new in the geometry digest.
The volume is of the modelled solid, which is solid where a real brick is hollow, so the mass estimate runs about double: a 2x4 brick reads near 5 g against a real 2.4 g.
That is fine for relative mass and wrong for absolute, so it is named an estimate and `inventory.knownMassGrams` overrides it.
An inertia tensor is not done and is only needed once an engine exists.

**3. Rigid components.**
Connected components over rigid edges only, with articulated edges as boundaries.
Testable headless, and it is the thing physics consumes, so it should be correct before an engine is chosen.

**3a. An articulated part, so joints are exercised. Done.**
A Technic brick 1x2 with a round hole and a 2L axle, measured from LDraw `3700` and `32062`.
The hole is one port at `[0, -2, 0]` with its axis on x, not two: it is one feature open at both ends, which is why axis matching became a property of the pair — a stud enters a clutch from one side only and must oppose it, a shaft may enter a hole either way and need only be collinear.
Both parts are boxes for collision. The hole is a void the connector graph knows about and the solid does not, which over-claims space by exactly the hole and so refuses more than a real part would — the safe direction.
The visible cost of having no mesh layer: the brick renders as a plain 1x2, because the hole is not geometry.

**4. Compound bodies and constraints. Done, except incremental rebuild.**
`derivePhysicsScene` turns the assembly graph into bodies and joints as plain LDU data, and `apps/web/src/physics/rapier-world.ts` runs them in Rapier.
One body per rigid component, one constraint per articulated joint, and no constraint per stud.
Mass comes from the catalog rather than from collider volume, so the engine cannot disagree with what a part weighs.
Rebuilding only the components a change touched is not done: today a change rebuilds the scene.

**5. Edit and simulation modes. Session done; the app toggle is not.**
`startSimulation` reads the document once, builds bodies from it, and afterwards reports only where those bodies have got to.
Restore is therefore not an operation at all, which is stronger than making it a reliable one: an operation that does not exist cannot fail or be interrupted halfway.
It also sidesteps a real conflict — a solver produces a brick resting at 23.37 and tilted four degrees, and no such thing is a lattice position.
Apply, if it is ever wanted, needs its own answer about where off-lattice parts live, and should be a separate posed layer rather than a write to the build.
Still to do: the mode toggle in the app, and a viewport that draws part poses instead of document transforms while a session runs.

**6. Indexing and instancing, when measured.**
A connector index and instanced rendering are both real needs at 1465 parts and both are premature before a number says so.
The repository's rule is to build the measurable intermediate first, so these wait on a profile that names them.

## Notes on the demonstration

The specified vertical slice is a wheeled cart, which needs items 1 through 5 and a wheel, an axle and an axle-bearing brick in the catalog.
It is the right target: it exercises rigid assembly, articulation, support and simulation together, and none of it can be faked.

A useful intermediate exists first, though, and costs almost nothing: place two compatible parts, snap, save, reload, render.
Every piece of that already works here, so it is a regression test rather than a build.

## The official model's world placements were mirrored, and step 1's target was unbuildable

Found 2026-08-07 while asking why step 1's arrow family contained no legal placement, and corrected the same day. It is upstream of the placement question and of every frame admitted from Builder, so it is stated before the plan.

`resolveBuilderBoneTransform` read a Builder Bone through `diag(1,-1,1)` — determinant **-1**, a reflection. The correct LXFML-to-LDraw change of basis is `diag(1,-1,-1)`, a 180-degree rotation about x, determinant +1, which is the frame the rest of the repository already declares: `scripts/builder_ldraw_frame.py` writes it as `diag(25, -25, -25)` including the 25 LDU-per-stud scale, and `docs/design/part-model.md` states it in words. `resolveBuilderBoneTransform` was the one place that disagreed.

**The sign vector is shared by two halves of one map, and negating the position alone makes it worse rather than better.** The rotation is conjugated by the same `S` the translation is scaled by; changing one turns a reflection into something that is not a rigid motion at all. Both halves moved together, at `apps/web/e2e/real-build-builder-calibration.ts` lines 189-210 and 231-235, and they now read one declared constant.

Three independent lines said so, and each is recorded with the number it produced.

A global test against the official `.ldr` export, over 1465 bricks and 173 designs, requiring each design to admit one part frame: the z-negated reading explains **1439 of 1440** instances, the reading the repository had **656**. Rotations only, never touching a position, over the 1129 confidently aligned bricks: `diag(1,-1,-1)` explains **1129/1129** instances and **155/155** designs, `diag(1,-1,1)` **443** and **102**, and the two improper controls fewer still.

A physical-interlock census of the assembly under six candidate readings, which never opens the `.ldr` export at all: over the 40 placeable instances of the 14 pinned designs, `diag(1,-1,-1)` is the only reading with **zero** stud/stud and **zero** clutch/clutch coincidences and zero body overlap, and it is also the one with the *most* interlock — 186 stud/clutch coincidences over 49 connected pairs against the incumbent's 129 over 37 — so the zero is not bought by pulling the parts apart. Under the reading the repository had, 62 distinct world points carried two studs at once.

The consequence at step 1 is checkable without any of that reasoning, and the editor is the arbiter because it refuses a placement nothing holds up at the command. At the repository's previous canonical transforms — `80015;E` at `[0,8,0]` yaw-180 and `30565;E` at `[60,0,-20]` yaw-0 — the second piece was **refused**: "would rest 8 LDU above the build plate with nothing under it". Negating z alone is also refused. At the corrected transforms — `80015;E` at `[0,8,0]` **yaw-0** and `30565;E` at `[40,0,-40]` **yaw-0** — the editor places both, each is held by connections rather than by the plate, and it derives **three** stud/clutch pairs between them.

So the run had been searching for a step-1 placement that cannot exist. That is also why every one of the four arrow-family displacements was refused for want of support: the family was being resolved toward an unbuildable target.

Why no existing check caught it is the part worth keeping. `scripts/extract-builder-shell.py` writes Shell vertices as `-25·v`, negating all three axes, which is correct because the Android asset bundle's frame is left-handed — the chiral design `54383;F`, a *right* wedge plate, lands on the official LDraw surface at p95 1.250 LDU under that decode and cannot be fitted at all with z flipped, p95 10.714. That decode is unchanged. The mirrored reading applied the same handedness to LXFML Bone data, which is right-handed, so **the local part frames were right and only the world placements were mirrored** — and every check the repository had was a check of one part against its own surface, which a mirror leaves exactly as correct as before. Step 1's own two parts cannot settle it either: both are mirror-ambiguous under a free upright fit, scoring within 0.03 LDU either way.

### What moved, and what did not

The 14 pinned `designFrames` — every frame there was at the time — are **unchanged**, string for string, including their selection method and witness margin: the fix is world-only. `BUILDER_STEP1_GEOMETRY_BUNDLE`, the LDraw archive and closure digests, the 14 `expectedCatalog*Digest` rows, `BUILTIN_CATALOG_VERSION` and `MIGRATABLE_CATALOG_VERSIONS` are all untouched, and no package imports the harness — `git grep -n "e2e/real-build" -- packages` is empty.

What moved is world truth. All four `BUILDER_STEP1_CALIBRATION_CASES` negate their `positionLdu` z, and two of the four exchange quarter turn, because conjugating a yaw by the extra z flip inverts it: `[270,-16,244] yaw-0 → [270,-16,-244] yaw-0`, `[270,-580,104] yaw-90 → [270,-580,-104] yaw-270`, `[390,-572,104] yaw-180 → [390,-572,-104] yaw-180`, `[410,-580,104] yaw-270 → [410,-580,-104] yaw-90`. The origin policy's `expectedComposedTransform` moves from `[540,-4,194] yaw-180` to `[560,-4,-194] yaw-0` and its `expectedEmptyEnumerationTransform` from `[0,8,0] yaw-180` to `[0,8,0] yaw-0`. The report is a truth change rather than a shape change, so `BUILDER_CANONICAL_CALIBRATION_SCHEMA` is `lego.builder-canonical-calibration/8` and a retained `/7` artifact is refused by name.

`output/real-build/builder-canonical-calibration.json` and `output/real-build/action-ledger.json` are re-derived, not hand-edited; every piece's `evidenceDigest` changes because `pieceEvidenceDigest` hashes `builderCalibrationDigest`. **Every retained run bundle under `output/real-build/runs/` predates this and was scored against the mirrored target**; they are kept only as history and a replay will reject them on the ledger digest rather than silently agreeing.

**Settled, and it inverts what this paragraph said an hour ago: the pipeline is right and the export is the outlier.** Two frames were named here as not reproducing the `.ldr` export. Neither claim survives.

`3832;G` reproduces the export exactly, in both instances. So do `3032;F`, `3034;J`, `3460;N`, `3795;I`, `60479;F` and `91988;F`. All were flagged by comparing `orientationId` as a string: for a 2x10 plate a yaw 180 degrees away is the part's own self-symmetry, every stud and every body point identical. **An orientation comparison that is not taken modulo the part's own symmetry manufactures false positives**, and it manufactured seven here.

The shared `(0,-4,0)` is not a residual at all — it is `ldrawToCatalogLocalTransform`, which every row already declares (fourteen when this was measured, fifteen since `3020;L`). LDraw puts a plate's origin on its top face and this catalog centres the body on `y=0`; half of an 8 LDU plate is 4. Once it is accounted for, **39 of the 40 upright instances reproduce the export to the LDU** — 42 of 43 since `3020;L` was pinned, with the same single outlier.

The fortieth is `80015;E`, and the pin is right. Three part-local sources agree on it: the stud correspondence is genuinely unique — one exact bijection over four yaws and five anchor choices, so `frameCandidateCount: 1` is honest; `80015.dat` matches the catalog blueprint bit for bit; and the Builder Shell meets the LDraw surface at p95 1.2514 LDU at the pin against 56.58 at the export-implied frame.

What settles it needs no Builder data at all. A coplanar interpenetration audit of the export's own 1470 parts finds **exactly two** collisions in the entire model, and both are that one brick — `80015` into `30503` by 632 LDU² and into `54383` by 70 LDU². Placing it where the pipeline does removes both and leaves the model collision-free, and takes its stud coincidences from 2 to 5, of which 3 are with `30565` — the `connections=3` the step-1 golden pins. Inverting the two frames says why: the pinned origin lands on a stud cell with material under it, the export-implied origin lands **inside the ring's hole, where `80015` has no material at all**. It is a whole-cell index error in the LXFML-to-LDraw exporter, which is why the rotation and the y are perfect and only x and z move, by exactly three cells and two.

So the step-1 golden asserting `[40,-8,-40]` with three connections is correct, and `[100,-8,0]` — recorded here earlier as the answer, and placeable by the editor — is where the export wrongly puts the brick. **The export is a corroborating witness, not an oracle**, and this is the one place it disagrees.

### 3020;L, derived from its own two sources

The design that blocked printed steps 5 and 7 is pinned, and the pin is `catalogToBuilderLocalTransform: {positionLdu: [30,-4,10], orientationId: "upright-yaw-90"}` for `builtin:plate-2x4`, at `frameSelection: catalog-part-self-symmetry`, `frameCandidateCount: 2`, `frameEquivalenceClassCount: 1`, p95 = max = **1.305568 LDU** against the expanded LDraw surface.

The two legs of `builder-type23-frame-plus-ldraw-surface/3` were computed apart and then compared, which is the only thing "independently verified" can mean here.
The Builder leg reads the type-23 `Custom2DField` out of `3020.xml` inside the bundle — a 9x5 node lattice at identity rotation and translation `(-0.4, 0.32, -1.2)`, whose eight `0:4:1` male nodes map through `diag(25,-25,-25)` to `builderStudCentersLdu` `x ∈ {0,20,40,60}`, `y = -8`, `z ∈ {0,20}` — and puts them in exact integer correspondence with `plate-2x4`'s eight stud connectors. That yields exactly two frames, `[30,-4,10] yaw-90` and `[30,-4,10] yaw-270`, whose residual is a pure half turn about the part's own origin, which is the 2x4 plate's own symmetry, so they are one class and no mesh is consulted to choose.
The LDraw leg never sees the stud lattice. It searches every upright quarter turn against a 2 LDU translation lattice for the rigid motion that puts Builder's own 48-vertex Shell closest to the expanded `parts/3020.dat` surface, and its global optimum over the 54 admissible frames is `[-30,8,-10] yaw-0` (with the half-turn twin `[30,8,10] yaw-180`) at mean 0.3245 LDU and max 1.3056 LDU, beating the nearest non-symmetric alternative by 2.39x.
The Builder-to-LDraw motion the two authored lattices predict — Builder frame, then catalog, then LDraw, with no surface input anywhere — is `[-30,8,-10] yaw-0`. **The two derivations agree exactly, to the LDU and to the quarter turn.** The quarter turn is not a free parameter either: reading `ldrawToCatalogLocalTransform` at `yaw-0` or `yaw-180` misses the surface by 20.000001 LDU, and `yaw-90` and `yaw-270` differ by the plate's own half turn and produce identical evidence.

The frame also lands where the family's arithmetic says it should. Every 2xN plate already pinned reads `[(N-1)*10, -4, 10] yaw-90` — 3795 `[50,-4,10]`, 3034 `[70,-4,10]`, 3832 `[90,-4,10]`, 91988 `[130,-4,10]` — and 3020 at N=4 reads `[30,-4,10]`. That was derived from 3020's own field with no reference to the others.

The 1.305568 is not slack in the fit and it is worth saying what it is, because every other simple plate reports 1.299038. **Twelve of the sixteen unique Builder Shell vertices land on the LDraw surface at 0.000000 to 0.000002 LDU** — exactly on it. The only four that do not are the corners of the underside cavity's ceiling ring, where Builder's Shell insets 3.75 LDU and sits 1.25 LDU deeper than LDraw's cavity ceiling; on a square part that reads `sqrt(0.25^2 + 1.25^2 + 0.25^2) = 0.75 * sqrt(3) = 1.299038`. On 3020 the four read `1.305568`, `1.299038`, `1.296754` and `1.290179`, because Builder's own ring is authored off-square: a corner sits at `66.28197` where `66.25` is exact and at `26.1989` where `26.25` is, and `sqrt(0.25^2 + 1.25^2 + 0.28197^2)` is `1.305568` exactly. That 0.03 LDU is in the source asset, not in the frame, and a frame error cannot produce it: the candidates are quantized by exact integer stud correspondence, so the nearest wrong frames sit 20 and 40 LDU away, and any of them would move all sixteen vertices rather than leave twelve at zero.

Verified by consequence rather than by the error going away.
All three `3020;L` instances that resolve now reproduce the `.ldr` export exactly, at lines 463, 524 and 542; the two blocked bricks are `ee5dafb5` at export `(380,-8,-194)` and `479c0207` at export `(500,-8,-194)`, both identity rotation, matched modulo the plate's own half turn. Across the whole model the count goes from 39 of 40 calibrated instances reproducing the export to **42 of 43**, with the same single outlier — `80015;E`, where the export is the thing that is wrong.
The set accounting closes: printed steps 1 to 8 supply 18 pieces and now resolve **18 of 18**, against 16 before.
A solid-body interpenetration audit over the calibrated bricks finds **zero** overlaps involving `3020;L`. Step 7 does raise two `PART_STUD_BODY_COLLISION` issues in the editor, and neither is a 3020: both are `51739;H`'s wing plate against `3795;I` and `54383;F`, which is a pre-existing property of that mesh-derived body.

One consequence is not yet closed and is a handoff, not a defect. `output/real-build/action-ledger.json` binds `builderCalibrationDigest`, so it is stale by one hop: it holds `sha256:7553ee6d…` against the regenerated report's `sha256:74fe664e…`. Stage 1 of `real-build-inputs.spec.ts` — the reviewed source pins — passes and stage 3 rewrites the calibration, but stage 4 cannot rebuild the ledger while `output/real-build/catalog-coverage.json` does not reproduce from its own identification answers, which is an unrelated in-flight regeneration. **Re-run `LEGO_REAL_BUILD_REGENERATE_INPUTS=1` once that settles**, or the next run refuses on the ledger digest rather than on anything about this frame.

And it was looked at. Printed page 12 draws step 5 placing a **green** 2x4 plate flat and studs-up in the notch beside the round hull, and the render of the calibrated prefix after step 5 puts it in the same place with the same long axis. Printed page 13 draws step 7 from the flipped viewpoint with the blue 2x8 crossing between two green 2x4 plates; the derived transforms put green `[-220,-140]`, blue `[-140,-100]` and green `[-100,-20]` edge to edge along x on the same `y=8` layer, which is that cross. Note the colour while you are there: material 28 on these two bricks is **green**, not dark bluish gray, which the parts bin on page 12, the `.ldr` export's colour 2, and `part_description_retrieval.py`'s "Element 302028 (Green Plate 2 x 4)" all say independently.

One thing to carry forward rather than fix: `80015;E` is the single pinned design where both part-local checks are mirror-blind. An improper fit — mirror in z with the same translation — maps the same five studs and scores digit for digit identically on the surface, because the quarter ring's stud set and body are both diagonally symmetric. It was not the cause of anything here, and the frame search only walks the four proper yaws so it was never reachable, but it is where two of this repository's lessons meet in one part.

## Placement: search against the next panel, not inference from this one

Nothing here places a piece yet, and the booklet does not say where pieces go.
It gives placements rather than connections; roughly half its step highlights are open contours, because the new parts go behind built ones; some steps are drawn exploded, so the highlight fixes shape and orientation but not position; and a panel difference finds the right stud without fixing the offset.
Treated as an inference problem — read this panel, output a transform — that is underdetermined, and the parts of it that are underdetermined are exactly the ones a rule cannot close.

It is not underdetermined as a search problem, because the booklet renders the same growing object 359 times.
Panel N+1 contains everything placed at step N. A wrong placement therefore does not merely produce a wrong model; it produces a visibly wrong panel N+1, and N+2, and every panel after. Each future step is a free check on every past placement, so the loop is: propose a placement, render the model from the panel's own camera, compare, and on disagreement undo and take the next candidate.

That reframing dissolves the three hard cases rather than solving them. An exploded step need not be read at all, because a *later* step is not exploded — as written this said "the following step", which is false on this booklet: printed steps 1 and 2 are both exploded and step 3 is the first seated panel, and arrows kept 2, 2, 0 says which is which for free. An open contour does not matter when the whole render is being compared instead of the highlight. And "the right stud but not the right offset" describes a candidate set, which is what a search wants.

The oracle already exists: `compareBuilds` scores a rebuild per step, `capture_model_views()` renders canonical views, and the camera is solved — a panel's own stud grid fits the booklet's angle and scale with no part identities. The lattice, the connector graph and the collision check prune candidates before any render, and the depletion walk narrows which part is being placed.

Vision belongs here in two distinct jobs, and the distinction is measured rather than stylistic.
Reading an exploded panel — which floating piece belongs where, on which face, which way up — is general visual sense and cannot be hardcoded; that is a PROPOSER, open-ended, and cheap to be wrong because a wrong candidate is discarded by the next panel.
Asking whether a render matches a printed panel is a CHECKER, and it must be posed as same-or-different: the open N-way form measured 39.9 percent self-consistent on this booklet where the closed binary form scored 84 of 84 between two independent raters.
Proposer output stays untrusted data, as every other provider result here does.

Symmetry defers the check rather than defeating it, and that distinction sets the real requirement.
A placement that renders identically to the correct one contradicts nothing at the step it is made — but it is only *locally* symmetric. A 2x2 plate rotated a quarter turn is indistinguishable alone; attach a 1x2 to one of its edges and the two orientations diverge. The booklet does precisely that, because the model keeps growing on top of every placement, so a symmetric mistake surfaces as soon as something lands that breaks the symmetry, and then it fails like any other wrong placement.
The frame calibration met the same shape and settled it up front, with an independent witness at a measured margin, because a catalog frame is fixed once and reused everywhere. A placement is not: it sits in a sequence that keeps testing it.

So the requirement is not to resolve ambiguity before committing, which is expensive and sometimes impossible. It is to be able to go back far enough when it surfaces — deep backtracking, and a history the search can actually rewind.
That also gives the loop its own measurable: how many steps had to be undone, and how far back the deepest reversal reached. A number that can be recorded and driven down, where "prove this placement is unique" cannot be.
Building it right in one shot is not the goal; noticing and recovering is.

`runBacktrackingSearch` now exists and reports those numbers. It commits to the best candidate, keeps every rejected alternative, and walks back to the shallowest step with an untried one; `BuildTree` addresses a node by its parent and its placement, so returning to an abandoned branch finds the work already there and the wrong branch survives as counterevidence rather than as a gap.
Two things about it are worth stating because both were wrong on the first attempt and neither was visible from the passing tests.
The number it exists to produce was zero on precisely the run where it is the answer: a reversal was recorded only when the search advanced again afterwards, so a search that committed thirty steps and unwound all thirty reported none, and the last descent — by construction the deepest one made — was the one never counted.
And its per-step allowance was a silent cap whose verdict named the wrong subject: with the default of four alternatives and six placements at a step of which only the sixth is viable, it reported that the booklet could not be satisfied by any placement this catalog and camera can produce, for a limit the caller had set. Withheld alternatives are counted now and the verdict says which of the two it is holding.

`runBacktrackingSearch` is still not wired into the real booklet run, and a step that *has* a scoring signal still settles against its own panel through `selectUniquePlacementScore`. What is wired is the half of this section a signal-less panel needs: a step whose panel prints no highlight defers to the next one and is settled there, which is what placed the first two pieces. The uniqueness requirement is not removed, it is moved — the deferral demands a margin too, and refuses by name when it is not met, because the alternative at 400 candidates is a confident wrong pick. Replacing that refusal with a beam that keeps its alternatives and unwinds is the next change, and the list it has to recover from is now a real one rather than a hypothetical.

**Before any of that, the loop was scoring against the wrong face of the model.**

This set is built partly upside down. The booklet turns it over mid-build and marks each turn with a printed icon, and over printed steps 1 to 12 that icon is a strict toggle of which side the panel is drawn from: steps 1-3 studs up, 4 underside, 5 studs up, 6 studs up, 7 underside, 8 studs up, 9 studs up, 10 underside, 11 still underside. Read by eye off pages 11 to 15; every one of them checks.
The cue was detected and understood and then consumed by nothing. `deriveTransitionPanelFeatures` finds the icon exactly, an earlier pass correctly concluded it annotates the viewpoint rather than naming the action, and correctly refused to map it to the `rotation` action class — and then no viewpoint state was ever built. `transition-classifications.json` still holds 25 attachments and one final-view, zero rotations, nothing before step 44.
The camera fit cannot catch the error, because it is fitted to the panel's own stud grid and a projected square lattice reads the same from above and below. It reports a positive elevation on every panel it fits, flipped ones included, and its own legend already says it is a fit quality and not a proof.
So from step 4 onward a candidate render would be compared against the opposite face of the printed drawing. No frame and no identification fixes that, which puts this above `3020;L` in the order: clearing that frame would have started the placement loop and then failed it for a reason nothing in the run reports.

**All three parts of it are now fixed, with numbers.**

The icon count was an undercount, and the cause was not attribution. `extractPageShapes` stacked only the transform on save/restore, so `fillHex` leaked forward across a restore; page 13 draws the icon twice and the second takes its white from the restored state, so it was reported `#000000` and `isRotationIcon` — which keys on `#ffffff` — never saw it. Restoring the whole graphics state takes the booklet from **39 icons to 43**, and steps 8, 131, 142 and 253 appear. "39 icons, one per page" was that leak stated as a finding.
`derivePanelFaces` now folds the icons into a `panelFace` per printed step, carried on `TransitionPanelFeatures`. It is scored against a blind reading of the pages rather than against the icon: two independent raters read the rendered panels of steps 1 to 43 without being shown the icon or each other's answers and agreed on all 43, and the fold reproduces **43 of 43**. Before the fill-colour fix the same comparison scored **7 of 43** — a single missed icon inverts the parity of every step after it and later icons keep toggling from the wrong phase, so it never resynchronises. Underside panels in the opening are 4, 7, 10, 11 and 16. The judged truth is committed at `apps/web/test/fixtures/panel-face-ground-truth.json` and gated in Vitest.
The seed — that printed step 1 is drawn studs up — is an assumption and is named as one; `derivePanelFaces` takes it as a parameter, so one judged panel fixes the phase of every other by parity.

**Where the face has to be applied, measured.** Not at the camera fit. A below-view lattice at azimuth A is exactly the same lattice as an above-view at azimuth -A — `a(A, -e) = a(-A, e)` and `b(A, -e) = -b(-A, e)`, and negating one basis vector spans the same lattice — so a stud grid cannot separate the two faces even in principle, and the fitter's search over re-basings always reaches the positive-elevation twin. Refitting all forty panels of the camera-fit run a second time as below-views returned no solution on any of them, the five drawn from underneath included. The 33 of 40 it fits and the 7 it refuses have nothing to do with the face.
So `panelFace` is a render-time input: the panel fit supplies azimuth, scale and phase, and the icon supplies the sign of the elevation a candidate is rendered at. Rendering one plate at +35 and -35 degrees confirms the sign does move the camera below. Its underside comes out featureless, because `builtin:plate-6x6` is a parametric box with no tubes, but `scoreStepDelta` is silhouette-only — region IoU plus stroke recall and boundary precision — so missing interior detail never reaches the score.

The transformation is `(-A, -e)` rather than `(A, -e)`, because the fit reports the negated azimuth when the true view is below — strictly, `-A` modulo 90, since the lattice pins the azimuth no further than its quarter-turn coset. `(A, -e)` does put the camera below the model; it arrives from the wrong side of it. This was checked by rendering a deliberately asymmetric model under all four sign combinations, but that probe was not retained, so the claim rests on the algebra above and on the closed-loop drive below rather than on a committed artifact.
What remains ambiguous after that is the azimuth branch. A quarter turn maps A to A+90, which folds to the same value modulo 90 and so cannot produce two different reported azimuths; the 55.1 and 34.8 degree groups the run reports over its first forty panels are the *mirror* branch, plus or minus A modulo 90, which is the same above/below identity again. The grouping does not line up with the faces: step 7 does not fit at all, steps 12 to 15 are studs-up yet sit with 10 and 11, and step 16 is underside yet fits with the 55-degree run. Neither an explanation nor a correlation is established here, and asserting one would be inventing it. What is established is that the branch is a small candidate set, resolved the way the rest of this section resolves things — render each and let the panel score them.
Printed step 4, once it fits, is the cleanest single instance of the mirror and does not settle the question either. Its measured basis is its neighbours' reflected in x to within half a pixel — `a` = (36.10, 14.60) against step 3's `b` = (-36.00, 14.55), `b` = (-24.76, 20.44) against step 3's `a` = (25.18, 20.60) — at the same elevation, 35.01 against 35.03, and the same printed pitch, 16.134 points per stud against 16.193 and 16.014 either side. So its azimuth reads 34.71 where theirs read 54.97 and 54.24. That is exactly what one underside panel between two studs-up ones should look like, and steps 12 to 15 are the counterexample that stops it being a rule.

**The loop itself now survives a flip, measured on a synthetic booklet.** `build-search.spec.ts` drives the real enumerator, renderer, highlight extractor, score and beam over a six-step booklet with two underside panels, twice — once told which face each panel was drawn from, once not. Told, it rebuilds 6 of 6 with the drawn placement ranked first at every step. Not told, it ranks 5 of 6 and diverges at step 3.
The failure mode is the part worth knowing, because it is not a refusal. At the first underside panel the face-blind run never rendered the correct placement at all: the candidate projects nowhere near the highlight, so the proximity prune discarded it before scoring and the best score fell from 0.96 to 0.294. The beam survived on a wrong candidate and carried the error to the end. A face-blind loop does not stop — it produces a confidently wrong build, which is exactly the failure a per-step check is supposed to prevent and would not have.
Only one of the two underside steps loses, though, and the margin is one step. At the second the face-blind run did render the drawn placement and ranked it first at 0.559, so "no placement can satisfy it" is false at half the underside panels; what a wrong face costs depends on how far the projection moves. Only the inequality is asserted in the test — the 6/6 and 5/6 are reported, not gated, and can drift.

**The printed run now reads it.** `real-build-run.ts` is no longer face-blind: `viewForPanelFace` applies the icon's sign to the fit, and both consumers take the corrected view — the renderer *and* `panelProjectionFromFit`, because a face-blind projection would convert the printed arrows into the wrong displacements before any render happened. The face rides on `RealBuildPanelSpec` and is reported per step, bound by the browser-output contract to the face the inputs declared, so the run can no longer report a face it was not given.

It is nullable and a null is a refusal, not a default. The fold is a running parity from step 1, so it is derivable only over a contiguous prefix whose icons have all been read; a step outside that prefix fails `panel-face-unknown` rather than rendering studs-up. Defaulting there would reintroduce exactly the silent error, since a face-blind step reports a low score and not an error.

Measured on the booklet through printed step 16: the derivation the run performs returns underside at **4, 7, 10, 11, 16** — the blind-judged set exactly — and the render camera on those steps moves from azimuth 55.10 elevation 35.59 to **-55.10 and -35.59**. `panel-face.spec.ts` still reproduces the fixture's icons from the booklet itself after the fold moved to `src/assembly/panel-face.ts`, where a Vitest suite now also pins the algebra: the two faces span the same lattice through the real `panelProjectionFromFit` (`a` identical, `b` exactly negated), and `(A, -e)` matches neither.

**The flipped render now happens on a printed panel, and the panel that blocked it was blocked by the fit's ranking rather than by its face.** Printed step 4 refused with `camera-fit-failed`: 9.11px from the closest upright axonometric against the 0.02 gate, on a 92.19px pitch — twice what every panel around it measures. The face was not the cause and could not have been; the cause was one line of the candidate sort.

`fitStudLattice` builds a candidate lattice from every pair of autocorrelation peaks and ranks them by explained peaks, then by coarsest unit cell, and only then by the axonometric residual. On step 4 two candidates explained six peaks each: the panel's own grid at 43.8px and a cell of 1106, and a lattice with twice that cell that no upright axonometric view can print. Coarsest won, at 10% of pitch, and the panel was refused on a number produced by a candidate that had already lost on the criterion that matters. The gate was never the problem — the right answer was sitting second in the list at 0.5% of pitch, four times inside it.
So the residual is now consulted where the candidate is chosen and not only after it has been. Whether a candidate is an axonometric projection at all, by the same threshold and the same value, ranks above coarseness: the coarseness preference exists to stop a finer lattice halving the pitch, and that can only mean something among readings that are readings. This does not widen the threshold and cannot — a candidate that passes the new key still has to pass the same gate at the end, on the refined and re-reduced basis.
Measured over the same forty panels, before and after, per panel: **exactly one changes**. Step 4 goes from refused to azimuth 34.71, elevation 35.01, 43.77px per stud, residual 0.208px — **0.475% of pitch**, against the 2% gate — and 16.134 points per stud where its neighbours measure 16.193 and 16.014. Every other panel's azimuth, elevation, pitch, residual and coherence are unchanged to every digit reported. Fitted panels 32 → **33**. `overlay-004.png` is the check that matters: the predicted cells land on the drawn tubes, the folded cell shows a ring at 0.25 of a pitch with contrast 1.3, and the autocorrelation inset goes from a smeared diagonal ridge to a grid.

A second defect in the same function is found, reproduced and deliberately **not** fixed here. `canonicalPair` reads its argument order as information: peaks arrive below the horizon, so a left-pointing one can only be `b` and a right-pointing one only `a`, and a pair whose left-pointing member is passed first is silently dropped rather than swapped. Printed step 7 is refused by exactly that — its grid steps are (23,19) and (-33,13), the left-pointing one is the picture's strongest repeat and the right-pointing one only its ninth, so the pair never forms; trying both orders recovers it at **0.2% of its pitch** on the camera its neighbours measured. It is left undone because the pairs it adds are not all good ones: on step 4 they include a lattice at exactly twice the pitch which then wins that panel's ranking on explained peaks, so the two changes have to be measured together. The reproduction is in the function's own comment.

With the fit fixed, printed step 4 renders. It reaches the placement search, extracts a highlight of 816px of closed stroke, and scores **220 candidate renders** — the first flipped render this loop has ever compared against a printed panel. It then fails for a different reason and a real one: `ambiguous-placement-score`, the best two placements of `builtin:plate-1x8` separated by **0.0011** against a required margin of 0.01, at a best score of 0.273. A long thin plate slid along its own axis barely changes its silhouette, which is the degeneracy the next panel exists to settle — panel 5 draws that plate seated and unhighlighted. The exhaustive half of the benchmark declined separately, needing 240 renders against an explicit 220 budget, so the two strategies cannot be compared at step 4 until one of them has an answer.

The mirror correction above is why that failure was worth re-measuring rather than reasoning about further. `canonicalTransform` is the target `benchmark-disagreement` is a disagreement *with*, and until 2026-08-07 that target was mirrored, so a score of zero at step 1 was a measurement of a target that no longer exists. The four-step probe has now been driven and replaces it: printed step 1 completes 2 of 2 by lookahead, steps 2 and 3 complete 1 of 1 each, and the only `benchmark-disagreement` left in the first four steps is step 4's, where the pruned search refuses on an ambiguous score and the exhaustive one declines on budget — a disagreement between a strategy that looked and one that did not, which is what that code says it is for.

### The deferral now covers a panel that scores but cannot choose, and printed step 5 still cannot settle step 4 (2026-08-08)

**The trigger is extended and the sentence above about *why* step 4 is ambiguous is wrong.** The deferral used to fire on one condition — a panel with no scoring signal at all — and step 4 has a signal that merely fails to discriminate. `ownPanelCannotSeparate` adds the second trigger, admitting `ambiguous-placement-score` and `tied-placement-score` and nothing else: `zero-placement-score`, `no-placement-candidate`, `incomplete-placement-scoring` and `resource-budget-exhausted` each say the step was not looked at properly rather than that the drawing does not distinguish two seats, and the same code is also spent on non-finite scoring evidence, which is why the predicate demands two finite scores and a valid minimum as well as the code. The refusal is asked of the *pruned* decision rather than the benchmark's verdict, because at step 4 the benchmark reports `benchmark-disagreement` for a reason that has nothing to do with the panel — the exhaustive half needs 240 renders against a 220 budget.

What the two tied placements actually are, measured: `builtin:plate-1x8` at `[60,-8,-50] upright-yaw-270` scoring **0.2734538947219428** against `[80,0,-50] upright-yaw-270` at **0.2723736926391197**, margin **0.0010802020828231118** on a 0.01 requirement. They differ by `[+20,+8,0]` — **one stud across the plate and one plate-height down**, not a slide along the plate's own axis. The wedge is not ambiguous at all: both branches settle it at `[60,8,-20] upright-yaw-0`, score 0.36635. A per-piece score whose ceiling is one piece's share of a highlight ringing the whole step is what 0.273 is; the two seats are an axonometric degeneracy of that camera, not a symmetry of the part.

**A step that has a panel must narrow with it, or the product is unscoreable.** Carrying step 4's whole product forward the way step 1's is carried gives `240 x 334 = 80,160` candidates against a 512 budget, and the run refused exactly there. `placementsOwnPanelCannotSeparate` reads `selectUniquePlacementScore`'s own rule down the ranking instead of across the top two — a gap of at least `minimumScoreMargin` is a real separation on this panel, so the placements *within* that margin of the best are the ones it has not separated — and carries only those. No new number: printed step 4 narrows **240 of 240 to 2** on the plate and **334 to 1** on the wedge, a product of 2. What that narrowing can get wrong is dropping the drawn placement, which is precisely why the deciding gate is the winner's absolute agreement rather than its margin.

**Printed step 5 cannot settle step 4, and the reason is its own contour rather than the candidates.** Panel 5 prints **2 open highlight contours** — 1429px of stroke, `closedContourRate` 0, no filled region — so `alreadyBuiltMask` removes an outline and nothing else, and the long plate and green 2 x 4 that step 5 *places* stay inside the art step 4's prefix is required to explain. Measured on that panel: built art 21,442px; the settled three-step prefix alone leaves **11,447** built-only pixels, and either whole-step candidate leaves **8,556**. Both candidates therefore report agreement around 0.53 against the 0.85 bar — 0.529091 and 0.525042, separated by 0.004 — and the first run of the extension refused them with `weak-deferred-agreement`, whose message asserts the winner is the least wrong of the wrong ones. That claim is not established, so the deferral now checks the panel before it enumerates anything: a lookahead highlight that encloses no region cannot supply the exclusion the agreement metric is defined on, and the refusal says so and names the contour as the ceiling.

**Two further measurements, both taken by looking and both open. The first has since been explained, and its stated mechanism was wrong — see "The camera a panel drawn from underneath needs" above.** Panel 4's registration of the settled prefix is weak — `anchorIou` **0.393**, against 0.903 at step 2 and 0.627 at step 3 — and it is weak in a way no camera choice fixes: rendering the prefix at all sixteen quarter-turn and sign variants of the fitted view and registering each against panel 4's already-built art gives a flat **0.400 to 0.444**, best at azimuth 235.29. The step's own highlight is a closed contour running diagonally across the whole assembly, so what is left to register against is a fan with a band cut out of it. Every step-4 score, including the 0.0011, is measured through that registration. *(The sweep's conclusion holds and its explanation does not: the answer was outside the swept family, because turning a model over also rolls the image half a turn and no azimuth or elevation expresses that. Excluding the highlight from both sides moves panel 4 only from 0.3933 to 0.4821, so the band it cuts is not what the registration was losing; supplying the roll and the quarter turn moves it to 0.831192.)* Evidence looked at, all under `output/build-search/zzz-step4/`: `panel-4.png`, `panel-5.png`, `p4-candidate-0.png` (prefix alone against panel 4's built art), `p4-candidate-1.png` and `p4-candidate-2.png` (the two whole-step candidates against the printed contour), `p5-plain-0.png` (prefix alone at panel 5) and `p5-plain-1.png`, `p5-plain-2.png`.

The number of record is unchanged and was re-measured after the change: at `--last-step 3`, `stepsComplete` **3**, `piecesPlaced` **4**, `finalParts` 4, structural hash `sha256:6d46478c0565ad70fefb4fb459db91282991d642c93e1115f430947cb96f5120`, with step 1's deferral reporting the same 400 candidates, agreement 0.903118 and margin 0.124622 it did before. A partially-complete prefix cannot seal, so `--last-step 5` writes no such numbers.
