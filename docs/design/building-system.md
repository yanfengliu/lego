# Building system: current assessment and ordered work

Status date: 2026-08-26

This document is the current position of the editor and printed-booklet build system. [`spec.md`](spec.md) owns product, domain, and authority contracts; [`learning-system.md`](learning-system.md) owns run evidence and replay; [`part-model.md`](part-model.md) owns catalog truth; and the [devlog](../devlog/summary.md) owns history.

## Executive status

The offline manual editor is usable today. It stores local projects in IndexedDB, renders and validates the versioned `BrickDocument`, imports and exports the supported LDraw subset, and plays authored build steps.

The studio can ingest an instruction PDF, retain bounded page and text metadata, and fingerprint it, but it does not turn pages into build steps. The reader, action-ledger generator, placement search, and scoring loop remain opt-in local tooling driven through Playwright and scripts; see the [real-build runbook](../runbooks/real-build.md).

Catalog `/26` has 98 definitions: 61 use parametric rendering and 37 use bundled LDraw meshes. Sixteen mesh definitions are render-only promotions that preserve their preceding connector, allowance, and collision truth; twenty-one are fully measured definitions, and 77 definitions retain project-authored physical recipes. `npm run parts:check` covers all 98 definitions and is part of `npm run verify`.

All 29 predecessor mesh-backed definitions retain consolidated historical `/18` native-resolution exterior review under collision model `/3` and validator set `/3`; the separately reviewed `/19` `41682`, `/20` `2877`, `/21` `3040`, `/22` `4519`, `/23` `32064`, `/24` `11212`, and `/25` `33909` tranches are historical too. The current `/26` `78329` tranche separately inspects all eight source/catalog pairs at native resolution and reviews every pair `same`; seven are decoded-RGBA exact and the isometric pair is visually indistinguishable at original size. [`part-model.md`](part-model.md#current-catalog) owns the exact packet, manifest, batch, review hashes, and pixel measurements. No consolidated all-37 `/26` recapture is claimed. The evidence certifies only source-normal surfaces exposed by its named views, not hidden interiors, exact collision, connector fit, grip, stability, insertion access, or trusted placement.

Migration to `/26` now binds each of the 27 reviewed source truths to both its exact part/color roster and an exhaustive source-commit connector projection. The independent Git-archive gate verifies 2,311 current endpoints at `sha256:ff3ec2777e568cdab960bea84bdf54dd6aafb26497c262ebdfa42487312c3f93`; the `/25` source row has 2,301 endpoints at `sha256:0d2515f203b36c14ea14c3ab70c22aa3adcefef088ea0ba03ea3d9d3f2d52d54`, with shared identities unchanged and ten additive `78329` endpoints: five studs and five underside clutches. Across the registry, exhaustive shared-identity comparison retains 22 endpoint deltas, three reachable pairs at unchanged `sha256:7431a242907aa9829ead6a279d0b530fe5f5d00ee31e6ddc1576fe66a8a07add`, and zero reachable-pair deltas. Later, removed, or changed endpoints still refuse, while stable endpoints migrate without converting unrelated draft-invalid transforms into a migration failure.

The retained measured `/13` run is `2026-08-12T10-28-52-560Z-34694c87c62e-26128a38-4d9e-4294-9091-6aae9b3ca367`, an unauthenticated, incomplete legacy diagnostic under artifact-manifest `/3`, run-contract `/2`, browser-output `/2`, result/score `/4`, and replay-closure `/3`. The exact source- and data-attested step-5 shortcut reproduced scores `0.81657223796034` and `0.9367520589707421`, selected the second origin at `sha256:47ae3d353885f5de11b685a4bec4ca1132554a19e1f1e30454281252f7d64c93`, and produced an eight-piece browser candidate through step 5. Step 6 then retained four unseparated origins; the step-7 carry expanded three parents for 2,218 + 2,169 + 3,650 = 8,037 narrowing renders and retained eight immutable lineages before the next 599-render request was refused because 8,037 + 599 exceeds 8,192. No partial frontier was admitted, and step 7 remained blocked. The end-to-end legacy inspector now intentionally rejects this retained tuple because its identification match is `/2` while current semantic replay requires `/3`. The ignored-run regression hard-pins the replay closure, prepared-options and browser-output CAS roles, plus the 14,896-byte diagnostic prefix at `sha256:2edf84fbf1eab57e86cd2670f9bdb5e60a7ac33dbda454f22d9c9a85cbf8b70f`. It applies frozen generation-2 browser predicates and the semantic completion projection to those exact bytes, reproducing five globally valid and buildable prefix rows plus the expected `run-incomplete`, five `visual-evidence-unverified`, and one `official-frame-calibration-missing` refusals. This narrower check grants no current publication authority and does not authenticate the historical score, manifest, PDF/render source, or official target.

The historical pointer binds replay-closure digest `sha256:1c27df8a95c655f7508436489e8e31f486f806c7a5382df76d53e0a80801a66c` and artifact-manifest digest `sha256:4dc5ce021e03ba0bc86667a64a1948f9ad54d2d80f95eb887b559a593c7036aa`. The score file is `sha256:cbdf5b5502448011356b7fdb15f655734e97021853a45f41d64f63fd3f9e042e`; `diagnostic-prefix.json` is `sha256:2edf84fbf1eab57e86cd2670f9bdb5e60a7ac33dbda454f22d9c9a85cbf8b70f`. Run `2026-08-12T09-14-05-246Z-32668097b507-2989d382-2a93-470c-aadb-14d91107a904` remains the immutable artifact-manifest `/2`, score `/3` predecessor. Current publication requires artifact-manifest `/4`, run-contract `/5`, browser-output `/3`, and result/score `/5`; run-contract `/4` is frozen inspection-only. A `downstream-only` closure beginning at browser output is eligible, but metadata-only or input-rejection publication and verification are intentionally refused until a typed digest-bound rejection witness can reproduce the exact refusal. No production run exists under that tuple.

The fixed-workload, accounting-bounded 2026-08-23 control establishes the complete `/15` source workload without executing retained replay source. It reads the pinned `/13` prefix, parent witnesses, prepared panel and PDF only as data. Its first phase reconstructs and closes all four ordered `/13` parents to their reviewed source hashes through a narrowly frozen truth-only compatibility projection: the exact reviewed `/13` truth digests and catalog ordinal basis plus the 85-part source allowlist are checked, only truth is transiently projected to `/15` for operation admission, `/13` truth is restored, newly added part IDs are refused, and every retained structural parent hash is verified. Only after all four source snapshots have closed does a second phase migrate those four immutable snapshots from `/13` to `/15`: exactly `25269` and `28802` become available, no catalog interpretation changes, blockers or part mutations occur, and current parent candidate IDs are derived for `/15` expansion while the historical source IDs remain retained coordinates. The active reviewed-additive successor and production migration contract now target `/26`, where `25269`, `28802`, `35787`, `11253`, `15254`, `41682`, `2877`, `3040`, `4519`, `32064`, `11212`, `33909`, and `78329` are thirteen additive definitions. A fresh parent-only headless Playwright reconstruction closed the four immutable `/13` source parents, preserved every part, and rebound the production current structural hashes to `[349f031229e98f9869f218849b9a7a84dd337cd74edc534639fb5753530cf6f7, aafbd400b179b22aa052b528338489e44ef3ba2d05dbe15d7e514648d64d1006, b67a24885463d1613f513112dbad1e9c080df09cb57177c7f4f9598098ab8046, 191b67add0491ab084e4216bef1cdf607f6d8b3eb1da25791c5a4b96ac1c624f]`; the synthetic current pins are `[b8f3baf0843074feec51fdfbbf47181c683d0629472ae8e552b6f8607c627391, cfd428b3800064c2358db2166c6051ef1f831f53df00bca6da644e11ebb3d79f, c683e42d275e43531d93bc1e7ff371c98cc8fbe3824c08f20ef3d6388b7bf183, a7c533c60deebe0f658b209cd58514c4f05456c5f9fb0d6c2b2ecc39aa71f7d7]`. The historical `/26` catalog-session source attestation covered 3,607 files at `sha256:e0080ad0e0e93d8e132f12dfff6f17d1807c462b90ae8697c3244666c6b3751a`; the current executable-source successor covers 3,660 files at `sha256:fd74cde3eb9e5f809ae24c36253ba99720204f0b6b4221874957c949fae1779e`. This is only a focused `/13`-to-`/26` compatibility rebind: the retained long browser denominator remains historical `/15` evidence, and no `/16` through `/26` 14,172-row depth or score rerun is claimed. The exported mechanical helper and any caller-supplied pin array remain `caller-supplied-unverified`; only the production wrapper binds the fixed module-owned source/current hash pairs and concrete production dependencies, then records its frozen result in a private captured-intrinsic `WeakSet` as `reviewed-private-pins`, so a forged public property is not authority. Retained structural hashes do not bind historical revision strings or canonical wire bytes. In fixed source-parent order, the historical retained step-7 narrowing completes 2,218 + 2,169 + 8,271 + 1,514 = 14,172 narrowing-render rows across 30 atomic batches and constructs 17 immutable parent-bound diagnostic leaf records under a diagnostic-only 32,768 ceiling; the renderer reports 14,188 total calls because each parent has four additional calls outside that narrowing ledger. The actual diagnostic ledger accepts that complete narrowing workload; a separate shadow production ledger reproduces the historical boundary at 8,037 reserved plus the next atomic request of 599 against the unchanged 8,192 limit. Node independently re-enumerates the exact offers, applies the score-margin policy to retained render rows, and verifies the ordered DAG and 17 leaves; it does not rerender masks or scores from pixels. The retained run admits no production frontier, publishes no document, freezes the complete supplied browser input and proves the host, browser-before and browser-after JSON SHA-256 identical, and proves the isolated blank runner's storage begins and ends unchanged.

The retained historical `/15` whole-scene run is `output/gate3-step7-diagnostic/runs/2026-08-23T19-35-22.906Z-8e14c4606400`: semantic trace digest `sha256:8e14c4606400b07eb4d1cbc145cb06f9dee1475cc907568870ac0e2c8ad9ca41`, exact trace file 10,902,807 bytes at `sha256:9c6f7a8fa5ce8bbc79393e9a7b530da48ef55d7ffb8516a5dc79964401f21b4b`, summary 25,295 bytes at `sha256:0ef2de83976096d6991a4a66c6026a6c5309242148e93008e5cfbcca3eaf857f`, step-7 panel 150,099 bytes at `sha256:852afab099a21d2776e9a1d69ea7c34d7a40509c46f7014c9b9de70db6d18dcf`, and artifact manifest 630 bytes at `sha256:9ede00208fd978e5542822beb650b8a422e3bded2333145fd843caba14da815d`. Independent read-back recomputed every retained byte, manifest and semantic trace digest; matched all 3,651 bootstrap-locked files and 158,569,077 bytes to the then-current 2026-08-23 checkout at `sha256:179f25b78776e6f5a0d9b6008a4eca5108145f60ea05a2bc8ce91c265b68e610`; and matched the duplicate-preserving 179 executable request and response URL multisets at source-execution digest `sha256:acabbf14d921bedb87bcc41aa384ba6c0923b442068962b75c4cac014f484409` and served-JavaScript digest `sha256:5b51f53c8d87e1e8a97bdd328f6fa409f8330c79d16f14b391f490bcbee9c90e`. Full-page, retained-panel, inventory-detail and assembly-detail inspection against booklet page 13 found the same four 1x parts, orientation cue, highlighted blue/green cross, black wings and surrounding geometry without clipping or substitution.

Request, response, request-failure, redirect, page, dedicated-worker and service-worker observation remains attached through complete context closure. Exactly the post-installation-authorized owned blob probe and fixed external CSP probe may bypass routing, each only as one `GET`/`script` request with `requestfailed="csp"`, no response and no redirect; missing, early, duplicate, malformed or additional unrouted requests are fatal. The data-URL probe is violation-only. The retained run closed 183 unique request observations as 181 routed requests plus those two exact failures, retained zero pending routes, pages or workers, and matched all 179 executable route events to all 179 captured responses. A separate focused delayed pagehide/ACK control proves a close-time same-origin fetch body is consumed before closure and appears in both manifests; the full diagnostic intentionally has no close-time control URL. Retained executable-response evidence contains URL, status, type, byte count and SHA-256 rather than bodies, so it is evidence of observed served bytes rather than an executable replay bundle. Browser PDF fetching and executable-response capture still materialize exact bodies before enforcing their respective browser-side byte ceilings; host stat/digest binding and the closed fixed source make this workload safe enough to measure, but do not establish a hostile-input memory bound. Publication uses no-delete exclusive writes, file fsync, owner marker and device/inode checks, same-parent rename and bounded sequential read-back. On Windows this is cooperative detection, not a directory-fsync seal, crash-durability guarantee, simultaneous-at-return proof or malicious same-user race defense; injected post-open, write or fsync failures deliberately retain any created partial or zero-byte pathname.

The retained whole-scene trace remains the semantic denominator, not authority to raise or otherwise change the fixed 8,192 budget, admit a partial frontier, or replay unauthenticated code. Its exact retained `/15` lineage analysis finds zero whole-scene cache hits and 516 parent-ordinal candidate coordinates whose masks depend on prefix context, bound by `sha256:b06d73e587e6180d916c1accda24ca0270d776bc4326d6c44b00bc34b6c2fdc9` and per-parent vector `[65,64,387,0]`; the superseded migrate-first run measured 518 because revision-seeded part IDs changed scene draw order and equal-depth ownership under `LEQUAL`. The implemented depth route captures one dense prefix surface for each collision-free batch and one sparse isolated-probe surface per parent-scoped cache miss through the same instruction renderer and orthographic camera, composes only strict-nearer 24-bit depth, and falls back to the exact whole-scene silhouette whenever equal stored depth makes opaque ownership order-sensitive. Because the legacy probe mask is keyed by magenta pixels, a batch whose prefix already contains `builtin:magenta` captures no prefix or isolated probes and instead uses one exact whole-scene render per row. A refundable pre-work lease holds each batch's worst-case subject-raster cost before any rendering, charges only the prefix, probe, and required fallback rasters actually used, and releases its slack; the fixed RGB8 depth-pack pass is reported separately and cannot masquerade as a subject render.

The retained historical `/15` final-source depth run is `output/gate3-step7-depth-diagnostic/runs/2026-08-23T22-34-21.749Z-52fd9638354f`. It reproduces all 14,172 logical render rows, 30 ordered batches, 17 leaves, and the baseline render-row, candidate-record, batch, outcome, and logical-reservation digests exactly while charging 6,559 subject rasters: 30 prefix captures, 6,414 probe captures, and 115 exact whole-scene equal-depth fallbacks, 1,633 below 8,192; this measured prefix contains no probe-color collision. Its 6,444 depth-pack passes cover 2,210,292,000 pixels outside that subject-raster ledger; the parent-local exact-render cache records 7,758 hits, 6,414 misses, 1,294 evictions, 1,280 peak entries, 4,805,936 peak fragments, and 38,447,488 peak payload bytes. Every lease proves `committedBefore + maximumRequested <= 8192` and the final committed value is 6,559. The semantic trace digest is `sha256:52fd9638354f10a4b5efd348681d9d053ebcdf65f5fe9ff85065b162d37118c3`; the 10,317,484-byte trace is `sha256:acc1130c5ca1b3b56683aeb7cd95d96db66d96828a86097e2a763212fd79feb0`; the 16,066-byte summary is `sha256:621022fe96b116500fe706450c2b34647c64c9d7ce4251e5d5741127e8a2e7d9`; the 636-byte manifest is `sha256:cc6612bb4955f8aa90eb7a2dd70441d35e45998a414d2055adcb1286a77903d1`; and the visually inspected 150,099-byte step-7 panel remains `sha256:852afab099a21d2776e9a1d69ea7c34d7a40509c46f7014c9b9de70db6d18dcf`. The run closes 3,657 allowed source files at bootstrap manifest `sha256:f72c21f564e94578868dc36ec16561581b74e2176f371986715bdb7b10614355`, 186 request observations as 184 routed requests plus the two exact CSP failures, source-execution manifest `sha256:1ba8a25bba61a980681a22627700886154780e9897b9ae06c215dde142755766`, served-JavaScript manifest `sha256:e794482c79fe25b1fdf283d430951ed4d543fc25eaa6a7ca40cadd2394dffc7a`, identical blank-runner storage before and after, no production frontier, and no document publication. The production farther-step attempt now supplies this physical subject-raster ledger and depth composer under the unchanged 8,192 limit; the legacy whole-scene path remains explicit for callers that do not opt in.

Exploratory read-only analysis considered conservative open-contour zero bounds, same-batch mask reuse, and positive score upper bounds, but no current hash-bound analyzer or retained per-row proof artifact reproduces its pruning counts. Those numbers therefore grant no ledger credit and are not current evidence. Any such route must retain the certified row set, exact bound inputs, skipped-row mask evidence and baseline selection parity, and it must reserve the proved work before a batch begins rather than learning a cheaper charge after observation.

The deterministic post-search target-equivalence audit proves that the retained eight-part candidate is not the official target under any proper upright yaw plus integer-LDU translation. The exact metadata, step, identity-group, and complete catalog-realization comparison leaves one proper fit at yaw 90 degrees and zero translation through steps 1 and 2, then first empties at the step-3 design `6106`. A physical x-reflection maps all eight placement origins, their connector, collision, allowance, and bounds layers, and 30 independently inferred compatible contacts, but none of the eight placements preserves the exact flat render triangle-and-normal topology. Reflection is diagnostic only, never completion. The search result therefore remains a structurally valid diagnostic prefix: canonical `finalParts`, `structuralHash`, and document are zero, null, and null, while `diagnostic-prefix.json` retains eight parts, 30 document connections, `throughStepNumber: 5`, structural hash `sha256:47ae3d353885f5de11b685a4bec4ca1132554a19e1f1e30454281252f7d64c93`, and `targetEquivalence: "unreconciled"`.

A bounded camera-hand primitive represents the as-fitted and x-reflected lattice hands and registers all four quarter turns in each hand by binary silhouette. On the chiral synthetic fixture the unique winner is x-reflected turn 90 at shift `[17,-23]`, IoU 1, while the best proper-hand score is about 0.476; a symmetric 2x4 plate reaches IoU 1 in both hands and returns `camera-handedness-unresolved` while retaining all eight hypotheses. `RealBuildPanelCameraRegistration` validates, copies, and deeply freezes `{latticeHand,latticeDeterminant,registrationPanelStepNumber,turnDegrees,shiftPx}` as panel-local raster evidence, never as physical-transform authority. The resolver detaches, canonicalizes, freezes, and hash-checks the candidate, copies its masks, checks the shared ledger around hash and renderer callbacks, and atomically reserves complete eight-way families before work. The current runner uses it to retain the exact canonical empty-root family in report 1, then refuses before placement because its scorer and deferred/farther schemas do not carry those lineage IDs; later rows are exact zero-elapsed, zero-raster, zero-candidate causal blocks. The `/3` reader exact-binds each report action and evidence digest to its prepared panel, bounds and parses the terminal document once, binds it to accepted hash/part continuity, global validity, ordered step semantics, canonical metadata and manual provenance, requires the exact empty root after this refusal, and lets a zero-piece transition advance only through one independently validated empty canonical `BuildStep` that genuinely changes the structural hash without changing parts. Nested evidence `/2` commits camera, capture, raster, mask-digest, score, centre, shift, hand, determinant, and turn facts but lacks exact mask bytes for independent score recomputation. Fixed-action call sites enter the opaque guard and return `fixed-ledger-frame-unresolved` before their executor because no trusted authority producer exists. These live refusals prevent a false repair; they do not establish RGB, occlusion, or a physical branch from silhouette alone.

The current cutover prerequisites now separate repeatable document identity from branch history. `document:sha256:*` is stable for a structural candidate; `lineage:sha256:*` commits its exact direct parent, global run-root origin, prefix step and decision/evidence identity. The generic lineage-evidence `/1` parser takes bounded UTF-8 JSON bytes, derives score groups and exact-tie refusal, and explicitly reports completion authority absent. Live scalar and multi-parent camera adapters accept only resolver-branded immutable results, require exact same-prefix central parents, keep one camera measurement ID across convergent parents, and derive distinct attempt lineages. Candidate snapshots bind exact canonical `BrickDocument` bytes as well as the structural hash. The normalized farther DAG retains convergence without duplicating ancestry, but its executable origin, carry, transition and panel APIs have no issuer. Prepared-run and prepared-search inputs likewise produce bounded inspection only. The automatic placement helper takes a branded base plus detached witnesses, atomically prepares the exact printed `BuildStep`, compiles placement twice to bind final AI provenance, independently replays and hard-validates the combined transition, and returns no search, lineage or acceptance authority. A standalone atomic compiled-branch batch now composes those inspection prerequisites: it snapshots every detached offer, reserves the whole ordered proposal set before compilation, shares one exact child and physical transition across convergent parents, and retains distinct parent-specific edges whose compiler receipts and child bytes reproduce in Node. Budget or closure failure retains the exact request and no frontier. A separate bytes-only observation sidecar now binds an unresolved compiled frontier to exact MSB-packed mask slices and the score thresholds derived from the same prepared run input, replays optimal silhouette registration, keeps candidate-and-camera ties unresolved, and reproduces a convergent accepted-transition summary without authority. A browser branch-evidence index separately integrity-binds dense run-wide compiled and observation roles. A local semantic inspector parses the prepared run before role transport, rejects internally orphaned source/camera tables, and measures and cumulatively charges exact retained root groups/identities, search parents/proposals, child candidates, transitions, edges, witnesses/operations, legacy and typed observations, source/camera tables, selection/accepted-transition reference arrays, canonical-document bytes and pixel work before document reconstruction. It then reconstructs each bounded root once for admission-work measurement and charges conservative three-pass compiler graph/byte work-policy units including step preparation before compiler replay or mask registration. The established public lineage `/1` parser retains its prior semantic contract; the aggregate replay-work gate is explicit on the local semantic and bytes-only observation paths, and typed observation preflight accepts only the exact replay-admitted work inspection. The inspector exact-binds the indexed step, prepared-run digest, printed-step identity, full compiler metadata, and each ordered proposal identity, catalog part, and color before verifying any typed closure; requires legacy observation-generation, selection, and acceptance fields to remain empty; separately verifies the typed closure and any nonempty committed raw role; and labels retained typed failures as verified inspection rather than reproduced external failure. The work-policy units are not literal CPU or allocation counts. A separate detached browser-output `/4` envelope and reader closes the missing local tuple after first snapshotting a closed data-only non-Proxy input. Its descriptor-only 359-panel source manifest streams exact RGBA and H/P/D/W roles under aggregate limits, rejects source record/array Proxies, and refuses if the captured synchronous raster-derivation primordials drift; camera rows bind exact child bytes, D4 recipes, renderer inputs, matrices, packed masks and replayed registration, permit only exact range-and-descriptor aliases or otherwise disjoint local ranges, and map closure-local offsets into one ordered global role with an explicit base; transition rows independently apply one zero-piece `BuildStep`; and selected multi-root branch edges induce exact `compiled-observation` reports, neutral legacy visual fields and zero replay timing, evidence-derived failures, identity bindings and terminal-document continuity. Every retained report first rejects nonempty prepared coverage failures, unresolved callouts, or missing designs, so missing-role and transition projections cannot erase them. That replay is inspection only: PDF-render execution provenance, provisional identity, physical frame, user placement and completion authority remain absent; completed fixed-ledger and `multi-build-copy` rows are refused; and no live producer enters the runner.

The current prefix boundary keeps source indexing and reconstruction scope distinct. A prepared request may cover steps 1..50 while the source-evidence container still requires all 359 dense panel descriptors. Run-contract `/5` retains the canonical exact-PDF-bound panel-source envelope from frozen `/4` and additionally requires the exact verified source-art-rebound role; replay rebuilds all page/bounds/callout commitments, derives the bounded face sequence, and exact-compares prepared page, face, bounds and boxes. Coverage `/3` keeps all 859 physical source/index rows but nulls identity, confidence, element and catalog fields after the exact requested prefix; frozen `/2` remains immutable replay only. Runtime planning exposes only requested panels for action execution, reports and document mutation; the configured two-panel suffix is passive raster observation, so panel 51 may help score step 50 but its action and pieces cannot be read, expanded, reported or placed. Action-ledger `/4` likewise requires explicit `requestedLastStep`, exact same-prefix coverage and `expectedPrintedSteps=359`, and binds the rebound digest. Current prefix-50 coverage was regenerated, but ledger publication refused before writing on 174 incomplete actions and 11 missing official-frame calibrations, leaving the retained step-25 ledger as stale counterevidence. None of these boundaries supplies PDF parser/render execution, physical-frame, placement, accepted-document or completion authority.

The source-raster foundation now has a separate authority-free diagnostic path. H is high-resolution cleaned art, P isolates at high resolution before downsampling, D downsamples before isolating as a counterfactual, and W is the independently derived work-raster candidate. An exact-byte stage trace retains H/P/D and intermediate masks with opaque non-reproducing source descriptors; source-parity `/4` retains production, W, and XOR bytes for five comparison classes across a dense 359-step opt-in browser path and binds the exact PDF, full prepared-panel manifest, bootstrap/execution/served source closure, runtime, browser result, and atomic publication. The 2026-08-22 real-booklet run completed all 359 rows under PDF digest `sha256:baef0a373164b58d7c982984b52d4e50b10cc59ed28007acb456faa72359bd27`, prepared-panel digest `sha256:976e1a1f1a33837d2b8f284f0942dba56bbd6d1076024de177d583800dae9954`, and measurement digest `sha256:39a810d8a30f28fb93bb619f2ce522ce01e95146e8fe14666d6606270c0cb615`, retaining 3,759,314 capture bytes, 38,593,159 packed-evidence bytes, and 116,474,520 provenance bytes. The browser work-RGBA, candidate policy, and candidate derivation commitments are explicitly not independently reproduced, the source-RGBA diagnostic column is visualization-only, and the result remains calibration-only with authority absent.

The exact-five calibration path derives fixed step/page rows 90/79, 101/87, 346/213, 358/218, and 359/219 from all 359 prepared panels. Its browser constructor and Node reader close exact high/work RGBA, H/P/D stages, independently re-derived W, pairwise P/D/W facts, and ten lossless PNGs. A separate publisher cross-binds that exact capture to the PDF, full 359-row prepared manifest, source snapshot, provenance, served source, checkout, runtime, and transient browser-result bytes; derives an audit-only execution identity; writes the supporting manifests, five roles, ten PNGs, and provenance into a content-addressed run; and atomically replaces the discoverable summary last. The retained publication remains `pending-unreviewed` under `sha256:979157ed12ef36fcd59a85aba13268d56a47d77ee886070cd31221805d756462`. An authority-absent request inspector reparses that publication and accepts only exact publication-derived W equality against the inspected truth packet, then computes a private request identity and deterministic review-presentation digest. Exported challenge and receipt validators establish closed record structure only; a shared detached exchange parser cross-binds those records and one caller-supplied observation without claiming live freshness or authority. The bounded canonical receipt inspector snapshots a caller-supplied trust pin before challenge issuance, verifies the signed receipt fields against the exact locally held challenge state and nonce, checks the expected review presentation plus audience/origin/device/install/release and key identity, applies shared ordered wall-clock samples plus a private monotonic deadline before and after cryptographic work, rejects future or expiry-crossing consumption, recomputes the domain-separated consumption-event hash and next ledger root, and gates event and checkpoint-transition reuse in process. It accepts only the exact locally issued two-minute CSPRNG challenge once, bounds in-flight inspections at 64 and retained successful receipts at 4,096, and returns authority absent. Production admission still always refuses because no released broker or authenticated production pairing/key/checkpoint source can create the module-owned runtime event capability; only a Vitest module mock exercises the positive admission result. Even that result is exact-five-only, with physical transforms, placement, fixed actions and dense 359-step completion explicitly unauthorized. The live browser-output `/4` producer, runner integration, document mutation and completion remain absent.

The atomic batch now accepts multiple exact root-document groups under the same bounded preparation, rejects repeated lineage IDs and duplicate exact canonical-byte payload groups while permitting structurally equal documents with different canonical bytes, and gives every root and descendant an `exactLineageId` that commits its structural lineage ID, exact parent, canonical-byte digest and UTF-8 length. It resolves every proposal against its own retained parent snapshot, proves the worst bounded zero-frontier terminal envelope fits before reservation, and preserves the existing one-reservation/no-partial-frontier contract across the complete multi-root request. Separate bounded root, unique-child and final-wire ceilings still permit a success envelope to fail late, but only into the pre-admitted terminal evidence. A separate deterministic role finalizer derives a terminal-respecting strictly increasing placement-step sequence from module-produced batches, rejects row and observation Proxies, preflights compiled and observation role totals plus cumulative lineage, closure, raster and compiler work before raw observation-role copying, verifies digest-stable optional observation closures, self-parses the finished dense roles, and exposes only fresh copies. The finalizer cannot classify skipped panels because it receives no prepared run; the complete `/4` reader later requires each gap to be an exact prepared transition. The writer remains transport-only and is not connected to the live runner.

## Capability matrix

| Area | Current state | Important limit |
| --- | --- | --- |
| Manual editor | Implemented | Selection and authoring remain primarily single-part; grouping, submodel authoring, richer step editing, and user-facing articulation are incomplete. |
| Projects | Implemented | Projects and autosaves are browser-local IndexedDB state; there is no production sync service. |
| Catalog and palette | Implemented, incomplete set coverage | Catalog `/26` has 98 definitions and includes source-measured `25269`, distinct `28802`, `35787`, `11253`, `15254`, `41682`, `2877`, `3040`, `4519`, `32064`, `11212`, `33909`, and `78329`. The current catalog intersects 20 of the booklet ledger's 121 required leaf identities at `sha256:4e5df0a3aa5723b5aa5abef96e705562913c560a8a7452a2cf29582c2f05ba8f`. The new `78329` row authenticates catalog geometry and connectors rather than a printed crop or placement; its seven retained locators at steps 265, 268, 271, 274, 277, 282, and 336 are stale and self-contradictory, the stale `33909` step-76 and `32064` step-39/101 locators remain counterevidence, authentic manifest-v6 `/5` inputs remain absent, step 26 still exposes an unresolved Builder record that names `10201`, and design family `3245` remains unresolved. Catalog admission therefore does not advance ledger truth beyond step 25 or establish a verified frontier. |
| Connectors | Port compatibility implemented; wire semantics partial | Ten port kinds and six compatibility rules exist, while serialized edges and attach programs still carry only `stud-tube` and infer the exact port pair from references. |
| Rendering | Implemented | Thirty-seven definitions use bundled source-derived mesh surfaces with pinned transforms and normals. Their 37 root closures resolve to 211 unique source files by 30 authors at geometry-roster digest `sha256:c182bd6a4b4ec4f189dfc5846d7982bb4ef3d00f2ac04d250a63f7080354d9ad`; 209 files declare CC BY 4.0 and two use the selected CC BY 4.0 branch of a dual-license expression. Large models are not instanced, and exterior render equality does not prove hidden interiors or physical collision. |
| Part visual admission | Harness implemented; current `/26` `78329` tranche reviewed | The 29 predecessor definitions retain 232 reviewed historical `/18` pairs, while `41682`, `2877`, `3040`, `4519`, `32064`, `11212`, and `33909` retain their separate historical `/19` through `/25` eight-pair tranches. Current packet `sha256:3dcce6065d94e243c65485c0b29effba307ae2e9e508ef3c49fa7e8d6a059531`, capture batch `sha256:0305f056cff14aa658d801f73a5cd852aff851c13378dc19ab7f08962f1c26f6`, native-pair manifest `sha256:774dd19d354961b1da5907a8b31dea01d97160bf152f6152184acc8699bf397b`, review `sha256:f16c939d3f333b5b581532010093a98f0d368d5964aeeb59f77a94358e8d482b`, and review batch `sha256:0031abc4205d40e9c2d2e1543674bc7a3bd12916c32ef5c75200db0c34fdface` bind the eight current `/26` `78329` pairs. Seven are decoded-RGBA exact; the isometric pair is visually indistinguishable at original size, and all eight were inspected and reviewed `same`. No all-37 `/26` recapture is claimed. Hidden interiors still need an interior, cutaway, or other independent witness. |
| Physical geometry | Conservative and deterministic | Render-only promotions deliberately preserve their preceding collision recipes. The measured `25269` uses 26 collision boxes and one LDCad-authored central clutch. The measured `28802` uses 23 collision boxes, two LDCad-authored clutches, and six outward stud frames; its four side studs remain unusable under the upright-only transform policy. The measured `35787` uses 66 collision boxes and three exclusively LDCad-authored clutch cells. The non-lattice `11253` accessory uses 78 body boxes, one exact source-radius stud cylinder, and one LDCad-authored clutch; its source-bound stud seats on a strict local deck face because the rollers extend below it. The measured `15254` arch uses 167 body boxes, six source-radius stud cylinders, six LDraw-authored top studs, and two end clutches authored by its exact framed Builder revision-J record. The measured `41682` bracket uses 54 body boxes, two source-radius side-stud cylinders, two LDCad-authored z-negative studs, and four LDCad-authored underside clutches; the side studs remain unusable under the upright-only transform policy. The measured `2877` grille brick uses 26 body boxes, two source-radius stud cylinders, two LDraw-authored top studs, and two clutches authored by its exact framed Builder revision-E record. The measured `3040` slope uses 67 body boxes, one source-radius stud cylinder, one LDraw-authored top stud, and two clutches authored by its exact framed Builder revision-F record; its 11-file, 13,050-byte LDraw closure emits 178 triangles stored as 184 vertices. The measured `4519` axle uses 41 collision boxes over its exact 176-triangle surface and three discrete LDCad-authored axle seats at x `[-20,0,20]`; that discrete catalog projection does not authorize continuous sliding or collision relief through an axle bore. The measured `32064` Technic brick follows moved root `32064` to `32064a`, emits 458 triangles, and uses 23 body boxes, two source-radius stud cylinders, two top studs, two underside clutches, and one female axle-hole endpoint at `[0,-2,0]` with normal `+X` from the exact capless, sliding, `YOnly` A6 × 1 LDCad declaration. The measured `11212` plate emits 844 triangles and uses 129 body boxes, nine exact source-radius stud cylinders, nine studs, and nine underside clutches from its exact LDCad route. The measured `33909` plate emits 220 triangles and uses 41 body boxes, two exact source-radius stud cylinders, two studs along one edge, and four underside clutches from its exact LDCad route. The measured `78329` plate emits 460 triangles and uses 39 body boxes, five exact source-radius stud cylinders, five studs, and five underside clutches from its exact LDCad route. The `11253`, `3040`, `11212`, `33909`, and `78329` source studs keep their exact measured radii for ordinary collision and use a nominal 6 LDU profile only on exact validated stud/tube edges; end-to-end gates prove each admitted connection clears only its authorized pair while unconnected, misaligned, and third-body overlaps retain conservative collision. Source probes establish clear paths and recessed center stops where reported, not grip strength, full-footprint backing, minifig fit, or stability. Builder record presence or contradiction alone grants neither part connector authority. Conservative recipes remain unsuitable as claims of exact hollow-interior solids. |
| LDraw interchange | Implemented for a bounded subset | Unsupported parts, transforms, references, and semantics must be refused or preserved explicitly; a text round trip does not prove acceptance by another consumer. |
| Booklet PDF in the studio | Ingestion only | Page-to-build-step interpretation is explicitly unimplemented in the UI. |
| Reader and action ledger | Explicit bounded-prefix compiler and retained-source replay implemented; retained input currently stale and invalid | The compiler preserves the full 359-step source/index contract, requires a caller-supplied prefix and same-prefix identity coverage, emits both bounds, and never assembles above the request. Current replay synchronously reconstructs all 359 panel/callout commitments from a PDF-bound canonical retained source role, exact-compares prepared prefix and passive geometry, and validates ledger panel evidence against that reconstruction. The retained source and manifest are still derived data with source-execution authority absent. The retained ledger artifact predates the prefix provenance and contains only rows 1 through 25 against old 359-step coverage; current consumers fail closed rather than treating it as truth. |
| Transition labels | Experimental, raster-blind | The current classifier does not inspect pixels, so its label is not visual proof of a change. |
| Placement and panel scoring | Experimental, currently fail-closed at step 1 | Browser-output `/3` retains the exact eight-way canonical root but refuses before placement because scoring and deferred/farther state do not carry lineage IDs; the frozen legacy path measured bounded N/N+1/conditional-K behavior but is inspection-only. |
| Atomic compiled branch inspection | Implemented outside the runner | Exact detached offers across bounded distinct root-document groups, one aggregate reservation, parent-specific compiler inputs, canonical child bytes, convergent physical transitions and lineage edges replay in Node; no observation score, selection, acceptance, document mutation or browser-output authority is issued. |
| Compiled observation closure | Implemented outside the runner | Exact mask bytes, deterministic registration, prepared thresholds, camera-group selection and selected transition projection replay in Node; browser provenance, report continuity, completion authority and document mutation remain absent. |
| Browser-output `/4` evidence and replay | Implemented as an authority-absent standalone reader; no live producer | A one-time closed outer snapshot binds prepared, branch, source, camera and transition roles; streamed 359-panel source evidence and D4 child-render evidence cross-bind through closure-local ranges and ordered global mask-role bases; selected placement and zero-piece transition rows deterministically induce exact replay-only reports, evidence-derived failures, identity bindings and terminal bytes. Source execution, physical frame, placement and completion authority remain absent, completed fixed-action rows refuse, and public browser output remains `/3`. |
| Observation-source parity | Stage trace and source-parity `/4` diagnostic implemented; real 359-step probe retained | Exact H/P/D stages and five dense production/W comparison classes are bounded and source/runtime/PDF/prepared/browser bound under measurement digest `sha256:39a810d8…`; browser commitments remain opaque, source RGBA is visualization-only, authority is absent, and the diagnostic is not browser-output `/4`. |
| Five-panel parity calibration | Source-bound publication, authority-absent request, and receipt verifier implemented; production admission unavailable | Five bounded roles and ten lossless PNGs close high/work RGBA, H/P/D, pairwise facts, and independently re-derived W. The receipt verifier binds one fresh local challenge, exact scope, expected review-presentation digest and a recomputed ledger transition under Ed25519 using caller-supplied trust snapshotted before issuance, but returns authority absent because that supplied trust cannot enter the module-owned production event capability; no released broker or authenticated pairing source exists. Even a future admitted decision is scoped to those five source-parity frames and cannot authorize physical transforms, placement, fixed actions or 359-step completion. |
| Multi-panel model checker | Quarantined contract and adapter | A source-bound N/N+1/conditional-K refusal-only checker passes mocked adversarial tests, but it has no PDF-crop producer, consent preflight, driver consumer, or successful live verdict. |
| Partial-run evidence and target equivalence | Implemented locally, split by schema generation | Current `/3` browser rows retain the camera root, typed refusal, and blocked suffix; the pinned frozen `/2` browser-role check preserves the historical farther evidence and unreconciled diagnostic without admitting it to current publication. |
| Replay | Inspection only | Current artifact `/4` has no production run and permits only eligible downstream browser closure, refusing metadata-only/input-rejection publication and verification; the retained legacy tuple fails the current full inspector at match `/2`, while independently pinned replay-closure, prepared-options, browser-output, and diagnostic-prefix bytes permit frozen `/2` browser inspection plus exact semantic projection of the five-step diagnostic and its seven non-authorizing failures. Executable and production-sealed replay are unbuilt. |
| Backtracking | Library and tests only | `BuildTree` and `runBacktrackingSearch` exist, but the booklet driver does not use them. |
| Physics | Kernel and development session implemented | Rigid components, articulated joints, compound bodies, Rapier integration, and a cart demo exist; simulation, pose controls, inertia, and incremental rebuild are not complete user workflows. |
| Companion and evaluator | Libraries/specification only | There is no production broker, signing service, credential proxy, independent evaluator, or autonomous promotion path. |

## Measured booklet frontier

These are measured facts with explicit limits, not a completed-build claim.

| Measure | Current result | Interpretation |
| --- | --- | --- |
| Printed sequence | 359 of 359 steps read | Sequence coverage is complete for the prepared booklet. |
| Set accounting | 1,464 assembled pieces; 1,465 inventory pieces | The extra `31510` separator is inventory that the instructions never place. |
| Catalog | 98 definitions at `/26` | 61 render parametrically; 16 use source meshes with preserved physical truth; 21 use fully measured mesh definitions; 77 retain project-authored physical recipes. The deterministic required-leaf intersection is 20 of 121 at `sha256:4e5df0a3aa5723b5aa5abef96e705562913c560a8a7452a2cf29582c2f05ba8f`. |
| Current `/26` truth | Overall `sha256:3226590b11882fea03d8a6370d4ca3c6c8201feaddb56882a243a69acba627e9` | Catalog `sha256:f86310b89f3224cff7a8d571de5a26fd36440ab46235abf1cf530e2f65f41b37`, connector `sha256:93f0a5fc899083be25c5364266e7046b397683204e0e0991f106425ec5a99059`, collision `sha256:7e9905d9f988c288eaeddee3d7befb7af79266518612bbba171d9b7f7fb1c463`, transform `sha256:a8694ddcdc39da5afd946a6012ac2588233bebe2eed457e8501cf572661b2956`, and unchanged validator `sha256:fb0676931eb66a0096f393794d0be1297227811a77b986c0a1d05847ee3127d4`; the current connector-authority projection contains 2,311 endpoints at `sha256:ff3ec2777e568cdab960bea84bdf54dd6aafb26497c262ebdfa42487312c3f93`. The migration registry contains 27 reviewed source truths, with 22 exhaustive shared-identity endpoint deltas, three reachable pairs at unchanged `sha256:7431a242907aa9829ead6a279d0b530fe5f5d00ee31e6ddc1576fe66a8a07add`, and zero reachable-pair deltas. |
| Part standard | 98 definitions; `25269`, `28802`, `35787`, `11253`, `15254`, `41682`, `2877`, `3040`, `4519`, `32064`, `11212`, `33909`, and `78329` scorecards have 0 hard failures | Their composites are `0.9793537209`, `0.9869554553824675`, `0.987506819809668`, `0.9150638159547076`, `0.998083894257325`, `0.9762273815018168`, `0.9754544869364302`, `0.9856779517112434`, `0.990325`, `0.9694824982385262`, `0.9925023760839874`, `0.9955832518073061`, and `0.9968390298840539`; source-connector rows with separate exact-source gates remain deliberately unscored, and `parts:check` remains the authoritative whole-catalog gate. |
| Historical `/13`–initial-`/17` visual evidence | 28 parts; 224 of 224 visible pairs reviewed `same` under their original truth | The five historical review tranches remain immutable evidence under their original catalog, collision, and validator coordinates; they are not rebound as current after truth changed. [`part-model.md`](part-model.md#current-catalog) retains their exact hashes and measurements. |
| Historical consolidated `/17` visual admission | 28 parts; 224 of 224 original 640 × 640 pairs reviewed `same` | Capture batch `sha256:edd8aa795b2e5e65d12eb39418463aa9cf1d95f147223221653a9f75abd32f1a`, native-pair manifest `sha256:6292be1e70cc1a927387b285bd64db21aa370537aa25f8fe6d7cb9722309738b`, and review batch `sha256:e53a12ba863a544b1596174bcc225d912117e7639f7105222137e134ad3cf686` remain immutable under `/17`: 210 pairs are RGBA-exact and 14 differ by 119 pixels total at maximum channel delta 34. |
| Historical consolidated `/18` visual admission | 29 parts; 232 of 232 original 640 × 640 pairs reviewed `same` | Capture batch `sha256:2f4102907a3b3b059200bf4aa096779d66b34cf51e5d4823dcdcd419b5e017f3`, native-pair manifest `sha256:97ca40846cdbe4417dc5f98bcf6554516efdca8c90ee37d42f31a36f4d2bd73e`, and review batch `sha256:fcb60f2530d193ef08213701be747e3c9dc805a4f96a985316a22b358cc25ed0` remain immutable under `/18`. Of 232 pairs, 217 are RGBA-exact and 15 differ by 120 pixels total at maximum channel delta 34. Twenty-eight outcomes transferred only after exact old/current image and render-input identity; all eight `15254` pairs were freshly inspected, seven exact and one isometric delta-1 pixel, while every predecessor received an original-size isometric and underside-oblique sweep. This remains historical evidence under its original `/18` truth, not current `/26` truth. |
| Historical `/19` `41682` visual admission | 1 part; 8 of 8 original 640 × 640 pairs reviewed `same` | Packet `sha256:6265de5e960fdb4d9b839ebe1f36a046f6f8b95626965ffbda1a7dc50fcfb7ec`, capture batch `sha256:d7005fc30dd7fa84acadb64012efaa1c8023f6af8720f8bdf0f5e746dd680d18`, native-pair manifest `sha256:b382da75b01ea732d79a303c706dbba30d29fdabc5d9f46906a9bca322871320`, review `sha256:9b5e29a382f5fa408bd94f1e40e9497358c3ec465d4c8e4d6fad6e7802a0373e`, and review batch `sha256:6860016197eb346118fd039df36ec4086c58d9f46cbe36510535c2dc2e4df922` bind the immutable historical tranche. All eight are RGBA-exact and were inspected at original size. No all-30 `/19` recapture is claimed. |
| Historical `/20` `2877` visual admission | 1 part; 8 of 8 original 640 × 640 pairs reviewed `same` | Packet `sha256:d1d13723c8ed7bd9d5e213de6c489a046bb17e25c68e07e5bdae35733e096794`, capture batch `sha256:805efd95b5792c420e44c53cb95d5eb4f124c71888984d1660862176fb34bb63`, native-pair manifest `sha256:1fa91b2fc77086065786645613dc7a880a0773c3d843db531a18707492b69d04`, review `sha256:27c234910da41cfe16301b3ac997d831201b4b9b74ccf8f7f7cb35d43ef9b2c6`, and review batch `sha256:e482d05f9a77a132c4df8916eb89aaf617d0d3b766d7ec0fe532e11ef94aa33e` bind the immutable historical tranche. Seven pairs are RGBA-exact; underside-oblique differs by one delta-1 pixel with foreground IoU 1 and mean absolute RGB delta `8.138020833333334e-7`. All eight were inspected at original size. No all-31 `/20` recapture, independent-frame, hidden-interior, exact-collision, clutch-fit, or stability claim follows. |
| Historical `/20` `2877` editor audit | Search, placement, structure, and seven views verified under `/20` | Searching `2877` and choosing `Place at origin` produced globally valid document `sha256:c692331b132f52a4aaee3934dc88ad7dd99e7478862a1800b432fdb019c7b76f`. The 194,264-byte full-editor screenshot is `sha256:3346e445323a8ae300dd75fc3f308ff64cf1e807a12e35b25421725dcb50bdab`; all seven canonical views were inspected and the browser recorded no console or page errors. |
| Historical `/21` `3040` visual admission | 1 part; 8 of 8 original 640 × 640 pairs reviewed `same` | Packet `sha256:ea184956f0dc3138f861c1c1775d33392255f1a71ff22985f71e6f4b42867846`, capture batch `sha256:46b3900a1fe08c89552dc53ab708e9b3ec9a6ddb6c998ee5f539eaac1d6c99fe`, native-pair manifest `sha256:f39ab61dcb849da1575ac138e0b76f47dfc71d24938f3b36fdf3288c9ff726dd`, review `sha256:c4b45d6c6dac46585eadd9385c2972f4e93df7cccf0a926472536ea6ea533fea`, and review batch `sha256:530b3191de665f2de9df055de64429138149dd1243e719bc7dec927d4e5e2916` bind the immutable post-repair tranche. Seven pairs are RGBA-exact; isometric differs by one delta-1 pixel with foreground IoU 1 and mean absolute RGB delta `8.138020833333334e-7`. All eight were inspected at original size. No all-32 `/21` recapture, independent-frame, hidden-interior, exact-collision, clutch-fit, or stability claim follows. |
| Historical `/21` `3040` editor audit | Search, placement, structure, and seven views verified under `/21` | Searching `3040` and choosing `Place at origin` after the connection-profile repair produced globally valid document `sha256:752fc4da4bf61139d24d14743443a4b8d5fd3cbbe4f21f1986e8d13a8d2f50a0`. The 123,501-byte full-editor screenshot is `sha256:cb9e123e4979cf3728dc880a28e584f4ed0fe28a7333ff0c8d1322e35c5c77f4`; all seven canonical views and the filtered palette were inspected at original resolution, and the retained audit records empty console- and page-error arrays. |
| Historical `/22` `4519` catalog and visual admission | Exact surface, collision field, three discrete axle seats, and 8 of 8 exterior pairs reviewed `same` under `/22` | The four-file, 10,983-byte official closure rooted at `parts/4519.dat` (`sha256:ecec609013e9d7af63c352cb61d990077c005c4c5453e121b3d192914ab55ff0`) supplies 176 triangles at bounds `[-29.5,-6,-6]..[29.5,6,6]` and a 41-box collision field. The pinned direct LDCad shadow route projects one exact capless, centered, sliding `SNAP_CYL` A6 6 × 60 declaration into three discrete axle seats at x `[-20,0,20]` with normals `[-X,+X,+X]`. Seven native-resolution source/catalog pairs are pixel-exact; underside-oblique differs by one delta-1 pixel with foreground IoU 1, and all eight were inspected and reviewed `same`. This remains catalog admission only: it does not establish continuous sliding, grip, stability, axle-through-bore collision allowance, or trusted printed step-45 crop identity, and it advances no action or booklet frontier. Its evidence hashes remain `/22` coordinates rather than current `/26` hashes. |
| Historical `/22` `4519` editor audit | Search, placement, structure, and seven views verified under `/22` | Searching `4519` showed 94 total definitions and three Axles entries; `Place at origin` produced globally valid and hard-valid document `sha256:eaac23045e78e09b3f71bc83ff906d10db5cffb7349c2370d44c55dd55174e9a` under truth `sha256:7f64021239ab6395a3666f1f72908fd420b73065909822bc68e5226785bfa12e`, with rendered bounds `[-1.475,-0.3,-0.3]..[1.475,0.3,0.3]`. The full UI and isometric, front, back, left, right, top, and underside views were inspected at original size and showed the expected geometry and framing. No app-logic errors occurred; the console retained one favicon 404 plus WebGL precision and shadow-map deprecation warnings. This remains an immutable `/22` audit rather than a current `/26` editor claim. |
| Historical `/23` `32064` catalog admission | Exact moved-to surface, conservative collision, and five discrete connectors | Root `parts/32064.dat` (`sha256:b6240d5798083701834cec8f566d7fca05cbc51123fad8500d3125fa68b4c465`) moves to `parts/32064a.dat`; the exact 23-file, 27,103-byte closure at `sha256:85d190886138b76cb9a98f4d3351652d6fea64423bc576a4ef1116a99dfd686e` supplies 458 triangles. The measured declaration uses 23 body boxes, two source-radius stud cylinders, two top studs, two underside clutches, and one female axle hole at `[0,-2,0]` with normal `+X`, projected from the exact capless, sliding, `YOnly` A6 × 1 LDCad route. Composite `0.9694824982385262` has zero hard failures. This does not establish continuous sliding, axle-through-bore collision relief, grip, stability, insertion access, or trusted placement, and a structurally valid axle edge may still remain body-collision-blocked. |
| Historical `/23` `32064` visual admission | 8 of 8 original 640 × 640 exterior pairs reviewed `same` | Packet `sha256:c4728ac4722ccecbb630e8a9a386df09514a1666caa047e27016482e21c785ca`, capture batch `sha256:717249d0a06b0b2f094836316d9b4047bbd371f63eea18536c1a3e481814786e`, native-pair manifest `sha256:c39b9392d4b33a3066588c6d072b795fb9f8ae65162b414b7b511ff163e1d5bc`, review `sha256:8292738d680a785b076b01ed9626899bd1ee6b78c9a2ac1fe574bdcdf2673c09`, and review batch `sha256:24f5d697c01e8c0e65018b0042067de272c4f393dd78510c1abe9b1142cc5565` bind the tranche. Seven pairs are pixel-exact; isometric differs by three delta-1 pixels with foreground IoU 1 and mean absolute RGB delta `2.44140625e-6`. All eight were inspected at original size. No all-34 recapture, hidden-interior, exact-collision, connector-fit, or stability claim follows. |
| Dated local `/23` `32064` dev-server observation | Search, placement, structure, and seven views observed under the run-reported `/23` truth | On 2026-08-24, the local Vite development server showed 95 total definitions and two Technic bricks entries when searching `32064`; selecting the sole result and choosing `Place at origin` produced globally valid document `sha256:1293be3ca50674cf12534ad8a29dc4da67f9a4694ca4f19ccde002b2827c8d26` with no validation issues and rendered bounds `[-0.5,-0.6000000000000001,-1]..[0.5,0.8,1]`. The 140,108-byte full-editor screenshot is `sha256:1dc0ee3dce94bd2bf67796bf6065cfcbf11e2c09996cb5e01a2b43a744390e4f`, and the audit JSON is `sha256:a6702297dda4f61ee379e038e4955ef2cb9504c515745982a83d58b64adf9feb`. The full UI, filtered palette, and seven canonical views were inspected at original resolution; no console or page errors occurred, while Three.js shadow-map deprecation and Chromium readback-stall warnings remained non-fatal. The ignored record binds document, truth, view, and image hashes but not app source, the executed script, browser build, or a production build, so it is not an exact current-source or production-editor receipt. |
| Historical `/24` `11212` catalog admission | Exact regular-plate surface, conservative collision, and eighteen lattice connectors | Root `parts/11212.dat` (`sha256:c527adbbc5db2983cdc9d0b28481d57a248fd0125d8aa00c13aebd7c32b6633f`) expands through an exact ten-file, 11,078-byte closure into 844 triangles over 873 stored vertices. The square geometry is quarter-turn symmetric, with yaw zero canonical. The measured declaration uses 129 body boxes and nine source-radius stud cylinders. The exact LDCad walk consults `p/stud.dat`, `p/stud4.dat`, and `parts/11212.dat`; active `p/stud.dat` metadata supplies nine studs, active root metadata supplies nine underside clutches, and `p/stud4.dat` has only a disabled anti-stud declaration. Ordinary collision keeps radius `6.0001514980873605`; nominal 6 applies only to exact validated edges. Composite `0.9925023760839874` has zero hard failures. Revision-I record metadata from the checksum-pinned native pack reports nine clutches without a reviewed frame and remains counterevidence. |
| Historical `/24` `11212` visual admission | 8 of 8 original 640 × 640 exterior pairs reviewed `same` | Packet `sha256:a82d963c0e29fafdc9a9f76d6b1c8ab7c73f6127fb989e346f0ef449cba9f4fa`, capture batch `sha256:b8db94807b8616c62aee7e63e918ad8a14dac6af6eec314541508b76cedee6df`, native-pair manifest `sha256:fe0c29059f648a47febc555f853820f98540784314d247d7b0bbe90e933d7241`, review `sha256:17e76699de8bbf1ff96a447dd035c868906d3073901dd0f824a7da67fa4ae326`, and review batch `sha256:ca0ced2b265b389eb959cc321cc3cce70ed3ff34f0910e735fca8e086e29893e` bind the tranche. Seven pairs are pixel-exact; isometric differs by one delta-1 pixel with foreground IoU 1 and mean absolute RGB delta `8.138020833333334e-7`. All eight were inspected at original size. No all-35 recapture, hidden-material, exact-collision, grip, stability, or placement claim follows. |
| Dated local `/24` `11212` editor observation | Search, placement, structure, and seven views observed under the run-reported `/24` truth | On 2026-08-24, a headless Playwright pass through the real UI showed 96 total definitions and 31 Plates entries when searching `11212`; selecting the sole 844-triangle preview and choosing `Place at origin` produced globally valid document `sha256:ae554c96f474c9e0b7f2c7de9c4fbd59aed09617e8e01950077aa68d02b6d6b3` with no validation issues and rendered bounds `[-1.5,-0.2,-1.5]..[1.5,0.4,1.5]`. The 206,633-byte full-editor screenshot is `sha256:d867cb32c673873d163406c3612287c3cd93e830f04b625914f382d0c4935b09`, and the 11,874-byte observation is `sha256:511cf592b8f902cce0efb88b937f8af66573086865f2dd0eae88a6d8e803dbdc`. The full UI, filtered palette, and seven canonical views were inspected at original resolution; the straight underside is grid-occluded, so the separately reviewed clean-scene underside-oblique pair carries the tube claim. No console or page errors occurred. The ignored record does not bind app source, executed script, browser build, or a production build and is not an exact current-source or production-editor receipt. |
| Historical `/25` `33909` catalog admission | Exact asymmetric surface, conservative collision, and six lattice connectors | Root `parts/33909.dat` (`sha256:8da6789db82746f179997ed4b917d00d34d03a6486d6aa27c76d17c9b21d8609`) expands through an exact nine-file, 10,203-byte closure into 220 triangles over 242 stored vertices. The measured declaration uses 41 body boxes and two source-radius stud cylinders. The exact LDCad walk consults `p/stud.dat`, `p/stud4.dat`, and `parts/33909.dat`, supplying two studs along one edge and four underside clutches on the regular 2 × 2 lattice. Ordinary collision keeps radius `6.0001514980873605`; nominal 6 applies only to exact validated edges. Composite `0.9955832518073061` over 401,980 sampled points has zero hard failures. Builder revision-E metadata is unframed and remains count-only corroboration. |
| Historical `/25` `33909` visual admission | 8 of 8 original 640 × 640 exterior pairs reviewed `same` | Packet `sha256:bb640b711f5cf7aea4d48ff77676965ab4f78e650a2dd136ea40e18da6bec906`, capture batch `sha256:34fce749a7eaa53d1e43f7782425fc1b7cccb7b1bfa3b051955e88e7f48abc3b`, native-pair manifest `sha256:af2edbcfb887588926bb61e7fc1042c6ac0283aea654e2f7562b8ba2f2979627`, review `sha256:b3afd9c82bd1081cbeb24b5c8cf892a09abfa44e9a3d9883304ad31efbc1050f`, and review batch `sha256:a502a85d9fcc2d04689d5c172e5f7f0ec95c4fe79cf68b555a1d4b0f28a2fc17` bind the tranche. All eight pairs are pixel-exact and were inspected at original size. No all-36 recapture, hidden-material, exact-collision, grip, stability, or placement claim follows. |
| Dated local `/25` `33909` editor observation | Search, placement, structure, and seven views observed under the run-reported `/25` truth | On 2026-08-24, a headless Playwright pass through the real UI showed 97 total definitions and 32 Plates entries when searching `33909`; selecting the sole result and choosing `Place at origin` produced globally valid document `sha256:1694c915d36a1a4110193046949ffb10df41dfa61d60783683d02634c336d065` with no validation issues. The 198,617-byte full-editor screenshot is `sha256:cbd823589a92c7e6fb23f9522d2bae624358e2d79cf4c3b14f310431ecf6b03c`, and the 12,112-byte observation is `sha256:7b673bfca74ecf97cfbea02c01b993356348f30e566c9162e5ce1e0b897b8f12`. The full UI, filtered palette, and seven canonical views were inspected at original resolution; the straight underside is grid-occluded, so only the separate clean-scene underside-oblique pair carries the ring-and-cavity claim. No console or page errors occurred. The ignored record does not bind app source, executed script, browser build, or a production build and is not an exact current-source or production-editor receipt. |
| Current `/26` `78329` catalog admission | Exact regular-plate surface, conservative collision, and ten lattice connectors | Root `parts/78329.dat` (`sha256:79ec75c5092750b0f2022dab9c7561376d8b2b33fc3dea7059081ef273d4f7fc`) expands through an exact nine-file, 8,761-byte closure at `sha256:d203ae681cfa3842e210b894d46e69e555e64e638796d260c3a2cabdb474f283` into 460 triangles over 489 stored vertices. Yaw 90 plus `[0,-4,0]` gives width-first body bounds `[-10,-4,-50]..[10,4,50]` and visual bounds `[-10,-8,-50]..[10,4,50]`. The measured declaration uses 39 body boxes and five source-radius stud cylinders. The exact LDCad walk through `p/stud.dat`, `p/stud3.dat`, and `parts/78329.dat` supplies five studs and five underside clutches on the regular 1 × 5 lattice. Ordinary collision keeps radius `6.0001514980873605`; nominal 6 applies only to exact validated edges. Composite `0.9968390298840539` over 659,766 sampled points has zero hard failures. Builder revision-F metadata is unframed and remains count-only corroboration. |
| Current `/26` `78329` visual admission | 8 of 8 original 640 × 640 exterior pairs reviewed `same` | Packet `sha256:3dcce6065d94e243c65485c0b29effba307ae2e9e508ef3c49fa7e8d6a059531`, capture batch `sha256:0305f056cff14aa658d801f73a5cd852aff851c13378dc19ab7f08962f1c26f6`, native-pair manifest `sha256:774dd19d354961b1da5907a8b31dea01d97160bf152f6152184acc8699bf397b`, review `sha256:f16c939d3f333b5b581532010093a98f0d368d5964aeeb59f77a94358e8d482b`, and review batch `sha256:0031abc4205d40e9c2d2e1543674bc7a3bd12916c32ef5c75200db0c34fdface` bind the tranche. Seven pairs are decoded-RGBA exact; the isometric pair is visually indistinguishable at original size, and all eight were inspected. No all-37 recapture, hidden-material, exact-collision, grip, stability, insertion-access, or placement claim follows. |
| Dated local `/26` `78329` editor observation | Search, placement, structure, and seven views observed under the run-reported `/26` truth | On 2026-08-24, a headless Playwright pass through the real UI showed 98 total definitions and 33 Plates entries when searching `78329`; the sole 460-triangle LDraw-backed preview and `Place at origin` produced globally valid document `sha256:7365cbf1980f43536b40472b1b8857856a635730a49ba9a9501503337488800f` with no validation issues. The 199,383-byte full-editor screenshot is `sha256:be21961042d5eb17668ded5c93d523662aa78e51dea5110bd90da98e7e790a7d`, and the 13,276-byte observation is `sha256:71acb1ce24c0fb3e46939cd18c75127dc0ad507f4493d81b1ed365af2e72a5db`. The full UI, filtered palette, and seven canonical views were inspected at original resolution; the straight underside is grid-occluded, so only the separate clean-scene underside-oblique pair carries the internal-rail, tube, and cavity claim. No console or page errors occurred. The ignored record does not bind app source, executed script, browser build, or a production build and is not an exact current-source or production-editor receipt. |
| Current manifest-v6 source closure | 881 callouts / 1,512 pieces: 859 physical / 1,464 and 22 semantic / 48 | The full-booklet run now binds exact Node/browser PDF bytes and producer-side component-group conservation. Independent exact replay is pinned for steps 18 and 347 only, not every booklet pixel. It corrects printed step 18's quantity-two crop from the adjacent `3069` tile to the distinct `25269` quarter tile. |
| Current deterministic identification and coverage `/3` | 187 physical prefix rows retained without an external model; 162 catalog-resolved and 25 unidentified | The exact 530,702-byte report at `sha256:bd7b03a53fa9ccecb52784271a9e8a96a172a3ce0ea225834b93eabd3630667f` preserves all 859 physical source/index rows and nulls every identity/catalog field on the 672 rows after printed step 50. It measures 41 of 49 action-bearing steps and 255 of 277 pieces catalog-placeable, with covered prefix 27. Six designs remain absent: `3245c`, `2453b`, `49307`, `99563`, `73230`, and `35464`. The stopped legacy `/4` answer file remains quarantined and is not relabelled as current `/5` evidence. |
| Exact source-art rebound | Steps 2, 4, and 16 close one source-art class; steps 108, 119, 134, and 145 remain counterevidence | The exact PDF/manifest compiler scans 859 physical rows and emits 9,786 canonical bytes at `sha256:a58a55e65c19e2771defe02fc9d37e24c00246bbed9dd375d8d0a2f16382897d`. Native full-versus-isolated rendering closes 22 operations per member with no outside-paint interference. Modern and legacy Chromium PDF.js `5.4.149` independently reproduce the same three browser-only diagnostic windows; all six full/isolated PNGs were inspected and show the complete black wedge, outlines, and `1x` label without clipping. Coverage upgrades only the extant step-2 `4160025`/`30503` claim from the sole direct step-4 pair-judged anchor and binds that row to the rebound artifact digest. The raw relation still grants no semantic identity, catalog, placement, or completion authority by itself. |
| Exact legacy-recut diagnostic | 73 positive relations / 113 pieces survive exact crop equivalence; no coverage change | Frozen full-booklet callout `/5` and current `/6` reproduce one ordered 881-row source roster at `sha256:a494a35f15b9038bd36c64846ce5835f2bcaddaec00f4eb8b00e38ee9d95fbe2`: 859 part-art rows, 22 semantic rows, and a first-50 physical denominator of 187 rows / 320 pieces while expected printed steps remain 359. Each compile pass authenticates only the 170 truth-linked first-50 PNGs, not every crop file; the three-pass CLI reports 510 images and 43,833,660 decoded pixels under its fixed 50,331,648-pixel workflow cap. It accepts 25/26 positive relations/pieces by identical PNG bytes and 48/87 by exact decoded prefix plus complete exact-background bottom-row removal. It retains eight transferable negative relations / ten pieces, refuses the changed second identity under negative row n25, refuses positive n38 because 144 retained pixels change at maximum channel delta 81, and preserves two unjudgeable relations / four pieces. The exact 85-row relation commitment is `sha256:bacedddff0083438324e106c45b55e8eea61b866572387d9f147eab011aa927a`; the 102,513-byte ignored artifact is `sha256:1f7a0fdcb2d665bd3fd30b8e5666307a18c884607640e6b229ef2077f3ebbc8c`. No answer `/4`, semantic identity, catalog admission, coverage trust, physical frame, placement or completion authority enters, and no consumer yet promotes these relations into coverage. |
| Exact legacy-recut semantic cut | 70 listed relations / 107 pieces gain semantic element identity; no coverage change | The exact current 187-callout / 320-piece prefix quantity cut is derived from the pinned official Builder sequence while the 881-row / 359-step source roster remains intact. Atomic per-step element-capacity comparison keeps 24/25 exact-PNG and 46/82 strict-recut relations, quarantines three official conflicts / six pieces and the changed n38 relation / two pieces, and assigns no physical Brick UUID. The official cut and complete accepted/quarantine rosters are commitment-pinned; the 42,105-byte artifact is `sha256:e92ef982f9039b7fd94fb2cdca23fa5e56fb34fb6820ef4fa7ee9b999a0a63ea`. Its actual three-reservation publisher discloses 510 crops, 43,833,660 decoded pixels, three model-index calls / 5,709,507 input bytes, and six full XML decodes / 11,419,014 decoded bytes under fixed caps; a fourth protected callback refuses before input reads. The listed rows alone carry semantic identity; authentication, source execution, prepared run, physical frame, assignment, catalog admission, coverage trust/publication, document mutation, placement, accepted-document, and completion authority remain absent. No coverage or action-ledger consumer is integrated in this unit. |
| Exact source-art semantic transfer | 16 additional relations / 40 pieces; safe semantic roster 86 / 147, residual 101 / 173 | The full 881-row / 359-step source index remains authenticated while only its 187-callout / 320-piece first-50 prefix is measured. Exact decoded RGB24 identity plus an ordered strict image closure owns cross-member semantics; page translation, terminal resource alpha-renaming, and nearest-milli numeric normalization are declared and hostile-tested. Every member separately retains nonempty support, internal isolated/full support-RGBA equality, and zero on-support interference; page translation may change raster phase, so cross-member mask/RGBA equality is not claimed. The exact 211,319-byte artifact is `sha256:4be7bd77d386a7a656019affe9c995e77135080a7aa90df19e43a6f2167ab721`; its real two-lifecycle workflow reports 19,750,648 component pixels, 1,846,598 decoded pixels, 76 full pages, 290 isolated renders, 290 controls, two official indexes, and four full XML decodes. It grants semantic identity only to its listed safe rows and supplies no source-execution, physical assignment/frame, catalog, coverage, action, placement, document, replay, acceptance, or completion authority. No production consumer is integrated. |
| Current prefix-50 action-ledger attempt | Publication refused with 185 validation failures; no new ledger bytes written | Correct rebound binding admitted one additional direct identity and reduced the genuine failure count from 186 to 185. The remaining failures are 174 `action-ledger-incomplete` and 11 `official-frame-calibration-missing`. The compiler corroborates the requested cursor through step 50 only as source alignment; it does not prove those actions complete, and the stale retained step-25 ledger remains counterevidence. |
| Retained 10:28 legacy measurement | Steps 1 through 7 | Immutable artifact `/3` + contract `/2` + browser `/2` + result/score `/4` completed five rows and placed eight diagnostic pieces; step 6 refused at the 8,192-render aggregate narrowing ceiling and step 7 was blocked. The exact full-tuple inspector rejects its match `/2`; only independently pinned closure/prepared/browser bytes and frozen browser `/2` semantics are currently rechecked, with no current authority. |
| Current browser-output `/3` boundary | Step 1 refusal; zero pieces | The runner creates the exact canonical empty document, atomically reserves and retains all eight D4 root seeds, then refuses before placement because scoring is not carried through their lineage IDs; terminal bytes reproduce that root exactly, and later requested rows are exact zero-work causal blocks. Genuine validated zero-piece `BuildStep` continuity is covered separately; forged no-ops cannot advance the hash. No current-generation production run has been published. |
| Private step-1/panel-2 compiled-camera diagnostic | Eight roots reach typed observation closure; authority absent | The standalone sidecar exact-binds all eight D4 roots to one empty canonical snapshot, uses one supplied compiler call per unique physical transition plus mandatory independent Node replay, retains every lineage edge and logical camera reservation, and independently replays packed scores, selection, compiled edges and receipts. One unique offer measures 8 edges, 1 supplied call, 1 child, 64 logical branches and 8 renders; two offers measure 16, 2, 2, 128 and 16. One prepared child scene now supplies all eight D4 renders and is disposed before the next child: a non-square 320 × 192 real-WebGL control matches an independent direct path on all eight ordered mask digests, retains eight distinct nonempty silhouettes, and measures scene derive/dispose 8/8 → 1/1 while all eight renders remain; the two-child control pins peak live scenes at one. Factory raster dimensions, frame dimensions and panel metadata are immutable and exact-bound to detached source before work; source drift, configuration mutation, render failure and cleanup failure are gated, and cleanup failure prevents later preparation. Search 7 and camera 63 still refuse their complete families before compilation and rendering respectively. The exact-version-bound current `/26` `80015` → `30565` enumerator is separately pinned at 4 × 100 connected continuations, but the integrated visual control remains synthetic and uses only its first ordered offer. Fitted view, frame target/radius and centre remain unbound to the opaque source descriptor, and the selected edge's serialized camera recipe remains opaque until browser-output `/4`; no current source-authenticated frame, placement or completion claim follows and public `/3` remains unchanged. |
| Fresh step-1/2/3 deferral probe | Four proper-yaw truth branches measured; no winner | Step 1 has no detected highlight regions or stroke and all four enumerated proper-yaw candidates score 0. Each truth branch admits 100 step-2 continuations with exact gauge IoU 1, but the truth ranks 35, 31, absent after proximity, and 24 at panel 2; at panel 3 it ranks 9, 171, 115, and 92. Every branch exceeded the probe's render allowance. This probe covered four proper yaws, not the full eight D4 camera roots, and demonstrates that present later-panel score is not placement authority. |
| Step 4 | Settled in a source-stable diagnostic | Exact underside surfaces let the own-panel scorer settle both additions with joint visual score `0.8578158458`; retained panel and build images were inspected together. |
| Step 5 local separation | `0.002799` margin | The leading own-panel candidates are closer than the registered `0.01` minimum. |
| Step 5 through panel 6 | Agreements `0.600683` and `0.763502` | The best is below the registered `0.85` agreement threshold; retained images confirm that panel 6 occludes the disputed relation. |
| Exact calibrated step-5 K | `0.81657223796034` versus `0.9367520589707421` | Source- and data-attested direct scoring of the exact two origins at panel 7 selected the second by `0.12017982101040214`; this shortcut is not generic carry. |
| Step 6 after origin selection | Four unseparated step-6 origins | The next-step carry expanded three parents and retained eight lineages after 2,218 + 2,169 + 3,650 = 8,037 renders; the next 599-render request refused because it would exceed 8,192, with no partial frontier. |
| Fixed-8,192 depth carry | All four step-6 parents; 17 step-7 leaves | The fresh semantics-preserving run reproduced all 14,172 logical rows and every baseline selection digest with 6,559 charged subject rasters: 30 prefixes, 6,414 parent-local probe misses, and 115 exact whole-scene equal-depth fallbacks. The production farther-step attempt now uses the same refundable pre-work lease and depth route; batches with an existing magenta probe-key collision conservatively use exact whole-scene rows, while depth-pack passes and pixels remain separate accounting. |
| Visual-search diagnostic frontier | Five printed steps; eight pieces | Node independently validates and retains the selected candidate as `diagnosticPrefix`, but the complete deterministic target audit leaves its target equivalence explicitly unreconciled. |
| Official proper-frame-equivalent frontier | Two printed steps | A unique yaw-90, zero-translation mapping fits steps 1 and 2; the proper-frame frontier first empties at design `6106` on step 3. The eight-placement x-reflection preserves 30 inferred compatible contacts and the physical realization layers but not exact flat render topology. |
| Synthetic camera-hand registration | X-reflected turn 90, shift `[17,-23]`, IoU 1; best proper hand about 0.476 | The chiral fixture separates all eight silhouette hypotheses, while a symmetric 2x4 plate ties both hands at IoU 1 and is refused; the live runner consumes only the eight-way empty-root boundary, and no hand gains physical authority. |
| Canonical completion | Unavailable | Current rebound and coverage evidence verify, but prefix-50 action-ledger publication refuses on 185 named identity/action/frame failures before browser work. If that closure becomes publishable, browser-output `/3` still has the independently gated step-1 empty-root refusal. The private lineage-aware sidecar closes only synthetic step-1/panel-2 plumbing, and the historical proper-frame-equivalent frontier remains two printed steps. Current-source placement evidence, physical-frame authority, generic deep backtracking, final visual audit, and the requested step-50 prefix are incomplete. |

## What the current refusal means

Booklet page 11 draws printed step 4 from below and visibly exposes hollow clutch rings, ribs, walls, and cavities. Catalog `/13` now renders those source surfaces instead of the former dark slab while preserving established connector and collision truth. This closes the rendering defect; it does not turn conservative collision into an exact interior model.

Panel N+1 is the minimum later witness for a placement at N, not a guarantee that the placement is observable. Panel 6 is the concrete counterexample for step 5: it hides the disputed relation, while panel 7's underside view reveals it. The calibrated shortcut binds the exact two step-5 origin hashes and atomic piece witnesses, exact prepared steps 5 through 7, exact run options and input digests, and an attested captured source closure before scoring panel 7 directly. Any data or source drift disables the shortcut rather than broadening it into a generic rule.

The retained source panel and both scored candidate renders were inspected together. Their scores select the second origin and advance the visual-search diagnostic to step 5, but that is only a claim about what the registered image comparison prefers. After exact report, binding, canonical-part, metadata, and step-ownership checks pass, finalization's bounded audit evaluates official transforms under the four proper upright yaws plus one global integer-LDU translation; same-step identical identities are exact multisets, and catalog-proved self-symmetry is the only orientation quotient. Every accepted placement must match complete connector, collision, allowance, bounds, and flat render triangle-and-normal realization. Official transforms enter only this post-selection evaluator; feeding them into enumeration or scoring would make the evaluator choose its own answer.

The proper-frame frontier is uniquely yaw 90 degrees with zero translation through step 2 and first empties at the step-3 `6106`. A diagnostic x-reflection maps all eight origins, the connector, collision, allowance, and bounds layers, and 30 independently inferred compatible contacts; exact flat render triangle-and-normal topology still mismatches, so determinant -1 never authorizes completion. This has the same broad handedness shape as the historical Builder basis failure, but it is not evidence that the repaired Builder calibration regressed. The retained official inputs and searched document each survive their own bindings; the unresolved defect is the shared visual/search camera-world hand, which can make reflected placements look coherent to the scorer. Canonical output therefore stays empty while `diagnosticPrefix` preserves the exact structurally valid counterexample without relabeling it as the target.

The registration primitive makes that determinant a first-class visual hypothesis rather than silently assuming the fitter's positive representative. It applies the hand after face correction and after each candidate quarter turn, scores every hand-and-turn pair with its own translation, and refuses an exact cross-hand tie. Its synthetic chiral winner exactly reproduces the reflected target at turn 90 and shift `[17,-23]` with IoU 1, more than half a point above the best proper-hand score of about 0.476; the symmetric-plate fixture proves the refusal path. `RealBuildPanelCameraRegistration` snapshots and deeply freezes that result together with `registrationPanelStepNumber`, but only as a panel-local observation. The resolver gives the document one stable hash-bound `candidateId`, gives each root seed or observation a unique `lineageId` plus explicit parent, requires the observing panel to move strictly forward, and reserves complete eight-way families atomically. The live runner now constructs the exact canonical empty `real-build` document and uses this boundary to reserve and retain its eight unregistered step-0 seeds before reading a placement callback. It then intentionally refuses printed step 1 because placement scoring and deferred/farther records do not carry those lineages. The browser-output `/3` boundary parses bounded terminal bytes once and proves that this refusal leaves the exact root; every later row must name step 1 and retain zero attempts, placements, validation, candidates, renders, cameras and document drift. A transition with no pieces is not treated as a hash-preserving omission: it advances only when the terminal document contains one unique canonical empty `BuildStep` whose report validation exactly reproduces the independently validated target prefix. Nested evidence schema `/2` commits PDF/page/crop/face, exact retained panel and candidate captures, camera, raster dimensions, built/excluded/render-mask digests, score, centre, shift, hand, determinant, and turn; exact mask bytes are not retained, so deterministic score recomputation is unavailable and positive placements cannot use that evidence as completion authority. Because the measurement is binary silhouette and reflection reverses depth, it remains neither an RGB or occlusion comparison nor authority to construct a reflected physical document.

The audit accepts 1..1,464 unique identity/part rows, requires safe-integer upright transforms and known catalog definitions, and fails closed when a realization layer is unsupported. A per-audit cache reuses each catalog definition's proven self-symmetries and each definition-plus-relative-transform comparison, bounding repeated-part work without weakening the complete-realization requirement.

Fixed-ledger omissions and `multi-build-copy` actions cannot safely place their raw official transforms because the runner has no resolved physical search-frame state. Their runner call sites now route through the opaque authority guard before any hash, source lookup, placement, assessment, or executor read. The guard rejects panel-camera evidence, reflected or unresolved decisions, source mismatches, and even a well-shaped caller-supplied proper identity claim with `fixed-ledger-frame-unresolved` because no exported trusted producer can mint its authority. Mixing fixed rows with searched rows under a non-identity proper frame would still create a hybrid document, so the executable refusal remains until an independent physical audit establishes the mapping.

After selecting the step-5 origin, step 6 retains four unseparated candidates. Historical execution expanded three parents for 2,218, 2,169, and 3,650 narrowing-render rows, retained eight immutable lineages at aggregate 8,037, then refused the next 599-row request before work because 8,037 + 599 exceeds the fixed 8,192 limit. The additive-migration denominator completes all four parents at 14,172 logical narrowing rows and 17 leaves; the semantics-preserving depth control reproduces every one of those rows, all selection digests, and the same leaves with 6,559 charged subject rasters under a worst-case pre-work lease. The production farther-step attempt now uses that subject-raster ledger, so step-7 render capacity is no longer the immediate production gate. No partial frontier enters the document, and the optimized diagnostic still grants no target, physical-frame, acceptance, or document authority.

The existing single-panel placement reader cannot repair this gap. It can request same-step anchors that its consumer rejects, parse fields the consumer ignores, and narrow away settled truth; its inputs are not bound into the real-build run. The quarantined multi-panel checker improves transport and evidence contracts, but until a consent-checked producer and deterministic driver consumer exist it cannot certify or mutate a build.

The driver also cannot claim overall completion after placement alone. The deterministic proper-frame audit names reflection as a target-equivalence failure, the live browser boundary retains the exact empty-root camera family but refuses before placement, and every resolver result still reports physical authority unresolved. Fixed-action callers now fail closed, no trusted authority producer exists, and the required final Node-side visual audit remains unimplemented.

The bounded acceleration decision remains the step-1 seam rather than random later catalog growth. One derived Three.js child scene already supplies all eight D4 cameras with ordered digest parity against an independent renderer, measured scene derive/dispose 8/8 → 1/1, and peak live prepared scenes one; all eight WebGL renders still execute. The current `/26` no-model enumerator pins four first-piece placements times 100 connected continuations at `sha256:18647bdea99ba4756aee66466ef6193efc6eda36d1417bd7318a7f5fe4bf0469`. Its executable proper-C4 quotient proves those 400 raw candidates form exactly 100 four-member proper-yaw orbits at roster `sha256:24e68a134cf86c181ede701c2f189d1f2816af4a83510e2a841f270249d5ce72` and quotient `sha256:e9f23510849153d022bb0aafd6dbf5281bbf6c519c7aff8d14bdefd0fd1145b9`; it retains all raw IDs, support data, 100 representatives, and 400 inverse mappings while rejecting reflection, incomplete families, aliases, and witness/provenance drift. The integrated reducer runs exactly twenty deterministic closures of five representatives under cumulative fixed-8,192 search and camera ledgers, compiles 100 unique transitions, executes 800 physical renders for 800 representative/camera scores, and preserves 6,400 logical camera branches. Before inverse expansion or global ranking, a separate current-population verifier reconstructs all 300 omitted raw members and runs 60 closures of five members across eight cameras under its own cumulative fixed-8,192 ledger; every closure preserves the exact existing role-byte and pixel-visit cap, and all 2,400 packed-mask digests must equal the mapped masks already retained by the 800-row reduction. The same-run proof cost executes on every reducer invocation and remains separate from algorithm work: 800 reduced renders plus 2,400 verification renders cover the 3,200 raw baseline, prove score/tie preservation for this execution, and leave the exact global result unresolved at margin zero with no batch-local winner, accepted transition, or document. The preceding 32-render asymmetric same-factory control remains instrument calibration only and grants no arbitrary-backend or physical-frame claim. The genuine exact-three browser run retained integration `sha256:4087bd1e7ce0be01549dfceb72b26b9deaa7eeee8bf32c700aa00c71e57614bc`; each of its eight current-population camera sheets retained all 100 representatives, and a separately configured 20-pixels-per-unit camera-zero control rerendered those same 100 exact ordered documents. All nine sheets were visually inspected as nonempty, bounded, and unclipped; the 32-frame calibration sheet retained exact four-way row parity. A browser-owned data boundary additionally accepts only one bounded JSON string and locally parsed or constructed C4 containers; a Chromium adversarial control rejected object, array, and nested Proxies without invoking any traps. The exact-three source packet maps placement steps `[1,2,3]` to registration panels `[2,3,4]`, requires the complete 359-label index and all four page-11 panels, and independently reproduces digest-bound high/work RGBA and packed masks. Its genuine one-fetch capture retained manifest `sha256:6b09685905bab077c254adf1e8b32ee2eebd7eefac76d04da678ff47a77c3138`, 20,009 manifest bytes, and 8,592,000/2,150,000/738,816 role bytes; all six high/work images were inspected against booklet page 11. Source execution, prepared-run, physical-frame, placement, accepted-document, and completion authority remain absent, so there are still zero current authenticated placements for steps 1–3. The independently verified source-art rebound plus the direct step-4 pair-judged anchor now binds step 2's extant `4160025`/`30503` coverage claim to rebound artifact `sha256:a58a55e65c19e2771defe02fc9d37e24c00246bbed9dd375d8d0a2f16382897d`; it does not create placement authority. The current prefix-50 census remains a separate measurement—covered prefix 27, 41 of 49 action-bearing steps, and 255 of 277 placeable pieces—with 174 incomplete-action and 11 official-frame-calibration failures still preventing ledger publication.

## Ordered work

### Current bounded campaign

1. Reconstruct printed steps sequentially through a fully evidenced step-50 prefix. Each completed step requires current source, identity, placement, replay, structural, and multi-view visual evidence. The source/indexing contract continues to cover all 359 printed labels, but steps 51–359 are deliberately outside this campaign and no reconstruction claim may include them.

### Later product work

1. Build and integrate the separately released broker-owned authenticated one-use user-event consumer for the exact-five request without adding any repository-local mint. The repository-side bounded canonical Ed25519 receipt verifier now exists but deliberately returns authority absent; production still needs authenticated pairing/key/release/checkpoint state, durable atomic consume-and-append, rollback defense, broker-owned review presentation, loopback authentication, and the private runtime-capability bridge. Separately bind source execution provenance plus the provisional source identity that the current detached `/4` tuple leaves absent. Add a full nonempty 359-panel browser-output tuple only after those boundaries can reproduce; neither the source-parity measurement digest nor the exact-five publication audit identity may become a bearer capability. Keep `/3` and frozen `/2` byte semantics unchanged.
2. With bounded multi-root atomic compilation and the deterministic two-role finalizer now complete as standalone authority-absent prerequisites, add a private live `/4` run session that issues a one-use prepared-step capability only after detached preflight, source-module loading, exact PDF digest verification and post-await drift recheck; own all frontier roots, lineages, ledgers and compiler calls inside that session; invoke one globally reserved batch over every exact current frontier parent; and pass only its module-produced results plus verified observation bytes into the finalizer without exposing a public self-certifying issuer. Preserve the exact `/3` eight-root refusal until this whole path and Node reproduction are green.
3. Add result/score `/6`, replay closure `/4`, and artifact-manifest `/5` over independently verified browser-output `/4`. Node must recompile every selected transition, recompute every score and selection, derive report/document continuity, retain only compact evidence summaries outside the external roles, and withhold completion until the whole tuple reproduces.
4. Produce authentic manifest-v6 `/5` identification answers through a reviewed full producer with explicit external-model authorization, regenerate current coverage and an action ledger whose direct rows bind `vision-kept` plus `pair-judged-same` evidence, then rerun the production booklet path through the now-integrated fixed-8,192 depth carry. Do not transplant retained `/3` or `/4` answers into `/5`: the live 1..12 attempt proved that missing authentic answers leave coverage unbound and the run input-rejected before browser execution. Step-7 render capacity is no longer this path's immediate blocker.
5. Add interior or cutaway evidence wherever the completed exterior packet leaves a claimed cavity hidden; keep every unseen claim `not-observable` meanwhile.
6. Build the scoped consent-checking producer for the quarantined checker, prove each crop against its PDF page and bounds, bind deterministic face state and candidate renders, then integrate only refusal or backtracking consequences after preregistered counterexamples pass.
7. Generalize the measured one-intervening-step path into immutable deep backtracking and publish reversal depth; the current farther carry is not that generic search.
8. Implement the trusted physical-frame authority producer and final Node-side visual audit after the search frame is reconciled; fixed-action callers must remain refused until then.
9. Resolve the printed-step-26 Builder-versus-booklet source mismatch without forcing either witness to agree, add the missing independent `41769;G` official-frame calibration exposed at printed step 11, and resolve step-28 design family `3245` without converting its observed c-surface best fit into an identity claim. The exact quarantine comparator measures c at 0.391277 LDU topology-weighted symmetric interior RMS versus b at 1.081863 and a at 1.706129, but its fixed result remains unresolved because no decisive margin was predeclared, the upstream expected/body MD5 contradiction remains open, and the derived model's ten b references lack the converter and M-to-b alias authority that could reconcile them with the primary XML and geometry. `/26` has admitted `28802`, `35787`, `11253`, `15254`, `41682`, `2877`, `3040`, `4519`, `32064`, `11212`, `33909`, and `78329` with independent source and catalog evidence; admissions through `/25` retain bounded visual evidence under their exact historical truths, and `78329` retains a separately reviewed current `/26` tranche. The seven stale `78329` locators at steps 265, 268, 271, 274, 277, 282, and 336, the stale step-76 `33909` locator, `4519`'s printed step-45 crop identity, and `32064`'s stale step-39 pair-judged-same and step-101 unreviewed locators remain untrusted, and none of these catalog facts can repair the absent authentic manifest-v6 `/5` answers or by itself advance the action ledger beyond step 25.
10. Move the proven reader, placement, evidence, and playback path behind a user-facing booklet workflow while keeping manual editing available offline.
11. Build the separately released broker, executable replay boundary, and independent evaluator only after the local loop produces evidence worth sealing.

## Manual-editor work beyond the booklet frontier

The editor still needs multi-selection, grouping and submodel authoring, richer build-step editing, user-facing articulated pose controls, and simulation controls.

A complete 1,465-piece model also needs measured rendering and interaction work. The current renderer creates groups, bodies, studs, and instruction outlines per part; connector indexing, instancing, incremental rebuild, and profiling should be driven by real frame-time and memory measurements.

## Evidence rule

Every frontier change records the exact requested prefix, catalog version, input closure, completed and verified step counts, piece count, refusal code, score or margin, and the images that make the number interpretable. For each deferred step N, retain immutable parent and descendant lineages, exact source and scored-candidate captures, shared budget reservations, and family-only decisions that leave ambiguous descendants unresolved; retain panel N+1 and the first farther panel reached within policy, and record hidden or unavailable claims as `not-observable`. Report visual-search progress, proper-frame target equivalence, and canonical completion as three separate numbers; an unreconciled `diagnosticPrefix` never populates canonical final fields.

Raw runs stay under ignored `output/` and `var/runs/`. Stable conclusions live here, failed approaches and historical hashes live in the devlog, and promoted failures become tests or fixtures.
