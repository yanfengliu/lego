# Devlog Summary

Newest first. One line per session that changed behaviour: what moved, what it cost, what it refused.
History lives here; `docs/design/building-system.md` holds the current measured position.

- 2026-08-07: **The booklet places its first bricks** — printed step 1 of set 6651557 completes, `complete/deferred-lookahead 2/2 placed`, after reading 0 since the repo began. Settled not by its own panel (no highlight — nothing is built yet to outline — and drawn exploded) but by **step 2's panel**, the cumulative-panel design finally wired; an independent probe that never ran the loop picked the same branch and matched the score to 17 significant figures. Getting there cleared six upstream defects, every one a check that could not fail: publication demanded zero step rows from runs that necessarily have them; the sealed replay's module graph escaped through vite's dep pre-bundle, `/@vite/client` and a checkout-absolute sibling path (served requests 22 fulfilled/4 blocked → 110/1); `MAXIMUM_REPLAY_ROLE_COUNT` was 20 against a closure emitting 21, unreachable until a run first succeeded; **`resolveBuilderBoneTransform` read the LXFML Bone through `diag(1,-1,1)`, determinant −1** — every world placement in the 1465-brick model mirrored, surviving eight part admissions because every check compared a part to its own surface, which a mirror leaves exactly as correct as before (interlock census: 64/65 impossible stud∩stud and clutch∩clutch coincidences → **0/0** under `diag(1,-1,-1)`, and a rotation-only fit 1129/1129 vs 443); arrows converted at half scale (`workFactor` 2) repo-wide; and `enumeratePlacements` seeded origins only from free studs, so step 2's placement — three of the wedge's own studs entering the round plate's underside clutches — was structurally unreachable (stud-seeded 226 origins, clutch-seeded 260, **entirely disjoint**). Step 2 then moved `benchmark-disagreement` → `whole-step-score-too-low` 0.2009/0.45 → **0.5729 against a per-panel ceiling of 0.5881**, 97.4% of the maximum any placement can reach there, refused on 46px of 2793 outside the printed contour; the design assumption that "an exploded step need not be read because the next one is not exploded" is false here — steps 1 *and* 2 are exploded, step 3 is the first seated panel, and `arrows kept` 2/2/0 already discriminates them. Owner caught by eye what four passes of XML-reading missed twice: the booklet turns the model over mid-build and prints an icon saying so (39 → **43** icons once `extractPageShapes` stopped leaking fill colour across a PDF `restore`; face fold 7/43 → **43/43** against two blind raters, majority-class baseline 0.8837), and step 2's residual is a *wrong candidate ranked first*, not a short arrow — two outlines drawn over each other show the seven-plate placement matching the contour and sitting wholly inside while the winner's shape simply disagrees. Corrections to my own record, all committed: the mirror prescription I wrote (negate the Bone position's z) scores *worse* than the incumbent and both halves of the basis had to move; `80015;E`'s frame is right and the `.ldr` **export** is the outlier, proved from inside the export by a collision audit finding exactly 2 interpenetrations in 1470 parts, both that brick; `scoreExplodedStep` is not a ghost scorer and wiring it would have done nothing. Rules: looking at the pictures and reading the next panel are now core rules in `local-rules.md`, session-start reading, after both were forgotten repeatedly while written down as design arguments. Lessons 49 → 52 net (3 dropped: one graduated to a gate, two superseded — including the page-chrome rule whose reasoning caused the flip icon to be dropped).
