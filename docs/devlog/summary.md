# Devlog Summary

- 2026-08-09: **Fifty-eight parts draw the shell their LDraw files model, and three defects hid in the half nobody had looked at** — `part-shell.ts` derives the cavity from a part's own footprint (erode by the measured 4 LDU wall, roof it with the 4 LDU ceiling, stand a tube at the centre of every complete 2x2 block of stud cells), so every brick, plate, tile, jumper plate, grille tile, technic brick and the corner plate now has a real underside: `parts:check` **137 → 24**, with `body-is-hollow-where-it-clutches` **56 → 0** and `underside-is-drawn` **73 → 16**. The 16 left are the bodies the rule cannot reach — a wedge's sloped prism, an arc's plan, and the staircases an arch and a curved slope are — and the 8 mesh-first parts are still step 4. **No connector moved**: of the fifteen reviewed Builder design pins, nine had geometry and collision digests move and not one had its connector digest move. The three defects: a tube's collision primitive **cannot be a cylinder**, because `collisions.ts` gives a body cylinder its bounding box and a tube sits between four studs 10√2 LDU away, so every correctly seated stack of two 2-wide parts reported `PART_STUD_BODY_COLLISION` against its own tubes *with the connection declared* — replaced by the largest box inside the tube circle, whose corners meet that circle exactly where the studs are, swept over all 9,801 integer positions the schema can express with zero wrong admissions; the tube's **144 triangles were wound backwards** so `FrontSide` culled every one and the tubes were in the scene, counted by a passing test, and invisible — found by rendering the underside and looking at it; and a shell's **box decomposition is not canonical under rotation**, so the Builder frame self-symmetry proof called every square plate asymmetric until it was changed to compare occupied volume rather than the chopping. Catalog **/9 → /10**, `/9` kept as a reviewed snapshot and its truth hash independently recomputed from HEAD's own pinned inputs. The booklet prefix is unchanged — **5/5 steps, 8 pieces** at `--last-step 5` and **4/4, 6** at 4 — so the honest count stays **3 verified printed steps**: step 4's geometry is now the part, but its chosen transform is still the mirror of the ledger's, which is the standing mirroring gap.
- 2026-08-09: **A clutch is a hole, and the backing policy had been asking whether it was filled** — `faceHoldsStud` asked "is a whole stud's footprint of this face backed by solid?" and both faces used it. Right for a stud, which stands on the body; exactly backwards for a clutch, which needs a cavity, and solid behind it is what makes it impossible. Split without touching the stud side: `cavityHoldsStud` admits a clutch on **clearance** (nothing crosses the cylinder the stud sweeps), **grip** (a wall or tube in that band reaches the stud's own 6 LDU circle) and **seat** (the cavity is roofed, so the stud bottoms out), and a clutch passes if either predicate says yes — a strict widening, proved a no-op by dumping all 85 parts' connectors, primitives and allowances before and after: **byte-identical, catalog hash unmoved**. The grip range is zero because `3020.dat` makes it zero — cavity face at 16, clutch centre at 10, stud radius 6 — so the interference fit is the measurement and not a tolerance. Then `builtin:plate-2x4` got the measured shell: ceiling slab plus four walls, five boxes, `parts:check` **139 → 137** (`underside-is-drawn` 74→73, `body-is-hollow-where-it-clutches` 57→56) with **nothing else in the catalog changing in any field**, all 8 clutches and 8 studs surviving, and `undersideMode` now *derived* from the geometry as `modelled-shell-cavity` rather than declared — fill the cavity back in and it reverts to `semantic-tube-seat-grid` on its own. **The plan's "tube outer radius is not measured" is refuted, and it was an incomplete search rather than a missing file**: `stud4.dat` is absent from the cache but `1-4stud4.dat` is not, and its line 14 places `1-4cylo.dat` scaled **8** against line 13's 6, with `1-4ring3.dat` scaled 2 capping the annulus — so the tube clears the stud by 0.142 LDU at 10√2 = 14.142 against 6+8, and the fit is at the wall. Tubes still left out of the shell, now for a stated reason: `bodyBoxesLdu` takes boxes and an annulus drawn as a box fills its own bore. Looked at the pictures rather than the scores: from below the plate is a **tray** — four wall bottoms as a bright rim, ceiling recessed 4 LDU inside — against `plate-2x3`'s flat face at the same viewpoint. Catalog `/8 → /9`, `/8` recorded as a reviewed historical snapshot at 262d274. **All three booklet baselines moved and none moved because of a placement**: 5/8, 4/6 and 3/4 are unchanged and every transform is the one on record, but `structuralDocumentValue` hashes the pinned truth, so restoring `truth.catalog` and `truth.collisionModel` to their `/8` digests in each retained `document.json` and changing nothing else reproduces `f15eadfa`, `64017a83` and `6d46478c` bit for bit.
- 2026-08-08 (night): **Printed panel 7's camera is right and step 6's agreement went 0.4482 → 0.8837, past the 0.85 bar** — two changes to the lattice ranking and neither works alone. `canonicalPair` is order-free (a peak is a repeat, not an arrow; step 7's grid steps (23,19) and (-33,13) always arrived left-first and were dropped), and `explainsStrongestPeak` now ranks above the explained-peak count, because that count is monotone under refinement and so can never refuse a lattice built on a drawing's sub-pitch ribbing — printed step 4's `a=(14,6) b=(-25,21)` explains 7 peaks against its own grid's 6 with **no site at (36,14)**, the picture's loudest repeat, at 23.75% of pitch against 0.47%. Measured as a 2x2: order-free alone drops step 4 to a refusal at 10.34 pt/stud and accepts step 38 at 7.43; the strongest-peak key alone changes nothing at all; together **35 fitted, 34 at the booklet's own 15.93-16.54 pt/stud** (was 33/33), steps 4 and 7 both fitted, 31/35/38/39/40 still refused, 8 camera runs, harness assertions unchanged. `overlay-007.png` reads `az 55.15 el 35.31, residual 0.123px` with **174 predicted studs on the drawn tube rings** where it used to mark nothing and fold to diagonal streaks. **The lever the last session named — `foldedStudShape`'s radial contrast per candidate — is refuted the other way round from the previous four**: emphatic and right on the synthetic grids (14.9 against 5.6, 20.0 against 3.1) and first on only **11 of 36** booklet panels. So are lexicographic-by-peak-strength (accepts 31@6.7 and 32@5.8) and harmonic-under-strongest-peak (35 of 36 panels, and the synthetic grid's azimuth at 79.04 where it was drawn at 34). **Cost, stated not tuned around: printed 32 flips from refused to accepted at 21.00 pt/stud**; it is drawn nearly edge-on and its callout boxes are joined to the assembly by their pointer lines, so their art enters the grid — cause is upstream of the ranking, and over **1698 rule/pairing combinations** every rule fitting 34 accepts it and every rule refusing it loses step 7. **Ground truth by construction agrees for once instead of refuting**: over 735 synthetic panels (7 shapes × 7 azimuths × 5 elevations × 3 scales) recovery goes **476 → 532** and mis-fits **83 → 53**, with 19 losses of which 17 land on a refusal; four gains are pinned as a test that fails on the reverted module. Step 6 now refuses `ambiguous-deferred-placement`: best two of 36 at **0.8837 against 0.8804**, 0.00328 apart against the 0.02 noise floor. That is a lookahead question, not a camera one. Baselines held: 5→5/8/`f15eadfa`, 4→4/6/`64017a83`, 3→3/4/`6d46478c`.
- 2026-08-08 (evening): **Printed step 6's frontier moved twice and stopped at a camera, and four lattice rankings that look like the fix are all refuted** — step 6 defers, so at `LEGO_REAL_BUILD_LAST_STEP=6` it refuses for want of a panel to defer to; at 7 it refused because **printed step 7's highlight is two open contours, 1338px of stroke enclosing no filled region**, and the deferral could only subtract a *closed* one to stop charging step 6's prefix with drawing step 7's pieces. That refusal was printed step 5's mistake one level up — arithmetically right about a question the panel does not answer — and about half this booklet's contours are open, so a lookahead that can only read a closed one cannot finish the booklet. Fixed the same way: when the settling panel's own new pieces cannot be removed, its built art is a **superset** of what any step-N candidate can draw, so the measure becomes containment (drop the term charging a candidate for ink no candidate could own) and the existing 0.02 margin still has to be cleared. Step 6 then narrowed **1 of 187 x 2 of 534 x 2 of 627 x 10 of 553** = 4187 renders against a 4096 cost ceiling — raised to 8192, which is a cost ceiling and not a correctness bar, exactly as `maxRendersPerPiece` 220→1024 was — and scored 36 whole-step candidates. **It refuses at 0.4482 against the 0.85 bar**, and that number is the diagnosis: the same registration gives 0.9031, 0.8898 and 0.8312 on panels 2, 3 and 4, so less than half of what a 12-piece prefix draws lands on panel 7's art because **panel 7's camera is wrong**, not because the placements are. Two deferral defects fixed on the way, both live the moment a deferral crosses to an underside panel: the lookahead camera dropped `upSign` and never resolved the panel's quarter turn, and now does both by registering the settled prefix the way `anchorStepCamera` does, publishing `lookaheadUpSign`, `lookaheadTurnDegrees`, `lookaheadTurnAnchorIou`, `lookaheadTurnMargin` and `lookaheadMeasure` — and a settled deferral that cannot name its face and turn is now rejected by the output validator. **The lattice ranking is where this stops, and the handed-over one-line fix is not one.** `canonicalPair` reading argument order as information is real and does cost printed step 7 its grid; making it order-free recovers that grid at 0.21% of pitch and simultaneously admits printed step 4's `a=(14,6) b=(-25,21)`, a lattice on the fifth-of-a-pitch repeat that panel draws, which counts **7 of 16 peaks against the panel's own 6** at 23.75% of pitch against 0.47% — so step 4 goes from fitted to refused. Printed step 4's own grid is second on every key that separates it from that: beaten on harmonic response, the module's own whole-picture measure, by its own **index-4 superlattice at exactly twice its pitch** (0.1622 against 0.1401). Printability first fixes both and **destroys the gate** — all 40 sampled panels then "fit", including ones reporting 10.6, 21.0 and 77.2 points per stud against the booklet's 16.1. And `coherence` first, the strength of the two repeats a pair is built from, scores **17 of 17** on the sampled printed panels, keeps step 4, recovers step 7, lifts the fitted count to 34/40 — and puts a **synthetically drawn grid's azimuth at 79.0 where the test drew it at 34.0**, failing 11 of the module's 20 tests. Ground truth by construction beat agreement with neighbours, so all of it was reverted and the numbers written into the code. One correctness fix survived and is measured: `countExplained` no longer counts a peak "explained" by the lattice's **own origin**, which a coarse cell collects wholesale because the tolerance is a fraction of its cell — panel 7 had a 2448px-cell candidate claiming the peaks at (-8,3) and (-13,5) **8.5px and 13.9px away** on a picture whose grid step is 40px. Across the 40-panel harness that changes exactly two panels, both stay refused, and panel 7's refusal goes from a misleading 1.42px to 15.19px from axonometric. Baselines bit-identical: 5/8/`sha256:f15eadfa…`, 4/6/`sha256:64017a83…`, 3/4/`sha256:6d46478c…`.
- 2026-08-08: **An open contour is scored against the printed line, and the two hard tests that sound like containment are both false of the right answer** — printed step 5's panel prints 2 contours and neither closes, so `highlight.mask` is empty by construction and the joint gate's union was 0px. The refusal was arithmetically correct about the wrong thing: it measured each piece's **area** against an enclosed region that does not exist, and never consulted the 1429px the booklet drew. Measured against the stroke instead — each piece's **visible boundary**, widened by the run's existing 3px tolerance — the same reuse question has an answer: **347px and 907px** explained exclusively, against a calibrated minimum of 8. Then two candidate hard tests, both shipped or coded and both **refuted by scanning all 374 distinct placements of the Plate 2 x 14 and all 174 of the Plate 2 x 4**. *"Every printed pixel is explained"* ran as a gate for one full run and refused at **1254 of 1429**; the 175 it misses are the outer row of a two-pixel stroke drawn outward of the silhouette by more than the tolerance, the best placement is the **unique maximum** at 907 of its contour's 1081px, and no placement does better — so the residue is the artist's offset and the test measures the drawing. *"No printed pixel lies inside the piece"* is worse: the same best placement has **433** printed pixels inside its own silhouette, because a two-pixel line straddling a boundary is half inside it, and **166 of 374** placements cross nothing at all because they are nowhere near the drawing. A stroke straddles the boundary it marks. So the hard test moved into the search: `rankStepDelta` ranks an open contour by `strokeRecall` alone, dropping the precision term that asks what share of the candidate's boundary the booklet printed — which is the occlusion that opened the contour — and the accepted placement must clear the existing `minimumScoreMargin` or defer to the next panel as step 4 does. It keeps containment's property that a spilling candidate cannot win, because spilled boundary explains no printed pixel; margins measured **0.6347 against 0.4962** and **0.2428 against 0.1854** against a 0.01 bar. Localising the 175px is what made this a two-run investigation instead of a search: `jointVisual` now carries `evidenceKind`, `printedEvidencePixels` and `unexplainedBoundsPx`, and box `[36,353,286,445]` named the 2 x 14's own contour immediately. `contourStrokeMask` is new on the extraction (schema `/2`) because a hard test cannot be stated on `strokeMask`, which keeps the specks the extractor itself rejected. **The prefix moves**: `stepsComplete` **5**, `piecesPlaced` **8**, `finalParts` 8, hash `sha256:f15eadfa…`, step 5 placing `builtin:plate-2x4` in Dark Green at `[60,8,0] upright-yaw-270` and `builtin:plate-2x14` at `[160,8,100] upright-yaw-270`. Both pins are bit-identical after: 4 steps / 6 pieces / `sha256:64017a83…` and 3 steps / 4 pieces / `sha256:6d46478c…`. Looked at every claim: the four keyed components of panel 5 painted apart (the 2 x 14's two long edges, the green plate's two arcs, each broken exactly where the piece goes behind built art), and finally the printed contour keyed straight off panel 5 and drawn over `step-005-build.png` — it traces both placed pieces edge for edge and stops where the booklet stops. Colour mattered nowhere: nothing on the placement road reads `colorId`, and the booklet's first non-grey piece scored like any other.
- 2026-08-08: **The step-4 anchor was not eaten by the highlight; the camera was upside down and a quarter turn out** — the handed-over mechanism was that step 4's closed contour cuts a band out of the art `alreadyBuiltMask` leaves, so `anchorIou` 0.393 and every number measured through it. It is **refuted as the cause**: excluding the highlight from both sides lifts panel 3 from 0.6267 to 0.8898 and panel 4 only to 0.4821, and the overlap does not order the anchors — panel 3 loses **67.3%** of its art to its own highlight and registers higher than panel 4, which loses **53.4%**. Looking is what found the real cause. `step-4-zoom.png` shows hollow underside tubes where `step-3-zoom.png` shows raised studs, so `panelFace: "underside"` is right and the camera built from it is not: turning a model over is a half-turn about a horizontal axis, which carries `+Y` to `-Y`, and `viewForPanelFace` negated azimuth and elevation and left the image upright. That half-turn is **provably outside the family the fitted view sweeps** — with up pinned to `+Y` a model's own up axis projects above the view axis at every azimuth and every elevation short of straight down — which is why the handed-over sweep of all sixteen quarter-turn and sign variants was flat at 0.400-0.444 and could not have found it. Three defects, all in the registration: `upSign` on `OrthographicViewParameters` and `panelProjectionFromFit` carries the half-turn; `anchorStepCamera` now resolves the **quarter turn**, which a projected square lattice provably cannot pin (`a(A+90) = b(A)`, `b(A+90) = -a(A)`) and which every rotate-the-model icon reopens; and it registers outside the panel's own highlight, without which the plain IoU puts the **wrong** turn first on panel 3, 0.6435 against 0.6267. Re-measured per panel, anchor and chosen turn: **2 → 0.903118 turn 0** (unchanged), **3 → 0.889836 turn 0** (was 0.626687), **4 → 0.831192 turn 90** (was 0.393292). Looked at `next-step-4-outline.png` — the prefix silhouette traced over the printed panel scallop for scallop, excluding both highlighted new pieces — and at `next-step-5-outline.png`, where the same prefix outlines exactly the half-disc panel 5 draws as already built and neither of the two pieces panel 5 places. **The prefix moves for the first time in two days**: `stepsComplete` **4**, `piecesPlaced` **6**, `finalParts` 6, hash `sha256:64017a83…`, step 4 placing `builtin:plate-1x8` at `[0,8,30] upright-yaw-270` and `builtin:wedge-plate-4x4-cut-corner` at `[-20,8,80] upright-yaw-180`. The three-step baseline is bit-identical — 3 steps, 4 pieces, hash `sha256:6d46478c…`, step 1's deferral still 400 candidates at 0.903118 and margin 0.124622 — and step 3's placement is unchanged despite its anchor shift moving 19px. One resource bound had to move with it and it is not a correctness bar: both per-piece render budgets were 220 against step 4's 240 and 334 enumerations, so both strategies refused with `resource-budget-exhausted` before rendering anything; 1024 is about twenty seconds a piece at the run's measured 21ms, and exceeding it still refuses rather than truncating. Step 5 now refuses on its own defect — 2 **open** contours, no filled region, so `highlight-reuse-unexplained` with union 0px — which is the blocker the previous session already named and is untouched by any of this.
- 2026-08-08: **The deferral learns the second way a panel fails to answer, and printed step 5 turns out unable to settle step 4 for a reason that is its own contour** — the one-step lookahead used to fire only when a panel printed no highlight at all; printed step 4 has an 816px closed highlight that simply cannot discriminate, and `ownPanelCannotSeparate` now sends that case to the next panel too. The predicate admits `ambiguous-placement-score` and `tied-placement-score` and refuses `zero-placement-score`, `no-placement-candidate`, `incomplete-placement-scoring` and `resource-budget-exhausted` — each of those says the step was not looked at properly rather than that the drawing does not distinguish two seats — and it demands two finite scores as well as the code, because the same code also reports non-finite evidence. It is asked of the *pruned* decision, not the benchmark, whose `benchmark-disagreement` at step 4 is about the exhaustive half needing 240 renders against a 220 budget and says nothing about the panel. **The handed-over explanation of the ambiguity is refuted by its own numbers**: the two tied placements are `[60,-8,-50] upright-yaw-270` at **0.2734538947219428** and `[80,0,-50] upright-yaw-270` at **0.2723736926391197**, margin **0.0010802020828231118** — they differ by `[+20,+8,0]`, one stud across the plate and one plate-height down, *not* a slide along the plate's own axis, and the wedge is not ambiguous at all (`[60,8,-20] upright-yaw-0`, 0.36635, identical on both branches). **A step that has a panel has to narrow with it**: carrying step 4's product forward the way step 1's is carried gives `240 x 334 = 80,160` candidates against a 512 budget and refused there, so `placementsOwnPanelCannotSeparate` reads `selectUniquePlacementScore`'s own rule down the ranking instead of across the top two — 240 of 240 become **2** on the plate and 334 become **1** on the wedge, product 2, with no new number introduced. **Then panel 5 refused, and looking is what said why.** It prints 2 **open** contours — 1429px of stroke, `closedContourRate` 0, no filled region — so `alreadyBuiltMask` removes an outline and nothing else and the long plate and green 2 x 4 that step 5 *places* stay inside the art step 4 must explain: built art 21,442px, of which the settled three-step prefix alone leaves **11,447** unexplained and either candidate **8,556**, giving agreements 0.5291 and 0.5250 against the 0.85 bar. The first cut reported that as `weak-deferred-agreement`, which asserts the winner is the least wrong of the wrong ones; that is not established, so the deferral now checks the panel before enumerating and refuses by naming the contour as the ceiling. Two open findings measured on the way: panel 4's `anchorIou` is **0.393** against 0.903 and 0.627 at steps 2 and 3, and rendering the prefix at all **sixteen** quarter-turn and sign variants of the fitted view is flat at **0.400–0.444**, so no camera choice rescues it — the step's own closed highlight cuts a diagonal band out of the very art the registration has to use. Every step-4 score, the 0.0011 included, is measured through that. Prefix unchanged and re-measured after the change: `stepsComplete` **3**, `piecesPlaced` **4**, hash `sha256:6d46478c…`, step 1's deferral still 400 candidates at agreement 0.903118, margin 0.124622.
- 2026-08-08: **The step-4 camera refusal was reporting the losing candidate's number** — printed step 4, the booklet's first underside panel, was refused at 9.11px from the closest upright axonometric against a 0.02 tolerance on a 92.19px stud pitch, and the pitch was the tell nobody read: every panel around it measures 40–44px. The panel's own grid was in `fitStudLattice`'s candidate list the whole time, **second**, at 43.83px and **0.474% of pitch** — four times inside the gate that refused the panel — beaten by an index-2 sublattice of itself that explained exactly as many peaks with twice the cell and won the coarsest-cell tie-break at 10.1%. So the reported 9.11px belonged to a candidate that had already lost on the criterion the run cares about, and the handed-over lead — that an underside panel needs a different family of views — is refuted by the same data: no face option could have helped, because the right lattice was already there and the fitter never asked whether the one it preferred was a projection at all. The fix is that same threshold consulted one step earlier, ranked above coarseness and below explained peaks; the gate value is untouched and the winner still passes the identical gate on the refined basis. Over the same forty panels, before and after, **exactly one changes**: step 4 goes refused → azimuth **34.71**, elevation **35.01**, residual **0.475% of pitch**, **16.134 points per stud** against 16.193 and 16.014 either side, fitted panels 32 → **33**, every other panel identical to every digit reported. Its basis is step 3's reflected in x to within half a pixel, which is what an underside panel reads as to a fit that can only report positive elevation — and it does not generalise, since steps 12–15 are studs-up and still sit in the mirror branch. Looked at `overlay-004.png` before and after: ellipses twice the size of the drawn tubes become cells sitting on the tubes, and the autocorrelation inset goes from a smeared diagonal ridge to a grid. **The blocker moved**: at `--last-step 4` step 4 now extracts its 816px closed highlight, scores **220 candidate renders** — the first flipped render this loop has compared against a printed panel — and fails on `ambiguous-placement-score`, the best two placements of `builtin:plate-1x8` separated by **0.0011** against a 0.01 margin, best score 0.273, with the exhaustive half declining separately at 240 renders over an explicit 220 budget. A partial prefix cannot seal, so the number of record is the like-for-like `--last-step 3` run: `stepsComplete` **3**, `piecesPlaced` **4**, `finalParts` 4, structural hash `sha256:6d46478c…` unchanged. Found, reproduced and deliberately unshipped: `canonicalPair` drops every peak pair whose left-pointing member arrives first, which is exactly why step 7 is refused — both orders recover it at **0.2% of pitch** on its neighbours' camera — but the pairs it adds include a lattice at twice step 4's pitch that then wins step 4's ranking, so the two changes have to be measured together rather than one at a time.
- 2026-08-08: **The gate blocking the ledger was a tautology, and the "15 demoted pieces" were never demoted** — the brief was that `answers/3 → answers/4` had made 15 of the 17 pieces in printed steps 1–8 `pair-judged-same` instead of `vision-kept`, so the ledger could not publish and the reconstruction reported 0 steps and 0 pieces. Reproducing the claims underneath the published labels overturned both halves. **14 of the 15 are `vision-kept` underneath**; they read `pair-judged-same` because `booklet-catalog-coverage.mjs:373` deliberately lets a judged verdict outrank the vision label in both directions, which has been true since `0452f75` on 2026-08-05 and has nothing to do with today's schema. Only **3 callouts (2 distinct cards) are genuinely `self-contradicted`**, and both picks are visibly right: card-0050's query is the 80015 quarter-ring and candidate 1 is the same part in the same pose, card-0029's is the 6-long wedge and candidate 1 matches it against a 4-long twin — the model simply miscounted studs by one in the free description it gave in the same breath (`6x6` for `5 x 5 / 4 x 4`, `6x2` for `6 x 3`), so the size clause fired on a correct pick. `card-0050` was already named as self-contradicted in `0452f75`'s own commit message three days ago, which is the proof today's prompt did not cause it. **The real defect was the assertion itself.** `real-build-action-ledger.spec.ts:68` demanded `toBe("vision-kept")`, and it was written at `2080299` — 53 minutes before `0452f75` — when `directPiece` **hard-coded that same string into every piece**, so it asserted a constant against itself and tested nothing. `0452f75` replaced the literal with the claim's own confidence and fixed six sites to read one definition; its message says the brief naming two sites was wrong because there were six. **There were seven.** The spec was the one it missed, and removing the literal silently converted a tautology into a gate demanding one particular trust source — while the tracked contract beside it (`real-build-action-ledger.test.ts:293`) already read `only "vision-kept" and "pair-judged-same" callouts may become a ledger piece`, and the ledger on disk already carried 22 pair-judged pieces. So the line now asserts membership in `TRUSTED_IDENTIFICATION_CONFIDENCES` rather than a string, which is a deleted duplicate and not a widened gate: **17 weak labels stay refused** (`unanswered`, `self-contradicted`, `vision-overruled`, `pair-judged-different`, `handedness-unverified`, `description-unverifiable` and 11 more), nothing untrusted can reach the line anyway because the cut refuses it and `requireTrustedIdentificationConfidence` throws, and the publisher now prints the split so a run says which mechanism carried it. Measured: ledger republishes **26 pieces `[pair-judged-same=23, vision-kept=3]`, 0 refusals, 0 evidence failures through printed step 8**; the run goes from `input-rejected` with **27 `action-ledger-incomplete` failures to `incomplete` with 0**, `stepsComplete 3`, `piecesPlaced 4`, `finalParts 4`, structural hash present. Looked at all three `step-NNN-build.png` against their panels: step 1's corner plate seats onto the quarter-ring with both ring ends protruding, step 3's fan silhouette matches the printed outline including the right-edge tab and the lower-left diagonal, and panel 3 corroborates step 2's wedge seated where the build put it. The prefix number did not move — 3 steps and 4 pieces, same as 2026-08-07 — because **the blocker moved later**: at `--last-step 8` step 4 now fails `camera-fit-failed`, 9.11px from the closest upright axonometric against a 0.02 tolerance on a 92.19px stud pitch, and steps 5–6 are blocked behind it. Also found and left as a work item: `handednessVerdicts` is **inert in the coverage path** — `booklet-catalog-coverage.mjs` calls `claimsFor` without the `handedness` argument, so all 4 mirror-paired picks become `handedness-unverified` there while the scorer resolves the same 4 to `vision-kept` from the card pixels, two of them at steps 11 and 17.
- 2026-08-08: **`3020;L` is derived and pinned, and the two sources agreed to the LDU before anything was written** — the design blocking printed steps 5 and 7 is `builtin:plate-2x4` at `[30,-4,10] upright-yaw-90`, `catalog-part-self-symmetry`, p95 = max **1.305568 LDU**. The two legs of `builder-type23-frame-plus-ldraw-surface/3` were computed apart: the type-23 `Custom2DField` in `3020.xml` gives eight male nodes at `x ∈ {0,20,40,60}`, `y=-8`, `z ∈ {0,20}` through `diag(25,-25,-25)`, whose exact integer correspondence with the catalog's eight stud connectors yields two frames differing by the plate's own half turn — one class, no mesh consulted; independently, a blind search over every upright turn and a 2 LDU translation lattice, scoring Builder's own Shell against the expanded `parts/3020.dat` surface with no stud lattice in it at all, has its global optimum at **the same transform**, `[-30,8,-10] yaw-0`, mean 0.3245 LDU, beating the nearest non-symmetric alternative by 2.39x. Method validated first by reproducing `3032;F`'s admitted pin byte for byte from raw sources, including both slice digests. Consequences, not the absence of an error: printed steps 1–8 go from **16 of 18 resolved to 18 of 18**; export agreement across the model goes **39/40 → 42/43** with the same single known outlier `80015;E`; solid-body interpenetration involving 3020 is **zero** (step 7's two `PART_STUD_BODY_COLLISION` are `51739;H` against `3795;I` and `54383;F`, pre-existing). Looked at: booklet page 12 step 5 and page 13 step 7 against renders of the calibrated prefix — the blue 2x8 sits between the two green 2x4 plates edge to edge on the `y=8` layer, which is what page 13 draws. Material 28 on these bricks is **green**, not dark bluish gray. Bundle 1065564 → **1091772** bytes, LDraw closure 101 → **102** files.
- 2026-08-07: **The widened vision reply gets a reader, and a gate that keeps it read** — the schema now lets the call say how the query differs from its pick and write a sentence, which is worth nothing on its own: the rotate icon was detected, measured and correctly named, then consumed by nothing for weeks, and that inverted the face parity of every step after it. So `observations` writes `output/part-identification/observations.md` — recurring themes, every declared difference, every doubt with the cause it stated, every two-way ambiguity, every note in full — and `observations:check` (new, in `verify` beside `node:check`) goes red when answers carry observations and no report was derived from them or the report is stale, naming what was collected, what did not read it, and the one command that fixes it. Themes are **discovered, not declared**: term frequency over the notes with no domain lexicon, because a hand-written word list is the same mistake as a hand-written list of failure modes. On a live pinned-model pass over cards 39/79/128 it found `edge · left · mirror · right · run · step · stud` from two sentences, and both wedge picks named their mirror candidate — **2 on card-0039 and 4 on card-0079** — which I confirmed against the printed cards by eye and then wrongly promoted to `vision-kept`, because naming the twin decides nothing: the twin's number is the same number whichever hand was picked, and feeding `visionPick` pick **2** with the note "candidate 1 is the mirror" returned that same label carrying element 6392747, Wedge Plate 6 x 2 **Left** — the swapped hand and the correct answer, indistinguishable. The hand is now read from the card's own pixels in `part-identification-handedness.mjs`, silhouette IoU of the query against each hand and against each hand mirrored, and that swapped input returns `handedness-refuted` with no element; the note count survives under the name it had earned, `mirrorPairAwareness`, which says in its own field that it decides nothing. The gate's second edge runs everywhere: a mutation probe over a fixture proves **7 of 9 answer fields change what the pipeline claims, with `confidence` and now `note` changing only the report** — `note` demoted from `decides` to `reported` the moment the pixels took the handedness decision off it — which records the measured 0-of-859 inertia rather than laundering it green — and a fixture with no assignment contest correctly demotes `alsoCouldBe` to inert, so the probe measures consumption and not presence. `reask` adds one bounded closed two-way question where a mirror twin went unnamed or a second choice was offered; it is generation-1, bound to the answers digest it came from, and **may only ever remove trust** — the question names both candidates, so letting its reply satisfy the grader's mirror check would be the question grading itself. Cost noted: a 6-card vision batch timed out at 14 min and two concurrent 3-card batches contended into the same wall; 3 cards alone landed in under 10.
- 2026-08-07: **The candidate lab is deleted, and it took an app the brief had listed as must-survive** — `packages/generation`, `apps/harness`, `apps/web/src/generation`, `AssistantPanel.tsx`, `candidate-lab.spec.ts` and `update-generation-pin.mjs` go together: **41 files, 6,954 lines**. The brief kept `apps/harness/src/deterministic-maker-replay.ts` on the grounds that it consumes `AssemblyPatch`; grepping first showed that claim inverted — the file's subject is `CapturedMakerPopulationResult` from the deleted package for 363 of its 364 lines, and its single `AssemblyPatch` touch verifies candidates the deleted maker produced. All six harness modules were that one machine, no booklet code imported it, and the owner authorized deleting the app rather than leaving a workspace that cannot compile. What the cut nearly took silently: `apps/companion/src/test-run-recorder.test.ts`, 439 lines and the **only** coverage of the companion's surviving run-bundle recorder, which manufactured its fixture through generation + harness; it now states the `DeterministicMakerOutput` and capture-manifest bytes directly, which is the boundary the recorder actually sees. Those two protocol schemas stay for the same reason — after this they have a consumer and no producer, and deleting them would break the recorder. `verify`'s one failing test was `candidate-lab.spec.ts`, so the deletion is what makes it green; `lego.app-observation/1` loses six candidate keys and `BrickViewport`'s `previewing` prop dies with them. Workspaces 8 → **6**, BOM 14 stale rows → 0.
- 2026-08-07: **The acceptance ceremony leaves the protocol, and the trap was the neighbour, not a reference** — `PresentedPatchEnvelope` and `AcceptanceAuthorization` are deleted: two schema roots and their definitions, two generator map entries, two exported validators with their `SCHEMA_IDS` keys, and their contract-test fixtures. Both were dead on every axis — zero producers, zero consumers, and a `--no-ignore` sweep of `var/` and `output/` found no instance ever minted, only content-addressed copies of the source files themselves. The assembled protocol goes 29 → **27** root branches and 87 → **85** interfaces, and `validators.generated.js` 51,334 → **49,313** lines, all four generated files rewritten by `schema:generate` and never hand-edited. The hazard was adjacency: `Ed25519SealV1` sits between the two definitions in the schema and between the two interfaces in the generated types, and it is what keeps `NativeSealedRunManifestV1` — the sealed-run manifest the booklet loop writes on every run — alive, so cutting "the acceptance block" as one contiguous span takes it. No version bump and no migration: `PROTOCOL_VERSION` stays `lego.protocol/1` (which `node:check` asserts by exact string), nothing anywhere hashes the schema's bytes, and neither type appears in `BrickDocumentV1`, `TruthSnapshot` or the catalog, so no saved document can be reinterpreted by their absence. `AssemblyPatchV1` lost its last `$ref`-er and stays live as the compiler's return type.
- 2026-08-07: **Three `--check` gates stop guessing why they failed** — each compared whole file bytes and reported the domain change it usually means, so `pin:check` on a translated checkout printed `holds sha256:366fefbf… but this build produces sha256:366fefbf…`, the same digest twice, and sent the reader to regenerate a value that had not moved. The comparison was never wrong; the message sat an altitude above it. All three now name the pinned values, dependency rows or exported symbols that actually moved, or say the bytes moved while the meaning did not and quote the first differing line. Found while auditing error messages as a class, which is also how the write path's "the deterministic path itself changed" turned out to fire when nothing had.
- 2026-08-07: **A nested agent worktree could still be committed, and only this machine knew not to** — `.claude/worktrees/` was held out of `git status` by `.git/info/exclude` alone, which is per-machine and does not survive a clone, so a rebuilt machine had the hole open again: in a fresh clone with no such rule, one planted checkout offered **315 files** to `git add` from `apps/` alone, against **0** once the tracked rule exists. `/.claude/worktrees/` anchored, not `.claude/`, because `launch.json` beside it is tracked and should stay so. The lint half of the same hole — eslint's traversal finding two candidate `tsconfigRootDir`s and refusing to parse **976 files** it had no quarrel with — was fixed concurrently in another session, which also widened `var/runs/**` + `var/state/**` to `var/**` after browser-drive probes in `var/audit/` reddened the gate; this session's duplicate commit was dropped rather than merged. The rest of `verify` was re-run with a real nested worktree in place and none of it is exposed: `tsc`, vitest and Playwright anchor discovery at `apps/`/`packages/`/`testDir` (148 and 29 files found, **0** from the nested checkout), `check-bom` and `check-production-bundle` walk fixed roots, `.prettierignore` already carried `.claude`, and no gate config reads `.gitignore` at all.
- 2026-08-07: **A brief said step 2 refuses; it settles, and the real finding was a test that could not fail** — the diagnosis handed in was that the exploded ghost ranks the wrong candidate first at 0.5729 against a 0.5881 ceiling; the run at HEAD reports `complete`, containment 1.000, **0px outside**, and I confirmed it by eye before by score. What *was* live: `real-build-exploded-step.test.ts` built its synthetic panel with an **empty stroke mask**, so `strokeF1` was identically 0 and `scoreStepDelta`'s blend a monotone transform of `regionIou` — swapping the ranking key for the blend left the whole suite green while on the printed panel it promotes a ghost spilling 46px (blend **0.5968** against the contained ghost's **0.5806**). The suite's panel now carries the stroke the booklet draws, outward of the silhouette; both swaps were performed and both now fail. Per-seat selection went containment-first (0 of 105 seats diverge today). `building-system.md`'s superseded refusal section is marked as such — it is where the stale brief came from.
- 2026-08-07: **`learning-system.md` stops describing a product that no longer exists** — 1,186 → **420** lines, rewritten around the booklet run rather than the copilot's maker loop, replacing the 8-line substitution note a previous pass had bolted on top. Grepping before cutting overturned the plan twice: the three run/attempt/candidate state tables match `run-ledger-policy.ts` transition for transition and were kept verbatim, and `replayLevel` is live across five files under `apps/web`, driving a seven-module `real-build-replay*` closure the booklet loop writes on every run. What was actually dead was the `civ-engine` fleet projection and the `PassOpened`/`PassTerminalRecord`/`PassVerdict` block — **391 lines of TypeScript with zero call sites anywhere in the repository**.
- 2026-08-07: **The AI copilot is cut from the specification, and the spec described the wrong product twice over** — `spec.md` goes 561 → 383 lines, losing full/scoped generation from a text brief, the candidate tray, variant exploration, the provider strategy list and Gates 3–7. The second error was the larger one: the old spec said "booklet" **zero times** while `building-system.md` opened with "the goal is in `spec.md`: read a printed instruction booklet". Grepping before cutting overturned the premise — `AssemblyPatch` is the live compiler output type, and `PresentedPatchEnvelope`, `AcceptanceAuthorization`, `BuildBrief`, `ProviderCapabilities` and `ActorObservation` are live schemas with contract tests, so their contracts stayed and a `Cut:` section names what the surviving code now serves.
- 2026-08-07: **The booklet places its first bricks** — printed step 1 completes, `2/2 placed`, after reading none since the repo began, and it was settled not by its own panel but by **step 2's**. The deepest of six cleared defects: `resolveBuilderBoneTransform` read the LXFML Bone through a reflection, mirroring every world placement in the 1465-brick model, invisible because every check to date compared a part to its own surface (impossible stud coincidences 64/65 → **0/0**). Step 2 still refuses, honestly, at 97.4% of its panel's ceiling.
- 2026-08-06: **The booklet turns the model over, and nothing had consumed the icon** — set 6651557 is built partly upside down; a fill colour leaking across a PDF `restore` had undercounted its rotate icons at 39, and the corrected 43 take the panel-face fold from 7/43 to **43/43** blind-judged. Face-blind the closed loop does not stop, it builds confidently wrong (5/6, diverging at step 3). Re-cutting inventory thumbnails as assigned components took Builder-truth shortlist recall 0.88 → **1.000**. Two gates were found measuring nothing.
- 2026-08-05: **The catalog opens and identification stops guessing** — the first eight measured parts are admitted (77 → 85 definitions), blind pair judging becomes a trust source at **84/84** rater agreement, and build input failures fall 92 → 62 → **12** while `stepsComplete` stays 0. Five checks had quietly stopped checking, the worst a set-accounting test only ever preflighted at a run length where its clause is never evaluated. Into the small hours, recall@6 measured **96.0%**, refuting the hypothesis that retrieval explained vision's 39.9% self-consistency.
- 2026-08-04: **A false blocker had stranded ~4,000 approved lines** across two sessions — "Git writes are quota-locked for four days" was disproved by one command, releasing a 20-commit burst of part-admission foundations. The oriented-box kernel's differential test was comparing it against a copy of its own arithmetic: 0 disagreements with the kernel, **598** with exact truth, every one in the same direction. Builder collision was refused as a source — its boxes are inset 0.25 LDU and the real part escapes by up to **4.675**.
- 2026-08-03: **The 6651557 LDraw source routes are audited, metadata only** — 117 of 121 required leaves resolve to exact roots across 439 closure files (896,002 bytes); 4 do not, including 3245. No geometry is committed: `bom:check` binds the live audit sha256 and fails if the artifact reaches 256 KiB, and it already sits at **251,402**. `test:python` was created and wired into `verify`, because a passing-but-ungated suite is not a gate.
- 2026-08-02: **An adversarial review gutted the panel-fit evidence** — three assertions had no failing input, one was inverted, and pure noise under an invented grid scored 0.5px at a hit rate of **1.000**, so the proof that a panel is a stud grid became an honest weak hint. Part naming reached **1308/1465** pieces by global assignment against the booklet's own printed inventory. The real booklet stayed blocked at printed step 1 until an analytic arc primitive added the three round-corner plates it needs (catalog to 77).
- 2026-08-01: **The closed loop rebuilds 6 of 6, and the fault was in the probe** — it keyed "the part just added" as `parts[parts.length - 1]`, which `applyBuildOperations` does not order by insertion, so about half the time the mask highlighted the base plate. Nothing in the enumerator, the score or the driver changed. What gave it away was an impossibility: two identical spellings of one placement scoring 0.38 and 0.96. Physics landed the same day, on a ground slab 20 micrometres thick until a brick fell straight through it.
- 2026-07-31: **Measurable intermediates replace "did the right model come out"** — booklet parsing went 358/359 steps to **359 of 359** at coverage 1.000, and the reported 3102-piece callout total fell to **1622** once the back-of-book inventory was separated from step callouts. Two beliefs died by looking rather than by testing: the brick art is raster, not vector, and a stud-pitch probe's confident 6 px on every page was an artefact of its own detrending that had agreed with itself four times.
- 2026-07-30: **The editor becomes buildable, and the first booklet is read** — lattice placement, click-to-place, per-placement build steps, and a catalog grown well past 14 parts (truth /1 → /3, with a migration that refuses rather than reinterprets). Five defects surfaced only by driving the UI: camera snap-back, an empty scene framed inside the first brick, mid-air placement, studded tiles, a palette overflowing under the canvas. First PDF read: 224 pages, 67 MB, ~20s — hashing after pdfjs detached the buffer fails outright.
- 2026-07-22: **AGENTS.md is reshaped to the lean fleet form** — 160 lines deleted, 64 added, the fleet constitution carried verbatim so `sync-fleet` stops flagging it. The imported blob ceiling it brings, nothing over 1 MiB in ordinary Git, forbids the **1.53 MB** generated Ajv validator this repo has tracked and rewritten on every schema change since day one. Unresolved: either that file needs a recorded exemption or generation moves to build time, which breaks `node:check`.
- 2026-07-14: **The coherent-unit commit boundary is written down** — review each unit immediately, stage only its scoped files, and never commit failing, in-flight, or partial work merely as a checkpoint. It arrives five days after the 31,794-line single commit that motivated it. Doc-only; nothing executable changed.
- 2026-07-12: **Headless-first becomes a mandatory rule rather than a default** — a visible-browser or GUI path now requires a stated reason first. The section exists specifically to opt out of the surrounding document's "adaptable defaults" framing. Doc-only.
- 2026-07-11: Nothing worth a line. One chore commit relocating the brick-assembly-loop skill out of the user-global Codex directory into repo-owned `.claude/` and `.codex/`; 32 lines, no behaviour.
- 2026-07-10: **Three scoped-patch bypasses closed 72 minutes after shipping** — required attachments were checked against the patch's own declared operation payload rather than the result document, a patch could invent the anchor part it needed, and a part with no resolvable bounds escaped the volume check entirely. All three trusted something the patch controls. The compiler snapshot bumped /1 → /2, which is a determinism-contract change. The tracked Ajv validator blob grew 418 KB → 1.53 MB the same day.
- 2026-07-09: **Design first, then a 31,794-line drop** — 1,909 lines of spec and learning-system landed before a single package existed, then the whole Gate 0/1 workspace (85 files) arrived as one commit nobody could have reviewed as a unit. The scoped-patch policy shipped the same night carrying three bypasses. 24 of the range's 29 commits have empty bodies, so everything recorded for these two days was reconstructed from diffs.
