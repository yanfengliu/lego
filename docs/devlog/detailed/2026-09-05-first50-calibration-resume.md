# First-50 calibration resume — 2026-09-05

## Run the real gate before extending the camera search

The existing `real-build-prefix50-step44-real-domain-calibration.spec.ts` failed before source access because its pre-unlock import registry omitted current lazy dependencies. The two existing `real-build-step44-playwright-source-closure.test.ts` cases reproduced the refusal. Registering the exact source-safe capability, catalog, official-model, and browser helper imports made both modes pass. The held-out raster remains a post-unlock import; calibration thresholds and source pins did not change.

## Test cleanup must not own the live ledger

The independent critic found that `real-build-prefix50-step44-later-source-ledger-controls.ts` provisioned the working checkout's repository genesis and unconditionally removed it in teardown. With an existing operator-provisioned genesis, setup could fail and teardown could still delete that pre-existing state. Each control now creates a fresh synthetic repository and registers it for cleanup before provisioning. Operation markers stay inside that root. The durability suite now removes only its unique run directory rather than a shared output parent.

The ledger controls have their own discoverable test entry point. A stale browser build can no longer prevent those controls from running during module import. The ownership regression executes the real setup and cleanup hooks against an in-memory filesystem, forces provisioning failure, and checks the exact operation trace. It reintroduces the live-root provision and deletion only as VM text; the same ownership assertion rejects that mutation. Both ownership cases passed, and the independent critic accepted the repair and its stated bounds.

The first ledger run had eight passes and two five-second wall-clock timeouts while real calibration was running. These controls launch bounded OS key-protection and Node processes; their suite-local case limit is now thirty seconds, with all semantic assertions retained. The sequential ledger, ownership, and source-closure run passed all fourteen cases in 39.01 seconds. The real process-kill durability suite passed its four Windows-applicable cases in 377.93 seconds; its POSIX ownership/mode case was skipped.

## Refuse unavailable camera evidence before expensive reproduction

The fresh real calibration used output identity `page44-real-domain-calibration-resume-20260905-ecec3a0b7f784147a8117514e0cc3645`. It spent 12.7 minutes rebuilding the required materials, then refused because the independent signed camera-axis branch remains unavailable. Its private staging directory `.page44-calibration-staging-227bf3f2-7ac8-4434-8fda-827cbdbb5485` remains under the ignored review root as counterevidence. It did not qualify Steps41/42 or open the held-out source.

An availability-only check now runs after live source-lock validation but before material reproduction or staging creation. It cannot mint a geometry or source receipt; the existing receipt still performs the geometry and commitment checks. The unchanged real calibration command reaches the same refusal in 247 milliseconds of test execution and 10.951 seconds end to end. This is faster failure, not successful calibration. All six publication tests passed sequentially in 15.68 seconds. Removing the early call made the new test fail on a synthetic heavy-material sentinel; restoring it passed. Neither mutation executes material reconstruction or reads the PDF.

Both external reviewers were retried under the multi-CLI runbook: Codex CLI 0.153.4 returned HTTP 401, and Claude CLI 2.1.261 reported an expired OAuth session. Neither supplied a review. The independent subagent read the actual files and supplied the ownership finding, repair review, and source-free mutation probe. The seven scoped files had identical hashes before and after the external review attempts.

## Source inventory and real static-browser integration

The BOM source census initially refused the repository-authored Poppler job-launcher DLL. A narrow admission now requires its exact path, 13,312-byte binary, and complete 15,376-byte C# source in the same census population. Two independently rerun deterministic builds reproduced the binary byte for byte without executing it. The ordinary C# literal census remains active. Thirty-one focused tests passed, `bom:check` passed 25 source/data records, and an independent critic accepted the exact-pair policy. The opt-in build verifier is trusted-local reproduction, not a hermetic toolchain or signing boundary.

The full gate then exposed formatting drift in 22 files and two unused test bindings. After repair, formatting, lint, typecheck, and all Python scripts passed. The two affected test files passed fourteen cases in 311.50 seconds. The broad two-worker Vitest baseline was interrupted after multiple failures because its dot reporter deferred failure details; it is incomplete evidence, not a full-suite verdict. The replacement runs verbosely with one worker and stops at the first failure. Before interruption, the current-evidence compiler emitted the frozen 211-candidate return batch with the unchanged input digest `5b8aab325eca84433ed49922772002f3981cb883bb70889436509c9aa3974894`; that is still authority-free return evidence.

A synthetic headless check of the actual production bundle found two independent integration failures: inspection hooks were absent, as the production guard intentionally requires, and the parent-seeding helper requested two private `/src/*.ts` modules that the six-asset static server cannot serve. The raw report is `output/playwright/step44-static-seed-diagnostic-20260905-ui3/report.json`. The real Import/Export controls preserved the one-brick structural hash `eaaa5151e18d4f48caaea5873f7b3fb5707521f6206c9cf93a7d636a23004cb6`; its visible render was inspected. The unavailable CLI was not treated as a product failure: the probe used the installed browser library with an isolated profile and finally cleanup.

`npm run build:instruction-review` now emits a separate static build under ignored `apps/web/e2e/static-app/dist`. Only that build enables the existing inspection/capture hooks; the default production configuration and hook-exclusion check remain intact. The harness seeds through the real Import control after proving an LDraw round trip preserves the requested structure. Import deliberately reattributes provenance. An independent critic found that an already-matching hash could satisfy the wait before asynchronous file reading finished; the helper now returns before starting an import when the live structure/count/validity already match. The regression requires exactly two filechooser events across red, blue, and repeated-blue requests.

The owned-browser integration case passed with the actual pinned static server in 6.45 seconds, including browser/server process cleanup. The separate diagnostic produced no failed requests or page errors, preserved the blue structure hash `1e6aa095cdb4c23749a01d6c19ef3d4526051ed0cacc06e033a3867a7adff489`, and captured seven native model views. All seven were inspected individually. Their digest-bound record is under `output/playwright/step44-static-seed-diagnostic-20260905-review-build`; these are synthetic harness checks, not catalog or booklet visual admission. Two review builds emitted identical asset bytes. The normal production bundle guard still passed.

The reviewed three-root source closure remains 527 modules with no policy violations and an unchanged explicit I/O roster. Six static asset paths move to the isolated build. Sixteen source hashes change: fourteen are this session's reviewed edits or formatting, while two tracked-clean historical changes add only an erased site-phase type marker and extract the unchanged lattice comparator. Their prior source bytes were recovered from commits `7471a362aca6ad29e64adc7402d12d3ef7457117` and `59227bfd10ad39d193b6c17fe3b7459115ba6be5`. The refreshed closure commitment is `c8e560872ac7675710d9b494a2e119ebd57e036f7b970ac38212141ac0ece836`. The eight authority/closure tests and four lattice-selection tests passed in 13.38 seconds; no PDF or calibration threshold was repinned.

## Test the camera premise before extending its implementation

An independent source-free derivation proves that exact ordered stud pitch, full top-circle ellipse shape, signed top/base center displacement, and genuine top-face visibility uniquely determine scaled orthographic orientation outside degenerate views. The reproducible experiment passes 450 camera cases and 1,833 assertions, with maximum projection-entry error `9.421284197230761e-9`. It also demonstrates that mislabeled signs can remain algebraically valid, grazing views are ambiguous, and uniform length rescaling is unidentifiable. Its script digest is `0287916d6ed289281b8728800a474da72d2a65488ec4a5cf3bcbe10d23b922d1`, retained under `output/hard-problem/step44-first50-golden-20260831/stud-circle-observability-20260905`. This proves only the ideal-correspondence kernel. Realistic catalog-rendered pixel controls and automatic signed correspondence recovery remain required before protected source use.

This is an integration and test-ownership repair, not a golden first-50 claim. Fresh real calibration, downstream visual replay, and a complete full verification pass remain required. No live repository genesis was provisioned during this repair, and no Step51 assembly was attempted.

## Real instruction captures must use the instruction finish

The source-free four-stud experiment found a concrete measurement defect before attempting pixel correspondence: `capture_instruction_view` labeled the live presentation render as `instruction-art`. All four native images were inspected separately. Only 1.9754%, 0.4739%, 0.2345%, and 34.1907% of their interior pixels matched the declared instruction palette. None contained an exact stud-cap or stud-wall pixel. The frozen plan, code, native captures, hashes, and cleanup result remain under `output/hard-problem/step44-first50-golden-20260831/stud-circle-pixels-20260905/capture-iLnz6Q`; ellipse fitting stopped at the preregistered instrument refusal, not an impossibility claim.

The new real-browser regression reproduced the defect independently on a blue brick: all 12,059 subject pixels in its first view were outside the expected palette. Its other three art views also had no on-palette subject pixels, and the antialiased semantic masks contained 280–569 off-palette edge pixels. The unmodified failing capture packet remains at `output/playwright/instruction-art-integration/run-Uddf3k`.

Instruction capture now derives a separate instruction-finish scene and borrows only the live renderer's GL context. Art and semantic masks share that instruction geometry and a non-multisampled sRGB render target with no lights or tone mapping. It allocates no new GL context and does not resize the visible canvas or move its presentation scene. A `finally` path restores renderer state and disposes the temporary target and projection. A critic caught that Three.js reports default-canvas viewport coordinates even while a render target is active; the helper now refuses a nested offscreen target before allocation rather than claiming arbitrary target restoration. A stale document is also refused before rendering.

The browser regression passes all four views and four scales with zero off-palette art or mask pixels, stable repeated art, matching art/mask camera matrices, and identical seven-view presentation captures before and after. Both static browser integration tests passed in 12.76 seconds. Six focused unit cases cover success, target-binding/render/readback faults, stale input, and a nested-target refusal. These bounds do not qualify the PDF camera or establish real stud/base correspondence.

The verbose full-suite attempt stopped at `scripts/part-identification-prefix50-verified-projection.test.mjs`: the old caller supplies no genuine `selectedSubBuildReturn`, which canonical compilation now correctly requires. That test remains red; no selection was forged and no completion assertion was removed. A separate run passed the four projection-only roster/digest cases but found a stale source-corroboration expectation that omits the later part-scoped repair orientations. The complete first-50 and full verification gates remain unmet.

The final instruction-capture, automation, static-contract, and source-authority selection passed all 26 cases in 13.22 seconds. The ordinary production build and hook-exclusion guard passed. The reviewed three-root closure still contains 527 modules; this capture repair changes only its static manifest, HTML, and two emitted JavaScript asset rows. Its new aggregate commitment is `9c188e3a5e6c3856ecbbcc07526289adf03fd401873e5d5f023f161757ba42bd`. The earlier closure commitment above records the preceding static-import repair, not the final capture bytes.

## Preserve failed pixel hypotheses instead of choosing a flattering view

Rerunning the unchanged color-only experiment after the capture repair produced 100% on-palette interior pixels in all four views. It still refused: its frozen cap-tone rule found 6, 6, 5, and 5 components because some body faces have nearby colors. The replacement packet remains under `stud-circle-pixels-20260905/capture-MGpips`; the prior mislabeled-finish packet remains intact.

A separate geometry-based branch examined every exact-color connected region without named cap colors, expected camera input, or a best-four selection. Its fixed 12 positive cases recovered four stud tops in 11 cases, but all four top/base pairs in only one. All four absent/rectangle/extra-count/missing-count controls refused. Nearby top-rim and body boundaries still produce competing base-arc hypotheses. One low-angle native-scale view also loses three caps under the frozen shape rule. The maximum center error of 0.3153 pixels covers only 45 detected tops, not all 48 expected tops; the 1.0751-pixel base bound covers only 20 unique base candidates. Neither partial population establishes a working detector.

The branch's plan, code, all 16 native PNGs, per-image alternatives, scoring, and digest-bound visual review are retained under `output/hard-problem/step44-first50-golden-20260831/stud-ellipse-geometry-20260905/capture-wh5zk7` and its parent. Both the author and the chief-of-staff inspected every native image. All captures completed with no page errors or failed requests and confirmed browser, process-tree, and server cleanup. No threshold was relaxed, old failure overwritten, or protected source opened. A future branch must prove connected cylinder-wall/base identity and handle raster quantization; selecting the largest-support arc would not establish either claim.

## Complete gate baseline, not a passing implementation

The uninterrupted default eight-worker `npm run verify` reached Vitest after schema, Node, migration-history, observations, BOM, parts, lessons, notices, format, lint, typecheck, the instruction-review build, and all Python scripts passed. Vitest finished in 973.20 seconds: 47 failed files, 546 passed files, two skipped files; 91 failed tests, 4,257 passed tests, 66 skipped tests, and two todo tests. Seven suites failed during setup. The command exited 1 and did not reach its browser or final-build stages.

The failures include exact legacy catalog/runtime contracts, the unavailable canonical Step-44 return, Poppler roster drift, pre-lock import policy, and expensive setup/case timeouts under load. Those timeouts are not classified as semantic defects without sequential reproduction; the blind-production case had passed sequentially earlier. A separate ordinary browser run uses a unique ignored output root so it cannot overwrite earlier counterevidence. The goal remains active, and this full-gate result is not permission to update answer pins or suppress required tests.

The Poppler refusal is confirmed environment drift: its pinned 178-file tree now contains 179 files, including a new `bin/psl-5.dll`; 82 existing files differ in size or digest. `pdftoppm.exe` changed from 50,176 to 65,840 bytes. A read-only inventory at `output/step44-workflow-repair-20260905/poppler-roster-delta.json` preserves every expected/actual digest. No binary was executed for that inventory. Searches of the runtime cache and repository output/state found no alternate retained `pdftoppm.exe`. The frozen toolchain commitment was not changed.

## Restore historical catalog projections without moving their hashes

An independent investigation traced ten historical catalog-test failures to six newer orientation grants leaking into the reconstructed /29 definitions. Removing only those grants recovered every original expected hash in memory before editing any test. Four affected parts account for an 84-byte delta in the earlier prefixes; the later slope's two additional grants bring it to 126 bytes. Connector and collision bytes did not change. Immutable source commit `6b02e50732d7908374b168ce0582476ac0422769` independently supplies the thirteen older grants across eleven parts.

The test-only projection now removes a closed six-pair list, preserves every older grant and unrelated field, tolerates absent out-of-prefix parts, and refuses missing or duplicated expected grants on present parts. All ten original digest expectations are unchanged. The focused single-worker run passed eleven files and 75 tests in 11.20 seconds; scoped lint and diff checks passed. This does not update runtime catalog definitions or the separate legacy runtime bridges.

The chief-of-staff independently read the implementation and historical source, then mutation-tested the real helper gate. Leaving all six grants in place made the historical-map assertion fail on the extra `proper-m-00n0n0n00` label. An over-broad upright-only filter made the same assertion fail on the missing older `proper-m-00nn000p0` label. Both mutations were restored in `finally` paths, and the helper's SHA-256 returned to `a2dfc0e0527a56a86c61995e070ad1a94381d7006ee5a97132a2511e13ec5af7`. All fifteen helper tests then passed in 1.41 seconds. The gate proves the closed historical projection; it does not grant the current first-50 model completion or placement authority.

The separate source-corroboration case now obtains all three existing Step-41/42/42-43 repair proofs through their verifier and opaque-proof reader APIs. It binds all eleven repaired ordinals 270..280 and the six exact new part/orientation pairs, then requires the thirteen raw-source pairs plus those six repairs to equal all nineteen current policy pairs. It passed in 196.77 seconds including current-source reproduction; an independent critic accepted its bounded claim. The two complete-prefix tests still require canonical compilation and still lack a genuine Step-44 selection. Neither assertion was removed or replaced with diagnostic compilation.

## Ordinary browser baseline violated the campaign's source isolation

The chief-of-staff incorrectly assumed that default-browser opt-in skips excluded every protected-source measurement. `real-panel-registration.spec.ts` actually processes all first-fifty panels without an opt-in flag, including the protected holdout. Reading the test exposed the mistake while the ordinary suite was running. The exact task-owned npm/browser process tree was terminated, but pending stdout revealed that panel registration had already completed and returned diagnostics. No protected PNG was inspected and no camera selection or threshold change was based on those diagnostics. That does not undo the boundary violation.

The ordinary run is disqualified for camera qualification and is not a complete browser gate. Its unique test output and the existing test's shared `output/real-panel-scoring` writes are excluded from camera derivation and promotion. The prior shared-root bytes were not inventoried, so their preservation cannot be claimed. `output/step44-workflow-repair-20260905/browser-boundary-incident.md` records the exact command, ownership-checked termination, affected roots, and scope. Camera qualification is paused pending an independent isolation review. Explicit source-free browser tests can continue; the default whole-browser gate must wait until the protected qualification stage permits it. The frozen contract is not waived or rewritten by this incident.

## Playback is a readable preview, not an implied verification

The manual-browser failure expected `verified` where the implemented membership-only sequence correctly displayed `preview`. The test now checks that label, its explanation, `membership-preview`, `exact: false`, and a null trace commitment while retaining every structural, validation and renderer-hash assertion. Native inspection also found a real layout defect: the conditional playback bar created a fourth child in a three-row workspace and clipped the bar/footer. A separate source-free run proved the new containment gate red before the CSS fix.

The workspace now uses an auto-sized playback row only while the bar is present. Four source-free manual-building/panel-resize cases passed in 28.6 seconds. An independent critic found that direct-child containment alone permits nested text clipping; the refined gate also checks positive element area and nested text ranges. That case passed in 7.5 seconds. Squeezing the nested readout to one pixel with hidden overflow then made the real browser gate fail in 11.4 seconds; the mutation was restored in a `finally` path. The two desktop screenshots and all seven native model views were individually inspected, with a digest-bound review under `output/playwright/first50-playback-layout-reviewed-20260905`. This proves only the two-step manual preview at the two tested desktop sizes.

The ordinary production build, typecheck and hook-exclusion guard passed after the layout change. The static review build changes 79 minified CSS bytes plus generated chunk references. Its asset commitment is `9bac6bfc1b3abd577f3826e4ed8e123997216b66bb5d9386635dcc50bc60078c`; the exact three-root closure remains 527 modules with no policy violations and unchanged I/O roster, now committed as `6747a2642bbb01cf2d10177ce1ca5d9333abbe4a14c39ba03414ddffd4c2892a`. The prior image packets keep their older asset identities; neither PDF pins nor camera thresholds changed.

After the asset refresh, both actual static-app/instruction-capture integration cases passed in 12.17 seconds, and the eight later-source/closure plus six capture-resource cases passed in 15.59 seconds. The independent critic accepted the final playback change and inspected both exact desktop PNGs. Scoped formatting, lint and whitespace checks passed. The prequalification browser restriction is now recorded in `docs/policies/local-rules.md`; it changes gate ordering, not the full-gate requirement. No first-50 completion, commit or push is claimed.

## Independently confirm five recoverable Poppler files, not a restored toolchain

The installed manifest declares Poppler `26.07.0=h6618ce5_3`, while the frozen roster requires 26.05.0. A read-only fetch of the exact conda-forge `poppler-26.05.0-h4b9d284_3.conda` archive returned 2,813,146 bytes with SHA-256 `378623132f942a83051cedcddd1b1e2ebf01b983d633f1da701cf86bb805009a`. Independent Python 3.12.14 standard-library ZIP and tar parsers reproduced the first probe's exact size and digest matches for `pdfinfo.exe`, `pdftoppm.exe`, `poppler.dll`, `poppler-cpp.dll`, and `poppler-glib.dll`. All 162 package entries were inspected. Only five of the 178 frozen roster files occur in this package; the other 173 are not supplied by it. This is byte-recovery evidence, not installation, complete dependency recovery, or camera qualification.

The independent parsers verified ZIP CRCs and tar header checksums; a corrupted-header control failed with `tarfile.ReadError: bad checksum`. Node's Zstandard decompressor is shared with the first probe, so decompression is not an independent implementation. The first probe's limited manual tar parser does not validate header checksums; its exact-archive SHA check and this independent parse support the bounded five-file result, not a general archive-validation claim. Reproduction scripts and their digest-bound report are retained as `output/step44-workflow-repair-20260905/poppler-recovery-independent-review.mjs`, `poppler-recovery-independent-parse.py`, and `poppler-recovery-independent-review.json`. Their script SHA-256 values are `4eb74eff91f8b76206494079644a9c3a49912aa2f1e33e68c48554287b8cd1a0` and `419d4f29bb218a9b3d252e3be91182dc9da7da8d0da9de155f5c9d5631e98e03`. The first probe and report remain unchanged. Package contents stayed in memory; no Poppler binary was installed or executed, no PDF was read, and no shared runtime or pin changed. All synchronous parser children exited; no browser or server was started.

## Independent isolation review requires a user decision

The separate `output/step44-workflow-repair-20260905/source-isolation-independent-review.md` review, SHA-256 `e4f05f80cbb3cf75503c56881512d7fd09417830894b076fd31608342d3f7634`, concludes that the accidental run is permanently disqualified and establishes no admissible ordinary retry under the frozen contract. Isolation repairs cannot retroactively restore withheld-source status. A successor evaluation protocol and its resulting evidence claim require the user's explicit decision; camera qualification remains paused. The five-file Poppler result above does not resolve that independent boundary.

## Isolate Poppler unit coverage and recover 177 frozen files offline

Two of five mocked-spawn unit cases failed before their framing and stderr assertions because they first verified the drifted installed Poppler tree. The test now supplies exact synthetic files through a mocked configuration factory while retaining the actual verifier, request framing, and response parser. Its original assertions remain, with added actual-factory commitment, factory-refusal, public-wrapper drift, hostile caller-override, and malformed-stdout controls. All eight tests passed independently in 239 ms. Six production/integration file hashes stayed unchanged, and a separate read-only call to the actual installed-runtime verifier still refused `Step-44 reviewed Poppler file set drifted from its exact roster.` The before snapshot is a documented LF reconstruction of the worker's captured read, not an original-byte attestation; the final snapshot exactly matches live SHA-256 `6da1a4778c711112b042fb28c4ec92f4bfab36de9a3ab9b6a41d1b95259d4b9b`.

The separate ignored recovery now retains 177 of 178 frozen files: 96 installed exact copies and 81 recovered from upstream archives, totaling 96,809,656 bytes with 122 DLL/EXE files. Independent hashing confirmed every recovered name, size and digest against the unchanged roster, all six directory names, and unique single-link regular files without aliases or extra names. The required `etc/fonts/conf.d/README` remains missing: 1,111 bytes at SHA-256 `9ceb73ef4579b882c732286e500719f027dd875e2ac23a84003680b355eeb311`. The 1,021-byte upstream and 1,075-byte installed alternatives remain unadmitted. No runtime was installed or executed, and no root commitment or pin changed.

The final recovery manifest is `output/step44-workflow-repair-20260905/poppler-exact-recovery-195139/final-manifest.json`, SHA-256 `cbd026c6cdb6fe3e030d4c3e51dc62f8e8d3c1be3eae123b3d2a24f2ab4ebb3a`. All 38 package-report and seven source-script digests checked; additions reconcile to 81, and the distinct represented archives total 54,723,536 bytes. The independent review confirms retained provenance metadata, not a second extraction of all archives; original archive URLs for the 96 installed copies remain unknown. `output/step44-workflow-repair-20260905/poppler-scoped-acceptance-review.md` records bounded acceptance of the unit repair and incomplete recovery. Windows/pinned-helper dependencies, real-runtime refusal, incomplete full verification, and the pending user decision on a successor source-evaluation protocol remain unchanged.
