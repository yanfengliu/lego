# 2026-09-24

## Build player, milestone 1: npm start plays all 359 steps beside the booklet

The owner reset the product to one thing: play set 21066's build step by step and compare each step with its booklet page (plan: `docs/work/1_booklet-reset/plan.md`, rules: `docs/policies/local-rules.md#the-product`).

- `npm run booklet` gained a player stage (`tools/booklet/player-stage.ts`) that writes `output/booklet/player/21066/`: `model.mpd` (1,464 parts, one `0 STEP` per printed step, 526 official LDraw files and 6 Builder-mesh stand-ins packed in, 1.9 MB) and `steps.json` (page, parts added, alignment, sub-build flag). The brick separator, which no step places, is left out.
- The player (`apps/web/src/player/`) is the default page; the editor moved to `/editor.html`. `npm start` (`tools/player/start.mjs`) regenerates stale data, then serves and opens it.
- Looking found one fault no gate saw: the Parts Tracker's unofficial 7236 stood 240 LDU out of the back of the apple, and 6801 and 7302 lay across the wall, because those files use another frame than the official export posed them in. All six designs the official library lacks now come from the Builder mesh pack, posed from the LXFML, and a bounds probe finds no part outside the model's hull.
- Sub-builds show in final position (259 of 359 steps build one); showing them apart is a later improvement.

## Frontier 3: catalog `/32`, sub-build attach timing, valid through 44

**Action:** Two branches merged into `worktree-agent-aded985416829baa0`. Catalog `/32` (4332f6e, 69c27ca, 19fb4b2): 41682 gains the two clutch seats its pinned LDCad root authors in its wall's back recess (a square-S6 socket, two cells, facing +Z; `undersideClutch:4` and `:5`, so `:0`-`:3` keep their ids), a measured clutch row may carry its outward normal, and 41682's collision is measured as BFC solid intervals per column (62 boxes, from 54). The two 1-LDU boxes that stood out of its back face came from a 4.4e-16 LDU `_clip` sliver of the flange-top triangles counted as plan area by the height field. `/31`→`/32` only adds endpoints: a `/31` document with edges on 41682's old ports migrates unchanged; `migration-history:check` reports 33 source truths and 476 carried deltas, as before. New class gate `clutch-seat-collision-class.test.ts` seats a probe in every catalog clutch; its red proofs are in `gate-proofs.md`. Attach timing (b35ef5f): `tools/booklet/sub-build-attach.ts` attaches each sub-build at the printed step the booklet joins it, since inside a repair window the model's unit order is not the booklet's. `status.json` now records the reference build's range and any cut (0c13d38), and the opt-in playback spec walks exactly that range.

**Result:** `npm run booklet` after the merge: valid through step 44 (280 parts), 44 valid, 0 invalid, 315 catalog-blocked, first blocked 45 (4519 origin-not-integral x3); no step adds an issue. Steps 41-44 needed no fix of their own: they carried step 40's issues. The reference build holds printed steps 1-44 with no cut. Its stop at 37 was not attach timing: the file draws a sub-build where it ends up, the strip built over steps 38-43 touches nothing until the 41682 brackets seat it at 40 (the file's states: 38 and 39 are 257 + 2 and 257 + 8 parts apart, 40 is one piece of 269), and the exporter writes only a connected last state. Before `/32` the valid prefix ended at 39, inside that gap.

**Looked:** the editor served from this worktree, via Import, Build and Next step, default angle zoomed and orbited to the back; verdicts 31-37 verified, 38-39 subassembly, 40-44 verified. Steps 31-44 against pdf.js renders of pages 35-45 and a scale-4 crop of page 45's strip: parts and places agree, unmirrored; the strip stands on its side on the upper deck with axle holes up and white slopes forward, as page 45 draws it. The file looked at was `reference-build.mpd` sha256 `ca2959e8a281bb0c…` (the `--write-baseline` run reproduced it); the screenshots were not kept. An editor still at `/31` (the main checkout's preview server) refuses the `/32` file with `Connection connection-844 is not uniquely inferred`.

**Validation:** `tools/booklet` vitest 88 (87 passed, 1 skipped); the opt-in `reference-build-playback.spec.ts` passed on the 1-37 file and failed as intended with its recorded reason blanked. `historical-connection-semantics.test.ts` split to 351 lines. The catalog branch reported typecheck, lint, format, docs:budget, lessons, notices, parts, bom, migration-history, test:python and vitest (4,036) green; the full `npm run verify` on the merged branch is recorded in the handoff.

## Batch 4 integration: identity alignment merged with catalog `/31`

**Action:** Integrated two independent branches into `integrate/batch-4`: identity alignment (994a460, G3c-2: `npm run booklet` identifies every callout's part and aligns each step by identity, falling back to counts only where a callout is flagged) and catalog `/31` (a840684: seven measured parts gain the `nominal-stud-tube/1` connection profile, plus a reviewed 41682 frame correction). A read-only check of the identity-alignment branch first confirmed: a step's verdict is `"identity"` only when it has zero flagged callouts (`align-stage.ts` `verdictOf`), so the headline's exact/count-fallback/mismatch buckets cannot mislabel a count-matched step as identity-matched; the residual risk (a wrong part of equal count among the 43 flagged steps) is disclosed in both the headline and the defect register, not hidden; `npm run lint` and the full `tools/booklet` + identify test suites passed against the branch; mutating `stepCountsMatch` to ignore identity and always compare by count turned 5 of 9 `align-identity.test.ts` tests red, reproducing the branch's own devlog claim verbatim (including the exact `'BDDP','ABBL'` vs `'BBLP','ABDD'` assertion message); no official-model content is committed anywhere in the diff (`/output/` stays gitignored, `status/booklet-baseline.json` carries only aggregate counts). Both branches merged with `--no-ff` and no design-level conflicts: only `status/booklet-baseline.json`, `docs/devlog/summary.md` and `docs/design/building-system.md`'s executive-status paragraph needed manual resolution, all mechanical (the alignment stage's own numbers are untouched by the catalog change, since alignment runs before catalog/playback and never reads catalog truth).

**Result:** Re-running `npm run booklet` on the merged code reproduced the interim conflict resolution bit-for-bit once reformatted: 316 exact identity / 43 count fallback / 0 mismatch (unchanged by the catalog merge, as expected), playback valid through step 39 (265 parts, 3 corrections: 77844, 80015, 41682), first invalid step 40 `DISCONNECTED_ASSEMBLY` (41682's recessed flange face has no clutch for the 1x2 plate's studs it takes, per the existing playback entry below). First catalog-blocked step unchanged at 45.

**Found by looking at the two artifacts together, not by any gate:** the app-playable reference build (`output/booklet/reference-build.mpd`) reaches only printed steps 1-37, cut short at 38 — two steps behind playback's own valid-through-39. `tools/booklet/reference-build.ts`'s own doc comment explains why: playback holds some sub-build apart until the step that attaches it, which its per-step validity tolerates, but the single document the reference build exports cannot represent as connected, so it stops one step short of where that would show. This is the same mechanism already flagged for step 31 on the identity-alignment branch (which resolved when the range there was 1-31 vs 1-32, a 1-step gap that closed once step 31's own parts were fixed); it recurs here as a 2-step gap further down the model, not a new defect and not related to 41682's collision at step 40.

**Validation:** `npm run lint` clean; `tools/booklet` + `apps/web/src/instructions/identify` vitest suites 168/168 passed before merging branch A. Docs fixed: `docs/design/building-system.md` executive status and every stale mention (corrections count 2 → 3, reference-build range 1-32 → 1-37, the obsolete "step 33 `PART_STUD_BODY_COLLISION`" frontier line, the stale ordered-work item); `docs/work/1_booklet-reset/plan.md` Done/Todo lists.

## Booklet playback: steps 32-39 clear, step 40 waits on 41682's flange clutches

**Action:** Diagnosed each invalid step from 32 to 44 of `npm run booklet`'s reference playback by rebuilding the colliding pair from `output/booklet/reference-playback.json` and asking the kernel's own collision primitives why it collided.

**Step 32 (and 33-39):** a `3023` plate seated on a `15254` arch collided with every arch stud it held, by 0.00015 LDU. The arch's stud cylinders carry the LDraw source radius 6.0001514980873605, and without the `nominal-stud-tube/1` profile `penetrationCoveredByAllowance` computes a radial clearance of 6 - 6.00015 < 0, so no validated edge can excuse the stud. A census of `PART_DEFINITIONS` found seven measured parts with that radius and no profile (2450, 79491, 28802, 15254, 41682, 2877, 32064), against 14 that already declared it. Steps 33 and 35 were the same class on 15254 and 2877. Catalog `/31` declares the profile on all seven (commit be8684e, with its migration report and a carry class for saved edges); `stud-seat-collision-class.test.ts` now seats a 1 x 1 plate on all 1,125 catalog studs. Playback moved from valid through 31 to valid through 40.

**Step 40-44, believed and proved false:** step 40 passed after the `/31` fix, but only because the export's 41682 frame is wrong. The export frames LDraw 41682 (Bracket 2 x 2 - 1 x 2 Up Centred) upright, so step 41's 1 x 2 plates lay on its 2 x 2 base and ran 4 LDU into the centred flange at every one of its 7 instances. The booklet's inventory thumbnail of element 6250020 and the step-40 callout draw the flange as a horizontal ledge with its two studs up, with the base as a wall behind it, and step 41 presses the plates onto those studs. A reviewed correction turns the frame a quarter turn about x, which puts the LXFML origin on the flange's recessed back face at LDraw (10, -10, 4), one plate below a stud: the same relation a 1 x 2 plate's LXFML origin has with its stud. Checked against the export's neighbours: the whole column of plates at that stud row stacks through the flange with no overlap, and the upper plates now connect to the flange studs. The correction needed `studColumnAt` to read a side stud's column along its normal. The honest frontier is therefore 39: with the frame corrected, step 40's lower plate pushes its studs into the flange's back recess (an anti-stud recess with a tube in LDraw), and the catalog part has no clutch there, while its conservative collision boxes fill the recess and stand 1 LDU proud of the rim face (local z 4 to 5).

**Trip hazards:** the three 41682 instances at step 248 now need orientation `proper-m-n0000p0p0`, which the catalog does not allow for the part; that step was catalog-blocked already. The 41682 correction applies only while the export shows the reviewed frame (turn180, origin (10, 8, 10)).
## Evidence cleanup: ignored output/ and var/ from about 42 GB to 2.8 MB

**Timestamp:** audited 2026-09-23 at 982634d; deleted by the owner before 2026-09-24

**Action:** An audit listed every entry under the ignored `output/` (43.6 GB) and `var/` (213 MB) and traced which paths a current entry point reads by default. The owner deleted everything else. Kept: `output/official-model/` (the LXFML and LDraw files `npm run booklet` reads), one frame-registry file under `output/real-build/history/` (compared only under `LEGO_RUN_EVIDENCE=1`), `output/part-identification/prefix50-semantic-closure.json` (an optional comparison line in `scripts/identify-booklet.mjs`), and seven score files the gates rewrite.

**Result:** About 2.8 MB remain. `output/real-build/runs/` alone had been 37.6 GB. `npm run booklet` reproduces its numbers from the kept inputs (re-run below).

**Trip hazards:** Living docs that cited deleted files by byte count and sha256 were cleaned in the design-docs trim below; devlog entries still name deleted paths, as history. `docs/runbooks/real-build.md` still cites deleted run directories; it documents the retiring `real-build-*` family and was not edited here.

## Canon sync to FLEET.md 973eb18eaf96, after the fleet canon trim

**Timestamp:** 2026-09-24 00:41 (002a6af; fleet 20a77f3)

**Action:** The fleet canon was trimmed in the fleet repo, and the `AGENTS.md` canon block was re-synced from it by the sync script (36 lines added, 78 removed). The session-size rule is now canon R6, so `docs/policies/local-rules.md` no longer repeats it.

## Design docs cut to contracts plus the measured position, and the reset's records

**Timestamp:** 2026-09-24

**Action:** The five design docs were cut from 479,409 to 215,422 bytes. Retired first-50 status presented as current was replaced by pointers to the executive status in `building-system.md`, `npm run booklet`, and `docs/work/1_booklet-reset/plan.md`; history is left to this devlog and branch `archive/first50-campaign-wip-20260908` (f44f1b8), not re-homed. Citations to deleted evidence were removed. Durable contracts (domain, trust, authority, consent, migration, evidence lifecycle, part organisation, threat-model rules) were kept, shortened where they repeated. Also: the defect register gained the owner's four reported problems (token burn, two weeks uncommitted, the 25.6 KB README, 42 GB of evidence), `npm run evidence:budget` was added, the reset plan's todo list was brought up to date, and `docs/policies/local-rules.md` lost its duplicate of canon R6.

**Result:** Bytes, before → after: `spec.md` 114,165 → 60,061; `building-system.md` 119,053 → 12,103; `learning-system.md` 101,867 → 33,936; `part-model.md` 78,362 → 45,223; `threat-model.md` 65,962 → 64,099. `npm run booklet`, re-run at 384a70d, matches the executive status: valid through step 31 (184 parts) with the two export-frame corrections, step 32 `PART_STUD_BODY_COLLISION`, 31 valid / 13 invalid / 315 catalog-blocked of 359, first catalog-blocked step 45.

**Reasoning:** Statements found false and fixed: `spec.md` said a saved edge on a 15573 grid clutch "refuses rather than migrating" (the `/29`→`/30` migration carries 56 such endpoint deltas forward across 28 rows); `building-system.md` gave two frontiers at once (the retired step-28 selected path and the harness's step 31) and said "ten port kinds" where the catalog types eleven connector kinds; `spec.md` described the retired model-pilot transport as current identification and never named the closed-set identifier.

**Validation:** `npm run docs:budget`, `npm run lessons:check`, the fleet work-docs check, Prettier on every changed doc but `threat-model.md` (with an empty ignore path, because `.prettierignore` excludes `docs/`; Prettier 3.9 pads that file's ten-column threat tables to 141,590 bytes, so it keeps compact tables and is not Prettier-formatted), and a relative-link and anchor check over every changed doc and every doc linking into them. `scripts/check-evidence-budget.test.mjs` passes 3/3 and goes red when the budget comparison is removed.

**Notes:** `evidence:budget` is not part of `npm run verify`; a clean checkout always passes it, so it runs only when invoked. Wiring it into session start is open in the plan. `spec.md` stayed at about 60 KB, above the review's 40 KB estimate, because what remains is contract text.

## G3f-3: Playwright specs moved onto the LEGO_RUN_EVIDENCE opt-in

**Timestamp:** da37b92

**Action:** 24 of 26 booklet-gated `.spec.ts` files were converted from a bare `hasSampleBooklet`/`existsSync` skip to `apps/web/e2e/run-evidence-gate.ts`'s `skipWithoutRunEvidence`. One compound case kept its `onlyIf`. `real-build-step7-gate3-diagnostic.spec.ts` was already unconditional-by-default and fails loudly, not skips, when opted in, so it was left alone. `run-evidence-gate-coverage.test.mjs` now also scans Playwright's spec population, read from `playwright.config.ts`'s own `testDir`/`testMatch`; `hasSampleBooklet` is a named existence token because it is always imported, never locally declared, so the scan's one-hop resolution could not otherwise reach it.

**Result:** `npm run test:browser` default gate: 59 passed/14 skipped (main) → 45 passed/28 skipped here (exactly the 14 converted specs, same booklet-present machine).

## G3b: the reference build played in the editor, one printed step at a time

**Timestamp:** 2026-09-24, on 461991a

**Action:** `npm run booklet` now also writes `output/booklet/reference-build.mpd` (`tools/booklet/reference-build.ts`): the valid prefix of reference playback, in the editor's own LDraw subset, with one editor step per printed step. The document name says it is a reference build from LEGO's official model and not read from the booklet. Each step is named `Printed step N (p. P) · reference`, plus notes for no new parts, a sub-build drawn in its final place, and colour stand-ins (LDraw 47 is drawn in Very Light Gray and tagged `colour-stand-in-ldraw-47`). The model is moved by whole studs to sit centred on the editor's grid. The editor's Import and Build playback needed no format change. The one product change is the playback bar's readout (`apps/web/src/components/build-playback-readout.ts`), which now says "no new parts" on a zero-piece step instead of showing nothing where the "+N" would be. `apps/web/e2e/reference-build-playback.spec.ts` (only with `LEGO_RUN_EVIDENCE=1`) imports the file through the Import button and steps forward and back. At each printed step it checks the step name and page, the part count and "+N" against the booklet's own printed callout counts, and that the rendered document changes.

**Result:** Printed steps 1-31, 184 parts, 29 colour stand-ins, one hard-valid document. Every step shows "verified" in the playback bar. The spec passes, and it goes red (`+182` where `+1` was expected) when the export puts steps 2-31 into one step. Screenshots from the served app were compared by eye with pdf.js renders of booklet pages 11, 14, 15, 24, 33, 34 and 35. At steps 1, 10, 20 and 29, the part kinds, counts and colours match the callouts, apart from the colour stand-ins.

**Found by looking, not by any gate:** (1) The editor draws the model as a mirror image of the booklet. Step 31's layout matches the booklet only after a left-right flip. The renderer maps LDU to Three.js as (x, −y, z), which has determinant −1. A separate worker owns that fix. (2) Step 31 passes the kernel, and its piece count matches its callouts (4). But 2 of its 4 pieces differ in kind and colour from the printed callout. The aligner matches steps by callout counts only, never by element, so no current check can see this.

**Validation:** `npm run verify` passed. `test`: 3,983 passed and 35 skipped, in 500 files with 12 skipped. `test:browser`: 45 passed and 29 skipped; the new spec is skipped by default. The new spec passes under `LEGO_RUN_EVIDENCE=1` in 1.3 min. A reference build cut short at a state the editor cannot hold as one document (for example, two components resting apart on the plate) is shortened, and the harness summary names the first step it left out.

## Renderer handedness: LDU reaches Three through the half-turn about X

**Timestamp:** branch `worktree-agent-a67c783b78a7229d5` off 461991a

**Action:** `packages/rendering/src/coordinates.ts` mapped catalog LDU to Three as `(x, -y, z)` and rotations as C·R·C with C = diag(1, -1, 1): det C = -1, a reflection. The map is now the half-turn about X, `(x, -y, -z)`, the frame Three's own `LDrawLoader` gives LDraw data. One sign vector drives it, `MESH_RENDER_AXIS_SIGNS = [1, -1, -1]` in the catalog, read as `LDU_TO_THREE_AXIS_SIGNS`; `threeToLduVector`, `lduDirectionToThree` and `lduToThreeBasisMatrix` joined it. Every place that carried its own copy of the flip now reads it: source-mesh normals and winding (the triangle reversal that hid the reflection from `FrontSide` culling is gone), procedural wedge and arc prisms (winding now chosen in scene space), picking (`threePointToLdu`), the palette's SVG preview (`part-preview-projection.ts`), the booklet loop's lattice steps (`sceneStepToLdu` in `arrow-placement.ts`, `projectLdu`), the admission harness's LDrawLoader side (now baked into scene units with production's own arithmetic, `bakeLDrawSourceIntoCatalogThree`, after an object-matrix path left one `underside-oblique` pixel one sRGB step apart), the viewport grid plane, the axis legend (now `X`, `-Y`, `-Z`, the inspector's axes), and five e2e helpers. `RENDERING_VERSION` moved to `lego.rendering/2` and the admission view policy to `/2`. Saved documents did not change.

**Result:** The step-31 reference build of 21066, imported through the app's Import LDraw control from `npm run booklet`'s `reference-playback.json` (184 parts, trans-clear drawn white), now matches booklet page 35 without flipping: rounded white end right, blue brick left, arches at the back; before, the other worker's capture had them mirrored. The 2x4 wedge plates placed through the palette show LDraw's hands in the viewport, the canonical views and the palette preview. The canonical view directions kept their numbers; they now look from the side each is named for (`front` from LDraw -Z, where a slope faces), where before `front`, `back` and `isometric` looked at the far side, mirrored. Synthetic fixtures built on the mirrored picture moved: `build-search` turned-over azimuth 41 -> 139 (face-aware 6/6 there, 4/6 at 41), the deferral fixture's documents mirrored in z at the same azimuth 55, which reproduces all 15 old settlements exactly (a picture mirror at 180 - 55 = 125 cannot, because `registerPrefixAgreement` scans shifts left to right and a mirrored mask can settle on another shift), handedness IoUs (as-fitted 0 from 0.4761 to 0.3457, winner unchanged), registration and C4 tables.

**Gates and red proofs:** `packages/rendering/src/handedness.test.ts` (determinant +1 for vectors and all 24 proper transforms against `makeRotationX(PI)`; the left/right wedge plates draw LDraw's own vertices; opposite hands in the top view; procedural solids have positive signed volume), `apps/web/src/viewport/picking-round-trip.test.ts`, `apps/web/src/components/part-preview-hand.test.tsx`. With the whole old mapping restored (signs `[1, -1, 1]` plus the old triangle reversal) all eight hand checks went red, among them `expected -1 to be close to 1` (determinant), 576 unmatched vertices per wing, and the pick `expected 130 to be close to 90`. Inverting the prism side rule made both signed volumes negative (-1.53, -1.67).

**Trip hazards:** The deferral file's containment control refused its displaced drawing only on an exact tie at 1 between seven candidates, each walked 180 to 331 px, far past `REGISTRATION_REACH_PX` = 48, so that negative control is fragile and the constant's comment is wrong. Flipping the signs alone, without the old triangle reversal, left the pick gate green: the meshes were inside out, so the raycast skipped the near face and landed on the far one, which is where the front is. A red proof has to restore the whole old behaviour. The procedural wedge wound its side walls inward before this change too; no catalog part reaches that path today. `apps/web/src/physics/rapier-world.ts` maps LDU to physics as `(x, -y, z)` and hands back physics-frame quaternions that `simulation-session.ts` treats as document-frame: yaw-only bodies are right, a tipped one is drawn tilted the wrong way. It is not a Three.js boundary and was left for its own task — fixed later the same day; see "Physics review fix" below.

**Hypothesis (2026-08-12 mirror build):** consistent and sufficient, not replayed. Under the old renderer, `old_render(W) = proper_render(M_z W)`, so a build matches a booklet picture only if it is the z-mirror of the drawn one, or its x-mirror once the camera fit turns 180 degrees; the 2026-08-12 audit found the retained prefix equal to the official one under a global x reflection, modulo a proper yaw. The retired pipeline and its runs are gone, so the convergence itself was not re-run.

## Physics review fix: the simulation session carried the same handedness bug

**Timestamp:** branch `worktree-agent-a67c783b78a7229d5`, review follow-up on 08d94b4

**Action:** The trip hazard above ("a tipped [body] is drawn tilted the wrong way") is fixed, not deferred. `rapier-world.ts` returns Rapier's raw Y-up sim-frame quaternion unchanged; `simulation-session.ts` forwarded it as `PartPose.rotation` although its own doc comment claimed it was already in the document's Y-down frame, and used that same unconverted rotation to carry a part's rest offset with its body. Both now go through `simRotationToDocument`, conjugating by diag(1, -1, 1) — elementwise `(x, -y, z, -w)` — the same axis flip `toSimulation`/`toLdu` already apply to points. `apps/web/e2e/cart-demo.spec.ts` dropped its ad hoc Matrix4 conjugation for the new `lduRotationToThreeQuaternion` (`coordinates.ts`), the same basis change `lduTransformToThreeMatrix` applies to a part's rest orientation, generalised to a free rotation.

**Gates and red proofs:** `apps/web/src/physics/tilt-direction.test.ts` carries a known Rapier rotation through the session into document space and then through the document-to-scene conversion, checking the actual tilt direction and which side renders lowest. Anchored to a tilt about the LDU Z axis, not X: an X-axis tilt happens to render identically under main's old ad hoc `(-x, y, -z, w)` formula and under this fix (the two wrongs it compounds cancel for that one axis only), so it would not have caught that regression; Z does. Confirmed red both with this branch's pre-fix formula (raw quaternion forwarded) and with main's old ad hoc formula, restored to green after; scratch proofs and the reviewer's own probe are under `scratchpad/review-handedness/` (task-run evidence, not committed).

**Non-blocking, fixed too:** `picking-round-trip.test.ts` stayed green when `MESH_RENDER_AXIS_SIGNS` was reverted to its old `[1, -1, 1]` on its own, because a position round trip cannot tell a genuine hand from a self-consistent mirror-and-unmirror — undoing a sign flip with the same sign flip always recovers the original point. Added a check on the picked face's own outward normal, converted back to LDU the same way as a point: Three.js's front-face culling always hands back a normal facing the camera in scene space regardless of the bug, so the conversion is what exposes whether it agrees with LDraw's independent "front is -Z" convention. Confirmed red against that same revert, restored to green after.

## G3c-2: alignment by identity, and identification scored against the official model

**Timestamp:** 2026-09-24, on d84979c

**Action:** `npm run booklet` gained an identify stage (`tools/booklet/identify-stage.ts`). It runs the product's closed-set identifier, `identifyBooklet`, on the booklet PDF alone: no model call, no answer key. It turns each printed step's callouts into an identity target: the pieces of each element identification trusts, plus the quantities of the callouts it flags (its residuals), which are matched by count only. The aligner (`align.ts`, `align-repair.ts`, criterion in `align-identity.ts`) holds each step to that target. The repair treats trusted callouts as fixed slots. When the smallest balanced window cannot be redistributed, a cluster held to identity tries larger balanced windows. The alignment by counts alone still runs as a comparison line. `identify-score.ts` scores identification against the model per step: agreement with the model's run order, the parts the booklet moves to another step (with direction), and each callout after alignment. The report lines are in `summary-identify.ts`. New limit tests cover `maxOperatorsPerPage` (`pdf-scan.test.ts`) and `maxDecodedPixelsPerPage` (`identify-page-pixels.test.ts`, which lowers the limit through a module mock).

**Result:** 359 steps: 316 exact identity (303 by run order, 13 re-laid by repair; 26 of the 316 print no callouts, so 290 steps with callouts match exactly), 43 count fallback (65 flagged callouts), 0 mismatch; 9 repair windows, all solved, 44 bricks moved. By counts alone, as before: 338 by run order, 18 by repair, 3 unmatched (steps 156-158, repair budget). Identity gives 22 steps other parts than counts did: 31-33, 52-54, 62, 63, 96, 98-100, 156-158, 227-229, 231, 232, 329 and 330. Identification against the model: 313 of 333 steps with callouts agree by run order. The other 20 differ only where the booklet prints a part in another step than the model builds it: 20 pieces earlier, 24 later, 0 unexplained. After alignment, 859 of 859 callouts agree. The 794 trusted callouts agree by construction; the 65 flagged ones are informative. Playback with the two frame corrections is valid through step 32 (194 parts), up from 31 (184): 32 valid, 12 invalid, 315 catalog-blocked. The harness takes about 25 s, up from 11 s, because identification takes about 13 s.

**What was believed and proved false:** "Valid through step 31" was read as per-step progress. It measured the kernel validity of whatever bricks callout counts picked. Step 31's run held two dark bluish grey 1x3 bricks with counts {2,1,1}, the same as the booklet's callouts. The booklet swaps two sibling sub-builds at pages 35-36, so counts matched on the wrong parts. Step 32's `PART_STUD_BODY_COLLISION`, the reported frontier, was a light bluish grey 1x2 plate (uuid 6c885693) that counts put at step 32. The booklet draws it at step 33. Replayed alone at step 33, it raises the same collision with the black 15254 arch from step 30, so the collision follows the plate, not the step. Replayed one brick at a time, all 11 of step 33's new collisions involve a 15254 arch.

**Caught in review of the first commit:** the headline's count of exact steps that print no callouts indexed the filtered list of exact steps as if it were the list of all steps, and reported 22 where there are 26. A zero-callout step after a count-fallback step in `align-identity.test.ts` now pins it.

**Caught by looking:** The first identity repair kept the earliest-built bricks of each element where they were and moved the last-built. Step 31 then got the sub-build's first 1x2x2 plus a 1x2x2 built four units later at the far end of the model (unit 55), not the sub-build's second one (unit 51). Counts, identity and kernel validity were all right; the editor screenshot at step 31 showed a black brick standing apart. The repair now pairs surplus and shortfall in step order and gives the brick the model builds nearest the receiving step. Afterwards step 31 is exactly units 50-52: the booklet's sub-build of a 2x4 plate, two black 1x2x2 bricks and a light grey 1x2 plate. It was compared by eye with a pdf.js render of page 35. All 44 moved bricks share their deepest sub-build with a brick of the step that receives them.

**Validation:** `tools/booklet/align-identity.test.ts` (9 tests). Swapping the criterion for count-only logic turns 5 of the then-8 red, including "expected [ 'BDDP', 'ABBL' ] to deeply equal [ 'BBLP', 'ABDD' ]". Moving the latest-built brick instead turns the sub-build case red. The operator-limit test goes red when the limit check is removed (another error, about restore without save, is thrown instead). The pixel-limit tests go red on `>=` and on a removed check. `tools/booklet/main.test.ts` feeds the harness a blank one-page PDF: identification fails by name, the run exits 1, and alignment falls back to counts and says so. `apps/web/e2e/reference-build-playback.spec.ts` passes under `LEGO_RUN_EVIDENCE=1` on the new 32-step file. Typecheck, lint, and the tools and identify tests pass.

**Trip hazards:** The 43 count-fallback steps still match their flagged callouts by count, so a wrong part of equal count there would pass. After a repair, a step's sub-build attach timing still follows its run (`unitEnd`). Step 31's sub-build is held apart in playback and joins the model at step 32, though the booklet attaches it in step 31. `--write-baseline` writes JSON that Prettier reformats, so run Prettier on `status/booklet-baseline.json` before committing.
