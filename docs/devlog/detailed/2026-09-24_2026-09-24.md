# 2026-09-24

## Booklet playback: steps 32-39 clear, step 40 waits on 41682's flange clutches

**Action:** Diagnosed each invalid step from 32 to 44 of `npm run booklet`'s reference playback by rebuilding the colliding pair from `output/booklet/reference-playback.json` and asking the kernel's own collision primitives why it collided.

**Step 32 (and 33-39):** a `3023` plate seated on a `15254` arch collided with every arch stud it held, by 0.00015 LDU. The arch's stud cylinders carry the LDraw source radius 6.0001514980873605, and without the `nominal-stud-tube/1` profile `penetrationCoveredByAllowance` computes a radial clearance of 6 - 6.00015 < 0, so no validated edge can excuse the stud. A census of `PART_DEFINITIONS` found seven measured parts with that radius and no profile (2450, 79491, 28802, 15254, 41682, 2877, 32064), against 14 that already declared it. Steps 33 and 35 were the same class on 15254 and 2877. Catalog `/31` declares the profile on all seven (commit be8684e, with its migration report and a carry class for saved edges); `stud-seat-collision-class.test.ts` now seats a 1 x 1 plate on all 1,125 catalog studs. Playback moved from valid through 31 to valid through 40.

**Step 40-44, believed and proved false:** step 40 passed after the `/31` fix, but only because the export's 41682 frame is wrong. The export frames LDraw 41682 (Bracket 2 x 2 - 1 x 2 Up Centred) upright, so step 41's 1 x 2 plates lay on its 2 x 2 base and ran 4 LDU into the centred flange at every one of its 7 instances. The booklet's inventory thumbnail of element 6250020 and the step-40 callout draw the flange as a horizontal ledge with its two studs up, with the base as a wall behind it, and step 41 presses the plates onto those studs. A reviewed correction turns the frame a quarter turn about x, which puts the LXFML origin on the flange's recessed back face at LDraw (10, -10, 4), one plate below a stud: the same relation a 1 x 2 plate's LXFML origin has with its stud. Checked against the export's neighbours: the whole column of plates at that stud row stacks through the flange with no overlap, and the upper plates now connect to the flange studs. The correction needed `studColumnAt` to read a side stud's column along its normal. The honest frontier is therefore 39: with the frame corrected, step 40's lower plate pushes its studs into the flange's back recess (an anti-stud recess with a tube in LDraw), and the catalog part has no clutch there, while its conservative collision boxes fill the recess and stand 1 LDU proud of the rim face (local z 4 to 5).

**Trip hazards:** the three 41682 instances at step 248 now need orientation `proper-m-n0000p0p0`, which the catalog does not allow for the part; that step was catalog-blocked already. The 41682 correction applies only while the export shows the reviewed frame (turn180, origin (10, 8, 10)).
