# Calibration browser request policy — 2026-09-05

Calibration now uses an explicit owned browser context with service workers blocked. Context HTTP and page-WebSocket routes are installed before the first page and navigation. HTTP permits only credential-free GET/HEAD requests to the owned origin and seven exact paths from the six frozen static assets; WebSockets never connect to a server. Bounded enum counters retain refusal reasons without URLs, headers, bodies, or file contents. A caught request error cannot clear the final refusal. The legacy browser path and accepted calibration bootstrap remain unchanged.

Independent review and coordinator inspection accepted this delivery and final-lifecycle policy. The [six-file handoff](../../../output/calibration-browser-policy-20260905/guard-63d84f85/HANDOFF.md), SHA-256 `cccce5876f4b75f490a21c7da1dd833248b0de58e4238a902b683deeb0bd1129`, and [after manifest](../../../output/calibration-browser-policy-20260905/guard-63d84f85/after-manifest.json), SHA-256 `42c62be5b9a4acb30f72faa577f134808affae613a0fa9e6ce3b564eedd93c9f`, bind the reviewed code and task-only diffs. The coordinator independently reran the four explicit test files: 39/39 checks passed. The source-closure suite included admitted identity-only byte hashing; the browser controls used synthetic inputs and performed no booklet measurement. The worker's scoped format, lint, TypeScript and diff checks passed. No full repository gate or source-bearing qualification was run.

The [coordinator's retained results](../../../output/calibration-browser-policy-20260905/coordinator-acceptance) cover one synthetic positive flow, nine HTTP/WebSocket policy controls, and two local-file controls. The positive imports two exact one-part documents through the real UI, skips a repeated import, and checks the resulting structural hash and validator. The coordinator opened all seven 640 × 480 captures individually; the centered blue part is nonempty and unclipped in isometric, front, back, left, right, top and underside views. Their hashes match the worker's [native binding](../../../output/calibration-browser-policy-20260905/guard-63d84f85/native-final-binding.json). These pictures verify the synthetic instrument, not booklet correspondence.

The nine request-policy controls cover foreign fetch, popup, second page, an owned-origin filesystem-style path, recipe path, unknown asset, query variant, POST and page WebSocket. Each first proves its canary reachable over HTTP and a WebSocket upgrade, then records an explicit refusal, zero canary hits and a closed canary. The two file controls attempt the intended operations and complete their callbacks; both operations are rejected before sentinel contents are read. Fetch relies on browser-native rejection with no policy count; navigation produces a foreign-origin policy refusal. Temporarily disabling the guard made both selected mutation controls fail: foreign fetch reached the canary and direct file navigation read the sentinel. Exact code bytes were restored afterward.

Review found a consequential remaining order gap: `policy.finish()` runs after the trusted callback and cleanup, while Step-43 consume remains inside that callback. A clean policy checkpoint before that consume is not implemented by this increment. The callback also retains Node and context methods; this is not a malicious-harness capability boundary or general network/filesystem isolation. Stage-1 readiness still requires that earlier checkpoint and the remaining Node/child-read, output-ownership and diagnostic-release boundaries. Source admission, exact Poppler restoration, signed camera correspondence, genuine Step-44 selection and full first-50 verification remain incomplete. The accepted disclosed protocol and disqualified incident are unchanged.

All 12 strict result records report browser, process-tree and server cleanup; the legacy and mutation cases also report cleanup. The separate Windows process census returned access denied, so no independent OS census is claimed. No server was intentionally left running. Ignored captures, sentinels, mutation failures and review evidence remain retained for the unresolved readiness work. No commit or push occurred.
