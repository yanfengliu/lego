# Devlog — 2026-08-13 through 2026-08-14

## Manifest-v6 component conservation corrected a duplicated step-18 crop

**Timestamp:** 2026-08-14 01:49–08:13

**Action:** Replaced independent per-label physical cropping with bounded joint component-group assignment over exact shared source boxes, actual PDF text masks, ranked-only physical publication, exact group non-reuse, and a manifest-v6 source-component record that binds absolute bounds, foreground count, raw member count, and a page-raster-domain SHA-256. Publication now snapshots and validates exact schemas and paths before bounded writes, derives one common content-addressed run from manifest-visible fields, and exposes only strict downstream records. The browser authenticates the exact fetched PDF bytes against the Node authority; independent clipped-PDF replay reproduces the selected component groups for printed steps 18 and 347, while the documentation deliberately does not claim a full independent rerender of all 881 crops.

**Result:** Full-booklet dry validation covered 881 callouts / 1,512 pieces, then publication wrote immutable run `e49496b28d8fecb0ccc158a1` and manifest digest `sha256:c8d20cfe87ef9d21488725b393b94e61870fcc82b26bb497ea734fc7b97a67bf` with 859 physical callouts / 1,464 pieces, 22 semantic callouts / 48 pieces, 196 pages, and zero failures. The old step-18 quantity-two crop had reused the adjacent `3069` tile; v6 retains the visually distinct quarter tile and independently derives element `6514469`, official design `25269`. Deterministic regeneration produced features `sha256:13574857858b9b71ce3132ee797ff346597d0d4c20eace70eaf83408d4a523cb`, match `sha256:a188417b3ca89e7c8b5fad54cdd8f9cd9d3d96dd258e0394deb8e7c8954787b9`, distances `sha256:5da4baba89c3229da4cb38273a151976763008ef63c82e6330e41f9916b0ee8f`, 288 refined groups, and card run `1c3c26e84cdcb27ca3e3ef57`. The corrected crop is lead of group 284 and ranks `6514469` first, but its margin over `6172366` is only `0.0011277315972525598`.

**Adjudication boundary:** The old answer bundle remains byte-exact in digest-suffixed history and was not rebound to the new match or cards. A single bounded call for card 284 failed with provider status 401 and the static remediation `claude auth login --claudeai`; no new answer file was retained. Consequently no score, coverage, depletion report, action ledger, retrieval report, document, catalog admission, or completion claim was regenerated. The retained coverage remains manifest-v5 history and the retained ledger remains byte-identical at `sha256:aae1d15884bef09e6258d77cc27012acede7ac5360b6b3ebd8e13c15ab1301b2` with its known 97 canonical validation failures.

**Validation:** The exact producer crop, independent step-18 replay, and independent step-347 replay pass headlessly and were visually inspected; a frozen full-booklet dry run and the publication each pass in about 2.1 minutes. The consolidated v6 boundary passes 281 of 281 tests across 29 files, and the exact source attestation passes 8 of 8 checks over 3,384 files at `sha256:8f7aa5cbe8c9f793c60d17a7d42306b730dc6a7825862db9c07f3b88b6174518`. The authoritative `npm run verify` passes in 720.8 seconds: all schema, Node-consumer, observation-consumer, BOM, part, lesson, notice, formatting, lint and TypeScript checks are green; Python runs 476 tests with 31 intentional historical/live-generation skips; Vitest passes 2,531 assertions with 3 skips across 280 files; Playwright passes 40 headless tests with 9 explicit opt-in skips; and the production bundle guard passes. A 114-file delivery scan found no credential material, dependency-manifest drift, or promoted file above 256 KiB.

**Code reviewer comments:** Independent adversarial review found and drove regressions for physical legacy fallback, detached benchmark arithmetic, source-byte drift, ambiguous and duplicate component ownership, disconnected component groups, hostile PDF and JSON resource amplification, publication path and prototype hooks, mixed run identities, exact consumer geometry, action-ledger item-number authority, and consumption after recorded closure failure. The final frozen review additionally caught unbounded action-ledger row/failure work and missing semantic-crop byte authentication; bounded preflight/failure accumulation and missing/tampered semantic-crop regressions close both, and re-review found no remaining P0, P1, or P2 issue. Fresh model adjudication and the unbuilt full-raster independent verifier remain explicit boundaries rather than weakened checks.

## Exact report replay exposed an invalid ledger and closed false truth paths

**Timestamp:** 2026-08-13 20:45–2026-08-14 01:48

**Action:** Replaced Python report-side schema-and-digest-edge checks with bounded canonical Node replay for identification, adjudication, coverage, score, and action-ledger artifacts. The bridge authenticates exact retained paths and bytes, caps request, output, diagnostics, time, heap, JSON structure, descriptor work, and subprocess cleanup, and keeps synthetic fixture expectations in a separate explicit test verifier. Official Builder XML now uses one strict streaming parser with bounded UTF-8, nodes, depth, attributes, identities, and exact unnamespaced hierarchy.

**Result:** Canonical compilation reproduces the retained action-ledger digest `sha256:aae1d15884bef09e6258d77cc27012acede7ac5360b6b3ebd8e13c15ab1301b2`, disproving the initial suspicion that its bytes had merely drifted. Canonical validation nevertheless returns 97 failures: 24 `official-frame-calibration-missing` and 73 `action-ledger-incomplete`. Retrieval and description reports now refuse before publishing metrics, name those bounded categories, state that exact bytes already reproduced, and create no output file.

**Truth correction:** Reviewer replay showed that treating every refusal as positive Builder truth contributed 48 of 83 Builder records and made 6 of 43 mapped clusters contain conflicting official elements. Positive Builder truth now comes only from accepted `action.pieces`; generic refusals remain counterevidence. Exact accepted-callout descriptors and quantities replace cluster-lead ranks and aggregate pieces: 20 of 44 accepted pieces are non-leads, spanning 15 unique non-lead callouts, and 23 of 35 unique accepted callouts have quantities different from their cluster aggregate. Cluster-level recall is separately and explicitly scored against the published lead row, while lead-only cause diagnostics omit member-local rows rather than substituting another picture.

**Other reviewer corrections:** Exact score replay restored the authenticated vision-confound diagnostic; accepted-only step fields and sensitivity prose no longer claim to describe refused identities; the contaminated `302028` probe now comes only from exact blind negative crop/element verdicts; legacy `/3` documentation now states that current match `/3` semantics reject the retained match `/2`; deterministic depletion fixtures no longer mint an invalid ledger; hostile XML, JSON, digest, and descriptor diagnostics stay bounded; and every verifier cleanup path attempts process reaping and collector joins.

**Boundary:** This work authenticates and refuses retained evidence; it does not repair or republish the ignored ledger, change a pair verdict, promote a refusal, admit `25269` or `28802`, advance step 26, mutate a document, grant acceptance authority, or complete any of the 359 printed steps.

**Validation:** Focused Python suites pass 240 tests with 18 intentional live-generation skips across the report IO, official XML, action-ledger, identification, retrieval, and description contracts. The changed JavaScript and TypeScript integration set passes 175 of 175 tests across 16 files. A live quick-report smoke exits 1 with the exact 24/73 validation categories and leaves its requested output absent. The source attestation passes against 3,335 registered files at `sha256:a68ca5e2dc8fa940036970e4924f41899125a71201ddbd40a365b6d872711969`. The authoritative `npm run verify` passes in 812 seconds: 2,359 Vitest assertions pass with 3 skips across 257 files, 37 browser tests pass with 9 opt-in skips, the complete Python chain passes, and schema, source, BOM, lessons, notices, formatting, lint, typecheck, and production-build gates are green. The pre-commit scan finds no secret material, dependency-manifest changes, promoted run output, or file above the repository's promotion ceiling.

**Code reviewer comments:** Independent review found the digest-edge-only Python closure, refusal-to-truth promotion, non-lead rank substitution, unbounded inventory and XML work, arbitrary cluster-row selection, accepted-only diagnostic overclaims, stale score and legacy documentation, generic verifier stage, and cleanup/error amplification after the first implementation had reported green. Each finding gained a hostile or exact-generation regression; the final read-only audit reported no remaining P0, P1, or P2.
