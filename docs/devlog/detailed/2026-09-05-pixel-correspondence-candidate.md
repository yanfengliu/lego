# Pixel correspondence candidate rejected — 2026-09-05

The single admitted source-free candidate completed its fixed 112-image measurement and exited 1. It passed 0/12 complete positive frames, while all four original negatives and all 96 mutations were refused. Every detector result was a refusal. There were no resource/input failures, and all 112 results completed provenance replay. Negative refusal therefore demonstrates neither useful discrimination nor correct wall association when every positive is also refused. The preceding geometry branch's 1/12 complete positive result remains separate rejected evidence.

The candidate fits ellipses to inside/outside pixel centers with a positive-margin linear program, then requires child-bound top/base wall evidence and exactly four accepted tops. All twelve positive images instead emitted 5–28 fitted top regions. Ten had no unique base; the two smaller `d` views had six unique-base hypotheses between them and only four matched entries passed the fixed scoring bounds. None satisfied the complete-frame contract. These are development measurements, not a signed camera or a claim that real-source ambiguity is impossible.

The [freeze](../../../output/first50-campaign-control-20260905/pixel-correspondence/freeze.json), SHA-256 `84269a286e6b60af387e515c46c69db9a324bb2ead2b2c033be1192f5f7a655c`, bound 110 files, the exact input manifest and the resolved local Python/package identity. Every bound file still matched after execution. All 112 immutable detector outputs were written before the measurement scorer first read its retained synthetic truth. The [terminal record](../../../output/first50-campaign-control-20260905/measurement-terminal.json), SHA-256 `ccbe2239308f58367a020bbc32e4ea11941f400e3d98df3ffe1b8b50ccb2b72f`, retains the sole invocation and exit status. The [report](../../../output/first50-campaign-control-20260905/pixel-correspondence/measurement-1/report.json), SHA-256 `075d511812506273456af0bf2f0d3fe6eb8062c44584beae84f0644b31633bec`, and [detection roster](../../../output/first50-campaign-control-20260905/measurement-handoff.json), SHA-256 `5f1ceff79a9d5aeff487bacece11565f0e9a4e5591d3a0a06e839571fd143e4e`, bind the full outcome. The local runtime commitment is not hermetic runtime authority.

All 16 originals and 96 constructed controls were inspected separately at native 720 × 470 before freezing. Independent whole-component replay verified that all four cap regions survived each mutation and the entire selected wall/local rim was erased. The earlier audit report verified declared cap indices only; its [supplement](../../../output/first50-campaign-control-20260905/whole-component-review-supplement.json) records the stronger component census and that correction. One distant replacement contains a 56-pixel arc plus four isolated selector-qualified wall-boundary pixels. Its refusal applies to that exact mixed distractor. These distant controls do not establish a general clean-arc, nearby-rim, or neighboring-child association result, and they were never presented as minimal connection cuts.

The inherited local lineage contains two tested detector hypotheses, three capture executions including a capture-mode instrument failure, and a successful ideal-correspondence prerequisite. The coordinator conservatively admitted this distinct candidate as the fifth investigated local increment. That admission is now consumed. Pre-execution code corrections and authored mechanics checks remain recorded separately; no global remaining budget or reset was certified. No tuning, second replay, new candidate, source-bearing invocation, or qualification attempt followed this result. The coordinator and persistent implementer goals remain active; their status does not grant another attempt or source capability.

The wrapper waits for each direct Python child's close event, and the Node measurement command exited. The report's `ownedChildrenClosed: true` field is literal; there are no per-PID close receipts, so it does not independently prove descendant cleanup. No browser, server or GUI was launched. The earlier independent Windows process census was denied, so it is not counted as a successful cleanup inventory. The redundant interim terminal copy was removed after the complete terminal record was retained; inputs, prior rejected packets, constructed controls, raw detections and review evidence remain necessary counterevidence. Protected-source readiness, exact Poppler restoration, genuine camera correspondence, Step-44 selection, Steps 1–50 replay and full repository gates remain incomplete. No Step-51 mutation, commit or push occurred.
