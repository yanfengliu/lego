# Devlog — 2026-08-28

## Exact first-50 frames, occurrence provenance, and structural returns became separate opaque evidence stages

**Timestamp:** 2026-08-28 10:59 PDT

**Action:** Added three bounded reproducible stages downstream of the exact first-50 action preparation: a catalog-frame registry, a full actual-occurrence/world reconciler, and a structural-event artifact. The frame registry combines 27 mesh-backed frames, 30 existing geometry-proved parametric frames, and five exact archive-derived frames without widening four unresolved alias classes. The world reconciler now consumes three opaque verifier capabilities—proposal, frame registry, and action preparation—so every direct versus `MultiBuild` classification and every actual/source/master relation comes from the verified schedule rather than a static table. The Builder parser retains subbuild-completion events beside physical phases, allowing the zero-piece printed step 44 to bind the exact official return boundary without inventing a part phase or connection edge.

**Measured result:** The 98,383-byte frame artifact is `sha256:16a76321d1cbd8bb1308dff9c58ee61507f67c0139b17aa50a5d8415c789e2f2` with frame-table digest `sha256:66d69363a3d7d42127f70ccb42e0a7c764efceba4d96386b9751abd9f5dde973`. The 408,269-byte reconciliation artifact is `sha256:63d5872f92d208755dae34ecab7bad31c23b9b4cdcfb4165bbbc3159d281156e`: 320 exact actual rows, 309 reconciled worlds, eleven quarantined direct rows, eleven action-derived copy rows, occurrence commitment `sha256:63ab9b3bce415598f3a7b7fa0b407f29502df86f6a761fb541191fef28ca7b84`, and unchanged world commitment `sha256:da7932eeaeb115e9db38ac7ef01f9b4142df46fd1d3b2eb0a484e09aafa989fe`. Connector-only topology over the first eight rows reproduces one component and seven expected contact pairs after 19,008 pair comparisons over 214 catalog connectors. The official parser reproduces 561 physical phases and 132 separately ordered completion events; the 7,292-byte structural artifact at `sha256:cdb563b81f89aa9110cc70cc392f7415616aeb3e534ee907547879f8ec82c03e` digest-binds those parser outputs while publishing event 8 at the phase-71/72 boundary with the exact 23 child members.

**Critic repairs:** Independent semantic review rejected an unauthenticated `attachment` classifier that had been mandatory to emit `join-subassembly` while claiming `authorityUsed:false`; the official artifact now emits only `sub-build-complete`, the attachment reading is explicitly non-gating unauthenticated interpretation, and connection projection remains deferred. Independent instrument review rejected the self-authored copy-occurrence table, an underbound occurrence commitment, exact-current suites that all skipped without private inputs, output mutations that tested byte equality instead of transform algebra, and generation CLIs that wrote before checking their reviewed pins. The repaired chain derives copy provenance from opaque action preparation, binds callout/design/catalog/color/quarantine and actual/source/master identities, runs clean-checkout nested-parser/rotated-translation/quarantine/member-path gates, checks retained disk bytes, and refuses drift before replacing ignored evidence.

**Methodology:** A read-only exact-archive quarantine audit separated the remaining work instead of encouraging alias guesses: `41769.dat` and `41770.dat` are identity moved-root redirects to their existing `a` roots; `10201.dat` is an exact alias of `2436b.dat` but materially differs from `28802.dat`; and `3245b.dat` materially differs from `3245c.dat`. The next catalog tranche therefore requires two new measured definitions plus two exact occurrence-scoped redirects, while preserving the current eleven-row artifact as counterevidence until its successor independently verifies.

**Authority boundary:** Steps 51–359 remain index-only. The artifacts authenticate neither source execution nor user intent and grant no catalog admission for the four quarantined classes, connection/collision legality, action authority, document legality, placement, replay, mutation, acceptance, or completion. The retained production action ledger still ends at printed step 25, so this unit is necessary evidence for the first-50 build but not a step-50 frontier.

**Validation:** Current generation reproduces the exact frame, reconciliation, and structural artifact bytes; the write-before-verify guard deliberately refused a one-word structural drift without overwriting the old artifact, then accepted the corrected pin. Clean-checkout semantics pass nested Builder parsing, action-derived copy provenance, rotated nonzero frame translation with inverse round trip, quarantine nullity, structural-return authority, pure frame-ambiguity, and alias-widening tests. The final critic found and then cleared a parser split at 496/259 lines, the always-on synthetic frame suite, the structural parser/artifact wording distinction, and the three-input test title. Authoritative `npm run verify` passed 434 Vitest files / 3,602 tests with five skips in 189.50 seconds, 60 Playwright scenarios with thirteen explicit opt-in skips in 12.4 minutes, and the production build/bundle guard; both full and runtime audits reported zero vulnerabilities.

## Catalog `/29` closed first-50 identities while an authority-free selected-path diagnostic reached step 29

**Timestamp:** 2026-08-28 19:47 PDT

**Action:** Admitted exact official `10201` and `3245b`, advanced catalog truth to `/29`, regenerated the first-50 semantic, action-preparation, proposal, frame, reconciliation, structural-return, calibration, and proper-world evidence under their fail-closed current/history boundaries, and retained the production action ledger because semantic coverage `/4` remains identity-only. The catalog now has 106 definitions: 77 retain project-authored physical recipes, 29 are fully measured, and the render path contains 61 parametric definitions plus 45 source-normal mesh closures. The generator's 33 measured-pipeline and 12 render-only-pipeline rows resolve after overlay to 29 fully measured catalog definitions and 16 render-only promotions.

**Measured result:** Current truth is `sha256:54762419e4779c6c15566052062fcaa432cb45e3a13704b5af1563b4fa94e8eb`; catalog is `sha256:19c5e8a3f4e1d00d7747c8d3e0f377ee4391acc53915df8ead0c1830b75b8db6`. Thirty reconstructed source truths yield 2,339 endpoints at `sha256:b911e1d62c84c4abcb4c2fc5ae5568d41482b413beb9ef5186f7766a2e677688`, 440 reviewed endpoint deltas, and zero pair deltas. The current chain binds 588,467-byte semantic coverage `sha256:861d08a28dac94619e8c541e928d7803b4b6cab9fe9fa12da9f166fc0e46444d`, 317,152-byte action preparation `sha256:5fbab00b90c6ffbe6c9b09727819e0b3a964cebbd88138232bd2418df6100fb6`, 764,234-byte proposal `sha256:24c10640f118d2961dd297cff608b6978bd54eab85a37cf0c314f4711612f960`, 330,415-byte 66-alias frame evidence `sha256:bcf9702150b73cab1bd70d7ecd0bf33b3b3917522ce4f0ca892be56424b861a1`, 651,618-byte zero-quarantine reconciliation `sha256:4037ecb9cc60bc63bae38b963abeef8096d7405f2da80a40e79fe60fdff4092b`, and 7,292-byte structural-return evidence `sha256:ea1ee9791575ecd858cf13b076d0b3c6de4ebfca9a51a268a9242d3e07667fe3`. The executable-source attestation covers 3,748 files at `sha256:00cb15b09681797b066aec5f3b0a63c660cbb8610f93bf097034349345b92d87`. Builder parity closes 43 of 43 TypeScript/Python design tuples, 43 of 43 Python Shell identities against their TypeScript source rows, and all 184 canonical LDraw closure files; the ordered Shell aggregate is `sha256:ce023de75e9c5214cd49ebc381e1842cdeb4bd75c6da39b055a11124f5dcd136`.

**Visual review:** Both admitted parts were inspected at native 640 × 640 resolution across top, bottom, front, back, left, right, isometric, and underside-oblique source/catalog pairs. Every paired PNG and decoded pixel was identical, reviews `sha256:785d38a23a993d74dae2e59dd19286bf8e6bdbed9c66498aa2316ec1aed4d1c9` and `sha256:a039a4f4e2411f03325907a555c684a2a7a42524e18dc770cb6db027fbce3c08` record `same`, and immutable batch `sha256:fba1e7d57f8c4c004c2bf05c2473a887a02f51b0e87dc9350c1fbb05ee076a49` binds the two outcomes. The real UI was also observed live headlessly at 1,600 × 1,000, 1,280 × 720, and 1,024 × 768: the Technic Axle 3 palette row exposed its four upright choices plus `proper-m-00pp000p0`, placed the proper pose at `[0,-18,0]` in a globally valid `/29` document, remained readable without clipping or overlap, and refused an unsupported inspector pose while preserving the valid document and naming the 24-LDU unsupported height. Those full-page, palette, inspector, and error-surface observations were not retained as screenshots. The retained orientation evidence is exactly seven 640 × 480 canvas captures—canonical isometric, front, back, left, right, top, and underside—which were inspected as nonblank, centered, unclipped, and showing the vertical axle in ground contact. The task-owned browser and server were closed after capture. The retained pixels support those seven canvas-view conclusions only; the unretained live observations and these captures establish no hidden-interior, connector, collision, printed-frame, first-50 placement, or completion authority.

**Methodology:** The earlier 325-enumeration / 115.316-second run is retained as unsound counterevidence: its remaining-only dead-state memo conflated different documents and placement histories. The repaired state-complete memo commits the complete within-step search state including raw part order. Search-state preparation and recursion were split from the authoring operations so both production modules remain below the 500-line preference without changing the raw-order memo key, per-state prepared world, or injected-enumerator test boundary. The authority-free `lego.real-build-prefix50-selected-path-diagnostic/1` commits the first locally complete order selected at each printed step, backtracks only within the current step, never revisits an earlier committed step, and charges its node budget cumulatively across the prefix. On that selected committed prefix it stops after printed step 28 with 163 compiled parts and reports `selected-committed-prefix-within-step-blocker` at printed step 29 occurrence 165 on `builtin:jumper-plate-1x2`. An abstract isolated search-helper `[1,2]` versus `[2,1]` control models the early-return false-negative class; its synthetic tile topology is not a production-valid compiled model, so it does not prove a currently reachable production case. Independently, the implementation has no cross-step backtracking, so the selected-path result cannot support a globally first blocker or infeasibility claim. Canonical `lego.real-build-prefix50-exact-compilation/2` remains unavailable because it requires exact completion of all 50 steps, 320 parts, 51 state commitments, and the occurrence-30 proof. A successful exact completion would remain an existence witness; any future global infeasibility claim requires a separately implemented and measured continuation solver that revisits earlier choices. Retained 2026-08-28 output recorded source `[280,-36,-124]/upright-yaw-270`, target `[-280,-24,70]/upright-yaw-270`, `enumerationCount=749`, `searchNodeCount=256`, and 188.80 seconds. That retained output also recorded 65 complete enumerations for occurrence 165; its final one reported `freeStuds=191`, `freeClutches=367`, raw stud/clutch counts `382/367`, `distinct=617`, `below=226`, `colliding=124`, and `accepted=267`. These are historical selected-path fields rather than a fresh canonical compilation result, and the aggregate counts do not diagnose the selected-path refusal.

The blocker `/1` is also distinct from `lego.builder-proper-world-diagnostic/2`, the 78,884-byte closed-field Builder-frame census artifact; the shared `/2` suffix does not make that proper-world diagnostic a canonical exact-compilation result.

**Critic repairs:** Historical migration now evaluates every saved source truth under its own reviewed transform policy, so current `/29` legality cannot launder a non-upright pose claimed under `/28`. The `/28`→`/29` migration report separately names the four validated-profile definitions and the two through-bore definitions and reports both connector semantics and connection-conditioned collision semantics for each group instead of describing the successor as purely additive or either group as a single-domain change. Full-suite verification then exposed a stale Gate-3 `/13` to `/29` bridge expectation that still required an empty live interpretation list. The bridge now byte-authenticates the exact four-part row, rejects omitted, extra, reordered, or mutated rows, and an end-to-end changed-endpoint control proves affected unconnected parts migrate while connected changed endpoints fail before the frozen `/26` projection; the formerly failing three-suite cascade passes 44 of 44. The complete `npm run verify` gate then passed 3,790 Vitest tests with five skips, 60 Playwright tests with 13 opt-in skips, and the production bundle guard. The diagnostic compiler's dead-state memo commits complete within-step search state and raw part order while its schema now states the first-locally-complete committed-prefix policy, within-step-only backtracking, absence of cross-step backtracking, and cumulative prefix node budget; the actual authoring, attachment, diagnosis, and placement-enumeration paths consume only each part's explicit proper-orientation grants. Reviewed text extensions, including C# and Python consumed by source attestation or BOM checks, are normalized to LF in `.gitattributes` except for the enumerated exact-byte `-text` paths; the checkout gate rejects CRLF on every applicable reviewed source path while leaving those byte-preserving exceptions intact. BOM census `/2` now counts unique normalized path/span atoms rather than inferred aggregate payloads: exact whole-comment `sha256:`, `git:`, and `md5:` values are the only exemption from the `>=128` refusal and remain in the ledger and telemetry. The attempted repo-wide terminal-symbol/module-resolution route and the generated G3 duplicate-literal roster, generator, test, and seven binding modules were rejected and removed. The surviving direct G2 declarations are guarded separately by a binding-aware TypeScript program and Python `ast`/`symtable` analysis that discover every canonical import, exact-match each per-file production-role/reference inventory, reject shadowing and rebinding, and apply a conservative public-AST outcome/def-use normal form to refuse dead, conditional, swallowed, or bypassed matches. The contract deliberately recognizes only the enumerated production shapes; it is not a general control-flow graph, terminal-symbol resolver, or semantic-equivalence proof. Twenty-two gratuitously split source literals were rejoined, and a focused source-policy control now refuses literal-only binary `+` composition in JavaScript, TypeScript, and Python plus Python implicit adjacent string literals when a direct literal leaf contains a census-sized atom; method calls, template substitution, conditionals, formatting, f-strings, dynamic composition, and short composition remain outside that syntactic contract. Two final byte-identical `/2` runs recorded `totalCharacters=330852`, `atomCount=6097`, classification characters `{non-exempt:330852, terminal-comment-metadata:0}`, atom-class characters `{exact-64-lower-hex:240704, exact-40-lower-hex:5640, exact-32-lower-hex:2560, base64-padding:312, identifier-like-alnum:16460, base64-symbol:65176}`, and ledger `sha256:3668a6a8346ba60f472d0d775545193c97728cf4ce173896841a083e43974067`. Structural-event publication uses the fail-closed no-overwrite publisher and refuses both late-create and late-change races; a differing verified candidate requires explicit promotion. The Python action-ledger contract fixture now owns its verifier patch in `ActionLedgerReportContractTests.setUpClass` with `addClassCleanup`, so importing that class into aggregate `scripts/part_retrieval_ceiling_test.py` no longer loses the synthetic verifier or emits the misleading generic `action-ledger-reproduction` failure despite byte-identical ledgers; `test_synthetic_verifier_fixture_follows_the_class_across_collection` covers the collection boundary. Direct control passed 17 of 17 tests in 25.645 seconds, and the aggregate passed 81 tests with 16 skips in 34.938 seconds.

**Whole-diff review repairs:** The final independent staged review found that `learning-system.md` still labeled the superseded `/28` frame, world, structural, and visual packets as current, and that the materially rewritten `migration.test.ts` had grown to 1,020 physical lines past the repository's 1,000-line hard ceiling. The canonical document now carries the `/29` 66-alias, zero-quarantine packets and distinguishes historical `/28` from current `/29` visual evidence. The two general refusal cases moved into the existing boundary suite, leaving the files at 983 and 128 lines; both suites pass 31 of 31 tests. Rebinding those exact executable source bytes moved the 3,748-file source attestation to `sha256:00cb15b09681797b066aec5f3b0a63c660cbb8610f93bf097034349345b92d87` and the BOM census to 330,852 characters / 6,097 atoms at `sha256:3668a6a8346ba60f472d0d775545193c97728cf4ce173896841a083e43974067`.

**Migration reporting:** The same review backfilled six previously silent behavioral boundaries: the first exact `/4` truth's same-version jumper repair, `/4`→`/5`, `/5`→`/6`, `/8`→`/9`, `/9`→`/10`, and `/10`→`/11`. Exact predecessor-truth discrimination prevents the `/4` row from applying to either later `/4` snapshot, every row is intersected with the source truth's exact catalog roster, and the archived `/10` gate independently derives all 58 shell-cavity definitions rather than copying the runtime list. The editor migration notice now intersects affected definitions with the opened model and reports the union of relevant render, construction, connector, and collision categories instead of calling every interpretation an appearance change.

**Workflow boundary:** `LEGO_REAL_BUILD_REGENERATE_THROUGH=calibration` verifies source pins and semantic coverage, publishes the 54,993-byte `/29` calibration at `sha256:69555bf4a0b7a7beaeb4f98b6d0e5750fa28b6946a957cdc5772633602636a70` and the 78,884-byte proper-world diagnostic `/2` at `sha256:6a26f70df0aa6faac4361a195bd2d95931f8f46acd2e56ecc7c7f052ea0aa940`, and leaves the action ledger untouched. The full chain reports `BLOCKED` at the intended action-authority boundary because semantic coverage is identity-only; that is neither calibration failure nor placement progress.

**Authority boundary:** Catalog `/29` closes the exact first-50 catalog identities but does not establish their placement sequence. The selected-path diagnostic completes printed step 28 with 163 compiled parts on its first-locally-complete committed prefix and reports a within-step blocker at printed step 29 occurrence 165 on `builtin:jumper-plate-1x2`; its cause remains undiagnosed, earlier step choices remain unexplored, and no globally first blocker or infeasibility claim follows. Printed step 29 is not complete, step 50 is not assembled, steps 51–359 remain index-only, and no trusted action ledger, placement, replay, mutation, acceptance, or completion authority follows.

**Validation:** `npm run bom:check`, `npm run notices:check`, `npm run lessons:check`, focused Prettier, and `git diff --check` passed. `npm run migration-history:check` reproduced 30 source truths / 2,339 current endpoints / 440 truth-row endpoint deltas / zero pair deltas, and `npm run parts:check` reproduced 106 parts with no violations. The first full-suite attempt exposed a stale current-catalog step-one enumeration digest and a 240-second per-test ceiling that the retained diagnostic exceeded only under suite contention; the digest was re-derived under `/29`, and the ceiling was raised to 360 seconds without changing search semantics or expected output. The latest retained local execution of `npx.cmd vitest run apps/web/test/real-build-prefix50-exact-compiler.test.ts apps/web/test/real-build-prefix50-exact-compiler-search-state.test.ts apps/web/test/real-build-prefix50-exact-compiler-current-evidence.test.ts` ran all 19 tests across three files with no skip in 185.28 seconds of suite wall time and preserved the step-29 selected-path observation. That execution depends on seven ignored evidence inputs and is not a clean-checkout repository gate; its wall time does not refresh the retained 188.80-second diagnostic or its detailed source, target, and occurrence-165 enumeration fields. After the Gate-3 live-bridge repair, the complete rerun passed all authoritative gates: 470 Vitest files / 3,790 tests passed with five documented conditional skips, 60 Playwright tests passed with thirteen documented opt-in skips, and the production build plus bundle guard passed.
