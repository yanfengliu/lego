# Devlog — 2026-08-28

## Exact first-50 frames, occurrence provenance, and structural returns became separate opaque evidence stages

**Timestamp:** 2026-08-28 10:59 PDT

**Action:** Added three bounded reproducible stages downstream of the exact first-50 action preparation: a catalog-frame registry, a full actual-occurrence/world reconciler, and a structural-event artifact. The frame registry combines 27 mesh-backed frames, 30 existing geometry-proved parametric frames, and five exact archive-derived frames without widening four unresolved alias classes. The world reconciler now consumes three opaque verifier capabilities—proposal, frame registry, and action preparation—so every direct versus `MultiBuild` classification and every actual/source/master relation comes from the verified schedule rather than a static table. The Builder parser retains subbuild-completion events beside physical phases, allowing the zero-piece printed step 44 to bind the exact official return boundary without inventing a part phase or connection edge.

**Measured result:** The 98,383-byte frame artifact is `sha256:16a76321d1cbd8bb1308dff9c58ee61507f67c0139b17aa50a5d8415c789e2f2` with frame-table digest `sha256:66d69363a3d7d42127f70ccb42e0a7c764efceba4d96386b9751abd9f5dde973`. The 408,269-byte reconciliation artifact is `sha256:63d5872f92d208755dae34ecab7bad31c23b9b4cdcfb4165bbbc3159d281156e`: 320 exact actual rows, 309 reconciled worlds, eleven quarantined direct rows, eleven action-derived copy rows, occurrence commitment `sha256:63ab9b3bce415598f3a7b7fa0b407f29502df86f6a761fb541191fef28ca7b84`, and unchanged world commitment `sha256:da7932eeaeb115e9db38ac7ef01f9b4142df46fd1d3b2eb0a484e09aafa989fe`. Connector-only topology over the first eight rows reproduces one component and seven expected contact pairs after 19,008 pair comparisons over 214 catalog connectors. The official parser reproduces 561 physical phases and 132 separately ordered completion events; the 7,292-byte structural artifact at `sha256:cdb563b81f89aa9110cc70cc392f7415616aeb3e534ee907547879f8ec82c03e` digest-binds those parser outputs while publishing event 8 at the phase-71/72 boundary with the exact 23 child members.

**Critic repairs:** Independent semantic review rejected an unauthenticated `attachment` classifier that had been mandatory to emit `join-subassembly` while claiming `authorityUsed:false`; the official artifact now emits only `sub-build-complete`, the attachment reading is explicitly non-gating unauthenticated interpretation, and connection projection remains deferred. Independent instrument review rejected the self-authored copy-occurrence table, an underbound occurrence commitment, exact-current suites that all skipped without private inputs, output mutations that tested byte equality instead of transform algebra, and generation CLIs that wrote before checking their reviewed pins. The repaired chain derives copy provenance from opaque action preparation, binds callout/design/catalog/color/quarantine and actual/source/master identities, runs clean-checkout nested-parser/rotated-translation/quarantine/member-path gates, checks retained disk bytes, and refuses drift before replacing ignored evidence.

**Methodology:** A read-only exact-archive quarantine audit separated the remaining work instead of encouraging alias guesses: `41769.dat` and `41770.dat` are identity moved-root redirects to their existing `a` roots; `10201.dat` is an exact alias of `2436b.dat` but materially differs from `28802.dat`; and `3245b.dat` materially differs from `3245c.dat`. The next catalog tranche therefore requires two new measured definitions plus two exact occurrence-scoped redirects, while preserving the current eleven-row artifact as counterevidence until its successor independently verifies.

**Authority boundary:** Steps 51–359 remain index-only. The artifacts authenticate neither source execution nor user intent and grant no catalog admission for the four quarantined classes, connection/collision legality, action authority, document legality, placement, replay, mutation, acceptance, or completion. The retained production action ledger still ends at printed step 25, so this unit is necessary evidence for the first-50 build but not a step-50 frontier.

**Validation:** Current generation reproduces the exact frame, reconciliation, and structural artifact bytes; the write-before-verify guard deliberately refused a one-word structural drift without overwriting the old artifact, then accepted the corrected pin. Clean-checkout semantics pass nested Builder parsing, action-derived copy provenance, rotated nonzero frame translation with inverse round trip, quarantine nullity, structural-return authority, pure frame-ambiguity, and alias-widening tests. The final critic found and then cleared a parser split at 496/259 lines, the always-on synthetic frame suite, the structural parser/artifact wording distinction, and the three-input test title. Authoritative `npm run verify` passed 434 Vitest files / 3,602 tests with five skips in 189.50 seconds, 60 Playwright scenarios with thirteen explicit opt-in skips in 12.4 minutes, and the production build/bundle guard; both full and runtime audits reported zero vulnerabilities.
