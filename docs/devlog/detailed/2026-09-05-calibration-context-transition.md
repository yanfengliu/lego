# Calibration closes its first context before Step-43 consumption

## Final refusal had arrived too late

Independent review of the accepted request-delivery guard found that its final policy assertion followed the entire calibration callback, including Step-43 consumption. The source-free reproduction caught a refused foreign fetch and still advanced a synthetic consumption counter to 1 before the lifecycle returned failure. Blocking delivery and reporting final failure had not prevented that transition.

The calibration gate now uses two sequential guarded contexts within one owned browser, static server and exclusive log. It captures and evaluates Steps 41/42, creates the genuine session, closes the first context, and awaits the unchanged policy's pending-route drain and sticky refusal check. Only successful finalization reaches `afterCalibrationClosed`. That transition retains the live source-lock check and consumes the same in-memory opaque session once, before creating the fresh Step-43 capture context. Refused calibration skips consumption and the second context; later failure cannot replenish the opportunity. Legacy callers, camera law, receipts, thresholds, runtime pins and the closed qualification-tree roster are unchanged.

## The controls distinguish refusal from cancellation

The fixed 14-case phase population covers clean ordering, callback/policy/context-close failures, refused calibration, second-phase and outer cleanup failures, the real gate's sole consumer location, caught foreign fetch, a tracked in-flight route, a scheduled request race and a successful two-context import/capture flow. Caught and tracked in-flight refusals keep the synthetic consumption counter at 0. The held-route control observes actual context closure while request metadata remains pending; transition waits for policy completion and refuses after the released metadata fails. No close/drain hang occurred, and an instrument timeout is a failure rather than permission to advance.

The scheduled request was canceled or unobserved before context destruction, with zero canary delivery. Its counter advanced only after the first page was closed and its context had no pages. That result is explicitly not counted as policy refusal. Moving the transition before awaited first-context finalization made all three selected ordering controls fail, including consumption counter 1 in both request controls; exact bytes were restored.

## A stale config-reading instrument was repaired separately

The first combined selection passed 44/45. Its sole failure came from an older import-closure helper that required literal AST setup properties after the accepted bootstrap had introduced conditional calibration setup selection. The authorized correction calls the real operation selector and config factory, requires independently fixed calibration setup and existing teardown identities, and preserves every graph root, dependency traversal, forbidden-source assertion and post-unlock registry. It does not import or execute the selected setup. A test-only ordinary-operation mutation failed the exact setup assertion; restored closure suites passed.

The independent critic and coordinator accepted the exact five-file revision. The coordinator then reran the explicit six-file selection: **45/45 passed, exit 0, 88.86 seconds**. This comprised 14 phase controls, 12 strict-browser controls, one legacy integration, six opaque-capability checks and two six-case closure suites. Lint, typecheck, scoped formatting and task-only diff checks also passed. The earlier 44/45 result and both mutation failures remain retained.

## Evidence and admission limits

The frozen implementation [handoff](../../../output/calibration-context-transition-20260905/phase-79d31c/HANDOFF.md) has SHA256 `3c5a6a5f7bd0472202ca53ed551d07947b6f96f9046f1a9e5bedcc2625281e8d`; its [five-file manifest](../../../output/calibration-context-transition-20260905/phase-79d31c/after-manifest.json) has SHA256 `d4565f3eddf443f8733a25e3d87430d37d9111e99589996793727c3223fc63a5`. The coordinator's [final log](../../../output/calibration-context-transition-20260905/coordinator-acceptance/selected.log) has SHA256 `690db07807148cf3ae7f0b2d8442aea1033c44975c1517dbf9162ef32cebb812`. These ignored roots retain raw results, captures, pre-edit snapshots and mutations; tracked documentation records conclusions and provenance only.

The coordinator independently checked all 16 real result records: browser, owned process-tree and server cleanup flags were true, and all created canaries were closed with zero forbidden delivery. Fourteen positive PNGs matched the individually inspected native 640 × 480 bytes. The seven views show the synthetic one-stud part and establish the bounded fresh-context UI import/capture flow. The separate Windows process census was denied, so this is not an independently successful OS process inventory.

The Node callback remains trusted and source-bound; a Playwright Page still exposes Node-side context methods. This repairs browser-content ordering, not an unforgeable capability or general OS/network isolation. Browser controls used synthetic documents and owned loopback/file sentinels. The selected live closure checks read admitted exact PDF/material bytes solely for identity hashing, without decoding, rasterization or measurement. No actual calibration session was qualified or protected source consumed by these controls.

Complete Stage-1 readiness, runner output and attachment ownership, the missing exact runtime file, genuine camera correspondence and the full first-50 campaign remained unresolved at acceptance. The rejected 112-image camera candidate and disqualified source incident remain rejected. No source qualification, default/full verification, commit or push followed this checkpoint. The [disclosed-verification protocol](../../runbooks/first50-disclosed-verification.md) and all subsequent stage admissions still apply.
