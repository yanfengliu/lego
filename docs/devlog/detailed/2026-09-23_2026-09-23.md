# 2026-09-23

## `npm run booklet`: the first milestone of the booklet harness

**Action:** Added `tools/booklet`, a Node-only harness that reads the booklet PDF (pdf.js), aligns every official brick of set 21066 to a printed step, measures catalog coverage, checks the official LDraw export's frames, and replays the official model step by step through the brick kernel. It writes per-step rows to the ignored `output/booklet/status.json` and `output/booklet/reference-playback.json` (and refuses a `BOOKLET_OUT` that Git would not ignore), prints at most 40 summary lines, and compares headline counts with the committed `status/booklet-baseline.json`. The official model loads only through `tools/booklet/answer-key/`; an `eslint.config.js` rule bars product code (`packages/*/src`, `apps/*/src` and their config roots, every script extension) from loading `tools/booklet` or naming `output/official-model` as a path, and `tools/booklet/answer-key-guard.test.ts` lints each construct through the real config.

**Measured result:** A full run takes about 7 to 10 s. Read: steps 1..359 contiguous on pages 11-219 after removing 219 overprinted labels; 859 step-owned callouts sum to 1,464, and the only other callout-size labels are page 84's 4 labels / 14 pieces (an announcement) and page 132's 1 label / 1 piece on the bag-7 opening, 864 / 1,479 in all; the inventory has 276 rows / 1,465 pieces. Align: 338 steps match by run order, 18 are fitted by the local repair, and 3 (156-158) stay unmatched because the repair window 155-159 stopped at its 400,000-node budget; 1,464 bricks are assigned and the one brick no LXFML step adds is 31510. Catalog: 172 designs, 77 exact, 5 interchangeable mould revisions, 90 missing; the first uncovered design is needed at step 51 (86996) and the first absent colour at step 18 (LDraw 47). Pairing: 1,422 bricks are verified by instance agreement, and 42 designs placed once plus 1 multi-part brick are unverified. Export frames: of 8 pinned designs, 6 agree, 93273 differs from its pin only by the part's half-turn symmetry, and 77844 disagrees; 163 designs have no pin. Playback: valid through step 1 as exported; with the 77844 pin and the reviewed 80015 origin applied as a separate correction layer, valid through step 28, first failure at step 29 (`DISCONNECTED_ASSEMBLY`, 15573).

**Believed and proved false:** The older 881 / 1,512 source count was a different reading of the same pages. It is the 859 / 1,464 step callouts plus the 22 larger "2x"/"4x" sub-build multipliers (sum 48), which are not parts; the harness recomputes this every run. The committed "valid through step 28, blocked at 29" frontier was a placement search (first locally complete order per step), not a compile of official poses; the corrected official poses now reach the same step 28 on their own.

**What the review caught (review-g3a):** The first write-up said 359 of 359 steps aligned. 16 of them matched only because the repair chose their bricks to fit the callouts, and the window 155-159 was reported solved after its search hit the node budget, so its fit was never proven minimal. The summary now splits run order from repair and reports the budget window as unsolved. It also claimed the pairing was proved, but the invariance failures and colour conflicts were never read, and a design placed once cannot be checked by instance agreement at all. The alignment's doc claimed the inventory check "closes the gap"; it compares per-element totals and never sees how bricks are split into steps.

**The 80015 explanation, corrected:** The first write-up said turning 80015 a quarter turn clears the step-2 overlap, so the export's 80015 frame was the first suspect. The review showed the harness is not the cause: the registry frame for 80015.dat matches the catalog and the studs in 80015.dat. The export's 80015 frame is wrong in its origin, not its rotation. The quarter turn clears step 2 but disconnects step 1, while keeping the export's rotation and moving the origin to the stud at LDraw (-10, 8, -70) gives valid through step 25. 77844's export frame (origin (20, 8, 20)) contradicts `scripts/builder_ldraw_frame_pins.py` (turn180, (40, 8, 0)). Fixing both gives valid through step 28, first failure at step 29: the committed frontier.

**Trip hazards:** The 80015 correction is fitted by a playback search over the ring's stud positions, not derived from a Builder record; it applies only while the export shows exactly the reviewed frame, and the run checks each run that the new origin sits under a catalog stud. The frame registry (`output/real-build/history/prefix50-ldraw-catalog-frames-reviewed-move-bcf97021….json`) is ignored, so a clean clone has none: without it playback infers frames, and the inference disagrees with the registry on 24 of 35 parametric rows, which the run now prints in capitals. The harness used to drop the top-face offset for a parametric part whose catalog declares a turn, so the 2 x 14 plate (91988) landed 4 LDU off; `tools/booklet/frames.test.ts` pins the fix.

## `npm run verify` green on a clean checkout, and the run-evidence opt-in

**Timestamp:** 2026-09-23 11:48–16:10

**Action:** The summary lines for 6260e8d and feea2f8 carried this whole record and ran to 985 and 1,956 characters. The local rules now cap a summary line at 300, so the full text moved here unchanged.

**Result:** **`npm run verify` exits 0 again** (19 min in a worktree without ignored inputs; Vitest 3731 passed, 64 skipped, 0 failed over 477 files, against 31 failed on main before). Tests that read ignored run evidence now skip by default, each naming why, and run under `LEGO_RUN_EVIDENCE=1`: 13 Vitest files holding 56 cases, and 2 Python cases. The five `*.score.test.ts` runs moved to `npm run test:score`. Every tar call goes through `scripts/tar-archive.mjs`, because Git Bash's GNU tar reads `C:\` as `host:path` and cannot read a zip at all. The visual-admission scripts' Vite servers run without a file watcher, whose crawl of the main checkout starved a module fetch past 60 s (112.8 s against 18.7 s on a first run there). `isRotationIcon` and `panelContainsRotationIcon` came back from f44f1b8. eslint and prettier no longer abort on an unreadable `.pytest_cache`; prettier needs its exclusions on the command line, since it scans `.` before reading `.prettierignore`.

**Code reviewer comments:** **Review finding F1 (`part-identification-2453-builder-identity.test.mjs`'s `describe.runIf` reading 4 `output/` and 6 external `C:/tmp` paths with no `LEGO_RUN_EVIDENCE` wiring) closed across its whole defect class.** A repo-wide sweep for the same shape — `existsSync`/`os.path.exists` feeding a `runIf`/`skipIf`/`skipUnless`, or a `cond ? describe : describe.skip` ternary the review's own grep missed — found 13 more pre-existing instances beyond its ~15-file F2 catalog (9 JS/MJS, 4 Python). All 24 files (14 JS/TS/MJS, 9 Python, plus `package.json`) now route through `describeWithRunEvidence`/`itWithRunEvidence`/`requires_run_evidence`; opted in, an absent or drifted input fails by name (verified live: `LEGO_RUN_EVIDENCE=1` against the newly-converted files throws `ENOENT`/`AssertionError`, never a silent skip). `npm run test:python` now passes `-v` to every invocation so a Python skip's reason shows by default instead of only under an explicit `-v` (F3). A new `scripts/run-evidence-gate-coverage.test.mjs` scans every vitest-included test file and every `scripts/*_test.py` for the unwired shape and fails on any instance outside one documented exception (`real-build-transition-bundle.test.ts`'s tautological absence-branch test); proved red against a scratch reproduction of F1's exact pattern, never against the worktree. Several newly-gated tests had their real evidence already present on this machine (the sample booklet, external LDCad/LDraw archives under `C:/tmp`) and were actually running before this fix: `npm test` moved from 3731 passed/64 skipped (3795 over 477 files) to 3729 passed/33 skipped (3762 over 478 files) — the net drop is `describeWithRunEvidence` collapsing a disabled block to one skipped placeholder instead of one per original case, not a lost check — and `test:python` moved from 50 to 57 skipped across a steady 608 tests, the rise fully attributable to evidence this machine happens to have. A focused re-review of feea2f8 returned merge; its two comment nits were fixed on the integration branch in 9833963.

## Closed-set callout identification: 72 of 74 judged callouts, 74 of 74 with errata

**Timestamp:** 2026-09-23 09:53–11:35

**Action:** `apps/web/src/instructions/identify/` reads the booklet with pdf.js, cuts each count label's picture out of its image XObjects, and scores it against the 276 inventory thumbnails at the fixed 1.334x callout scale (mask IoU, luminance correlation, colour distance). It assigns whole drawings to elements by min-cost flow with branch and bound, the inventory counts as capacities. No model is called. `node scripts/identify-booklet.mjs` writes the ignored `output/booklet/identify.json` and scores Steps 1-50 against `scripts/fixtures/part-identification-truth-first50-labels.json`.

**Result:** On 6651557.pdf at the integration head: 864 callouts, 859 of them in numbered steps; 313 distinct drawings; the assignment proven optimal after 1 node; 72 of 74 judged callouts right, and 74 of 74 with the 2 recorded truth errata, both shape-only verdicts that named the grey twin of a green and of a dark blue part. The run took 8.4 s.

**Code reviewer comments:** Review g3c found that a composite picture's key named no page, so callouts at one spot on two pages shared a demand; splitting it moved 312 → 313 drawings. Nothing refused an element assigned past its inventory count. Two mutations survived every test: dropping the capacity check's call from `report()`, and passing a verdict between callouts that share a composite key (E1). Both now fail a test, and 23 of 23 mutations were killed.

**Notes:** Only the judged-callout line measures accuracy. The capacity constraint forces the reconciliation, first-choice agreement compares two of the module's own stages, and inherited verdicts agree by construction. `maxOperatorsPerPage` and `maxDecodedPixelsPerPage` have no test yet.

## Booklet-reset milestone 1 integrated, and the one gap the merge opened

**Timestamp:** 2026-09-23 16:17–16:46

**Action:** Merged onto `integrate/booklet-reset-1` from b1e1d0e, in order and with `--no-ff`: workflow reform (23be69a), gate fixes (feea2f8), part identification (0271962), booklet harness (ee3e7af). Only the harness merge conflicted, in `tsconfig.json`'s include list and in the devlog summary, and both kept both sides. `eslint.config.js`, `vitest.config.ts` and `package.json` merged cleanly with both sides' changes.

**Result:** `npm run verify` exit 0 in 16 min 22 s at c2a3d7a: Vitest 3,881 passed and 33 skipped over 499 files, Python 608 ran with 57 skipped, and Playwright 59 passed with 14 skipped. `npm run booklet` and `node scripts/identify-booklet.mjs` reproduced every figure the branch owners reported.

**Reasoning:** The merge opened one silent gap. The gate-fix branch's class check copied vitest's include list into four roots of its own, and the harness branch added a `tools/` root to vitest's include. Merged, `npm test` ran the `tools/` tests and the check never read them. The check now reads the include from `vitest.config.ts`, so the two lists cannot drift apart again.

**Validation:** A staged `tools/booklet` probe using `describe.runIf(existsSync(...))` failed the fixed check by name and passed the pre-fix copy. No real `tools/` or identify test has that shape.

## Frame truth: catalog `/30`, and the step-29 jumper

**Timestamp:** 2026-09-23 17:05–19:50

**Action:** Every parametric part's LDraw-to-catalog frame is now catalog truth. `scripts/derive-ldraw-catalog-frames.mjs` measures it from the byte-pinned official LDraw archive: of the four upright yaws, a candidate must put the file's full extent exactly on the catalog bounds and every catalog stud on a stud primitive, and candidates the file's own faces cannot tell apart count as one placement. 59 of 61 rows derive; `32062` and `3483` are a reviewed choice among measured candidates. `ldrawToCatalogFrame` reads them, so the kernel no longer gives a parametric part a zero offset and (except the 2 x 14 plate) no turn. `npm run booklet` no longer reads the first-50 frame registry; it compares against it only under `LEGO_RUN_EVIDENCE=1`. The 15573 jumper gained `undersideClutch:center`, a half-pitch alternate seat under the rule 99563 already used, now shared by mesh admission and the parametric factory (`alternate-clutch-seats.ts`). Catalog `/29` → `/30` (truth `sha256:7672dd78…`), with a migration report naming the 61 frame changes (`ldraw-interchange-frame`) and 15573's connector and collision change; LDraw subset `/1` files still import through the frame they were written with.

**Result:** Without the registry, playback is valid through step 31 with the two export-frame corrections (it was 3; 28 with the registry), and through step 1 as exported. Step 32 is the first failure, `PART_STUD_BODY_COLLISION`. The registry agrees with catalog truth on all 66 rows, exactly. Both corrections are still needed: without the 80015 origin playback stops after step 1, without the 77844 pin after step 25.

**Reasoning:** Step 29 was not a frame fault. Its four jumpers sit centred on the single top stud of four 11253 roller skates, half a pitch off their own grid, and the parametric jumper declared only the two grid clutches at ±10, so no clutch was over the stud. The part's title ("with 1 Centre Stud, without Understud"), LEGO's own model and the pinned LDCad route (cross-check only, nothing copied) agree it grips a stud there. The registry's parametric rows had come from LEGO Builder geometry and a campaign run file; the official LDraw files give the same 35 frames under the facts-only BOM policy. The two corrections fix the export's LXFML-to-LDraw frames, upstream of catalog frames, so frame truth could not retire them.

**Validation:** The frame-convention, derivation, 15573 connection and `/29` migration tests each went red when their defect was put back (zero parametric offset; seat removed; `/29` deltas dropped). `LEGO_RUN_EVIDENCE=1` re-derives the committed frame table byte for byte from the archive and compares it with the registry. A saved `/29` document migrates unless it has an edge on a 15573 grid clutch, whose capacity semantics changed; that edge is refused by name.
