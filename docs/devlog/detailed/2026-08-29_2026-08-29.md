# Devlog — 2026-08-29

## Prefix-50 playback evidence was rejected until Step44 becomes operation-replayable

**Timestamp:** 2026-08-29 19:25 PDT

**Action:** Added an opt-in prefix-50 Playwright evidence lane under `LEGO_REAL_BUILD_REQUIRED=1`, where pre-discovery source locks and transitive current-evidence readers fail closed. App observation distinguishes the authored document from the active playback preview and exposes position, terminal position, step identity, added-part count, preview structural hash, preview validation, blocking codes, buildability and connectivity. The lane independently reconstructs the terminal connection graph, exact-compares canonical persisted document bytes before and after reload, checks that Start/Step49/Step50 preview navigation does not write into IndexedDB, and only afterward reloads isolated parent and child documents before restoring the combined draft.

**Measured result:** The browser bridge unit passed 2 of 2 tests; focused ESLint and Prettier checks passed; the default Playwright invocation discovered the replay and skipped it before `beforeAll` without starting the exact compiler. A completed workspace typecheck passed before the support-file extraction; after extraction, focused checks remained green while a concurrent unrelated `rosterSummary` edit in the Step44 return path temporarily blocked the aggregate typecheck. The opted-in long replay and canonical 211-candidate enumeration remain deliberately unrun until Step44's rigid return is represented and verified as ordinary replayable operations.

**Critic repairs:** The former Step49 attachment loop could pass with no images, and Step50's presentation-scene pixel count could be satisfied by the grid and shadow plate. The evidence helper now captures full-page playback state plus default and close interactive zoom, seven presentation views and seven model-only views for the 311-part parent, nine-part child and restored 320-part compound; the model-only route hides grid and shadow plate, checks non-background model bounds in every view, and retains a JSON receipt binding document and renderer hashes, canonical-document digest, image digests, part count and pixel measurements.

**Rejected claim:** A later independent harsh review found that `deriveBuildSequence` restricted the final document by step membership. Because Step44 adds no parts and the rigid child return had directly replaced existing transforms and connections in the final document, both Step43 and Step44 previews already showed the post-return pose. The test's independent oracle repeated the same final-document restriction and compared only part counts, so it could not detect the missing transition. Exact replay now requires a versioned, authority-free transition trace that binds normalized operations plus before/after document commitments, demonstrates distinct Step43 and Step44 states, and invalidates after structural edits; membership-only playback must identify itself as a non-exact preview.

**Capture-race repair:** The same review found that one shared in-flight capture promise could return a presentation result to a concurrent model-only caller and validated the second caller's mode only after coalescing. The capture path now validates first and serializes every caller through a recovering queue without coalescing results; focused queue and automation tests pass 4 of 4 and workspace typecheck passes.

**Evidence retention incident:** The harsh-review Playwright invocation recreated the ignored `test-results/playwright` directory and removed an older failed trace before replacement evidence existed. That task-owned trace was stale counterevidence but should have been retained until the replacement run; it is unrecoverable. The new workflow therefore treats operation-trace reproducibility as a cheap preflight before candidate rendering and keeps task-owned counterevidence until post-fix evidence is complete.
