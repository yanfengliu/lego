# Calibration owns runner output before Playwright cleanup

## Setup validation came after an independent writer

Readiness review found that selected calibration still inherited `test-results/playwright`. Installed Playwright clears its output directory before global setup, so a setup-only assertion could report failure after shared output was deleted. CLI output/reporter overrides, the independent last-run writer and `testInfo.attach({ path })` also needed explicit ownership checks. Synthetic controls reproduced those writer effects; no production calibration run or user output was used to establish the defect.

Calibration now exclusively creates a fresh parent under `output/playwright/step44-calibration-runs`, stores its ordinary owner record beside a fixed disposable `playwright` child, and checks directory/file identities on reuse. Playwright may recreate the child without replacing the durable parent. Repeated preparation in one live root keeps one claim; a direct connected worker validates inherited coordinates, while another CLI invocation cannot adopt them. The installed Windows shim uses `@playwright/test/cli.js`, so both that exact package entry and `playwright/cli.js` are supported in normal `test` mode. UI, watch, list and arbitrary programmatic entry paths are outside this calibration-only contract; other operations retain their configuration behavior.

Raw output and override checks precede source snapshots. Resolved setup always checks output before the generic required flag can skip source bootstrap, then retains manifest and held-lock checks in their existing order. The calibration spec checks project/per-test output before publication and around attachment copying. The bounded helper uses only Node built-ins at runtime; its operational record is neither source authority nor authentication against another same-user process.

## A configuration dependency and a test fixture needed correction

Requiring calibration output exposed an existing dependency: production source-closure discovery called the full config factory only to select setup. A pure lifecycle-hook selector now serves both callers without inventing an output claim for discovery. Production closure changed only its selector import and setup expression; graph roots, traversal, forbidden imports and registries remain unchanged. Tests retain independent literal hook expectations and traverse the validated full-factory hooks. Wrong-selector, factory-divergence and source-before-output mutations fail executed assertions, with exact bytes restored.

The first workspace typecheck caught an insufficient `FullConfig` cast in the deliberately partial mocked setup fixture. Its correction adds an explicit test-local cast through `unknown` and a comment; independent comparison confirmed every assertion and production file unchanged. Workspace and separate root-config typechecks then passed. The integration test remains one focused 588-line file, below the 1,000-line hard ceiling, to keep its forwarding observer, synthetic fixtures, process ownership and writer controls together.

## Accepted measurements and their different permissions

The synthetic integration gate uses installed Playwright 1.61.1 and Vitest 4.1.10. It exercises eight active CLI/environment override controls, a separate shared-output config refusal and five isolated helper guard mutations with actual writer effects. Those five and the three caller mutations above make eight runner mutations with genuine assertion failures; restored production bytes pass.

The earlier implementation-owner selection passed **80 tests**, with one Windows file-symlink control unavailable and two live source-closure cases excluded. Its [gate record](../../../output/step44-calibration-runner-output-20260905/integrated-e5c19507c1d1/root-gates-final.json), SHA256 `79624f7793607d3601df1b299556c867b6b32a5305fffade47d26683774de8ce`, retains the original typecheck failure, rerun, explicit test selections and cleanup bounds.

The parent coordinator later admitted two unchanged live closure cases for identity hashing only and included the eleven adjacent crop checks. Its seven-file rerun passed **93 tests with one unavailable control out of 94**, exit 0 in **26.99 seconds**. The [parent log](../../../output/step44-calibration-runner-output-20260905/parent-acceptance-20260905/selected.log) has SHA256 `79b264d0bfbdb7b774ef3f7ac4d0a068f2b7e2bf9a625cd83e1ea65c5addf6fc`. The difference is 76 runner passes plus four synthetic closure cases, two separately admitted identity-only closure cases and eleven crop checks. The later permission does not change the earlier run's exclusions. Identity hashing did not decode or rasterize the PDF/material payloads.

The accepted twelve-file [manifest](../../../output/step44-calibration-runner-output-20260905/integrated-e5c19507c1d1/manifest-v2.json) has SHA256 `b9f58682ad39de71613edadb8961a2d0e9bea8cc116990bdde591dd71a5a1b4f`; all twelve implementation files and both frozen native-unit source files matched their hashes and byte counts. Independent caller, foundation, CLI and focused fixture reviews accepted the bounded changes. The parent's fresh [CLI receipts](../../../output/step44-calibration-runner-output-20260905/integration-lcNfih/receipts.json), SHA256 `0d90ada749dfc34d4edaa6f37d48c8cdcc637244bd82a32768cde55c143fee7f`, record both genuine CLI entries, 22 direct closes and 28 unique PIDs, with zero timeouts, remaining recorded processes or signal errors. The parent independently found all 28 PIDs absent, matched all eight positive artifacts by hash and byte count, and opened both fresh trace ZIPs, each containing fourteen parsed runner events.

## Adjacent crop checks remain synthetic

The accepted crop-test boundary passed **11/11** in its separate [coordinator log](../../../output/calibration-crop-harness-20260905/coordinator-acceptance/selected.log), SHA256 `ab088bf0a83e419c7a54b61117a656edd87598e70fe1103560d3ef6acae21108`. Its source test is bound at SHA256 `ab17865d6783dcf6a1e81978f4e5246df222a546b9974118a5b514a8418a0559`. Four refusal cases made zero artifact-reader calls; seven admitted/failing-output cases made one each. Every case made zero native spawns, removed its owned crop directories and preserved neighbor sentinels. Moving the reader before the unauthorized-request check made the control fail with one read instead of zero. The invalid withheld-data request stops at request mismatch, before the genuine capability validator; it is not proof of that validator's behavior. The wrapper and reader remain synthetic, so this neither executes a native runtime nor decodes source material.

## Remaining limits

The unavailable file-symlink control is not a pass. Real junction, dangling-junction, hard-link and replacement controls did execute. Process evidence covers normal closure of recorded PIDs; forced cleanup, inaccessible-PID and `EPERM` cleanup branches were not exercised. Earlier logs, failures and reviews remain retained, including a later correction that an older ZIP-size label described uncompressed trace-entry sizes. The fresh parent ZIP inspection above is separately bound.

Output containment does not establish diagnostic-content release, general OS isolation or complete Stage-1 readiness. Exact runtime recovery, genuine camera correspondence and the full first-50 campaign remain incomplete. The failed camera candidate and disqualified source incident stay rejected, with no camera retry admitted. Source-measurement admission remains closed. No default/full verification, native execution, qualification, commit or push followed this increment; the disclosed protocol and later stage decisions still govern those actions.
