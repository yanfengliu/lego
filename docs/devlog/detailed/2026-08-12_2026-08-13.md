# Devlog — 2026-08-12 through 2026-08-13

## Regenerated identification closure advances the prepared ledger but keeps refusals

**Timestamp:** 2026-08-13 19:15–20:45

**Action:** Regenerated match `/3`, distances `/3`, cards `/4`, prompt-bound Claude Opus 5 answers `/4`, all six score `/2` variants, score-summary `/2`, catalog coverage `/2`, Builder calibration, and action-ledger `/2` from the exact retained PDF, callout, inventory, card-image, element-resolution, and pair-truth bytes. The answer collector preserved every validated prior response after a bounded call failed, so an incomplete model pass remains measurable instead of becoming either an all-or-nothing rerun or a fabricated answer.

**Result:** The current run has 285 cards and 273 retained answers; clusters 1, 2, 3, 5, 38, 218, 219, 220, 221, 282, 283, and 284 remain explicitly unanswered. The headline adjudicated one-to-one score identifies 820 of 859 callouts, leaves 39 unidentified, holds 233 of 276 elements exact, reconciles 1,345 of 1,465 inventory pieces, and reports 58 over-claimed plus 120 under-claimed pieces. Exact crop-plus-current-claim truth binds 68 callouts / 104 pieces in this headline generation while 14 preserved verdicts remain inert; the preceding dry deterministic derivation's 67 / 104 and 15 remain historical facts rather than being rewritten.

**Prepared chain:** Catalog coverage is complete only through printed step 22 because step 23 needs two absent `25269` quarter tiles; across the whole report 149 of 333 callout-bearing steps are complete and 866 of 1,403 claimed pieces are catalog-placeable. The separately prepared, unauthenticated action ledger retains rows 1–25, 44 direct pieces — 38 pair-judged and 6 vision-kept — and 92 explicit refusals; steps 2, 9, 13, 15, 18, and 23 remain empty. Step 26 is left out because its trusted callout claims two `28802` pieces while the official Builder cut carries twelve design entries with no `28802`, so every later row remains outside the ledger. Source-boundary alignment is not complete trusted placement or reconstruction.

**Claude failure diagnostics:** The first live authentication failure put its 401 cause on stdout and retained no answer. The child-boundary diagnostic now reports only UTF-8 byte counts, an integer 100–599 API status, the exact allowlisted terminal reason `api_error`, omitted-result length, and static remediation; a 401 names the supported `claude auth login --claudeai` command, a 429 names bounded waiting, and other statuses name authentication and pinned-model access checks. It never copies raw stdout, stderr, provider result, prompt or path, session material, model output, or an unknown terminal reason into the error. Bounded retries still timed out on twelve cards, which remain unanswered evidence rather than guessed content.

**Visual check:** The three unjudged step-13 query/claim pairs for `3460`, `91988`, and `3958` were opened at original detail and each looked like the same part on both sides. That contextual inspection did not edit pair truth or grant trust: current coverage still labels the first two `self-contradicted`, the third `unanswered`, and the action ledger retains zero step-13 pieces.

**Boundary:** Regeneration does not authenticate a model answer, transfer a verdict between crops, admit a catalog part, mutate a user document, grant execution or acceptance authority, or produce browser-output `/4`. The current browser runner still refuses at step 1 with zero pieces; the prepared ledger's farther source cursor is an input diagnostic, not a completed browser build.

**Validation:** The two repaired stale-edge files pass 15 of 15 tests, the focused identification integration passes 144 of 144 across 14 files, the strict-JSON scope passes 35 of 35, and the source-attestation scope passes 8 of 8 against the exact 3,334-file closure `sha256:e7fbc68c455f775f55e78a5fee7cc49b5fdaa57caedbe81c2a05e2a20f1ab9fb`. The authoritative `npm run verify` passes 256 unit-test files with 2,326 tests and three skips, 37 headless browser tests with nine explicit opt-in skips, all 428 Python tests with 31 intentional historical skips, every schema, generated-file, BOM, part, lesson, notice, formatting, lint and type check, and the production build guard. An early focused review found no remaining issue in those two stale tests; the broader report-trust failures found afterward are recorded in `2026-08-13_2026-08-14.md`.

## Identification refinement stops cross-crop trust at the step-13 defect

**Timestamp:** 2026-08-13 16:30–19:15

**Action:** Versioned the deterministic identification closure to match `/3`, distances `/3`, truth `/3`, coverage `/2`, score `/2`, and score-summary `/2`. The matcher first reproduces the exact legacy threshold-only visual groups and then refines only inside each group by the member's independently ranked unique top inventory element plus the existing direct-distance bound; no member may move into another legacy group. Pair judgements now bind an exact full crop SHA-256 and claimed element, vision confidence belongs only to the crop actually shown, and nonlead members remain unreviewed instead of inheriting the lead's answer. Cards and answers remain `/4`, but their digest edges force complete regeneration when match bytes change.

**Result:** A read-only derivation over the retained authenticated features produces 285 groups from 269 legacy bases, splitting 15 bases while preserving all 859 physical callouts and 1,464 pieces and making no cross-base merge. The disputed step-13 crop `sha256:de08a478afe862014eba5d5656fbc14ce3a7146c03b8f2b15f85d4a88ba07d8e` becomes a singleton whose descriptor ranks element `395826` / design `3958` at `0.02892358597981141`, ahead of inherited element `4166619` / design `41539` at `0.03986153076112695`. Exact crop-and-current-claim binding retains 67 judged callouts covering 104 pieces; 15 historical verdicts remain preserved but inert because their claimed element no longer matches the current derived claim. None was rewritten to make the new assignment pass.

**Truth migration:** The tracked truth file moved from shortened-digest `/2` to full-digest `/3`. Commit `59ca5b7` contains the pre-truncation full hashes; all 82 verdicts and both unjudgeable rows match those historical crop bytes and the current feature crop identities, while element IDs, outcomes, notes, rater metadata, order, and counts remain unchanged. Byte-identical repeated crops may share one exact verdict, but a common prefix, visual similarity, or cluster membership cannot transfer it to another crop.

**Reasoning:** Thumbnail similarity is an economy for choosing which drawing to show, not evidence that two crops name one part. Review rejected a global guarded greedy rerun because it could merge members across old groups, required distances to bind the exact new match, required full rather than prefix crop identities, and found cluster-level truth propagation in both JS scoring and Python reports. The final monotone refinement preserves every old visual boundary and makes member-local evidence stay member-local.

**Bounded consumers:** Python readers now require the exact features `/3` -> match `/3` -> distances `/3` and cards `/4` -> answers `/4` digest graph, full truth `/3`, coverage `/2`, and score-summary `/2`; mixed generations fail closed. Descriptor shape is validated before ranking, and every quick/full comparison family is charged before work: lead reproduction, exact-member ranking, conservative miss ablation, same-mould sibling comparisons, and, in full mode, every nonlead inventory row and within-cluster pair. The current 285-group derivation needs 265,763,456 descriptor cells in quick mode and 391,591,536 in full mode, both below the 536,870,912-cell ceiling.

**Boundary:** No ignored match, distances, cards, answers, score, coverage, action ledger, or run artifact was regenerated. The retained artifacts remain a prior generation and current consumers refuse to mix them. The dry derivation does not authenticate the step-13 identity as human truth, advance the ledger, admit a catalog part, mutate a document, grant authority, or replace the historical exact-five/source-parity publications; no new dense source-parity probe or native-image review was run.

**Validation:** Focused JS tests pass 119 of 119, the focused Python work-contract suite passes 16 of 16, the full Python gate passes, and the exact source attestation covers 3,309 files at `sha256:21e2abc83f7761ace7b7e8a310c0ea8202a3aee3ac5e0b314d30cf760f6b5ea5` with all six checks passing. The authoritative full `npm run verify` result and the independent post-repair harsh verdict are pending the final integrating run.

## Step-13 alignment diagnosis is explicit but remains unbound

**Timestamp:** 2026-08-13 15:55–16:20

**Action:** Added a pure, bounded diagnostic for the fixed printed-step 12–14 window anchored after step 12 at identity cursor 26. Its accessor-free parser rejects proxies, sparse or decorated arrays, custom prototypes, coercion hooks, reordered steps or phases, cursor drift, duplicate or split phases, and incomplete whole-phase windows before comparing callout units with Builder identities. The result separately retains whole-phase boundary ranges and member-local identification signals, freezes the full output, and fixes source verification and admission authority absent.

**Result:** The current synthetic shape partitions step 12 as cursor `[25,26)` with `3795`, step 13 as `[26,29)` with `3958`, `3460`, and `91988`, and step 14 as `[29,32)` with `77844`, `3032`, and `3023`. Its step-13 `41539` claim disagrees only with Builder `3958`; a lower member-crop candidate distance and a 36-versus-64 stud-core count both support `3958`, so the helper returns `misidentification-indicated-unverified`. One member-local signal is insufficient, conflicting member-local signals are ambiguous, and an inherited `same` verdict on a different crop contributes no support. A caller-asserted nested phase can produce only `boundary-indicated-unverified`, never authenticated source truth.

**Visual inspection:** Opened the ignored original-detail crop `output/callout-thumbnails/runs/1e37c50ffee4df7741ac6722/p17-q1-x46d591-y469d673.png` (`sha256:de08a478afe862014eba5d5656fbc14ce3a7146c03b8f2b15f85d4a88ba07d8e`) and saw the current member as a 6x6 plate. Separately opened the inherited lead crop `p14-q1-x437d251-y419d109.png` (`sha256:83a213b9ff8d5e0f2a721dc9415288c8c65088f7394fd52bdb24d557e5a34c08`) and saw an 8x8 plate. These ignored images are task-run diagnostic evidence only: the helper does not read or authenticate them, they were not promoted into Git, and the observation is not truth, admission, or authority.

**Boundary:** This unit does not mutate the action ledger, advance its aligned-through step, change the official model or callout coverage, bind either caller-supplied source digest to retained bytes, admit a part identity, or authorize document mutation. A separately source-bound review would be required before any ledger correction.

**Validation:** The focused diagnostic suite passes 8 of 8 cases. The repinned exact source attestation covers 3,309 files at `sha256:9365c2263b28a5ff1b4689fab82efd8a454aaacf30db678a1f4d16aae12a9eeb` and passes all six source-attestation checks. The authoritative `npm run verify` gate then passed from schema checks through the browser suite and production build.

## Verified branch bytes and prepared input now support repeated inspection

**Timestamp:** 2026-08-13 15:20–15:30

**Action:** Browser branch-role transport now retains one private verified copy of each compiled and observation role and returns fresh storage for exact indexed step slices, so caller mutation, detachment, or mutation of an earlier read cannot alter later inspection. Prepared-run bytes now undergo one bounded parse whose branded authority-absent inspection supports exact step and observation-policy lookups without reparsing hostile input.

**Result:** These helpers remove repeated transport and prepared-input parsing from the next semantic-reader boundary while preserving existing wrapper behavior and refusing unbranded inspections or missing steps. They do not implement a browser-output `/4` reader or live run, close opaque source/camera commitments for selected provenance, mutate a document, or grant completion authority.

**Validation:** The exact source attestation covers 3,306 files at `sha256:4d36cdc57460cd9da1ed8032459bd0fd66bd8be9e6d04d4642dee513bd9708be`; its focused suite passes all six checks. The authoritative `npm run verify` gate passes 250 unit-test files with 2,282 tests and one skip, 37 headless browser tests with nine explicit opt-in skips, all Python, schema, generated-file, BOM, part, lesson, notice, formatting, lint and type checks, and the production build guard. Independent harsh review found no P0, P1, or P2 in the helper unit.

## Real exact-five execution exposes and closes three scale defects

**Timestamp:** 2026-08-13 13:35–14:30

**Action:** Ran the exact-five source-parity calibration against the real 6651557 booklet under the required bootstrap source lock, inspected every failure artifact as it appeared, and repaired three defects the synthetic path did not exercise. The execution mirror now lives in a process-owned contained child beneath the repository's ignored `tmp/` so Vite can serve the locked modules without admitting the whole operating-system temp tree; panel evidence hashing now ignores caller insertion order while preserving the historical `/2` producer byte order under a fixed digest vector; and calibration base64 validation now uses a bounded linear canonical scan with safe-integer caps and cause-specific errors instead of a stack-overflowing large-string regular expression. The oversized ledger facade was also split into focused 490-line validation and 233-line contract modules without changing its public import path.

**Result:** The final source-locked run passed all four calibration browser cases in 1.6 minutes and published five roles, ten PNGs, and eight provenance roles under audit-only execution identity `sha256:979157ed12ef36fcd59a85aba13268d56a47d77ee886070cd31221805d756462`. It binds the 19,918-byte capture `sha256:d974db003cdb40be34f4b49b2c21d367b944bd8dc8998d47cd683d3d7b5166c5`, 177,812-byte full prepared manifest `sha256:976e1a1f1a33837d2b8f284f0942dba56bbd6d1076024de177d583800dae9954`, and 28,091,127-byte browser result `sha256:e41b58b62164b1152854284af90ee02db478ae15d5c1a7210a21468fe63f2de7`. Its bootstrap lock covers 3,342 files and 136,622,543 bytes, its execution mirror covers 3,975 files and 212,508,851 bytes, its served-source bundle covers 142 files and 78,823,165 unique bytes, and its three served-response roles cover 36,213,840 bytes. The discoverable summary remains `pending-unreviewed` with authority absent for `pending-human-review/1`; this run is not human adjudication, source truth, calibration admission, browser-output `/4`, document mutation, completion, or permission to launch the dense 359-step probe.

**Measurement:** P/D, P/W, and D/W respectively measured step 90 as IoU 0/0/1 with 7,056/7,056/0 differing pixels; step 101 as 0.9988090512107979/0.3538677918424754/0.3541080472706809 with 3/4,594/4,591 differing pixels; step 346 as 1/0.49537376488251106/0.49537376488251106 with 0/43,359/43,359 differing pixels; step 358 as 0.9642158233156276/0.7160058127465228/0.7425783682790118 with 128/1,368/1,240 differing pixels; and step 359 as 1/0.9714194036350301/0.9714194036350301 with 0/2,494/2,494 differing pixels. These values diagnose derivation disagreement; no aggregate or high IoU chooses which mask is truth.

**Visual inspection:** Opened every final PNG at native detail: `090-high-45c4ed7bb8f89a37497859c4202b972533a36288b5363d5ebc553643a3dd1cbb.png`, `090-work-1f44c573d64c3e7a6c7b8b9d510601b3f61f9f61416af39f492da4446f9b7bf2.png`, `101-high-a738b103a741a951cac509cd495ec85e9796a6ce4632b2c4c8f1249b7592b34c.png`, `101-work-ad5fb4936b711fb0e47661f98ee898b1551ad69dfe941a92750bf3f962300304.png`, `346-high-0f327e9565408bbcdb10cadf44f62ca4dc5604fbe1cf09bfddc637529da2c620.png`, `346-work-2c020d6399dcd317c072159e8a96e2bd901a9e552d4de24a1c8adea62c30e229.png`, `358-high-8ec811b21cf741e57675bd7b9df45c89bd4d4fc86d411f68e4599c4cf2e44e98.png`, `358-work-0618f08af74197204edab37fc94d792247b169df9cc2104ed52cf4c2fefa1d3f.png`, `359-high-509799cf04de9c65895083f9f6f8de95402552af33ad1238c18759245f5fdecd.png`, and `359-work-be79d29807ce80b0791c1c9c65e5d1420e0e52704a604a8deead9dfd07c33cb0.png`. Every high/work pair preserved the same full crop, panel composition, callouts, arrows, highlights, and alignment; expected downsampling reduced fine outlines, studs, and the small multilingual footer at step 358. This is visual QA of capture fidelity only, not mask adjudication or human-review authority.

**Validation:** The exact source attestation now covers 3,305 files at `sha256:8664ff4b19c47351fc99ff126947f18105984bae0a6c66ac722b2fbc984a912c` and passes 6 of 6 checks. The authoritative `npm run verify` gate passes 248 unit-test files with 2,277 tests and one skip, 37 headless browser tests with nine explicit opt-in skips, all Python, schema, generated-file, BOM, part, lesson, notice, formatting, lint and type checks, and the production build guard. The final source-locked exact-five run passes 4 of 4 separately. Independent harsh review found no remaining P0, P1, or P2 in the repaired execution, digest, transport, and cleanup boundaries.

**Code reviewer comments:** Review prevented a silent `/2` digest migration by requiring the canonical field order to preserve the historical producer bytes, required the Vite mirror smoke to skip off Windows, strengthened cleanup from prefix-only naming to process-owned directory identity, split base64 error causes, and moved the digest vector into its own focused test so every touched source file stays below 500 lines.

## Exact-five publication binds source execution without minting authority

**Timestamp:** 2026-08-13 11:29–13:14

**Action:** Added a separate source-bound publication wrapper around the exact-five capture. It requires the exact full 359-row prepared-panel manifest and held source snapshot/provenance, cross-binds the PDF, five-row contract, transient browser-result bytes, bootstrap/execution/served source, checkout and runtime closure, and derives the execution identity internally as audit data. The publisher writes the capture manifest, full manifest, five external roles, ten PNGs and provenance beneath an identity-addressed run directory, then atomically replaces the fixed discoverable summary last; its retained reader reopens every byte class and reproduces the identity.

**Result:** The exact-five pixels are now retained with a reproducible source execution rather than a caller-authored identity. Publication still fixes authority to absent and review state to `pending-unreviewed`; the audit digest is not a bearer capability, human judgement, calibration admission, browser-output `/4` result, document mutation, or completion claim. No human-owned issuer exists, and the real-booklet exact-five and dense probes were not run.

**Reasoning:** Captured pixels alone could prove their internal H/P/D/W derivation but not which source execution produced them. Requiring the full manifest bytes and reconstructing the exact source closure closes that gap without collapsing provenance into authority; writing the one discoverable summary only after every content-addressed dependency prevents a partial run from becoming current.

**Validation:** The focused publication and capture matrix passes 23 of 23 tests; the ordinary Playwright loader and synthetic constructor pass 2 of 2 while the real-booklet exact-five opt-in remains explicitly skipped. The repinned source attestation covers 3,302 files at `sha256:a811a66facf6acd0ba44e7e553a5a756936099a74688ea781884df4ea77187b9` and passes 6 of 6 checks. The authoritative `npm run verify` gate passes 2,272 unit tests with one skip, 36 headless browser tests with nine explicit opt-in skips, every Python, schema, generated-file, BOM, part, lesson, notice, formatting, lint and type check, and the production build guard. Independent harsh review found no remaining P0, P1, or P2 in the publication closure. The real-booklet exact-five and dense probes were not run.

**Code reviewer comments:** Independent review required bounded canonical manifest parsing before deep reconstruction, explicit UTF-8 BOM refusal, one-shot capture-shape discrimination, preflight before output-tree creation, a privately branded retained artifact, and hostile tests that coherently rebind self-reported digests before semantic reproduction refuses them.

## Exact-five browser capture closes pixels without claiming their execution

**Timestamp:** 2026-08-13 08:13–11:29

**Action:** Added a strict browser-only capture for fixed steps/pages 90/79, 101/87, 346/213, 358/218, and 359/219, derived from the complete prepared-panel manifest. It retains five bounded external roles and ten lossless PNGs for exact high/work RGBA, H/P/D stages, W, and pairwise facts; the hostile Node reader reconstructs the transient wire, decodes the PNGs to the retained pixels, replays H/P/D, and independently re-derives W from the work raster and crop.

**Result:** The capture is pending, authority-free, deliberately unbound to an execution identity or publication/source closure, and not retained by the opt-in spec. It cannot authenticate human review, launch the dense probe, enter browser-output `/4`, mutate a document, or claim completion. The real-booklet exact-five opt-in was not run.

**Reasoning:** A bare execution-identity digest would have been self-asserted because this unit cannot reproduce the source snapshot that supposedly issued it, so the field was removed rather than retained as false provenance. The capture closes pixel derivation only; a later publication wrapper must independently bind its exact browser bytes to the source execution before a human-owned issuer can consume it.

**Validation:** Focused browser-input, calibration-input, stage-trace, review-foundation, and capture tests pass 41 of 41; the ordinary headless loader and synthetic browser constructor pass 2 of 2 while the real-booklet opt-in remains explicitly skipped. The source attestation covers 3,293 files at `sha256:a1095109068f71d1f23d6d469c7d8e3087c3e4d4bc3efc5317cb053961f100d8` and passes 6 of 6 checks. The authoritative `npm run verify` gate passes 2,264 unit tests with one skip, 36 headless browser tests with nine explicit opt-in skips, every Python, schema, generated-file, BOM, part, lesson, notice, formatting, lint and type check, and the production build guard. Independent harsh review found no remaining P0, P1, or P2 in the capture closure. The real-booklet exact-five and dense probes were not run.

**Code reviewer comments:** Review forced aggregate typed-array preflight before any copy, bounded adversarial PNG chunk splitting, caller-labelled PNG decode errors, a nondegenerate P/D/W fixture, and a W forgery that rebinds every self-reported digest before independent re-derivation refuses it.

## Source-stage parity closes diagnostic bytes without choosing truth

**Timestamp:** 2026-08-13 03:24–08:13

**Action:** Extracted the live high-resolution and work-raster derivation into one shared measured path, added an exact packed stage trace, and added source-parity `/4` as an opt-in dense 359-step by five-class browser diagnostic. Publication now binds the exact PDF and prepared-panel manifest, bootstrap and execution source closures, served responses and retrievable served source, runtime and checkout identity, exact browser-result bytes, packed production/candidate/XOR evidence, PNG diagnostics, and contained atomic output. A separate exact-five foundation derives its fixed panel contract from all 359 prepared rows, structurally inspects the bounded external-review packet value, compares candidate W, and deliberately exposes no full-probe execution capability.

**Result:** H is the high-resolution cleaned-art mask, P isolates at high resolution before downsampling, D downsamples before isolating as the counterfactual, and W is the independent work-raster candidate. Exact stage bytes and dense parity evidence are inspectable, while work-RGBA, policy, and derivation browser commitments remain opaque and the source-RGBA column remains visualization-only. Truth absent, evidence drift, W difference, and exact W equality all return `needs-adjudication`; no human-owned issuer, execution-to-W provenance, browser-output `/4`, runner integration, document mutation, or completion authority exists. The full 359-step real-booklet probe was not run.

**Reasoning:** The production pipeline and work-raster candidate differed in operation order, so a final mask comparison could show disagreement without revealing whether it arose before or after downsampling. Retaining H/P/D makes that order measurable, while keeping W separate prevents the diagnostic from manufacturing its own expected answer. A hash over source descriptors or a packet string claiming human review cannot establish who reviewed the pixels or which execution produced W, so exact byte equality remains an inspection fact until a human-owned issuer and execution-to-W provenance boundary exist.

**Validation:** The focused source-stage, candidate, parity, hostile publication/parser, served-source, and calibration matrix passes 94 of 94 tests. The final source attestation covers 3,273 files at `sha256:b4f2050b391a074b9743a2217dccc5b098d64c6857c3fd0683eabfe2eff24249`, and its attestation suite passes 6 of 6 checks. The authoritative `npm run verify` gate passes 2,249 unit tests with one skip, 34 headless browser tests with eight explicit opt-in skips, every Python, schema, generated-file, BOM, part, lesson, notice, formatting, lint and type check, and the production build guard. Both dependency audits report zero vulnerabilities after pinning the dev-only transitive Nano ID dependency to 3.3.18, and the canonical Vitest pool now uses half the advertised workers after the uncapped 31-fork pool produced load-only timeouts. Independent review found no remaining P0, P1, or P2 issue. The real 359-step probe was not run.

**Code reviewer comments:** Independent review held the hostile outer input, exact checkout-root/source mapping, served-response and source-bundle retrievability, browser-result causality, bounded publication, opaque browser commitments, and unverified review claim as separate boundaries. The accepted design keeps parity schema `/4` distinct from browser-output `/4` and makes exact calibration equality incapable of authorizing work.

## Atomic branch compilation preserves convergence without granting execution

**Timestamp:** 2026-08-12 21:36–2026-08-13 00:41

**Action:** Completed the inspection-only bridge from detached opaque enumerator offers through one prepared-search reservation and the restricted automatic BuildProgram compiler into compiled-lineage `/1`. The batch retains the exact ordered search request, compiler metadata, root and child canonical bytes, deterministic receipts, physical transitions and parent-specific lineage edges; independently reruns every unique transition in Node; serializes only a digest- and length-bound base64 descriptor; and leaves observations, selection, score, completion, acceptance and document mutation absent. Browser-local provisional preparation now commits detached run, source/module, fetched-PDF, panel, crop, action, raster, mask and fit facts, but generation `/3` treats preparation defects as shadow-only and preserves its exact camera-handedness refusal.

**Result:** Eight root lineages reserve eight proposals, converge to one exact child and physical transition, and retain eight distinct child lineage edges. A seven-slot ledger refuses the eight-request batch before any compiler call and emits no child frontier. Compiler substitution, a non-reproducing refusal, thrown or trapped output, replay mismatch, child alias, a crossing aggregate child-byte cap, and aggregate evidence closure all retain typed terminal evidence with zero child candidates, transitions or edges. Unique child canonical bytes and the complete evidence envelope are each bounded at 64 MiB, and a sequential ledger remains terminal after its first refusal.

**Reasoning:** Proposal identity was initially stored on the shared physical transition, which made legitimate convergence impossible; moving it to each lineage edge preserves both one physical result and every parent history. Reservation scalars alone could not prove what was refused, mutable `Uint8Array` output could drift after parsing, and self-reported compiler failure could manufacture a deterministic refusal, so the exact request, fresh decoded storage and current-compiler replay are part of the evidence closure. Root canonical bytes and child strings also cannot be repeated once per proposal or retained once per work because legal maximum shapes would amplify bounded input into unbounded memory.

**Validation:** Focused automatic-compiler, prepared-step/search, compiled-lineage, atomic-batch, enumerator-witness and enumeration suites pass 62 of 62 tests; the exact 3,189-file source closure reproduces `sha256:5301bb801dee5727238aad340f9fd1e9f6d40dadea05ddf27e7e5f76faab31a1`. The authoritative `npm run verify` gate passed 2,053 unit tests with one skip, 33 headless browser tests with seven opt-in skips, all Python, schema, generated-file, BOM, lesson, notice, formatting, lint and type checks, and the production build guard. Both dependency audits reported zero vulnerabilities.

**Code reviewer comments:** Independent adversarial review caught that provisional-preparation defects could overwrite the frozen `/3` refusal, parent-specific proposal IDs prevented graph convergence, budget refusal retained an unbound scalar count, compiler failure could self-certify, returned wire bytes were mutable, witness and root-byte work expanded before aggregate limits, transition lookups became quadratic, compiled child strings accumulated before their aggregate cap, and a fake public compiler success could expose an unbounded document before admission. Each defect now has a focused regression or a module-owned brand, and the standalone batch still cannot mint run, visual, completion or acceptance authority.

## Compiled observation replay stays separate from browser provenance

**Timestamp:** 2026-08-13 00:47–03:21

**Action:** Added a separate compiled-observation-closure `/1` rather than changing the committed compiled-placement-lineage `/1` wire. Its only public verifier accepts bounded lineage bytes, bounded closure bytes, nullable exact mask-role bytes and a WeakSet-branded policy derived from the prepared run bytes. It domain-separates source, camera and observation IDs, retains own-panel or strictly later lookahead mode, and keeps panel, crop, source, exclusion, D4-camera and renderer facts as explicitly opaque commitments for a later browser-output `/4` provenance check. Added a branch-evidence `/1` index that binds dense slices in separate compiled-evidence and observation roles without embedding their bytes in browser JSON.

**Result:** Node reproduces MSB mask packing and padding, target-space exclusion, raw centroid registration seed, stride-4 multiscale search, strict score tie order, final stride-1 IoU or containment, prepared minimum score and strict margin, candidate-and-camera ranking, convergent lineage selection and the exact compiled transition summary. Raw-empty source rows remain `not-observable`; a nonempty source hidden entirely by exclusion scores zero; equal top camera groups remain unresolved even for one candidate; typed failures are explicitly unverified. Own-panel mode admits printed step 359, while lookahead requires a later panel. The role transport validates fatal UTF-8, exact role vocabulary, segment limits, strict step order, dense coverage, SHA-256 digests and a 512 MiB combined ceiling, returning frozen metadata and no authority.

**Reasoning:** Extending compiled-placement-lineage `/1` in place would silently reinterpret retained evidence, and mask digests plus caller-supplied scores could not reproduce the live registration decision. The first selector draft also merged equal cameras for one candidate, contradicting the established candidate-and-camera grouping, and an unbranded structural policy let a caller lower its own threshold. A sidecar preserves `/1`, exact bytes preserve the trust boundary, and a policy derived from the prepared input makes selection a replayed result rather than a self-certified field. Source and camera IDs cannot become provenance merely by hashing caller descriptors, so that claim remains deliberately deferred to `/4`.

**Validation:** The integrated compiled-lineage, atomic-batch, accepted-revision, prepared-policy, registration, observation-closure and browser-role matrix passes 93 of 93 focused tests. The authoritative `npm run verify` gate passes 2,117 unit tests with one skip, 33 headless browser tests with seven opt-in skips, every Python, schema, generated-file, BOM, part, lesson, notice, formatting, lint and type check, and the production build guard. The repinned 3,203-file source closure reproduces `sha256:26c74b3c880e4e7cf1896ceabde4ee8c0f0e297a2d4df8c831d097575c82edbf`, and both dependency audits report zero vulnerabilities. Independent hostile-boundary, algorithm-parity, scale, transport and documentation reviews found no remaining P0, P1 or P2 issue. The frozen browser-output `/3` and compiled-placement-lineage `/1` wire remain unchanged; only accepted-transition validation now binds the exact common direct-parent revision.

**Code reviewer comments:** Reviewers caught the incompatible in-place `/1` observation change, arbitrary policy injection, self-hashed but unproved source/camera provenance, same-candidate multi-camera tie promotion, unreproduced failure claims, raw-empty semantic drift, unbound parent revision, repeated root-byte and child-output amplification, mutable evidence bytes, incomplete budget-refusal witnesses, and a premature outer `/4` type that did not match its byte parser. The final unit keeps failures non-certifying, replays current compiler and registration behavior, preflights aggregate work before role access, removes the guessed outer envelope, and names the remaining provenance and runner work instead of claiming it.
