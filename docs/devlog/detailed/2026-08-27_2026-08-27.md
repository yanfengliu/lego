# Devlog — 2026-08-27

## Deterministic placement-growth instrumentation and a three-dimensional collision broadphase replaced a contention-sensitive Gate-3 measurement

**Timestamp:** 2026-08-27 23:03 PDT

**Action:** Replaced the placement-growth gate's suite-contended wall-clock assertion with exact deterministic work accounting over the unchanged 120-step seed, part cycle, legal-orientation population, connector rules, and collision semantics; replaced the collision world's 100-LDU X/Z-only lookup with an immutable 40-LDU X/Y/Z spatial hash while retaining the former finding order and 5,000-finding cap; and retained wall/process CPU timing only as an explicitly instrumented diagnostic.

**Instrument audit:** Four executions produced the same 120-step non-time population fingerprint while the wall-clock late/early ratio ranged from `1.918663` to `3.104289`, proving that scheduling could decide the old gate. Independent profiling then measured a real algorithmic failure underneath that noise: the former X/Z-only broadphase grew to `3.7861` bounds checks per transform by quarter and the exact counterfactual 2-D cell-entry work grows `3.616842` under the final aggregate denominator. The replacement gate divides summed work by summed distinct transforms in each quarter, not a median that could hide late spikes, and binds both exact ordered candidates and all eleven enumeration-count fields.

**Result:** The frozen trajectory still reaches 120 steps / 121 parts with accepted candidates `168` minimum, `1,403` median, `3,520` maximum, and `2,388` at the final step. Its ordered-candidate aggregate is `sha256:5438fbb22bab86c36e72cc441632d1addead783e51b3b16ba141d249941cdf3a` and its catalog/count/candidate population aggregate is `sha256:b98fb9f500f0c9446f9a2e4e5429ecea6e53a5cc786bc849539dba2fc0642481`. Every accounted production category stays below the unchanged hard `<3` ceiling: legacy-order cell checks are the maximum at `2.310218`, broadphase cell entries are `2.012715`, primitive AABB tests are `1.999566`, and exact narrowphase tests are `1.768095`; the deliberately bad 2-D and all-primitives controls remain visible at `3.616842` and `5.645593`.

**Critic repairs:** Independent review rejected a candidate-only digest that could hide changed rejection/count semantics, three upright connection-free collision-oracle cases that could not prove general finding order, and counters that omitted legacy-key construction. The repaired negative control includes every enumeration count, the byte-exact legacy/new oracle now covers proper non-upright transforms, real connection allowances, 40/100-LDU boundaries, multi-cell geometry, duplicate finding classes, and the 5,000-finding prefix, and the gate counts world/candidate legacy keys plus Set materialization. The chief-of-staff reread additionally rejected median-of-per-step work ratios and changed the hard comparison to quarter totals. The final harsh critic found that `ldraw.ts` had reached 1,019 physical lines beyond the hard 1,000-line ceiling; exact frame/error conversion now lives in a focused 186-line module and leaves the importer/exporter at 859 lines without changing its public surface or focused round trips.

**Runner methodology:** Two sixteen-worker full-corpus attempts produced migrating timeout-only failures, and an initially parallel eight-worker experiment was discarded because the simultaneous focused lane competed for CPU and Vitest's unlocked duration cache. Serial isolated trials retained all 431 files / 3,576 tests and the exact Gate-3 digests: four workers passed in 305.34 seconds, while eight workers passed cache-free in 180.94 seconds. The workspace now fixes eight workers rather than a percentage that would silently expand on a larger host; no retry, global-timeout increase, workload reduction, assertion reduction, file exclusion, or whole-suite serialization was used.

**Authority boundary:** This unit makes placement enumeration measurably scalable and its gate reproducible; it does not authenticate official transforms, resolve the eleven quarantines, establish connector/component reconciliation, create a canonical placement, advance replay, or claim any printed-step frontier. Steps 51–359 remain index-only and outside the campaign.

**Validation:** The merged focused run passes 9 files / 53 tests; its frozen score completes in 62.287 seconds. Then-current executable-source attestation covers 3,693 files at `sha256:be58974bd531ecdb880828392d743723d1cdbc28bc5c026e94c62241d54a7552`. One uninterrupted post-critic `npm run verify` passes schema, Node-consumer, migration-history, observation, BOM, part-standard, lesson, notice, format, lint, TypeScript, Python, JavaScript, real-browser, and production-build gates: Vitest retains 431 files / 3,576 tests in 163.20 seconds, Playwright retains 73 cases with 60 passing / 13 intentional skips in 11.5 minutes, and the browser re-measures exactly 50 printed steps on pages 11–53.
