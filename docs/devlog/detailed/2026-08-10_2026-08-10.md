# Devlog — 2026-08-10

One render-truth unit moved the first booklet underside witness onto official geometry without pretending that its preserved physical approximation became exact too; the same evidence pass exposed a separate green-but-wrong vision result.

## The remaining twelve render gaps closed without importing physical claims

**Timestamp:** 2026-08-10 22:00–23:13

**Action:** Added a distinct render-only admission route for the twelve remaining `parts:check` failures, using pinned official LDraw roots for the 2×4 and 2×3 handed wedge plates, 3×6 right wedge plate, 1×4 and 1×6 arches, 1×2 through 1×4 curved slopes, and 1×1 and 2×1 cheese slopes. The generated declaration has no connector, allowance, or collision fields; admission overlays only mesh, exact bounds, frame witnesses, and attribution on the preceding parametric definition, retains connector/collision objects by reference, and byte-compares connectors, allowances, collision, connector-grid centre, and partial-overhang evidence against `/12` literals.

**Result:** Catalog version moved `/12` → `/13` with the roster fixed at 85 definitions: 61 remain parametric-rendered, sixteen are in-place source-mesh promotions with preserved physical recipes, and eight remain full measured definitions. `parts:check` moved 12 → 0. The bundle now contains 24 roots, 163 unique files, 27 named authors, 162 `CC-BY-4.0` declarations plus the one dual-licensed `30503` root, 16,385 stored vertices, and 14,919 triangles.

**Reasoning:** The first production comparison disproved the assumption that indexed source positions plus `computeVertexNormals()` preserved LDraw appearance: it averaged across type-2 hard edges and pillowed planar faces while silhouette IoU stayed 1. The corrected expander matches the pinned Three `LDrawLoader` policy, including type-2 hard boundaries, conditional-line non-boundaries, the 67.5° fallback, complete CW face reversal, and the p3–p1 diagonal for CW quads. All 24 assets therefore carry stored source-faithful normals. Compared with the provisional generated-mesh baseline, source-faithful triangulation changes for 23 assets; `54200` already had the same generated triangle set, but its `/12` catalog drawing was parametric, so `/13` correctly reports render-geometry changes for all 24. Bounds and body/stud groups remain byte-identical for the twelve previously bundled assets. The one regenerated `80015` height-field delta is unused evidence because its render promotion still discards measured collision in favour of the predecessor recipe.

**Validation:** The archive-backed `/3` generator check remeasured all 24 assets in 29.5 seconds with zero hard failures, matched all eight generated files byte for byte, and wrote report `sha256:04a5d99e9384ed6baa5a803ecf6b9d66a9c8c8d7a5682d23035dd73973f9e785`. Focused Python smoothing, expansion, and generation tests passed 4 + 19 + 35 cases; focused catalog, migration, renderer, and truth tests pin 24 normal changes, 24 render-geometry changes, twelve bounds changes, model-specific deduped migration notices, and the unchanged connector and collision digests. The final catalog hash is `sha256:100283423bf1cfecfdfec5ba2216d1834a9eb19b1757c71772f7fa53223190d6`; connector taxonomy remains `sha256:57489cb5a3b5e1bf367984c2768318f151e19051d2b1b6ee3713a7e6ef53f6a2` and collision remains `sha256:a14d660a6b24a63326ab6c24865fc07ea59496b1cf48002cea83a4b615724edb`. The `/13` Builder source audit changed all fifteen definition pins and exactly six geometry pins while leaving every connector and collision pin unchanged; the ordered ignored-input rebuild then regenerated coverage, calibration, and the twelve-step action ledger, whose 26 direct pieces retain two pre-existing evidence refusals.

**Notes:** The generated mesh table totals 1,024,119 bytes, 24,457 bytes below the 1 MiB ordinary-Git ceiling, and is split into a 765-byte registry plus 181,669-, 145,418-, 387,885-, and 308,382-byte chunks. The last two exceed the 256 KiB review threshold because they are the attributed, integrity-bound runtime triangles and normals needed to reproduce source shading without a separate asset store or runtime decoder; splitting keeps every file below 1,000 lines and the generator proves the chunks reproduce from pinned inputs. Native source-versus-production visual admission remains open, and the sixteen promotions deliberately retain conservative predecessor collision recipes.

## Four first-witness parts now draw the expanded official surface

**Timestamp:** 2026-08-10 19:25–19:58

**Action:** Promoted `30503`, `6106`, `30565`, and `80015` in place from their parametric render recipes to the exact expanded official LDraw meshes. The promotion changes visible geometry and bounds only: each definition retains its existing connector, allowance, collision, and semantic provenance arrays. `80015` keeps the seven clutch seats proved by the byte-pinned Builder source, including its two partial-overhang seats, rather than inheriting the five seats in the LDCad shadow.

**Result:** Catalog version moved /11 → /12 while the roster stayed at 85 parts; bundled mesh assets moved 8 → 12 and the pinned LDraw closure moved to 118 files. `parts:check` fell 16 → 12. These four definitions account for five of the six instances visible in printed step 4 because `30503` appears twice, so the flat generic undersides blocking that first witness now render the source rings, ribs, walls, and cavities.

**Reasoning:** The measured height-field collision generator fills a column from the highest sampled surface downward. That cannot reconstruct hollow truth from open, non-CSG LDraw meshes, so using it would make the picture exact while silently replacing known placement behavior with an unproved solid. Render and physics were therefore separated explicitly: the exact mesh is the visual source, while `collisionMode: preserved-catalog-recipe` names the conservative legacy approximation instead of calling it mesh-derived or hollow.

**Validation:** After deliberate emission, the corrected archive-backed `--check` independently remeasured all 12 mesh-backed parts, ran the pinned Prettier, and matched all three committed files byte for byte. Its `/2` report binds the 10,130-byte source pilot and 46,200-byte Builder-frame input by SHA-256; the four new scores are 0.982329–0.988201 and all pinned clutch seats are accounted for at 7/7, 10/10, 26/26, and 11/11. Focused catalog, rendering, mass, wedge, convex, admission, migration, and truth tests passed; regressions pin the 85-part order, the four in-place replacements, exact source modes, byte-identical connectors and collision primitives, bounded preserved collision envelopes, and named `/11` → `/12` visual reinterpretations. `recipes/6651557.pdf` page 11 and the retained step-4 render were inspected together; the source exposes underside cavities where the old render showed a slab.

**Notes:** “Exact” in this entry applies only to the expanded LDraw render surface. The preserved collision recipes still fill some visible voids and cannot support a claim of exact interior physics; the remaining 12 `parts:check` failures are separate render-geometry work. `mesh-assets-6651557.ts` is 321,069 bytes, above the 256 KiB review threshold but below the 1 MiB text ceiling; ordinary Git is justified because these exact expanded triangles are the attributed runtime render input for all 12 surfaces and the generated table is kept reproducible from pinned local sources rather than introducing an asset-store dependency for this private workspace.

## The narrowing stayed safe and lost the answer

**Timestamp:** 2026-08-10 19:57

**Action:** Replayed the retained step-4 panel reading against the settled document and the full enumerator output, then ran the oracle and single-field perturbation controls in `panel-reading-booklet.test.ts`.

**Result:** The model correctly called the panel an underside view but described both pieces as beside one another with zero overlap. Its 1x8 reading narrowed 480 placements to 12 and dropped the settled transform; its wedge reading retained all 190, could not resolve the not-yet-placed anchor, dropped the settled transform, and made the product unusable. The oracle kept the settled wedge while narrowing 190 → 2. All 6 focused tests were green because the safety invariant held: no survivor came from outside the enumerator.

**Reasoning:** Candidate provenance and visual truth answer different questions. Subset-only narrowing prevents a model from inventing an illegal placement, but it cannot make the model's description correct or guarantee that the real answer survives. The booklet also disproved one-panel lookahead as a universal witness: the next panel can occlude the disputed face, so inspection must continue to the first farther panel that reveals it and say `not-observable` if none does.

**Validation:** The anchor is `recipes/6651557.pdf` page 11, retained run `2026-08-09T06-52-45-853Z-622f66a3fe8d-2c210f0e-d99f-42c4-85f8-189bf740031c`, ignored readings under `output/panel-placement/`, and the focused Vitest replay. The transferable rule is recorded in `docs/learning/`, and the face-specific admission plus N+1-or-farther witness requirements are consolidated in `docs/policies/local-rules.md`.

## Multi-panel vision gained an executable refusal boundary

**Timestamp:** 2026-08-10 21:58–23:15

**Action:** Added an independent N/N+1/conditional-K vision checker around immutable source, candidate, prompt, face, lineage, response, and budget packets. Added a subscription-CLI adapter whose task-owned stdio MCP server exposes exactly one empty-argument image tool once while CLI built-ins and setting sources stay disabled; the exact stream-json tool/result trace is retained and the temporary root is identity-scoped and cleaned. A separate harsh-critic pass then found that the mocked trace carried one unrelated image and still passed, recomputed outer digests could bypass nested canonical and postflight checks, impossible farther sequences transmitted N/N+1 before refusing, model-visible text carried unnecessary repository identifiers, and mid-run failures trapped completed attempts.

**Result:** The answer space is exactly `same`, `different`, or `unjudgeable` with seven bounded reasons. `different` vetoes the whole same-step atomic candidate group, `same` only corroborates pending deterministic validation, and exhausted ambiguity records `not-observable`; no result may certify, mutate, bypass a validator, or claim authenticated authority. The corrected transport parser requires the retained tool result to reproduce every bound label and image block exactly; canonical reconstruction defeats rehashed request/attempt forgeries; hard step, PNG, pixel, byte, call, token, cost, time, and retention ceilings cannot be widened; full-sequence preflight happens before transmission; later calls bind remaining cumulative budgets, with CLI cost/time enforcement and token/retention postflight refusal; the model brief and child environment are minimized; and a typed failure carries prior sealed attempts. Conditional lookahead tests encode the inspected booklet facts: step 4 underside, step 5 studs-up, step 6 unable to establish step 5's green plate, and step 7 the first clear farther underside witness.

**Reasoning:** A hard cap at N+1 can accept silence: the next panel may hide exactly what the check exists to verify. A general CLI `Read` grant fixes transport by destroying source containment, while text-only streaming never sends pixels at all. Binding exact PNG bytes and serving them through one no-argument image tool makes the visual boundary executable and auditable without giving the model a repository handle.

**Validation:** `npm run test:vision` covers 56 contract, adversarial mutation, sequence, direct-MCP, subprocess-MCP, and mocked-CLI cases, including one-field tampering, recomputed-digest attacks, malformed/oversized PNGs, face-fold and hard-budget bounds, unchanged N/N+1 lineage, unordered duplicate parts, same-step atomic groups, complete preflight, N+1 occlusion/K visibility/no K, exact four/six-image ordering, wrong-image tool results, bounded stdio and turns, redundant call-field mutations, prior-attempt recovery, minimized environment, and cleanup faults. A live no-`Read` probe reached Claude authentication but stopped with `OAuth access token has expired. Re-authenticate to continue.` No live visual verdict exists, the real-PDF packet producer is not wired, and the real-build driver does not consume this checker.
