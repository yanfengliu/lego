# Devlog — 2026-08-26

## Exact embedded source art extended sparse semantic identity without crossing into placement

**Timestamp:** 2026-08-26 01:39–03:06 PDT

**Action:** Added a standalone compiler, strict PDF image-closure protocol, real two-lifecycle CLI, and independent opaque verifier above the exact 70-relation legacy-recut semantic handle. It authenticates the full 881-row / 359-step source roster, exact 70,238,655-byte PDF, and official first-50 quantity cut while measuring only the 187 part-art callouts / 320 pieces through printed step 50. Exact classes require the same decoded RGB24 digest, dimensions, kind, milli-quantized linear transform, and ordered strict-allowlisted image closure; terminal resource names are alpha-renamed only after exact dependency/paint equality, page translation is normalized with its clip coordinates, and nearest-milli rounding is translation-invariant.

**Result:** Sixteen source-art relations / 40 pieces extend the sparse semantic roster from 70 / 107 to 86 / 147. One hundred one relations / 173 pieces remain residual. Every measured member separately retains a nonempty support mask, internal isolated/full support-RGBA equality, and zero on-support interference; page translation can change raster phase, so cross-member mask/RGBA equality is not claimed. The final ignored artifact is 211,319 bytes at `sha256:4be7bd77d386a7a656019affe9c995e77135080a7aa90df19e43a6f2167ab721`.

**Authority boundary:** The artifact exact-compares a closed identity-only authority object and publishes no source execution, prepared run, physical Brick assignment, frame, transform, catalog admission, coverage trust or publication, action, document mutation, replay, placement, accepted document, or completion authority. No production coverage or action-ledger consumer is integrated, and steps 51–359 retain indexing only.

**Critic repairs:** Independent review first rejected broad decoded-image grouping because it omitted the exact clip/program and per-member interference proof. The repair moved to a full ordered strict closure and deliberately reduced automatic transfer. A second review proved that resource alpha-renaming, milli quantization, and cross-member raster-phase differences were real but underspecified; schema `/2` now names and gates each normalization instead of claiming literal operand or raster equality. The final review found an origin-dependent sub-milli dead zone that made a relative `0.0006` path coordinate hash differently after pure page translation. Removing that shortcut and adding positive and negative translated threshold probes restored one nearest-milli rule. The final independent rerun reports no supported P0–P2 finding.

**Validation:** Focused Vitest passes 12/12 across the compiler, PDF-program, and CLI suites. Prettier, targeted ESLint, workspace TypeScript, and the real two-lifecycle CLI pass. The real workflow reproduces 19,750,648 component pixels, 1,846,598 decoded RGB24 pixels / 5,539,794 bytes, 76 full-page renders, 290 isolated renders, 290 control renders, two official-model indexes over 3,806,338 input bytes, and four full XML decodes / 7,612,676 bytes under fixed limits. Current executable-source attestation independently reports 3,660 files at `sha256:fd74cde3eb9e5f809ae24c36253ba99720204f0b6b4221874957c949fae1779e`.
