# Devlog — 2026-08-26

## Exact suffix catalog admissions close first-50 catalog coverage without physical-frame authority

**Timestamp:** 2026-08-26 12:48 PDT

**Action:** Added exact-suffix catalog policy and generated shard-H mesh/blueprint definitions for official `3245c` and `2453b`, while keeping bare and cross-suffix variants absent. Catalog `/28` advances the migration registry and source-derived geometry/connector/collision truth without changing the four authorable upright yaws or treating Builder revision rows as physical-frame authority.

**Result:** Catalog `/28` has 104 definitions: 61 parametric, 43 mesh-backed, 16 render-only promotions, 27 fully measured, and 77 with project-authored physical recipes. Overall truth is `sha256:643185fe21f0d0c77a7aada8b170395f11bb7da1079f97d5c0cd0a03d7464f1b`; the ordered geometry roster is `sha256:60ce979e2b452eb8d3a233f769e7e15ac3a5f5af84ae8b802d94d5cbb8d6b0b2`; 29 reviewed source truths project 2,326 endpoints at `sha256:d6759940824fb337ffbbe8d99c8461f74c945bdf3d2cd7a17113523d64c382ba`. The 43 closures resolve 228 unique files by 33 authors, with 224 direct CC BY 4.0 files, four selected CC BY 4.0 dual-license options, and 19 exact LDCad routes.

**Visual verification:** A genuine headless admission run retained `3245c` packet `sha256:da3821158f5b04b87e8bc67bcd5d17d4aed79afaf7a254ca07cf05178df0c033`, `2453b` packet `sha256:5b96dea2398dfbc49c1c1d01ca9f631770d966c967c594534f7e53abacd30734`, capture batch `sha256:7ed52e1312df351154134a60d4eb0279bb6cc24f31da758957ba5e84bdcdddcb`, native-pair manifest `sha256:15c3d13113213b2cbb7d10ec31deff8dbdeffe54ad052478088296111caa1371`, and review batch `sha256:2d27d92845e619e8ff55f5b034001fbdba80a4bcc71095046e4b1e9536ce85b0`. All sixteen original-resolution pairs were inspected; every PNG and decoded-RGBA pair is exact, `3245c` visibly preserves the open underside without a central understud, and `2453b` visibly preserves its solid top stud and square underside socket.

**Current source and C4 visual rerun:** The genuine headless exact-three workflow reproduced packet `sha256:6b09685905bab077c254adf1e8b32ee2eebd7eefac76d04da678ff47a77c3138`, 20,009 manifest bytes, and 8,592,000 / 2,150,000 / 738,816 high/work/mask role bytes in one lifecycle. All six current high/work images were inspected and retain complete, correctly framed booklet steps 2–4. Current `/28` quotient `sha256:5465db578166f8944e7ec0c0bc980b6ee18b87df5153c9959f9af7af9c87c00c` produced integration `sha256:b719382e202ac6433b34ae9419322e30e463c5a36ba406897ca0ba686598b363`, calibration `sha256:da0bb43963240589bc23929e9fb0860ef04af52fec60a26897b75eedc103a015`, closure aggregate `sha256:6c28f2bf103162d743359bd4465c69e8d8c66f4def6a2c7fddad0d08bd2afa23`, renderer configuration `sha256:0eac516a7e7c5cae475ff4a0138d8569375456fb7a53f752b837464a2decdcbb`, and source binding `sha256:72fbffe386b9e33810dc9e6ae55711993ec3f2ea5ced6d1b2b8eed28282220ad`. All eight 100-representative camera sheets and the separate camera-zero zoom-out sheet were inspected as complete, nonempty, bounded, and unclipped; the fixed 8,192 ledgers remain unchanged, and no winner, transition, document, frame, placement, or completion authority follows.

**Coverage and authority boundary:** Regenerated semantic coverage `/4` is 588,467 bytes at `sha256:a12d5744f3f4417628e53227aaa4c35d9aee0eba5fdce7b865087e6f97dfbfad`, retains all 859 source/index rows, keeps 672 step-51-through-359 identities authority-null, and measures all 49 part-bearing first-50 rows / 320 pieces catalog-placeable. Required-leaf intersection deliberately remains 24 of 121 at `sha256:1065c3a3d04787974aa43c8ede60c1937930884bdf297488d3731dc8c99ea591`; exact `3245c` and `2453b` catalog identities do not bind Builder `3245;M` or `2453;I`, and coverage `/4` remains refused by production action consumers. No source execution, assignment, physical frame, action, placement, replay, document mutation, acceptance, or step-frontier authority follows.

**Critic repairs:** Fresh independent review found no catalog implementation or evidence P0–P2 defect, but rejected stale current-state prose and two incorrect score literals. Canonical docs, runbook, BOM, and devlog now separate historical `/27` evidence from current `/28`, and the cited generator scores are corrected to `0.9834695090275227` for `3245c` and `0.9999272287622425` for `2453b`. The first full gate then exposed only concrete version rebinds: the frozen `/26` diagnostic bridge now requires exact current `/28` truth and removes exactly the four `/27` plus two `/28` runtime-only identities before comparing the unchanged 98-row projection; Builder calibration `/8` changes only fifteen catalog-definition pins and retains 15 exact frames at 20,379 bytes / `sha256:6352e7f753d977b81200e2a7e988342d1edd92d34717064626d2545d44c0b664`; the current enumerator and quotient metadata pins advance without moving their 400-row raw roster. Post-fix review found no remaining supported P0–P2 issue and confirmed that no source, frame, placement, or completion authority widened.

**Validation:** Focused catalog/truth tests pass 311 assertions, migration/element tests pass 101, Python measured/source/LDCad checks pass 237, the schema-`/6` generator check is 258,087 bytes at `sha256:4631f079c210ed75c4a80662955ac96f3d5e822f9deeeb7d675fbbb66d827e1e` with zero hard failures and exact bytes across all eighteen generated outputs, coverage and current-source tests pass 18 assertions, and source attestation covers 3,671 files at `sha256:3144f5c27d6c2117e77e695737f1e5bd0a16245102da258d569c44be0a714643`. The concrete full-gate rebind failures are repaired; authoritative `npm run verify` passes 420 Vitest files / 3,492 tests with five intentional skips, 59 Playwright tests with 13 intentional skips, the production build, and the bundle guard. `npm run audit` and `npm run audit:runtime` each report zero vulnerabilities.

## Exact embedded source art extended sparse semantic identity without crossing into placement

**Timestamp:** 2026-08-26 01:39–03:06 PDT

**Action:** Added a standalone compiler, strict PDF image-closure protocol, real two-lifecycle CLI, and independent opaque verifier above the exact 70-relation legacy-recut semantic handle. It authenticates the full 881-row / 359-step source roster, exact 70,238,655-byte PDF, and official first-50 quantity cut while measuring only the 187 part-art callouts / 320 pieces through printed step 50. Exact classes require the same decoded RGB24 digest, dimensions, kind, milli-quantized linear transform, and ordered strict-allowlisted image closure; terminal resource names are alpha-renamed only after exact dependency/paint equality, page translation is normalized with its clip coordinates, and nearest-milli rounding is translation-invariant.

**Result:** Sixteen source-art relations / 40 pieces extend the sparse semantic roster from 70 / 107 to 86 / 147. One hundred one relations / 173 pieces remain residual. Every measured member separately retains a nonempty support mask, internal isolated/full support-RGBA equality, and zero on-support interference; page translation can change raster phase, so cross-member mask/RGBA equality is not claimed. The final ignored artifact is 211,319 bytes at `sha256:4be7bd77d386a7a656019affe9c995e77135080a7aa90df19e43a6f2167ab721`.

**Authority boundary:** The artifact exact-compares a closed identity-only authority object and publishes no source execution, prepared run, physical Brick assignment, frame, transform, catalog admission, coverage trust or publication, action, document mutation, replay, placement, accepted document, or completion authority. No production coverage or action-ledger consumer is integrated, and steps 51–359 retain indexing only.

**Critic repairs:** Independent review first rejected broad decoded-image grouping because it omitted the exact clip/program and per-member interference proof. The repair moved to a full ordered strict closure and deliberately reduced automatic transfer. A second review proved that resource alpha-renaming, milli quantization, and cross-member raster-phase differences were real but underspecified; schema `/2` now names and gates each normalization instead of claiming literal operand or raster equality. The final review found an origin-dependent sub-milli dead zone that made a relative `0.0006` path coordinate hash differently after pure page translation. Removing that shortcut and adding positive and negative translated threshold probes restored one nearest-milli rule. The final independent rerun reports no supported P0–P2 finding.

**Validation:** Focused Vitest passes 12/12 across the compiler, PDF-program, and CLI suites. Prettier, targeted ESLint, workspace TypeScript, and the real two-lifecycle CLI pass. The real workflow reproduces 19,750,648 component pixels, 1,846,598 decoded RGB24 pixels / 5,539,794 bytes, 76 full-page renders, 290 isolated renders, 290 control renders, two official-model indexes over 3,806,338 input bytes, and four full XML decodes / 7,612,676 bytes under fixed limits. Current executable-source attestation independently reports 3,660 files at `sha256:fd74cde3eb9e5f809ae24c36253ba99720204f0b6b4221874957c949fae1779e`.

## Exact aggregate order reconciled printed steps 31 and 32 without granting physical assignment authority

**Timestamp:** 2026-08-26 03:07–04:16 PDT

**Action:** Added a bounded compiler and independent opaque verifier over the exact current callout manifest, the official Builder XML, three reviewed source/inventory identities, and exact phases 49 through 54. The derivation tests the official 180 + 4 + 10 quantity cut instead of assuming that each printed boundary is an independent slice through Builder order.

**Result:** The reviewed rows falsify the naïve cuts with shortfalls of 1, 1, and 2 pieces. Repartitioning the six exact phases yields four pieces for printed step 31 and ten for step 32 while conserving the complete combined 14-piece multiset. The canonical `/2` artifact is 6,680 bytes at `sha256:66451b2324142d9f563b731739532c38be06e34018eb8397bacfe4a0e6245810`.

**Authority boundary:** Publication contains reviewed semantic rows, aggregate printed-step multisets, contradictions, input commitments, and a mandatory disclosure. It omits direct Brick UUID-to-callout rows but makes no confidentiality claim: the open pinned XML and repository derivation uniquely reconstruct the five reviewed associations. That diagnostic exposure grants no assignment, source-execution, prepared-run, frame, transform, catalog, coverage, production-ledger, placement, replay, document-mutation, acceptance, or completion authority.

**Critic repairs:** Independent review rejected a first shape that serialized direct physical UUID membership, then rejected an enumerable low-entropy phase commitment. A fresh final critic proved that the exact step multisets plus open official XML still uniquely reconstruct the five reviewed associations, so secrecy was impossible in this deterministic open-source unit. Schema `/2` takes the different route: it removes the unused private-proof cache, requires an explicit reconstructibility disclosure, and gates authority absence instead of pretending omission is confidentiality. A second critic then found the unit had inherited a legacy claim that truth-linked crop bytes were authenticated even though it consumes only manifest/XML bytes and reviewed digest metadata; the source index is now unit-owned and says `not-consumed-reviewed-digests-only`. The final independent rerun reports no supported P0–P2 finding.

**Validation:** Focused Vitest passes 9/9. Prettier, targeted ESLint, and workspace TypeScript pass. The verifier reproduces the exact 6,680 bytes and digest from the pinned manifest and official XML, rejects phase/order/member drift, quantity-only substitutions, incomplete review maps, inherited crop-authentication claims, added authority or false-disclosure fields, byte-role mutation after the first asynchronous boundary, and forged verifier handles.

## Exact residual review and global conservation closed semantic identity through printed step 50

**Timestamp:** 2026-08-26 04:17–05:41 PDT

**Action:** Added a bounded prefix-50 semantic compiler and independent opaque verifier over the exact source-art semantic handle, callout and inventory manifests, element-resolution and official-model bytes, and all 101 residual callout/inventory pairs. The 86-relation / 147-piece safe roster is carried without reopening its crop files. The residual 41 / 66, 57 / 102, and 3 / 5 relation/piece review partitions each reopen one manifest-bound callout crop and one manifest-bound inventory crop per relation, then join to a separately pinned original-resolution outcome row over the exact identity, element, and two crop digests; only explicit `same` can enter the closure.

**Result:** All 101 residual relations / 173 pieces have explicit `same` outcomes in a 30,429-byte retained review packet at `sha256:286696d9254e89d027eb4a244d176cb8aff064991655347865ce8d3d5f1012b7`. Together with the safe roster they close semantic element identity for all 187 first-50 relations / 320 pieces. An independent exact parse of the official first-320 sequence aggregates to the same 86 elements / 320 pieces with zero element-quantity differences; it remains aggregate-only and does not recreate the invalid independent step-31/32 slicing. The canonical 92,426-byte `/1` artifact is `sha256:1902af68a13cb629d9dbac1707c8c5c6998ec355cfcb6ef4dad2fc938f76155b`.

**Visual verification:** All 101 callout/inventory pairs were inspected side by side. The 57-row broad roster was reviewed across fifteen high-resolution four-row sheets, the 41-row static roster across eleven sheets, and the remaining three pairs individually; every comparison was `same`. The exact retained outcome file contains 101 unique identities, 101 unique identity/element/callout-digest/inventory-digest tuples, and 101 explicit `same` values.

**Authority boundary:** The complete 881-row / 359-step source/index contract remains intact, scope is exactly printed steps 1–50, and `suffixStepsReconstructed` is false. Published part-number and color values are resolution metadata only. No authenticated source execution, prepared run, physical Brick assignment, frame, transform, catalog admission, coverage trust or publication, action, production ledger, placement, replay, document mutation, accepted document, or completion authority follows.

**Critic repairs:** The first independent review rejected the original closure because 98 residual rows had candidate mappings without separately bound visual outcomes and because only the semantic aggregate total, not the official first-320 aggregate, was gated. The repair added the exact 101-row outcome input, admitted only explicit `same`, published each outcome in manual evidence, parsed the official sequence through the repository parser, and exact-compared both 86-element aggregates. The fresh independent rerun reports no supported P0–P2 finding and confirms that the aggregate check does not manufacture per-step physical assignments.

**Validation:** Focused Vitest passes 12/12 in the live-evidence configuration. Prettier, targeted ESLint, and workspace TypeScript pass. The verifier independently reproduces the exact artifact bytes and digest, rejects fake opaque handles, missing review roles, malformed or non-`same` outcomes, duplicate/missing/extra residual relations, contamination, crop-byte drift, review/model/resolution drift, global quantity drift, artifact mutation, forbidden authority, and a clean-checkout skipped live-evidence pass as insufficient.

## Proper source frames expanded without widening placement or changing `/26` truth

**Timestamp:** 2026-08-26 05:42–07:16 PDT

**Action:** Added one deterministic generator and byte-for-byte drift gate for a shared TypeScript/Python registry containing every determinant-`+1` signed-permutation rotation. Mesh resolution and admission, measured and render-only factories, LDraw source-frame correction, visual admission, and Python measurement arithmetic now consume the 24-member registry; document transforms, rendering coordinates, placement composition, catalog manifests, and every current part's legal orientations continue to consume only the four legacy upright yaws.

**Result:** The ordered roster is `sha256:57446894cd2b917eb5463672655baa012d7c03539ba4147cd89e9c774a309201`; it contains 24 unique IDs, 24 unique matrices, all 576 closed products, every inverse, and no reflection. The first four rows preserve the exact legacy IDs and matrices. Current catalog `/26`, catalog `sha256:f86310b89f3224cff7a8d571de5a26fd36440ab46235abf1cf530e2f65f41b37`, transform `sha256:a8694ddcdc39da5afd946a6012ac2588233bebe2eed457e8501cf572661b2956`, and overall truth `sha256:3226590b11882fea03d8a6370d4ca3c6c8201feaddb56882a243a69acba627e9` remain byte-identical. The local versioning rule therefore defers `/27` and the `/26` historical migration snapshot until the first production part actually consumes a non-upright frame.

**Authority boundary:** This is frame vocabulary, not an admission. No current definition uses a non-upright frame, no `legalOrientationIds` array widens, and no identity, geometry, connector, collision, allowance, physical frame, source execution, assignment, coverage, action, placement, document mutation, replay, acceptance, or completion authority is created.

**Critic repairs:** Root review reproduced a `KeyError` in the scorer's hidden four-row inverse map; the repair derives the transpose inverse through the complete generated registry and gates a genuinely non-self-inverse position-and-direction round trip. Independent harsh review then found that the importable generated Python filename missed the repository's generic `*.generated.* -text` rule, so a clean Windows checkout would translate LF to CRLF and fail the exact-byte gate. An exact `.gitattributes` entry now preserves those bytes. The fresh critic accepted the repaired diff with no residual P0–P2 finding.

**Validation:** Generator drift, 10 focused Vitest files / 76 tests, 62 measured-part Python tests, workspace TypeScript, targeted ESLint, targeted Prettier, and `git diff --check` pass. Current executable-source attestation passes 8/8 over 3,663 files at `sha256:5aa813bb77e9427b24c3062a3bcadecc00710427248a7004193a710fe571bedd`. The full `npm run verify` gate passes, including 415 Vitest files / 3,429 tests, 59 Playwright tests with 13 intentional skips, the production build, and the bundle guard; `npm run audit` and `npm run audit:runtime` each report zero vulnerabilities.

## Four measured admissions and shared connector capacity advanced catalog truth to `/27`

**Timestamp:** 2026-08-26 07:17–08:52 PDT

**Action:** Admitted exact official `99563`, `73230`, `35464`, and `49307` surfaces with bounded collision and connector declarations, advanced catalog and migration truth to `/27`, and added explicit shared-capacity cells for the three `99563` underside clutch endpoints. The center endpoint claims both half-cells while each outer endpoint claims one, so both outers may be occupied together but center plus either outer refuses order-independently with `PORT_CAPACITY_EXCEEDED` under the same part instance. Capacity semantics propagate through editor, search, prepared, and automatic placement paths rather than remaining a validator-only annotation, and a catalog-wide grouped-connector gate covers the declaration shape.

**Result:** Catalog `/27` has 102 definitions: 61 parametric, 41 mesh-backed, 16 render-only, 25 fully measured, and 77 with project-authored physical recipes. Current truth is overall `sha256:614c61787b6c45d645e3e84c71dd931a15c258535a1959ee4b3aa1906303b70f`; catalog `sha256:ffb0eb6e68edcb91298b04a3c899a11417b70b07aac062c42f4c1051c20f50ee`, connector `sha256:5153c1c3d58db63962698768885c0630b1c2c926a220e5895e7d55442ebbc7f1`, collision `sha256:1e727bf61b482bcaf8587f44175e46238926126de241ae0248a5e23b942118bd`, transform `sha256:ec8ce034cb7f39169783692259ec25bb028b95bce6d456917f88bd9bebebb03d`, and validator `/4` `sha256:ac785c8f5ac9f2d642bf53c8ef51764b7954c981355b1d7d508a2228a5f1bf55`. The 28-source-truth migration registry contains 2,319 endpoints at `sha256:f72d9f2d06d5e593d1e6b00bff4c31d02f07a42f230b18d15963a5b437d36ad0`, 22 registry deltas, and three unchanged reachable pairs at `sha256:7431a242907aa9829ead6a279d0b530fe5f5d00ee31e6ddc1576fe66a8a07add`. Required-leaf intersection reaches 24 of 121 at `sha256:1065c3a3d04787974aa43c8ede60c1937930884bdf297488d3731dc8c99ea591`; the retained coverage `/3` artifact predates `/27` and is not relabeled current.

**Visual and identity evidence:** Four exact admission packets at `sha256:061237ddb1ab9cb5761d51010320241ab2bd2f3521d0e51fd8231588a3246167`, `sha256:7aec985e3c8ca578146e7142d443040d62cba1d3007beaaedb5deb0b7fa84066`, `sha256:76e692a39570aecd4bfde387b73221a6105133fcfa6d3a68cc9080ad12d84c66`, and `sha256:3236d09e03c4f9268badab5f66a5e3b088bdc0376abd4ffc687f24bfea9c029a` feed capture batch `sha256:b4794cf1d3fd69e2ff8047f21a10720f4ab39da943875c0290610da2538fd863`, native-pair manifest `sha256:00cac7e4faf3add0f6efddeb707e184fc50fe5775e5a3f9b1a88d1b00e004c17`, and review batch `sha256:88fcdefe513746664816be0d17003df772b982f6c7a781dd2e2c20844ec4c10e`. All 32 native pairs were inspected and reviewed `same`; 29 are decoded-RGBA exact, the tile isometric pair differs in two channel values at delta 1, the Technic and slope isometric pairs differ in one channel value each, and all foreground IoUs are 1. The `3245` diagnostic's calibrated geometry selects c, but served-body and ETag MD5 values conflict with the manifest, so identity, catalog, frame, and placement authority remain false. The `2453` suffix is absent and remains `not-observable`, with every authority false.

**Authority boundary:** This unit changes catalog, geometry, connector-capacity, collision, migration, and exterior visual truth only. It grants no printed identity, physical Brick assignment, source execution, prepared run, frame, placement, replay, action-ledger, document mutation, acceptance, or step-frontier authority. The complete 359-step source/index contract remains intact; reconstruction scope is exactly printed steps 1–50, and steps 51–359 are intentionally not reconstructed.

**Critic repairs:** Pre-final independent review found that validator `/4` initially needed an explicit historical `/3` source-compatibility path and a repin of current connection semantics; the migration and compatibility gates were repaired. Review also required capacity propagation through editor, search, prepared, and automatic placement paths, a catalog-wide grouped-connector gate, and exact capacity diagnostics and scope checks. Fresh post-integration review then found four supported P2 classes: max-length validation messages could truncate before the prior owner and remediation; prepared and automatic preflights retained only anonymous occupied sets; the reusable admission gate allowed cloned physical clutch frames under disjoint group IDs; and compiler manifests plus the measured-plan provenance header still described pre-capacity semantics. Bounded diagnostics now put remediation and a digest-disambiguated prior owner first while retaining full IDs structurally, both preflights map every claim to its exact owner path, mesh admission rejects duplicate `kind + position + normal` frames, and the provenance header names reviewed capacity membership. Kernel compiler `/3` is `sha256:c1f284ebfce1e262e58be877eba73acf364d161d1df2c6b13374470e9f6f17ef`; automatic compiler `/3` is `sha256:5d657ffa305adec6dfdac37a91ce53f7c830a2a0a406eb7dd57b7c5c1a10383d`. Their metadata-only rebind preserves the exact proper-C4 raw roster while moving the current executable quotient to `sha256:67c9642155e95db895d03a9fd8b9df9521d73fd3cf8ef91a4aa9f9b0e5ef0190`; frozen non-authoritative `/26` projections now execute the retained enumerator `sha256:18647bdea99ba4756aee66466ef6193efc6eda36d1417bd7318a7f5fe4bf0469` and quotient `sha256:e9f23510849153d022bb0aafd6dbf5281bbf6c519c7aff8d14bdefd0fd1145b9` controls without running placements under historical truth. No supported P0–P2 issue remains after these fixes.

**Required-gate repairs:** The Python hard ceiling exposed four source modules at 507–567 lines, so cohesive render-only plans, generated headers, report rows, and catalog-contract tests moved into separately gated modules; a fresh critic then restored the aggregate discovery of the two moved tests and the public `RenderOnlyPartPlan` import. Full Vitest exposed stale `/26` assumptions after the truthful `/27` migration: frozen diagnostic and Gate-3 evidence now requires exact `/27` runtime truth and removes only the four exact `/27` additions before comparing the historical `/26` projection, all fifteen current Builder definition pins and the ignored calibration were rebound to `/27` without moving geometry, connector, or collision pins, and the current 400-offer digest plus slope-family expectation were updated. Independent review found no remaining P0–P2 issue in either repair.

**Validation:** The ignored measured-part generator report `output/.codex-wip/prefix50-admission/measured-part-emission-27-check.json` is `sha256:503380ca45e69f2aa9a3e588227f6d9375f542fa846dd6bfac32a9259eaf02aa` with zero hard failures. The geometry roster is `sha256:35360f1c8ea40aecaf521a58502ecf6ad20aae47d00ea92b4b1877de3d6ca6da` over 224 files—222 CC BY 4.0 and two dual-licensed selections—by 30 authors, with 17 exact LDCad connector routes. Current executable-source attestation passes 8/8 over 3,667 files at `sha256:a882f6c9ed6bd8d7f741e40a747191a4c480af1c8f07bfa5972b7ee0794e36a8`. The authoritative `npm run verify` passes 418 Vitest files / 3,475 tests with five intentional skips, 59 Playwright tests with 13 intentional skips, the production build, and the bundle guard; `npm run audit` and `npm run audit:runtime` each report zero vulnerabilities.

## Complete first-50 semantic identity now publishes current catalog coverage without action authority

**Timestamp:** 2026-08-26 08:53–11:31 PDT

**Action:** Added one module-owned replay of the exact legacy-recut, source-art, and complete prefix-50 semantic chain plus a separate coverage `/4` compiler and CLI. The compiler accepts only the opaque verified semantic handle and exact full-booklet manifest and element-resolution bytes, projects a requested prefix from 1 through 50, and retains the entire 859-row physical source index. The publisher preserves differing prior coverage bytes by exclusive digest-named copy before atomic replacement; the historical 530,702-byte `/3` report remains ignored counterevidence at `sha256:bd7b03a53fa9ccecb52784271a9e8a96a172a3ce0ea225834b93eabd3630667f`.

**Result:** The retained 92,426-byte semantic closure reproduces at `sha256:1902af68a13cb629d9dbac1707c8c5c6998ec355cfcb6ef4dad2fc938f76155b`. Coverage `/4` is 592,243 bytes at `sha256:c7dd0ef1e5783384a44e123546efce82bb0da2ebc69364dc7780b2bddfa53e0b`; it binds catalog `/27` at `sha256:ffb0eb6e68edcb91298b04a3c899a11417b70b07aac062c42f4c1051c20f50ee`, publishes all 187 first-50 semantic identities / 320 pieces, preserves their semantic method and published-versus-official design evidence, and retains 672 later source identities with semantic evidence, confidence, element, and catalog resolution null. Current catalog coverage is 44 of 49 action-bearing steps, covered prefix 29, and 308 of 320 pieces placeable. Only `3245c` at steps 30–32 (three callouts / seven pieces) and `2453b` at steps 49–50 (two / five) remain catalog-absent.

**Authority boundary:** Coverage `/4` is a measurement, not an action source. The semantic closure itself remains identity-only with `coverageTrust:false` and `coveragePublication:false`; only the separate compiler publishes the coverage report. The step-26 row explicitly retains published `28802` versus official `10201` rather than hiding that action blocker behind its exact catalog resolution. Production identification replay continues to accept only coverage `/2` or `/3`, and `prefix50-semantic-closure` remains outside the trusted placement-confidence set. No authenticated source execution, prepared run, physical assignment, frame, action, ledger, placement, replay, document mutation, acceptance, or completion authority follows, and steps 51–359 remain unreconstructed.

**Critic repairs:** Independent harsh review rejected the first report because its live resolver depended on mutable catalog truth without binding a version or digest, it dropped `evidenceMethod` and `officialDesignId` counterevidence including the step-26 contradiction, and every substantive projection test skipped on a clean checkout. The repair binds catalog `/27` and its exact snapshot digest, retains the semantic relation on every prefix row, adds an always-on synthetic dense-prefix/suffix/catalog/mismatch projection gate, and pins the genuine output bytes and digest. Final documentation review then corrected two authority-attribution errors: the separate compiler publishes coverage while the closure's coverage-publication authority remains false, and suffix source identities remain present while only semantic and catalog authority fields are null. The final independent rerun reports no supported P0–P2 finding.

**Validation:** Focused semantic, closure-refusal, trusted-confidence, and source-boundary Vitest passes 83/83; the always-on semantic-coverage suite passes 10/10 by itself. Targeted Prettier and ESLint, workspace TypeScript, exact CLI replay, and `git diff --check` pass. Current executable-source attestation passes 8/8 over the exact reported 3,667 files at `sha256:5e11c484f692b8feb397ed86070a5ba5fd1c96a389070d5a2031164fdd139089`.
