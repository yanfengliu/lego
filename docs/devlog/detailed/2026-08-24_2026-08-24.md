# Devlog — 2026-08-24

## Exact crop equivalence bridged frozen truth without importing legacy answers

**Timestamp:** 2026-08-25 22:42–23:30 PDT

**Action:** Added a standalone compiler and independent verifier for the exact frozen full-booklet callout `/5`, current `/6`, and tracked first-50 pair truth `/3`. The two manifest bytes are module-pinned and must reproduce the same ordered identity, page, step, quantity, vector-coordinate, box, evidence-kind and region roster across all 881 rows. That keeps `expectedPrintedSteps=359`, 859 part-art plus 22 semantic rows, and the first-50 physical denominator of 187 callouts / 320 pieces while explicitly leaving steps 51–359 unreconstructed. Crop-byte work is narrower and named: each compile pass opens only 85 truth-linked relations across both generations, for 170 PNGs and 14,611,220 decoded pixels against a fixed 16,777,216-pixel ceiling. The CLI's compile, independent verify, and published-byte rebound passes report 510 images and 43,833,660 pixels under an explicit 50,331,648-pixel workflow cap.

**Result:** The 102,513-byte ignored artifact is `sha256:1f7a0fdcb2d665bd3fd30b8e5666307a18c884607640e6b229ef2077f3ebbc8c`, and its exact 85-row relation commitment is `sha256:bacedddff0083438324e106c45b55e8eea61b866572387d9f147eab011aa927a`. Twenty-five positive relations / 26 pieces are exact PNG bytes and another 48 / 87 prove the current decoded RGBA is an exact same-width prefix whose removed bottom rows contain only the legacy crop's exact opaque corner background, retaining 73 / 113 in total. Four plus four negative relations retain the same two equivalence classes for 8 / 10; the second identity under negative row n25 refuses because the crop shape and 5,038 retained pixels change, and positive n38 refuses because 144 retained pixels change at maximum channel delta 81. Two relations / four pieces remain unjudgeable. Verdict, crop-comparison disposition, and authority are separate fields, so a refused negative is retained counterevidence rather than disappearing into an accepted denominator.

**Authority boundary:** Production compilation accepts only module-owned pins, production verification returns an opaque private `WeakMap` handle, inspected JSON is deeply frozen, and verified bytes leave only as copies. Synthetic pins exist only behind an explicitly unbranded test seam. Exact input-role checks reject the quarantined legacy answer `/4` and every extra evidence role. The artifact states that it authenticates only truth-linked first-50 crop files, not every file named by the full manifests, and grants no source execution, prepared run, physical frame, semantic identity, catalog admission, coverage trust or publication, document mutation, placement, accepted document or completion authority. No coverage or action-ledger consumer is integrated in this unit.

**Critic repairs:** The first harsh review found caller-supplied expectation pins could receive the production private brand, the returned brand exposed mutable nested JSON and bytes, extra answer roles were silently ignored, locale-dependent sorting entered deterministic output, the full 359-step roster claim was not executable, decoded-image work had no aggregate bound, negative/refusal denominators were conflated, the CLI executed on import under broad default byte ceilings, and the first module exceeded the preferred 500-line limit. Production pins and branding are now inseparable, the closure is opaque and copy-only, input roles are exact, code-point ordering is explicit, both complete ordered rosters must match a pinned digest, selected pixels preflight before decode, orthogonal accounting exposes retained and refused positive/negative populations, the CLI is guarded and capped at 512 KiB, and source validation was split into a focused module; both behavior modules remain below 500 lines. A second pass found that one-pass pixel accounting understated the three-pass CLI, unlinked crop absence was not exercised, authority assertions were partial, and equal-count refusal swaps could preserve aggregate pins. The CLI now reports and caps total workflow work, poison unlinked-prefix and step-51 crop files prove the compiler does not read them, tests exact-compare every authority field, and production pins both the entire relation projection and the final artifact plus exact n25/n38 refusal diagnostics.

**Validation:** The focused adversarial Vitest suite passes 6/6 over exact bytes, valid recuts, retained-pixel and suffix changes, width changes, unjudgeable retention, artifact mutation, answer-role injection, pin and schema drift, roster change and reorder, cross-run paths, missing and duplicate identities, aggregate overflow, corrupt PNGs, poison unlinked crops, opaque branding, immutable inspection and copy-only bytes. Targeted ESLint and formatting pass, and the real compiler/independent reader reproduce the exact artifact and accounting above. Current source attestation passes 8/8 over 3,660 files at `sha256:7a3d7d53b5e215815bbf91dc74d64eb94566b3d0c7bc08d8f274ce207da7e16c`; `npm run verify` exits zero through 405 Vitest files with 3,374 passing tests and five intentional skips, 59 headless Playwright passes with thirteen intentional opt-in skips, and the production bundle guard; full-lockfile and runtime-only audits both report zero vulnerabilities.

## Exact source-art evidence entered the bounded prefix without granting placement authority

**Timestamp:** 2026-08-25 13:46–22:42 PDT

**Action:** Added an exact PDF/manifest compiler and independent reader for the shared embedded-art class at printed steps 2, 4, and 16. The retained artifact binds the exact PDF, current v6 callout manifest, decoded embedded-art class, operation closure, native full/isolated RGBA and suffix counterexamples. A separate Chromium gate performs one booklet fetch and independently rerenders the three diagnostic windows through modern and legacy PDF.js `5.4.149`. Coverage `/3` preserves all 859 physical source/index rows but nulls identity, confidence, resolution and reason fields on the 672 rows after the requested step-50 prefix. Action-ledger `/4` and run-contract `/5` require the verified rebound role; frozen coverage `/2`, action-ledger `/3`, and run-contract `/4` remain immutable replay-only predecessors.

**Result:** The compiler emits 9,786 bytes at `sha256:a58a55e65c19e2771defe02fc9d37e24c00246bbed9dd375d8d0a2f16382897d` from PDF `sha256:baef0a373164b58d7c982984b52d4e50b10cc59ed28007acb456faa72359bd27` and manifest `sha256:c8d20cfe87ef9d21488725b393b94e61870fcc82b26bb497ea734fc7b97a67bf`. Coverage upgrades only the extant step-2 `4160025`/`30503` claim through the direct step-4 pair-judged anchor and records the rebound artifact digest as that row's input. The 530,702-byte prefix-50 report is `sha256:bd7b03a53fa9ccecb52784271a9e8a96a172a3ce0ea225834b93eabd3630667f`: 162 resolved plus 25 unidentified prefix rows, 41 of 49 action-bearing steps, 255 of 277 pieces, and covered prefix 27. Genuine ledger publication refuses without writing on 174 incomplete actions and 11 missing official-frame calibrations, one fewer incomplete action than before the correct rebound admission.

**Visual and authority boundary:** All six freshly regenerated Chromium full/isolated images were inspected at original resolution. Each shows the complete black wedge, outlines, and `1x` label without clipping or outside paint, and full/isolated pairs are visually identical. This evidence grants no source-execution, prepared-run, physical-frame, placement, accepted-document, or completion authority. The complete 359-step source/index contract remains intact, but reconstruction is capped at printed step 50 and steps 51–359 stay outside scope.

**Critic repairs:** Independent harsh review found that the first coverage upgrade retained the manifest digest instead of the rebound artifact digest, publication accepted caller-supplied verifier callbacks, a later test observer could mutate final bytes after verification but before pointer publication, frozen run-contract `/4` routed raw PDF bytes into coverage `/2`, and ordinary current and legacy artifact verification could accept replay/CAS bytes changed during asynchronous source-art verification. Coverage now exact-binds the artifact; publication imports its fixed module-owned verifier after initialization, ignores extra caller arguments, exposes only a pre-verification observer, and admits the artifact manifest before replay work. Current, publication, and frozen legacy consumers all re-read and re-fingerprint their manifest/replay/CAS boundary after asynchronous verification, while `/4` retains PDF only as raw replay evidence and supplies neither PDF nor rebound evidence to coverage `/2`. Hostile tests prove fake callbacks remain uncalled, final-directory mutation refuses publication, same-size CAS and replay-manifest races are rejected, and synchronous input-admission failures remain synchronous without unhandled promise rejections. The final re-review reports no remaining supported P0–P2 finding.

**Validation:** Focused source-art, coverage, identity-trust, action-ledger, replay/publication, TypeScript, ESLint and formatting checks pass; the final async-verification slice passes 96 tests in five files. The genuine two-route browser capture passes and the fresh prefix-50 publisher refuses with exactly the expected 185 typed failures without replacing the retained ledger. Full `npm run verify` passes 404 Vitest files with two skipped, 3,368 tests with five skipped, 59 headless Playwright tests with 13 intentional opt-in skips, and the production build; both lockfile and runtime audits report zero vulnerabilities. The final executable-source attestation covers 3,660 files at `sha256:fb901a8bf7b93616e4d345c1c6050d729ef35a6da3d11008555bead458abdd99`.

## The 359-step source contract gained an explicit bounded reconstruction prefix

**Timestamp:** 2026-08-25 12:37–13:46 PDT

**Action:** Separated the immutable 359-step booklet source/index denominator from one explicit `requestedLastStep`. Catalog coverage must be compiled through exactly that request; action-ledger assembly, validation and emitted provenance retain both values and never emit a higher row. Runtime planning gives action execution, reports and document mutation only the requested panels, while the configured two-row suffix remains passive raster observation. Run-contract `/4` now digest-binds a mandatory canonical `panel-source` envelope to the exact PDF bytes: complete source/index data plus exact page-shape rows through the bounded suffix. Local replay reconstructs all 359 panel bounds and manifest callout boxes before prefix projection, independently derives faces through the observation bound, exact-compares every prepared execution/passive page, face, bounds and boxes, and feeds the reconstructed panel-evidence map to current action-ledger validation.

**Result:** A step-50 request retains source labels 1..359, executes panels 1..50, and exposes panels 51..52 only as later visual witnesses. Panel 51 may help settle step 50, and step 50 may be the intervening requested action for step 49, but panel 51's action and pieces cannot enter farther expansion, create a report, invoke placement or mutate a document. Coherent prepared-panel/run-contract geometry or face forgeries now fail against the unchanged retained panel source rather than self-confirming. Publisher entry points require `LEGO_REAL_BUILD_LAST_STEP`; zero, implicit, out-of-range and coverage-prefix-mismatch cases refuse before writing. The Python report consumer independently requires and bounds the new provenance field. The retained 359-step coverage and step-25 ledger predate this contract and remain counterevidence rather than current prefix-50 inputs.

**Authority boundary:** This unit changes scope enforcement and reproduces retained derived source commitments only. The canonical retained `InstructionSourceV1` and v6 manifest are not a replay of the PDF parser, vector extraction or raster renderer and authenticate no new crop, part identity, transition, physical frame, placement, accepted document or completion. The complete source/index suffix is preserved for bookkeeping and later passive evidence, but steps 51..359 are intentionally outside the current reconstruction request and cannot be inferred complete from a successful prefix.

**Validation:** Focused action-ledger, publication, input-chain, prepared-source, panel-source replay, panel-window, passive-lookahead and placement-consumer coverage passes, including nine direct panel-source mutation controls and a full replay-closure geometry forgery. The repaired fixture cluster passes 164 tests in 19 files, strict current-JSON admission passes 47 tests, and its direct consumers pass 101 tests. Full `npm run verify` passes 399 Vitest files with two skipped, 3,339 tests with five skipped, 59 headless Playwright tests with 12 intentional opt-in skips, and the production build; both lockfile and runtime audits report zero vulnerabilities. Canonical unique prepared-run encoding changes only the current quotient commitment from historical `sha256:660f8c7c8ea2eac42e7c006acd911099ac28e10af2b8e055adad711cd9643421` to `sha256:e9f23510849153d022bb0aafd6dbf5281bbf6c519c7aff8d14bdefd0fd1145b9`, preserving the roster, 100 representatives and all 400 mappings. A hostile passive panel uses throwing `action` and `pieces` Proxies while step-50 raster access succeeds with zero trap, farther-executor or placement calls. The final independent critic found no supported P0–P2 finding.

**Source attestation:** The exact current captured executable-source closure advances from the 3,635-file proper-C4 integration pin at `sha256:8231cce97b7d96d982b0ac0ab0c1612cfa9956fe2c158715d872bdd51b928dfd` to 3,650 files at `sha256:7fabfaed3744ecaf01549f6e0e600547ac540171999087a5d57a5f8b87db2215`. This authenticates only the calibrated executable-source condition and not retained source derivation, PDF execution, score, visual frame, placement or completion.

## The fixed-8,192 proper-C4 quotient became a semantics-preserving render reduction

**Timestamp:** 2026-08-25 10:06–12:37 PDT

**Action:** Integrated the exact current 100-orbit quotient through twenty deterministic closures of five representatives under one cumulative fixed-8,192 search ledger and one cumulative fixed-8,192 camera ledger. The reducer retains all 100 representatives, all 400 inverse mappings, 800 representative/camera rows, 3,200 inverse-expanded raw/camera rows, and one global aggregation; no closure can choose a local winner. Before inverse expansion or ranking, a separate current-population verifier reconstructs every one of the 300 omitted proper-yaw members, renders them in sixty five-member closures across all eight cameras under its own fixed-8,192 ledger and the same per-closure byte/pixel cap, and requires each packed mask to equal its exact mapped representative commitment. The 2,400-render verifier runs on every reducer invocation and is recorded separately from the 800-render algorithm rather than called a reusable or one-time proof.

**Result:** The genuine exact-three browser run retains integration `sha256:4087bd1e7ce0be01549dfceb72b26b9deaa7eeee8bf32c700aa00c71e57614bc`, closure aggregate `sha256:5d16f192f91f29cd17c87c183ffb0e5b4bc34567d3252e1ad0a7c818aecac47c`, renderer configuration `sha256:0eac516a7e7c5cae475ff4a0138d8569375456fb7a53f752b837464a2decdcbb`, and source binding `sha256:72fbffe386b9e33810dc9e6ae55711993ec3f2ea5ced6d1b2b8eed28282220ad`. Search closes at 800/8,192, camera association closes at 6,400/8,192, the separately budgeted population verifier closes at 2,400/8,192, and exact inverse expansion leaves the global result unresolved at margin zero. Accepted transition/document, source-execution, prepared-run, physical-frame, placement, and completion authority all remain absent; the reduction therefore authenticates no placement, and step 2's unreviewed `30503` identity remains the first reconstruction blocker.

**Visual verification:** The same genuine headless run regenerated and inspected the six step-2/3/4 booklet high/work images at original resolution. It also retained the exact ordered 100 reducer documents, cross-checked their structural hashes against the reducer rows, rendered all 100 at each of the eight D4 cameras into eight separate contact sheets, and rerendered those same documents at camera zero under a separately committed 20-pixels-per-unit wider-scale factory. All nine current-population sheets were inspected at original resolution and contained 100 nonempty, bounded, unclipped silhouettes; the wider control was visibly distinct from the primary 30-pixels-per-unit view. These visual-only 100 renders have separate preparation/render/disposal counts and do not enter algorithm or proof ledgers or digests.

**Critic repairs:** Independent harsh lanes first found that a 32-render calibration sample could not prove current-population equivariance, browser builds had lost the Node Proxy rejection guarantee, the first visual contact retained only current camera zero, and documentation mislabeled per-invocation proof work as one-time. The final implementation exhaustively compares all omitted current-member masks before ranking, accepts browser integration only through one bounded JSON string plus locally branded containers, rejects hostile object/array/nested Proxies without invoking traps, sweeps all eight actual representative cameras plus an alternate scale, and describes the 2,400 renders as same-run work. The fresh final critic inspected all fifteen regenerated images and the complete diff and reported no remaining supported P0–P2 semantic or visual finding.

**Source attestation:** The exact current captured executable-source closure advances from the historical 3,621-file quotient-gate predecessor at `sha256:2933b0302dc56910bdbed4bd02cc4a9acb6d06e687d8be2dee101927bc540eb9` to 3,635 files at `sha256:8231cce97b7d96d982b0ac0ab0c1612cfa9956fe2c158715d872bdd51b928dfd`. This pin authenticates only the calibrated executable-source condition; it does not authenticate the caller-supplied source execution, score, visual frame, placement, or completion.

**Validation:** Focused proper-C4 Vitest passes 46/46, full TypeScript, targeted ESLint and formatting pass, the hostile-browser boundary passes 1/1, the genuine exact-three/C4 Playwright run passes 4/4 in 3.1 minutes, and current source attestation passes 8/8. The first repository-wide browser process exited silently before its final three lines; those exact tests then passed 2/2 with one intentional skip in isolation, and a fresh authoritative `npm run verify` exited zero through 508 Python passes with 31 skips, 3,245 Vitest passes with five skips, 59 headless Playwright passes with twelve skips, and the production bundle guard. Full-lockfile and runtime-only audits both report zero vulnerabilities.

## Exact-three source bytes became a strict independently replayed packet

**Timestamp:** 2026-08-25 09:30–10:06 PDT

**Action:** Added one module-owned packet over placement steps `[1,2,3]` and registration panels `[2,3,4]`. Writer and hostile reader independently require page 11, exactly 359 indexed step labels, all four materialized page panels, ordered panel commitments, and digest-bound high RGBA, work RGBA, and packed-mask role slices; the reader recomputes each panel commitment and mask instead of trusting manifest claims. All returned roles detach from caller-owned bytes, and source execution, prepared-run, physical-frame, placement, accepted-document, and completion authority stay structurally absent.

**Result:** The genuine headless one-fetch capture reproduces manifest `sha256:6b09685905bab077c254adf1e8b32ee2eebd7eefac76d04da678ff47a77c3138`, 20,009 manifest bytes, and 8,592,000/2,150,000/738,816 high/work/mask bytes with one page render/dispose plus one PDF-document/loading-task destroy. All six regenerated step-2/3/4 high/work images were inspected at original detail against booklet page 11; callouts, arrows, highlights, rotation cue, underside geometry, labels, and footer remain correctly framed. This is current source imagery only: there are zero current authenticated placements for steps 1–3, and step 2's unreviewed `30503` identity remains the first identity blocker.

**Reasoning:** The first packet shape accepted arbitrary page numbers and any 3..359 source-count claims, so a self-consistent truncated or alternate-page packet could have retained the exact-three brand. The independent critic classified that as P2. Exact constants and hostile writer/reader cases now make the page-complete 359-label contract inseparable from the three emitted panels without relaxing the separate dense 359-row source-parity schema.

**Validation:** The exact-three unit passes 6/6, the combined exact-three/C4 slice passes 12/12, full TypeScript, targeted ESLint, and targeted formatting checks pass, and the genuine Playwright capture passes 4/4. The post-fix independent harsh critic reran the focused checks, inspected all six fresh images, and reports no remaining P0–P2 finding.

## The current step-1 roster gained an exact proper-C4 quotient gate

**Timestamp:** 2026-08-25 09:30–10:06 PDT

**Action:** Added a deterministic gate that validates the exact current `/26` step-1 roster before grouping any rows, forms only proper-yaw four-member orbits, retains one deterministic representative per orbit, and carries every raw ID, part ID, operation, support fact, connection, offered-candidate record, projected witness, provenance field, and inverse mapping needed to recover the original 400 candidates. Proxies, accessors, sparse/custom arrays, aliases, reflected or incomplete families, duplicate yaws, prepared-source drift, raw-target drift, and a population above 128 orbits fail closed.

**Result:** The exact 400-row roster at `sha256:24e68a134cf86c181ede701c2f189d1f2816af4a83510e2a841f270249d5ce72` closes into 100 four-member orbits at quotient `sha256:660f8c7c8ea2eac42e7c006acd911099ac28e10af2b8e055adad711cd9643421`, with 100 representatives and 400 inverse mappings. The quotient projects physical root-edge obligations from 3,200 to 800 and logical camera branches from 25,600 to 6,400, a 75% accounting reduction. These are not observed renders: twenty deterministic five-representative closures, a single global aggregator, camera/render equivariance, exact mask/shift/score/ranking and tie reproduction, and one cumulative fixed-8,192 ledger remain the next unit.

**Reasoning:** Generated first-part IDs make the raw 400 rows byte-distinct, so grouping by raw IDs cannot expose the proper-yaw symmetry. The gate first validates and freezes the complete raw evidence, then uses a provenance-bound witness projection as the orbit key without discarding what compilation and inverse expansion still need. No target pixel, score, local winner, reflection, or placement authority enters grouping.

**Validation:** The C4 unit passes 6/6, the combined exact-three/C4 slice passes 12/12, full TypeScript, targeted ESLint, and targeted formatting checks pass. An independent harsh critic reviewed orbit algebra, determinant, preservation and inverse-expansion fields, refusal cases, branding, and authority absence and found no supported P0–P2 issue; the critic specifically required that projected accounting not be described as completed rendering.

**Source attestation:** The six new executable e2e modules advance the exact current captured source closure from the historical 3,615-file predecessor at `sha256:5c6dd8e0d1ba38b239897842bccb2a40fceb94c203c5f7f12c70589d596f0523` to 3,621 files at `sha256:2933b0302dc56910bdbed4bd02cc4a9acb6d06e687d8be2dee101927bc540eb9`. The two new test modules do not enter that closure. This repin authenticates executable-source condition only; it does not authenticate the caller-supplied positioned text, rebind scores, integrate renders, or grant placement or completion authority.

**Repository gate:** `npm run verify` exits zero through schemas, Node/toolchain, historical migration, observations, BOM, part standard, lessons, notices, formatting, lint, typecheck, the complete Python chain, 3,225 passing Vitest cases with five intentional skips, 57 passing headless Playwright cases with twelve intentional opt-in skips, and the production bundle guard. Full-lockfile and runtime-only audits both report zero vulnerabilities.

## Steps 2–4 retain exact conditional commitments with one vector page

**Timestamp:** 2026-08-24 22:50–2026-08-25 00:44 PDT

**Action:** Extracted a label-only global page index and page-complete panel derivation so a scoped caller can select the pages containing requested steps before vector work. The public scoped result is no longer structurally substitutable for `RealBuildPanelEvidence`: it exposes distinct `commitmentDigest` values, a domain-bound detached caller-source snapshot, and top-level source-text, prepared-run, placement and completion authority absence. Strict descriptor snapshots reject Proxies, accessors, sparse/custom arrays and caller methods without invoking them; intrinsic byte snapshots ignore shadowed typed-array properties; selected text and bytes detach before the first await; positioned labels, actual PDF viewports, callout boxes and final panel cells remain finite, ordered and in-page; and one pdfjs loading task owns cleanup exactly once while primary and cleanup failures both survive.

**Result:** The exact booklet still pays the necessary cold 224-page text ingest and indexes all 359 labels, but scoped steps 2–4 reduce callout vector/operator-list pages from 171 to page 11 alone, materialize the four jointly cut page-11 panels, and emit three commitments. Across current focused runs the legacy full derivation observed 4.10–4.50 seconds while bracketed scoped derivations observed 84.2–101.6 milliseconds; those timings are deliberately non-authoritative, while the 171 → 1 page trace is deterministic. For the genuine freshly ingested source, steps 2–4 retain commitments `sha256:5e5bee4479b9ca1a5037c0c255a793c677766f46b5d2686b4d698c0668ddc717`, `sha256:685bc60dff569cf15354b53593da3bd4042a3eb56cea43d2917f3ef5b4b4fa71`, and `sha256:808b0ac770e15a04a047ce090a0b08ad0e42e01bd23c7c2133030af45ca0e49b`.

**Reasoning:** Filtering three rows after the legacy full derivation saved no work. Page-local vector extraction is the smallest cold-path acceleration because panel cuts remain a joint fact of every label and callout on the selected page, while step-number glyph inference remains global. A content-addressed full-manifest cache under `var/state/` could eliminate the 224-page text pass on warm runs, but persistence and correct invalidation across the PDF digest/length, pdfjs version, panel schema and transitive derivation source are a separate high-risk unit and were not smuggled into this change. Caller `contentHash` equality with copied PDF bytes still does not prove that positioned text was parsed from those bytes, so this unit says parser replay `not-performed` and does not call itself current-source authority.

**Source attestation:** The two new runtime source modules advance the exact current captured source closure from the 3,613-file predecessor at `sha256:65ef77cf44352941faddfeaf8b9fd3ff151f3f5e8c9374082cd44de965f4ec2a` to 3,615 files at `sha256:5c6dd8e0d1ba38b239897842bccb2a40fceb94c203c5f7f12c70589d596f0523`. This repin authenticates the executable-source condition for existing calibrated exceptions; it does not authenticate the caller-supplied positioned text, rebind retained scores, or grant source-text, prepared-run, placement or completion authority to the scoped projection.

**Validation:** The focused instruction, hostile-boundary and unchanged dense source-parity slice passes 74/74, the genuine-booklet control reproduces the exact three commitments and deterministic 171 → 1 trace, source attestation passes 8/8 at the 3,615-file pin, targeted TypeScript and ESLint checks pass, and production source files remain below 500 lines. The authoritative `npm run verify` gate exits zero with 508 Python cases passing and 31 intentional skips, 3,213 Vitest cases passing and five skips, 54 headless Playwright cases passing and eleven intentional opt-in skips, and the production bundle guard; both full-lockfile and runtime-only audits report zero vulnerabilities.

**Code reviewer comments:** Independent branches found that the first attempts copied caller bytes too late, invoked caller array behavior, returned mutable selected pages across an await, double-destroyed the same pdfjs loading owner, masked parser failures with page cleanup, left finite-but-overflowing geometry open, and let the scoped result inherit legacy evidence field shapes. The final gates exercise each trap against a control, preserve both failure objects, verify pdfjs ownership against the installed 5.4.149 implementation, reject three rows at the unchanged 359-row boundary, and keep caller-source commitment separate from authenticated source parity. The final harsh re-review reports no P0, P1 or P2 finding.

**Notes:** No browser pixels, candidate renders, prepared input, action ledger, user document or 8,192 accounting boundary changed in this nonvisual data-path unit. The next honest build unit remains a separate exact-three, authority-absent diagnostic packet for placements 1–3 and lookahead panels 2–4; this projection is only its bounded input seam.

## Step-1 camera hypotheses reuse one bounded child scene

**Timestamp:** 2026-08-24

**Action:** Extracted a prepared silhouette renderer that derives and paints one instruction scene, enables silhouette mode once, renders multiple camera views through that scene, copies the renderer's reusable readback before mask extraction, and disposes deterministically. The private step-1/panel-2 compiled-camera diagnostic accepts only the repository-created factory, exact-binds its immutable raster dimensions, frame dimensions and registration-panel number to the detached source before compiler or renderer work, snapshots its complete rendering configuration at construction, and disposes each child immediately after its eighth attempted hypothesis while retaining an outer abnormal-exit cleanup pass. The one-shot helper remains as a compatibility wrapper for unrelated subjects.

**Measured browser control:** A real headless Playwright control rendered one asymmetric three-part catalog document at a non-square 320 × 192 raster. The prepared route and an independent inline direct path produced the same ordered eight SHA-256 mask digests; all eight prepared digests were distinct and every silhouette area exceeded 100 pixels. The contact sheet was inspected at original resolution and showed eight complete, unclipped hand/turn views. Direct rendering derived and disposed eight scenes; the prepared route derived and disposed one, an exact 87.5% reduction in scene construction and disposal for one child. A two-child diagnostic regression proves `prepare-1, dispose-1, prepare-2, dispose-2` ordering and peak live prepared scenes of one rather than retaining a whole frontier of scenes.

**Admission and cleanup gates:** Non-square 1 × 4 factory metadata is refused against a detached 2 × 2 source even though both have four pixels, a panel-3 factory is refused against panel 2, and neither path reaches scene derivation or rendering. Mutating the constructor's rendering functions, renderer, fitted view, frame, centre, dimensions and panel after factory creation leaves the exact original eight mask digests, views and frame unchanged. Shared readback storage is copied per view, a persistent render failure still attempts the complete eight-way family before returning `camera-failed`, setup failure disposes its partial scene, and any setup or final disposal failure rejects the diagnostic rather than admitting evidence. Once cleanup fails, later child factories do not run; a branded combined setup/disposal failure propagates as fatal context-discard state rather than being downgraded to a camera miss.

**Acceleration census and boundary:** The raw current `/26` 400-offer `80015` → `30565` product is not itself 100 proper-C4 orbits: deterministic generated first-part IDs make all 400 raw rows singleton groups, and each second connection names that generated ID. A read-only census over the existing projected witness bytes measured exactly 100 groups of four; the projection is not an orbit gate and validates then omits candidate `partIds` and `restsOnBuildPlate`, rewrites generated connection targets to witness references, and injects prepared catalog/color identity. Within each measured group both witness transforms rotate, while local ports, connection kind, ordering and witness references remain exact. A future quotient may therefore sit only after projection, must retain raw member IDs and support data because projected bytes cannot reconstruct byte-identical compiler children, and must inverse-expand all 400 raw candidates while proving masks, shifts, scores and ranking parity. For member rotation `q` and lattice determinant `d`, its representative camera turn must be `t_rep = t_member - d*q (mod 360)`: as-fitted subtracts `q`, x-reflected adds `q`, and hand/determinant stay unchanged. No executable quotient or orbit gate is implemented. The previously considered step-3 observer was deliberately not retained because candidate snapshots alone could not prove that visual selection chose them or bind target design/material identities. A future observer needs a branded committed-selection closure with exact parent-child transition links and a pinned target ledger. Fitted view, frame target/radius and centre are snapshotted against caller mutation but are not inspectably bound to `sourceDescriptorDigest`; no source-authenticated physical frame, current source mask, placement, accepted document or completion authority is created by this optimization.

**Source attestation:** The three new runtime source modules advance the exact current captured source closure from the 3,610-file predecessor at `sha256:bc6ce49eea384eb796d27e7f5035415ee36ef76dc834868bb20faa09eb371499` to 3,613 files at `sha256:65ef77cf44352941faddfeaf8b9fd3ff151f3f5e8c9374082cd44de965f4ec2a`. This repin authenticates the executable-source condition for existing calibrated exceptions; it does not rebind retained scores or grant the private diagnostic source, frame, placement or completion authority.

**Verification:** Focused TypeScript, 61 Vitest cases across the camera/diagnostic boundary, and the real headless Playwright parity control passed. The authoritative `npm run verify` gate then exited cleanly with 508 Python cases passing and 31 intentionally skipped across 539 declared, 3,177 Vitest cases passing and five skipped across 379 passing and two skipped files, 54 headless Playwright cases passing and eleven intentional opt-in skips across 65 declared, and the production bundle guard. Both full-lockfile and runtime-only audits report zero vulnerabilities. The independent harsh review's configuration binding, peak-live resource, fatal-cleanup and non-circular measurement findings were converted into the gates above, and its final review reported no remaining P0-P2 issue.

## Step 1 reaches a bounded compiled-camera closure without gaining authority

**Timestamp:** 2026-08-24 20:02–20:51 PDT

**Action:** Added a private step-1/panel-2 diagnostic beside the unchanged public browser-output `/3` runner. It retains the exact eight D4 empty-root identities, exact-binds every identity to the same canonical snapshot, fans whole-step offers through the atomic compiled-branch batch, renders the camera frontier once per unique child document and D4 hypothesis, packs the captured masks into a typed compiled-observation closure, and immediately replays that closure through the independent verifier. A non-test producer now supplies the previously missing closure between the compiled lineage batch and observation verifier. Every result exposes `acceptedDocument: null`, completion and provenance authority remain absent, and the public `/3` root refusal, zero-piece terminal document and canonical user document are untouched.

**Measured work:** One unique offer retains eight lineage edges while using one supplied compiler call, one unique physical transition, one child document and eight physical renders for 64 logically reserved camera branches; the closure contains eight edge observations rather than serializing all 64 frontier accounting rows. Two unique offers retain sixteen edges while using two supplied compiler calls, two transitions, two child documents and sixteen renders for 128 logical branches; identical synthetic scores correctly leave that closure unresolved. Search budget 7 refuses all eight offered edges before compilation or rendering, and camera budget 63 refuses the complete 64-branch family after compilation but before any render. The batch still performs its mandatory independent Node replay, so `suppliedCompilerCalls` deliberately measures only calls to the injected compiler and is not a claim that compilation logic executes only once in total. The in-process diagnostic regression binds the selected edge back to its as-fitted, determinant-1, turn-0 parent root and the lookup key includes determinant, but the serialized closure treats the D4 camera recipe as opaque; independent closure replay proves score, selection, compiled edge and receipt continuity, not D4 provenance, until browser-output `/4` cross-binds that recipe.

**Enumerator control:** The current `/26` no-model catalog path for step-1 designs `80015` then `30565` deterministically offers four first placements at `[0,8,0]` in yaw order 0, 180, 270 and 90. Each admits exactly 100 distinct connected second placements, all with a connection witness and none resting on the build plate. The complete 4 × 100 product and exact `builtin.basic-parts/26` version are pinned together at `sha256:18647bdea99ba4756aee66466ef6193efc6eda36d1417bd7318a7f5fe4bf0469` without reading official transforms. A separate plumbing control carries only the first ordered two-piece offer through synthetic 2 × 2 masks and observes a reproducible selected transition with two diagnostic pieces, but that is neither a real-source visual choice nor a booklet placement claim.

**Boundary defect and gate:** The first integrated run exposed that exact candidate snapshots intentionally stabilize their arrays with private own symbols while the camera boundary accepts only dense plain-JSON containers. Passing the in-process snapshot therefore failed before rendering. The diagnostic now crosses that boundary from already-bounded canonical bytes through a fresh JSON parse; the focused regression executes the real boundary and would reproduce the former extra-property rejection. This repair does not relax either module's validator or reconstruct a branded result from report JSON.

**Review repairs:** The harsh critic found that caller-owned source evidence could change during an injected renderer callback after the frontier copied it but before the closure copied it. One strict data-property snapshot now detaches every descriptor and private mask before any compiler or renderer callback, the frontier digest is cross-checked against that snapshot, and a mutation regression proves that zeroing the caller's mask during the first render does not change either frontier or closure bytes. The critic also found closure limits were enforced after rendering; raster limits now preflight before compilation, and exact post-batch camera, observation, role-byte and pixel-visit counts preflight before the frontier, with a protected zero-render refusal gate. Camera keys now include determinant, while documentation narrows independent replay to what the serialized closure can actually prove.

**Source attestation:** The three new runtime source modules advance the exact current captured source closure from 3,607 files at `sha256:e0080ad0e0e93d8e132f12dfff6f17d1807c462b90ae8697c3244666c6b3751a` to 3,610 files at `sha256:bc6ce49eea384eb796d27e7f5035415ee36ef76dc834868bb20faa09eb371499`; the earlier value remains the historical `/26` catalog-session condition rather than current executable source.

**Current limit and acceleration decision:** The new sidecar proves robust work deduplication and authority-preserving composition, not current booklet perception. The retained prepared input is legacy `/2`, the current action ledger has null transforms and stale bindings, and the source-parity masks are calibration evidence rather than current source RGBA plus child-render closure. No current real step-1 camera tuple exists, so no real booklet candidate is selected. The next bounded measurement is real-source eight-way ambiguity followed only through step 3; if design `6106` still empties proper-frame equivalence, camera/world handedness is the next gate and deeper search stops. Random later catalog admission remains deferred.

**Acceleration path:** The complete 400-offer step-1 product fans over eight roots to 3,200 retained edges and requires 25,600 logical camera branches, so the unchanged fixed 8,192 ledger correctly refuses it before rendering even though physical work is deduplicated. The first missing unit is a distinct authority-absent current steps-1–3 source/prepared packet; it may rasterize only registration panels 2–4, a 359 → 3 or 119.7× panel-row reduction, but must benchmark wall time because full-booklet panel-boundary derivation may still be required and must not weaken the existing exact 359-panel source-parity contract. Scene reuse is now measured and gated: one derived scene serves the eight cameras with exact direct-path mask parity and immediate per-child disposal, reducing scene construction by 87.5%. The raw 400 offers are singleton groups because generated part IDs differ; a separate read-only census over existing projected witness bytes measured exactly 100 proper-C4 groups of four, but no executable quotient or orbit gate exists. Any future quotient must inverse-expand the exact raw offers and prove `t_rep = t_member - d*q (mod 360)` camera, mask, shift, score and ranking parity; more than 128 orbits, any non-exact offer/connection/provenance reconstruction, any composed-score mismatch, or any reflection disqualifies that route. A measurement-only target observer remains unimplemented because it must bind a branded committed-selection closure and pinned target ledger, stop after step 3, and leave selected candidate IDs unchanged whether enabled or disabled. Raising or resetting 8,192, selecting the first offer, using official transforms to prune, treating reflection as physical, reusing calibration masks as current source, or caching pruned scores as exhaustive remains forbidden.

**Validation:** The two new diagnostic and current-catalog-enumerator files pass 9/9 focused cases, the expanded atomic-batch, camera-frontier and compiled-observation slice passes 48/48, and the exact current source-attestation closure passes 8/8 at 3,610 files and `sha256:bc6ce49eea384eb796d27e7f5035415ee36ef76dc834868bb20faa09eb371499`. The authoritative `npm run verify` gate exits zero with 508 Python cases passing and 31 intentionally skipped, 3,168 Vitest cases passing and five skipped across 378 passing and two skipped files, 53 headless Playwright cases passing and eleven intentional opt-in skips, and the production bundle guard. Both full-lockfile and runtime-only audits report zero vulnerabilities. The independent harsh critic's source-drift, late-resource-preflight and opaque-D4 findings are closed and its final re-review reports no remaining actionable issue. No rendered product surface changed in this nonvisual diagnostic unit, so no separate screenshot claim is made; the full real-UI headless suite remains green.

## 78329 advances measured catalog truth without advancing the printed build

**Timestamp:** 2026-08-24 17:15–19:33 PDT

**Action:** Added official design `78329`, Plate 1 x 5, as one complete measured catalog identity. Official root `parts/78329.dat` at `sha256:79ec75c5092750b0f2022dab9c7561376d8b2b33fc3dea7059081ef273d4f7fc` expands through an exact nine-file, 8,761-byte CC-BY-4.0 closure at manifest `sha256:d203ae681cfa3842e210b894d46e69e555e64e638796d260c3a2cabdb474f283` into 460 triangles over 489 stored vertices. Yaw 90 plus `[0,-4,0]` is the selected catalog frame; body bounds are `[-10,-4,-50]..[10,4,50]`, visual bounds are `[-10,-8,-50]..[10,4,50]`, and the 659,766-point candidate score is `0.9968390298840539` with zero hard failures.

**Connector and collision boundary:** The checksum-pinned LDCad walk consults `p/stud.dat`, `p/stud3.dat`, and `parts/78329.dat`, and exactly authors five visible studs plus five underside clutches on the regular 20-LDU 1 × 5 lattice. The conservative collision field contains 39 positive-volume body boxes plus five source-radius stud cylinders, and all five clutch-room checks pass. Ordinary collision retains exact radius `6.0001514980873605`, while nominal radius 6 is selected only for a validated stud/clutch edge. The kernel regression proves that an exact aligned port pair validates and its real upper collision clears only for the exact valid edge, while a misbound edge remains a collision. Builder revision F remains unframed count-only corroboration rather than connector authority.

**Truth and migration:** Catalog truth advances `/25` → `/26`, 97 → 98 definitions, 36 → 37 mesh-backed surfaces, and twenty → twenty-one fully measured definitions while 61 parametric definitions, sixteen render-only promotions, and 77 project-authored physical recipes remain. Current overall truth is `sha256:3226590b11882fea03d8a6370d4ca3c6c8201feaddb56882a243a69acba627e9`, with catalog `sha256:f86310b89f3224cff7a8d571de5a26fd36440ab46235abf1cf530e2f65f41b37`, connector `sha256:93f0a5fc899083be25c5364266e7046b397683204e0e0991f106425ec5a99059`, collision `sha256:7e9905d9f988c288eaeddee3d7befb7af79266518612bbba171d9b7f7fb1c463`, transform `sha256:a8694ddcdc39da5afd946a6012ac2588233bebe2eed457e8501cf572661b2956`, and unchanged validator `sha256:fb0676931eb66a0096f393794d0be1297227811a77b986c0a1d05847ee3127d4`. The explicit `/25` → `/26` migration preserves the 97-part historical roster from source commit `cf8996f015eee595d76ef79f06c15169f674aca6`, adds only `builtin:plate-1x5`, and refuses a forged `/25` document that pre-seeds the future identity. Connection history projects 2,311 endpoints at `sha256:ff3ec2777e568cdab960bea84bdf54dd6aafb26497c262ebdfa42487312c3f93`, with the `/25` row pinned at 2,301 endpoints, shared identities unchanged, ten additive `78329` endpoints, three reachable pairs unchanged at `sha256:7431a242907aa9829ead6a279d0b530fe5f5d00ee31e6ddc1576fe66a8a07add`, 22 endpoint-delta rows, and zero pair-delta rows.

**Generated and coverage evidence:** The archive-backed generator reproduces all 37 roots and 211 unique files with zero hard failures; 209 files are CC-BY-4.0 and two dual-licensed files use their CC-BY-4.0 option. Its fresh `/26` check report is 219,322 bytes at `sha256:7c71d75f3d8f2388fbd83a01bbc74aabc6eeb8345c910e1f54bc8d05893b3f22`, while the initial write report is 219,309 bytes at `sha256:56cea66dcd26f4eceda2a63efbf500144106c1db028d5e1c0d259413edde48de`. The geometry roster is `sha256:c182bd6a4b4ec4f189dfc5846d7982bb4ef3d00f2ac04d250a63f7080354d9ad`, and the deterministic required-leaf intersection advances 19 → 20 of 121 identities at `sha256:4e5df0a3aa5723b5aa5abef96e705562913c560a8a7452a2cf29582c2f05ba8f`. All seven retained `78329` locators at steps 265, 268, 271, 274, 277, 282, and 336 are stale and self-contradictory, so they supply no printed identity, crop, placement, action-ledger, or frontier authority; the trusted action ledger remains through step 25 and the step-26 `28802`/`10201` contradiction remains unresolved.

**Visual and editor evidence:** The one-part `/26` admission capture batch is `sha256:0305f056cff14aa658d801f73a5cd852aff851c13378dc19ab7f08962f1c26f6`, packet `sha256:3dcce6065d94e243c65485c0b29effba307ae2e9e508ef3c49fa7e8d6a059531`, native-pair manifest `sha256:774dd19d354961b1da5907a8b31dea01d97160bf152f6152184acc8699bf397b`, review `sha256:f16c939d3f333b5b581532010093a98f0d368d5964aeeb59f77a94358e8d482b`, and review batch `sha256:0031abc4205d40e9c2d2e1543674bc7a3bd12916c32ef5c75200db0c34fdface`. All eight original 640 × 640 source/catalog pairs were inspected at native size; seven are decoded-RGBA exact and the isometric pair is visually indistinguishable. A separate fresh headless pass used the real editor UI to search `78329`, observed 98 definitions and 33 plates, selected the sole 460-triangle LDraw-backed result, and placed it at the origin. The one-part document was globally valid at `sha256:7365cbf1980f43536b40472b1b8857856a635730a49ba9a9501503337488800f`; the 199,383-byte full-editor screenshot is `sha256:be21961042d5eb17668ded5c93d523662aa78e51dea5110bd90da98e7e790a7d`, and the 13,276-byte observation is `sha256:71acb1ce24c0fb3e46939cd18c75127dc0ad507f4493d81b1ed365af2e72a5db`. The whole editor and canonical isometric, top, front, back, left, right, and underside views were inspected at original resolution; the direct underside view is grid-occluded, so the internal-rail, tube, and cavity assessment comes only from the clean underside-oblique source/catalog pair. No console or page errors occurred.

**Resolved-prefix verification:** The exact retained 14,896-byte diagnostic prefix at `sha256:2edf84fbf1eab57e86cd2670f9bdb5e60a7ac33dbda454f22d9c9a85cbf8b70f` migrates from its historical truth to `/26` without blocking reasons, exports to MPD, and imports through the real editor UI as eight parts, 30 connections, and five `BuildStep` rows containing `[2, 1, 1, 2, 2]` parts. Fresh Build-mode playback visits positions 0 through 5 with cumulative counts `[0, 2, 3, 4, 6, 8]`; every nonzero step reports `verified`, validation stays globally valid with zero issues, and no console or page errors occur. The position-5 isometric is 136,164 bytes at `sha256:dcd8285e510cf8954480141f04996a302f062b1eec1117853b061928f75c05d9`, while the full editor is 243,715 bytes at `sha256:dbf71c1398a02a501f325be2fc0d1a101cc1551a7cca9e38129e27b97d6ea969`. All six playback frames and the final isometric, top, underside, front, back, left, and right views were inspected at original resolution; the additions remain visible, in frame, and coherent across the sweep. This verifies migration, structural validity, and playback for the five-step reflected diagnostic only: exact proper-frame target equivalence remains proven only through printed steps 1 and 2, first fails at step-3 `6106`, and no canonical frontier advances.

**Resolved-population audit:** Canonical current production has zero completed placement steps; the official proper-frame-equivalent prefix is exactly steps 1–2; the historical visual-search diagnostic is exactly steps 1–5; and ledger rows 1–25 are source alignment rather than placement or completion. The five diagnostic prefixes add `[2, 1, 1, 2, 2]` pieces, remain globally valid and buildable under the current `/26` projection, and recompute structural hashes `sha256:ccabbc6ddc40ae030eecfbb1bee7aea21190abf85417077b87adfe6f6e71ddf1`, `sha256:35e4918ccf6785087c408cbdbf65b34b110d7e918ae71b79f8589b9e0c3c8bd4`, `sha256:81c223022109da24fd0737e343eae913f11fc5f6553f755d320c815cdc83eac6`, `sha256:0d5ff1160553a0001fbc19afae7e2e0f2c70f0e225c82288bbbe52e1621c93a9`, and `sha256:47ae3d353885f5de11b685a4bec4ca1132554a19e1f1e30454281252f7d64c93` for steps 1 through 5. The retained action ledger has 44 accepted pieces plus 92 refusals across 136 required pieces; only steps 1, 3, 4, 6, 8, 10, 12, and 17 are quantity-complete. Its historical v5-coordinate result remains 97 failures, while a fresh exact-current-input recomputation returns 186: 162 `action-ledger-incomplete` and 24 `official-frame-calibration-missing`. Authentic `/5` answers remain absent, so the publisher refuses before compilation.

**Frozen replay instrument repair:** The actual retained `/13` part and color allowlists contain the same 85 part IDs and 45 color IDs as the reviewed `/26` predecessor but in a different order. The frozen bridge had compared those set-valued constraints with `JSON.stringify`, so it rejected the exact ignored diagnostic before semantic projection even though the synthetic fixture passed. Exact member comparison now admits permutations while separate duplicate and substitution controls still fail closed. The ignored-run regression also pins the diagnostic file and applies the frozen semantic completion projection, reproducing `run-incomplete`, five `visual-evidence-unverified`, and one `official-frame-calibration-missing` failures. The focused file passes 9/9 including the exact local retained generation; this closes the replay-instrument defect without granting full-tuple, score, manifest, source-render, official-target, or production authority.

**Acceleration audit:** A fresh no-model deterministic census completed in 4.2 seconds and found a catalog-covered prefix through step 27, 164 of 331 covered steps, and 952 of 1,380 placeable pieces. All sixteen callouts through step 7 have an exact deterministic proposal, but seven remain geometry-only and therefore authority-absent. The immediate throughput bottleneck is the literal step-1 refusal after eight D4 roots, not the fixed-8,192 reducer or later catalog coverage. The next bounded unit will join the existing atomic branch batch, unique-physical-work compiler, deduplicated eight-camera frontier, and authority-absent observation closure; it must retain eight roots, compile once per unique transition, render eight cameras per unique child document, account every edge, leave the user document untouched, and stop at step 3 if `6106` still empties the proper-frame frontier. Random catalog admission is deferred: the first misses are step-28 `3245c` and step-29 `99563`, after the earlier step-26 `28802`/`10201` contradiction.

**Gate-3 and render-reduction boundary:** The reviewed-additive successor, frozen legacy projection, final parent migration, host verification, and four independent current structural-hash pins now bind `/26` and exactly thirteen post-`/13` definitions while all source document hashes remain immutable. Synthetic current hashes are `[b8f3baf0843074feec51fdfbbf47181c683d0629472ae8e552b6f8607c627391, cfd428b3800064c2358db2166c6051ef1f831f53df00bca6da644e11ebb3d79f, c683e42d275e43531d93bc1e7ff371c98cc8fbe3824c08f20ef3d6388b7bf183, a7c533c60deebe0f658b209cd58514c4f05456c5f9fb0d6c2b2ecc39aa71f7d7]`; production current hashes are `[349f031229e98f9869f218849b9a7a84dd337cd74edc534639fb5753530cf6f7, aafbd400b179b22aa052b528338489e44ef3ba2d05dbe15d7e514648d64d1006, b67a24885463d1613f513112dbad1e9c080df09cb57177c7f4f9598098ab8046, 191b67add0491ab084e4216bef1cdf607f6d8b3eb1da25791c5a4b96ac1c624f]`. The current source attestation covers 3,607 files at `sha256:e0080ad0e0e93d8e132f12dfff6f17d1807c462b90ae8697c3244666c6b3751a`; all fifteen Builder rows change only `expectedCatalogDefinitionDigest`, with geometry, connector, and collision pins unchanged. Official input-chain regeneration wrote ignored calibration schema `/8` at `sha256:74ca22e1f7831a3959be232e0fced96e46fbc2797e6d6d1e8f92f360fd4bc097` and then refused the absent authentic answer file before calling a model, rebuilding coverage, or overwriting the action ledger. No fresh 14,172-row long-depth run, score rebind, production frontier, or document publication is claimed. The fixed-8192 semantics-preserving render reducer remains the already verified implementation from commit `0a81eb4`; this session did not duplicate or rewrite it.

**Validation:** Catalog admission and notices checks pass 131 tests across eighteen files, migration checks pass 97 tests across eight files, Gate-3/source checks pass 151 tests across fourteen files plus nineteen truth/history tests, Builder calibration passes 7/7, the exact resolved-population slice passes 79/79, the actual retained frozen replay passes 9/9, the final source-attestation closure passes 8/8 at 3,607 files and `sha256:e0080ad0e0e93d8e132f12dfff6f17d1807c462b90ae8697c3244666c6b3751a`, and the fresh parent-only Playwright control passes. The authoritative `npm run verify` gate exits zero with 508 Python cases passing and 31 intentionally skipped across 539 declared, 3,159 Vitest cases passing and five skipped across 376 passing and two skipped files, 53 headless Playwright cases passing and eleven intentional opt-in skips across 64 declared, and the production bundle guard. Both full-lockfile and runtime-only audits report zero vulnerabilities.

**Independent review and gate repairs:** The harsh whole-diff critic found stale `/25` current-authority counts in the part model, BOM, mesh registry comment, and a 26-truth test name; those now report 37 mesh roots, 211 unique files, current roster `sha256:c182bd6a4b4ecf189dfc5846d7982bb4ef3d00f2ac04d250a63f7080354d9ad`, and 27 reviewed truths. The resolved-step audit separated zero canonical steps, two proper-frame steps, five diagnostic steps, and 25 source-alignment rows, then exposed both the historical-97/current-186 ledger drift and the order-sensitive frozen replay defect; both are now documented and the latter is a tested exact-member gate over the actual retained bytes. Review also caught `measured_part_plan.py` at the forbidden 500-line boundary; it is 499 and now included in the executable under-500 check. Final attestation rebinding followed every source repair. No credential-like addition, unsafe collision or migration behavior, oversized new blob, unexpected untracked source, or unrelated edit remained. `migration.test.ts` at 982 lines and `measured_part_plan_test.py` at 499 are green but must be split before another catalog version rather than compressed further.

## 33909 advances measured catalog truth without advancing the printed build

**Timestamp:** 2026-08-24 16:04–17:15 PDT

**Action:** Added official design `33909`, Plate 2 x 2 with 2 Studs on One Edge, as one complete measured catalog identity. Official root `parts/33909.dat` at `sha256:8da6789db82746f179997ed4b917d00d34d03a6486d6aa27c76d17c9b21d8609` expands through an exact nine-file, 10,203-byte CC-BY-4.0 closure at manifest `sha256:72174370ab6b3d2e0d00d7b72a0687a67da1cccd4014f1f799e113eecb504a15` into 220 triangles over 242 stored vertices, partitioned as 124 body and 96 stud triangles. Yaw zero plus `[0,-4,0]` is the selected catalog frame; body bounds are `[-20,-4,-20]..[20,4,20]`, visual bounds are `[-20,-8,-20]..[20,4,20]`, and the 401,980-point candidate score is `0.9955832518073061` with zero hard failures.

**Connector and collision boundary:** The checksum-pinned LDCad walk consults `p/stud.dat`, `p/stud4.dat`, and `parts/33909.dat`, and exactly authors two visible studs at `[-10,-4,10]` and `[10,-4,10]` plus four underside clutches on the regular 20-LDU 2 × 2 lattice. The conservative collision field contains 41 positive-volume body boxes plus two source-radius stud cylinders, and all four clutch-room checks pass. Ordinary collision retains exact radius `6.0001514980873605`, while nominal radius 6 is selected only for a validated stud/clutch edge. A three-part kernel regression proves that a lower and upper edge each clear only their own authorized collision pair and that both edges together are globally valid. Builder revision E remains unframed count-only corroboration rather than connector authority.

**Truth and migration:** Catalog truth advances `/24` → `/25`, 96 → 97 definitions, 35 → 36 mesh-backed surfaces, and nineteen → twenty fully measured definitions while 61 parametric definitions, sixteen render-only promotions, and 77 project-authored physical recipes remain. Current overall truth is `sha256:364ef046160736292eb51b331ce27ff246fa8940e16b256d53a68b9656a6018f`, with catalog `sha256:77f8faaacf9e0ad21f74bab3a06daab8e5cb4df088ee672d21da1e639ad76036`, connector `sha256:5c1ee759633b3962e41e26a3f94f296fdd07b3450381f2613ee018caba8ba48d`, collision `sha256:8a39981fddfbd1d4e9a5e4a21656105094a11dbdfb35305cb4da07c51263c742`, transform `sha256:e8066b7f1c3c18530536525bbc569a6dff4b311d4c2002c2d0c55f2cde30c4f5`, and unchanged validator `sha256:fb0676931eb66a0096f393794d0be1297227811a77b986c0a1d05847ee3127d4`. The explicit `/24` → `/25` migration preserves the 96-part historical roster from source commit `ec2387bf8b3b1a8d70a11e95c6c6547049037886`, adds only `builtin:plate-2x2-two-studs`, and refuses a forged `/24` document that pre-seeds the future identity. Connection history projects 2,301 endpoints at `sha256:0d2515f203b36c14ea14c3ab70c22aa3adcefef088ea0ba03ea3d9d3f2d52d54`, with three reachable pairs unchanged at `sha256:7431a242907aa9829ead6a279d0b530fe5f5d00ee31e6ddc1576fe66a8a07add`.

**Generated and coverage evidence:** The archive-backed generator reproduces all 36 roots and 208 unique files with zero hard failures; 206 files are CC-BY-4.0 and two dual-licensed files use their CC-BY-4.0 option. Its fresh `/25` check report is 210,666 bytes at `sha256:5a358bee3beb3f7181cac34fd0703c2f7521c08393a7139ba2f363fa81fe37b9`, while the initial write report was 210,653 bytes at `sha256:af2bec62a01a69212b57bc403f536957e942ad1e4613253da2807bdf03009354`. The geometry roster is `sha256:ab51029593fc1297c11f33f718f269fd9bf14cf1b42e5daec34ed44526f11a75`, and the deterministic required-leaf intersection advances 18 → 19 of 121 identities at `sha256:7859f65b300f4e8a2b6dae85cffedbcd71e5f54fcffd8f688089c3353c549f28`. The step-76 locator is stale and self-contradictory, so it supplies no printed identity, crop, placement, action-ledger, or frontier authority; the trusted action ledger remains through step 25 and the step-26 `28802`/`10201` contradiction remains unresolved.

**Visual and editor evidence:** The one-part `/25` admission capture batch is `sha256:34fce749a7eaa53d1e43f7782425fc1b7cccb7b1bfa3b051955e88e7f48abc3b`, packet `sha256:bb640b711f5cf7aea4d48ff77676965ab4f78e650a2dd136ea40e18da6bec906`, native-pair manifest `sha256:af2edbcfb887588926bb61e7fc1042c6ac0283aea654e2f7562b8ba2f2979627`, review `sha256:b3afd9c82bd1081cbeb24b5c8cf892a09abfa44e9a3d9883304ad31efbc1050f`, and review batch `sha256:a502a85d9fcc2d04689d5c172e5f7f0ec95c4fe79cf68b555a1d4b0f28a2fc17`. All eight original 640 × 640 source/catalog pairs were inspected at native size and are byte-identical. A separate fresh headless pass used the real editor UI to search `33909`, observed 97 definitions and 32 plates, selected the sole result, and placed it at the origin. The one-part document was globally valid at `sha256:1694c915d36a1a4110193046949ffb10df41dfa61d60783683d02634c336d065`; the 198,617-byte full-editor screenshot is `sha256:cbd823589a92c7e6fb23f9522d2bae624358e2d79cf4c3b14f310431ecf6b03c`, and the 12,112-byte observation is `sha256:7b673bfca74ecf97cfbea02c01b993356348f30e566c9162e5ce1e0b897b8f12`. The whole editor and canonical isometric, top, front, back, left, right, and underside views were inspected at original resolution; the direct underside view is grid-occluded, so the ring and cavity assessment comes only from the clean underside-oblique source/catalog pair. No console or page errors occurred.

**Gate-3 and render-reduction boundary:** The reviewed-additive successor, frozen legacy projection, final parent migration, host verification, and four independent current structural-hash pins now bind `/25` and exactly twelve post-`/13` definitions while all source document hashes remain immutable. The current source attestation covers 3,604 files at `sha256:ba13563c252361846501b8d3a634e178561e75ba15d61dbaf62f7dedf0454245`; all fifteen Builder rows change only `expectedCatalogDefinitionDigest`, with geometry, connector, and collision pins byte-identical. Official input-chain regeneration wrote the ignored current calibration and then refused the absent authentic answer file before calling a model, rebuilding coverage, or overwriting the action ledger. No fresh 14,172-row long-depth run, score rebind, production frontier, or document publication is claimed. The requested fixed-8192 semantics-preserving render reducer was already present in commit `0a81eb4`; inspection and its executable controls confirmed the fixed cap, retained semantics, and deterministic output, so this session did not duplicate or rewrite it.

**Validation:** The archive-backed generator reproduces all committed tables, Python plan/emitter checks pass 32/32, focused catalog and brick-kernel coverage passes 601 tests across 67 files, migration coverage passes 94 tests across ten files, and the final source-attestation gate passes 8/8. Gate-3/source checks pass 166 tests across sixteen files, the wider source/input slice passes 61 across nine, the final focused recheck passes 43 across four, Builder calibration passes 7/7, and the parent-only Playwright control passes. The authoritative `npm run verify` gate passes 507 Python cases with 31 intentional skips across 538 declared, 3,148 Vitest cases with five skips across 373 passing and two skipped files, 53 headless Playwright cases with eleven intentional opt-in skips across 64 declared, and the production bundle guard. Both full-lockfile and runtime dependency audits report zero vulnerabilities.

**Rejected routes and review:** Candidate `15571` passed the Python score but live TypeScript admission correctly refused its mixed connector-lattice parities; candidate `2310` was rejected because its LDCad frames conflict with visible LDraw frames. Those existing gates selected a different measured part without opening new hardening work. The independent whole-diff critic found that the plan still called the stale step-76 locator a first use, the devlog omitted `/25`, canonical current-position documents still described `/24`, source/test narratives still said 35 meshes and nineteen measured parts, and the runbook retained the historical 3,601-file source pin as current. The repairs make `/24` historical, add the complete `/25` visual, measurement, editor, migration, Gate-3, and frontier boundary, and align source counts to the live catalog. The critic then reran source attestation after those repairs and caught the expected byte drift inside its broad closure; the final 3,604-file digest `sha256:ba13563c252361846501b8d3a634e178561e75ba15d61dbaf62f7dedf0454245` passes 8/8. The executable migration instrument, not cross-document agreement, fixes the historical count at 22 truth-row endpoint deltas. Secret, size, line-count, ignored-artifact, process, and stale-current sweeps found no remaining code blocker.

## 11212 advances measured catalog truth and Gate 3 rebinds only its fixed parents

**Timestamp:** 2026-08-24 13:47–16:04 PDT

**Action:** Added official design `11212`, Plate 3 x 3, as one complete measured catalog identity. Official root `parts/11212.dat` at `sha256:c527adbbc5db2983cdc9d0b28481d57a248fd0125d8aa00c13aebd7c32b6633f` expands through an exact ten-file, 11,078-byte CC-BY-4.0 closure at manifest `sha256:8ff079db5d230fbba570a54ef1718c37a33db1059b31034fdd5a5ba9f12e0c73` into 844 triangles over 873 stored vertices. The square plate is quarter-turn symmetric, so yaw zero plus `[0,-4,0]` is the canonical representative. Body bounds are `[-30,-4,-30]..[30,4,30]`, visual bounds are `[-30,-8,-30]..[30,4,30]`, and the candidate score is `0.9925023760839874` with zero hard failures.

**Connector and collision boundary:** The exact checksum-pinned LDCad walk consults `p/stud.dat`, `p/stud4.dat`, and `parts/11212.dat`; active `p/stud.dat` metadata authors nine studs, active root metadata authors nine underside clutches on the centered 20-LDU 3 × 3 lattice, and `p/stud4.dat` supplies no active meta because its anti-stud declaration is disabled. The conservative field contains 129 positive-volume body boxes plus nine source-radius stud cylinders; all nine clutch-room checks and all eighteen lattice checks pass. Ordinary collision retains exact radius `6.0001514980873605`, while nominal radius 6 is selected only for an exact validated stud/clutch edge. A three-part kernel regression proves that the lower and upper edges each clear only their own authorized pair, both edges together are globally valid, and the unconnected controls still report both body/stud collision pairs. Revision-I record metadata from the checksum-pinned native pack reports nine clutches but supplies no reviewed frame, so it remains count-only counterevidence rather than selected authority.

**Truth and migration:** Catalog truth advances `/23` → `/24`, 95 → 96 definitions, 34 → 35 mesh-backed surfaces, and eighteen → nineteen fully measured definitions while 61 parametric definitions, sixteen render-only promotions, and 77 project-authored physical recipes remain. Current overall truth is `sha256:09288fc048ec112225b9e605df7af2d2e9692031b9eb7a89755575956af4c10d`, with catalog `sha256:8cf9f35a1a692f285994c1819d1063fde6912f7b0ef949fcca1ae2adadeaa65e`, connector `sha256:c88a4334befcea378749b7a31d7c46fb0d0a5818f5a5914c608e0ed9ef506623`, collision `sha256:5e4de952a9aa7b49211e563ef2d397572b805ffa6f80b3f96995a7511daff693`, transform `sha256:cf509b04cfab06646a74144cdcda8efc2f6313f7658fe7d2c08d77f53af7e56a`, and unchanged validator `sha256:fb0676931eb66a0096f393794d0be1297227811a77b986c0a1d05847ee3127d4`. The explicit `/23` → `/24` migration preserves the 95-part historical roster from source commit `d99b74d355684c8ceaca0ad6f2df76d96ebe4937` at truth `sha256:af781e7356e28622fb13afcb571d28495a0962d6aa78ef70d988126a9c4aeefb` and adds only `builtin:plate-3x3`. An independent execution of that source commit reproduced 1,583,353 definition bytes at `sha256:ceff2b3acbb53f647eea56d6ae4d271c1c048f94c7a035faf8a60b2979521ba3`, connector-plus-collision `sha256:cd14902b4fda0525457091ed7e59820113cf52dd5cbcf15d0c88f58690bc486f`, and collision-only `sha256:2c6a27bf11f5f9d16ef8535b053df64b8c46940af4875a94b3380cf0d52b7bee`; the `/24` test reproduces all three from its first 95 rows after restoring the `/23` label. Immutable history now covers 25 reviewed source truths and projects 2,295 current endpoints at `sha256:6dfd3657c9d4d3af8815fa6dad9cb2906416436239a07cfdcb968932b90f2ab1`, retaining 22 endpoint deltas, three reachable pairs at unchanged `sha256:7431a242907aa9829ead6a279d0b530fe5f5d00ee31e6ddc1576fe66a8a07add`, and zero pair deltas.

**Generated, attribution, and coverage evidence:** The measured-table generator adds `11212` only to measured mesh shard G, measured blueprint shard G, and the bundled-source table while reproducing all sixteen owned outputs. Emission report `sha256:ba7044a928a878edc27520f1553d14cd9ac7bf5d3f81c0ca4f69e70f2e392def` and the 203,106-byte `--check` report `sha256:7aeccd85ad90106af16eb5c152a93162e3cb22be1793731e75673d1d02f45719` cover 23 measured-pipeline plus twelve dedicated render-only-pipeline rows with zero hard failures; four measured-path rows remain final render-only promotions, so catalog authority is nineteen fully measured plus sixteen render-only. The geometry roster covers 207 unique files for 35 roots at `sha256:1f5cfc0ffe6edcf6e1cdc011e6ec4bd2304b3af32c8acc1ae3ba7d116bd15e25`; 205 files declare CC BY 4.0, `parts/30503.dat` and `parts/32064a.dat` use their CC BY 4.0 option from a dual declaration, and 30 authors are credited. The deterministic required-leaf intersection advances 17 → 18 of 121 identities at `sha256:71648d320171d583cbff1ad4768e2e9e192747db48af67cf529e7c3649021847`. That intersection authenticates no printed crop or placement, does not advance the retained action ledger beyond step 25, and does not resolve the printed-step-26 `28802` versus Builder `10201` contradiction.

**Visual and editor evidence:** The one-part `/24` admission packet is `sha256:a82d963c0e29fafdc9a9f76d6b1c8ab7c73f6127fb989e346f0ef449cba9f4fa`; capture batch `sha256:b8db94807b8616c62aee7e63e918ad8a14dac6af6eec314541508b76cedee6df`, native-pair manifest `sha256:fe0c29059f648a47febc555f853820f98540784314d247d7b0bbe90e933d7241`, review `sha256:17e76699de8bbf1ff96a447dd035c868906d3073901dd0f824a7da67fa4ae326`, and review batch `sha256:ca0ced2b265b389eb959cc321cc3cce70ed3ff34f0910e735fca8e086e29893e` bind all eight original 640 × 640 source/catalog pairs. Seven are RGBA-exact; isometric differs by one channel value in one pixel at foreground IoU one and mean absolute RGB delta `8.138020833333334e-7`. Every raw source and candidate image was inspected at original size and reviewed `same`; the underside-oblique view exposes four tube rings, walls, ribs, cavities, winding, and shading. A separate fresh headless pass searched `11212` through the real UI, confirmed 96 definitions and 31 Plates entries, selected the sole 844-triangle preview, used `Place at origin`, and observed one globally valid part, zero connections, zero issues, and document `sha256:ae554c96f474c9e0b7f2c7de9c4fbd59aed09617e8e01950077aa68d02b6d6b3` from both automation surfaces. The 206,633-byte full-editor screenshot is `sha256:d867cb32c673873d163406c3612287c3cd93e830f04b625914f382d0c4935b09`; the 11,874-byte observation is `sha256:511cf592b8f902cce0efb88b937f8af66573086865f2dd0eae88a6d8e803dbdc`. The full UI, palette, isometric, and orthographic captures were inspected at original size; the straight underside is grid-occluded and therefore carries no tube claim, which remains bound to the clean-scene underside-oblique admission pair. No console or page errors occurred.

**Gate-3 and fixed-8,192 boundary:** The active reviewed-additive successor, frozen legacy projection, final parent migration, host verification, four synthetic current pins, and four production current pins now target `/24` and exactly eleven post-`/13` definitions while preserving every immutable source hash. A fresh source-locked parent-only Playwright run reconstructs all four `/13` parents before migration, preserves every part, and passes with production current hashes `[51c7b65a3796e4e5296d8fdddd1baaa8e05fc91c3155b503fa4a67afcf7c09d3, dfb0a11b7d19aa844b1c8ea7695f9eaabfa863981b6cc58d49392dba600e98a1, 4635c3c8e813873e0cd67c04f41d8dbe59d3559a2f0da47b779426d95234effe, 208b52a6d30873e8306a231253fe0851c9f9b210dd30fa5232ecf3bd8cf49e67]`; current source attestation covers 3,601 files at `sha256:253fe23d52798792c55f6a46988168048740222cdfc4d58415241a4f3fb5210c`. Requested diagnostic and parent-only modes now fail closed if the exact sample booklet is absent, while ordinary non-requested runs and booklet-free prewarming retain their intentional behavior. Parent-only mode emits no image and does not execute or rebind the historical 14,172 rows, scores, 17 leaves, or frontier. The fixed-8,192 reducer required no new implementation because commit `0a81eb4` already carries the semantics-preserving depth route; the retained historical `/15` control remains 14,172 logical rows, 30 batches, 17 leaves, and 6,559 charged subject rasters, 1,633 below the limit, with all baseline digests preserved. This session validated that existing behavior without claiming a `/24` long run.

**Input-chain boundary:** Regenerating catalog-derived stage 3 produced the current schema-`/8`, fifteen-frame, 20,379-byte Builder calibration at `sha256:20bb0da3fe852e1eeefd55e1ccc70579be1b25a1e18c640251d152afaa706c9b`; all geometry, connector, and collision pins stayed fixed while catalog-definition digests moved. The deterministic chain then refused before stage 4 because `output/part-identification/answers-claude-opus-5.json` is absent. No answer was synthesized, no external model or CLI was called, no action ledger was rewritten, and no later artifact was published.

**Validation:** The final exact source-attestation suite passes 8/8 at 3,601 files and `sha256:253fe23d52798792c55f6a46988168048740222cdfc4d58415241a4f3fb5210c`; the source-locked parent-only browser reconstruction passes after that rebind. The regenerated schema-`/2`, 129,256-byte LDCad shadow report at `sha256:e0cf07bf00542614993f4d72fe5d72458d60d05f97895803cfc6d842c258cc74` now records the checksum-pinned 2,069,952-byte native pack at `sha256:e5bb745faa79c5e7cb525eb0a11a8443815a0c4805c85644204b26c462ac636d` as count-only counterevidence with no frame or connector authority. Independent review found and closed the predecessor-payload, migration-history, requested-mode fail-closed, conflicting-mode, consulted-shadow-authority, ungated-yaw, and native-pack self-description gaps, then reported no remaining actionable finding in the stabilized diff. The authoritative `npm run verify` gate passes 506 Python cases with 31 intentional skips across 537 declared, 370 Vitest files plus two skipped files with 3,136 passing tests and five skips, 53 headless Playwright cases with eleven intentional opt-in skips across 64 declared, and the production bundle guard. Both full-lockfile and runtime dependency audits report zero vulnerabilities.

## 32064 advances measured catalog truth while Gate 3 stays on its retained denominator

**Timestamp:** 2026-08-24 10:55–13:46 PDT

**Action:** Added official-inventory design `32064`, Technic Brick 1 x 2 with Axle Hole, as one complete measured catalog identity; the official set inventory carries two. The exact official moved root `parts/32064.dat` at `sha256:b6240d5798083701834cec8f566d7fca05cbc51123fad8500d3125fa68b4c465` resolves to `parts/32064a.dat` and expands into a 23-file, 27,103-byte closure at manifest `sha256:85d190886138b76cb9a98f4d3351652d6fea64423bc576a4ef1116a99dfd686e`, 458 triangles, exact catalog bounds `[-10,-16,-20]..[10,12,20]`, 23 surface-derived body boxes, and two source-radius stud cylinders. Revision-I record metadata from the checksum-pinned native pack supplies no reviewed frame and remains counterevidence only.

**Connector result and boundary:** The exact checksum-pinned LDCad composition through `p/axlehol5.dat`, `p/stud2.dat`, and `parts/32064a.dat` authors two underside clutches and one capless, sliding, `YOnly` female `A 6 1` axle-hole declaration. Its composed source midpoint `[0,10,0]` with `+Z` normal becomes catalog `[0,-2,0]` with `+X` normal under the one measured part frame. Together with two source studs this yields five discrete endpoints. The fixed 561,932-point containment probe finds zero escaped points, both clutch cells retain measured room, and the scorecard reaches composite `0.9694824982385262` with zero hard failures; the axle-hole row remains explicitly unscored under its separate exact-source gate. A valid axle/axle-hole edge still produces `PART_BODY_COLLISION`, so this admission grants no continuous sliding, axle-through-bore collision relief, grip, stability, insertion access, or trusted placement.

**Truth and migration:** Catalog truth advances `/22` → `/23`, 94 → 95 definitions, 33 → 34 mesh-backed surfaces, and 17 → 18 fully measured definitions while 61 parametric definitions, sixteen render-only promotions, and 77 project-authored physical recipes remain. Current overall truth is `sha256:af781e7356e28622fb13afcb571d28495a0962d6aa78ef70d988126a9c4aeefb`, with catalog `sha256:d7df28c96d3b4d8c31267289a972f0441c9b275ab1d65aa21a2247ca7f1d7a19`, connector `sha256:57eb657485d5049c5e3624e2811886473b0f230463815fd6ddfd677329b8c62f`, collision `sha256:daeb4dcd18ecb29153b425c6c9db060b087a5486e0238dbe3673cfdd521e6cfa`, transform `sha256:397a1cddf7cba68e3fae67753075cdd82003f5a0d2d00ebbae066848610d3d27`, and unchanged validator `sha256:fb0676931eb66a0096f393794d0be1297227811a77b986c0a1d05847ee3127d4`. The explicit `/22` → `/23` migration preserves the 94-part historical roster and adds only `builtin:technic-brick-1x2-axle-hole`; immutable history now covers 24 reviewed source truths and projects 2,277 current endpoints at `sha256:5c441a333206827791b01f59643c145102ddf28d1410667d183fadafc0d0d84c`, retaining 22 endpoint deltas, three reachable pairs at unchanged `sha256:7431a242907aa9829ead6a279d0b530fe5f5d00ee31e6ddc1576fe66a8a07add`, and zero pair deltas.

**Generated and coverage evidence:** The measured-table generator appends only the measured-G mesh and blueprint shards, emits report schema `/6` over sixteen generated outputs, and reproduces all 34 roots with zero hard failures; its final check report is `sha256:57a7a8909f2f6d9ff27e79185970a796acd70f82d6e94b4b922de8c672768d07`. Its 22 measured-pipeline plus 12 dedicated render-only-pipeline rows are generation-path counts: the catalog factory consumes four measured-path rows as final render-only promotions, yielding the catalog's separate eighteen fully measured plus sixteen render-only authority split. The geometry roster covers 206 unique files for 34 roots at `sha256:0ffbf90922d0eb36d6f81ac65aa30cb530c7f1475e50349876ac91414dc248f9`; 204 files declare CC BY 4.0, `parts/30503.dat` and `parts/32064a.dat` use their CC BY 4.0 option from a dual declaration, and 29 authors are credited. The deterministic required-leaf intersection advances 16 → 17 of 121 identities at `sha256:c5b20fa64d9d0457822de5bbecc3a66f4f1633b2004e7e23342a3c95f99c6633`. The retained step-39 pair-judged-same and step-101 vision-unreviewed rows remain stale locator/counterevidence only; neither authenticates printed identity, adds an action, nor advances a verified printed-step frontier.

**Critic repairs and instrument truth:** Independent review caught that the first real-editor audit hashed a second palette crop instead of the bytes retained on disk, that fixture strings reproduced two exact CC-BY-SA shadow meta lines, that the release notice named per-file contributors only as a class, that the containment sampler silently capped long triangles while presenting the requested spacing as achieved, that Gate-3 helper names still said V22, and that one migration test exceeded the 1,000-line hard ceiling. The editor audit was rerun with one bound screenshot byte stream and every fresh view was reinspected; the shadow fixtures now express synthetic equivalent declarations while the pinned-source tests still read the real files; the generated notice names Roland Melkert, Philippe Hurbain, and Jason McReynolds and refuses an unmapped authoritative route; sampling now refuses nonpositive or nonfinite spacing, safely caps finite-subnormal requests without overflow, exposes requested/effective/cap diagnostics in emitter schema `/6` and generic scorer schema `/2`, and keeps the deliberately pinned Builder/LDCad `/1` envelopes byte-identical; helper names now say V23; and shared additive rosters reduce the migration test to 967 lines. The `32064` row requests and achieves maximum effective spacing 0.25 LDU across 561,932 points with zero capped triangles; eight older rows disclose their cap-limited coarser grids without rewriting their historical reports.

**Visual and editor evidence:** The one-part `/23` admission packet is `sha256:c4728ac4722ccecbb630e8a9a386df09514a1666caa047e27016482e21c785ca`; its capture batch is `sha256:717249d0a06b0b2f094836316d9b4047bbd371f63eea18536c1a3e481814786e`, native-pair manifest `sha256:c39b9392d4b33a3066588c6d072b795fb9f8ae65162b414b7b511ff163e1d5bc`, review `sha256:8292738d680a785b076b01ed9626899bd1ee6b78c9a2ac1fe574bdcdf2673c09`, and review batch `sha256:24f5d697c01e8c0e65018b0042067de272c4f393dd78510c1abe9b1142cc5565`. All eight original 640 × 640 source/catalog pairs were inspected at native size and reviewed `same`: seven are pixel-exact, while isometric differs in three pixels at maximum channel delta one, foreground IoU one, and mean absolute RGB delta `2.44140625e-6`. A separate fresh headless pass used the real UI through the local Vite development server to search `32064`, observed 95 total definitions and two Technic bricks, selected the sole result, and used `Place at origin`. The one-part document was globally valid at `sha256:1293be3ca50674cf12534ad8a29dc4da67f9a4694ca4f19ccde002b2827c8d26` under the truth reported by that run, with rendered bounds `[-0.5,-0.6000000000000001,-1]..[0.5,0.8,1]`; the 140,108-byte full-editor screenshot is `sha256:1dc0ee3dce94bd2bf67796bf6065cfcbf11e2c09996cb5e01a2b43a744390e4f`, and the audit JSON is `sha256:a6702297dda4f61ee379e038e4955ef2cb9504c515745982a83d58b64adf9feb`. The whole editor, filtered palette, inspector, and canonical isometric, front, back, left, right, top, and underside captures were inspected at original resolution and showed the expected two open studs, transverse axle opening, body walls, and exposed underside structure. No console errors or page errors occurred; the non-fatal messages were the existing Three.js shadow-map deprecation and Chromium readback-stall warnings. The ignored record binds document, truth, view, and image hashes but not app source, the executed script, browser build, or a production build; it is a dated local pixel observation, not an exact current-source or production-editor receipt.

**Gate-3 and calibration boundary:** The reviewed-additive successor, frozen legacy projection, final parent migration, host verification, and four independent current structural-hash pins now bind `/23` and exactly ten post-`/13` definitions; all source document hashes remain immutable. Focused Gate-3 and frozen-artifact verification passes 52/52 without a fresh 14,172-row long-browser run, `/16`-through-`/23` score rebind, production frontier, or document publication. All fifteen retained Builder calibration rows change only `expectedCatalogDefinitionDigest`; geometry, connector, and collision pins remain byte-identical. The regenerated schema-`/8`, fifteen-frame, 20,379-byte calibration report is `sha256:67406e1b76eda0909436c25ec1ebf76f0e2f72a49270b58147a68079a63fbc54`; stage 4 then correctly refused the absent authentic answer file before writing an action ledger. The exact current source attestation covers 3,598 files at `sha256:ca8411a777c37f97d070f095d0948fd57258a00237bacedf5292ce057621598e`.

**Validation:** Catalog tests pass 199/199 across 27 files, brick-kernel tests pass 353/353 across 33 files, migration-focused tests pass 71/71 across six files, the exact source-attestation suite passes 8/8, and the focused Gate-3 plus frozen-artifact slice passes 52/52 across six files. The migration-history check reports 24 source truths, 2,277 current endpoints, 22 endpoint deltas, and zero pair deltas; the aggregate LDCad, measured-table, plan, emitter, source-connector, sampling, and scorecard Python gates pass, including the finite-subnormal spacing and generic-scorer routing regressions, and the archive-backed generator reproduces every committed table plus the 34-root report at unchanged `sha256:57a7a8909f2f6d9ff27e79185970a796acd70f82d6e94b4b922de8c672768d07`. The authoritative `npm run verify` gate passes 504 Python cases with 31 intentional skips across 535 declared, 366 Vitest files plus two skipped files with 3,118 passing tests and five skips, 53 headless Playwright cases with eleven intentional opt-in skips across 64 declared, and the production bundle guard. Both full-lockfile and runtime dependency audits report zero vulnerabilities.

## 4519 advances the measured catalog without granting axle-through-bore semantics

**Timestamp:** 2026-08-24 08:52–10:55 PDT

**Action:** Added official-inventory design `4519`, Technic Axle 3, as a complete measured catalog identity whose first observed printed callout is step 45 at quantity three while the official set inventory carries four. The exact official root `parts/4519.dat` at `sha256:ecec609013e9d7af63c352cb61d990077c005c4c5453e121b3d192914ab55ff0` expands through `p/axleend2.dat`, `p/axlehol8.dat`, and `p/box2-9p.dat` into a four-file, 10,983-byte closure with 176 triangles at exact bounds `[-29.5,-6,-6]..[29.5,6,6]`. Its 41-box source-derived collision field is deliberately conservative. The direct LDCad `parts/4519.dat` shadow at `sha256:f5c2c6057cae2fd6cd77bc50b932bb3692ee42645b3d034e3fe944333be68344` supplies one capless, centered, sliding, unscaled, unmirrored male `A 6 60` shaft declaration; the exact bridge projects the established one-module lattice seats at x `[-20,0,20]` with normals `[-X,+X,+X]` and refuses ineligible or nonrepresentable eligible snaps without rounding.

**Result and boundary:** Catalog truth advances `/21` → `/22`, 93 → 94 definitions, 32 → 33 mesh-backed parts, and 16 → 17 fully measured mesh definitions; the 61 parametric definitions, sixteen render-only promotions, and 77 project-authored physical recipes remain unchanged. The fixed 805,136-sample scorecard reports composite `0.990324952447412` with zero hard failures, while its axle rows are explicitly unscored and owe admission only to the separate exact LDCad gate. A real axle-through-bore placement still produces `PART_BODY_COLLISION`: the new edge test proves only that the three exact ports participate in the existing axle/pin-hole taxonomy, not that collision relief, continuous sliding, insertion access, grip, or stability exists. The stale callout identity remains untrusted, so no action is added and no verified printed-step frontier advances.

**Truth and migration:** Current overall truth is `sha256:7f64021239ab6395a3666f1f72908fd420b73065909822bc68e5226785bfa12e`, with catalog `sha256:3700b53f804db905fc0b7b1f41f5e2b5d3f60f79dd6ee6ae0bc1f33ed2f99176`, connector `sha256:ba08980d0b651273651f5abd00a9eda0da412ef1ce82fbf8252f09dbff6db1fc`, collision `sha256:29d9de56ab3e4215749d51b14923457528f2afd04ce6c149731802db65e748b0`, transform `sha256:3ac16864f8a77c198b0cf78d055bedfee61990c84ff2a14fbe2ef2684632071d`, and unchanged validator `sha256:fb0676931eb66a0096f393794d0be1297227811a77b986c0a1d05847ee3127d4`. The explicit `/21` → `/22` migration preserves the 93-part historical roster, adds only `builtin:axle-1x3`, and refuses a forged `/21` document that pre-seeds the future part. Immutable migration history now covers 23 source truths and projects 2,272 current endpoints at `sha256:006157a4816e7dc9a001b0c15a7c2e45fdbaeb611142b428458457f2ea4b8ff2`, retaining 22 reviewed endpoint deltas and zero reachable-pair deltas.

**Generated and coverage evidence:** The measured-table generator appends only `4519` to new measured-F mesh and blueprint shards, records optional source connector rows under report schema `/5`, and reproduces all 33 roots with zero hard failures. Its final check report is `sha256:0fa43c6c77612485a75841a337aeea556d3a0669d6ab7d3305a605542a676540`. The geometry roster covers 199 unique files for 33 roots at `sha256:5d91f9b4ed0c2dc6472edb82e335c6ca65a390824bef7dbb960a6aeaaafa44b7`; 198 files declare CC BY 4.0 and the one dual-licensed file uses its CC BY 4.0 option, with 28 credited authors. The deterministic required-leaf intersection advances 15 → 16 of 121 identities at `sha256:6a0f740749980db97f031d45a4cf539b8460b579cc9f4abf8ac706642a78bb30` without treating the stale printed crop as identity authority.

**Visual and editor evidence:** The one-part `/22` admission packet is `sha256:c9284e7846fab3e27a1c324b332db9f96d2aab592bc84a94c0c6f27567ac11a9`; its capture batch is `sha256:0f529c77a5b97186a7d9cab1bfbff7aa9b32bb55e22c8b40b81c7f961550fc59`, native-pair manifest `sha256:878250df3ab4c3478ff1c7a7c7999c16dc9d2adcef954883427b823829fd188a`, review input and outcome `sha256:e2e652851df5b2fa8d7a8ed57428d149c29ee77d5c93dd9e56ca945e3bfeb0af`, and review batch `sha256:5c3e655e5ee041c2dff3a61420292ee88c28fe303550e1f86298655daed52c08`. All eight original 640 × 640 source/catalog pairs were inspected at native size and reviewed `same`: seven are RGBA-exact, while underside-oblique differs in one pixel by one channel value, with foreground IoU 1 and mean absolute RGB delta `8.138020833333334e-7`. A separate fresh headless pass used the real UI to search `4519`, select `Technic Axle 3`, and place it at the origin; the one-part document is globally valid at `sha256:eaac23045e78e09b3f71bc83ff906d10db5cffb7349c2370d44c55dd55174e9a`, and the 149,044-byte full-editor screenshot is `sha256:24be88e817b974e62012aa068d30937ba5d17c72399a60a954b992809e373a89`. The whole editor and canonical isometric, front, back, left, right, top, and underside views were inspected at original resolution. The only console error was the development favicon 404; WebGL precision and shadow-map deprecation warnings remained non-fatal.

**Gate-3 and validation boundary:** The active reviewed-additive successor and parent-migration contract now bind `/22` and exactly nine post-`/13` definitions. Current parent hashes were mechanically re-derived and focused Gate-3 execution remains green, but the retained 14,172-row long-browser denominator is historical `/15` evidence: no `/16` through `/22` long rerun, score rebinding, production frontier, or document publication is claimed. Focused verification passes 287 Vitest cases, 44 aggregate LDCad cases, all measured-plan/table/emitter/source-connector/scorecard entrypoints, the 23-truth migration-history check, TypeScript, BOM, generated-table reproduction, visual review, and all eight exact source-attestation cases. The final source closure contains 3,593 files at `sha256:8afbd8b35b7009b91fa2302de8fb7e7f13f150255770fa6cbe39175392362f6d`. Authoritative whole-workspace verification passes 515 Python tests with 31 intentional skips, 3,105 Vitest cases with five skips across 363 passing and two skipped files, 53 headless Playwright cases with 11 intentional opt-in skips across 64 declared, and the production bundle guard; full-lockfile and runtime audits both report zero vulnerabilities.

**Full-gate calibration repair:** The first authoritative gate proved that all fifteen retained Builder calibration source rows still pinned their `/21` catalog-definition bytes. The bounded `/22` repair changes only `expectedCatalogDefinitionDigest`, five rows in each of the three pin shards; independently recomputed geometry, connector, and collision digests remain unchanged, and the focused calibration gate passes 7/7. Regeneration deliberately reused rather than rewrote `output/real-build/catalog-coverage.json`: that retained 1,071,226-byte artifact at `sha256:a9716a14e415bc8b66e76499cd811f6f0fbbc1fdb06e3b05bb5da860df2d3ff3` remains counterevidence that still records the step-45 `4519` identity as absent. Stage 3 wrote the ignored schema-`/8`, fifteen-frame calibration report at 20,379 bytes and `sha256:d0a5d1128132151bd25980947577bfecc84201bca8f52370483e1a8c3fee9751`; stage 4 then correctly refused the missing authentic answer file before writing an action ledger. The retained 90,825-byte ledger stays byte-identical at `sha256:aae1d15884bef09e6258d77cc27012acede7ac5360b6b3ebd8e13c15ab1301b2`. This was a calibrated-report refresh with deliberate coverage reuse, not a refreshed whole input chain; requesting coverage regeneration would instead refuse at stage 2 before replacing either coverage or calibration.

**Independent review corrections:** The axle-bridge critic found that one nonrepresentable eligible snap could abort an otherwise valid batch and that explicit scale or mirror modifiers were ignored; per-snap refusal and exact modifier rejection now gate both classes. Historical truth initially drifted because the shared LDCad provenance bytes changed for every prior measured part, so `/22` now uses a part-local additive axle provenance constant while `/21` and every older snapshot retain their exact bytes. The whole-unit critic corrected an overbroad statement about malformed shadow syntax, corrected type comments that still described every authored connector as female, and reproduced that an arbitrary axle can score cleanly only in the measurement-only generic scorer: no caller candidate reaches emission, because the sole emitter builds its candidate internally and exact-gates design `4519`, three seats, the direct shadow path, and the pinned whole-tree source before scoring. A final line-count sweep found `migration.test.ts` at 1,007 lines; removing stale commentary and redundant visual separation leaves all executable files below the hard 1,000-line ceiling without dropping a case.

## Step 16 gains decoded source-art evidence, not semantic identity

**Timestamp:** 2026-08-24 07:26–08:52 PDT

**Action:** Added a bounded authority-free embedded-image measurement, exact opaque-ground canonicalizer, cross-binding compiler and conditional real browser probe for three current callouts. The 70,238,655-byte booklet is pinned at `sha256:baef0a373164b58d7c982984b52d4e50b10cc59ed28007acb456faa72359bd27`; the current callout manifest, features, tracked pair truth and element resolution are pinned at `sha256:c8d20cfe87ef9d21488725b393b94e61870fcc82b26bb497ea734fc7b97a67bf`, `sha256:13574857858b9b71ce3132ee797ff346597d0d4c20eace70eaf83408d4a523cb`, `sha256:c7b6aa8990ab9771a4de7c960ffa0b2e69a0d26e8e802ff28d1be4cc8291ca0c` and `sha256:9fb2abe8f764f3381135b378c7940f63b69a77ed0f6db8a8f28ba2d8224b3a30`. PDF.js `5.4.149` enumerates effective save/restore/transform state, accepts only exact RGB24 stride, requires one component-containing image paint at each pinned operator, snapshots mutable inputs before the first await, and removes only translation from the embedded-art digest.

**Measured result:** The judged page-18 step-14 `3023` source and page-20 step-16 target both decode to 5,355 RGB bytes at `sha256:947a808863fc4f864e6957a2f4aaf927a442b08cd8ace55b81297db971e6468c`, with the same `[24.23026,0,0,16.29094]` linear transform and translation-free embedded-art digest `sha256:2d34094e6fa953628bd0d5daf61fea3e362db78809c690fc202c24ca7d2f532c`. The page-20 `35480` image decodes to 4,128 RGB bytes at `sha256:3c127f2802e3fa9ae8be255eb0fc6d58bad91a4e7dabf6b67daa2a5176098236` and embedded-art digest `sha256:72169cac1fe9f77e777792de0a85321a36ee75a1e7c98fe0fe6b124db602d81c`. Its 165 × 118 current crop is the exact first 77,880 decoded RGBA bytes of the 165 × 122 legacy crop; the 2,640-byte suffix is exactly four complete `[140,148,148,255]` rows. Both exact-background trims yield the same 155 × 108 RGBA at `sha256:e5809c58b9b26e73e407b37950eab09415db49a16c34ee07f3cbd5beb6b060d0`, framed as big-endian width, big-endian height and row-major RGBA at `sha256:773195db76206514426028e82f24049ab5152fa53cfd04ba20017dda9926ed31`.

**Binding and authority boundary:** Each witness is cross-bound to its exact page, printed step, quantity, label coordinates, component bounds, foreground count, absolute component digest, PDF operator pin, authenticated manifest crop digest and freshly rendered ranked crop. The proof JSON carries that binding, all four attached PNG digests, both canonical summaries and the exact recut report. The result always says `admissionAuthority: "none"`, `semanticIdentityClaimed: false` and `coverageTrustGranted: false`; it does not prove clipping, blending, interpolation, surrounding composition or target semantic identity, and it creates no `/5` confidence, coverage row, action, catalog admission, placement, document mutation or farther printed-step frontier. The exact real-data Playwright case is deliberately conditional and skips in a clean checkout without the ignored booklet and current outputs; generic association and mutation controls remain part of the ordinary unit gate.

**Visual evidence:** A fresh headless HTML-report run regenerated all four attached crops. The page-18 source and page-20 target both visibly show the same black 1 × 2 plate, while the legacy and current `35480` crops both visibly show the same rounded gray 1 × 2 plate with two open studs and differ only in surrounding ground. The images, report and test output remain ignored task evidence and are deleted after review rather than promoted to Git.

**Validation:** The focused source-art controls pass 15/15, the exact source-attestation suite passes 8/8, and the conditional real browser case passes 1/1. The current source closure contains 3,588 files at `sha256:60f54cb3c54a2fa457b2734100c1572b57fc5276e434ca86c71b13baee3a8d09`. The authoritative `npm run verify` gate passes all 500 Python tests with 31 intentional skips, 360 Vitest files plus two skipped files with 3,093 passing tests and five skips, 53 headless Playwright cases with 11 intentional opt-in skips across 64 declared, and the production bundle guard. Both full-lockfile and runtime audits report zero vulnerabilities.

**Independent review corrections:** The harsh critic reproduced a false association by holding the genuine page-18 label fixed while redirecting the primitive measurement to unrelated operator 39 and its caller-supplied rectangle. The primitive measurement remains intentionally geometry-local, but the new compiler now refuses that redirect because its operator and rectangle cannot match the authenticated manifest component and freshly regenerated crop; a coherent operator-plus-rectangle mutation gates the whole failure class. The same review required exact page/step metadata, bound PNG summaries, honest clean-checkout conditionality and field-specific rejection messages. The final binder reports the exact offending row, field, observed value and expected value, while mutations independently gate redirected geometry, page, step, crop digest, fresh component, attempted authority, collection count, missing key, duplicate key and duplicate identity. An initial full verification pass also exposed ordinary source-attestation drift from the new browser fixture, so the exact 3,588-file closure was derived from live bytes and mechanically repinned before the final gate.

## 3040 advances the measured catalog without advancing the booklet frontier

**Timestamp:** 2026-08-24 04:46–07:26 PDT

**Action:** Added official-inventory design `3040`, Slope 45 1 × 2, as a measured catalog identity whose first printed use is step 43 and whose set inventory quantity is six. The exact official root `parts/3040.dat` at `sha256:12c4aaf0642dc49397b2f269a64c9298f1650fce51a97d463f910f81a9d8068e` expands through the `3040b` alias into an 11-file, 13,050-byte closure at `sha256:2770e66191388e6ffc5b85c85782f9dabe63d3e90d36ea60ef876da33a44329a`, producing 178 triangles over 184 stored vertices. The checksum-pinned native Builder revision-F record at `sha256:63ab72a4ff3b2d85b58af6586a1592124ab42019a84cb5faef137ee699836b28` contributes two Custom2DField nodes only through the reviewed proper turn-0 frame with translation `[0,24,0]`, binary/review digest `sha256:17afd7907052b6e3e78343a6d26af45c81b7d277d80128b35e9f02c483905075`, and frame witness `sha256:65d6be01240cad2790e9fb54fabb056b99c232c26736b33b7340f8a85511a4bf`. Official LDraw ancestry authors the one top stud; Builder exclusively authors underside clutches at catalog coordinates `[0,12,-10]` and `[0,12,10]`; `p/stud3a.dat` remains the diagnostic `clutchTube` its source title declares and never becomes a visible stud or female connector authority. The generated collision field contains 67 conservative body boxes plus one source-radius stud cylinder, and the fixed 373,258-sample scorecard reports composite 0.9856779517112434 with zero outside samples and zero hard failures. Ordinary collision retains the exact measured stud radius, while validated stud/tube edges use the independently admitted nominal 6 LDU source-rounding profile.

**Result:** Catalog truth advances `/20` → `/21`, 92 → 93 definitions, 31 → 32 mesh-backed parts, and 15 → 16 fully measured mesh definitions; the parametric and render-only counts remain 61 and 16. The deterministic required-leaf intersection advances 14 → 15 of 121 identities at `sha256:fddf845fbe1b91beacc1469483424777860316ce56266bbfcc56937ff60253b8`. That catalog fact does not repair the absent authentic manifest-v6 `/5` answers, unresolved step-26 Builder identity contradiction, or unresolved step-28 `3245` family, so neither the action ledger nor any verified printed-step frontier moves.

**Truth and migration:** Current overall truth is `sha256:44044c90de3bb380f32c26db561bad1bd0f247c22ea35c54d75aa5ec6ef8f9a1`, with catalog `sha256:2e7bed932f81ae85af63d689924f66161ece3d3e12d3520d3839727054a8a73d`, connector `sha256:be31f7dc69941b200254ecea0e2e81af60954a2edb79790eb64ad5eba9bf354b`, collision `sha256:b953a7541a50fd1b32fb255356d760134c61c180f77c6059cbcdb42c9cecada1`, transform `sha256:2f1dd7d46273c829e7990f0e28a091ca68c0335089f785442088746ae23f10af`, and unchanged validator `sha256:fb0676931eb66a0096f393794d0be1297227811a77b986c0a1d05847ee3127d4`. The explicit `/20` → `/21` migration reviews all 22 historical truths and refuses a forged future document carrying 3040 beneath an older catalog claim. Its current connector projection contains 2,269 endpoints at `sha256:f84f1e0ec3e1d628b1bd49d869e6b9f3dbdb9d95be87b2fa81b396a566462d2b`; the new `/20` source row has empty endpoint and reachable-pair deltas, while the three new 3040 endpoints belong only to current truth and the three reachable pairs remain unchanged.

**Generated layout and source accounting:** The measured-table emitter preserves measured D as exactly the prior `2877` admission and appends only `3040` to new measured E mesh and blueprint shards. The source-pilot validation, TypeScript-literal emission, measured geometry, emission tests, plan tests, and shared test fixtures now live in focused modules; every touched Python source and test file is below 500 physical lines, and the direct measured-table test entrypoint gates that boundary. The source pilot now covers nine official roots with eight retained Builder records, 14,663 bytes at `sha256:a52e927a7901ab7fbcc680d7be14d397326f23bfa014e9b8c02aeceb5eeb1018`, and an 82-file official-source union; the exact Builder/LDraw frame report is 76,611 bytes at `sha256:a0efc10d4756f48ac5d4004dc4c812720121a10b2264e61394a509c7d6128afc`. The current geometry roster covers 195 unique LDraw files for 32 roots at `sha256:eb7badd407c2ff7473a0212fb8ddca7a93e0b22e152c4883a04e92c61f39496f`; 194 are CC BY 4.0 and one is dual-licensed, with 28 credited authors. The exact current source attestation covers 3,586 files at `sha256:335ffbc851f062a8e70451cd592a646b993229ddc9e6415993406b514f38b254`.

**Visual evidence:** The post-repair one-part `/21` admission packet is `sha256:ea184956f0dc3138f861c1c1775d33392255f1a71ff22985f71e6f4b42867846`; its capture batch is `sha256:46b3900a1fe08c89552dc53ab708e9b3ec9a6ddb6c998ee5f539eaac1d6c99fe`, native-pair manifest `sha256:f39ab61dcb849da1575ac138e0b76f47dfc71d24938f3b36fdf3288c9ff726dd`, review `sha256:c4b45d6c6dac46585eadd9385c2972f4e93df7cccf0a926472536ea6ea533fea`, and review batch `sha256:530b3191de665f2de9df055de64429138149dd1243e719bc7dec927d4e5e2916`. All eight original 640 × 640 source/catalog pairs were inspected at native size and reviewed `same`: seven are RGBA-exact, while isometric differs in one pixel by one channel value, with foreground IoU 1 and mean absolute RGB delta `8.138020833333334e-7`. The matched renderer applies the reviewed recipe frame to both source and candidate, so the exact source triangles, frame witness, role separation, connector route, collision sampling, and real-editor pass carry the non-pixel claims; no hidden-interior, exact-collision, clutch-fit, or stability claim is made.

**Editor evidence:** A separate fresh headless Playwright pass after the connection-profile repair used the real catalog search for `3040`, selected `Slope 45 1 x 2` in the Slopes family, and clicked `Place at origin`. The resulting one-part document is globally valid at `sha256:752fc4da4bf61139d24d14743443a4b8d5fd3cbbe4f21f1986e8d13a8d2f50a0` under current truth; the ignored 123,501-byte full-editor screenshot is `sha256:cb9e123e4979cf3728dc880a28e584f4ed0fe28a7333ff0c8d1322e35c5c77f4`. The whole editor, filtered palette, inspector, and canonical isometric, front, back, left, right, top, and underside views were inspected at original resolution, with a coherent slope, top stud, open underside tube, and walls; the non-visible female connector cells remain Builder-derived evidence, not a pixel claim. The retained audit records empty console- and page-error arrays.

**Gate-3 boundary:** The active reviewed-additive successor and production parent-migration contract now bind `/21` and exactly eight post-`/13` definitions. A required parent-only browser run reconstructed and retained all four current parents under the new truth. The retained 14,172-row long-browser denominator remains historical `/15` evidence; no `/16` through `/21` long rerun, score rebinding, production frontier, or document publication is claimed. A local input-chain regeneration refreshed only deterministic Builder calibration and then refused at the absent ignored `output/part-identification/answers-claude-opus-5.json`; no external model was invoked and the stale action ledger was not presented as current.

**Validation:** Focused 3040 catalog and real-connection coverage passes 10/10, migration and immutable-history coverage passes 65/65, the focused Gate-3, legacy, and migration cluster passes 111/111, the synthetic Gate-3 hardening cluster passes 27/27, current-source attestation passes 8/8, the required parent-only Playwright case passes 1/1, and the exact measured emitter reproduces all 32 admitted roots with zero hard failures. The authoritative `npm run verify` gate passes all 500 Python tests with 31 intentional skips, 357 Vitest files plus two skipped files with 3,078 passing tests and five skips, 52 headless Playwright cases with 11 intentional opt-in skips across 63 declared, and the production bundle guard. The part-standard audit reports no violations across all 93 definitions, the dependency and geometry-notice checks pass, and both full-lockfile and runtime audits report zero vulnerabilities. Raw archives, Builder records, visual captures, reports, traces, screenshots, and source paths remain ignored and uncommitted.

**Independent review corrections:** The harsh code critic proved that 3040's exact source-radius top stud collided with a plate body even when an exact connector edge was declared, a usability failure the structural catalog test did not exercise. The repaired plan independently declares the established `nominal-stud-tube/1` validated-connection profile while preserving the exact radius for ordinary collision, and a new end-to-end kernel gate now proves the disconnected overlap control fails while the connected top stud and both underside clutch seats validate. The same review caught a formatting failure, split the first Python admission-test overflow, and corrected the pilot's Builder-record count. The documentation critic separated the diagnostic `clutchTube` role from female-connector authority, removed an unsupported pixel claim about hidden connector cells, and required the fresh editor audit to retain `pageErrors: []`. A final repair audit then found four touched Python files still above the preferred 500-line focus limit; cohesive source-pilot validation, TypeScript literals, measured geometry, emission tests, plan tests, and fixtures were extracted behind compatibility imports, and a new boundary regression keeps every touched Python file below 500. Each correction is now an executable gate or a recheckable evidence statement.

## 2877 advances the measured catalog without advancing the booklet frontier

**Timestamp:** 2026-08-24 03:04–04:46 PDT

**Action:** Added official-inventory design `2877`, Brick 1 × 2 with Grille, as a measured catalog identity whose first printed use is step 35 and whose set inventory quantity is 16. The exact official root `parts/2877.dat` at `sha256:46c3a0dc6329d3f0be9b3b4485b5e2e41295bad750a0f4c4b6a2e6631c0c0029` expands through a seven-file, 12,845-byte closure into 264 triangles over 375 stored vertices. The checksum-pinned native Builder revision-E record contributes two Custom2DField nodes only through the reviewed turn-180 frame with translation `[10,24,0]`, witness `sha256:234d34ebbabe20f9d2a4b351b31ed2922356b63c7a0bd01203b2bb22ccbff78d`; its 0.291634 LDU mean shell distance beats the opposite mirror class's 0.763225 by 2.617×. Official LDraw stud ancestry exclusively authors the two top studs, Builder exclusively authors the two underside clutches, and the diagnostic LDraw underside tube is not relabelled as clutch truth. The generated conservative collision field contains 26 boxes plus two source-radius stud cylinders, and the fixed scorecard reports 0.9754544869364302 with zero hard failures.

**Result:** Catalog truth advances `/19` → `/20`, 91 → 92 definitions, 30 → 31 mesh-backed parts, and 14 → 15 fully measured mesh definitions; the parametric and render-only counts remain 61 and 16. The deterministic required-leaf intersection advances 13 → 14 of 121 identities at `sha256:48df99ab6b39a41cc95a1dc70ea3a584ee21bd9ddfcea272c6a8eb42ad5d047d`. That catalog fact does not repair the absent authentic manifest-v6 `/5` answers, unresolved step-26 Builder identity contradiction, or unresolved step-28 `3245` family, so neither the action ledger nor any verified printed-step frontier moves.

**Truth and migration:** Current overall truth is `sha256:9c4c32efcaf9bc5f2a251e77188134075f58ca536c6da6148e34b93419d84ad2`, with catalog `sha256:343846c404bce8b33127724f77fc64b7b2f260ea921b2db9a9c8fe38b1929347`, connector `sha256:6306333aaebbc66453d8a27c88c3e632e503fa183cebaa62f29717d3b651a554`, collision `sha256:6a11a85b4b17be5c93f16f2b36b0a8deab0fb8c33f3a97626c4b5ab76aa69534`, transform `sha256:205d3e0461e6d778b9108d45c4927352ae5923be47288306b8e731b1fcef0a5c`, and unchanged validator `sha256:fb0676931eb66a0096f393794d0be1297227811a77b986c0a1d05847ee3127d4`. The explicit `/19` → `/20` migration reviews all 21 historical truths. Its current connector projection contains 2,266 endpoints at `sha256:863b65218ceb2522510b7bf2e52f4cd9749c7b87fc5979499b9ef191523f6799`; the new `/19` source row has empty endpoint and reachable-pair deltas, while earlier rows retain the already reviewed 22 endpoint deltas across four source truths and zero reachable-pair deltas.

**Generated layout:** Appending `2877` to the original measured blueprint module first raised it to 1,021 lines and correctly failed the repository's 1,000-line hard ceiling. The emitter now preserves every preceding generated definition in its original shard and places only `2877` in append-only measured D mesh and blueprint shards. The measured-table gate pins the preceding C/main cutoff at `41682`, pins D to exactly `2877`, and keeps every generated module below the ceiling; the reproducible check report is `sha256:792315b6b1d214b4308ed26a3453790fc6d7955c0891b5349b5676b5e330e048`.

**Visual evidence:** The one-part `/20` admission packet is `sha256:d1d13723c8ed7bd9d5e213de6c489a046bb17e25c68e07e5bdae35733e096794`; its capture batch is `sha256:805efd95b5792c420e44c53cb95d5eb4f124c71888984d1660862176fb34bb63`, native-pair manifest `sha256:1fa91b2fc77086065786645613dc7a880a0773c3d843db531a18707492b69d04`, review `sha256:27c234910da41cfe16301b3ac997d831201b4b9b74ccf8f7f7cb35d43ef9b2c6`, and review batch `sha256:e482d05f9a77a132c4df8916eb89aaf617d0d3b766d7ec0fe532e11ef94aa33e`. All eight original 640 × 640 source/catalog pairs were inspected at native size and reviewed `same`: seven are RGBA-exact, while underside-oblique differs in one pixel by one channel value, with foreground IoU 1 and mean absolute RGB delta `8.138020833333334e-7`. The oblique underside exposes the open bottom, centre tube, walls, and ribs; the direct top and underside editor silhouettes do not independently expose hidden material, so no hidden-interior, exact-collision, clutch-fit, or stability claim is made.

**Editor evidence:** A separate fresh headless Playwright pass used the real catalog search, selected `Brick 1 x 2 with Grille`, and clicked `Place at origin`. The resulting one-part document is globally valid at `sha256:c692331b132f52a4aaee3934dc88ad7dd99e7478862a1800b432fdb019c7b76f`; the ignored 194,264-byte full-editor screenshot is `sha256:3346e445323a8ae300dd75fc3f308ff64cf1e807a12e35b25421725dcb50bdab`. The whole editor, palette result, inspector, and canonical isometric, front, back, left, right, top, and underside views were inspected, with no console or page errors.

**Gate-3 boundary:** The active reviewed-additive successor and production parent-migration contract now bind `/20` and exactly seven post-`/13` definitions. A required parent-only browser run reconstructed and retained all four current parents under the new truth. The retained 14,172-row long-browser denominator remains historical `/15` evidence; no `/16` through `/20` long rerun, score rebinding, production frontier, or document publication is claimed. An attempted local input-chain regeneration refreshed the Builder calibration but then refused at the absent `output/part-identification/answers-claude-opus-5.json`; no external model was invoked and the stale action ledger was not presented as current.

**Validation:** The eight-part source pilot retains a 13,138-byte measurement-only report at `sha256:95261febe75acd715dffa6f315a56154bd80a7d61c45b9db6e0e2c34d15deffc`, and the exact current source attestation covers 3,581 files at `sha256:84723aa3d0f91a35651819a29f960b26c21d6a05f6116841c475773c518d2747`. The focused catalog, migration, Gate-3 and source suite passes 254/254, and the required parent-only Playwright case passes. The authoritative `npm run verify` gate passes all 495 Python tests with 31 intentional skips, 354 Vitest files plus two skipped files with 3,062 passing tests and five skips, 52 headless Playwright cases with 11 intentional opt-in skips across 63 declared, and the production bundle guard. Both full-lockfile and runtime audits report zero vulnerabilities. Every changed tracked file is below 256 KiB; raw archives, Builder records, visual captures, reports, traces, screenshots, and source paths remain ignored and uncommitted.

**Independent review corrections:** The catalog-side critic required an exact digest over all 26 emitted body-box coordinates rather than trusting only count and spot checks. The generator critic required explicit append-only cutoff assertions proving the old C/main shards end at `41682` and both D shards contain exactly `2877`. The documentation review then corrected an overbroad statement that all historical connector deltas were empty: only the new `/19` → `/20` row is empty, while the four older reviewed source truths retain their 22 endpoint deltas. The final whole-unit critic found that the new migration cases had left `migration.test.ts` at 1,118 lines, above the hard 1,000-line ceiling despite green tests; the immutable history-pin, uniqueness, and roster-binding block now lives in focused `migration-history.test.ts`, leaving the behavior suite at 983 lines and the history suite at 138 with all 49 cases preserved. Those corrections are now executable gates or exact source statements rather than review-only observations.

## 41682 advances the measured catalog without advancing the booklet frontier

**Timestamp:** 2026-08-24 01:39–03:04 PDT

**Action:** Added official-inventory design `41682`, Bracket 2 × 2 with 1 × 2 Vertical Studs, as the first catalog identity whose printed use is step 40. The retained official LDraw root and thirteen-file dependency closure contain 336 triangles and 399 stored vertices in the exact catalog frame `[0, 6, 0]`; the generated collision field has 54 conservative boxes plus the two source-radius side-stud cylinders, and the exact LDCad shadow contributes four exclusive underside clutches at the source-authored `y=14` plane. Builder carries no `41682` record, so no Builder-derived connector or identity claim was invented.

**Result:** Catalog truth advances `/18` → `/19`, 90 → 91 definitions, 29 → 30 mesh-backed parts, and 13 → 14 fully measured mesh parts; the parametric and render-only counts remain 61 and 16. The fixed scorecard reports 0.9762273815018168 with zero hard failures. The measured required-leaf intersection advances 12 → 13 of 121 identities with digest `sha256:e5f3931d7bc1908df68dcae51df9c044bef6e8a72a216abbe3df6aa087706452`, but this identity alone cannot cross the unresolved earlier printed steps, so neither the action ledger nor a printed-step frontier moves.

**Truth and migration:** Current overall truth is `sha256:e34fcc8ac627f0dcfdb1d779246a723101d765f931830a4c06514d9daff75c26`, with catalog `sha256:90eae14b0755f6c2b9d5515f4e5db53966d938b5d9867ee1aed90b09ea247016`, connector `sha256:03ccce5b7d3ad14c6b9c9749abb3a806139ade4f786919d54e989ca1a14c6750`, collision `sha256:911806345cb509dad7c3b0c923f8d87364c66e71627ffec9bd934b4df344f3fd`, and transform `sha256:d2888660cff26c2f5665e76c02fecc532b3a04aada1810695495230eb5f664d9`; validator truth is unchanged. The explicit `/18` → `/19` migration reviews all 20 historical truths and projects 2,262 current endpoints with digest `sha256:4224f8ca202557d357bd4c7a94707fc9d3e58617e2d87a8e8e16059d516a58ba`, 22 endpoint deltas, and zero pair deltas rather than silently reinterpreting an older document.

**Visual evidence:** The one-part `/19` admission packet is `sha256:6265de5e960fdb4d9b839ebe1f36a046f6f8b95626965ffbda1a7dc50fcfb7ec`; its capture batch is `sha256:d7005fc30dd7fa84acadb64012efaa1c8023f6af8720f8bdf0f5e746dd680d18`, native-pair manifest `sha256:b382da75b01ea732d79a303c706dbba30d29fdabc5d9f46906a9bca322871320`, review `sha256:9b5e29a382f5fa408bd94f1e40e9497358c3ec465d4c8e4d6fad6e7802a0373e`, and review batch `sha256:6860016197eb346118fd039df36ec4086c58d9f46cbe36510535c2dc2e4df922`; all eight source/catalog pairs are exact RGBA matches. A separate fresh headless Playwright pass used the real catalog search, selection, and Place at origin controls, then inspected the whole editor and all seven canonical views. Its ignored full-editor screenshot is 148,469 bytes at `sha256:64f8b4e5f73f81d5c2ec20d16d310d4c6ae58e07421adc3dbac9be56aea88469`; the seven-view contact sheet is 153,499 bytes at `sha256:ac7b79b35113deda722d30dfc5581a20c1758fb3ab453ee6c689ac7b69e517ca`. The saved one-part document is globally valid at `sha256:8483c7c80a9807413a22cbbdadc09399fbccac9a035a448cbfb0ff635077810a`; its T-profile, paired side studs, top openings, wall thickness, and underside structure remain coherent across isometric, front, back, left, right, top, and underside views after an explicit time advance. The console's only error is the development-only missing favicon, alongside the existing Three.js shader precision and shadow-map deprecation warnings.

**Validation:** Focused migration coverage is 66/66 green, focused source-attestation and Builder-calibration coverage is 15/15 green, the focused Gate-3 application suite is 59/59 green, and the production parent-only Playwright check retains all four current `/19` parents. The final exact source attestation covers 3,577 files at `sha256:96fef656ab8439b0b5315a656fcc5e433a0426962785b25b392953b3ed0bb5db`. The authoritative `npm run verify` gate passes all 494 Python tests with 31 intentional skips, 352 Vitest files plus two skipped files with 3,051 passing tests and five skips, 52 headless Playwright cases with 11 intentional opt-in skips across 63 declared, and the production bundle guard. Both full-lockfile and runtime audits report zero vulnerabilities. The only staged file above 256 KiB is the 547,240-byte generated `mesh-assets-6651557-measured-c.ts`; it is the reproducible canonical TypeScript source for the admitted mesh registry and remains below the 1 MiB text ceiling. The retained `/15` long-browser denominator and historical all-29 `/18` visual tranche were not rebound or rerun; this session captures only the new 41682 packet under `/19`, so the documents preserve that evidence boundary instead of implying a current-truth recapture of every predecessor.

**Independent review corrections:** The harsh final critic reproduced nine stale catalog-test failures that the focused admission suite had missed: four predecessor tests still asserted 29 closures and 189 sources, included the newly appended 41682 in historical payload snapshots, and one still treated `/18` as live. Those gates now use current 30/190 totals while taking fixed append-only historical slices and restoring `/19` labels to their source truth; 23 catalog files pass 175/175 after the repair. The same review removed a false 60 → 61 parametric delta, corrected a migration-test comment from six to eight LDCad-owned measured routes, split seven source-pilot exact-bound fixtures from 41682's later emission evidence, replaced the missing-part error's Builder-only recipe with source-neutral requirements for an exact LDraw closure, reviewed source-to-catalog frame, connector authority, and collision decomposition, and corrected the BOM's mistaken use of lattice phase 10 as the two side studs' raw coordinates. Focused exact-LDU, element-resolution, and migration coverage then passes 76/76.

## 3245 revision M has a geometric best fit, not an admissible identity

**Timestamp:** 2026-08-24 01:08–01:39 PDT

**Action:** Added a separate quarantine-only variant controller for the exact retained 85,098-byte Builder `3245;M` bundle. It copies the already reviewed thirteen-distribution parser environment into two fresh private roots, runs each decoder under the existing Windows Job containment, requires byte-identical canonical Shell output, applies the pre-existing `lego-builder-native-to-catalog-ldu/1` proper basis before exterior extent-and-centre registration leaves the two residual 180-degree-symmetric frames, and measures every official `3245a`, `3245b`, and `3245c` surface in both directions over a fixed 8-LDU interior sample population. Mirrored registrations are excluded by that independent native-frame contract, not by the rectangular exterior. The existing discovery path still refuses the bundle because it exposes zero primitive XML assets; its assertion was not moved or weakened.

**Result:** Both isolated decodes reproduce Shell path ID `28275397554061611`, 120 vertices, 76 triangles, and worker-report digest `sha256:4ee214cd48cfbabcab9a4513ca40f6e2dd5632dfe4034bfdf8a616a995ffb811`. The two residual frames give identical scores. `parts/3245c.dat` is the observed best fit at 0.391277 LDU topology-weighted symmetric interior RMS, followed by `parts/3245b.dat` at 1.081863 and `parts/3245a.dat` at 1.706129; its candidate-discriminative samples against a and b land 0.032275 and 0.035755 LDU RMS from the Builder Shell, while the rejected candidates' corresponding distinctive populations land 1.966934–2.863372 LDU away. The fixed report nevertheless says `verdict=unresolved`, `supported=false`, and `catalogAdmitted=false`: the contract declared no numerical decisive margin before these target scores were observed; the expected Builder-manifest MD5 `a679d0929e777a86573469a63ce841dd` still contradicts the independently bound response body and ETag MD5 `bdce3745e99adf9c3bfb0708161c6875`; and the checksum-pinned derived LDR's ten `3245b.dat` references remain authority-free because neither its converter nor its revision-M alias table is retained.

**Reasoning:** Shell vertex-to-candidate distance alone ranked c but could not distinguish a from b, because adding a surface can only reduce that one-way distance. The released measurement therefore samples both the decoded Shell and each candidate surface, computes both directions, and separately reports candidate points farther than 0.5 LDU from each alternative. Four independent synthetic controls prove the instrument ignores one rectangle's diagonal flip, detects equal-exterior interior-plane shifts in both directions, and detects a missing interior surface; they contain no 3245 data and cannot retroactively supply the absent acceptance margin. The deduplicated per-triangle barycentric grid can change population and weighting under more general subdivision, so the exact scores belong only to this fixed topology-weighted instrument and are not claimed tessellation-invariant. Geometry cannot repair source authority, so neither the empirical separation, the primary model's ten `3245;M` rows, nor the derived LDR's conflicting b rows is relabelled as an admission decision.

**Validation:** The focused nine-case Python suite covers the four independent instrument controls, established-native-basis plus shared-body frame census, the limited opposite-diagonal control, two-sided missing-surface detection, bounded one-Shell decoding, count drift, strict unresolved authority and counterevidence schemas, all-three-candidate coverage, and both residual frames. The unchanged fixed live command also binds the checksum-pinned derived LDR sibling of the primary XML, succeeds against the exact dependency snapshot, bundle, official archive, primary model, derived cross-check and capture record, and writes only ignored evidence under `output/real-build/3245-identity/`; raw decoded arrays remain inside deleted controller-owned temporary directories and never enter Git.
