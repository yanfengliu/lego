# Calibration bootstrap isolation — 2026-09-05

Calibration inherited the ordinary `global-setup.ts`, whose static imports discover recipes and load Vite before function-level guards. The operation-selection gate reproduced that wrong entry point. Calibration now selects an eleven-line server-free bootstrap that reads the required source manifest and then asserts its held lock. Other operations keep their prior setup and every operation keeps its prior teardown.

The source-closure walker derives its setup root from the selected operation. Calibration refuses any reached ordinary setup, sample-booklet discovery, Vite lifecycle/configuration, or direct/subpath Vite import; camera-only retains its development-server roots. The walker follows dependencies before refusing them instead of hiding reached paths. The external dependency-byte census remains intact. The [worker's exact six-file handoff](../../../output/calibration-bootstrap-isolation-20260905/e817788cc0b04ebb908d021fb6a07372/HANDOFF.md), SHA-256 `02f50b4fa780d584550cd4dce1e00f23aba1cc5019547f445d91ceb5f21e0939`, binds before/after manifests, task-only diffs, and mutation logs.

Independent exact-code review accepted the repair without material findings. The coordinator independently reran the three focused operation/setup/closure files: 25/25 cases passed under Vitest 4.1.10 in 4.20 seconds, with all six source hashes unchanged before and after. Scoped ESLint, `tsc --noEmit`, and `git diff --check` exited zero. The operation case failed before repair; omitting the new setup's held-lock call, adding a recipe-discovery import, and disabling the calibration Vite guard each made its synthetic control fail. Mutation cleanup restored the exact candidate bytes.

The two live closure cases performed admitted identity-only hashing of the fixed PDF, material roster, implementation and dependencies. They decoded no PDF/material payload, rasterized no panel, scored nothing, and created no browser, server or camera receipt. Complete Stage-1 readiness across browser requests, Node/child reads, output ownership and diagnostic release remains unproved. The separate camera correspondence, exact Poppler recovery, genuine Step-44 selection, later UI replay and full repository gates remain required. Neither accepted successor-protocol bytes nor its review changed.

All invoked test, formatting, lint and compiler commands exited. The worker's Windows process census returned access denied, so no general no-process census is claimed. No browser, GUI, ordinary server, Poppler process or background helper was launched. No commit or push occurred.
