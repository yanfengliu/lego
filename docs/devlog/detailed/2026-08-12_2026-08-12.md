# Devlog — 2026-08-12

The session directly measured the exact deferred step-5 alternatives at the first revealing panel, then rejected the tempting completion claim when an independent semantic audit proved that the coherent eight-piece result was mirrored. It next made the missing horizontal camera determinant executable as a bounded primitive, added panel-local observation contracts, composed them into a pure eight-branch resolver, and finally integrated only the boundaries the runner can honestly enforce: exact empty-root retention and pre-executor fixed-action refusal. Visual-search progress, official-frame equivalence, and canonical output remain three separate claims.

## Browser and artifact verification now close their terminal and historical boundaries

**Timestamp:** 2026-08-12 13:02–14:07

**Action:** Hardened browser-output `/3` around one bounded parse of terminal `documentJson`, independently reconstructed transition-prefix witnesses, accepted hash and part continuity, ordered canonical `BuildStep` semantics, global validity, canonical root metadata and manual provenance. Added exact root-refusal and later blocked-row zero-work policies. Split artifact-manifest `/4` verification from the frozen artifact `/3` inspector, made current publication and verification reject metadata-only and input-rejection closures without a typed digest-bound rejection witness, and moved candidate-only browser-positive tests to explicit frozen `/2` inspection.

**Result:** A genuine validated zero-piece transition advances to the structural hash of its unique empty canonical step while keeping the part count fixed; an absent, duplicate, hostile, unvalidated, or hash-preserving no-op witness is rejected. Root refusal must terminate at the exact canonical empty document, and every later blocked row must name the first failure without validation, candidate, camera, render or document drift. Only downstream browser-output closures are eligible for current artifact `/4`, no current production artifact exists, and the 10:28 artifact `/3` + contract `/2` + browser `/2` + score `/4` run remains immutable inspection-only evidence.

**Reasoning:** A report-level hash can be internally consistent while terminal bytes contain a forged no-op step, extra metadata, reordered ownership, or later work after a refusal. Likewise, a caller-authored metadata-only failure cannot prove which input was rejected or reproduce its score, and a historical artifact ceases to be frozen if its verdict changes whenever current finalizer predicates evolve. Closing each boundary with independently reconstructed or generation-local evidence prevents a newer schema label from manufacturing authority.

**Validation:** Transition regressions cover the genuine empty-step hash advance and forged no-op, absent, unvalidated and hostile witnesses; terminal regressions cover hash, part, validity, ordered step semantics, metadata, provenance, exact action/digest binding, exact root, zero-elapsed blocked suffixes, and pre-placement rendering failures with forged raster evidence while preserving honest measured failure time. Artifact tests cover publication and verification rejection for metadata-only and input-rejection closures, exact current downstream projection, frozen `/2` browser/farther/capture and atomic identity mutations, pinned semantic/prefix revalidation of rehashed connection, membership, region and transform attacks, and a completed deferred-lookahead outcome whose selected decision is forged into a weaker refusal after mutating its scores. Candidate-only farther mutations pass only through the frozen inspector; current `/3` remains at the eight-root step-1 refusal pending lineage cutover. The current source closure contains 3,130 files and reproduces `sha256:4b70ad712a681d3c5733f9af6188d9fe7705b1c4f11ced31a1ffaac6a065fea6`.

**Code reviewer comments:** Independent review found six authority gaps: a hash-preserving zero-piece report could stand in for a real step; final document bytes were not exact-bound to accepted report continuity; blocked rows could retain hidden work; metadata-only rejection summaries could be published without reproducible rejection evidence; legacy completion inspection called evolving current predicates; and a completed deferred placement could delete its selected decision by relabeling retained evidence as a refusal. Each finding now has a fail-closed generation-specific boundary rather than a prose-only warning. The ordered next unit remains stable document `candidateId` plus unique `lineageId` and explicit parent through deferred and farther state; no physical fixed-action mapping was added.

## The live runner retains the camera root and refuses the authority it does not have

**Timestamp:** 2026-08-12 10:26–13:01

**Action:** Cut current generation to run-contract `/3`, browser-output `/3`, result/score `/5`, and artifact-manifest `/4`. The runner now deep-detaches the complete JSON-like run input before asynchronous preparation, including panels, order and page bindings, digests, accounting, coverage, and budgets; re-snapshots the caller input after module and PDF preparation; and refuses any drift before raster, search, or placement. It globally sorts detached printed steps, verifies contiguous order before rasterization, creates the exact canonical empty `real-build` document, and atomically reserves all eight D4 step-0 seeds. Report 1 retains nested panel-camera evidence `/2`; fixed-ledger omissions and `multi-build-copy` call sites enter the opaque physical-authority guard before their executor. Module, page, PDF-document, and loading-task failures use bounded non-invoking diagnostics, page rendering is on demand, and cleanup failures preserve completed reports, document bytes, and input digests in typed failed output.

**Result:** Current execution deliberately refuses printed step 1 before any placement callback because candidate scoring is not carried through the eight immutable root lineages, leaves the canonical document empty, and records every later requested step as causally blocked. The fixed-action paths return `fixed-ledger-frame-unresolved` without reading their executor because no trusted authority producer exists. Panel-camera measurement commits the PDF/page/crop/face, exact retained captures, camera, raster dimensions, mask digests, score, centre, shift, hand, determinant, and turn, but exact mask bytes are absent, so independent recomputation is unavailable and positive-piece completion remains unauthorized.

**Reasoning:** Retaining one scalar camera hand or allowing the legacy placement path to run beside eight uncarried roots would silently discard lineage and recreate the mirror defect under a stronger-looking schema. The executable refusal makes the missing integration visible without rewriting historical evidence or granting a caller-controlled frame decision physical authority.

**Validation:** Focused runner, browser-output, evidence, frontier, fixed-action, replay, contract, and adversarial suites exercised exact root hashing, atomic eight-slot reservation, hostile complete-input detachment and drift, global ordering, nested descriptor/array traps, atomic lineage-continuity mutation, page-render failure, non-invoking diagnostics, cleanup failure, causal blocking, fixed-action executor unreadability, measurement cross-binding, and positive-placement refusal. No new current-generation production run was created. Retained run `2026-08-12T10-28-52-560Z-34694c87c62e-26128a38-4d9e-4294-9091-6aae9b3ca367` remains immutable inspection-only evidence under artifact-manifest `/3`, run-contract `/2`, browser-output `/2`, result/score `/4`, and replay-closure `/3`; its historical scores and hashes are unchanged and verified by the exact legacy inspector.

**Code reviewer comments:** Independent boundary review required the eight roots to bind the kernel-derived canonical empty-document hash, prevented a seeded root from authorizing a structural transition, kept later blocked rows from reopening work, made cleanup failures retain rather than erase evidence, and separated current artifact publication from the frozen legacy manifest. The next unit must split frozen `/2` candidate-only farther parsing from a `/3` lineage-aware substrate, then carry stable `candidateId` plus unique `lineageId` through deferred and farther records while preserving both live refusals.

## Source-attested real-build boundaries gained headroom without changing behavior

**Timestamp:** 2026-08-12 08:14–10:25

**Action:** Extracted direct placement search from `real-build-run.ts`, exact score and diagnostic verification from `real-build-artifacts.ts`, hostile carry/panel/capture/tandem validation from `real-build-farther-report-parser.ts`, and the options plus failure contracts from `real-build-safety.ts`. Public re-exports, validation order, error text, serialized bytes, mutable center updates between pieces, and fail-closed behavior remain unchanged. The source-attestation repin exposed that raw snapshot hashes depended on whichever files happened to be LF or CRLF in the active checkout, so `.gitattributes` now keeps ordinary source extensions at LF while its later `-text` rules continue protecting byte-pinned fixtures and generated inputs.

**Result:** The former 999-, 982-, 987-, and 981-line boundaries are now 803, 630, 403, and 681 lines, with every new focused helper below 500 lines. The calibrated source closure remains 3,087 files, but its digest is now the Git-reproducible LF value `sha256:3e27aa75f98fd27ae2b49379b12213ff09ec932b90102756fec6f3d3c88723f2` rather than the mixed-worktree value `sha256:75827cbf8090043fb7396f74cdcef9fbeb749785f3352217391fac61bdf39c23`.

**Reasoning:** The next lineage and report-schema cutover needs room inside already near-ceiling control files, but extraction cannot be allowed to change the current measured run. A source pin that passes only in the authoring checkout also cannot support reproducible execution: `core.autocrlf=true` translated 114 attested first-party files, so identical Git content produced different raw snapshot digests after checkout. Explicit LF attributes preserve the exact executed bytes while remaining independent of platform defaults.

**Validation:** Independent comparison checked emitted ASTs, public export sets, all 23 moved artifact errors, parser rejection order, direct-placement center closure, import cycles, secrets, and file ceilings. The exact 3,087-path closure measured `sha256:3e27aa75f...` from both normalized working bytes and Git index bytes; focused tests, type checking, lint, formatting, 1,820 unit tests, 33 browser tests, and the production bundle passed under the authoritative repository gate.

**Code reviewer comments:** A precommit reviewer caught the checkout-dependent pin after all ordinary gates were green: the mixed worktree matched `75827cbf...`, the index matched `3e27aa75...`, and a simulated fresh Windows checkout produced a third digest. The corrected checkout contract closes that portability gap before the extraction commit, so no failing intermediate pin enters history.

## The pure resolver retains eight lineages while fixed actions fail closed

**Timestamp:** 2026-08-12 07:26–08:13

**Action:** Added `resolveRealBuildPanelCameraBranches` around the existing hand measurement and observation contracts. It detaches, canonicalizes, deeply freezes, and rehashes the candidate document; snapshots built and excluded masks; checks shared-ledger state around hash and render callbacks; requires the registering panel to be strictly later than the prefix; and reserves all eight angular hypotheses atomically before any seed or render. A step-0 empty root yields eight unregistered seeds with null observation and shift, while a nonempty prefix retains all attempts and successful observations under one hash-bound `candidateId`, unique `lineageId` values, and an explicit `parentLineageId`. Added a separate physical-frame guard for fixed-ledger omissions and `multi-build-copy` actions.

**Result:** Over-budget input admits no seed or observation and runs no renderer. A later observation does not rekey the document candidate, a cross-hand tie stays unresolved, and a thrown or malformed render retains the completed counterevidence but selects nothing. Every resolution explicitly reports that silhouette is not physical-transform authority. The fixed-action seam returns typed `fixed-ledger-frame-unresolved` for every current claim—including panel-camera evidence and a well-shaped caller-supplied proper identity decision—and never reads its executor because no trusted authority producer exists.

**Reasoning:** Step 1 has no built silhouette from which to derive a translation, so representing its eight angular possibilities as unregistered roots avoids inventing `[0,0]` evidence. Reserving the complete family before work prevents a budget edge from privileging enumeration order, and hash-bound candidate identity plus separate child and parent lineage lets N+1 and K append observations without rewriting history. A caller-controlled decision object cannot certify the physical frame needed to transform fixed official actions; that capability must come directly from a future independent physical audit.

**Validation:** Focused resolver tests cover empty-root seeding, nonempty observations, stable candidate identity across different later panels, unique lineage, exact cross-hand tie refusal, pre-render atomic budget refusal, thrown and malformed renderer results, backward-panel rejection, document-hash mismatch, ledger poisoning, and input bounds. Fixed-action tests cover missing, panel-local, reflected, unresolved, wrong-source, and self-certified proper claims and prove that document hashing, part lookup, placement, assessment, and the executor property remain untouched. Type checking, lint, formatting, and the full repository gate are recorded with the commit rather than as evidence that the runner consumed either seam.

**Code reviewer comments:** Independent review of the fixed-action boundary found no path for current caller data to mint authority and confirmed that the executor is unread. The resolver deliberately remains outside the runner, report, scoring, and farther-lineage schemas, so this unit changes no retained run and does not close the 359-step reconstruction frontier.

## Panel-camera observations retain candidate identity without claiming a physical frame

**Timestamp:** 2026-08-12 05:48–07:25

**Action:** Replaced the overbroad frame value with `RealBuildPanelCameraRegistration` for `{latticeHand,latticeDeterminant,registrationPanelStepNumber,turnDegrees,shiftPx}`. Construction accepts only the two matched hand/determinant pairs, four quarter turns, a positive registering panel, and an exact safe-integer pixel pair, then snapshots, normalizes negative zero, and deeply freezes the copy. A stable `candidateId` remains the document-node identity, while `realBuildPanelCameraObservationId` separately binds step, lowercase document digest, registering panel, hand, determinant, turn, and shift. Panel-arrow evidence snapshots the face-corrected fit and raw displacement under the same panel, derives the q0 family once, and applies an exact integer D4 transform in original order without changing travel or off-line tolerances. Branch admission detaches and structurally hashes every document-camera pair before one atomic pair-budget reservation, retains exact cross-hand ties, and returns hand unresolved.

**Result:** One document node keeps the same candidate identity across N, N+1, and K while every panel-local registration has a distinct observation identity, so later evidence appends without rekeying lineage. All eight hand-and-turn observations remain distinct, q0 order and tolerance semantics survive D4 transformation, an over-budget pair batch admits nothing, and binary silhouette never selects a physical hand.

**Reasoning:** Pixel shift and silhouette hand have meaning only in the panel that measured them, so folding them into candidate identity incorrectly rekeys one immutable document node at each later observation and risks laundering raster alignment into world-transform authority. Deriving q0 separately under each transformed view also changes threshold behavior near a tolerance boundary; one q0 derivation followed by exact D4 preserves the measured family. This unit remains additive: the current runner, scorer, reports, farther branch state, and fixed actions do not call it, and fixed-ledger or `multi-build-copy` mapping requires separate physical-frame authority.

**Validation:** Focused tests cover all eight exact view and arrow mappings, stable candidate identity across distinct panel observations, panel binding, deep freezing and copy isolation, negative-zero canonicalization, malformed keys, mismatched determinant, invalid turns, hashes, shifts, views and arrow measurements, dense ordered q0 output, the 200-row family bound, empty-mask parity, document-hash verification, callback mutation, cross-hand ties, and atomic accepted and refused pair budgets. These tests exercise only pure prerequisites and do not rerun or reinterpret the printed booklet.

**Code reviewer comments:** Independent harsh and algebraic reviews caught the original physical-frame name, registration-qualified candidate identity, a q0 recomputation that could cross tolerance boundaries under transformed views, caller-document hashes that were trusted rather than recomputed, callback mutation between rows, and non-panel-bound arrow facts. The corrected boundary names its raster-only authority, separates candidate and observation identity, snapshots all inputs before callbacks, hashes detached documents before reservation, derives q0 once, and leaves hand unresolved; runner integration remains a later reviewed unit.

## Camera handedness became an explicit eight-hypothesis measurement

**Timestamp:** 2026-08-12 05:30–05:47

**Action:** Added the pure `LatticeHand` state and `viewForLatticeHand` transform for a face-corrected view, where x reflection maps `(A,e)` to `(180-A,-e)` without changing model-up. Added `anchorStepCameraLatticeFrame`, which applies each quarter turn before the hand transform, registers all four as-fitted and four x-reflected silhouettes with a separate translation, ranks all eight attempts, and refuses an exact best-score tie spanning both determinants as `camera-handedness-unresolved`.

**Result:** The chiral three-part fixture has one exact winner: x-reflected turn 90 at shift `[17,-23]`, IoU 1, while its best as-fitted hypothesis reaches about 0.476. A symmetric 2x4 plate reaches IoU 1 in both hands, retains all eight attempts, and returns the typed cross-hand refusal instead of selecting by enumeration order.

**Reasoning:** The printed square lattice cannot name its horizontal determinant, and treating the fitter's positive representative as physical truth is the precise path that can preserve a coherent mirror. Reflection also reverses depth, so binary silhouette can expose and rank the missing hypothesis but cannot establish RGB, occlusion, or a physical document branch. At this point the unit stopped before `prepareRunStepCamera`, observation IDs, arrow interpretation, fixed-ledger and `multi-build-copy` transforms, or retained lineage consumed the new state; the later prerequisite section above records the additive observation work completed afterward without implying runner integration.

**Validation:** Algebraic projection tests prove that x reflection negates one horizontal basis, preserves the other and model-up, remains distinct from an underside face, and conjugates quarter turns. Camera-registration tests render the chiral and symmetric documents through the production silhouette math, check all eight retained rows and per-row translations, assert the exact winner and typed tie refusal, and reject invalid dimensions and mask lengths before scoring. These tests prove the bounded registration contract only; they do not rerun the printed booklet or close its handedness frontier.

## Deterministic finalization now proves proper target equivalence by complete catalog realization

**Timestamp:** 2026-08-12 03:41–04:47

**Action:** Replaced the one-off semantic comparison with a bounded post-search audit in Node finalization. Only after exact report, identity-binding, canonical-part, metadata, step-ownership, and searched-transform checks pass does it evaluate official transforms under all four proper upright yaws and one global integer-LDU translation, match same-step identities as exact design/material/catalog/color multisets, and compare complete connector, collision, allowance, bounds, and flat render triangle-and-normal realization modulo catalog-proved upright self-symmetry. Official transforms remain evaluator-only and never enter enumeration or scoring.

**Result:** The measured candidate has one proper yaw-90, zero-translation frame through step 2; the frontier first empties at design `6106` on printed step 3. A diagnostic x-reflection maps all eight origins, their connector/collision/allowance/bounds layers, and 30 independently inferred compatible contacts, but 0/8 placements retain exact flat render triangle-and-normal topology. Reflection therefore remains a determinant-minus-one diagnosis, never completion: the retained prefix is still `targetEquivalence: "unreconciled"`, and canonical document, structural hash, and part count remain null, null, and zero.

**Reasoning:** Structural validity and compatible contacts can survive reflection, while complete catalog realization under a proper world frame is the acceptance contract. Keeping the physical mirror finding separate from the exact render-topology mismatch preserves useful counterevidence without laundering either into target truth or using the official answer to repair visual search.

**Validation:** Regression fixtures cover the eight exact production identity rows, same-step unordered groups, asymmetric and symmetric orientations, off-origin self-symmetry, unsupported realization refusal, reflected physical layers and inferred contacts, and the first step-3 mismatch. The audit admits only 1..1,464 unique identity/part rows with safe-integer upright transforms and known catalog definitions; an audit-local cache reuses self-symmetry and definition-plus-relative-transform comparisons for repeated parts.

**Code reviewer comments:** The completed audit closes the reproducibility gap in the earlier one-off finding, but it deliberately does not correct handedness or alter the retained run schemas. Fixed-ledger omissions and `multi-build-copy` actions still use raw official transforms because no resolved physical search frame exists; mixing them with searched rows under a non-identity proper frame forms a hybrid that the audit correctly refuses, so the handedness work must establish separate physical-frame authority and map fixed actions through it before the complete 359-step reconstruction can finish.

## Exact attested K selected the second step-5 origin before step 6 exhausted its budget

**Timestamp:** 2026-08-12 02:14–02:26

**Action:** Added a calibrated direct-origin path that can score only the two exact retained step-5 documents against panel 7 after panel 6 remains ambiguous. Admission binds both origin hashes and exact atomic piece witnesses, the prepared step-5/6/7 records, run options, all consumed input digests, and a derived captured-source attestation; the generic farther path still requires its complete intervening carry before conditional K.

**Result:** Live run `2026-08-12T09-14-05-246Z-32668097b507-2989d382-2a93-470c-aadb-14d91107a904` scored the two origins `0.81657223796034` and `0.9367520589707421`, a margin of `0.12017982101040214`, and selected `step-005:sha256:47ae3d353885f5de11b685a4bec4ca1132554a19e1f1e30454281252f7d64c93`. The browser completed five rows and held eight parts; ordinary step-6 aggregate narrowing then refused at the fixed 8,192-render ceiling and step 7 was blocked.

**Reasoning:** The earlier production path spent its entire narrowing allowance constructing step-6 descendants before it could ask the panel-7 question that directly distinguishes the two known step-5 origins. The shortcut is legitimate only as a frozen calibration over exactly those inputs and source bytes; making it generic, using official transforms in enumeration, or relaxing the budget would change the question rather than answer it.

**Validation:** The retained source panel and both exact candidate renders were inspected together, and the selected candidate image matches the revealing underside relation better than the rejected alternative. This first run published artifact-manifest schema `/2` and score schema `/3`; it remains immutable measurement history but is superseded for current verification by the `/3` and `/4` replacement recorded below.

**Code reviewer comments:** Independent review required the calibrated policy to fail closed over its complete data and source closure, prevented the official ledger from entering the scorer, and kept K lazy outside the exact shortcut. The resulting source attestation is a condition of the shortcut, not evidence that its visual answer is canonical target truth.

## A structurally valid eight-part prefix was a mirror, not the official target

**Timestamp:** 2026-08-12 02:27–03:15

**Action:** Audited the retained candidate once against official semantic placements under every proper upright quarter turn plus translation, checked the contact graph and reflected positions, and separated unreconciled diagnostic retention from canonical completion. Node finalization now emits `diagnostic-prefix.json` when candidate integrity survives but searched transforms differ from the raw official ledger and no independently proved proper mapping is retained; canonical document, hash, and part count remain absent, null, and zero. The reusable production implementation landed later in this session and is recorded above.

**Result:** Steps 1 and 2 have one proper instance-level fit at yaw 90 degrees with zero translation. Step 3 design `6106` is the first placement that no proper upright mapping can reconcile, and all eight retained positions plus their contact graph equal the official prefix under global x reflection. A separate rendered-mesh reflection check finds non-invariance already by step 2. The honest frontier is therefore five visually searched steps and eight structurally valid diagnostic parts, but only two official proper-frame-equivalent steps and no canonical completion.

**Reasoning:** A reflection preserves distances, collisions, and the contact graph, so structural validation cannot distinguish it from the intended hand. It is not a free world rotation, and using the official answer to repair the searched document after selection would hide the defect rather than close the visual loop. The old Builder basis incident belongs to the same handedness class but is not the cause established here: the current official input and searched candidate each survive their own bindings, while the unresolved ambiguity lies in the shared visual/search camera-world frame.

**Validation:** The semantic audit exhaustively tested the four proper upright yaws and translation, identified the unique step-1/2 fit and the exact step-3 failure, and reproduced the eight-part reflected position/contact relation. The replacement run below proves score schema `/4`, artifact-manifest schema `/3`, diagnostic-document verification, and downstream re-finalization bind the candidate as `targetEquivalence: "unreconciled"` without allowing it into canonical fields.

**Code reviewer comments:** The initial finalizer interpretation would have treated an official-frame mismatch as a tolerable completion failure while still publishing the searched document as canonical. The reviewer showed that this launders a mirror into target truth. The fix makes the failure class executable and separately names the diagnostic artifact, so no new prose-only lesson was added beside the regression boundary.

## Replacement publication closes diagnostic replay and preserves the next refusal

**Timestamp:** 2026-08-12 03:28–03:40

**Action:** Re-ran the exact production request after adding the diagnostic-prefix, score, artifact-manifest, and replay-closure bindings, then published the verified replacement without deleting or rewriting the earlier run.

**Result:** Run `2026-08-12T10-28-52-560Z-34694c87c62e-26128a38-4d9e-4294-9091-6aae9b3ca367` reproduced the two direct-K scores and selected hash exactly. Its diagnostic document has five contiguous steps, eight parts, 30 connections, structural hash `sha256:47ae3d353885f5de11b685a4bec4ca1132554a19e1f1e30454281252f7d64c93`, and `targetEquivalence: "unreconciled"`; canonical document, structural hash, and final part count remain null, null, and zero. Downstream, step 6 retained four unseparated origins; the step-7 carry expanded three parents for 2,218, 2,169, and 3,650 narrowing renders, retained eight immutable lineages at 8,037, and refused the next 599-render request before work because 8,037 + 599 exceeds 8,192. No partial frontier was admitted.

**Reasoning:** Supersession changes which immutable record current verification points at; it does not mutate the historical predecessor. Reproducing the direct measurement under the stronger schemas separates the stable visual result from the new evidence contract, while replaying the later budget refusal proves that retaining the diagnostic prefix did not quietly admit a step-7 branch.

**Validation:** The run uses artifact-manifest `/3`, score `/4`, and replay-closure `/3`. `current.json` binds replay digest `sha256:1c27df8a95c655f7508436489e8e31f486f806c7a5382df76d53e0a80801a66c` and artifact-manifest digest `sha256:4dc5ce021e03ba0bc86667a64a1948f9ad54d2d80f95eb887b559a593c7036aa`; the score file is `sha256:cbdf5b5502448011356b7fdb15f655734e97021853a45f41d64f63fd3f9e042e`, and `diagnostic-prefix.json` is `sha256:2edf84fbf1eab57e86cd2670f9bdb5e60a7ac33dbda454f22d9c9a85cbf8b70f`. The immutable manifest `/2`, score `/3` run `2026-08-12T09-14-05-246Z-32668097b507-2989d382-2a93-470c-aadb-14d91107a904` remains the historical predecessor and is superseded only for current verification.

**Code reviewer comments:** The replacement run itself supplied the final proof: the verifier reconstructed the diagnostic bytes from browser output, rebound score and manifest summaries, checked the separate file, and still preserved the exact aggregate refusal. No new issue was found beyond the already recorded handedness and fixed-budget frontiers.
