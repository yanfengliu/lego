# Devlog — 2026-08-21 through 2026-08-22

## Exact multi-root compilation and deterministic branch-role finalization

**Timestamp:** 2026-08-22 02:25–06:51

**Action:** Widened the standalone atomic compiled-branch producer from one canonical parent document to a bounded ordered set of exact root-document groups under one prepared step and one ledger reservation. Added an exact-lineage identity that preserves the generic structural lineage ID while committing its exact parent lineage, canonical-byte digest and UTF-8 length; every proposal now resolves through a private retained parent snapshot and every descendant commits its exact child bytes. Added a pre-reservation serializer/reparser over the budget-refused and three admitted zero-frontier terminal shapes, including maximum-width stable failure fields, so final-wire insufficiency refuses before ledger mutation. Added a transport-only role finalizer that accepts only module-produced batch results, derives a contiguous terminal-respecting prefix, admits aggregate role bytes and the semantic consumer's cumulative lineage, closure, raster and compiler-work policy before raw observation-role copying, rechecks measure/copy digests, verifies optional typed observation bytes, writes dense roles stepwise into private storage and returns only fresh copies. Captured the private-brand, ledger, retained-store, freeze, canonical-hash, JSON, encoder, typed-array and terminal-iteration intrinsics used after hostile descriptors or compiler callbacks can execute. Extended the established prepared-search inspection boundary to retain a complete exact identity, bind it to the exact snapshot bytes and refuse any generic convergent row once one structural candidate has multiple canonical byte bindings.

**Result:** Eight lineages over one exact root still share one physical transition and retain eight parent-specific edges, while two distinct root documents compile independently under one reservation. A structural-hash collision fixture proves two canonically distinct empty documents retain separate root and child byte groups, and swapping either exact parent or exact child identity onto the other's snapshot refuses before reservation. Budget-minus-one and prototype-poisoned terminal sizing leave the ledger at zero and call no compiler; the exact admitted terminal ceiling permits both unique compiler calls and converts the real oversized success envelope into `aggregate-evidence-closure` with no child frontier. A compiler that poisons array iteration or numeric canonicalization and then throws still closes into the pre-admitted failed zero-frontier result. The finalizer retains exact failed and budget-refused terminal bytes without observations, refuses a suffix before inspecting it, and survives mutated aliases plus poisoned JSON, encoder, typed-array, `Function.prototype.call`, `WeakMap`, `Object.freeze` and inherited `toJSON` dispatch while keeping its authority absent and retained roles private. Prepared search now admits exact(A)/exact(B) convergence only while every byte-distinct row stays exact and refuses a later generic(A) row regardless of ordering.

**Reasoning:** The first widening reused the structural document hash as the whole lineage commitment, so two different canonical documents that normalize to one structure could substitute for one another. It also sized only one assumed terminal form, called mutable typed-array and collection prototype methods after hostile input could run, and lacked a genuine successful-compilation path that crossed the final envelope ceiling. The exact-lineage layer was added rather than changing generic lineage `/1`, because existing retained evidence needs its structural compatibility while this positive producer needs byte-exact parent identity. Terminal sizing enumerates all bounded failure forms and the finalizer reuses the already measured semantic work boundary rather than inventing a second budget vocabulary.

**Validation:** `npm run test:browser-branch-semantic` names 21 focused files and passes 176 of 176 tests. The four atomic/writer adversarial files pass 41 of 41, including exact-parent and exact-child substitution, actual fail-late aggregate closure with a fresh successful high-cap control, no-reservation terminal overflow under poisoned array dispatch, real-ledger reservation under poisoned private-store dispatch, exactly-once provenance, deep immutability and private fresh-copy role storage; the canonical primitive suite separately passes 12 of 12. The served headless writer integration passes 2 of 2 browser tests. The exact current source closure is 3,427 files at `sha256:eb97d69089f080f9b18765d2662e796ddbe916b01ae2da34b9d538bdb8eb3593`, reproduced by all eight attestation tests. The authoritative `npm run verify` passes 295 Vitest files with 2,648 tests and three intentional skips, 42 headless browser tests with nine explicit opt-in skips, all 481 Python cases with 31 intentional skips, every schema, Node-consumer, observation, BOM, part, lesson, notice, formatting, lint and type gate, and the production build. `npm run audit` and `npm run audit:runtime` report zero vulnerabilities.

**Code reviewer comments:** An external multi-CLI review found the structural-hash parent-substitution flaw, late-bound typed-array copy, missing actual success-envelope overflow regression, suffix inspection after terminal evidence and absent devlog. Independent post-fix critics then found mutable private provenance/store dispatch, ambient ledger reservation, late-bound freezes, bypassable terminal array iteration, structurally swappable exact children, late-bound numeric canonicalization and role-reader typed getters, weak high-cap and private-store controls, and order-dependent mixed exact/generic prepared-search admission. Those counterexamples became exact parent-and-child canonical lineage commitments, captured runtime intrinsics, a fresh successful overflow control, active forged-store failures and regressions that compare evidence to the actual shared ledger and attempt to mutate every public branded layer. The final exact-lineage, hostile-runtime and proof critics are clean.

**Notes:** This unit changes no rendered surface, source panel, catalog geometry or user document, so visual verification is limited to the served headless integration rather than a model-view sweep. The producer, finalizer and reader remain standalone authority-absent prerequisites; they do not provide browser-output `/4`, source/camera/report/terminal provenance, a live run-session capability, score or acceptance authority, physical proof, completion, or document mutation.

## Aggregate branch work is admitted before semantic replay

**Timestamp:** 2026-08-21 23:10–2026-08-22 02:25

**Action:** Added a refusal-only local consumer for branch-evidence `/1`, prepared-run bytes, compiled lineages, typed observation closures, and raw observation roles. Split hostile lineage wire parsing from semantic replay with two unforgeable work inspections: internally orphaned source/camera rows refuse, then exact root groups/identities, search parents/proposals, child candidates, transitions, edges, witnesses/operations, legacy and typed observations, source/camera tables, selection/accepted-transition reference arrays, root/child canonical-document bytes, and raster work are cumulatively charged before document reconstruction. Each bounded root is then reconstructed once for admission-work measurement to derive and cumulatively charge the compiler's conservative three-pass graph/byte work-policy units, including step-preparation operations, before compiler replay or mask registration; typed observation preflight requires that exact replay-admitted inspection rather than the semantic-only brand. Added full transition printed-step metadata binding, exact prepared proposal binding, pre-replay closure/role binding, legacy selected-state refusal, authority-absent summaries, and `verified` closure language that does not claim an external typed failure was reproduced.

**Result:** The focused semantic wrapper suite grew from 8 to 31 cases. A genuine production-compiler 1+1-step lineage advances step-2 roots from the exact step-1 child bytes and identities and passes with placement and completion authority absent. A genuine 200+100-piece two-step lineage is individually compiler-admissible at both steps but crosses the cumulative 2,000,000 graph-visit policy ceiling; only the first step enters semantic validation. A genuine 100+100-piece pair similarly crosses a lowered test-only byte ceiling before second-step validation. Hostile presemantic controls separately refuse cumulative 8,193 roots, 8,193 child candidates, 8,193 search parents, and 33 operation-admissible 1,022-piece transition rows before later reconstruction or validation, while internally orphaned source/camera tables refuse before root reconstruction. Lowered-limit genuine two-step closures prove cumulative search-parent, source, and camera admission, while exact selected closures gate selected-lineage, accepted-lineage, and accepted-transition reference arrays before reconstruction. The malformed high-cardinality rows test structural admission only and are not described as replay-valid. Direct regressions preserve the established public lineage `/1` parser contract, reject forged structural and replay-work brands, charge terminal failed-compilation attempts, enforce exact and plus-one compiler work limits, and keep both observation preflight paths behind the explicit replay-work gate.

**Reasoning:** The first implementation checked aggregate counts after each lineage parser had already reconstructed documents and replayed every unique transition, so its ceiling could describe excess work without preventing it. After two focused passes exposed that same failure mode, the hard-problem search compared a second structural parse, caller-declared costs, and a branded two-phase parser. The branded split was selected because it measures actual parsed proposal pieces and connections and lets observation preflight reuse only an exact lineage inspection that has already passed replay-work admission and semantic validation. A later adversarial review caught that routing the new aggregate compiler ceiling through the established public `/1` parser would silently reinterpret retained bytes and let the atomic producer compile a whole frontier before losing it; a post-fix critic then caught that the observation helper still accepted the weaker semantic-only brand. The final boundary therefore preserves the public parser's prior semantic contract and applies a distinct replay-admitted brand explicitly on the new semantic and bytes-only observation paths.

**Validation:** `npm run test:browser-branch-semantic` names the exact fifteen-file regression denominator and passes 116 of 116 tests; it includes the compiler, role snapshots/transport, semantic sidecar/preflight/compiler-work/observation-work splits, observation closure/registration, lineage parser/work, and prepared-step authority. Genuine aggregate tests spy the semantic validator and prove an over-budget second step never enters it at the production graph ceiling or at a lowered test-only byte ceiling; snapshot instrumentation proves lowered cumulative search-parent, source, and camera limits and orphan source/camera rows refuse before later root reconstruction; exact selected closures prove every selection/accepted-transition reference array is charged before reconstruction; declared-byte mutation proves structural measurement recomputes actual UTF-8 length; separate pre-replay cases cover lineage/raw-role presence/length/digest binding and the distinct replay-admitted brand; and hostile wrapper coverage rejects proxied, shared-storage, and detached byte inputs. The exact current source closure moved from 3,409 files at `sha256:783f64fbd99cb0724b5d6575dc4d09af806eb1abab34404c9b0f217d3aff0e4f` to 3,417 files at `sha256:5524ce4d54838a297fbd320959fcd42e7a0a8dcd4e8a3ece43aac1e6de5bdd38` because this unit adds eight attested E2E source files, and its independent eight-test derivation suite passes. The authoritative `npm run verify` passes 293 unit-test files with 2,619 tests and three skips, 40 headless browser tests with nine explicit opt-in skips, all 481 Python cases with 31 intentional skips, every schema, consumer, BOM, part, lesson, notice, formatting, lint and type gate, and the production build. `npm run audit` and `npm run audit:runtime` report zero vulnerabilities. Independent code, security, and documentation critics are clean; a grounded external CLI review's only post-fix finding was the then-stale focused denominator, which is corrected above.

**Code reviewer comments:** Independent semantic, security, documentation, and external CLI critics found the late aggregate check, incomplete transition-name binding, misleading `reproduced` and `Verified` labels, missing cumulative/mutation tests, the accidental public-parser compatibility change, an observation helper that accepted semantic validation without replay admission, the wrong search-parent ceiling symbol, uncharged parent/source/camera and selection/accepted-reference rows, late orphan-table refusal, and a misnamed cross-date devlog file. Those findings drove the two-phase and distinct replay-admitted work brands, central metadata check, verified-inspection wording, explicit new-path replay gate, complete row accounting, pre-replay orphan checks, expanded regression suite, and corrected history path.

**Notes:** The 512 MiB branch-role ceiling bounds transport and integrity-checks role bytes against caller-supplied descriptors before per-step semantics; it authenticates no external producer and is not part of the later semantic work budget. Compiler graph/byte meters are conservative admission-policy units, not literal CPU, allocation, collision-comparison, or total kernel-work counts. The sidecar does not bind PDF/page/crop, H/P/D/W source facts, D4 camera or renderer provenance, report order, terminal documents, placement, completion, or user-document mutation and is not browser-output `/4`.
