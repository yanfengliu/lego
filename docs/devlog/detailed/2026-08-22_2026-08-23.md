# Devlog — 2026-08-22 through 2026-08-23

This session turned the historical step-7 budget refusal into a current-source, complete-workload denominator. The repeated pattern was that a plausible measurement was not evidence until its parent reconstruction, source execution, policy, browser input, replay and retained bytes were independently bound.

## All four source parents close before migration

**Timestamp:** 2026-08-22 22:58–2026-08-23 12:57 PDT

**Action:** Added a diagnostic-only reviewed-additive compatibility seam whose first phase reconstructs and closes all four ordered catalog `/13` step-6 parents under the retained truth basis, rejects newly added part IDs, restores `/13`, and verifies every reviewed source hash. Only after all four immutable source snapshots close does a second phase apply the reviewed `/13` to `/15` migration. The old migrate-first ordering remains only as a negative control. The exported mechanical helper labels arbitrary caller pins `caller-supplied-unverified`; only the production wrapper binds the fixed module-owned source/current hashes and concrete production dependencies and returns the privately `WeakSet`-branded `reviewed-private-pins` result. Legacy fixed placement is restricted to step 6; current placement owns step 7.

**Result:** Source parent IDs `a806c6…`, `e637db…`, `d3c69d…`, and `0ecf6d…` reconstruct exactly before migration; derived current IDs `6d34d6…`, `291251…`, `57d953…`, and `f43f8a…` identify the `/15` expansions while the source IDs remain retained coordinates. All four current parents complete at 2,218 + 2,169 + 8,271 + 1,514 = 14,172 narrowing-render rows across 30 batches and retain 17 immutable parent-bound leaves; the renderer reports 14,188 calls because each parent has four additional non-narrowing calls. The diagnostic ledger accepts the full narrowing work under 32,768; a separate shadow production ledger reproduces the historical 8,037 + 599 refusal against 8,192. No production frontier is admitted and no document is published.

**Reasoning:** Migrating the base before replaying retained operations looked equivalent because the catalog change is additive, but operation-generated IDs include document revision and therefore changed parent canonical identity. Reconstructing one parent and immediately migrating it also left the phase boundary weaker than the evidence claim. Every source parent had to close in the exact retained truth coordinate system before any reviewed migration could make the immutable set current, and a public property could not substitute for the private production-pin brand.

**Validation:** Fast prewarm and parent-only browser gates prove current source import, CSP enforcement, full-input serialization/freeze/digest, the all-four-before-any-migration phase boundary and the private production wrapper. The fresh full Playwright control passed in 12.4 minutes. Independent Node replay re-enumerates current offers, derives carried indices from the live score-margin policy, verifies the retained DAG and exact ordered 17 leaves, and rejects a live score-component tamper; it does not rerender masks or scores from pixels. Final source attestation passes 8/8 over 3,560 files at `sha256:92730668928ba3ac40cfe46f85c7aed604ceccf1a1847f7ae3d81b00b190e2fe`. The authoritative `npm run verify` passes Python's 481 cases with 31 intentional skips; 345 Vitest files plus two skipped files, with 2,961 passing tests and five skips; 48 Playwright cases plus ten intentional skips across 58 declared, with the browser phase taking 10.4 minutes; and the production build/bundle guard. Both dependency audits report zero vulnerabilities.

## Equal-depth masks are lineage-sensitive

**Timestamp:** 2026-08-23 00:06–02:40 PDT

**Action:** Replaced the superseded expected context-dependent-candidate count 518 with the corrected reconstruct-first count 516, then bound the entire sorted parent-ordinal/catalog/color/transform key set at `sha256:b06d73e587e6180d916c1accda24ca0270d776bc4326d6c44b00bc34b6c2fdc9` and its per-parent vector `[65,64,387,0]`. A reversed-order synthetic fixture gates sorting, classification and digest construction.

**Result:** Old and corrected runs have the same 14,172 row locators, 6,414 candidate coordinates, 5,599 unique masks, every offered transform and every score. Thirty-five row mask digests change; exactly three context-dependent coordinates are lost and one is gained, so 518 becomes 516. The final fixed run reproduced count, digest and vector exactly.

**Reasoning:** Placement IDs are revision-seeded, scene derivation sorts parts by ID, and `LEQUAL` makes ownership of equal-depth pixels draw-order dependent. The corrected lineage can therefore change localized mask identity without changing geometry or score. The result is a current-lineage render measurement, not an identity-free geometry invariant and not budget authority.

**Validation:** Two independent critics decoded the failed Playwright result, compared all 14,172 rows to the superseded trace, reproduced the old hashes with the migrate-first negative control, followed the ID-to-draw-order causal chain in the renderer, and returned `LAUNCH`. The next unchanged full run reproduced the exact count, digest and per-parent vector; later drift must fail the reproducibility gate and be investigated rather than rebased as a new expected value, especially because this trace does not pin the Chromium executable, GPU/WebGL adapter and driver, or OS runtime.

## The retained denominator is byte-bound and has only local diagnostic authority

**Timestamp:** 2026-08-23 02:40–12:57 PDT

**Action:** Ran the fixed full control in a blank inert runner with an exact local origin, deny-by-default CSP, redirect-free request interception, exact Vite query forms, current-source allowlisting and executable-response byte capture. Request, response, request-failure, page, dedicated-worker and service-worker observers remain attached through context closure, and final reconciliation checks redirect state from the retained Request identities. The browser recursively freezes the complete input and computes full JSON SHA-256 before and after; the host independently hashes the same input. Retention recomputes the semantic trace body before directory creation, binds the exact panel bytes, writes through the no-delete exclusive file path with file fsync, ownership checks and same-parent rename, and performs bounded sequential read-back before and after publication.

**Result:** Current retained run `output/gate3-step7-diagnostic/runs/2026-08-23T19-35-22.906Z-8e14c4606400` has semantic trace digest `sha256:8e14c4606400b07eb4d1cbc145cb06f9dee1475cc907568870ac0e2c8ad9ca41`; trace file 10,902,807 bytes at `sha256:9c6f7a8fa5ce8bbc79393e9a7b530da48ef55d7ffb8516a5dc79964401f21b4b`; summary 25,295 bytes at `sha256:0ef2de83976096d6991a4a66c6026a6c5309242148e93008e5cfbcca3eaf857f`; panel 150,099 bytes at `sha256:852afab099a21d2776e9a1d69ea7c34d7a40509c46f7014c9b9de70db6d18dcf`; and manifest 630 bytes at `sha256:9ede00208fd978e5542822beb650b8a422e3bded2333145fd843caba14da815d`. Independent read-back recomputed every retained byte and semantic digest, matched 3,651 locked bootstrap files and 158,569,077 bytes to disk at `sha256:179f25b78776e6f5a0d9b6008a4eca5108145f60ea05a2bc8ce91c265b68e610`, matched the duplicate-preserving 179 executable request and response URLs at source-execution digest `sha256:acabbf14d921bedb87bcc41aa384ba6c0923b442068962b75c4cac014f484409` and served-JavaScript digest `sha256:5b51f53c8d87e1e8a97bdd328f6fa409f8330c79d16f14b391f490bcbee9c90e`, confirmed one host/browser input digest `sha256:a4f8ff299a8b1de2a3932d967b216ff09fcf7622b3f1190496d5a0fc5e043229`, and visually matched full-page, retained-panel, inventory-detail and assembly-detail views to printed step 7 on booklet page 13.

**Reasoning:** Earlier versions could look closed while trusting caller-supplied output digests, loading the normal app and mutating IndexedDB, binding only host-side source metadata, permitting broader Vite queries or origin drift, observing executable requests without retaining byte digests, or probing CSP through Playwright evaluation that bypasses page CSP. A later fresh run proved another hidden mismatch: Chromium emits `request` and `requestfailed` for deliberate CSP-blocked blob and external imports without invoking `context.route`. Treating those two events as ordinary routed requests contradicted the harness's own negative control. These were measurement defects, not cosmetic harness issues.

**Validation:** The boundary accepts only the synthetic runner, exact PDF and bootstrap-locked source paths; follows zero redirects; records zero route-blocked requests; and requires exact bidirectional URL-multiset equality with executable byte capture. Exactly the post-authorization owned blob URL and fixed external URL may appear pre-route, each once as `GET`/`script`, `requestfailed="csp"`, no response and no redirect; missing, early, duplicate, malformed or additional requests remain fatal, and the data URL appears only as an enforced `script-src-elem` violation. The replacement trace closes 183 unique request observations as 181 routed requests plus those two exact CSP failures, with zero pending routes, pages, service workers or dedicated workers. A separate delayed pagehide/ACK browser control proves close-time response-body consumption appears in both manifests; the full diagnostic itself has `requiredCloseTimeControlUrl=null`. A same-origin module-initialization control proves eval is blocked without DevTools bypass. Only response URL, status, type, byte count and SHA-256 are retained, not executable bodies, so this is not an executable replay bundle. PDF fetching and executable-response capture materialize exact bodies before their browser-side ceilings; host stat/digest binding and the fixed closed source make the measured workload controlled, but do not prove hostile-input memory bounding. Windows publication is bounded sequential/cooperative detection, not a directory-fsync seal, crash durability, simultaneous-at-return state or malicious same-user race protection; injected post-open, write and fsync failures deliberately leave any created partial or zero-byte path because the failure path never deletes.

**Code reviewer comments:** Independent internal review caught the migrate-first parent error; stale retained-source assumptions; a hand-built ledger and duplicated production policy; use of the normal app runner; output, panel, candidate-policy and live-source binding gaps; broad source/origin/query admission; `onAfterRender` observation gaps; conflated candidate and lineage IDs; newly added part relabeling; host-only input hashing; trusted retention digests; a self-referential Node replay; unbound score-margin derivation; request, redirect, blob/data and CSP bypasses; one-way served-byte accounting; the false Playwright eval control; retroactive CSP-control authorization; and repeated cleanup reporting of one sticky semantic failure. It also forced every terminal authority/input/cleanup assertion ahead of the first retention write, split the original harness into focused modules below the 1,000-line hard ceiling, and rejected the live depth-capture denylist after executable subordinate-state probes disproved it; `real-build-step7-gate3-diagnostic-source.ts` and its source-boundary browser spec remain above the preferred 500-line target. Each closed item now has a direct assertion or fast/full gate; the unsafe capture adapter and unsupported structural-pruning counts were removed rather than documented as evidence. An accidental external CLI review attempt failed during TLS setup before any model response, produced no verdict or evidence, and its process and temporary directory were removed; every reviewer claim here refers only to the independent internal critics.

## Required-gate failures stayed bounded

**Timestamp:** 2026-08-23 05:00–12:57 PDT

**Action:** The first authoritative gate reported the six focused Vitest regressions named by the user: five panel-camera lifecycle cases and one browser-output transition-witness case no longer retained genuine native `Error` detail. Focused reproduction traced the class to the non-probing formatter's blanket object fallback. A naive `Error.isError` repair was also rejected because it used the live global receiver and accepted an inherited `descriptor.value`. The bounded repair captures the browser and host intrinsics it needs, recognizes host native errors through `util.types.isNativeError`, validates a data descriptor through the descriptor of its own `value` property, constructs error names through a null-prototype descriptor with captured `Reflect.defineProperty`, and uses captured `WeakMap` methods for the evidence brand.

**Result:** The original six paths again retain their genuine native failure names and messages while hostile objects, proxies, accessors, replaced globals and polluted prototypes remain non-probing. The next fresh full diagnostic completed all four parents, 14,172 narrowing-render rows, 30 batches and 17 leaves but failed only while closing the request observer. Its 10,582,405-byte unverified raw envelope was retained at `output/gate3-step7-diagnostic/unverified/2026-08-23T18-40-53-950Z-7ee273da3ad1-1093e459-dac5-4ba5-a317-e3b592774ead.json`, `sha256:25c9249fd49cdc7e9ef61cd8b24d7f5e5de91987c214ad227ba8483e70e6b5fd`, with `completeRun=false`, `publicationEligible=false` and source execution unverified. It recorded 183 unique request events versus 181 routed requests while all 179 executable source events matched all 179 served responses and no response read failed. The two unmatched requests were exactly the run-owned blob URL and fixed external policy-control URL, each a status -1 CSP failure with no route callback. The repair admits only those exact post-authorization request identities under the strict method/type/failure/no-response/no-redirect shape, keeps every other request fatal, and reports one semantic failure only once across repeated disposal.

**Reasoning:** A CSP-negative control is supposed to fail before routing, so requiring it to appear in the route ledger contradicted the control itself. Exempting every CSP failure would instead create an egress blind spot. Exact one-use URLs, Request identity, an authorization-time identity watermark and independent failure/response/redirect observation close only the measured browser seam. The raw envelope and the older verified `2026-08-23T11-44-27.549Z-0a42ba349ee9` run remained counterevidence until the new source-attested replacement passed full read-back and visual inspection; neither was used as the replacement denominator. After independent artifact review returned clean, both superseded evidence paths, the task-owned Playwright last-run marker and five temporary visual-review PNGs were removed, while the current run remained intact.

**Validation:** The error-retention repair passed its 115-case focused set and three forced-browser source-boundary controls before the observer defect was discovered. The final Gate-3-focused unit slice passes 132/132; the exact CSP observer browser gate passes success, unexpected-extra-request and late-authorization controls 3/3; the original source-boundary browser gate passes 3/3; typecheck and the complete repository gate pass. Independent critics reproduced both causes, rejected the retroactive-authorization gap, then returned clean after the identity watermark and negative browser control landed.

## Depth composition is an estimate, not the optimization

**Timestamp:** 2026-08-23 01:20–03:51 PDT

**Action:** Added and tested isolated caller-asserted depth-composition arithmetic and analyzed the exact retained workload without integrating it into candidate selection or changing the production ledger. An attempted live Three.js capture/preflight adapter was removed after independent review proved that a callback denylist could still execute through subordinate matrix state and mutate the graph during inspection.

**Result:** Whole-scene reuse has zero exact hits. Accounting separates 30 prefix layers from 6,414 parent-scoped candidate layers, estimating 6,444 physical depth renders and 7,728 fewer renders than the 14,172 baseline. Mask-score disagreement is zero, but 516 candidate coordinates have prefix-context-dependent masks.

**Reasoning:** The count fits below 8,192, but a lower accounting estimate is not semantics-preserving execution. Equal-depth ownership is already proved draw-order sensitive; exact surface partition, depth precision, opaque tie order, complete-mask parity and fallback behavior must be proved before any ledger reservation can charge the estimate.

**Validation:** Eight pure composition tests pass over caller-asserted buffers; no WebGL capture, calibration, readback or cleanup adapter remains. The retained trace explicitly says depth composition is not integrated, parity is not proved, exact partition identity is not proved and equal-depth tie count is unknown. Geometric zero bounds, reuse and positive upper-bound pruning likewise retain no current per-row proof artifact and receive no ledger credit.
