# Devlog — 2026-08-11

The session closed the exterior evidence debt created by catalog `/13`, measured but did not integrate farther-panel search, and replayed the repaired prefix from a source-stable snapshot. The visible catalog claim is now closed; hidden interiors, exact collision, step-5 placement, and the complete booklet remain open.

## The first completed visual review was validly bound and invalidly observed

**Timestamp:** 2026-08-11 00:18–00:52

**Action:** Captured eight policy-ordered source and production views for all 24 mesh-backed catalog definitions with shared cameras and an independent Three.js `LDrawLoader` source path, then published immutable review sidecars only after a second capture produced lossless native-size source/candidate composites. The first review used 320-pixel thumbnails made from the bound 640-pixel images and was retained as counterevidence instead of being overwritten or promoted.

**Result:** The admitted capture batch is `sha256:49bd56d126cfb5845b5cdd7ca5d67c4926d002673d4e036b914ef3140ad2ecd1`, its 192-pair native manifest is `sha256:26939334e464d4ee676a2b242b64cde80353f76cdf0541d99eb5063f1ee94b19`, and its 24-sidecar review batch is `sha256:e1094576c2250db8a9875828254f064384c195991304d363c5a2a9ff5a50c0dd`. All 192 visible outcomes are `same`: 181 pairs are RGBA-identical, ten pairs differ across twelve pixels by at most one channel value, and `cheese-slope-2x1` isometric differs across 104 pixels with maximum channel delta 34 and IoU `0.9997716005427846` but no visible geometry or shading difference at native size. The result says nothing about hidden interiors or physical collision.

**Reasoning:** A contact sheet is an index, not admission evidence. Half-resolution comparison can conceal the exact edge, winding, handedness, underside, or shading defect the review is meant to catch, and the immutable sidecar contract makes a fresh capture preferable to quietly rewriting a flawed conclusion. The larger cheese-slope raster delta is reported as observed variance without asserting an unproved antialiasing cause.

**Validation:** Two independent passes opened all 192 native pairs at original detail. The critic independently reconstructed every packet, sidecar, PNG, raw RGBA hash, composite region, metric, and batch digest; every composite preserved both 640×640 packet images without resampling. The browser regressions were also corrected to recognize production mesh previews and declared mesh tones rather than obsolete primitive DOM or six-color assumptions; the two focused Playwright files passed 4/4, with `offPaletteShare = 0` for all five instruction-render subjects.

**Code reviewer comments:** The critic caught both failures the first review missed: the helper had downsampled every image from 640 to 320 pixels, and the review notes attributed the nonzero raster delta to antialiasing without evidence. The rejected sidecars remain under ignored output as counterevidence and have no review-batch manifest.

## Panel 7 separates a parent family but does not settle the intervening step

**Timestamp:** 2026-08-10 23:46–2026-08-11 00:17

**Action:** Reconstructed the exact settled step-4 prefix, reused production step-5 own-panel narrowing, expanded every surviving step-5 parent atomically through all four step-6 additions, and scored all resulting children against the first visibly revealing farther panel at step 7. Added standalone immutable frontier, lineage, atomic-witness, contiguous-panel, and aggregate-budget primitives without connecting them to settlement or increasing a budget.

**Result:** Step 5 carried two complete parents after 574 narrowing renders. Step 6 produced five children from the first parent after 2,628 renders and four from the second after 5,981, for nine complete children and 8,609 narrowing renders—417 above the declared 8,192 global budget. Step 7 scored all nine: the first family topped out at `0.699940`, while the second ranged from `0.849100` to `0.864693`. That separates and favors one step-5 family by panel agreement, but the best two step-6 children remain only `0.00571165` apart against the `0.02` margin, so four descendants remain unresolved and production behavior is unchanged.

**Reasoning:** Giving each parent its own 8,192-render allowance would silently multiply the resource contract. Selecting the best child would also collapse two distinct claims: the later panel can identify the parent family while still failing to identify the atomic four-piece step-6 placement. The standalone unit therefore records family selection and unresolved descendants separately and refuses before returning a partial frontier when any aggregate limit is exceeded.

**Validation:** The exact headless probe ran for 6.7 minutes with no model call, network image transmission, or official transform oracle. Focused tests cover all-parent expansion, exact catalog-part/color multisets, skipped panels, frontier reach, out-of-range scores, invalid origin evidence, integer overflow, runtime immutability, `not-observable`, and precise refusal diagnostics.

**Code reviewer comments:** The first implementation counted atomicity but did not bind identity, allowed a farther panel to skip intervening evidence, accepted scores above one, called invalid children complete in refusal evidence, and collapsed distinct errors into generic messages. The harsh review corrected each defect; generic document payload-to-hash binding remains explicitly upstream-owned because the standalone unit cannot prove an arbitrary caller payload matches its supplied digest.

## A source-stable replay moved the verified frontier to step 4 and stopped honestly at step 5

**Timestamp:** 2026-08-11 01:01–01:06

**Action:** Ran the retained real-booklet probe through requested step 7 with catalog `/13` from a locked source mirror, published its incomplete local diagnostic, and inspected the newly retained step-4 and step-5 panel/build pairs.

**Result:** Run `2026-08-11T08-01-38-825Z-84c532dd32aa-e351e7e6-e0b5-4c78-845a-28ba97718932` has no input or source-drift failure, completed and validated printed steps 1 through 4, and placed six pieces. Step 4 settled both additions at joint visual score `0.8578158458244112`; its retained build now exposes the hollow underside rings, walls, ribs, and cavities drawn by the panel. Step 5 still refuses because its own leaders are separated by only `0.002799160251924393` against `0.01`, and panel 6 gives best deferred agreement `0.7635021804763502` against `0.85`; steps 6 and 7 are causally blocked rather than guessed.

**Reasoning:** The Playwright command exits red under `LEGO_REAL_BUILD_REQUIRED=1` because the required-run assertion rejects any incomplete execution. That red result is the intended evidence boundary, not source drift or a loss of the first four rows: publication first verifies and retains the source-stable byte closure, then the test refuses to describe the seven-step request as reproduced when only six of sixteen requested identities were placed.

**Validation:** `output/real-build/runs/current.json` names the run with artifact-manifest digest `sha256:be5ab0003524a6a6f9e5dadde97726cee958c95fe813189681ffa87acb52ff0a` and downstream replay-closure digest `sha256:e44374f7321ee9720d9786e2aeb5c8edb063878fe94dc106eba18cd1f85bf6c3`. The score digest is `sha256:a806df75c4a4d47cd1fff0349a22e842d18f1d640d510fde39b25661e01bd2ff`. Visual inspection confirmed the step-4 render matches the panel's exposed underside structure; the step-5 build correctly remains at the step-4 prefix after refusal rather than drawing either unproved branch.

**Code reviewer comments:** The earlier `/13` diagnostic had four complete rows but changed source during execution, so it could publish only as an unauthenticated drift diagnostic and could not move the verified frontier. The clean replay closes that exact gap while leaving the harder placement ambiguity untouched.

## Final adversarial review closed the evidence boundaries it challenged

**Timestamp:** 2026-08-11 01:18–02:02

**Action:** Re-ran the prescribed multi-CLI review boundary, then used independent repository critics when the external Codex reviewer could not connect through its certificate chain and the Claude reviewer stopped at expired OAuth. The critics attacked migration truth, LDraw winding, generated-source limits, review-sidecar publication, native-pair filesystem containment, Builder pin organization, and malformed farther-panel inputs rather than accepting the earlier green focused runs.

**Result:** Only explicit `BFC CERTIFY` now authorizes front-side culling; a bare `BFC CCW` remains uncertified and is refused. Every generated table and new handwritten source is below the repository ceiling, and the 15 Builder pins live in three focused shards behind their unchanged public façade. Review-batch publication reconstructs the aggregate outcome, rebinds every PNG/RGBA digest, and rereads retained PNG bytes. Native-pair generation binds descriptor identity and byte limits, stages and publishes beneath guarded ordinary ancestors, and cleans only the original directory identity. Farther-panel parsing snapshots exact plain data from hostile runtime values so malformed arrays, proxies, accessors, traps, or thrown values return a frozen typed refusal instead of escaping.

**Reasoning:** An outer hash proves only the bytes it covers, not that an aggregate agrees with its rows or that a nested image hash names the packet image. A lexical path check likewise proves nothing after an ancestor is replaced, and a TypeScript type proves nothing about a runtime proxy. The corrected boundaries reconstruct the claims they consume and bind the file or value identity before using it.

**Validation:** The regenerated `/3` report is `sha256:04a5d99e9384ed6baa5a803ecf6b9d66a9c8c8d7a5682d23035dd73973f9e785`; all eight generated modules reproduce and remain below 1,000 lines. Focused review publication tests pass 16 cases, farther-panel tests pass 13, Builder calibration/input-chain tests pass 12, and the path suite passes 43. A retained-batch smoke published all 192 native pairs through the guarded path and removed its exact task-owned directory afterward. Revalidating the admitted review batch reached only the intended immutable-manifest refusal after checking all retained packets, sidecars, outcomes, and images.

**Code reviewer comments:** The first implementation trusted a rehashed sidecar whose aggregate could contradict a `different` row, read packet images only when each sidecar was created, accepted an output tree whose ancestor could be swapped, bounded a pathname before opening a replacement file, treated bare `BFC CCW` as source certification, compressed a large pin table instead of organizing it, and returned live proxy arrays after nominal validation. The external review CLIs produced no verdict; their raw failures stayed under ignored task evidence and were not promoted, while the independent critics' concrete counterexamples became regression tests.
