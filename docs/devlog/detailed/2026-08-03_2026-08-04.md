# Devlog — 2026-08-03 → 2026-08-04

One commit on 2026-08-03 and twenty-one on 2026-08-04, almost all of the latter in a single burst between 20:55 and 23:31.
The reason for the burst is the first entry for 2026-08-04: a session had inherited a blocker that was false, believed it, and left roughly four thousand lines of already-approved work uncommitted. Retesting the claim released all of it at once.

## The 6651557 LDraw source routes are audited, and 4 of 121 leaves have no exact route

**Timestamp:** 2026-08-03

**Action:** Added the source-resolution audit for set 6651557: `scripts/ldraw_source_archive.py`, `scripts/set_6651557_ldraw_source_audit_plan.py`, `scripts/generate_set_6651557_ldraw_source_audit.py` and its Python test, plus the generated evidence file `packages/catalog/src/quarantine/set-6651557-ldraw-source-audit.generated.json` and the Vitest contract `packages/catalog/src/quarantine/set-6651557-ldraw-source-audit.test.ts`. Created the `test:python` script and inserted it into the `verify` chain. Extended `scripts/check-bom.mjs` with a record-specific rule for `ldraw-set-6651557-source-resolution-audit`, and added `lego.set-6651557-ldraw-source-audit/1` and `ldraw-source-resolution-only` to the forbidden-token list in `scripts/check-production-bundle.mjs`.

**Code reviewer comments:** None recorded — this commit carries a bare subject line and no body.

**Result:** 121 required leaves; 117 routes resolved (113 official exact roots, 3 unofficial, 1 keyword mapping) and 4 unresolved. The closure spans 439 unique source files totalling 896,002 bytes, each recorded with path, bytes, sha256, title, declared name, author, `!LDRAW_ORG` line and per-file license expression. The header states `admissionState: "blocked-unresolved-parts"` and an authority block where every claim — `catalogAdmitted`, `catalogFramesClaimed`, `collisionTruthClaimed`, `connectorTruthClaimed`, `partDefinitionsEmitted`, `runtimeExposed`, `runtimeFetchAllowed` — is `false`.

**Reasoning:** Facts about files, not the files themselves. The BOM record stays `included-metadata-only-attribution-bound` with `rightsPolicy: attribution-required-facts-only`, so the audit can be committed and reviewed before the separate, later decision about bundling geometry (see `8d2b720` on 2026-08-04).

**Validation:** `bom:check` now fails if the record's status, rights policy, declared license or `allowedRoles` are widened, if the BOM version string does not contain the live `generated audit sha256`, or if the artifact reaches 256 KiB. `check-production-bundle.mjs` fails if the audit's schema token reaches a shipped bundle. Without the `verify` wiring, the Python audit test would have been passing-but-ungated.

**Notes:** The generated JSON is 251,402 bytes against the check's 256 KiB (262,144-byte) hard stop — about 10 KiB of headroom, roughly 15 more file records. Anything that grows this artifact needs a plan for where it lives, not a raised constant.

## The four inherited days: a false blocker stranded ~4,000 approved lines

**Timestamp:** 2026-08-04

**Action:** Recorded two lessons in `docs/learning/lessons.md` and `docs/learning/lessons-evidence.md` (commit `12d328b`), the first of which explains why every commit below lands in one 20-commit burst between 20:55 and 23:31.

**Code reviewer comments:** Self-caught, by retesting an inherited claim instead of repeating it.

**Result:** An inherited handoff asserted that Git writes were unavailable behind an approval quota lasting four more days. The next session believed it, planned around it, and left roughly four thousand lines of independently approved work uncommitted across two sessions. One command disproved it: `.git` was writable, no stale lock existed, `git add` returned 0, and the first push succeeded. The same handoff carried the same shape a second time — "the only registered Python is 3.14.0" — disproved by one `where python` (see `a0dd249`). The original failure was real but session-local; what was false was its promotion into a standing restriction.

**Reasoning:** A blocker is the single inherited claim that must be retested before it is repeated, because its entire effect is to stop work. Every other inherited claim costs a wasted check if wrong; a false blocker costs all the work it forbids.

**Validation:** `lessons:check` green at 38 rules, format clean.

**Notes:** The second lesson landed in the same commit is the metadata-comparison finding written up under "The real-build run binds every input it consumes" below.

## Mesh-backed parts can be admitted, with drift measured rather than assumed

**Timestamp:** 2026-08-04

**Action:** Added `packages/catalog/src/mesh-assets.ts` (853 lines) and `packages/catalog/src/mesh-admission.ts` (728 lines) with their suites, extended `packages/rendering/src/geometry.ts` and `limits.ts` with a shared renderer-precision frame conversion and finish-/`includeStuds`-aware instruction geometry, and wired the preview path through `apps/web/src/components/PartPreview.tsx` and `apps/web/src/mesh-part-alignment.test.tsx`. Admission enforces exact clipped wedge bounds, strict topology, connector/collision/allowance cardinality, bounded registry/LRU/allocation, and Float32 round-trip validation on every coordinate and bound.

**Code reviewer comments:** None recorded.

**Result:** Maximum drift across 257 valid translations is 0.00000762939453125 LDU against a 0.0001 LDU cap. `[100000003, 0, 0]` is rejected rather than silently collapsing 3 LDU. The production mesh registry stays empty and the catalog stays at 77 definitions, version `/6`, hashes unchanged: this admits the capability, not any part.

**Reasoning:** The renderer works in Float32; admitting geometry without measuring what Float32 does to it would push an unmeasured error into the one place pixels are compared against printed panels. Measuring first turns "probably fine" into a number with a cap beside it.

**Validation:** 18 files / 196 tests, workspace typecheck clean.

## Booklet callout truth reconciles to 881 identities, proven by the real publisher

**Timestamp:** 2026-08-04

**Action:** Audited 15 further semantic callout identities in `apps/web/e2e/callout-contract.test.ts`, `callout-recovery-fixture.ts` and `callout-thumbnails.spec.ts` as true subassembly-repeat and vector-box-full regions carrying quantity-label masks, 10,000 foreground pixels, 16px clearance, and independently audited minimum dimensions.

**Code reviewer comments:** None recorded; the semantic contact sheet was visually inspected rather than accepted from the numbers.

**Result:** Exact accounting: raw 881 identities / 1,512 quantity; physical part art 863 / 1,472; semantic 18 / 40; 34 unique benchmark-class cases.

**Reasoning:** The semantic rows are real callouts that are not physical part drawings; folding them into the physical count or dropping them both produce a total that no downstream consumer can reconcile.

**Validation:** The real all-page headless publisher, not unit tests alone: 196 pages, 881/881 identities, 1,512/1,512 quantity, zero failures, equal identity hashes, evidence-aware strategy winner, 188.6s. Manifest SHA-256 `d1933a5ed8b2f9bd62b07610541e09fefb1129a9525d5546a69fbd19988150a1`. Gates: 1 file / 8 tests.

**Notes:** The 863 / 1,472 physical split was itself wrong by four labels and eight pieces, discovered on 2026-08-05 when a set-accounting mismatch forced a character-level re-read of the PDF. The classifier that permitted it — `evidenceContract` returning `part-art` for any identity absent from the curated fixture — failed open, and was closed on 2026-08-06 by publishing and gating on the type size each label is set in.

## Source truth is measured from pinned LDraw and Builder packs, and exceeds the current contract in four ways

**Timestamp:** 2026-08-04

**Action:** Added the measurement-only source toolchain: `scripts/builder-import-snapshot.py` (pinned private Builder import snapshot), `scripts/ldraw_surface_expander.py` (strict LDraw surface expander), `scripts/builder_native_source.py` (checksum-bound native pack reader), `scripts/extract-builder-shell.py`, `scripts/generate-builder-calibration.py`, and `scripts/generate-set-6651557-source-pilot.py` — the six-part pilot that scores the sources against each other.

**Code reviewer comments:** None recorded.

**Result:** The pilot measures six exact official closures — 51739, 77844, 30357, 35480, 5092, 93273 — covering 70 unique files checked against the complete 439-record audit: 2,829 source triangles and 1,818 unique positions. It records four ways the exact source exceeds the current contract: fractional LDraw bounds (51739, 93273), unavailable checksum-valid Builder surface (30357), non-upright Builder connectivity (5092), and non-axis-aligned Builder collision (5092, 35480, 51739, 77844, 93273). Report `output/real-build/set-6651557-source-pilot.json`, 10,130 bytes, SHA-256 `368753adec40d517c5063cbe23f28b9ff21108f0f8824bb0671b8c2575794613` (local evidence, not committed).

**Reasoning:** The expander enforces exact type-1 composition, BFC certification, winding and INVERTNEXT, determinant reversal, bounded recursion and triangle counts, finite coordinates, internal filename-space preservation, post-transform nondegenerate triangles, inherited visible-stud ancestry, and the official 0.025–179.9 degree interior-angle and 3-degree two-diagonal quad tolerances. The older non-BFC calibration parser stays usable for distance evidence only. Authority stays measurement-only-not-catalog-admitted: no frame, connector, collision, runtime or `PartDefinition` claim is made. The commit deliberately does not choose the replacement contract — it proves the current safe-integer bound and axis-aligned collision assumptions cannot admit the pilot.

**Validation:** 68 Python tests across 5 modules; BOM check green at 8 workspaces / 48 declarations / 24 third-party / 6 internal / 19 data records; notices check green.

## Owner decision: layer 1 bundles real LDraw geometry, and truth versions advance at first admission

**Timestamp:** 2026-08-04

**Action:** Replaced the "Open decision" section of `docs/design/part-model.md` with a resolved one and recorded two owner decisions in `docs/policies/local-rules.md`.

**Code reviewer comments:** This is the owner's own decision, on a question `part-model.md` had explicitly reserved for the owner.

**Result:** Layer 1 bundles real LDraw geometry. Obligations recorded as non-optional: per-file authorship and licence preserved; `docs/dependency-data-bom.md` and the third-party notices flip from "no LDraw geometry bundled, identifiers only" to geometry bundled under CC BY 4.0 with attribution required; reuse is still not training, which stays an unheld, separately recorded right. The BOM and notices flip at first admission, not at decision, because bundling is a claim about files actually present. Second decision: advance catalog, collision and validator truth versions at the first production admission rather than at contract change, keeping the preceding version as a historical migration snapshot so existing documents keep hashing as they did.

**Reasoning:** The reason it could not stay deferred is the booklet. Every claimed-built step is compared against its printed panel, so a generated approximation of a curve is not merely less pretty — it fails the one measurement the goal is scored by, for a reason unrelated to whether the build is right. Layers 2, 3 and 4 never depended on either decision.

**Validation:** Doc-only; the BOM/notices obligation is deferred to the commit that first bundles a geometry file.

## The lint gate was failing on artifact roots, 366 of 367 errors from stale probe directories

**Timestamp:** 2026-08-04

**Action:** Added `output/` and `var/` to the ignore list in `eslint.config.js` (8 lines).

**Code reviewer comments:** None recorded.

**Result:** ESLint ignored `dist`, `coverage` and `test-results` but not `output/` or `var/`, which AGENTS.md declares as the roots for task-run evidence. A replay test that snapshots repository sources into its own run directory, and a critic probe left behind by a denied cleanup, were both being linted as repository source: 366 of 367 errors came from two stale probe directories rather than from any code.

**Reasoning:** A gate that fails for reasons unrelated to the code trains people to ignore it. The cost is not the 366 errors, it is the one real error hidden behind them.

**Validation:** Lint clean afterwards across the later commits in the same burst.

**Notes:** Ignoring the artifact roots treated a symptom. The underlying leak — replay run roots accumulating under `output/` — was measured on 2026-08-05 and had already stopped, but nothing existed that would have said so.

## One implementation of the bounded-hostile-input rule, rather than a ceiling per call site

**Timestamp:** 2026-08-04

**Action:** Added `apps/web/e2e/bounded-file-read.ts`, `bounded-json.ts`, `contained-atomic-write.ts`, `contained-directory.ts` and `strict-json.ts` — bounded reads with explicit byte ceilings, strict JSON parsing that rejects prototype-polluting and duplicate keys, atomic writes bound to a held directory handle, and containment checks that survive a path being swapped underneath them.

**Code reviewer comments:** None recorded.

**Result:** 1,222 lines of shared primitives the real-build harness builds on.

**Reasoning:** AGENTS.md requires file imports, archives, JSON and provider output to be treated as hostile and bounded by bytes, depth, counts and paths. Per-call-site ceilings mean the weakest call site defines the guarantee.

**Validation:** Exercised through the harness commits later the same evening; `bounded-file-read.ts` was corrected in `40cb4de` when its metadata-based freshness check turned out to promise something metadata cannot deliver.

## A provisional oriented-box kernel, deliberately unwired, with the 1e-9 dead zone removed

**Timestamp:** 2026-08-04

**Action:** Added `packages/brick-kernel/src/oriented-box-overlap.ts` (298 lines) and its test. Separating-axis test over all 15 OBB axes (Gottschalk, Lin and Manocha section 5), with center-delta and projected-radius comparison, explicit nonfinite guards, and conservative analytic broad-phase spans using per-operation outward `nextUp`/`nextDown`.

**Code reviewer comments:** The commit states that a fresh independent critic was re-running every counterexample at the time of the commit, and warns explicitly against treating this as integrated or as a collision-support claim.

**Result:** Not exported from the brick-kernel index and referenced by no caller, so behavior is unchanged. The former fixed 1e-9 geometric dead zone is gone: it erased a real 5e-10 LDU axis-aligned penetration despite the positive-volume contract. Face normals are derived from adjacent authored edges, treating a tolerated matrix as the exact represented parallelepiped rather than normalizing or repairing source truth — a matrix accepted with 4.73e-10 round-off had a true face-normal separation of ~1.0e-8 LDU at ordinary 100-LDU extents while its almost-orthogonal edge axis reported overlap. Exact represented contact remains non-collision. The kernel refuses both corner output and overlap classification when coordinate precision collapses distinct source corners.

**Reasoning:** The 6651557 source pilot measured oriented Builder collision frames on five of six pilot parts, which the current axis-aligned representation cannot express. This is the candidate answer under review, committed unwired so the review has something concrete to attack without any part of the product depending on the outcome.

**Validation:** Ten tests covering those regressions, the sole-separator 4e-10 attack, large-coordinate rejection and bounds, translation invariance, arithmetic overflow, and 128 deterministic comparisons.

## The 3245 Builder shell is quarantine-reported without any admission authority

**Timestamp:** 2026-08-04

**Action:** Added `scripts/discover-builder-shell.py` plus `discover_builder_shell_core.py`, `discover_builder_shell_metadata.py` and a test module — a bounded quarantine report emitting exact bundle identity, one Shell tuple and canonical digest, and bounded primitive connector metadata and part identity when present. Wired the suite into `npm run test:python`.

**Code reviewer comments:** Hardened after three rejection rounds. Each pipe now actively caps at 4,001 captured bytes and terminates the exact Job on first overflow rather than capping after exit; the worker opens suspended and is assigned to the Job before resume, with exact-process `TerminateProcess` and `NtTerminateProcess` fallback plus `WaitForSingleObject`, so an unassigned worker cannot leak when `AssignProcessToJobObject` fails; thread-snapshot, suspended-thread and Job handles close through `CloseHandle` with `NtClose` fallback; publication uses `NtCreateFile`/`NtSetInformationFile` relative to a held non-reparse root, refuses write/delete sharing through rename, binds the exact `FILE_ID_INFO`, verifies bytes on the same handle, and deletes by exact handle on a failed postcheck; decoded groups are capped before append, including arbitrarily many empty ones.

**Result:** The reporter has no admission authority — it cannot write `SUPPORTED_SHELLS` or emit a `PartDefinition`. Real Windows fault injection forces every owned-handle close to fail and proves all three handles invalid afterward. Gates: 24 tests here, 92 across the whole Python suite.

**Reasoning:** 3245 is one of the four designs from the 2026-08-03 audit with no selected exact LDraw route, and an alias manufactured to fill that gap would be worse than the gap.

**Validation:** The suite was passing but ungated before this commit, which is not a gate.

**Notes:** This commit asserts that "the only registered runtime is `C:\Python314\python.exe` 3.14.0". That is false and was corrected 31 minutes later by `a0dd249`.

## CORRECTION: the interpreter was never the Builder-bundle barrier

**Timestamp:** 2026-08-04

**Action:** Replaced the prose barrier with an executable one: `assert_pinned_environment_for_retained_bundle` refuses to hand the exact retained identity to any loader unless the active import root satisfies the full 13-distribution RECORD contract _and_ UnityPy resolves inside it. Split `atomic_write_relative_windows` into `scripts/discover_builder_shell_publication.py` with its own test, and added `discover_builder_shell_worker.py` and `discover_builder_shell_test_support.py`.

**Code reviewer comments:** From the same review: publication was not failure-atomic — a failed postcheck could delete a report that had already replaced the previous one, leaving neither; four bare NTSTATUS messages carried no decoded name, file, cause, remedy or dead end; and `atomic_write_relative_windows` (256 lines) had been re-derived from scratch by three separate critics, which was the argument for splitting it out.

**Result:** The claim from `94ac0f0` was measured and is false: `miniconda3\python.exe` is CPython 3.13.9 64-bit and passes `validate_worker_runtime`; `miniconda3\envs\py313` is 3.13.10 64-bit and passes; `C:\Python314\python.exe` 3.14.0 and Python310 3.10.6 are rejected. `where python` resolves _first_ to the conforming 3.13 — the same interpreter `npm run test:python` already uses. The real barrier is that no interpreter can import UnityPy at all, and the pinned 13-distribution contract admits only a private RECORD-verified snapshot. Publication now runs every rejecting check on the held temporary handle _before_ the rename, and cleanup keys off `renamed`, not `published`. File sizes: core 613 → 456, entry 566 → 320, test 880 → 229; every source file now under 500 lines. BOM records the 3245-M bundle as a source (URL, 85,098 bytes, sha256 `1aa4e833...`, md5/ETag, object version, retrieval time, private noncommercial reference role, explicit refusal of redistribution, packaging, runtime fetch, upload, benchmark inclusion and training): 19 → 20 data records.

**Reasoning:** A barrier asserted in prose invites someone to step over it: read "the interpreter gate protects us", run `pip install UnityPy==1.25.3` to unblock the tool, and third-party parsing of an untrusted 85,098-byte bundle proceeds with nothing left in the way. The refusal message now names both dead ends explicitly — that the 3.13 interpreter check is not the barrier, and that `pip install` into an existing interpreter does not satisfy the gate.

**Validation:** The regression patches `validate_worker_runtime` to a no-op, so "the interpreter is not the barrier" is asserted as executable truth rather than prose. Both behavioural fixes were reverted and reconfirmed to fail. Gates: 27 tests here, 95 across the Python suite; BOM and lessons green.

**Notes:** The false claim was repeated from an inherited handoff without being tested. It is the second instance of the same shape as the Git-quota blocker recorded in `12d328b`.

## Part identification is hardened against substituted evidence

**Timestamp:** 2026-08-04

**Action:** Reworked the identification closure across `scripts/part-identification-artifacts.mjs`, `part-identification-io.mjs`, `part-identification-cards.mjs`, `part-identification-card-images.mjs`, `part-identification-contained-path.mjs`, `part-identification-contained-write.mjs`, `part-identification-call-snapshot.mjs`, `part-identification-strict-json.mjs`, `part-thumbnail-image-guard.mjs` and `booklet-catalog-coverage.mjs`.

**Code reviewer comments:** Repaired across several adversarial rounds. Findings: authenticated images were aliases of caller-owned `Uint8Array` storage, so a same-length mutation between authentication and the call could reach the model; snapshot creation did not rehash cards against their manifest digest; a lock helper could exit before explicit release and leave the call unpoisoned; contained writes bound a pathname rather than a file identity, so identity-check-then-unlink could delete an attacker replacement; root preflight could be defeated by renaming the root and installing an ordinary replacement tree; the coverage builder had a getter returning one `manifestBytes` value to the parser and a different one to the hash.

**Result:** Authenticated images are now defensive copies while retained evidence keeps the original bytes. A total decode budget is enforced _before_ native decoder allocation — per-image 32M-pixel limits multiplied by 4,096 cards had permitted roughly 512 GiB of aggregate work. Digest roles use a closed canonical whitelist that rejects reserved overrides; callout arrays and binding fields are snapshotted once; the feature PDF/manifest edge must equal the retained manifest source hash and digest. The public part-number validator now accepts real decorated LDraw identifiers — `973c27h27`, `4162pr0074`, `3070bpr9884`, `61406pat0009` — instead of falsely requiring digits plus a single suffix.

**Reasoning:** The identification closure decides which catalog part each booklet callout is, so anything that can change what the model saw, or what the record says it saw, forges catalog truth.

**Validation:** Regressions cover every finding: accessor mixes, post-snapshot mutation, reserved and insertion-order digest roles, nested callout and binding mutation, forged PDF/manifest edges with downstream rebinds, single reads of every artifact field, prelaunch same-length mutation, a callback reading a temporary replacement before restoration, and the real decorated identifiers.

## Source ownership is split by layer on measurements, and Builder collision is refused

**Timestamp:** 2026-08-04

**Action:** Recorded "Which source owns which layer" in `docs/design/part-model.md` (16 lines) after building two deliberately different strategies against the six-part pilot and scoring them on their numbers rather than their reports.

**Code reviewer comments:** The owner measured the Builder inset personally, alignment-free — Builder's own mesh against Builder's own boxes in one frame — so no registration error could explain it.

**Result:** Collision (layer 4) comes from LDraw: per-column height-field decomposition gives 37–276 bodies at 1.07–1.28x over-claim, with 3,400,701 sampled surface points and none outside the union, needing no new body kind. Builder collision is refused: its authored boxes are inset a uniform 0.25 LDU on every horizontal face, so the real part escapes them by 0.25 to 4.675 LDU. Inflation does not rescue it — closing the escape needs 3.21–5.66 LDU, after which the union overhangs by up to 10.29 LDU per face and grows 8.5–36.8x in volume. Connectors (layer 3) come from Builder, which reproduces LDraw-measured stud centres to 0.000 LDU and carries an authored functional claim geometry does not contain: 5092 declares one clutch and codes the neighbouring cell absent — the exact cell the LDraw rule scores 1 of 2 on, because the overhang is genuinely partial. Bounds (layer 2) come from LDraw exactly, as signed integer units at 10^-9, forced by 93273's -16.00016098 not being a double.

**Reasoning:** Neither strategy won outright and the split is not a compromise — each layer went to whichever source can actually be checked. Under-claiming collision is the one direction collision may never be wrong in. The per-slab convex-prism alternative uses far fewer bodies but over-claims up to 2.02x and produces 18-vertex hulls against the current 3–8 vertex limit. The one-declaration rule is defended rather than assumed: taking connectors and bodies from different sources is how they drift, so the Builder connector set is admitted only through a containment gate against the exact LDraw closure, making that expansion a prerequisite of the Builder path rather than a cross-check of it.

**Validation:** Doc-only; the numbers come from the pilot in `006f31d` and the scorer in `ee588d3`.

**Notes:** Gaps recorded as work items, not caveats: 14 of 121 designs are absent from the Builder pack, 20 required leaves depend on an unvalidated node family, and only 5 of 107 records carry an external review pin. Builder physics is refused as mass truth — 66 of 107 records declare mass exactly 1, which makes a 1x2 tile outweigh a 1x4 curved slope. A third source, the LDCad shadow library, was measured on 2026-08-05 and turned out to be the only route to female connectors for the 14 Builder-absent designs — but is CC BY-SA, so it may be measured and not shipped.

## The oriented-box kernel's differential test was comparing it to a copy of itself

**Timestamp:** 2026-08-04

**Action:** Rewrote the guards and tests of `packages/brick-kernel/src/oriented-box-overlap.ts` and its suite. The SAT algorithm itself is untouched.

**Code reviewer comments:** An independent critic checked the algorithm against exact BigInt and rational oracles over 14,700 configurations and found no error. Every defect was in the guard and in the tests that were supposed to protect it.

**Result:** Four findings, each with a number. (1) `referenceShareVolume` reused the implementation's entire numerical recipe — same cross products, same rescale, same accumulation order — so it disagreed with the kernel 0 times and with exact truth 598 times, wrong in the _same_ direction in 598 of 598. Replaced with an exact dyadic-BigInt oracle: inputs decomposed to mantissa times a power of two, 15-axis SAT with no division, normalization or rounding, and the centre difference taken exactly; a second test bisects the kernel's own contact distance and probes both sides at 1e-11 relative. (2) The precision-collapse guard tested whole-corner string distinctness, so a box whose entire span along one world axis had been annihilated passed because two corners differed by a 1.11e-16 sign flip elsewhere — 141.4% corner error, silently classified. Each world axis is now checked on its own, source span against realized span, and the message names the axis, both spans, the binary64 spacing at that centre, and what would satisfy it. (3) Two constants that decide catalog truth had nothing pinning them: the orthonormality tolerance could be widened from 1e-9 to 1e-3 with all ten tests still passing — admitting a shear that displaces a corner 0.1 LDU on a 100-LDU half-extent — and the outward `nextUp`/`nextDown` rounding that makes the broad phase provably conservative could be deleted entirely with all ten tests still passing. Both are now pinned on both sides, the second by asserting direction rather than closeness across 576 directed components. (4) `isProperOrthonormalMatrix` crashed with bare engine `TypeError`s on `undefined`, `null`, strings and `{length:9}` — pre-empting the module's own message for the commonest malformed box — and returned `true` for a `Float64Array`. It is now total, and `assertOrientedBox` was hardened as a class.

**Reasoning:** A reference implementation that shares the implementation's arithmetic cannot detect a formulation error; it mirrors it. A tolerance no test can move is a number nobody chose.

**Validation:** Every fix is mutation-verified: each mutant kills exactly the intended test, and the source was restored byte-for-byte after each run. Tests 10 → 15; workspace typecheck, lint and format clean.

## The real-build run binds every input it consumes, after three defects that would each have produced a confident wrong run

**Timestamp:** 2026-08-04

**Action:** Restructured the real-build harness across `apps/web/e2e/`: split `real-build-official.ts` (839 lines removed) into `real-build-environment.ts`, `real-build-input-files.ts`, `real-build-input-limits.ts`, `real-build-bootstrap-source.ts`, `real-build-artifact-publication.ts`, `real-build-replay-*.ts`, `real-build-builder-*.ts`, `real-build-highlight-*.ts` and `real-build-panel-specs.ts`, and corrected `bounded-file-read.ts`.

**Code reviewer comments:** The commit records three defects found before any live run; no external reviewer is named.

**Result:** (1) The replay environment schema rejected the repository's own pinned runtime. This Node 24 build reports an _empty_ version string for `nghttp3` and `ngtcp2` because it was compiled without QUIC, and the guard required at least one character — so `assertRealBuildEnvironment` threw on `process.versions` itself, and every live run would have died after the browser work, before writing its replay closure or artifact manifest, with no artifacts at all. Its message named five possible causes without saying which fired; each condition now reports its own observed value. (2) `sameFileState` compares dev, ino, size, mtime and ctime, but NTFS 15.6ms clock granularity makes two same-size writes indistinguishable — measured 72.5–79.0% collision — so a same-size rewrite returned attacker bytes with no error in 12–26 of 50 attempts. The guard test passed only because an incidental `await` let the clock tick; replayed without it, 30–33 of 50 mutations went undetected. Both closures fail: a second read cannot help because the dangerous rewrite completes _before_ the read and both reads then agree, and libuv maps `UV_FS_O_EXLOCK` to `share=0`, which denies readers too and still cannot see a finished rewrite. Content is now pinned by digest where a digest exists — six call sites already had one and were checking it only _after_ the read — and where none exists the error states the two known dead ends instead of advising a retry that cannot help. (3) The bootstrap source lock proved nothing: it read every open error as success, so a wrong root, a permissions error or a missing file all reported the lock as held; it bound the repository root from `process.cwd()` rather than the config module; and it accepted any live pid. An unprivileged process could hold an ordinary read handle, set the pid variable, and the run would report its immutability guarantee satisfied while nothing covered the source set.

**Reasoning:** An empty string is a real observation, not a malformed one — a schema that treats "absent because unbuilt" as "invalid" fails on the truth. Only a sharing violation now counts as held, the root is proved from the module's own location and the manifest's recorded anchors, and every file in the manifest must answer EBUSY, with the residual limit stated honestly: Windows cannot prove which process owns a handle without privileged APIs.

**Validation:** 67 files / 565 tests in `apps/web`; workspace typecheck, lint and format clean. Kept intact and unclaimed: highlight evidence proves renderer and source compatibility only — not PDF authentication, checkout origin, or visual correctness.

## Identification score artifacts were stale generations with no input digests

**Timestamp:** 2026-08-04

**Action:** Added `schemaVersion` and `inputDigests` over all nine inputs to `scripts/part-identification-score.mjs`; made `inventoryHeld`/`elementNames` return value and digest from ONE read; corrected two lesson anchors in `docs/learning/lessons-evidence.md`; split `part-identification.test.mjs` and `booklet-catalog-coverage.test.mjs`; raised the card-image ceiling with a shared budget threaded through both halves.

**Code reviewer comments:** The read-twice shape fixed here was already a finding elsewhere in the same unit. Two further defects closed from review: a double read of the declared `imagesFile` that let a getter validate one contained path and read another, and a conditional that skipped the feature-to-PDF digest edge entirely when neither binding was supplied.

**Result:** The suspected stale digests turned out to be claims, not artifacts — recompiling coverage reproduced 802,151 bytes byte-identically every time, so the reports were always right and only the quoted numbers were wrong; both lived in a gitignored handoff and no tracked file asserted either. Two lesson anchors _were_ stale in tracked docs, which matters more because AGENTS.md makes anchors load-bearing: they cited 870 callouts and 1,256–1,308 reconciled pieces that no live artifact produces, restated to the reproducible 863-callout numbers — 1,245 → 1,313 reconciled, 227 → 141 over-claimed, 39 → 11 never claimed — with their input digests named. All seven score artifacts were stale generations, built against callout run `add11ad1` while the live run is `d2cd3b61`, and carried no input digests at all while every sibling artifact bound its inputs; nothing could detect it. `part-identification.test.mjs` was 1,367 lines, over the 1,000-line hard ceiling: split to 186 across four focused suites plus a fixture; `booklet-catalog-coverage.test.mjs` 854 → 401 plus a closure suite. No test file now exceeds 455 lines.

**Reasoning:** The card-image ceiling was raised on booklet evidence rather than to make a number fit: at most one drawing per physical callout, 863 at the canonical 1280x756, is about 835 Mi, while the old 256 Mi bound admitted 273 cards at 98.4% full — five more clusters and retained evidence became unreadable by its own authenticator. Verification now charges two card sets rather than three, and the overflow message says a sealed run cannot be regenerated smaller instead of advising it. Five source modules remain over 500 lines and under the ceiling; splitting security-critical single-purpose modules was judged worse than leaving them.

**Validation:** Each closed defect has a regression proven to fail with the fix reverted. Gates: 9 files / 67 tests here; full verify chain green including 128 files / 1,214 tests and 32 browser tests.

## Body extents are declared exactly at a fixed 10^-9 scale, because 93273 is not a double

**Timestamp:** 2026-08-04

**Action:** Added `packages/catalog/src/exact-ldu.ts` (324 lines) with `ExactLdu` — signed integer units at a fixed scale of 9, canonical decimal parse and format, exact comparison against a double by nearest-double split with a BigInt tie-break, and an outward-projection gate — plus `catalog-exact-bounds.test.ts` and `truth-digest-stability.test.ts`, wired through `part-factory.ts`, `part-factory-support.ts`, `part-blueprint-types.ts` and `types.ts`.

**Code reviewer comments:** None recorded.

**Result:** 93273's bound of -16.00016098 expands to -16.00016098000000042134 and is not a double, and 93273 is one of the six parts that must be admitted, so neither rounding it nor dropping the part was available. The scope is deliberately small and measured rather than preferred: across the six pilot closures there are 96 coordinates; 6 are non-integer and only 2 are not exactly float64, both of them 93273's y minimum. Every stud-bound coordinate is an integer, so connectors need nothing; the construction lattice is integer by schema — `node:check` already rejects `positionLdu [0.5,0,0]` — so placement needs nothing. Nothing else changed: not `RigidTransform`, not enumeration, not brick-kernel collision, not rendering, not the browser, not protocol, not mesh assets or admission. The field is optional and absent unless a part declares it, so the catalog stays at 77 definitions, version `/6`, and no existing part re-hashes.

**Reasoning:** Version stays at `/6` deliberately: the owner decision recorded in `docs/policies/local-rules.md` the same evening advances truth versions at the first production admission, not at contract change, and this admits no part.

**Validation:** Hash stability is pinned rather than asserted — the five truth digests, the 77 definitions, the ordered `(id, contentHash)` roster digest and the exact 1,204,568-byte serialized catalog were captured BEFORE any edit and still hold. Nothing in the repo pinned the current truth digests before now: `REVIEWED_HISTORICAL_TRUTH_SNAPSHOTS` stopped at `/5`, and that gap is closed too. 200,015 distinct unit values round-trip text to units with 0 mismatches; eighteen rejection guards are each proven by mutation, and every message names the observed value and what would satisfy it. Float32 mesh validation is untouched: the exact-to-float64 residual on the only affected value is 4.2134e-16 LDU, 1.8e10 times smaller than the measured 7.62939453125e-6 drift, and its direction grows the solid. Gates: 34 files / 368 tests in catalog and brick-kernel, typecheck and format clean.

**Notes:** Known limits, none of them silent: collision primitives stay float64; 93273's LDraw frame is not centred, so its connectors need a frame decision before admission, not merely a precision one; and the projection gate is fail-closed rather than self-healing — a bound whose nearest double falls inward is refused, not nudged, because no measured value does that.

## A candidate part is scored before admission, and the score changed the plan twice

**Timestamp:** 2026-08-04

**Action:** Added the admission scorer: `scripts/score-part-admission.py` with `part_admission_contract.py`, `part_admission_geometry.py`, `part_admission_lattice.py`, `part_admission_ldraw_candidate.py`, `part_admission_scorecard.py` and `part_admission_surface.py`, wired into `npm run test:python`. Five axes: surface containment, connector coverage, lattice conformance, conservatism direction, and body budget. Under-claim is a HARD FAIL rather than a low score.

**Code reviewer comments:** The scorer caught two defects in its own candidate: a plan bucket that reported a point on a body face as outside, and boundary clips opening a full-width column outside the part, which had 5092 reading 38x21 LDU where its surface is 37x20.

**Result:** Baseline over the six pilot parts, LDraw column height field at 1 LDU — composite 0.9947 / 0.9889 / 0.9712 / 0.9901 / 0.9961 / 0.7270 for 5092, 30357, 35480, 51739, 77844, 93273; mean 0.9447. 3,400,701 surface points sampled, none outside any candidate at any swept column size — bit-for-bit the figure the recorded strategy reached independently. Two results change the plan rather than confirm it. First, 93273 is not lattice-alignable: 16.00016098 LDU is 2.0000201 plates, a residual of 0.00016098 against a 1e-6 tolerance, so the recorded fractional-bound pressure is now a number and falls through to compound-body reasoning. Second, the whole-footprint backing rule emits ZERO clutch cells on all six, because an LDraw underside is a cavity and nothing solid backs the footprint at the bottom face — and the measured tubes sit exactly 10 LDU, half a stud pitch, off the stud grid in at least one axis on every part. Female connectors are therefore not recoverable from LDraw geometry at all; Builder's authored field is not the better source for them, it is the only one, which puts the Builder-to-LDraw frame on the critical path.

**Reasoning:** AGENTS.md requires the measurable intermediate before work whose only honest verdict is far away; 121 leaves admitted by eye is exactly that failure. Under-claim is a hard fail because collision may refuse a placement a real part would allow and must never allow one it would not. Female coverage is reported and deliberately kept out of the composite rather than folded in as a figure that resembles agreement. Male coverage is disclosed as NOT independent: candidate and truth both derive from the same visible-stud components, so 8/8 at 0.0 LDU validates emission and lattice, not cross-source agreement.

**Validation:** The scorer is shown to fire on a real defect rather than only synthetic ones: a Builder-style inset probe hard-fails all six with 1,962,029 of 3,400,701 points outside and a maximum escape of 0.353553391 LDU — the measured 0.25 LDU face inset appearing at a corner as 0.25√2. Artifact `output/real-build/set-6651557-part-admission-scorecard.json`, 115,778 bytes, sha256 `e9907892e033fa0d11a0c7be7c6f186ac90f0b1699b002848ca2be816e0881a6`, byte-identical across two independent runs, with all six expansions bound to the approved source pilot before scoring (local evidence, not committed). Gates: 24 tests here, 119 across the Python suite.

**Notes:** "Builder is the only source for female connectors" held for one day. The LDCad shadow library, measured on 2026-08-05, turned out to supply them for the 14 designs Builder does not carry — at the cost of a CC BY-SA share-alike position that permits measuring and forbids shipping.

## Housekeeping in the same burst

**Timestamp:** 2026-08-04

**Action:** `a6ebde8` ignores Python bytecode — scripts run under `python -B`, but a single run that forgets the flag leaves ~300 KB of generated bytecode untracked-but-not-ignored, one `git add` away from being committed. `5d443dd` and `c7332f6` sync the fleet canon via `npm run sync-canon -- --apply`: the constitution was cut from 1,129 words to 578 (fleet `a502610`), one orchestration rule dropped and incident narratives moved to `fleet/FLEET.md` history, then reworded (fleet `8923ae5`) so the admission test no longer turns on what a model is assumed to be bad at — rules are stated as environment facts, owner decisions, or standards of done. `5e68d2e` tightens this repo's canon commit rule to commit-early-and-push.

**Code reviewer comments:** None recorded.

**Result:** Only the FLEET-CANON block changed in each sync.

**Notes:** If a rule appears to have vanished from AGENTS.md between 2026-08-03 and 2026-08-05, it is in `fleet/FLEET.md` history, not deleted.
