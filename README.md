# lego

A digital brick modeling studio with two surfaces: an offline manual editor for connection- and collision-validated brick assemblies, and an experimental closed loop that reads a printed LEGO instruction booklet, assembles its set, and checks each step against the booklet's own panel.

## Current state

### Web studio

The runnable React and Three.js studio supports local projects with integrity-checked IndexedDB persistence, a searchable part catalog, snapped placement and attachment, precise transform and color editing, deletion, undo and redo, live structural validation, manual build playback, and the app's bounded LDraw import and export profile.

The studio's **Instructions** control currently ingests a PDF locally into bounded page and text metadata plus a content hash. It does not render booklet panels, compile build steps, or apply a booklet run to the document.

### Experimental booklet tooling

The repository contains internal Node, Python, Vitest, and Playwright tooling for booklet parsing, panel measurement, placement search, visual scoring, retained evidence, and the set 6651557 reconstruction. This tooling is an opt-in development harness, not a production service or an automatic path into a user's document.

An authority-free source-stage tracer can retain exact H/P/D masks, while a separate source-parity `/4` diagnostic retains production, W, and XOR comparisons for five mask classes across the dense 359-step prepared panel sequence under a bound PDF, source/runtime closure, browser result, and prepared-panel manifest. The 2026-08-22 real-booklet run retained all 359 rows under measurement digest `sha256:39a810d8a30f28fb93bb619f2ce522ce01e95146e8fe14666d6606270c0cb615`; its browser work-RGBA, policy, and derivation commitments remain explicitly opaque, its source-RGBA column is visualization-only, and its authority remains absent.

A standalone browser-output `/4` reader now snapshots one closed data-only non-Proxy outer input before consuming its external branch, source, camera, and transition roles. It binds the exact prepared run and PDF across all 359 step/page/crop/callout rows; streams high/work RGBA and H/P/D/W source and exclusion masks; rejects source record/array Proxies, refuses raster derivation if its captured realm primordials drift, and reserves each sequential source step before byte-copy or derivation work so reentrancy cannot double-count or skip a panel; and verifies exact child canonical bytes, D4 camera recipes, renderer inputs, packed masks, scores, shifts, exact mask-descriptor aliases, closure-local mask offsets, and their ordered global role bases. Selected convergent multi-root placement edges and zero-piece transitions deterministically induce one terminal `BrickDocument`; selected placement reports must use the replay-only `compiled-observation` mechanism with neutral legacy visual fields and replay-neutral zero timing, while missing-role and terminal failures, identity bindings, and canonical bytes must equal evidence-derived projections. Every retained report refuses before classification when its exact prepared panel still contains coverage failures, unresolved callouts, or missing designs. Source render execution, provisional identity, physical frame, user placement, and completion authority remain explicitly absent, and completed fixed-ledger or `multi-build-copy` rows are refused.

The standalone atomic branch producer accepts a bounded ordered frontier over multiple exact root documents, gives each retained lineage a digest that commits both its structural lineage and exact canonical parent bytes, keeps exact-byte-convergent identities in one group, resolves each proposal against its own retained parent, proves a worst bounded zero-frontier terminal envelope fits, and reserves the entire cross-root request once before compilation. A transport-only finalizer accepts only module-produced batch results, requires a terminal-respecting strictly increasing placement-step sequence but cannot classify skipped prepared panels, admits aggregate role bytes and cumulative semantic/replay work before raw observation-role copying, rechecks measure/copy digests, verifies optional typed observation bytes, self-parses the completed roles, and publishes dense private roles through fresh-copy reads. The complete `/4` reader is the boundary that rejects a placement gap without its prepared transition report and evidence. There is still no live `/4` producer or run-session capability; the public runner remains `/3`, refuses before step-1 placement, and mutates no user document.

A private authority-absent step-1/panel-2 diagnostic reuses one prepared child scene across eight camera renders. A real non-square browser control preserves all eight ordered silhouette-mask digests while reducing measured scene derives and disposals from eight to one and keeping peak live prepared scenes at one; eight WebGL renders remain, no wall-clock speedup is claimed, and the diagnostic has no source-authenticated physical frame, placement or completion authority.

An exact-three source packet maps placement steps `[1,2,3]` to registration panels `[2,3,4]` on booklet page 11 while retaining the complete 359-label index and all four page-11 panel cuts. Its reader independently recomputes the three panel commitments and packed masks from digest-bound high/work RGBA roles; a genuine one-fetch browser capture produced manifest `sha256:6b09685905bab077c254adf1e8b32ee2eebd7eefac76d04da678ff47a77c3138` with 20,009 manifest bytes and 8,592,000/2,150,000/738,816 high/work/mask bytes. Source execution, prepared-run, physical-frame, placement, accepted-document, and completion authority remain absent.

An independent source-art rebound scans all 859 physical callouts from the exact 70,238,655-byte booklet and manifest and closes the shared drawing at printed steps 2, 4, and 16 while retaining steps 108, 119, 134, and 145 as suffix counterevidence. The 9,786-byte artifact is `sha256:a58a55e65c19e2771defe02fc9d37e24c00246bbed9dd375d8d0a2f16382897d`; a diagnostic Chromium capture binds the exact PDF and manifest, independently agrees across modern and legacy PDF.js routes, and grants no semantic, catalog, placement, or completion authority. A separate exact source-art semantic compiler now extends the sparse reviewed identity set from 70 relations / 107 pieces to 86 / 147 under the first-50 187-row / 320-piece denominator, leaving 101 / 173 residual while the complete 359-step source index remains intact. Its 211,319-byte authority-closed artifact is `sha256:4be7bd77d386a7a656019affe9c995e77135080a7aa90df19e43a6f2167ab721`; it is not yet a coverage or action input. Current deterministic catalog-coverage `/3` therefore remains unchanged: it preserves all 859 source/index rows, nulls identity and catalog fields for the 672 rows after requested step 50, and measures 41 of 49 action-bearing steps and 255 of 277 pieces catalog-placeable with covered prefix 27. The corresponding action-ledger `/4` publication still refuses, so no step-50 build frontier follows.

A bounded step-31/32 order reconciler separately proves that naïvely slicing the official Builder sequence at each printed quantity is wrong. After the exact 180-piece preceding prefix, phases 49 through 54 repartition printed steps 31 and 32 into 4 and 10 pieces; three reviewed identities expose naïve shortfalls of 1, 1, and 2 while the combined 14-piece multiset remains exact. The canonical 6,680-byte `/2` artifact is `sha256:66451b2324142d9f563b731739532c38be06e34018eb8397bacfe4a0e6245810`. It does not serialize direct UUID-to-callout rows, but the open pinned XML, reviewed rows, and repository derivation make the five reviewed physical associations reconstructible; the artifact declares that disclosure instead of claiming confidentiality. Its source index also states that crop bytes were not consumed and only reviewed crop digests were retained. This diagnostic evidence grants no assignment, frame, transform, catalog, ledger, placement, replay, document, or completion authority.

A separate exact semantic consumer cross-checks the surviving legacy recuts against the official Builder quantity cut for printed steps 1–50. Its 42,105-byte artifact at `sha256:e92ef982f9039b7fd94fb2cdca23fa5e56fb34fb6820ef4fa7ee9b999a0a63ea` publishes semantic element identity for exactly 70 listed callouts / 107 pieces, quarantines four incompatible or changed claims, preserves the complete 359-step source/index contract, and leaves physical Brick assignment, catalog admission, coverage publication, frame, placement, accepted-document, and completion authority absent. No coverage or action-ledger consumer uses it yet.

The current `/26` step-1 proper-C4 path deterministically partitions the 400 raw two-piece candidates into 100 exact four-member proper-yaw orbits, retains all 100 representatives and all 400 raw-to-representative inverse mappings, and rejects incomplete, reflected, aliased, or provenance-drifted families. Twenty ordered closures of five representatives share cumulative fixed-8,192 search and camera ledgers and render all 800 representative/camera pairs. Before one global rank may copy those scores through the inverse map, 60 separately budgeted verification closures render the other 300 current members across all eight cameras—2,400 proof renders under their own fixed-8,192 ledger and the same five-document pixel cap—and require every packed mask to equal its mapped representative commitment. Every reducer invocation therefore proves the 3,200-to-800 algorithmic reduction with a same-run 2,400-render exhaustive verification cost; its exact zero-margin tie yields no local winner, accepted transition, document, placement, or completion. The preliminary 32-render asymmetric calibration remains instrument evidence only, while exhaustive current-population parity supplies the score-and-tie claim and still grants no arbitrary-backend or physical-frame authority. The genuine-browser evidence captures every current representative at all eight D4 cameras plus a separately configured wider-scale control, without adding those visual-only control renders to the reduction or proof ledgers.

The fixed five-panel calibration path can capture steps/pages 90/79, 101/87, 346/213, 358/218, and 359/219 as exact high/work RGBA, H/P/D stages, independently re-derived W, pairwise P/D/W facts, and ten lossless PNGs, then replay those bytes in Node. A separate publication wrapper cross-binds that exact browser capture to the PDF, full 359-row prepared-panel manifest, source snapshot, provenance, served source, checkout, and runtime closure; derives an audit-only execution identity internally; writes five roles, ten PNGs, and the supporting manifests into its content-addressed run tree; and atomically replaces the discoverable summary only after every referenced byte has been written. The real-booklet exact-five opt-in retained those rows under execution identity `sha256:979157ed12ef36fcd59a85aba13268d56a47d77ee886070cd31221805d756462`. An authority-absent request inspector can reproduce exact equality between publication-derived W and an inspected truth packet. The exported challenge and receipt validators establish only closed wire structure; a shared detached exchange parser additionally cross-binds the two records to a caller-supplied wall-clock sample but cannot establish live object identity, cryptographic trust, one use, or authority. The bounded receipt inspector supplies the exact held challenge, canonical Ed25519 verification, snapshotted trust pin, expected review-presentation digest, recomputed next ledger transition, ordered four-sample wall-clock checks, and a private monotonic two-minute guard spanning verification, but its output remains authority absent and cannot create the module-owned production event capability. Production admission always refuses because no external authenticated one-use user-event consumer is integrated; the positive admission path exists only behind a mocked future-broker seam in tests. That five-row decision could never authorize physical transforms, placement, fixed actions, or dense 359-step completion even after broker integration.

The exact measured frontier and ordered work still missing live in [building-system.md](docs/design/building-system.md). See the [real-booklet runbook](docs/runbooks/real-build.md) to reproduce or regenerate the real-build inputs and runs without copying status into another document; it also names the separate panel-reading probe's present reproducibility limits.

Catalog render changes use the separate [part visual-admission runbook](docs/runbooks/part-visual-admission.md), which captures matched source-versus-catalog views and publishes an immutable explicit review without confusing the editor's presentation captures with admission evidence.

### Specified but unbuilt

`apps/companion` currently exports a library-level content-addressed artifact store, an unsealed test-namespace ledger, and a consent-gated test recorder. It has no HTTP server, production signing identity, credential proxy, authoritative production namespace, or runnable start command.

The production companion broker, unprivileged model worker, sealed replay lifecycle, and authoritative event surfaces remain specified rather than implemented. The text-brief AI copilot and its candidate lab were cut on 2026-08-07 and are not a deferred product; the [specification's removed-product section](docs/design/spec.md#removed-product-surface) records which shared contracts remain and why.

## Requirements and setup

- Node.js 24 or newer and npm 11 or newer.
- Python 3 available as `python` for the full verification suite.
- Chromium installed for Playwright browser tests.

From the repository root:

```powershell
npm ci
npx.cmd playwright install chromium
```

## Run the studio

```powershell
npm run dev
```

Open `http://127.0.0.1:5173`.

Choose a part and color in the catalog, then click in the viewport to place it; on an empty model, **Place at origin** provides the deterministic first placement. Select an existing part and use **Attach to selection** to add the chosen part on a free top stud. Use **Move**, **Rotate**, or the inspector for later edits. Each explicit placement becomes a playback step, available from **Build**.

Projects save locally in the browser. **Import** accepts the supported `.ldr` and `.mpd` subset, **Export LDraw** downloads the current document, and **Instructions** performs PDF ingestion only as described above.

## Checks

Run the repository's implemented verification gate with:

```powershell
npm run verify
```

`verify` checks schemas, Node consumers, observation consumers, provenance and notices, the green part-geometry standard, lessons, formatting, lint, types, Python derivation contracts, Vitest, Playwright, and the production browser bundle.

Dependency changes additionally require both audits:

```powershell
npm run audit
npm run audit:runtime
```

The authoritative command definitions are in [package.json](package.json).

## Design and policy

- [Product and architecture specification](docs/design/spec.md)
- [Part model](docs/design/part-model.md)
- [Building-system assessment and current measured position](docs/design/building-system.md)
- [Booklet-run evidence, replay, and promotion](docs/design/learning-system.md)
- [Current implementation threat model](docs/design/threat-model.md)
- [Real-booklet developer runbook](docs/runbooks/real-build.md)
- [Part visual-admission runbook](docs/runbooks/part-visual-admission.md)
- [Repository-local policy](docs/policies/local-rules.md)
- [Lessons index and evidence](docs/learning/lessons.md)
- [Historical devlog](docs/devlog/summary.md)
- [Dependency and data bill of materials](docs/dependency-data-bom.md)
- [Generated bundled-geometry notices](docs/bundled-geometry-notices.md)
- [Generated locked npm dependency notices](THIRD_PARTY_NOTICES.md)

The app owns brick-specific semantics. The sibling `3d-maker` repository remains a separate procedural-asset evolution studio; generic experiment, lineage, and evaluation interfaces may be shared only after both implementations prove the same need.
