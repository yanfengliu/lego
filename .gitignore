# Dependencies
node_modules/
.pnpm-store/
.npm/
/.yarn/*
!/.yarn/patches/
!/.yarn/plugins/
!/.yarn/releases/
!/.yarn/sdks/
!/.yarn/versions/
.pnp.*

# Build, test, and tool output
dist/
coverage/
.nyc_output/
.vite/
*.lcov
*.tsbuildinfo
.eslintcache
.stylelintcache

# Raw browser and reviewer evidence
test-results/
playwright-report/
blob-report/
.playwright-cli/
/tmp/review-runs/

# Product, broker, and recursive-loop artifacts
/var/runs/
/var/state/
/output/

# Logs and process state
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
*.pid
*.pid.lock

# Local environment and secrets
.env
.env.*
!.env.example

# Local caches
.cache/
.parcel-cache/

# The sample instruction booklet: 70 MB of copyrighted LEGO artwork, kept
# locally and never committed. It was swept into a commit once by a sweeping
# `git add`, which is what this line exists to prevent.
recipes/

# Downloaded LDraw part files, re-fetched on demand by scripts/ldraw-part-facts.mjs.
scripts/ldraw-cache/

# Scratch space.
tmp/
